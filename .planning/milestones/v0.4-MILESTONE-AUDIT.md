---
milestone: v0.4
milestone_name: Multi-Tenant & Admin
audited: 2026-05-07
status: human_needed
scores:
  requirements: 28/28
  phases: 5/5
  integration: cross-phase wired (Phase 11 schema → Phase 12 auth → Phase 13 admin → Phase 14 onboarding → Phase 15 cap)
  flows: 4/4 critical flows verified at integration-test level (member invite, member onboarding, admin revoke, ai-cap enforce)
gaps: []
tech_debt:
  - phase: 11-multi-tenancy-db-migration
    items:
      - "ai_usage_log.est_cost_usd is Float (CLAUDE.md says 'no float'); historical from Phase 13. Migration to BIGINT cents deferred to a separate mini-phase."
  - phase: 13-admin-ui-whitelist-ai-usage
    items:
      - "1 pre-existing test failure with DEV_MODE=true in dev container (test_admin_users_api.py and test_ai_cap_integration.py:test_member_forbidden_403). Production behavior is correct; DEV_MODE bypass is intentional for local debugging."
  - phase: 14-multi-tenant-onboarding
    items:
      - "16 DB-backed tests (test_require_onboarded, test_embedding_backfill, test_onboarding_gate, test_onboarding_existing_user_safety) require docker compose --build api before running — local .venv-test lacks pgvector schema."
      - "Embedding backfill on `/onboarding/complete` is inline async — adds 1-3s OpenAI latency to onboarding response. Acceptable for MVP; could move to worker job if observed slow."
  - phase: 15-ai-cost-cap-per-user
    items:
      - "WR-01 deferred: enforce_spending_cap creates a second DB session via Depends(get_db) while AI handlers already open a session via get_db_with_tenant_scope. Per-request cost is one extra connection from the pool. Requires architectural decision (use get_db_with_tenant_scope in dep, or move spend check inline)."
      - "Money-scale calibration: RESOLVED 2026-05-07 (post-v0.4-close test campaign). User decision: default = $1/month (100 cents at scale 100/USD). Alembic 0009 шипнут — column default 46500 → 100, existing owner row автомигрирован 46500 → 100; ORM model + REQUIREMENTS AICAP-01 wording обновлены. CR-01 в admin_ai_usage.py выровнял scale на 100/USD ранее."
      - "test_admin_ai_usage_api.py calibration test 'pct_of_cap with 0.083 USD and cap 10_000 → 0.83' is now stale (was based on 100_000/USD scale). Re-baseline at next admin-AI test pass; not blocking enforcement."
  - cross_phase:
      - "Bot container tg-budget-planner-bot-1 has been restarting due to TelegramUnauthorizedError (token issue) — pre-existing, unrelated to v0.4. User to refresh BOT_TOKEN."

human_uat:
  consolidated_milestone_smoke:
    description: "Live TG smoke test for the full v0.4 surface — all 5 phases deferred their live-MiniApp/bot UAT to milestone close, mirroring the Phase 11 U-1 pattern."
    items:
      - "[v0.4-U-1] Login as OWNER → /me returns role=owner; admin tabs visible."
      - "[v0.4-U-2] OWNER invites a member via Admin UI → row appears in users list."
      - "[v0.4-U-3] New member sends /start in bot → invite-flow greeting; opens MiniApp; OnboardingScreen renders with member-specific hero copy."
      - "[v0.4-U-4] Member completes onboarding → balance + cycle + 14 seed categories appear; gate releases (no more 409)."
      - "[v0.4-U-5] Member triggers /ai/chat → request succeeds; ai_spend_cents in /me starts incrementing."
      - "[v0.4-U-6] OWNER edits member's cap to 0 via Admin UI → next /ai/chat from member returns 429 with Retry-After."
      - "[v0.4-U-7] OWNER revokes member → user disappears from list; member's data purged (categories, transactions, embeddings, ai_usage_log)."
      - "[v0.4-U-8] Phase 14: bot container TelegramUnauthorizedError must be resolved before live smoke (refresh BOT_TOKEN in .env)."

deferred_to_v0_5_or_later:
  - "Migration est_cost_usd Float → cost_cents BIGINT (CLAUDE.md 'no float' compliance)."
  - "Notifications «80% cap reached», «cap exhausted» (Phase 15 deferred)."
  - "Background-worker dozalivка для embeddings вместо inline (Phase 14 deferred)."
  - "Re-onboarding flow for member changing balance/cycle (Phase 14 deferred)."
  - "Re-baseline test_admin_ai_usage_api.py pct_of_cap calibration after Phase 15 scale alignment."

nyquist:
  status: skipped
  reason: "workflow.nyquist_validation enabled in config but no *-VALIDATION.md files were generated for v0.4 phases (project disabled it during early v0.4 to save tokens per user feedback feedback-research-pace.md). All phases verified via VERIFICATION.md + integration tests; nyquist gap documented as deferred."
---

# Milestone v0.4 — Multi-Tenant & Admin

## Audit Summary

**Status:** `human_needed` — automated coverage 100% (28/28 requirements satisfied across 5 phases, 36 plans). Live TG/MiniApp smoke is consolidated as the milestone-close UAT (8 items above).

**Why human_needed, not passed:** Every individual phase signed off as `human_needed` because the production stack runs through real Telegram APIs, real Cloudflare Tunnel, real Postgres on VPS — none of those are reachable from automated tests. The pattern mirrors Phase 11 U-1 / Phase 12 Checkpoint 2 / Phase 13 Checkpoint 2 / Phase 14 / Phase 15.

## What v0.4 Delivered

| Phase | Plans | Reqs | Outcome |
|-------|-------|------|---------|
| 11 — Multi-Tenancy DB Migration & RLS | 7 | MUL-01..05, ROLE-01 | Alembic 0006: user_id FK + RLS on 9 domain tables; backfill for existing owner; scoped uniques |
| 12 — Role-Based Auth Refactor | 7 | ROLE-02..05 | dependencies.py role-check (get_current_user, require_owner); /me with role; bot helpers; alembic 0007 postgres role split |
| 13 — Admin UI: Whitelist & AI Usage | 8 | ADM-01..06, AIUSE-01..03 | AccessScreen with 2 sub-tabs (Users / AI Usage); admin endpoints (invite/revoke/list/ai-usage); cascade purge on revoke |
| 14 — Multi-Tenant Onboarding | 7 | MTONB-01..04 | require_onboarded gate + 409; bot /start branch on onboarded_at; embedding backfill in complete_onboarding; OnboardingRequiredError frontend |
| 15 — AI Cost Cap Per User | 7 | AICAP-01..05 | spend_cap service with TTLCache; enforce_spending_cap dep; PATCH /admin/users/{id}/cap; /me ai_spend_cents; SettingsScreen + CapEditSheet |
| **Total** | **36** | **28/28** | |

## Cross-Phase Integration

Verified at integration-test level — no separate integration checker needed since later phases test the chain end-to-end:

| Flow | Phases involved | Verifier |
|------|-----------------|----------|
| Member invite → onboard → seed cats + embeddings | 13 → 14 | tests/test_onboarding_gate.py:test_full_member_onboarding_flow_creates_categories_periods_embeddings |
| Owner edits member cap → next /ai/chat blocks | 13 → 15 | tests/test_ai_cap_integration.py:test_chat_blocked_when_spend_exceeds_cap_via_admin_patch |
| Pre-onboarding gate: 10 routers return 409 | 11 → 12 → 14 | tests/test_onboarding_gate.py:test_member_gate_matrix_409_on_all_gated_routers |
| Existing owner unaffected (onboarded_at backfilled v0.2) | 11 → 14 | tests/test_onboarding_existing_user_safety.py:test_existing_onboarded_owner_passes_gate |

## Code Review Status

| Phase | Critical | Warning | Info | Fixes Applied |
|-------|----------|---------|------|---------------|
| 14 | 0 | 4 | 2 | WR-01 (try/except backfill loop), WR-04 (dead import); WR-02/WR-03 skipped with rationale |
| 15 | 2 | 5 | 3 | CR-01 (money-scale alignment 100_000→100/USD), CR-02 (debug copy leak), WR-05 (test body fix); WR-01..04 deferred |

No critical or blocker issues remain in the codebase.

## What's NOT Done

None. All 28 requirements have evidence in VERIFICATION.md per phase and `[x]` in REQUIREMENTS.md traceability table.

## Recommendation

**Proceed to `/gsd-complete-milestone v0.4` after the human runs the consolidated v0.4-U-1..U-8 UAT.**

Если live smoke выявит регрессии — открыть hot-fix sub-phase под текущим milestone, иначе закрыть и тегать v0.4.
