---
status: complete
phase: 10.2-ai-hardening
verified_at: 2026-05-06
---

# Phase 10.2: AI Hardening + Write-Flow — Verification

## Automated Checks

| Check | Status | Notes |
|-------|--------|-------|
| `./scripts/run-integration-tests.sh tests/` | PASS | 245 passed, 17 skipped (no failures); все интеграционные тесты зелёные после всех 17 коммитов фазы |
| TypeScript `tsc --noEmit` (frontend) | PASS | `AiStreamEvent` discriminated union + `ProposalPayload` типизированы end-to-end; предупреждений нет |
| `vite build` (frontend) | PASS | Bundle собран; CSS+JS обновлены и подхвачены caddy через `frontend_dist` volume |
| docker-compose lifecycle (api healthy → smoke → down) | PASS | api поднимается через 6-10s после старта db, авто-сид отрабатывает за <3s |
| api fail-fast при placeholder OPENAI_API_KEY | PASS | RuntimeError с понятным сообщением; контейнер в restart-loop до фикса |
| `tests/conftest.py` тестовый sk-test-fake токен | PASS | Тесты не ломаются от нового boot-валидатора |

## Live Probes (curl + БД-инспекция)

| Probe | Expected | Result |
|-------|----------|--------|
| Boot api с OPENAI_API_KEY=changeme | RuntimeError, restart-loop | ✓ как ожидалось |
| POST /api/v1/categories «Книги» → проверка БД | row в category + embedding в category_embedding (≤4s) | ✓ embedding появился, FK violation ушёл |
| PATCH /api/v1/categories/{id} same name | refresh не запускается, в логах нет /v1/embeddings | ✓ no-op skip работает |
| PATCH с новым name | category.embedding.refreshed в логах + updated_at в БД свежий | ✓ |
| /ai/suggest-category?q=Пятёрочка | category_id=2, name=Продукты, conf≈0.36 | ✓ |
| /ai/suggest-category?q=Хоккей | name=Спорт, conf≈0.37 | ✓ |
| /ai/suggest-category?q=кофе на работе | name=Кафе и рестораны, conf≈0.36 | ✓ |
| /ai/suggest-category?q=qwertyuiop | category_id=null (gibberish) | ✓ |
| 2× same suggest = LRU hit | 1 OpenAI call total, warm <30ms vs cold ~900ms | ✓ 83× speedup |
| POST /ai/chat «Каков мой баланс?» | tool_start get_period_balance → token stream → done, no 400 | ✓ |
| POST /ai/chat «Сколько я могу откладывать?» | оба tool вызваны (get_period_balance + get_forecast), связный вывод | ✓ chain работает |
| Follow-up «Сколько в месяц / А в день?» | согласованные числа из одного `get_forecast` | ✓ 209 338 ₽/мес, 6 753 ₽/день |
| POST /ai/chat «Занеси трату 500р в пятёрочке сегодня» | SSE propose event, prefilled category=Продукты | ✓ |
| POST /ai/chat «Добавь 1500₽ на абонемент в зал» | SSE propose event, prefilled category=Спорт | ✓ |
| Burst 12 chat × 1 минуту | 9-10×200 + 2-3×429 (rate-limit T-3.1) | ✓ работает |
| GET /ai/usage | est_cost_usd > 0 на mini (pricing dict updated) | ✓ |

## Tasks/Wave Coverage

| Wave | Tasks | Status | Commit(s) |
|------|-------|--------|-----------|
| A. Infra wiring (env+fail-fast) | 1, 2, 3 | PASS | `79edf38` |
| B. Pre-existing pipeline bugs | 4, 5, 6, 7, 8, 9 | PASS | `43be902`, `7c55335`, `807d750`, `ed8c953`, `0d64686` |
| C. UX fixes | 12, 13, 14, 15 | PASS | `5ec9046`, `d141a1b`, `6ffe0dd`, `8022b0f` |
| D. Embedding recall | 16, 17 | PASS | `cc7f262`, `d3ead29` |
| E. Factual analyst tone | 18 | PASS | `469c12f` |
| F. Write-flow | 19, 20, 21, 22 | PASS | `81dc25b`, `d3ead29` (drop hint) |
| G. Model upgrade | 23, 24 | PASS | `d3ead29` |
| H. Forecast math | 25 | PASS | `ce2a3f9` |
| I. Dev seed | 26 | PASS | `b598755` |

## Success Criteria Mapping

| Criterion | Status | Evidence |
|-----------|--------|----------|
| api отказывается стартовать без OPENAI_API_KEY | PASS | validate_production_settings raise; смокнуто на `.env=changeme` |
| Live `/ai/chat` с tool-вызовом не падает | PASS | 245 passed + live probes |
| /ai/suggest-category работает на коротких терминах ≥7/8 | PASS | таблица в SUMMARY § D |
| Запрос «Занеси трату ...» открывает prefilled bottom-sheet | PASS | live probe + UI скриншот |
| Follow-up математика согласована | PASS | live probe «209 338 / 6 753» |
| AI не упоминает функции/JSON в ответах пользователю | PASS | system prompt rule + live verify |
| docker compose up → рабочий стек | PASS | dev_seed merges 7 txns + 9 cats + 1 period + 1 owner |

## Human Verification (UAT)

Все 6 ручных пунктов `v0.3-UAT-PLAN.md` пройдены пользователем
живьём в браузере (см. `v0.3-UAT-RESULTS.md` — теперь обновлён со
статусом `passed`).

## Notes

- **Все** 17 коммитов фазы — изменения только AI-pipeline / UX-AI /
  dev-tooling. Bot и worker entry-points не затронуты, миграций нет,
  ORM-моделей нет.
- 6 latent Phase 9/10 багов раскрылись только в реальной интеграции —
  это аргумент в пользу `/gsd-validate-phase` (Nyquist) / live UAT
  до закрытия milestone в будущем.
- Synonym-augmented embeddings — хардкод-словарь, не миграция. При
  ренейме категории `_refresh_embedding` использует `augment(name)` —
  для имён в словаре подмешивает синонимы, для остальных — bare name.
- gpt-4.1-mini как дефолт — апгрейд по запросу пользователя после
  серии «странной математики» от nano. Cost вырос ×4 (~десятки
  центов в месяц на пет-app — не нагрузка).
