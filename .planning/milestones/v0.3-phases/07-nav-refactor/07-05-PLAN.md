---
phase: 07-nav-refactor
plan: 05
type: execute
wave: 2
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - frontend/src/App.tsx
  - frontend/src/App.module.css
  - frontend/src/screens/MoreScreen.tsx
  - frontend/src/screens/MoreScreen.module.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - MGT-04

must_haves:
  truths:
    - "App.tsx использует новый TabId тип (home/transactions/analytics/ai/management)"
    - "managementView state (переименован из subScreen) управляет саб-скринами Управления"
    - "TransactionsScreen рендерится по tab === 'transactions'"
    - "ManagementScreen рендерится по tab === 'management'"
    - "AnalyticsScreen и AiScreen рендерятся по своим табам"
    - "historyFilter state остаётся (cross-tab: HomeScreen → история по категории)"
    - "MoreScreen.tsx УДАЛЁН из кодовой базы"
    - "Старые ActualScreen и PlannedScreen больше не импортируются в App.tsx"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Полностью переписанный routing под новый 5-табовый nav"
      exports: ["default App"]
    - path: "frontend/src/screens/MoreScreen.tsx"
      provides: "(УДАЛЁН)"
      exports: []
  key_links:
    - from: "App.tsx"
      to: "TransactionsScreen"
      via: "activeTab === 'transactions'"
      pattern: "activeTab === 'transactions'"
    - from: "App.tsx"
      to: "ManagementScreen"
      via: "activeTab === 'management' && !managementView"
      pattern: "activeTab === 'management'"
    - from: "App.tsx"
      to: "BottomNav"
      via: "new TabId type"
      pattern: "TabId"
---

<objective>
Wave 2: Полная интеграция — переписать App.tsx под новый 5-табовый nav, удалить MoreScreen.tsx, подключить все новые экраны Phase 7.

Purpose: Это финальный шаг связывания всех компонентов Phase 7. После этого плана приложение полностью работает с новым nav.

Output: Переписанный App.tsx + удалённый MoreScreen.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nav-refactor/07-CONTEXT.md
@.planning/phases/07-nav-refactor/07-03-SUMMARY.md
@.planning/phases/07-nav-refactor/07-04-SUMMARY.md

<interfaces>
<!-- Текущий App.tsx — полностью переписываем -->
<!-- Текущие state переменные: activeTab (TabId), subScreen, historyFilter -->
<!-- НОВЫЕ state переменные: -->
```typescript
type ManagementView = 'subscriptions' | 'template' | 'categories' | 'settings';
// (импортируем из ManagementScreen.tsx)

const [activeTab, setActiveTab] = useState<TabId>('home');
const [managementView, setManagementView] = useState<ManagementView | null>(null);
// subScreen → managementView (locked decision D-CONTEXT)
const [historyFilter, setHistoryFilter] = useState<number | null>(null);
// historyFilter ОСТАЁТСЯ (HomeScreen cross-tab навигация)
```

<!-- Новый BottomNav TabId из Plan 02: -->
```typescript
export type TabId = 'home' | 'transactions' | 'analytics' | 'ai' | 'management';
```

<!-- Экраны Plan 03 -->
```typescript
import { TransactionsScreen } from './screens/TransactionsScreen';
// Props: { categoryFilter?: number | null, onClearFilter?: () => void }
```

<!-- Экраны Plan 04 -->
```typescript
import { ManagementScreen, type ManagementView } from './screens/ManagementScreen';
// Props: { onNavigate: (screen: ManagementView) => void }
import { AnalyticsScreen } from './screens/AnalyticsScreen';
import { AiScreen } from './screens/AiScreen';
```

<!-- HomeScreen — остаётся без изменений, но onNavigateToSub адаптируется -->
```typescript
// HomeScreen.onNavigateToSub теперь принимает только 'template'|'categories'|'settings'
// но cross-tab: 'planned' → setActiveTab('transactions')
// Проверить текущую сигнатуру HomeScreen и адаптировать
```

<!-- Саб-скрины (переиспользуются без изменений — MGT-04) -->
```typescript
import { CategoriesScreen } from './screens/CategoriesScreen';
import { TemplateScreen } from './screens/TemplateScreen';
import { SettingsScreen } from './screens/SettingsScreen';
import { SubscriptionsScreen } from './screens/SubscriptionsScreen';
// Все 4 принимают: onBack: () => void
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переписать App.tsx под новый 5-табовый nav</name>
  <read_first>
    - frontend/src/App.tsx (полностью — текущий routing для полного понимания)
    - frontend/src/screens/HomeScreen.tsx (найти onNavigateToSub + onNavigateToHistory сигнатуры)
    - frontend/src/screens/ManagementScreen.tsx (ManagementView тип — только что создан)
    - frontend/src/screens/TransactionsScreen.tsx (TransactionsScreenProps — только что создан)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (subScreen→managementView; historyFilter остаётся)
  </read_first>
  <files>frontend/src/App.tsx</files>
  <action>
    Полностью переписать App.tsx. Новая структура:

    ```tsx
    import { useState } from 'react';
    import { useUser } from './hooks/useUser';
    import { OnboardingScreen } from './screens/OnboardingScreen';
    import { HomeScreen } from './screens/HomeScreen';
    import { CategoriesScreen } from './screens/CategoriesScreen';
    import { TemplateScreen } from './screens/TemplateScreen';
    import { SettingsScreen } from './screens/SettingsScreen';
    import { SubscriptionsScreen } from './screens/SubscriptionsScreen';
    import { TransactionsScreen } from './screens/TransactionsScreen';
    import { ManagementScreen, type ManagementView } from './screens/ManagementScreen';
    import { AnalyticsScreen } from './screens/AnalyticsScreen';
    import { AiScreen } from './screens/AiScreen';
    import { BottomNav, type TabId } from './components/BottomNav';
    import styles from './App.module.css';

    export default function App() {
      const { user, loading, error, refetch } = useUser();
      const [activeTab, setActiveTab] = useState<TabId>('home');
      const [managementView, setManagementView] = useState<ManagementView | null>(null);
      const [historyFilter, setHistoryFilter] = useState<number | null>(null);

      if (loading && !user) { /* loading state — без изменений */ }
      if (error || !user) { /* error state — без изменений */ }

      const isOnboarded = user.onboarded_at !== null;
      if (!isOnboarded) { /* onboarding — без изменений */ }

      const handleTabChange = (tab: TabId) => {
        setActiveTab(tab);
        setManagementView(null);
        if (tab !== 'transactions') setHistoryFilter(null);
      };

      return (
        <div className={styles.appWrapper}>
          <div className={styles.appRoot}>
            <div className={styles.screenContainer}>

              {/* Management sub-screens (рендерятся поверх всего) */}
              {managementView === 'subscriptions' && (
                <SubscriptionsScreen onBack={() => setManagementView(null)} />
              )}
              {managementView === 'template' && (
                <TemplateScreen onBack={() => setManagementView(null)} />
              )}
              {managementView === 'categories' && (
                <CategoriesScreen onBack={() => setManagementView(null)} />
              )}
              {managementView === 'settings' && (
                <SettingsScreen onBack={() => setManagementView(null)} />
              )}

              {/* Main tabs (скрыты когда показывается management sub-screen) */}
              {!managementView && activeTab === 'home' && (
                <HomeScreen
                  onNavigateToSub={(s) => {
                    // s может быть 'planned' (cross-tab) или 'template'|'categories'|'settings'
                    if (s === 'planned') {
                      setActiveTab('transactions');
                    } else {
                      setManagementView(s as ManagementView);
                    }
                  }}
                  onNavigateToHistory={(categoryId) => {
                    setHistoryFilter(categoryId ?? null);
                    setActiveTab('transactions');
                  }}
                />
              )}
              {!managementView && activeTab === 'transactions' && (
                <TransactionsScreen
                  categoryFilter={historyFilter}
                  onClearFilter={() => setHistoryFilter(null)}
                />
              )}
              {!managementView && activeTab === 'analytics' && <AnalyticsScreen />}
              {!managementView && activeTab === 'ai' && <AiScreen />}
              {!managementView && activeTab === 'management' && (
                <ManagementScreen onNavigate={(screen) => setManagementView(screen)} />
              )}

            </div>
            <BottomNav activeTab={activeTab} onTabChange={handleTabChange} />
          </div>
        </div>
      );
    }
    ```

    ВАЖНО: перед записью прочитать текущий App.tsx чтобы точно скопировать loading/error/onboarding JSX блоки без изменений.

    Проверить сигнатуру `HomeScreen.onNavigateToSub` — если там тип `'planned' | 'categories' | 'template' | 'settings'`, то приведение `s as ManagementView` корректно для не-'planned' значений. Если тип другой — адаптировать.

    НЕ импортировать ActualScreen, PlannedScreen, MoreScreen — они больше не используются в App.tsx.
  </action>
  <acceptance_criteria>
    - `grep -c "import.*MoreScreen" frontend/src/App.tsx` возвращает 0
    - `grep -c "import.*ActualScreen" frontend/src/App.tsx` возвращает 0
    - `grep -c "import.*PlannedScreen" frontend/src/App.tsx` возвращает 0
    - `grep -c "TransactionsScreen" frontend/src/App.tsx` возвращает >= 2
    - `grep -c "managementView" frontend/src/App.tsx` возвращает >= 4
    - `grep -c "subScreen" frontend/src/App.tsx` возвращает 0
    - `grep -c "AnalyticsScreen\|AiScreen" frontend/src/App.tsx` возвращает >= 2
    - `grep -c "historyFilter" frontend/src/App.tsx` возвращает >= 2
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>App.tsx переписан: новый TabId, managementView вместо subScreen, TransactionsScreen/ManagementScreen/AnalyticsScreen/AiScreen подключены. TypeScript чист.</done>
</task>

<task type="auto">
  <name>Task 2: Удалить MoreScreen, проверить Vite build</name>
  <read_first>
    - frontend/src/screens/MoreScreen.tsx (для подтверждения что НЕ импортируется нигде)
    - frontend/src/screens/MoreScreen.module.css (также удалить)
  </read_first>
  <files>
    frontend/src/screens/MoreScreen.tsx
    frontend/src/screens/MoreScreen.module.css
  </files>
  <action>
    1. Проверить что MoreScreen больше не импортируется:
       ```bash
       grep -rn "MoreScreen" /Users/exy/pet_projects/tg-budget-planner/frontend/src/
       ```
       Если есть результаты кроме самих файлов MoreScreen.tsx/.module.css — исправить до удаления.

    2. Удалить оба файла:
       ```bash
       rm /Users/exy/pet_projects/tg-budget-planner/frontend/src/screens/MoreScreen.tsx
       rm /Users/exy/pet_projects/tg-budget-planner/frontend/src/screens/MoreScreen.module.css
       ```

    3. Запустить TypeScript проверку:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit
       ```

    4. Запустить Vite build для финальной проверки:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm run build 2>&1 | tail -20
       ```

    Если Vite build падает из-за остаточных импортов — исправить до прохождения.

    ТАКЖЕ проверить: если ActualScreen или PlannedScreen импортируются где-то ещё (не в App.tsx) — оставить файлы. Если только в App.tsx использовались и теперь не используются — можно оставить (не удалять — это вопрос discretion, они безвредны).
  </action>
  <acceptance_criteria>
    - `test -f frontend/src/screens/MoreScreen.tsx` возвращает 1 (ненулевой код = файл НЕ существует)
    - `test -f frontend/src/screens/MoreScreen.module.css` возвращает 1 (файл НЕ существует)
    - `grep -rn "MoreScreen" /Users/exy/pet_projects/tg-budget-planner/frontend/src/ | grep -c "."` возвращает 0
    - `cd frontend && npm run build 2>&1 | grep -c "error"` возвращает 0
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>MoreScreen.tsx и MoreScreen.module.css удалены; нет остаточных импортов; Vite build проходит без ошибок; TypeScript чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx state → sub-screens | managementView state контролирует доступ к подэкранам |
| homeScreen cross-tab nav | onNavigateToHistory меняет activeTab — только UI state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Elevation of Privilege | managementView routing | accept | UI routing без влияния на API permissions; auth на backend |
| T-07-09 | Denial of Service | Vite build | accept | Build-time issue, не runtime security concern |
</threat_model>

<verification>
- `cd frontend && npm run build` — build успешен без ошибок
- `cd frontend && npx tsc --noEmit` — 0 ошибок
- `grep -rn "MoreScreen" frontend/src/` — 0 результатов
- App.tsx: managementView (не subScreen), TransactionsScreen, ManagementScreen, AnalyticsScreen, AiScreen
- historyFilter state присутствует и пробрасывается в TransactionsScreen
</verification>

<success_criteria>
- App.tsx переписан с новым 5-табовым routing
- MoreScreen.tsx удалён
- Vite build проходит без ошибок
- TypeScript компилируется чисто
- Все старые функциональности (саб-скрины Подписок/Шаблона/Категорий/Настроек) доступны через ManagementScreen
</success_criteria>

<output>
После завершения создать `.planning/phases/07-nav-refactor/07-05-SUMMARY.md`
</output>
