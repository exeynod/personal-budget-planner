---
phase: 07-nav-refactor
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - frontend/tests/e2e/nav-v03.spec.ts
  - frontend/tests/e2e/home.spec.ts
  - frontend/tests/e2e/ui-audit.spec.ts
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - TXN-01
  - TXN-02
  - TXN-03
  - TXN-04
  - TXN-05
  - MGT-01
  - MGT-02
  - MGT-04

must_haves:
  truths:
    - "Playwright тесты для nav-v0.3 существуют и падают (RED) до реализации"
    - "Тест проверяет наличие ровно 5 nav-табов с новыми лейблами"
    - "Тест проверяет под-табы История/План внутри таба Транзакции"
    - "Тест проверяет фильтр-чипы в Транзакциях (Все/Расходы/Доходы)"
    - "Тест проверяет меню Управление с 4 пунктами"
    - "Старые тесты home.spec.ts и ui-audit.spec.ts обновлены под новые aria-labels"
  artifacts:
    - path: "frontend/tests/e2e/nav-v03.spec.ts"
      provides: "E2E тесты для Phase 7 Nav Refactor"
      exports: []
    - path: "frontend/tests/e2e/home.spec.ts"
      provides: "Обновлённые home тесты (новые aria-labels)"
      contains: "Транзакции"
    - path: "frontend/tests/e2e/ui-audit.spec.ts"
      provides: "Обновлённые audit тесты (новые aria-labels)"
      contains: "Транзакции"
  key_links:
    - from: "nav-v03.spec.ts"
      to: "BottomNav (новый)"
      via: "aria-label кнопок"
      pattern: "aria-label=\"Транзакции\""
---

<objective>
Wave 0: Написать RED e2e-тесты для Phase 7 Nav Refactor ДО реализации.

Purpose: TDD-gate — тесты должны падать на текущем коде (старый nav с «История», «Подписки», «Ещё») и пройти только после завершения Phase 7.

Output: frontend/tests/e2e/nav-v03.spec.ts (новые тесты) + обновлённые home.spec.ts и ui-audit.spec.ts (исправлены aria-labels под новый nav).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-nav-refactor/07-CONTEXT.md

<interfaces>
<!-- Текущий BottomNav TabId (будет изменён в Plan 02) -->
<!-- Из frontend/src/components/BottomNav.tsx: -->
```typescript
export type TabId = 'home' | 'history' | 'planned' | 'subscriptions' | 'more';
const TABS = [
  { id: 'home', label: 'Главная' },
  { id: 'history', label: 'История' },
  { id: 'planned', label: 'План' },
  { id: 'subscriptions', label: 'Подписки' },
  { id: 'more', label: 'Ещё' },
];
```

<!-- Новый TabId после Phase 7 (целевое состояние): -->
```typescript
export type TabId = 'home' | 'transactions' | 'analytics' | 'ai' | 'management';
// Labels: Главная / Транзакции / Аналитика / AI / Управление
// AI tab: aria-label="AI", active color #a78bfa
```

<!-- Существующий паттерн e2e тестов: -->
// mockApiRich() — общий mock из ui-audit.spec.ts
// page.click('button[aria-label="История"]')  <- устарел
// page.click('button[aria-label="Транзакции"]')  <- новый
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Написать RED тесты nav-v03.spec.ts</name>
  <read_first>
    - frontend/tests/e2e/ui-audit.spec.ts (паттерн mockApiRich + waitForLoad + page.route)
    - frontend/tests/e2e/home.spec.ts (паттерн mockApi, структура тестов)
    - frontend/src/components/BottomNav.tsx (текущие aria-labels для понимания что изменится)
    - .planning/REQUIREMENTS.md строки 77-97 (NAV-01..04, TXN-01..05, MGT-01..04)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (locked decisions)
  </read_first>
  <files>frontend/tests/e2e/nav-v03.spec.ts</files>
  <behavior>
    Тесты должны ПАДАТЬ на текущем коде (старый nav) и ПРОХОДИТЬ после Phase 7.

    nav-01: 5 табов с новыми лейблами
    - `button[aria-label="Главная"]` видим
    - `button[aria-label="Транзакции"]` видим
    - `button[aria-label="Аналитика"]` видим
    - `button[aria-label="AI"]` видим
    - `button[aria-label="Управление"]` видим
    - Старые лейблы "История", "Подписки", "Ещё" НЕ видимы

    nav-02: AI таб имеет фиолетовый цвет когда активен
    - Кликнуть на `button[aria-label="AI"]`
    - Кнопка имеет CSS класс содержащий "active"
    - Проверить через `getAttribute('class')` что присутствует 'active' и 'ai'

    txn-01: Таб Транзакции содержит под-табы История/План
    - Кликнуть `button[aria-label="Транзакции"]`
    - Видим `button:has-text("История")` (SubTabBar)
    - Видим `button:has-text("План")` (SubTabBar)
    - Под-таб История активен по умолчанию

    txn-02: История группирует транзакции по дням с total
    - Переключиться на таб Транзакции → под-таб История
    - Виден day-header с датой
    - В day-header виден суммарный total за день (например «−275 ₽»)

    txn-03: Под-таб План показывает source-badge
    - Переключиться на под-таб «План»
    - Видны строки с badge (template / manual / subscription)

    txn-04: Фильтр-чипы Все/Расходы/Доходы видны в Транзакциях
    - Переключиться на таб Транзакции
    - Видим chip `button:has-text("Все")`
    - Видим chip `button:has-text("Расходы")`
    - Видим chip `button:has-text("Доходы")`

    txn-05: FAB в История открывает форму добавления факт-транзакции
    - На под-табе История: кликнуть FAB
    - Открывается BottomSheet с заголовком «Новая транзакция»

    mgt-01: Таб Управление показывает 4 пункта
    - Кликнуть `button[aria-label="Управление"]`
    - Видим: «Подписки», «Шаблон бюджета», «Категории», «Настройки»

    mgt-02: Каждый пункт Управления кликабелен и ведёт в саб-скрин
    - Кликнуть «Подписки» → виден заголовок SubscriptionsScreen

    placeholder: Аналитика и AI содержат «Скоро будет»
    - Кликнуть Аналитика → виден текст «Скоро будет»
    - Кликнуть AI → виден текст «Скоро будет»
  </behavior>
  <action>
    Создать `frontend/tests/e2e/nav-v03.spec.ts`.

    Использовать mockApiRich из ui-audit.spec.ts — СКОПИРОВАТЬ функцию внутрь файла (не импортировать, чтобы тест был самодостаточным). Добавить mock для `/api/v1/planned` возвращающий 2 строки с разным source.

    waitForLoad ждёт `button[aria-label="Главная"]` — это общий маркер загрузки для обоих nav (старого и нового).

    Структура файла:
    ```typescript
    import { test, expect } from '@playwright/test';

    async function mockApiRich(page) { /* копия из ui-audit.spec.ts + planned mock */ }
    async function waitForLoad(page) {
      await expect(page.locator('button[aria-label="Главная"]')).toBeVisible({ timeout: 10000 });
    }

    test('nav-01: 5 функциональных табов с новыми лейблами', ...)
    test('nav-02: AI таб имеет класс ai когда активен', ...)
    test('txn-01: Транзакции содержат под-табы История/План', ...)
    test('txn-02: История группирует по дням с day-total', ...)
    test('txn-03: Под-таб план показывает source-badge', ...)
    test('txn-04: Фильтр-чипы видны в Транзакциях', ...)
    test('txn-05: FAB в История открывает форму факт-транзакции', ...)
    test('mgt-01: Управление показывает 4 пункта меню', ...)
    test('mgt-02: Клик Подписки открывает SubscriptionsScreen', ...)
    test('placeholder: Аналитика и AI показывают Скоро будет', ...)
    ```

    ВАЖНО: Все тесты проверяют НОВОЕ поведение — они будут ПАДАТЬ на текущем коде, что является целью Wave 0.

    Запустить `cd frontend && npx playwright test tests/e2e/nav-v03.spec.ts --reporter=line 2>&1 | tail -20` и убедиться что тесты падают (FAILED, не ERROR — разница: FAILED = тест запустился но ожидание не выполнилось; ERROR = синтаксическая ошибка).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx playwright test tests/e2e/nav-v03.spec.ts --reporter=line 2>&1 | grep -E "(passed|failed|error)" | tail -5</automated>
  </verify>
  <done>Файл nav-v03.spec.ts существует; тесты запускаются без синтаксических ошибок; минимум 8 из 10 тестов FAILED (не ERROR). Это подтверждает RED-состояние.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить существующие тесты под новый nav</name>
  <read_first>
    - frontend/tests/e2e/home.spec.ts (полностью — строки 86-119)
    - frontend/tests/e2e/ui-audit.spec.ts (строки 149-256 — тест-методы с aria-labels)
  </read_first>
  <files>
    frontend/tests/e2e/home.spec.ts
    frontend/tests/e2e/ui-audit.spec.ts
  </files>
  <action>
    Обновить тесты под новую nav-структуру чтобы они ПРОШЛИ после Phase 7.

    В `home.spec.ts`:
    - Строка 107: `text=История` → `text=Транзакции`
    - Строка 108: `text=Подписки` → `text=Управление`
    - Строка 109: `text=Ещё` → УДАЛИТЬ (ещё не существует в новом nav)
    - Сохранить `text=Главная` (не меняется)
    - Добавить проверку `text=Аналитика` и `text=AI`

    В `ui-audit.spec.ts`:
    - `waitForLoad`: `button[aria-label="Главная"]` — оставить как есть (не меняется)
    - audit-03: `button[aria-label="История"]` → `button[aria-label="Транзакции"]`
    - audit-04: `button[aria-label="Подписки"]` → `button[aria-label="Управление"]`, затем клик по «Подписки» в меню
    - audit-05: `button[aria-label="Ещё"]` → `button[aria-label="Управление"]`
    - audit-06: `button[aria-label="Ещё"]` → `button[aria-label="Управление"]`
    - audit-07: `button[aria-label="Добавить факт-трату"]` → `button[aria-label="Добавить транзакцию"]` (проверить актуальный ariaLabel в Fab)

    Для audit-04 (SubscriptionsScreen): после клика «Управление» нужно кликнуть на «Подписки» в меню ManagementScreen.

    Обновить скриншот-пути: `03-history.png` переименовать в `03-transactions.png`; `04-subscriptions.png` → `04-management.png`; `05-more.png` → `05-management-subscriptions.png` — только в комментариях/путях, не переименовывать старые скриншоты.

    Запустить `tsc --noEmit` в frontend чтобы убедиться что нет TS ошибок в тестах.
  </action>
  <acceptance_criteria>
    - home.spec.ts: `grep -c "Транзакции" frontend/tests/e2e/home.spec.ts` возвращает >= 1
    - home.spec.ts: `grep -c "История" frontend/tests/e2e/home.spec.ts` возвращает 0 (старый лейбл удалён)
    - ui-audit.spec.ts: `grep -c "aria-label=\"Управление\"" frontend/tests/e2e/ui-audit.spec.ts` возвращает >= 3
    - ui-audit.spec.ts: `grep -c "aria-label=\"Ещё\"" frontend/tests/e2e/ui-audit.spec.ts` возвращает 0
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Транзакции" /Users/exy/pet_projects/tg-budget-planner/frontend/tests/e2e/home.spec.ts && grep -c "aria-label=\"Управление\"" /Users/exy/pet_projects/tg-budget-planner/frontend/tests/e2e/ui-audit.spec.ts</automated>
  </verify>
  <done>home.spec.ts и ui-audit.spec.ts обновлены: старые лейблы (История/Подписки/Ещё) заменены на новые (Транзакции/Управление); TS компилируется без ошибок.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app | E2E тесты обращаются к mock API, не к реальному бэкенду |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | mockApiRich | accept | Это тестовый mock, не боевой код; real API защищён HMAC |
</threat_model>

<verification>
- nav-v03.spec.ts существует и содержит 10 тестов
- Тесты запускаются (нет синтаксических ошибок)
- Тесты RED (FAILED, не ERROR) — это ожидаемое поведение
- home.spec.ts и ui-audit.spec.ts не содержат старых aria-labels (История/Подписки/Ещё)
- TypeScript компилируется без ошибок: `cd frontend && npx tsc --noEmit`
</verification>

<success_criteria>
- `frontend/tests/e2e/nav-v03.spec.ts` содержит тесты для NAV-01..NAV-02, TXN-01..TXN-05, MGT-01..MGT-02, placeholder
- Все тесты в nav-v03.spec.ts FAILED (не ERROR) на текущем коде
- home.spec.ts и ui-audit.spec.ts используют новые aria-labels
</success_criteria>

<output>
После завершения создать `.planning/phases/07-nav-refactor/07-01-SUMMARY.md`
</output>
