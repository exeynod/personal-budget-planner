---
phase: 07-nav-refactor
plan: 06
type: execute
wave: 3
depends_on:
  - "07-05"
files_modified:
  - frontend/tests/e2e/nav-v03.spec.ts
  - .planning/phases/07-nav-refactor/07-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - TXN-01
  - TXN-02
  - TXN-03
  - TXN-04
  - TXN-05
  - MGT-01
  - MGT-02
  - MGT-03
  - MGT-04

must_haves:
  truths:
    - "Все 10 e2e тестов nav-v03.spec.ts PASSED (GREEN)"
    - "Vite build проходит без warnings"
    - "TypeScript компилируется чисто"
    - "07-VERIFICATION.md создан с результатами проверки всех требований NAV/TXN/MGT"
    - "ROADMAP Phase 7 отмечен как Complete"
  artifacts:
    - path: ".planning/phases/07-nav-refactor/07-VERIFICATION.md"
      provides: "Результаты проверки Phase 7"
      contains: "NAV-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 отмечен как Complete"
      contains: "Phase 7: Nav Refactor"
  key_links:
    - from: "nav-v03.spec.ts"
      to: "новый BottomNav"
      via: "aria-label buttons"
      pattern: "PASSED"
---

<objective>
Wave 3: Финальная верификация Phase 7 — прогнать e2e тесты (должны быть GREEN), зафиксировать результаты в VERIFICATION.md, отметить Phase 7 Complete в ROADMAP.

Purpose: Подтвердить что все NAV-01..04, TXN-01..05, MGT-01..04 реализованы и протестированы.

Output: VERIFICATION.md + обновлённый ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-nav-refactor/07-CONTEXT.md
@.planning/phases/07-nav-refactor/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Прогнать e2e тесты и зафиксировать результаты</name>
  <read_first>
    - frontend/tests/e2e/nav-v03.spec.ts (тесты из Plan 01 — теперь должны быть GREEN)
    - frontend/tests/e2e/home.spec.ts (обновлённые тесты из Plan 01)
    - frontend/tests/e2e/ui-audit.spec.ts (обновлённые тесты из Plan 01)
  </read_first>
  <files>.planning/phases/07-nav-refactor/07-VERIFICATION.md</files>
  <action>
    1. Запустить полный e2e suite:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx playwright test tests/e2e/ --reporter=list 2>&1
       ```

    2. Запустить TypeScript проверку:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1
       ```

    3. Запустить Vite build:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm run build 2>&1 | tail -20
       ```

    4. Если nav-v03.spec.ts тесты всё ещё падают — исправить реализацию в соответствующих файлах (НЕ упрощать тесты). Разрешённые правки: фикс aria-label, фикс CSS классов, фикс условий рендера.

    5. Создать `.planning/phases/07-nav-refactor/07-VERIFICATION.md`:

    ```markdown
    # Phase 7: Nav Refactor — Verification

    **Date:** {сегодняшняя дата}
    **Status:** COMPLETE / PARTIAL (указать реальный результат)

    ## Test Results

    ### nav-v03.spec.ts
    | Test | Status |
    |------|--------|
    | nav-01: 5 табов с новыми лейблами | PASSED / FAILED |
    | nav-02: AI таб имеет класс ai когда активен | PASSED / FAILED |
    | txn-01: Транзакции содержат под-табы История/План | PASSED / FAILED |
    | txn-02: История группирует по дням с day-total | PASSED / FAILED |
    | txn-03: Под-таб план показывает source-badge | PASSED / FAILED |
    | txn-04: Фильтр-чипы видны в Транзакциях | PASSED / FAILED |
    | txn-05: FAB в История открывает форму | PASSED / FAILED |
    | mgt-01: Управление показывает 4 пункта меню | PASSED / FAILED |
    | mgt-02: Клик Подписки открывает SubscriptionsScreen | PASSED / FAILED |
    | placeholder: Аналитика и AI показывают Скоро будет | PASSED / FAILED |

    ### home.spec.ts
    - Total: N passed, M failed

    ### ui-audit.spec.ts
    - Total: N passed, M failed

    ## Requirements Checklist

    | Req | Description | Status |
    |-----|-------------|--------|
    | NAV-01 | 5 табов: Главная/Транзакции/Аналитика/AI/Управление | ✓ / ✗ |
    | NAV-02 | AI таб #a78bfa когда активен | ✓ / ✗ |
    | NAV-03 | Phosphor: House/ArrowsLeftRight/ChartBar/Sparkle/SquaresFour | ✓ / ✗ |
    | NAV-04 | Старые экраны реорганизованы без потери функциональности | ✓ / ✗ |
    | TXN-01 | Под-табы История/План в underline TabBar | ✓ / ✗ |
    | TXN-02 | История: группировка по дням с day-total | ✓ / ✗ |
    | TXN-03 | План: source-badge (template/manual/subscription) | ✓ / ✗ |
    | TXN-04 | Фильтр-чипы Все/Расходы/Доходы | ✓ / ✗ |
    | TXN-05 | FAB context-aware (actual vs planned) | ✓ / ✗ |
    | MGT-01 | Управление: 4 пункта Подписки/Шаблон/Категории/Настройки | ✓ / ✗ |
    | MGT-02 | Каждый пункт: иконка 36×36 + title + desc + chevron | ✓ / ✗ |
    | MGT-03 | Контекстные desc (статичные в Phase 7) | ✓ / ✗ |
    | MGT-04 | Саб-скрины переиспользуются без изменений | ✓ / ✗ |

    ## Build Status
    - TypeScript: 0 errors ✓ / N errors ✗
    - Vite build: success ✓ / errors ✗

    ## Known Issues / Deferred
    {список если есть}
    ```

    Заполнить реальными результатами из команд выше.
  </action>
  <acceptance_criteria>
    - `test -f .planning/phases/07-nav-refactor/07-VERIFICATION.md` — файл существует
    - `grep -c "PASSED" .planning/phases/07-nav-refactor/07-VERIFICATION.md` возвращает >= 8 (минимум 8 из 10 тестов green)
    - `grep -c "NAV-01\|TXN-01\|MGT-01" .planning/phases/07-nav-refactor/07-VERIFICATION.md` возвращает >= 3
    - `cd frontend && npm run build 2>&1 | grep -c "error"` возвращает 0
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx playwright test tests/e2e/nav-v03.spec.ts --reporter=line 2>&1 | tail -10</automated>
  </verify>
  <done>07-VERIFICATION.md создан с реальными результатами тестов; nav-v03.spec.ts имеет >= 8 PASSED; Vite build и TypeScript чисты.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 7 Nav Refactor полностью реализована:
    - BottomNav: 5 табов (Главная/Транзакции/Аналитика/AI/Управление) с Phosphor-иконками
    - TransactionsScreen: под-табы История/Plan + filter chips + context-aware FAB
    - ManagementScreen: 4 пункта меню с иконками 36px
    - AnalyticsScreen + AiScreen: «Скоро будет» placeholders
    - App.tsx переписан, MoreScreen удалён
    - E2e тесты GREEN
  </what-built>
  <how-to-verify>
    Открыть приложение в браузере (если dev server запущен: http://localhost:5173) или задеплоить на VPS.

    Проверить визуально:
    1. Bottom nav показывает 5 табов с русскими лейблами и иконками
    2. Таб AI подсвечен фиолетовым (#a78bfa) когда активен
    3. Клик «Транзакции» → показывает под-табы «История» / «Plan» + filter chips
    4. В под-табе «История» транзакции сгруппированы по дням, в заголовке дня виден total
    5. FAB в «История» открывает «Новая транзакция», в «План» — «Новая строка плана»
    6. Клик «Управление» → 4 пункта (Подписки первая), каждый с иконкой и desc
    7. Клик «Подписки» → открывает SubscriptionsScreen (полноценный, без регрессий)
    8. Клик «Аналитика» → «Скоро будет» placeholder
    9. Клик «AI» → «Скоро будет» placeholder с фиолетовой иконкой
    10. На HomeScreen: клик на категорию → открывает «Транзакции» с фильтром по категории
  </how-to-verify>
  <resume-signal>Введите "approved" если всё выглядит правильно, или опишите найденные проблемы</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Обновить ROADMAP Phase 7 → Complete</name>
  <read_first>
    - .planning/ROADMAP.md (строки 137-149 — Phase 7 секция)
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
    Обновить ROADMAP.md:

    1. Найти строку `- [ ] **Phase 7: Nav Refactor**` и изменить на `- [x] **Phase 7: Nav Refactor**`

    2. В секции `### Phase 7: Nav Refactor` обновить `**Plans**: TBD` на:
    ```
    **Plans**: 6 планов

    Plans:
    - [x] 07-01-PLAN.md — Wave 0: RED e2e тесты (nav-v03.spec.ts)
    - [x] 07-02-PLAN.md — Общие компоненты: BottomNav (5 табов), SubTabBar, PageTitle
    - [x] 07-03-PLAN.md — TransactionsScreen: HistoryView + PlannedView + filter chips + context-aware FAB
    - [x] 07-04-PLAN.md — ManagementScreen (4 пункта) + placeholder AnalyticsScreen + AiScreen
    - [x] 07-05-PLAN.md — App.tsx routing rewrite + удаление MoreScreen
    - [x] 07-06-PLAN.md — Verification: e2e GREEN + VERIFICATION.md + ROADMAP complete
    ```

    3. В таблице Milestone v0.3 обновить Phase 7:
    ```
    | 7. Nav Refactor | 6/6 | Complete | {сегодняшняя дата} |
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "\[x\].*Phase 7" .planning/ROADMAP.md` возвращает >= 1
    - `grep -c "07-01-PLAN.md" .planning/ROADMAP.md` возвращает 1
    - `grep -c "07-06-PLAN.md" .planning/ROADMAP.md` возвращает 1
    - `grep -c "6/6.*Complete" .planning/ROADMAP.md` возвращает >= 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c "\[x\].*Phase 7\|07-0.*PLAN" /Users/exy/pet_projects/tg-budget-planner/.planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md обновлён: Phase 7 отмечен Complete, список 6 планов заполнен, таблица milestone обновлена.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright → dev server | E2e тесты обращаются к mock API, не к боевому backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | e2e mock API | accept | Mock только для тестов; prod API защищён HMAC |
</threat_model>

<verification>
- nav-v03.spec.ts: >= 8 из 10 тестов PASSED
- home.spec.ts и ui-audit.spec.ts: проходят без ошибок связанных с новым nav
- Vite build: success
- TypeScript: 0 ошибок
- 07-VERIFICATION.md создан с чеклистом NAV/TXN/MGT
- ROADMAP Phase 7: [x] Complete
</verification>

<success_criteria>
- Все e2e тесты nav-v03.spec.ts GREEN
- 07-VERIFICATION.md создан и заполнен реальными результатами
- ROADMAP Phase 7 отмечен Complete
- Checkpoint: пользователь подтвердил визуальное соответствие
</success_criteria>

<output>
После завершения создать `.planning/phases/07-nav-refactor/07-06-SUMMARY.md`
</output>
