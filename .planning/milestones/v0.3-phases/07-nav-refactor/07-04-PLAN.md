---
phase: 07-nav-refactor
plan: 04
type: execute
wave: 1
depends_on:
  - "07-02"
files_modified:
  - frontend/src/screens/ManagementScreen.tsx
  - frontend/src/screens/ManagementScreen.module.css
  - frontend/src/screens/AnalyticsScreen.tsx
  - frontend/src/screens/AnalyticsScreen.module.css
  - frontend/src/screens/AiScreen.tsx
  - frontend/src/screens/AiScreen.module.css
autonomous: true
requirements:
  - MGT-01
  - MGT-02
  - MGT-03
  - MGT-04
  - NAV-04

must_haves:
  truths:
    - "ManagementScreen показывает 4 пункта: Подписки/Шаблон/Категории/Настройки"
    - "Каждый пункт — surface card с иконкой 36×36, title + desc + chevron"
    - "Контекстные desc — статичные строки для Phase 7 (динамика отложена)"
    - "AnalyticsScreen рендерит PageTitle 'Аналитика' + блок 'Скоро будет'"
    - "AiScreen рендерит PageTitle 'AI' + блок 'Скоро будет'"
  artifacts:
    - path: "frontend/src/screens/ManagementScreen.tsx"
      provides: "Меню 4 пунктов с Phosphor-иконками"
      exports: ["ManagementScreen"]
    - path: "frontend/src/screens/AnalyticsScreen.tsx"
      provides: "Placeholder экран Аналитика"
      exports: ["AnalyticsScreen"]
    - path: "frontend/src/screens/AiScreen.tsx"
      provides: "Placeholder экран AI"
      exports: ["AiScreen"]
  key_links:
    - from: "ManagementScreen.tsx"
      to: "App.tsx managementView state"
      via: "onNavigate callback"
      pattern: "onNavigate.*subscriptions|template|categories|settings"
    - from: "AnalyticsScreen.tsx + AiScreen.tsx"
      to: "PageTitle"
      via: "import PageTitle"
      pattern: "PageTitle"
---

<objective>
Wave 1c (параллельно с Plan 03): Создать ManagementScreen (4 пункта меню), AnalyticsScreen (placeholder), AiScreen (placeholder).

Purpose: Заполняет оставшиеся 3 таба нового nav. ManagementScreen заменяет MoreScreen (которая удалится в Plan 05). Placeholder экраны разблокируют UX-форму для Phase 8/9.

Output: 6 файлов (.tsx + .module.css). Старый MoreScreen.tsx НЕ удаляется здесь — удалится в Plan 05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nav-refactor/07-CONTEXT.md
@.planning/phases/07-nav-refactor/07-02-SUMMARY.md

<interfaces>
<!-- MoreScreen.tsx — основа для ManagementScreen -->
```typescript
// frontend/src/screens/MoreScreen.tsx
// Текущие 3 пункта: template / categories / settings
// НОВЫЕ 4 пункта: subscriptions / template / categories / settings
// Порядок: Подписки ПЕРВЫЕ (per D-NAV — locked decision)

type ManagementView = 'subscriptions' | 'categories' | 'template' | 'settings';

export interface ManagementScreenProps {
  onNavigate: (screen: ManagementView) => void;
}
```

<!-- Phosphor иконки для ManagementScreen (из CONTEXT.md specifics): -->
// Bell → Subscriptions
// FileText → Template
// Tag → Categories
// Gear → Settings
// Все уже используются в существующем MoreScreen.tsx

<!-- PageTitle из Plan 02 -->
```typescript
// frontend/src/components/PageTitle.tsx
export interface PageTitleProps { title: string; subtitle?: string; }
export function PageTitle({ title, subtitle }: PageTitleProps): JSX.Element;
```

<!-- MoreScreen.module.css — стили для копирования -->
// .root, .header, .title, .list, .row, .icon, .rowText, .rowLabel, .rowDesc, .chevron
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать ManagementScreen (4 пункта, PageTitle)</name>
  <read_first>
    - frontend/src/screens/MoreScreen.tsx (полностью — основа для ManagementScreen)
    - frontend/src/screens/MoreScreen.module.css (стили для переноса)
    - .planning/REQUIREMENTS.md строки 92-97 (MGT-01..MGT-04 детали)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (specifics: иконки, порядок пунктов, locked: Подписки первые)
  </read_first>
  <files>
    frontend/src/screens/ManagementScreen.tsx
    frontend/src/screens/ManagementScreen.module.css
  </files>
  <behavior>
    - 4 пункта в порядке: Подписки / Шаблон бюджета / Категории / Настройки
    - Иконки: Bell (Подписки) / FileText (Шаблон) / Tag (Категории) / Gear (Настройки)
    - Иконки size=36 (не 20 как в MoreScreen) согласно MGT-02
    - Каждый пункт: иконка 36×36 + title + desc + chevron
    - Контекстные desc (статичные для Phase 7, per deferred в CONTEXT.md):
      - Подписки: «Активные подписки и напоминания»
      - Шаблон: «Повторяющийся план для нового периода»
      - Категории: «Управление категориями расходов и доходов»
      - Настройки: «День начала периода, уведомления»
    - PageTitle: title = «Управление» (без subtitle)
    - onNavigate callback: тип ManagementView = 'subscriptions' | 'template' | 'categories' | 'settings'
  </behavior>
  <action>
    Создать `frontend/src/screens/ManagementScreen.tsx`:

    ```tsx
    import { Bell, FileText, Tag, Gear, type Icon } from '@phosphor-icons/react';
    import { PageTitle } from '../components/PageTitle';
    import styles from './ManagementScreen.module.css';

    export type ManagementView = 'subscriptions' | 'template' | 'categories' | 'settings';

    export interface ManagementScreenProps {
      onNavigate: (screen: ManagementView) => void;
    }

    const ICONS: Record<ManagementView, Icon> = {
      subscriptions: Bell,
      template: FileText,
      categories: Tag,
      settings: Gear,
    };

    const ITEMS: { id: ManagementView; label: string; description: string }[] = [
      { id: 'subscriptions', label: 'Подписки', description: 'Активные подписки и напоминания' },
      { id: 'template', label: 'Шаблон бюджета', description: 'Повторяющийся план для нового периода' },
      { id: 'categories', label: 'Категории', description: 'Управление категориями расходов и доходов' },
      { id: 'settings', label: 'Настройки', description: 'День начала периода, уведомления' },
    ];

    export function ManagementScreen({ onNavigate }: ManagementScreenProps) {
      return (
        <div className={styles.root}>
          <PageTitle title="Управление" />
          <div className={styles.list}>
            {ITEMS.map((item) => {
              const IconComp = ICONS[item.id];
              return (
                <button
                  key={item.id}
                  type="button"
                  className={styles.row}
                  onClick={() => onNavigate(item.id)}
                >
                  <span className={styles.iconWrap}>
                    <IconComp size={36} weight="thin" color="var(--color-primary)" />
                  </span>
                  <div className={styles.rowText}>
                    <div className={styles.rowLabel}>{item.label}</div>
                    <div className={styles.rowDesc}>{item.description}</div>
                  </div>
                  <span className={styles.chevron} aria-hidden="true">›</span>
                </button>
              );
            })}
          </div>
        </div>
      );
    }
    ```

    Создать `ManagementScreen.module.css` (на основе MoreScreen.module.css с обновлённым iconWrap):
    ```css
    .root {
      padding-bottom: calc(var(--bottom-nav-height) + env(safe-area-inset-bottom, 0px));
    }

    .list {
      padding: 8px 12px;
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .row {
      display: flex;
      align-items: center;
      gap: 14px;
      padding: 14px 12px;
      background: var(--color-bg-elevated);
      border: 1px solid var(--color-border);
      border-radius: 12px;
      cursor: pointer;
      text-align: left;
      width: 100%;
      font-family: inherit;
      transition: opacity 100ms;
      -webkit-tap-highlight-color: transparent;
    }

    .row:active { opacity: 0.7; }

    .iconWrap {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      flex-shrink: 0;
      background: color-mix(in srgb, var(--color-primary) 10%, transparent);
      border-radius: 10px;
    }

    .rowText {
      flex: 1;
      min-width: 0;
    }

    .rowLabel {
      font-size: 15px;
      font-weight: var(--weight-medium);
      color: var(--color-text);
      line-height: 1.3;
    }

    .rowDesc {
      font-size: 12px;
      color: var(--color-text-muted);
      margin-top: 2px;
      line-height: 1.3;
    }

    .chevron {
      font-size: 20px;
      color: var(--color-text-muted);
      flex-shrink: 0;
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "export.*ManagementScreen" frontend/src/screens/ManagementScreen.tsx` возвращает 1
    - `grep -c "export type ManagementView" frontend/src/screens/ManagementScreen.tsx` возвращает 1
    - `grep -c "subscriptions" frontend/src/screens/ManagementScreen.tsx` возвращает >= 2
    - `grep -c "PageTitle" frontend/src/screens/ManagementScreen.tsx` возвращает >= 2
    - `grep "id: 'subscriptions'" frontend/src/screens/ManagementScreen.tsx | head -1` — subscriptions должна быть ПЕРВОЙ в массиве ITEMS (строка выше template)
    - `grep -c "Bell" frontend/src/screens/ManagementScreen.tsx` возвращает 1
    - `grep -c "iconWrap" frontend/src/screens/ManagementScreen.module.css` возвращает >= 2
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>ManagementScreen.tsx с 4 пунктами (Подписки первая), PageTitle, иконками 36px. ManagementScreen.module.css с card-стилем. TypeScript чист.</done>
</task>

<task type="auto">
  <name>Task 2: Создать AnalyticsScreen и AiScreen (placeholder)</name>
  <read_first>
    - frontend/src/components/PageTitle.tsx (только что созданный — знать интерфейс)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (specifics: Placeholder screens: PageTitle + card "Скоро будет"; AI tab #a78bfa)
    - .planning/REQUIREMENTS.md строки 78-89 (NAV-01: «Скоро будет» для Аналитики и AI)
  </read_first>
  <files>
    frontend/src/screens/AnalyticsScreen.tsx
    frontend/src/screens/AnalyticsScreen.module.css
    frontend/src/screens/AiScreen.tsx
    frontend/src/screens/AiScreen.module.css
  </files>
  <action>
    Создать `frontend/src/screens/AnalyticsScreen.tsx`:
    ```tsx
    import { ChartBar } from '@phosphor-icons/react';
    import { PageTitle } from '../components/PageTitle';
    import styles from './AnalyticsScreen.module.css';

    export function AnalyticsScreen() {
      return (
        <div className={styles.root}>
          <PageTitle title="Аналитика" />
          <div className={styles.comingSoon}>
            <ChartBar size={48} weight="thin" color="var(--color-text-muted)" />
            <p className={styles.text}>Скоро будет</p>
            <p className={styles.sub}>Тренды расходов, топ категорий и прогноз остатка появятся в следующем обновлении</p>
          </div>
        </div>
      );
    }
    ```

    Создать `frontend/src/screens/AnalyticsScreen.module.css`:
    ```css
    .root {
      padding-bottom: calc(var(--bottom-nav-height) + env(safe-area-inset-bottom, 0px));
    }

    .comingSoon {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 60px 24px;
      gap: 12px;
      text-align: center;
    }

    .text {
      font-size: 18px;
      font-weight: var(--weight-medium);
      color: var(--color-text);
      margin: 0;
    }

    .sub {
      font-size: 13px;
      color: var(--color-text-muted);
      margin: 0;
      line-height: 1.5;
    }
    ```

    Создать `frontend/src/screens/AiScreen.tsx`:
    ```tsx
    import { Sparkle } from '@phosphor-icons/react';
    import { PageTitle } from '../components/PageTitle';
    import styles from './AiScreen.module.css';

    export function AiScreen() {
      return (
        <div className={styles.root}>
          <PageTitle title="AI" />
          <div className={styles.comingSoon}>
            <Sparkle size={48} weight="thin" color="#a78bfa" />
            <p className={styles.text}>Скоро будет</p>
            <p className={styles.sub}>Conversational AI-помощник с доступом к данным бюджета появится в следующем обновлении</p>
          </div>
        </div>
      );
    }
    ```

    Создать `frontend/src/screens/AiScreen.module.css`:
    ```css
    .root {
      padding-bottom: calc(var(--bottom-nav-height) + env(safe-area-inset-bottom, 0px));
    }

    .comingSoon {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: 60px 24px;
      gap: 12px;
      text-align: center;
    }

    .text {
      font-size: 18px;
      font-weight: var(--weight-medium);
      color: var(--color-text);
      margin: 0;
    }

    .sub {
      font-size: 13px;
      color: var(--color-text-muted);
      margin: 0;
      line-height: 1.5;
    }
    ```

    Обратить внимание: AiScreen использует `#a78bfa` (фиолетовый AI-акцент) для иконки Sparkle — визуально согласовано с AI-табом в BottomNav.
  </action>
  <acceptance_criteria>
    - `grep -c "Скоро будет" frontend/src/screens/AnalyticsScreen.tsx` возвращает 1
    - `grep -c "Скоро будет" frontend/src/screens/AiScreen.tsx` возвращает 1
    - `grep -c "PageTitle" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 2
    - `grep -c "PageTitle" frontend/src/screens/AiScreen.tsx` возвращает >= 2
    - `grep -c "a78bfa" frontend/src/screens/AiScreen.tsx` возвращает 1
    - `grep -c "Аналитика" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>AnalyticsScreen.tsx и AiScreen.tsx созданы с PageTitle + «Скоро будет» блок. AiScreen использует #a78bfa для Sparkle иконки. TypeScript чист.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ManagementScreen → sub-screens | onNavigate вызывает setState в App.tsx — нет прямого доступа к данным |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | ManagementScreen onNavigate | accept | Переключение view — UI state без side-effects на данные |
| T-07-07 | Information Disclosure | AiScreen placeholder | accept | Не содержит реальных данных |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — 0 ошибок
- ManagementScreen.tsx: 4 пункта, Подписки первая, PageTitle
- AnalyticsScreen.tsx: PageTitle «Аналитика» + «Скоро будет»
- AiScreen.tsx: PageTitle «AI» + «Скоро будет» + #a78bfa для Sparkle
- MoreScreen.tsx НЕ удалён (удалится в Plan 05)
</verification>

<success_criteria>
- 6 файлов созданы (ManagementScreen, AnalyticsScreen, AiScreen + их .module.css)
- TypeScript компилируется чисто
- ManagementScreen экспортирует ManagementView тип для App.tsx (Plan 05)
- Placeholder экраны содержат «Скоро будет» текст
</success_criteria>

<output>
После завершения создать `.planning/phases/07-nav-refactor/07-04-SUMMARY.md`
</output>
