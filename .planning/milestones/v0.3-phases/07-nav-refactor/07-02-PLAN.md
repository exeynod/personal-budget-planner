---
phase: 07-nav-refactor
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - frontend/src/components/BottomNav.tsx
  - frontend/src/components/BottomNav.module.css
  - frontend/src/components/SubTabBar.tsx
  - frontend/src/components/SubTabBar.module.css
  - frontend/src/components/PageTitle.tsx
  - frontend/src/components/PageTitle.module.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - TXN-01

must_haves:
  truths:
    - "BottomNav показывает ровно 5 табов с новыми Phosphor-иконками и лейблами"
    - "AI-таб имеет CSS-класс .ai.active с цветом #a78bfa"
    - "TabId тип экспортирован с 5 значениями: home/transactions/analytics/ai/management"
    - "SubTabBar рендерит underline-стиль табы (История/План) переиспользуемо"
    - "PageTitle рендерит h1 + опциональный subtitle"
  artifacts:
    - path: "frontend/src/components/BottomNav.tsx"
      provides: "Новый 5-табовый BottomNav с Phosphor-иконками"
      exports: ["BottomNav", "TabId"]
    - path: "frontend/src/components/SubTabBar.tsx"
      provides: "Underline-стиль под-табы для Транзакций (и Phase 8 Аналитики)"
      exports: ["SubTabBar", "SubTabId"]
    - path: "frontend/src/components/PageTitle.tsx"
      provides: "Общий h1 компонент с optional subtitle"
      exports: ["PageTitle"]
  key_links:
    - from: "BottomNav.tsx"
      to: "App.tsx"
      via: "TabId тип экспорт"
      pattern: "export type TabId"
    - from: "SubTabBar.tsx"
      to: "TransactionsScreen.tsx (Plan 03)"
      via: "SubTabId prop"
      pattern: "export type SubTabId"
---

<objective>
Wave 1a: Создать общие компоненты для Phase 7 — переписать BottomNav (5 табов + AI-акцент), создать SubTabBar (underline-стиль, переиспользуем в Phase 8) и PageTitle.

Purpose: Эти компоненты — контракты для Plan 03 (TransactionsScreen) и Plan 04 (ManagementScreen). Должны быть готовы до начала работы над экранами.

Output: 3 компонента (6 файлов: .tsx + .module.css) — фундамент для всей Phase 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nav-refactor/07-CONTEXT.md
@.planning/phases/07-nav-refactor/07-01-SUMMARY.md

<interfaces>
<!-- Текущий BottomNav.tsx — основа для рефакторинга -->
<!-- Текущие иконки: House, Receipt, CalendarBlank, Bell, DotsNine -->
<!-- Новые иконки (per D-NAV-03): House, ArrowsLeftRight, ChartBar, Sparkle, SquaresFour -->

<!-- Текущий паттерн icon weight: active ? 'fill' : 'thin' — оставить -->
<!-- Текущий CSS паттерн .active { color: var(--color-primary) } -->
<!-- AI-таб: добавить .ai.active { color: #a78bfa } -->

<!-- Из @phosphor-icons/react уже установлен, доступны все иконки -->
<!-- Проверить наличие: grep "ArrowsLeftRight\|ChartBar\|Sparkle\|SquaresFour" frontend/package.json не нужно — @phosphor-icons/react содержит все иконки -->

<!-- SubTabBar контракт (новый компонент): -->
```typescript
export type SubTabId = 'history' | 'plan';

export interface SubTabBarProps {
  active: SubTabId;
  onChange: (tab: SubTabId) => void;
  tabs: { id: SubTabId; label: string }[];
}
```

<!-- PageTitle контракт (новый компонент): -->
```typescript
export interface PageTitleProps {
  title: string;
  subtitle?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Переписать BottomNav (5 табов, Phosphor-иконки, AI-акцент)</name>
  <read_first>
    - frontend/src/components/BottomNav.tsx (полностью — текущая реализация)
    - frontend/src/components/BottomNav.module.css (текущие стили)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (секция specifics: AI таб #a78bfa, NAV-03 иконки)
  </read_first>
  <files>
    frontend/src/components/BottomNav.tsx
    frontend/src/components/BottomNav.module.css
  </files>
  <behavior>
    - TabId = 'home' | 'transactions' | 'analytics' | 'ai' | 'management'
    - 5 табов: Главная/Транзакции/Аналитика/AI/Управление
    - Иконки: House/ArrowsLeftRight/ChartBar/Sparkle/SquaresFour (all from @phosphor-icons/react)
    - active ? 'fill' : 'thin' weight паттерн — сохранить
    - .active класс → color: var(--color-primary) для всех кроме ai
    - .ai.active класс → color: #a78bfa (фиолетовый)
    - aria-label на каждой кнопке = label таба
    - aria-current={activeTab === id ? 'page' : undefined} — сохранить
  </behavior>
  <action>
    Переписать frontend/src/components/BottomNav.tsx:

    ```tsx
    import { House, ArrowsLeftRight, ChartBar, Sparkle, SquaresFour } from '@phosphor-icons/react';
    import styles from './BottomNav.module.css';

    export type TabId = 'home' | 'transactions' | 'analytics' | 'ai' | 'management';

    export interface BottomNavProps {
      activeTab: TabId;
      onTabChange: (tab: TabId) => void;
    }

    type TabIconProps = { id: TabId; active: boolean };

    const TabIcon = ({ id, active }: TabIconProps) => {
      const weight = active ? 'fill' : 'thin';
      const size = 26;
      if (id === 'home') return <House size={size} weight={weight} />;
      if (id === 'transactions') return <ArrowsLeftRight size={size} weight={weight} />;
      if (id === 'analytics') return <ChartBar size={size} weight={weight} />;
      if (id === 'ai') return <Sparkle size={size} weight={weight} />;
      return <SquaresFour size={size} weight={weight} />;
    };

    const TABS: { id: TabId; label: string }[] = [
      { id: 'home', label: 'Главная' },
      { id: 'transactions', label: 'Транзакции' },
      { id: 'analytics', label: 'Аналитика' },
      { id: 'ai', label: 'AI' },
      { id: 'management', label: 'Управление' },
    ];

    export function BottomNav({ activeTab, onTabChange }: BottomNavProps) {
      return (
        <nav className={styles.nav} aria-label="Навигация">
          {TABS.map(({ id, label }) => {
            const isActive = activeTab === id;
            return (
              <button
                key={id}
                type="button"
                className={[styles.tab, isActive ? styles.active : '', id === 'ai' ? styles.ai : ''].filter(Boolean).join(' ')}
                onClick={() => onTabChange(id)}
                aria-label={label}
                aria-current={isActive ? 'page' : undefined}
              >
                <span className={styles.icon}><TabIcon id={id} active={isActive} /></span>
                <span className={styles.label}>{label}</span>
              </button>
            );
          })}
        </nav>
      );
    }
    ```

    Обновить BottomNav.module.css — добавить в конец:
    ```css
    .ai.active {
      color: #a78bfa;
    }
    ```
    Остальные стили (.nav, .tab, .active, .icon, .label) оставить без изменений.
  </action>
  <acceptance_criteria>
    - `grep -c "ArrowsLeftRight" frontend/src/components/BottomNav.tsx` возвращает 1
    - `grep -c "SquaresFour" frontend/src/components/BottomNav.tsx` возвращает 1
    - `grep -c "Sparkle" frontend/src/components/BottomNav.tsx` возвращает 1
    - `grep -c "transactions" frontend/src/components/BottomNav.tsx` возвращает >= 2
    - `grep -c "management" frontend/src/components/BottomNav.tsx` возвращает >= 2
    - `grep -c "a78bfa" frontend/src/components/BottomNav.module.css` возвращает 1
    - `grep -c "ai.active\|\.ai\.active" frontend/src/components/BottomNav.module.css` возвращает 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "BottomNav"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>BottomNav.tsx содержит 5 табов с новыми иконками и типом; .module.css содержит .ai.active { color: #a78bfa }; TypeScript компилируется без ошибок связанных с BottomNav.</done>
</task>

<task type="auto">
  <name>Task 2: Создать SubTabBar и PageTitle компоненты</name>
  <read_first>
    - frontend/src/components/ScreenHeader.tsx (паттерн CSS Modules компонента с title)
    - frontend/src/components/ScreenHeader.module.css (паттерн стилей)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (SubTabBar: underline sticky TabBar; PageTitle: h1 + optional subtitle)
  </read_first>
  <files>
    frontend/src/components/SubTabBar.tsx
    frontend/src/components/SubTabBar.module.css
    frontend/src/components/PageTitle.tsx
    frontend/src/components/PageTitle.module.css
  </files>
  <action>
    Создать `frontend/src/components/SubTabBar.tsx`:

    ```tsx
    import styles from './SubTabBar.module.css';

    export type SubTabId = 'history' | 'plan';

    export interface SubTabItem<T extends string = SubTabId> {
      id: T;
      label: string;
    }

    export interface SubTabBarProps<T extends string = SubTabId> {
      active: T;
      onChange: (tab: T) => void;
      tabs: SubTabItem<T>[];
    }

    export function SubTabBar<T extends string = SubTabId>({ active, onChange, tabs }: SubTabBarProps<T>) {
      return (
        <div className={styles.bar} role="tablist">
          {tabs.map(({ id, label }) => (
            <button
              key={id}
              type="button"
              role="tab"
              aria-selected={active === id}
              className={[styles.tab, active === id ? styles.active : ''].filter(Boolean).join(' ')}
              onClick={() => onChange(id)}
            >
              {label}
            </button>
          ))}
        </div>
      );
    }
    ```

    Создать `frontend/src/components/SubTabBar.module.css`:
    ```css
    .bar {
      display: flex;
      gap: 0;
      border-bottom: 1px solid var(--color-border);
      background: var(--color-bg-elevated);
      position: sticky;
      top: 0;
      z-index: 10;
    }

    .tab {
      flex: 1;
      padding: 10px 12px;
      background: transparent;
      border: 0;
      border-bottom: 2px solid transparent;
      margin-bottom: -1px;
      font-size: 14px;
      font-weight: var(--weight-medium);
      color: var(--color-text-muted);
      cursor: pointer;
      font-family: inherit;
      transition: color 100ms, border-color 100ms;
      -webkit-tap-highlight-color: transparent;
    }

    .tab.active {
      color: var(--color-primary);
      border-bottom-color: var(--color-primary);
    }
    ```

    Создать `frontend/src/components/PageTitle.tsx`:
    ```tsx
    import styles from './PageTitle.module.css';

    export interface PageTitleProps {
      title: string;
      subtitle?: string;
    }

    export function PageTitle({ title, subtitle }: PageTitleProps) {
      return (
        <div className={styles.root}>
          <h1 className={styles.title}>{title}</h1>
          {subtitle && <p className={styles.subtitle}>{subtitle}</p>}
        </div>
      );
    }
    ```

    Создать `frontend/src/components/PageTitle.module.css`:
    ```css
    .root {
      padding: 16px 16px 8px;
    }

    .title {
      font-size: 22px;
      font-weight: var(--weight-bold);
      color: var(--color-text);
      margin: 0;
      line-height: 1.2;
    }

    .subtitle {
      font-size: 13px;
      color: var(--color-text-muted);
      margin: 4px 0 0;
    }
    ```
  </action>
  <acceptance_criteria>
    - `test -f frontend/src/components/SubTabBar.tsx` возвращает 0 (файл существует)
    - `test -f frontend/src/components/PageTitle.tsx` возвращает 0
    - `grep -c "export type SubTabId" frontend/src/components/SubTabBar.tsx` возвращает 1
    - `grep -c "export function SubTabBar" frontend/src/components/SubTabBar.tsx` возвращает 1
    - `grep -c "export function PageTitle" frontend/src/components/PageTitle.tsx` возвращает 1
    - `grep -c "position: sticky" frontend/src/components/SubTabBar.module.css` возвращает 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>4 новых файла созданы; SubTabBar экспортирует SubTabId тип и generic SubTabBarProps; PageTitle рендерит h1 с optional subtitle; TypeScript компилируется без ошибок.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → App state | BottomNav вызывает onTabChange, App управляет routing — нет прямого доступа к данным |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Spoofing | BottomNav | accept | Навигация — клиентский UI без аутентификации; auth на API-уровне |
| T-07-03 | Information Disclosure | SubTabBar | accept | Не передаёт данные, только UI-state |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — 0 ошибок
- BottomNav.tsx: 5 табов, TabId тип с 5 значениями
- BottomNav.module.css: .ai.active { color: #a78bfa }
- SubTabBar.tsx: generic компонент с SubTabId
- PageTitle.tsx: h1 + optional subtitle
</verification>

<success_criteria>
- 6 файлов созданы/изменены (3 .tsx + 3 .module.css)
- TypeScript компилируется чисто
- Все 3 компонента экспортируют правильные типы для Plan 03/04/05
</success_criteria>

<output>
После завершения создать `.planning/phases/07-nav-refactor/07-02-SUMMARY.md`
</output>
