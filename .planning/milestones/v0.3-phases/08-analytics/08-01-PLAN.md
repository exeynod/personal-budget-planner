---
phase: 08-analytics
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_analytics.py
autonomous: true
requirements:
  - ANL-07
  - ANL-08

must_haves:
  truths:
    - "Pytest-тесты для 4 analytics endpoints написаны и ПАДАЮТ (RED) до реализации бэкенда"
    - "Тест проверяет GET /api/v1/analytics/trend?range=3M → 200 c {points: [...]}"
    - "Тест проверяет GET /api/v1/analytics/top-overspend?range=1M → 200 c {items: [...]}"
    - "Тест проверяет GET /api/v1/analytics/top-categories?range=3M → 200 c {items: [...]}"
    - "Тест проверяет GET /api/v1/analytics/forecast → 200 c {insufficient_data, projected_end_balance_cents, will_burn_cents}"
    - "Все 4 endpoints возвращают 403 без X-Telegram-Init-Data"
  artifacts:
    - path: "tests/test_analytics.py"
      provides: "Contract tests для Phase 8 analytics endpoints"
      contains: "test_trend_returns_200"
  key_links:
    - from: "tests/test_analytics.py"
      to: "/api/v1/analytics/trend"
      via: "async_client.get"
      pattern: "analytics/trend"
---

<objective>
Wave 0: Написать RED contract-тесты для 4 analytics endpoints ДО реализации бэкенда.

Purpose: TDD-gate — тесты должны ПАДАТЬ с 404 на текущем коде (routes ещё не созданы) и ПРОХОДИТЬ только после выполнения Plan 08-02.

Output: tests/test_analytics.py с contract-тестами для всех 4 endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-analytics/08-CONTEXT.md

<interfaces>
<!-- Паттерн из tests/test_subscriptions.py (аналог) -->
```python
# _require_db() скипает тест если DATABASE_URL не задан
def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")

# auth_headers fixture
@pytest.fixture
def auth_headers(bot_token, owner_tg_id):
    from tests.conftest import make_init_data
    return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}

# async_client из conftest.py уже настроен
# Паттерн вызова: response = await async_client.get("/api/v1/...", headers=auth_headers)
```

<!-- Целевые ответы analytics endpoints (после реализации Plan 08-02) -->
<!-- GET /api/v1/analytics/trend?range=3M -->
{
  "points": [
    {"period_label": "Апр", "expense_cents": 150000, "income_cents": 200000},
    ...
  ]
}

<!-- GET /api/v1/analytics/top-overspend?range=1M -->
{
  "items": [
    {"category_id": 1, "name": "Кафе", "planned_cents": 10000, "actual_cents": 15000, "overspend_pct": 150.0},
    ...
  ]
}

<!-- GET /api/v1/analytics/top-categories?range=3M -->
{
  "items": [
    {"category_id": 1, "name": "Кафе", "actual_cents": 45000, "planned_cents": 30000},
    ...
  ]
}

<!-- GET /api/v1/analytics/forecast -->
{
  "insufficient_data": false,
  "current_balance_cents": 50000,
  "projected_end_balance_cents": 10000,
  "will_burn_cents": 40000,
  "period_end": "2026-05-31"
}
<!-- или если days_elapsed=0: {"insufficient_data": true, "projected_end_balance_cents": null, "will_burn_cents": null, "period_end": null} -->
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Написать RED contract-тесты tests/test_analytics.py</name>
  <read_first>
    - tests/test_subscriptions.py (паттерн: _require_db, auth_headers fixture, db_setup, async_client.get)
    - tests/conftest.py (make_init_data, bot_token, owner_tg_id, async_client fixtures)
    - .planning/phases/08-analytics/08-CONTEXT.md (locked decisions: response shapes, field names)
    - .planning/phases/08-analytics/08-PATTERNS.md (раздел "app/api/schemas/analytics.py" — точные имена полей)
  </read_first>
  <files>tests/test_analytics.py</files>
  <behavior>
    Тесты ПАДАЮТ с 404 до реализации (routes не созданы) и ПРОХОДЯТ после Plan 08-02.

    anl-auth-01: trend 403 без auth
    - GET /api/v1/analytics/trend?range=1M без заголовков → 403

    anl-auth-02: top-overspend 403 без auth
    - GET /api/v1/analytics/top-overspend?range=1M без заголовков → 403

    anl-auth-03: top-categories 403 без auth
    - GET /api/v1/analytics/top-categories?range=1M без заголовков → 403

    anl-auth-04: forecast 403 без auth
    - GET /api/v1/analytics/forecast без заголовков → 403

    anl-trend-01: trend 200 с range=1M возвращает points список
    - GET /api/v1/analytics/trend?range=1M с auth → 200
    - response.json() содержит ключ "points"
    - "points" — список (list)

    anl-trend-02: trend 200 с range=3M
    - GET /api/v1/analytics/trend?range=3M с auth → 200
    - response.json()["points"] — список

    anl-trend-03: trend point shape
    - Если points непуст — первый элемент содержит ключи "period_label", "expense_cents", "income_cents"
    - "expense_cents" — int (не float)

    anl-overspend-01: top-overspend 200 с range=1M
    - GET /api/v1/analytics/top-overspend?range=1M с auth → 200
    - response.json() содержит ключ "items"
    - "items" — список

    anl-overspend-02: overspend item shape
    - Если items непуст — первый элемент содержит ключи "category_id", "name", "planned_cents", "actual_cents", "overspend_pct"

    anl-topcat-01: top-categories 200 с range=3M
    - GET /api/v1/analytics/top-categories?range=3M с auth → 200
    - response.json() содержит ключ "items"

    anl-topcat-02: top-categories item shape
    - Если items непуст — первый элемент содержит ключи "category_id", "name", "actual_cents", "planned_cents"

    anl-forecast-01: forecast 200
    - GET /api/v1/analytics/forecast с auth → 200
    - response.json() содержит ключи "insufficient_data", "current_balance_cents"

    anl-forecast-02: forecast edge-case shape
    - response.json()["insufficient_data"] — bool
    - Если insufficient_data=True: "projected_end_balance_cents" is None или отсутствует
  </behavior>
  <action>
    Создать `tests/test_analytics.py`.

    Структура файла:
    ```python
    """Contract tests for Phase 8 Analytics endpoints.

    RED gate: routes /api/v1/analytics/* not yet implemented.
    Tests FAIL with 404 until Plan 08-02 creates them.

    Covered:
    - 403 without auth on all 4 endpoints
    - GET /api/v1/analytics/trend?range= → 200, {points: [...]}
    - GET /api/v1/analytics/top-overspend?range= → 200, {items: [...]}
    - GET /api/v1/analytics/top-categories?range= → 200, {items: [...]}
    - GET /api/v1/analytics/forecast → 200, {insufficient_data, current_balance_cents, ...}
    """
    import os
    import pytest

    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set — skipping DB-backed test")

    @pytest.fixture
    def auth_headers(bot_token, owner_tg_id):
        from tests.conftest import make_init_data
        return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}

    # --- 403 auth tests (do NOT require DB — routes will 403 before DB) ---

    @pytest.mark.asyncio
    async def test_trend_requires_auth(async_client):
        response = await async_client.get("/api/v1/analytics/trend?range=1M")
        assert response.status_code == 403

    @pytest.mark.asyncio
    async def test_top_overspend_requires_auth(async_client):
        response = await async_client.get("/api/v1/analytics/top-overspend?range=1M")
        assert response.status_code == 403

    @pytest.mark.asyncio
    async def test_top_categories_requires_auth(async_client):
        response = await async_client.get("/api/v1/analytics/top-categories?range=1M")
        assert response.status_code == 403

    @pytest.mark.asyncio
    async def test_forecast_requires_auth(async_client):
        response = await async_client.get("/api/v1/analytics/forecast")
        assert response.status_code == 403

    # --- 200 contract tests (require DB for real response) ---

    @pytest.mark.asyncio
    async def test_trend_range_1m_returns_200(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/trend?range=1M", headers=auth_headers)
        assert response.status_code == 200
        data = response.json()
        assert "points" in data
        assert isinstance(data["points"], list)

    @pytest.mark.asyncio
    async def test_trend_range_3m_returns_200(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/trend?range=3M", headers=auth_headers)
        assert response.status_code == 200
        data = response.json()
        assert isinstance(data["points"], list)

    @pytest.mark.asyncio
    async def test_trend_point_shape(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/trend?range=6M", headers=auth_headers)
        assert response.status_code == 200
        points = response.json()["points"]
        if points:
            p = points[0]
            assert "period_label" in p
            assert "expense_cents" in p
            assert "income_cents" in p
            assert isinstance(p["expense_cents"], int)

    @pytest.mark.asyncio
    async def test_top_overspend_returns_200(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/top-overspend?range=1M", headers=auth_headers)
        assert response.status_code == 200
        data = response.json()
        assert "items" in data
        assert isinstance(data["items"], list)

    @pytest.mark.asyncio
    async def test_top_overspend_item_shape(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/top-overspend?range=1M", headers=auth_headers)
        assert response.status_code == 200
        items = response.json()["items"]
        if items:
            item = items[0]
            for key in ("category_id", "name", "planned_cents", "actual_cents", "overspend_pct"):
                assert key in item, f"Missing key: {key}"

    @pytest.mark.asyncio
    async def test_top_categories_returns_200(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/top-categories?range=3M", headers=auth_headers)
        assert response.status_code == 200
        data = response.json()
        assert "items" in data
        assert isinstance(data["items"], list)

    @pytest.mark.asyncio
    async def test_top_categories_item_shape(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/top-categories?range=3M", headers=auth_headers)
        assert response.status_code == 200
        items = response.json()["items"]
        if items:
            item = items[0]
            for key in ("category_id", "name", "actual_cents", "planned_cents"):
                assert key in item, f"Missing key: {key}"

    @pytest.mark.asyncio
    async def test_forecast_returns_200(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/forecast", headers=auth_headers)
        assert response.status_code == 200
        data = response.json()
        assert "insufficient_data" in data
        assert "current_balance_cents" in data
        assert isinstance(data["insufficient_data"], bool)

    @pytest.mark.asyncio
    async def test_forecast_insufficient_data_null_fields(async_client, auth_headers):
        _require_db()
        response = await async_client.get("/api/v1/analytics/forecast", headers=auth_headers)
        assert response.status_code == 200
        data = response.json()
        if data["insufficient_data"]:
            assert data.get("projected_end_balance_cents") is None
            assert data.get("will_burn_cents") is None
    ```

    После написания запустить:
    `cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_analytics.py::test_trend_requires_auth tests/test_analytics.py::test_forecast_requires_auth -x --tb=short 2>&1 | tail -20`

    Убедиться: тесты auth (без DB) FAILED с assert 403 != 404 — это RED (не ERROR). Если видим синтаксическую ошибку — исправить до чистого RED.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_analytics.py::test_trend_requires_auth tests/test_analytics.py::test_top_overspend_requires_auth tests/test_analytics.py::test_forecast_requires_auth -v --tb=short 2>&1 | grep -E "(PASSED|FAILED|ERROR|error)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/test_analytics.py` — файл существует
    - `grep -c "def test_" tests/test_analytics.py` возвращает >= 12
    - `grep -c "analytics/trend" tests/test_analytics.py` возвращает >= 2
    - `grep -c "analytics/forecast" tests/test_analytics.py` возвращает >= 2
    - `python -m pytest tests/test_analytics.py::test_trend_requires_auth --tb=short 2>&1 | grep -E "FAILED|AssertionError"` — тест FAILED (не ERROR), что подтверждает RED-состояние (routes не созданы → 404, а тест ожидает 403)
    - Нет SyntaxError / ImportError в выводе pytest
  </acceptance_criteria>
  <done>tests/test_analytics.py существует; 12+ тестов; тесты запускаются без синтаксических ошибок; auth-тесты FAILED (RED) — 404 != 403 до реализации routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → API | Pytest тесты обращаются к тестовому async_client, не к production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | test auth_headers | accept | make_init_data генерирует валидный HMAC для bot_token из conftest; production-токен в env |
| T-08-02 | Information Disclosure | test_analytics.py | accept | Тестовый файл, не деплоится; БД-тесты скипаются без DATABASE_URL |
</threat_model>

<verification>
- tests/test_analytics.py существует
- Файл содержит 12+ функций test_*
- Тесты запускаются без синтаксических ошибок (pytest --collect-only не показывает ERROR)
- Auth-тесты (без DB) FAILED: 404 != 403 — это ожидаемое RED-состояние
</verification>

<success_criteria>
- tests/test_analytics.py содержит contract-тесты для ANL-07 (4 endpoints × auth + shape)
- Тесты RED (FAILED, не ERROR) до реализации Plan 08-02
- После Plan 08-02 все тесты с DATABASE_URL должны PASS
</success_criteria>

<output>
После завершения создать `.planning/phases/08-analytics/08-01-SUMMARY.md`
</output>
