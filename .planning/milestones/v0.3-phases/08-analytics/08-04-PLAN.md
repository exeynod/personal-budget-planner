---
phase: 08-analytics
plan: 04
type: execute
wave: 3
depends_on:
  - 08-03
files_modified:
  - frontend/src/styles/tokens.css
  - frontend/src/screens/AnalyticsScreen.tsx
  - frontend/src/screens/AnalyticsScreen.module.css
  - frontend/src/components/ForecastCard.tsx
  - frontend/src/components/ForecastCard.module.css
  - frontend/src/components/TopOverspendList.tsx
  - frontend/src/components/TopOverspendList.module.css
  - frontend/src/components/LineChart.tsx
  - frontend/src/components/LineChart.module.css
  - frontend/src/components/HorizontalBars.tsx
  - frontend/src/components/HorizontalBars.module.css
autonomous: true
requirements:
  - ANL-01
  - ANL-02
  - ANL-03
  - ANL-04
  - ANL-05
  - ANL-06

must_haves:
  truths:
    - "AnalyticsScreen заменён — больше не показывает placeholder 'Скоро будет'"
    - "Period chips 1M/3M/6M/12M видны и переключают range (ANL-01)"
    - "Forecast card отображает projected_end_balance_cents с зелёным/красным цветом (ANL-02)"
    - "При insufficient_data=true Forecast card показывает 'Недостаточно данных' (ANL-02)"
    - "TopOverspendList показывает категории с левым бордером danger/warn (ANL-03)"
    - "LineChart рисует SVG polyline/bezier с gradient fill под кривой (ANL-04)"
    - "HorizontalBars показывает top-5 категорий с цветными барами (ANL-05)"
    - "Skeleton placeholders показываются при loading=true (ANL-06)"
    - "Empty state 'Нет данных за период' с иконкой ChartLine если все блоки пусты (ANL-06)"
    - "CSS vars --chart-1..--chart-6 добавлены в tokens.css"
    - "TypeScript компилируется без ошибок"
    - "Vite build проходит без ошибок"
  artifacts:
    - path: "frontend/src/styles/tokens.css"
      provides: "CSS vars --chart-1 через --chart-6"
      contains: "--chart-1: #4ea4ff"
    - path: "frontend/src/screens/AnalyticsScreen.tsx"
      provides: "Полный экран аналитики с 4 блоками"
      contains: "useAnalytics"
    - path: "frontend/src/components/ForecastCard.tsx"
      provides: "Forecast card компонент"
      contains: "insufficient_data"
    - path: "frontend/src/components/LineChart.tsx"
      provides: "SVG line chart с bezier и gradient fill"
      contains: "linearGradient"
    - path: "frontend/src/components/HorizontalBars.tsx"
      provides: "Horizontal bars для top categories"
      contains: "barFill"
    - path: "frontend/src/components/TopOverspendList.tsx"
      provides: "Top overspend list с left border"
      contains: "overspend_pct"
  key_links:
    - from: "frontend/src/screens/AnalyticsScreen.tsx"
      to: "frontend/src/hooks/useAnalytics.ts"
      via: "useAnalytics(range)"
      pattern: "useAnalytics"
    - from: "frontend/src/screens/AnalyticsScreen.tsx"
      to: "frontend/src/components/ForecastCard.tsx"
      via: "import { ForecastCard }"
      pattern: "ForecastCard"
    - from: "frontend/src/components/LineChart.tsx"
      to: "SVG path bezier"
      via: "SVG C path commands"
      pattern: "linearGradient"
---

<objective>
Wave 3: Реализовать полный AnalyticsScreen с 4 chart-компонентами и CSS-стилями.

Purpose: Заменить placeholder экран на рабочую аналитику. Реализовать самописные SVG-чарты без внешних chart-libs (per CONTEXT.md D-locked decision).

Output: AnalyticsScreen.tsx (полный), AnalyticsScreen.module.css, ForecastCard, TopOverspendList, LineChart, HorizontalBars компоненты + их CSS modules, chart palette в tokens.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-analytics/08-CONTEXT.md
@.planning/phases/08-analytics/08-PATTERNS.md

<interfaces>
<!-- useAnalytics возвращает (из Plan 08-03): -->
```typescript
interface UseAnalyticsResult {
  trend: TrendResponse | null;          // { points: TrendPoint[] }
  topOverspend: TopOverspendResponse | null;  // { items: OverspendItem[] }
  topCategories: TopCategoriesResponse | null; // { items: TopCategoryItem[] }
  forecast: ForecastResponse | null;
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}
// range: AnalyticsRange = '1M' | '3M' | '6M' | '12M'
```

<!-- TrendPoint: { period_label: string; expense_cents: number; income_cents: number } -->
<!-- OverspendItem: { category_id, name, planned_cents, actual_cents, overspend_pct } -->
<!-- TopCategoryItem: { category_id, name, actual_cents, planned_cents } -->
<!-- ForecastResponse: { insufficient_data, current_balance_cents, projected_end_balance_cents|null, will_burn_cents|null, period_end|null } -->

<!-- formatKopecks из frontend/src/utils/format.ts -->
<!-- PageTitle из frontend/src/components/PageTitle.tsx — prop: title: string -->
<!-- Phosphor: import { ChartLine, Warning } from '@phosphor-icons/react' -->

<!-- tokens.css CSS vars (из существующего файла): -->
<!-- Spacing: --space-2, --space-3, --space-4, --space-12 -->
<!-- Colors: --color-bg, --color-surface, --color-text, --color-text-muted, --color-border, --color-primary, --color-danger, --color-warn -->
<!-- Typography: --text-sm, --text-base, --weight-medium, --weight-semibold, --font-sans -->
<!-- Border radius: --radius-md, --radius-full -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить chart palette в tokens.css и создать 4 chart компонента</name>
  <read_first>
    - frontend/src/styles/tokens.css (полностью — найти :root блок для добавления --chart-1..6)
    - frontend/src/components/DashboardCategoryRow.tsx (паттерн CSS module + formatKopecks usage)
    - frontend/src/components/DashboardCategoryRow.module.css (паттерн .barTrack, .barFill, .row)
    - frontend/src/components/HeroCard.tsx (паттерн surface card layout)
    - frontend/src/utils/format.ts (экспорты: formatKopecks, formatKopecksWithCurrency — проверить точные имена)
    - .planning/phases/08-analytics/08-PATTERNS.md (разделы ForecastCard, TopOverspendList, LineChart, HorizontalBars)
  </read_first>
  <files>
    frontend/src/styles/tokens.css
    frontend/src/components/ForecastCard.tsx
    frontend/src/components/ForecastCard.module.css
    frontend/src/components/TopOverspendList.tsx
    frontend/src/components/TopOverspendList.module.css
    frontend/src/components/LineChart.tsx
    frontend/src/components/LineChart.module.css
    frontend/src/components/HorizontalBars.tsx
    frontend/src/components/HorizontalBars.module.css
  </files>
  <action>
    **1. tokens.css** — добавить в :root блок (перед закрывающей `}`) chart palette:
    ```css
      /* Chart palette — Phase 8 Analytics (ANL-07) */
      --chart-1: #4ea4ff;
      --chart-2: #2ecc71;
      --chart-3: #ffd166;
      --chart-4: #ff5d5d;
      --chart-5: #a78bfa;
      --chart-6: #38bdf8;
    ```

    **2. ForecastCard.tsx** — создать:
    ```tsx
    import type { ForecastResponse } from '../api/types';
    import { formatKopecks } from '../utils/format';
    import { Warning } from '@phosphor-icons/react';
    import styles from './ForecastCard.module.css';

    interface ForecastCardProps {
      forecast: ForecastResponse;
    }

    export function ForecastCard({ forecast }: ForecastCardProps) {
      const projected = forecast.projected_end_balance_cents;
      const isNegative = projected !== null && projected < 0;
      const valueCls = isNegative ? styles.valueDanger : styles.valueSuccess;

      return (
        <div className={styles.card}>
          <div className={styles.label}>Прогноз на конец периода</div>
          {forecast.insufficient_data ? (
            <div className={styles.noData}>Недостаточно данных</div>
          ) : (
            <>
              <div className={`${styles.value} ${valueCls}`}>
                {isNegative && <Warning weight="fill" size={18} />}
                {projected !== null ? `${formatKopecks(projected)} ₽` : '—'}
              </div>
              {forecast.will_burn_cents !== null && (
                <div className={styles.sub}>
                  Сгорит {formatKopecks(forecast.will_burn_cents)} ₽
                </div>
              )}
            </>
          )}
        </div>
      );
    }
    ```

    **3. ForecastCard.module.css** — создать:
    ```css
    .card {
      background: var(--color-surface);
      border-radius: var(--radius-md);
      padding: var(--space-4);
    }
    .label {
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      font-weight: var(--weight-medium);
      margin-bottom: var(--space-2);
    }
    .value {
      font-size: 1.5rem;
      font-weight: var(--weight-semibold);
      display: flex;
      align-items: center;
      gap: var(--space-2);
    }
    .valueDanger { color: var(--color-danger); }
    .valueSuccess { color: var(--color-primary); }
    .noData {
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      font-style: italic;
    }
    .sub {
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      margin-top: var(--space-2);
    }
    ```

    **4. TopOverspendList.tsx** — создать:
    ```tsx
    import type { OverspendItem } from '../api/types';
    import { formatKopecks } from '../utils/format';
    import styles from './TopOverspendList.module.css';

    interface TopOverspendListProps {
      items: OverspendItem[];
    }

    export function TopOverspendList({ items }: TopOverspendListProps) {
      return (
        <div className={styles.list}>
          {items.map((item) => {
            const borderCls =
              item.overspend_pct > 100
                ? styles.borderDanger
                : item.overspend_pct >= 80
                  ? styles.borderWarn
                  : styles.borderNeutral;
            return (
              <div key={item.category_id} className={`${styles.row} ${borderCls}`}>
                <div className={styles.rowTop}>
                  <span className={styles.name}>{item.name}</span>
                  <span className={styles.pct}>{Math.round(item.overspend_pct)}%</span>
                </div>
                <div className={styles.rowSub}>
                  <span className={styles.planned}>план {formatKopecks(item.planned_cents)} ₽</span>
                  <span className={styles.actual}>факт {formatKopecks(item.actual_cents)} ₽</span>
                </div>
              </div>
            );
          })}
        </div>
      );
    }
    ```

    **5. TopOverspendList.module.css** — создать:
    ```css
    .list { display: flex; flex-direction: column; gap: var(--space-2); }
    .row {
      padding: var(--space-3);
      background: var(--color-surface);
      border-radius: var(--radius-md);
      border-left: 4px solid transparent;
    }
    .borderDanger { border-left-color: var(--color-danger); }
    .borderWarn { border-left-color: var(--color-warn); }
    .borderNeutral { border-left-color: var(--color-border); }
    .rowTop { display: flex; justify-content: space-between; align-items: center; margin-bottom: 4px; }
    .name { font-size: var(--text-sm); font-weight: var(--weight-medium); color: var(--color-text); }
    .pct { font-size: var(--text-sm); font-weight: var(--weight-semibold); color: var(--color-text-muted); }
    .rowSub { display: flex; gap: var(--space-3); }
    .planned, .actual { font-size: 11px; color: var(--color-text-muted); }
    ```

    **6. LineChart.tsx** — создать SVG line chart с cubic bezier:
    ```tsx
    import type { TrendPoint } from '../api/types';
    import { formatKopecks } from '../utils/format';
    import styles from './LineChart.module.css';

    interface LineChartProps {
      points: TrendPoint[];
    }

    const PAD_LEFT = 48;
    const PAD_RIGHT = 12;
    const PAD_TOP = 12;
    const PAD_BOTTOM = 28;
    const W = 320;
    const H = 160;
    const CHART_W = W - PAD_LEFT - PAD_RIGHT;
    const CHART_H = H - PAD_TOP - PAD_BOTTOM;

    function cubicBezierPath(pts: Array<[number, number]>): string {
      if (pts.length < 2) return '';
      let d = `M ${pts[0][0]} ${pts[0][1]}`;
      for (let i = 1; i < pts.length; i++) {
        const prev = pts[i - 1];
        const curr = pts[i];
        const cpx = (prev[0] + curr[0]) / 2;
        d += ` C ${cpx} ${prev[1]}, ${cpx} ${curr[1]}, ${curr[0]} ${curr[1]}`;
      }
      return d;
    }

    export function LineChart({ points }: LineChartProps) {
      if (points.length < 2) return null;

      const expenses = points.map((p) => p.expense_cents);
      const maxVal = Math.max(...expenses, 1);
      // Round up to next 5000 * 100 kopecks tick
      const TICK_STEP = 500_000; // 5000 roubles in kopecks
      const maxTick = Math.ceil(maxVal / TICK_STEP) * TICK_STEP;
      const tickCount = Math.min(Math.ceil(maxTick / TICK_STEP), 5);

      const xPos = (i: number) => PAD_LEFT + (i / (points.length - 1)) * CHART_W;
      const yPos = (v: number) => PAD_TOP + CHART_H - (v / maxTick) * CHART_H;

      const coords: Array<[number, number]> = points.map((p, i) => [xPos(i), yPos(p.expense_cents)]);
      const linePath = cubicBezierPath(coords);

      // Area fill: line path + right edge down + baseline left
      const lastX = coords[coords.length - 1][0];
      const baseY = PAD_TOP + CHART_H;
      const areaPath = `${linePath} L ${lastX} ${baseY} L ${PAD_LEFT} ${baseY} Z`;

      // Y-axis ticks
      const yTicks = Array.from({ length: tickCount + 1 }, (_, i) => i * TICK_STEP);

      return (
        <div className={styles.wrapper}>
          <svg viewBox={`0 0 ${W} ${H}`} className={styles.chart} aria-hidden>
            <defs>
              <linearGradient id="lineChartFill" x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stopColor="var(--color-primary)" stopOpacity="0.15" />
                <stop offset="100%" stopColor="var(--color-primary)" stopOpacity="0" />
              </linearGradient>
            </defs>
            {/* Y-axis grid lines and labels */}
            {yTicks.map((tick) => {
              const y = yPos(tick);
              return (
                <g key={tick}>
                  <line x1={PAD_LEFT} y1={y} x2={W - PAD_RIGHT} y2={y}
                    stroke="var(--color-border)" strokeWidth="0.5" />
                  <text x={PAD_LEFT - 4} y={y + 3} className={styles.yLabel}>
                    {tick > 0 ? `${formatKopecks(tick)}` : '0'}
                  </text>
                </g>
              );
            })}
            {/* Area fill */}
            <path d={areaPath} fill="url(#lineChartFill)" />
            {/* Line */}
            <path d={linePath} fill="none" stroke="var(--color-primary)" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
            {/* Data point dots */}
            {coords.map(([x, y], i) => (
              <circle key={i} cx={x} cy={y} r="3" fill="var(--color-primary)" />
            ))}
            {/* X-axis labels */}
            {points.map((p, i) => (
              <text key={i} x={xPos(i)} y={H - 6} className={styles.xLabel}>
                {p.period_label}
              </text>
            ))}
          </svg>
        </div>
      );
    }
    ```

    **7. LineChart.module.css** — создать:
    ```css
    .wrapper { overflow-x: auto; }
    .chart { width: 100%; height: auto; display: block; min-width: 240px; }
    .xLabel {
      font-family: var(--font-sans);
      font-size: 10px;
      fill: var(--color-text-muted);
      text-anchor: middle;
    }
    .yLabel {
      font-family: var(--font-sans);
      font-size: 9px;
      fill: var(--color-text-muted);
      text-anchor: end;
    }
    ```

    **8. HorizontalBars.tsx** — создать:
    ```tsx
    import type { TopCategoryItem } from '../api/types';
    import { formatKopecks } from '../utils/format';
    import styles from './HorizontalBars.module.css';

    const CHART_COLORS = [
      'var(--chart-1)',
      'var(--chart-2)',
      'var(--chart-3)',
      'var(--chart-4)',
      'var(--chart-5)',
    ] as const;

    interface HorizontalBarsProps {
      items: TopCategoryItem[];
    }

    export function HorizontalBars({ items }: HorizontalBarsProps) {
      const maxCents = Math.max(...items.map((i) => i.actual_cents), 1);
      return (
        <div className={styles.list}>
          {items.map((item, idx) => {
            const fillPct = (item.actual_cents / maxCents) * 100;
            const color = CHART_COLORS[idx % CHART_COLORS.length];
            return (
              <div key={item.category_id} className={styles.row}>
                <div className={styles.rowTop}>
                  <span className={styles.label}>{item.name}</span>
                  <span className={styles.amount}>{formatKopecks(item.actual_cents)} ₽</span>
                </div>
                <div className={styles.barTrack}>
                  <div
                    className={styles.barFill}
                    style={{ width: `${fillPct}%`, background: color }}
                  />
                </div>
              </div>
            );
          })}
        </div>
      );
    }
    ```

    **9. HorizontalBars.module.css** — создать:
    ```css
    .list { display: flex; flex-direction: column; gap: var(--space-3); }
    .row {}
    .rowTop { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 6px; }
    .label { font-size: var(--text-sm); font-weight: var(--weight-medium); color: var(--color-text); }
    .amount { font-size: var(--text-sm); color: var(--color-text-muted); }
    .barTrack {
      height: 6px;
      border-radius: 3px;
      background: var(--color-border);
      overflow: hidden;
    }
    .barFill {
      height: 100%;
      border-radius: 3px;
      /* NO transition — static per CONTEXT.md decision */
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\-\-chart-1: #4ea4ff" frontend/src/styles/tokens.css` возвращает 1
    - `grep -c "\-\-chart-6" frontend/src/styles/tokens.css` возвращает 1
    - `grep -c "insufficient_data" frontend/src/components/ForecastCard.tsx` возвращает >= 2
    - `grep -c "valueDanger" frontend/src/components/ForecastCard.tsx` возвращает >= 1
    - `grep -c "overspend_pct" frontend/src/components/TopOverspendList.tsx` возвращает >= 1
    - `grep -c "borderDanger" frontend/src/components/TopOverspendList.tsx` возвращает >= 1
    - `grep -c "linearGradient" frontend/src/components/LineChart.tsx` возвращает 1
    - `grep -c "cubicBezierPath\|strokeLinecap" frontend/src/components/LineChart.tsx` возвращает >= 1
    - `grep -c "barFill" frontend/src/components/HorizontalBars.tsx` возвращает >= 1
    - `grep -c "CHART_COLORS" frontend/src/components/HorizontalBars.tsx` возвращает >= 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <done>Chart palette в tokens.css; 4 компонента созданы; TypeScript компилируется без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Реализовать AnalyticsScreen.tsx и AnalyticsScreen.module.css</name>
  <read_first>
    - frontend/src/screens/AnalyticsScreen.tsx (текущий placeholder — прочитать чтобы увидеть что заменяется)
    - frontend/src/screens/HomeScreen.tsx (паттерн: loading guard, error display, empty state)
    - frontend/src/screens/HomeScreen.module.css (паттерн: .root, .muted, .error, .emptyState)
    - frontend/src/components/PageTitle.tsx (prop interface)
    - .planning/phases/08-analytics/08-PATTERNS.md (раздел "AnalyticsScreen.tsx" и "AnalyticsScreen.module.css")
  </read_first>
  <files>
    frontend/src/screens/AnalyticsScreen.tsx
    frontend/src/screens/AnalyticsScreen.module.css
  </files>
  <action>
    **AnalyticsScreen.tsx** — полная замена placeholder:
    ```tsx
    import { useState } from 'react';
    import { ChartLine } from '@phosphor-icons/react';
    import { PageTitle } from '../components/PageTitle';
    import { ForecastCard } from '../components/ForecastCard';
    import { TopOverspendList } from '../components/TopOverspendList';
    import { LineChart } from '../components/LineChart';
    import { HorizontalBars } from '../components/HorizontalBars';
    import { useAnalytics } from '../hooks/useAnalytics';
    import type { AnalyticsRange } from '../api/analytics';
    import styles from './AnalyticsScreen.module.css';

    const RANGES: AnalyticsRange[] = ['1M', '3M', '6M', '12M'];

    export function AnalyticsScreen() {
      const [range, setRange] = useState<AnalyticsRange>('1M');
      const { trend, topOverspend, topCategories, forecast, loading, error } = useAnalytics(range);

      const hasTrend = (trend?.points.length ?? 0) > 1;
      const hasOverspend = (topOverspend?.items.length ?? 0) > 0;
      const hasTopCat = (topCategories?.items.length ?? 0) > 0;
      const hasForecast = forecast !== null && !forecast.insufficient_data;
      const hasInsufficient = forecast?.insufficient_data === true;
      const allEmpty = !loading && !error && !hasTrend && !hasOverspend && !hasTopCat && !hasForecast && !hasInsufficient;

      return (
        <div className={styles.root}>
          <PageTitle title="Аналитика" />

          {/* Period chips */}
          <div className={styles.chips}>
            {RANGES.map((r) => (
              <button
                key={r}
                type="button"
                className={range === r ? styles.chipActive : styles.chip}
                onClick={() => setRange(r)}
                aria-pressed={range === r}
              >
                {r}
              </button>
            ))}
          </div>

          {/* Loading skeletons */}
          {loading && (
            <div className={styles.skeletons}>
              <div className={`${styles.skeleton} ${styles.skeletonCard}`} />
              <div className={`${styles.skeleton} ${styles.skeletonList}`} />
              <div className={`${styles.skeleton} ${styles.skeletonChart}`} />
              <div className={`${styles.skeleton} ${styles.skeletonList}`} />
            </div>
          )}

          {/* Error state */}
          {error && !loading && (
            <div className={styles.error}>
              Не удалось загрузить данные. Попробуй ещё раз.
            </div>
          )}

          {/* Content blocks */}
          {!loading && !error && (
            <>
              {/* Forecast */}
              {forecast && (
                <div className={styles.section}>
                  <div className={styles.sectionTitle}>Прогноз</div>
                  <ForecastCard forecast={forecast} />
                </div>
              )}

              {/* Top overspend */}
              {hasOverspend && (
                <div className={styles.section}>
                  <div className={styles.sectionTitle}>Топ перерасходов</div>
                  <TopOverspendList items={topOverspend!.items} />
                </div>
              )}

              {/* Trend line chart */}
              {hasTrend && (
                <div className={styles.section}>
                  <div className={styles.sectionTitle}>Тренд расходов</div>
                  <LineChart points={trend!.points} />
                </div>
              )}

              {/* Top categories */}
              {hasTopCat && (
                <div className={styles.section}>
                  <div className={styles.sectionTitle}>Топ категорий</div>
                  <HorizontalBars items={topCategories!.items} />
                </div>
              )}

              {/* Global empty state */}
              {allEmpty && (
                <div className={styles.emptyState}>
                  <ChartLine size={48} weight="thin" color="var(--color-text-muted)" />
                  <div className={styles.emptyHeading}>Нет данных за период</div>
                  <div className={styles.emptyBody}>
                    Добавь транзакции, чтобы увидеть аналитику
                  </div>
                </div>
              )}
            </>
          )}
        </div>
      );
    }
    ```

    **AnalyticsScreen.module.css** — создать:
    ```css
    .root {
      padding: var(--space-4) var(--space-4) var(--space-12);
      min-height: 100dvh;
      background: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-sans);
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }

    /* Period chips */
    .chips {
      display: flex;
      gap: var(--space-2);
      overflow-x: auto;
      scrollbar-width: none;
      padding-bottom: 2px;
    }
    .chips::-webkit-scrollbar { display: none; }
    .chip,
    .chipActive {
      flex-shrink: 0;
      height: 32px;
      padding: 0 var(--space-3);
      border-radius: var(--radius-full);
      font-family: inherit;
      font-size: var(--text-sm);
      font-weight: var(--weight-medium);
      cursor: pointer;
      border: 1px solid var(--color-border);
    }
    .chip {
      background: transparent;
      color: var(--color-text-muted);
    }
    .chipActive {
      background: var(--color-primary);
      color: #fff;
      border-color: var(--color-primary);
    }

    /* Sections */
    .section {
      background: var(--color-surface);
      border-radius: var(--radius-md);
      padding: var(--space-4);
    }
    .sectionTitle {
      font-size: var(--text-sm);
      font-weight: var(--weight-semibold);
      color: var(--color-text-muted);
      text-transform: uppercase;
      letter-spacing: 0.04em;
      margin-bottom: var(--space-3);
    }

    /* Loading skeletons */
    .skeletons {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
    }
    .skeleton {
      border-radius: var(--radius-md);
      background: var(--color-surface);
      animation: shimmer 1.4s infinite;
    }
    .skeletonCard { height: 80px; }
    .skeletonList { height: 120px; }
    .skeletonChart { height: 160px; }

    @keyframes shimmer {
      0%   { opacity: 1; }
      50%  { opacity: 0.5; }
      100% { opacity: 1; }
    }

    /* Error */
    .error {
      padding: var(--space-4);
      background: var(--color-surface);
      border-radius: var(--radius-md);
      color: var(--color-danger);
      font-size: var(--text-sm);
      text-align: center;
    }

    /* Empty state */
    .emptyState {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-3);
      padding: var(--space-12) var(--space-4);
      text-align: center;
    }
    .emptyHeading {
      font-size: var(--text-base);
      font-weight: var(--weight-medium);
      color: var(--color-text-muted);
    }
    .emptyBody {
      font-size: var(--text-sm);
      color: var(--color-text-muted);
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useAnalytics" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 2
    - `grep -c "ForecastCard" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 2
    - `grep -c "LineChart" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 2
    - `grep -c "HorizontalBars" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 2
    - `grep -c "TopOverspendList" frontend/src/screens/AnalyticsScreen.tsx` возвращает >= 2
    - `grep -c "Скоро будет" frontend/src/screens/AnalyticsScreen.tsx` возвращает 0 (placeholder удалён)
    - `grep -c "shimmer" frontend/src/screens/AnalyticsScreen.module.css` возвращает >= 1
    - `grep -c "chipActive" frontend/src/screens/AnalyticsScreen.module.css` возвращает >= 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
    - `cd frontend && npx vite build 2>&1 | grep -c "error"` возвращает 0
  </acceptance_criteria>
  <done>AnalyticsScreen.tsx полностью заменяет placeholder; все 4 chart-блока интегрированы; loading/error/empty states реализованы; TypeScript и Vite build чистые.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend render → API data | Данные из useAnalytics приходят с бэкенда через apiFetch с HMAC-auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Information Disclosure | AnalyticsScreen error display | accept | Показывается пользовательское сообщение без деталей ошибки API |
| T-08-10 | Tampering | SVG inline in DOM | accept | SVG только для отображения данных пользователя; нет user-controlled innerHTML; все значения форматируются через formatKopecks |
| T-08-11 | Information Disclosure | range param в URL запроса | accept | Range — enum значение, не PII; передаётся как query param к аутентифицированному endpoint |
</threat_model>

<verification>
- `grep -c "Скоро будет" frontend/src/screens/AnalyticsScreen.tsx` == 0
- `grep -c "useAnalytics" frontend/src/screens/AnalyticsScreen.tsx` >= 2
- `grep -c "linearGradient" frontend/src/components/LineChart.tsx` == 1
- `grep -c "\-\-chart-1" frontend/src/styles/tokens.css` >= 1
- `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` == 0
- `cd frontend && npx vite build 2>&1 | grep "built in"` — успешный build
</verification>

<success_criteria>
- AnalyticsScreen показывает 4 блока аналитики (ANL-01..ANL-05)
- Period chips 1M/3M/6M/12M переключают range (ANL-01)
- Forecast card с conditional coloring (ANL-02)
- TopOverspendList с left border danger/warn (ANL-03)
- LineChart SVG с bezier кривой и gradient fill (ANL-04)
- HorizontalBars с chart palette colors (ANL-05)
- Loading skeleton + empty state (ANL-06)
- TypeScript и Vite build чистые
</success_criteria>

<output>
После завершения создать `.planning/phases/08-analytics/08-04-SUMMARY.md`
</output>
