---
phase: 08-analytics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/services/analytics.py
  - app/api/schemas/analytics.py
  - app/api/routes/analytics.py
  - app/api/router.py
autonomous: true
requirements:
  - ANL-07
  - ANL-08

must_haves:
  truths:
    - "4 analytics endpoints зарегистрированы и доступны по /api/v1/analytics/*"
    - "Все endpoints защищены Depends(get_current_user) → 403 без auth"
    - "GET /api/v1/analytics/trend?range=3M возвращает {points: [{period_label, expense_cents, income_cents}]}"
    - "GET /api/v1/analytics/top-overspend?range=1M возвращает {items: top-5 по % перерасхода}"
    - "GET /api/v1/analytics/top-categories?range=3M возвращает {items: top-5 по actual_cents}"
    - "GET /api/v1/analytics/forecast возвращает {insufficient_data, current_balance_cents, projected_end_balance_cents, will_burn_cents, period_end}"
    - "Если days_elapsed=0: insufficient_data=true, projected_end_balance_cents=null"
    - "Деньги хранятся как int (BIGINT cents), никаких float в денежных полях"
  artifacts:
    - path: "app/services/analytics.py"
      provides: "4 async service functions с SQL aggregation"
      contains: "async def get_trend"
    - path: "app/api/schemas/analytics.py"
      provides: "Pydantic response models"
      contains: "class TrendResponse"
    - path: "app/api/routes/analytics.py"
      provides: "4 GET endpoints"
      contains: "prefix=\"/analytics\""
    - path: "app/api/router.py"
      provides: "analytics_router зарегистрирован в public_router"
      contains: "analytics_router"
  key_links:
    - from: "app/api/routes/analytics.py"
      to: "app/services/analytics.py"
      via: "analytics_service.get_trend(db, ...)"
      pattern: "analytics_service\\.get_"
    - from: "app/api/router.py"
      to: "app/api/routes/analytics.py"
      via: "public_router.include_router(analytics_router)"
      pattern: "include_router.*analytics"
---

<objective>
Wave 1: Создать полный бэкенд analytics — service layer + Pydantic schemas + API routes.

Purpose: Реализовать 4 analytics endpoints с SQL-агрегатами, которые проходят contract-тесты из Plan 08-01.

Output: app/services/analytics.py, app/api/schemas/analytics.py, app/api/routes/analytics.py, обновлённый app/api/router.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-analytics/08-CONTEXT.md
@.planning/phases/08-analytics/08-PATTERNS.md

<interfaces>
<!-- DB models из app/db/models.py — ключевые для агрегатов -->
<!-- ActualTransaction: id, period_id, category_id, kind (CategoryKind), amount_cents (BIGINT) -->
<!-- PlannedTransaction: id, period_id, category_id, kind, amount_cents -->
<!-- BudgetPeriod: id, period_start (DATE), period_end (DATE), status (PeriodStatus: active/closed) -->
<!-- Category: id, name, is_archived (bool) -->

<!-- Паттерн aggregate query из app/services/actual.py lines 351–376: -->
```python
planned_q = (
    select(
        PlannedTransaction.category_id,
        PlannedTransaction.kind,
        func.sum(PlannedTransaction.amount_cents).label("planned_cents"),
    )
    .where(PlannedTransaction.period_id == period_id)
    .group_by(PlannedTransaction.category_id, PlannedTransaction.kind)
)
rows = (await db.execute(planned_q)).all()
```

<!-- Period lookup: await db.get(BudgetPeriod, period_id) -->
<!-- _today_in_app_tz() из app/services/periods.py — текущая дата в Europe/Moscow -->

<!-- Pydantic schema паттерн из app/api/schemas/subscriptions.py: -->
```python
from pydantic import BaseModel, ConfigDict

class TrendPoint(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    period_label: str
    expense_cents: int
    income_cents: int

class TrendResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    points: list[TrendPoint]
```

<!-- Router паттерн из app/api/routes/subscriptions.py: -->
```python
router = APIRouter(
    prefix="/analytics",
    tags=["analytics"],
    dependencies=[Depends(get_current_user)],
)
```

<!-- get_cycle_start_day + UserNotFoundError из app/services/settings.py -->
<!-- Текущий app/api/router.py: last include_router на строке 117: -->
<!-- public_router.include_router(subscriptions_router)  # Phase 6 -->
<!-- Добавить после: public_router.include_router(analytics_router)  # Phase 8 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать app/api/schemas/analytics.py и app/services/analytics.py</name>
  <read_first>
    - app/services/actual.py (строки 34–60 imports, 323–430 aggregate pattern, compute_balance функция)
    - app/db/models.py (модели: ActualTransaction, PlannedTransaction, BudgetPeriod, Category, CategoryKind, PeriodStatus)
    - app/services/periods.py (функция _today_in_app_tz — текущая дата в Europe/Moscow)
    - app/services/settings.py (get_cycle_start_day, UserNotFoundError)
    - app/api/schemas/subscriptions.py (Pydantic ConfigDict pattern, BaseModel)
    - .planning/phases/08-analytics/08-PATTERNS.md (раздел "app/services/analytics.py" и "app/api/schemas/analytics.py")
  </read_first>
  <files>
    app/api/schemas/analytics.py
    app/services/analytics.py
  </files>
  <behavior>
    - get_trend(db, period_ids) → {"points": [{"period_label": str, "expense_cents": int, "income_cents": int}]}
    - get_top_overspend(db, period_ids) → {"items": top-5 по overspend_pct DESC, expense kind только}
    - get_top_categories(db, period_ids) → {"items": top-5 по actual_cents DESC, expense kind}
    - get_forecast(db, period_id, today) → {insufficient_data, current_balance_cents, projected_end_balance_cents, will_burn_cents, period_end}
    - days_elapsed=0: insufficient_data=True, projected_end_balance_cents=None
    - expense_cents/income_cents — int, не float
  </behavior>
  <action>
    **app/api/schemas/analytics.py** — создать с нуля:
    ```python
    """Pydantic response schemas for Phase 8 Analytics endpoints (ANL-07)."""
    from __future__ import annotations
    from typing import Optional
    from pydantic import BaseModel, ConfigDict


    class TrendPoint(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        period_label: str
        expense_cents: int
        income_cents: int


    class TrendResponse(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        points: list[TrendPoint]


    class OverspendItem(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        category_id: int
        name: str
        planned_cents: int
        actual_cents: int
        overspend_pct: float


    class TopOverspendResponse(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        items: list[OverspendItem]


    class TopCategoryItem(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        category_id: int
        name: str
        actual_cents: int
        planned_cents: int


    class TopCategoriesResponse(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        items: list[TopCategoryItem]


    class ForecastResponse(BaseModel):
        model_config = ConfigDict(from_attributes=True)
        insufficient_data: bool
        current_balance_cents: int
        projected_end_balance_cents: Optional[int] = None
        will_burn_cents: Optional[int] = None
        period_end: Optional[str] = None   # ISO date string
    ```

    **app/services/analytics.py** — создать с нуля:
    ```python
    """Analytics service — aggregate SQL queries for Phase 8 (ANL-07, ANL-08).

    All functions are read-only (no db.commit). They accept resolved period_ids
    from the route layer, not raw range strings.
    """
    from __future__ import annotations

    from datetime import date
    from typing import Optional

    from sqlalchemy import func, select
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.db.models import (
        ActualTransaction,
        BudgetPeriod,
        Category,
        CategoryKind,
        PlannedTransaction,
        PeriodStatus,
    )


    async def get_recent_periods(
        db: AsyncSession,
        *,
        n: int,
    ) -> list[BudgetPeriod]:
        """Return up to N most recent budget_periods (active + closed), ordered DESC."""
        q = (
            select(BudgetPeriod)
            .where(BudgetPeriod.status.in_([PeriodStatus.active, PeriodStatus.closed]))
            .order_by(BudgetPeriod.period_start.desc())
            .limit(n)
        )
        rows = (await db.execute(q)).scalars().all()
        return list(rows)


    def _range_to_n(range_: str) -> int:
        """Map range string to number of periods."""
        mapping = {"1M": 1, "3M": 3, "6M": 6, "12M": 12}
        return mapping.get(range_, 1)


    def _period_label(bp: BudgetPeriod) -> str:
        """Return short Russian month abbreviation for a budget period."""
        MONTHS = ["Янв", "Фев", "Мар", "Апр", "Май", "Июн",
                  "Июл", "Авг", "Сен", "Окт", "Ноя", "Дек"]
        return MONTHS[bp.period_start.month - 1]


    async def get_trend(
        db: AsyncSession,
        *,
        range_: str,
    ) -> dict:
        """SUM expense and income by period for N periods, ordered by period_start ASC."""
        n = _range_to_n(range_)
        periods = await get_recent_periods(db, n=n)
        if not periods:
            return {"points": []}

        period_ids = [p.id for p in periods]
        period_map = {p.id: p for p in periods}

        expense_q = (
            select(
                ActualTransaction.period_id,
                func.sum(ActualTransaction.amount_cents).label("total_cents"),
            )
            .where(
                ActualTransaction.period_id.in_(period_ids),
                ActualTransaction.kind == CategoryKind.expense,
            )
            .group_by(ActualTransaction.period_id)
        )
        income_q = (
            select(
                ActualTransaction.period_id,
                func.sum(ActualTransaction.amount_cents).label("total_cents"),
            )
            .where(
                ActualTransaction.period_id.in_(period_ids),
                ActualTransaction.kind == CategoryKind.income,
            )
            .group_by(ActualTransaction.period_id)
        )
        expense_rows = {r.period_id: r.total_cents for r in (await db.execute(expense_q)).all()}
        income_rows = {r.period_id: r.total_cents for r in (await db.execute(income_q)).all()}

        # Order ASC (oldest first) for chart left-to-right
        sorted_periods = sorted(periods, key=lambda p: p.period_start)
        points = [
            {
                "period_label": _period_label(period_map[pid]),
                "expense_cents": expense_rows.get(pid, 0),
                "income_cents": income_rows.get(pid, 0),
            }
            for pid in [p.id for p in sorted_periods]
        ]
        return {"points": points}


    async def get_top_overspend(
        db: AsyncSession,
        *,
        range_: str,
    ) -> dict:
        """Top-5 expense categories by overspend_pct = actual/planned*100, DESC."""
        n = _range_to_n(range_)
        periods = await get_recent_periods(db, n=n)
        if not periods:
            return {"items": []}

        period_ids = [p.id for p in periods]

        planned_q = (
            select(
                PlannedTransaction.category_id,
                func.sum(PlannedTransaction.amount_cents).label("planned_cents"),
            )
            .where(
                PlannedTransaction.period_id.in_(period_ids),
                PlannedTransaction.kind == CategoryKind.expense,
            )
            .group_by(PlannedTransaction.category_id)
        )
        actual_q = (
            select(
                ActualTransaction.category_id,
                func.sum(ActualTransaction.amount_cents).label("actual_cents"),
            )
            .where(
                ActualTransaction.period_id.in_(period_ids),
                ActualTransaction.kind == CategoryKind.expense,
            )
            .group_by(ActualTransaction.category_id)
        )
        planned_rows = {r.category_id: r.planned_cents for r in (await db.execute(planned_q)).all()}
        actual_rows = {r.category_id: r.actual_cents for r in (await db.execute(actual_q)).all()}

        # Only categories that have both plan and actual
        category_ids = set(planned_rows.keys()) & set(actual_rows.keys())
        if not category_ids:
            return {"items": []}

        # Load category names
        cat_q = select(Category).where(Category.id.in_(category_ids))
        cats = {c.id: c for c in (await db.execute(cat_q)).scalars().all()}

        items = []
        for cat_id in category_ids:
            planned = planned_rows[cat_id]
            actual = actual_rows[cat_id]
            if planned <= 0:
                continue
            overspend_pct = (actual / planned) * 100.0
            items.append({
                "category_id": cat_id,
                "name": cats[cat_id].name if cat_id in cats else str(cat_id),
                "planned_cents": planned,
                "actual_cents": actual,
                "overspend_pct": round(overspend_pct, 2),
            })

        items.sort(key=lambda x: x["overspend_pct"], reverse=True)
        return {"items": items[:5]}


    async def get_top_categories(
        db: AsyncSession,
        *,
        range_: str,
    ) -> dict:
        """Top-5 expense categories by total actual_cents DESC."""
        n = _range_to_n(range_)
        periods = await get_recent_periods(db, n=n)
        if not periods:
            return {"items": []}

        period_ids = [p.id for p in periods]

        actual_q = (
            select(
                ActualTransaction.category_id,
                func.sum(ActualTransaction.amount_cents).label("actual_cents"),
            )
            .where(
                ActualTransaction.period_id.in_(period_ids),
                ActualTransaction.kind == CategoryKind.expense,
            )
            .group_by(ActualTransaction.category_id)
            .order_by(func.sum(ActualTransaction.amount_cents).desc())
            .limit(5)
        )
        planned_q = (
            select(
                PlannedTransaction.category_id,
                func.sum(PlannedTransaction.amount_cents).label("planned_cents"),
            )
            .where(
                PlannedTransaction.period_id.in_(period_ids),
                PlannedTransaction.kind == CategoryKind.expense,
            )
            .group_by(PlannedTransaction.category_id)
        )
        actual_rows = (await db.execute(actual_q)).all()
        planned_rows = {r.category_id: r.planned_cents for r in (await db.execute(planned_q)).all()}

        if not actual_rows:
            return {"items": []}

        top_cat_ids = [r.category_id for r in actual_rows]
        cat_q = select(Category).where(Category.id.in_(top_cat_ids))
        cats = {c.id: c for c in (await db.execute(cat_q)).scalars().all()}

        items = [
            {
                "category_id": r.category_id,
                "name": cats[r.category_id].name if r.category_id in cats else str(r.category_id),
                "actual_cents": r.actual_cents,
                "planned_cents": planned_rows.get(r.category_id, 0),
            }
            for r in actual_rows
        ]
        return {"items": items}


    async def get_forecast(
        db: AsyncSession,
    ) -> dict:
        """Forecast end-of-period balance using daily burn rate.

        Algorithm:
          daily_rate = actual_expense_cents / max(days_elapsed, 1)
          will_burn_cents = remaining_days * daily_rate
          projected_end_balance_cents = current_balance_cents - will_burn_cents

        Edge case days_elapsed=0: return insufficient_data=True.
        """
        from app.services.periods import _today_in_app_tz

        today = _today_in_app_tz()

        # Find active period
        active_q = select(BudgetPeriod).where(BudgetPeriod.status == PeriodStatus.active)
        active_period = (await db.execute(active_q)).scalar_one_or_none()
        if active_period is None:
            return {
                "insufficient_data": True,
                "current_balance_cents": 0,
                "projected_end_balance_cents": None,
                "will_burn_cents": None,
                "period_end": None,
            }

        days_elapsed = (today - active_period.period_start).days
        remaining_days = (active_period.period_end - today).days

        # Total actual expense this period
        expense_q = (
            select(func.sum(ActualTransaction.amount_cents))
            .where(
                ActualTransaction.period_id == active_period.id,
                ActualTransaction.kind == CategoryKind.expense,
            )
        )
        income_q = (
            select(func.sum(ActualTransaction.amount_cents))
            .where(
                ActualTransaction.period_id == active_period.id,
                ActualTransaction.kind == CategoryKind.income,
            )
        )
        total_expense = (await db.execute(expense_q)).scalar_one_or_none() or 0
        total_income = (await db.execute(income_q)).scalar_one_or_none() or 0
        current_balance_cents = total_income - total_expense

        if days_elapsed == 0:
            return {
                "insufficient_data": True,
                "current_balance_cents": current_balance_cents,
                "projected_end_balance_cents": None,
                "will_burn_cents": None,
                "period_end": active_period.period_end.isoformat(),
            }

        daily_rate = total_expense / days_elapsed
        will_burn_cents = int(remaining_days * daily_rate)
        projected_end_balance_cents = current_balance_cents - will_burn_cents

        return {
            "insufficient_data": False,
            "current_balance_cents": current_balance_cents,
            "projected_end_balance_cents": projected_end_balance_cents,
            "will_burn_cents": will_burn_cents,
            "period_end": active_period.period_end.isoformat(),
        }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.services.analytics import get_trend, get_top_overspend, get_top_categories, get_forecast; from app.api.schemas.analytics import TrendResponse, TopOverspendResponse, TopCategoriesResponse, ForecastResponse; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def get_trend" app/services/analytics.py` возвращает 1
    - `grep -c "async def get_top_overspend" app/services/analytics.py` возвращает 1
    - `grep -c "async def get_top_categories" app/services/analytics.py` возвращает 1
    - `grep -c "async def get_forecast" app/services/analytics.py` возвращает 1
    - `grep -c "class TrendResponse" app/api/schemas/analytics.py` возвращает 1
    - `grep -c "class ForecastResponse" app/api/schemas/analytics.py` возвращает 1
    - `grep -c "projected_end_balance_cents: Optional\[int\]" app/api/schemas/analytics.py` возвращает 1
    - `python -c "from app.services.analytics import get_trend; print('ok')"` выводит "ok" без ошибок
    - `python -c "from app.api.schemas.analytics import ForecastResponse; print('ok')"` выводит "ok"
  </acceptance_criteria>
  <done>app/api/schemas/analytics.py и app/services/analytics.py созданы; все импорты работают; 4 service functions реализованы; ForecastResponse.projected_end_balance_cents Optional[int].</done>
</task>

<task type="auto">
  <name>Task 2: Создать app/api/routes/analytics.py и зарегистрировать в router.py</name>
  <read_first>
    - app/api/routes/subscriptions.py (router declaration, GET endpoint pattern, error handling)
    - app/api/dependencies.py (get_current_user signature, get_db signature)
    - app/api/router.py (последний include_router — после него добавить analytics)
    - app/services/settings.py (get_cycle_start_day, UserNotFoundError — для понимания, но analytics не использует cycle_start)
    - .planning/phases/08-analytics/08-PATTERNS.md (раздел "app/api/routes/analytics.py")
  </read_first>
  <files>
    app/api/routes/analytics.py
    app/api/router.py
  </files>
  <action>
    **app/api/routes/analytics.py** — создать с нуля:
    ```python
    """REST endpoints for Phase 8 Analytics (ANL-07).

    All endpoints are GET, read-only. Protected by get_current_user (OWNER_TG_ID).
    Range mapping: 1M=1 period, 3M=3 periods, 6M=6 periods, 12M=12 periods.
    Period resolution (active + closed, desc order) is done in the service layer.
    """
    from __future__ import annotations

    from typing import Annotated, Literal

    from fastapi import APIRouter, Depends, Query
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.api.dependencies import get_current_user, get_db
    from app.api.schemas.analytics import (
        ForecastResponse,
        TopCategoriesResponse,
        TopOverspendResponse,
        TrendResponse,
    )
    from app.services import analytics as analytics_service

    router = APIRouter(
        prefix="/analytics",
        tags=["analytics"],
        dependencies=[Depends(get_current_user)],
    )

    AnalyticsRange = Literal["1M", "3M", "6M", "12M"]


    @router.get("/trend", response_model=TrendResponse)
    async def get_trend(
        range: AnalyticsRange = Query(default="1M"),
        db: Annotated[AsyncSession, Depends(get_db)] = ...,
    ) -> TrendResponse:
        data = await analytics_service.get_trend(db, range_=range)
        return TrendResponse(**data)


    @router.get("/top-overspend", response_model=TopOverspendResponse)
    async def get_top_overspend(
        range: AnalyticsRange = Query(default="1M"),
        db: Annotated[AsyncSession, Depends(get_db)] = ...,
    ) -> TopOverspendResponse:
        data = await analytics_service.get_top_overspend(db, range_=range)
        return TopOverspendResponse(**data)


    @router.get("/top-categories", response_model=TopCategoriesResponse)
    async def get_top_categories(
        range: AnalyticsRange = Query(default="1M"),
        db: Annotated[AsyncSession, Depends(get_db)] = ...,
    ) -> TopCategoriesResponse:
        data = await analytics_service.get_top_categories(db, range_=range)
        return TopCategoriesResponse(**data)


    @router.get("/forecast", response_model=ForecastResponse)
    async def get_forecast(
        db: Annotated[AsyncSession, Depends(get_db)] = ...,
    ) -> ForecastResponse:
        data = await analytics_service.get_forecast(db)
        return ForecastResponse(**data)
    ```

    **app/api/router.py** — добавить импорт и регистрацию после существующего Phase 6 блока.

    Добавить в блок импортов (после строки `from app.api.routes.subscriptions import router as subscriptions_router`):
    ```python
    from app.api.routes.analytics import router as analytics_router
    ```

    Добавить регистрацию (после строки `public_router.include_router(subscriptions_router)`):
    ```python
    # Phase 8 sub-router — Analytics aggregates (ANL-07).
    public_router.include_router(analytics_router)
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.routes.analytics import router; print(router.prefix)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "prefix=\"/analytics\"" app/api/routes/analytics.py` возвращает 1
    - `grep -c "get_current_user" app/api/routes/analytics.py` возвращает >= 1 (в dependencies=)
    - `grep -c "analytics_router" app/api/router.py` возвращает >= 2 (импорт + include_router)
    - `grep -c "include_router(analytics_router)" app/api/router.py` возвращает 1
    - `python -c "from app.api.routes.analytics import router; print(router.prefix)"` выводит "/analytics"
    - `python -c "from app.api.router import public_router; routes = [r.path for r in public_router.routes]; print(any('analytics' in str(r) for r in public_router.routes))"` выводит True (или аналогичная проверка что роутер включён)
  </acceptance_criteria>
  <done>app/api/routes/analytics.py создан с 4 GET endpoints; analytics_router зарегистрирован в public_router; импорты не ломают существующие тесты.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/v1/analytics/* | Untrusted Telegram initData на входе каждого запроса |
| analytics service → PostgreSQL | SQL-агрегаты выполняются через async SQLAlchemy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing | GET /analytics/* | mitigate | Depends(get_current_user) на router-level — HMAC-SHA256 + OWNER_TG_ID проверка на каждом запросе |
| T-08-04 | Information Disclosure | analytics SQL aggregates | accept | Single-tenant MVP: нет user_id FK, данные только одного владельца; Postgres на закрытом VPS |
| T-08-05 | Tampering | range query param | mitigate | Literal["1M","3M","6M","12M"] в Annotated — FastAPI вернёт 422 при невалидном значении |
| T-08-06 | Denial of Service | aggregate queries on large data | accept | Single-tenant MVP, объём данных ограничен одним пользователем; materialized views — Phase 11 по ANL-08 |
</threat_model>

<verification>
- `python -c "from app.services.analytics import get_trend, get_top_overspend, get_top_categories, get_forecast; print('ok')"` — ok
- `python -c "from app.api.routes.analytics import router; print(router.prefix)"` — /analytics
- `grep -c "include_router(analytics_router)" app/api/router.py` — 1
- Auth-тесты из Plan 08-01 теперь должны PASS (403 без auth работает)
- DB-backed тесты из Plan 08-01 PASS при наличии DATABASE_URL
</verification>

<success_criteria>
- 4 analytics endpoints зарегистрированы и доступны
- 403 без X-Telegram-Init-Data на всех 4 endpoints
- GET /api/v1/analytics/trend?range=3M → 200, {"points": [...]}
- GET /api/v1/analytics/forecast → 200, {"insufficient_data": bool, "current_balance_cents": int, ...}
- days_elapsed=0 → insufficient_data=true, projected_end_balance_cents=null
- ANL-07 и ANL-08 покрыты
</success_criteria>

<output>
После завершения создать `.planning/phases/08-analytics/08-02-SUMMARY.md`
</output>
