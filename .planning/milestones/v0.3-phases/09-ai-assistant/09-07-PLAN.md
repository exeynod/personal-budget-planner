---
phase: 09-ai-assistant
plan: "07"
type: execute
wave: 5
depends_on:
  - "09-06"
files_modified:
  - frontend/src/screens/AiScreen.tsx
  - frontend/src/screens/AiScreen.module.css
autonomous: false
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "AiScreen показывает PageTitle 'Budget AI' с Sparkle аватаром"
    - "Empty state: 4 suggestion chips при пустом messages[]"
    - "Клик на chip → sendMessage(chipText) → начинается streaming"
    - "Каждое сообщение рендерится как ChatMessage bubble"
    - "Во время tool вызова ToolUseIndicator появляется между сообщениями"
    - "Стриминг-ответ появляется token-by-token (ChatMessage isStreaming=true)"
    - "Auto-scroll до конца при каждом новом токене"
    - "Кнопка очистки истории (иконка trash) → clearHistory()"
  artifacts:
    - path: frontend/src/screens/AiScreen.tsx
      provides: "Полный AI chat экран (заменяет placeholder)"
      contains: "useAiConversation"
    - path: frontend/src/screens/AiScreen.module.css
      provides: "CSS для chat layout (flex column, input bar, bubbles)"
  key_links:
    - from: frontend/src/screens/AiScreen.tsx
      to: frontend/src/hooks/useAiConversation.ts
      via: "useAiConversation() hook"
      pattern: "import.*useAiConversation"
    - from: frontend/src/screens/AiScreen.tsx
      to: frontend/src/components/ChatMessage.tsx
      via: "messages.map → ChatMessage"
      pattern: "import.*ChatMessage"
    - from: frontend/src/screens/AiScreen.tsx
      to: frontend/src/components/ToolUseIndicator.tsx
      via: "toolName && ToolUseIndicator"
      pattern: "import.*ToolUseIndicator"
---

<objective>
Заменить placeholder AiScreen полноценным chat UI: список сообщений, suggestion chips, input bar, streaming render, tool indicator, auto-scroll, очистка истории.

Purpose: Финальный шаг Phase 9 — пользователь видит работающий AI чат в TG Mini App. После этого плана все требования AI-01–AI-10 выполнены.
Output: Полный AiScreen.tsx + AiScreen.module.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-ai-assistant/09-CONTEXT.md
@.planning/phases/09-ai-assistant/09-PATTERNS.md
@.planning/phases/09-ai-assistant/09-06-SUMMARY.md

<interfaces>
<!-- Паттерн из AnalyticsScreen.tsx — screen layout, loading/error/empty states -->
<!-- Паттерн из 09-PATTERNS.md — AiScreen structure -->

Из 09-PATTERNS.md — AiScreen structure:
```tsx
import { Sparkle } from '@phosphor-icons/react';
import { PageTitle } from '../components/PageTitle';
import { ChatMessage } from '../components/ChatMessage';
import { ToolUseIndicator } from '../components/ToolUseIndicator';
import { useAiConversation } from '../hooks/useAiConversation';

export function AiScreen() {
  const { messages, streaming, toolName, streamingText, error, sendMessage, clearHistory } = useAiConversation();
  // ...
}
```

Suggestion chips из CONTEXT.md (4 фиксированных):
```
«Каков мой баланс?»
«Где я перерасходовал?»
«Сколько потратил на еду?»
«Сделай прогноз»
```

AI таб цвет: #a78bfa (фиолетовый) — уже установлен в BottomNav, сохранить.

Паттерн empty state (из 09-PATTERNS.md):
```tsx
{allEmpty && (
  <div className={styles.emptyState}>
    <Sparkle size={48} weight="thin" color="#a78bfa" />
    <div className={styles.emptyHeading}>Задай вопрос о своём бюджете</div>
  </div>
)}
```

Auto-scroll pattern:
```typescript
const bottomRef = useRef<HTMLDivElement>(null);
useEffect(() => {
  bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
}, [messages, streamingText]);
```

Input bar pattern:
```tsx
const [input, setInput] = useState('');
const handleSend = () => {
  const trimmed = input.trim();
  if (!trimmed || streaming) return;
  setInput('');
  sendMessage(trimmed);
};
// Enter key → handleSend
// Button disabled при streaming || !input.trim()
```

Из frontend/src/screens/AiScreen.tsx (текущий placeholder):
```tsx
// Текущее содержимое — placeholder "Скоро будет"
// Нужно полностью заменить
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: AiScreen.tsx — полный chat UI</name>
  <files>frontend/src/screens/AiScreen.tsx</files>
  <read_first>
    - frontend/src/screens/AiScreen.tsx (текущий placeholder — полное содержимое)
    - frontend/src/screens/AnalyticsScreen.tsx (паттерн screen layout, loading/error states)
    - frontend/src/components/PageTitle.tsx (сигнатура компонента: title prop)
  </read_first>
  <action>
Прочитать текущий AiScreen.tsx. Полностью заменить содержимое на полноценный chat UI:

```tsx
/**
 * AI Assistant экран — conversational chat с tool-use (AI-01..AI-04).
 *
 * - PageTitle "Budget AI" + Sparkle аватар (AI-01)
 * - 4 suggestion chips при пустой истории (AI-02)
 * - Streaming token-by-token через useAiConversation (AI-03)
 * - ToolUseIndicator во время вызова tool (AI-04)
 * - Auto-scroll при каждом токене
 * - Кнопка очистки истории
 */
import { useEffect, useRef, useState } from 'react';
import { Sparkle, Trash } from '@phosphor-icons/react';
import { ChatMessage } from '../components/ChatMessage';
import { PageTitle } from '../components/PageTitle';
import { ToolUseIndicator } from '../components/ToolUseIndicator';
import { useAiConversation } from '../hooks/useAiConversation';
import type { ChatMessageRead } from '../api/types';
import styles from './AiScreen.module.css';

/** 4 фиксированных suggestion chips (CONTEXT.md decision). */
const SUGGESTION_CHIPS = [
  'Каков мой баланс?',
  'Где я перерасходовал?',
  'Сколько потратил на еду?',
  'Сделай прогноз',
];

export function AiScreen() {
  const {
    messages,
    streaming,
    toolName,
    streamingText,
    error,
    sendMessage,
    clearHistory,
  } = useAiConversation();

  const [input, setInput] = useState('');
  const bottomRef = useRef<HTMLDivElement>(null);
  const inputRef = useRef<HTMLTextAreaElement>(null);

  // Auto-scroll при новых токенах и сообщениях
  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: 'smooth' });
  }, [messages, streamingText, toolName]);

  const handleSend = () => {
    const trimmed = input.trim();
    if (!trimmed || streaming) return;
    setInput('');
    sendMessage(trimmed);
  };

  const handleKeyDown = (e: React.KeyboardEvent<HTMLTextAreaElement>) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  const handleChipClick = (text: string) => {
    if (streaming) return;
    sendMessage(text);
  };

  const handleClear = async () => {
    if (streaming) return;
    await clearHistory();
  };

  const isEmpty = messages.length === 0 && !streaming;

  // Временное streaming-сообщение для отображения token-by-token
  const streamingMessage: ChatMessageRead | null =
    streaming && streamingText
      ? {
          id: -1,
          role: 'assistant',
          content: streamingText,
          tool_name: null,
          created_at: new Date().toISOString(),
        }
      : null;

  return (
    <div className={styles.root}>
      {/* Header */}
      <div className={styles.header}>
        <PageTitle title="Budget AI" />
        {messages.length > 0 && (
          <button
            className={styles.clearBtn}
            onClick={handleClear}
            disabled={streaming}
            aria-label="Очистить историю"
          >
            <Trash size={20} weight="regular" />
          </button>
        )}
      </div>

      {/* Messages area */}
      <div className={styles.messages}>
        {/* Empty state с suggestion chips */}
        {isEmpty && (
          <div className={styles.emptyState}>
            <Sparkle size={48} weight="thin" color="#a78bfa" />
            <div className={styles.emptyHeading}>Задай вопрос о своём бюджете</div>
            <div className={styles.chips}>
              {SUGGESTION_CHIPS.map((chip) => (
                <button
                  key={chip}
                  className={styles.chip}
                  onClick={() => handleChipClick(chip)}
                >
                  {chip}
                </button>
              ))}
            </div>
          </div>
        )}

        {/* История сообщений */}
        {messages.map((msg) => (
          <ChatMessage key={msg.id} message={msg} />
        ))}

        {/* Tool indicator — между user msg и AI response */}
        {streaming && toolName && (
          <ToolUseIndicator toolName={toolName} />
        )}

        {/* Streaming ответ AI token-by-token */}
        {streamingMessage && (
          <ChatMessage message={streamingMessage} isStreaming={true} />
        )}

        {/* Индикатор загрузки если streaming без токенов ещё */}
        {streaming && !streamingText && !toolName && (
          <div className={styles.thinking}>
            <span className={styles.dot} />
            <span className={styles.dot} />
            <span className={styles.dot} />
          </div>
        )}

        {/* Error state */}
        {error && !streaming && (
          <div className={styles.error}>
            Ошибка: {error}
          </div>
        )}

        {/* Anchor для auto-scroll */}
        <div ref={bottomRef} />
      </div>

      {/* Input bar */}
      <div className={styles.inputBar}>
        <textarea
          ref={inputRef}
          className={styles.input}
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="Спроси о бюджете..."
          rows={1}
          disabled={streaming}
        />
        <button
          className={styles.sendBtn}
          onClick={handleSend}
          disabled={streaming || !input.trim()}
          aria-label="Отправить"
        >
          <Sparkle size={20} weight="fill" color={streaming || !input.trim() ? '#999' : '#a78bfa'} />
        </button>
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep "AiScreen" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useAiConversation" frontend/src/screens/AiScreen.tsx` возвращает строку import
    - `grep "ChatMessage" frontend/src/screens/AiScreen.tsx | wc -l` возвращает минимум 3 (import + 2 использования)
    - `grep "ToolUseIndicator" frontend/src/screens/AiScreen.tsx` возвращает строку с компонентом
    - `grep "SUGGESTION_CHIPS" frontend/src/screens/AiScreen.tsx | wc -l` возвращает минимум 2 (definition + usage)
    - `grep "Каков мой баланс" frontend/src/screens/AiScreen.tsx` возвращает строку
    - `grep "Сделай прогноз" frontend/src/screens/AiScreen.tsx` возвращает строку
    - `grep "bottomRef" frontend/src/screens/AiScreen.tsx` возвращает строку с scrollIntoView
    - `grep "clearHistory\|handleClear" frontend/src/screens/AiScreen.tsx` возвращает строку
    - `grep "Sparkle\|Trash" frontend/src/screens/AiScreen.tsx | grep -c "phosphor"` возвращает минимум 1
    - `grep "lucide" frontend/src/screens/AiScreen.tsx | wc -l` возвращает 0 (запрещены)
    - npx tsc --noEmit не выдаёт ошибок для AiScreen.tsx
  </acceptance_criteria>
  <done>AiScreen.tsx заменяет placeholder; содержит все 8 UI элементов Phase 9</done>
</task>

<task type="auto">
  <name>Задача 2: AiScreen.module.css — chat layout стили</name>
  <files>frontend/src/screens/AiScreen.module.css</files>
  <read_first>
    - frontend/src/screens/AiScreen.module.css (текущее содержимое placeholder)
    - frontend/src/screens/AnalyticsScreen.module.css (паттерн CSS переменных, root layout)
  </read_first>
  <action>
Прочитать текущий AiScreen.module.css. Полностью заменить на chat layout:

```css
/* AiScreen — full chat layout (AI-01..AI-04) */

.root {
  display: flex;
  flex-direction: column;
  height: 100%;
  max-height: 100dvh;
  overflow: hidden;
  background: var(--tg-theme-bg-color, #ffffff);
}

/* Header с PageTitle и кнопкой очистки */
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 16px 0 0;
  flex-shrink: 0;
}

.clearBtn {
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
  color: var(--tg-theme-hint-color, #999999);
  border-radius: 8px;
  display: flex;
  align-items: center;
}

.clearBtn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

/* Область сообщений — скролл */
.messages {
  flex: 1;
  overflow-y: auto;
  padding: 12px 16px;
  display: flex;
  flex-direction: column;
  gap: 4px;
}

/* Empty state */
.emptyState {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 12px;
  margin: auto;
  padding: 32px 0;
  text-align: center;
}

.emptyHeading {
  font-size: 16px;
  font-weight: 600;
  color: var(--tg-theme-text-color, #000000);
}

/* Suggestion chips */
.chips {
  display: flex;
  flex-direction: column;
  gap: 8px;
  width: 100%;
  max-width: 320px;
}

.chip {
  background: var(--tg-theme-secondary-bg-color, #f0f0f0);
  border: none;
  border-radius: 12px;
  padding: 10px 16px;
  font-size: 14px;
  color: var(--tg-theme-text-color, #000000);
  cursor: pointer;
  text-align: left;
  transition: opacity 0.1s;
}

.chip:active {
  opacity: 0.7;
}

/* Thinking indicator (3 dots перед первым токеном) */
.thinking {
  display: flex;
  gap: 4px;
  align-self: flex-start;
  padding: 12px 16px;
  background: var(--tg-theme-secondary-bg-color, #f0f0f0);
  border-radius: 16px;
  border-bottom-left-radius: 4px;
}

.dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--tg-theme-hint-color, #999999);
  animation: thinking 1.2s ease-in-out infinite;
}

.dot:nth-child(2) {
  animation-delay: 0.2s;
}

.dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes thinking {
  0%, 100% { opacity: 0.3; }
  50% { opacity: 1; }
}

/* Error message */
.error {
  background: rgba(239, 68, 68, 0.1);
  color: #ef4444;
  border-radius: 12px;
  padding: 10px 14px;
  font-size: 14px;
  align-self: flex-start;
  max-width: 85%;
}

/* Input bar */
.inputBar {
  display: flex;
  align-items: flex-end;
  gap: 8px;
  padding: 10px 16px;
  padding-bottom: max(10px, env(safe-area-inset-bottom));
  border-top: 1px solid var(--tg-theme-secondary-bg-color, #f0f0f0);
  background: var(--tg-theme-bg-color, #ffffff);
  flex-shrink: 0;
}

.input {
  flex: 1;
  background: var(--tg-theme-secondary-bg-color, #f0f0f0);
  border: none;
  border-radius: 20px;
  padding: 10px 16px;
  font-size: 15px;
  color: var(--tg-theme-text-color, #000000);
  resize: none;
  outline: none;
  min-height: 40px;
  max-height: 120px;
  overflow-y: auto;
  line-height: 1.4;
  font-family: inherit;
}

.input::placeholder {
  color: var(--tg-theme-hint-color, #999999);
}

.input:disabled {
  opacity: 0.6;
}

.sendBtn {
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
  display: flex;
  align-items: center;
  border-radius: 50%;
  flex-shrink: 0;
}

.sendBtn:disabled {
  cursor: not-allowed;
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && npx vite build --outDir dist-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "flex-direction: column" frontend/src/screens/AiScreen.module.css | wc -l` возвращает минимум 2 (root + emptyState)
    - `grep "overflow-y: auto" frontend/src/screens/AiScreen.module.css` возвращает строку (messages scroll)
    - `grep "chip" frontend/src/screens/AiScreen.module.css | wc -l` возвращает минимум 3 (chips container + chip style)
    - `grep "animation" frontend/src/screens/AiScreen.module.css | wc -l` возвращает минимум 2
    - `grep "safe-area-inset-bottom" frontend/src/screens/AiScreen.module.css` возвращает строку (iPhone safe area)
    - `npx vite build` завершается без ошибок (TypeScript + CSS)
  </acceptance_criteria>
  <done>AiScreen.module.css содержит полный chat layout; vite build проходит</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → sendMessage() | Текст из textarea → API → LLM |
| LLM response → dangerouslySetInnerHTML | В ChatMessage.tsx только для assistant role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-19 | Tampering | suggestion chips | accept | 4 фиксированных строки — не динамический контент; XSS исключён |
| T-09-20 | Denial of Service | textarea input | accept | Rate limit на API-слое (Plan 09-05); UI disabled при streaming |
| T-09-21 | Tampering | clearHistory | accept | Hard delete по design; single-tenant — пользователь удаляет только свои данные |
</threat_model>

<verification>
```bash
cd /Users/exy/pet_projects/tg-budget-planner

# TypeScript проверка
npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -c "error" || echo "0 errors"

# Build проверка
npx vite build --outDir dist-phase9 2>&1 | tail -3
```

Ожидаемые результаты:
- 0 TypeScript ошибок
- vite build: "built in Xs"
</verification>

<success_criteria>
- AiScreen.tsx: 8 UI элементов (PageTitle, Sparkle аватар, 4 chips, messages list, ToolUseIndicator, streaming preview, input bar, clear button)
- AiScreen.module.css: flex column layout с scroll, safe-area, input bar
- TypeScript компиляция без ошибок
- vite build проходит
- Визуальная проверка: empty state → chips → отправка → streaming → done
</success_criteria>

<output>
После завершения создать `.planning/phases/09-ai-assistant/09-07-SUMMARY.md`

Затем выполнить финальную верификацию:
```bash
cd /Users/exy/pet_projects/tg-budget-planner
python -m pytest tests/ai/ tests/api/test_ai_chat.py tests/services/test_ai_conversation_service.py -v --tb=short 2>&1 | tail -20
npx tsc --noEmit --project frontend/tsconfig.json
npx vite build
```
</output>
