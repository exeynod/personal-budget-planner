---
phase: 09-ai-assistant
plan: "04"
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - app/ai/tools.py
  - app/ai/system_prompt.py
  - app/api/schemas/ai.py
  - app/services/ai_conversation_service.py
autonomous: true
requirements:
  - AI-05
  - AI-06
  - AI-07

must_haves:
  truths:
    - "4 tool-функции (get_period_balance, get_category_summary, query_transactions, get_forecast) возвращают dict"
    - "Tools никогда не бросают исключение — при ошибке возвращают {'error': 'message'}"
    - "system_prompt.py собирает системный промпт + бюджет-контекст с cache_control блоками"
    - "ai_conversation_service.py реализует get_or_create_conversation, append_message, get_recent_messages, clear_conversation"
    - "TOOLS_SCHEMA — список dict в формате OpenAI function calling"
  artifacts:
    - path: app/ai/tools.py
      provides: "4 async tool-функции + TOOLS_SCHEMA"
      exports:
        - get_period_balance
        - get_category_summary
        - query_transactions
        - get_forecast
        - TOOLS_SCHEMA
    - path: app/ai/system_prompt.py
      provides: "build_messages() — собирает messages list с cache_control"
      exports:
        - build_messages
    - path: app/api/schemas/ai.py
      provides: "Pydantic схемы ChatRequest, ChatMessageRead, ChatHistoryResponse"
      exports:
        - ChatRequest
        - ChatMessageRead
        - ChatHistoryResponse
    - path: app/services/ai_conversation_service.py
      provides: "CRUD сервис для AiConversation + AiMessage"
      exports:
        - get_or_create_conversation
        - append_message
        - get_recent_messages
        - clear_conversation
  key_links:
    - from: app/ai/tools.py
      to: app/db/models.py
      via: "select(BudgetPeriod), select(ActualTransaction)"
      pattern: "from app.db.models import"
    - from: app/ai/system_prompt.py
      to: app/ai/tools.py
      via: "TOOLS_SCHEMA в build_messages()"
      pattern: "from app.ai.tools import TOOLS_SCHEMA"
    - from: app/services/ai_conversation_service.py
      to: app/db/models.py
      via: "select(AiConversation), AiMessage"
      pattern: "from app.db.models import AiConversation, AiMessage"
---

<objective>
Создать слой бизнес-логики AI: 4 tool-функции над БД, системный промпт с prompt caching, сервис управления conversation, Pydantic схемы.

Purpose: Tools дают LLM доступ к реальным данным бюджета (AI-05). System prompt с cache_control снижает стоимость повторных запросов (AI-07). Conversation service обеспечивает persistence истории (AI-06).
Output: app/ai/tools.py, app/ai/system_prompt.py, app/services/ai_conversation_service.py, app/api/schemas/ai.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-ai-assistant/09-CONTEXT.md
@.planning/phases/09-ai-assistant/09-PATTERNS.md
@.planning/phases/09-ai-assistant/09-02-SUMMARY.md

<interfaces>
<!-- SQL-паттерны из app/services/analytics.py — прямые аналоги для tools -->

Паттерн SQLAlchemy из analytics.py:
```python
# Импорты моделей
from app.db.models import (
    ActualTransaction, BudgetPeriod, Category, CategoryKind,
    PeriodStatus, PlannedTransaction,
)

# Паттерн агрегатного запроса
q = (
    select(
        ActualTransaction.period_id,
        func.sum(ActualTransaction.amount_cents).label("total_cents"),
    )
    .where(
        ActualTransaction.period_id.in_(period_ids),
        ActualTransaction.kind == CategoryKind.expense,
    )
    .group_by(ActualTransaction.period_id)
)
rows = {r.period_id: r.total_cents for r in (await db.execute(q)).all()}

# Паттерн получения активного периода
q = select(BudgetPeriod).where(BudgetPeriod.status == PeriodStatus.active).limit(1)
period = (await db.execute(q)).scalar_one_or_none()
```

ORM-модели (из Plan 09-02):
```python
# AiConversation: id, created_at, messages (relationship)
# AiMessage: id, conversation_id, role, content, tool_name, tool_result, created_at
```

Формат TOOLS_SCHEMA для OpenAI function calling:
```python
TOOLS_SCHEMA = [
    {
        "type": "function",
        "function": {
            "name": "get_period_balance",
            "description": "Получить баланс (план/факт/дельта) активного бюджетного периода",
            "parameters": {"type": "object", "properties": {}, "required": []},
        },
    },
    {
        "type": "function",
        "function": {
            "name": "get_category_summary",
            "description": "Получить сводку по категориям бюджета (план/факт/остаток)",
            "parameters": {
                "type": "object",
                "properties": {
                    "category_id": {
                        "type": "integer",
                        "description": "ID конкретной категории (необязательно — без него все категории)",
                    }
                },
                "required": [],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "query_transactions",
            "description": "Получить список факт-транзакций с фильтрацией",
            "parameters": {
                "type": "object",
                "properties": {
                    "limit": {"type": "integer", "default": 10},
                    "kind": {"type": "string", "enum": ["expense", "income"]},
                    "category_id": {"type": "integer"},
                },
                "required": [],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "get_forecast",
            "description": "Получить прогноз остатка к концу текущего периода",
            "parameters": {"type": "object", "properties": {}, "required": []},
        },
    },
]
```

Формат build_messages() output:
```python
# messages list для OpenAI API с cache_control на системном промпте
[
    {
        "role": "system",
        "content": [
            {
                "type": "text",
                "text": "Ты бюджетный помощник...",
                "cache_control": {"type": "ephemeral"}  # prompt caching
            }
        ]
    },
    # ... история сообщений из БД ...
    {"role": "user", "content": "Каков мой баланс?"}
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: Tools registry + system prompt builder</name>
  <files>app/ai/tools.py, app/ai/system_prompt.py</files>
  <read_first>
    - app/services/analytics.py (SQL-паттерны: select, func.sum, где активный период, агрегаты категорий)
    - app/db/models.py (модели: BudgetPeriod, ActualTransaction, PlannedTransaction, Category, CategoryKind, PeriodStatus + новые AiConversation, AiMessage)
  </read_first>
  <action>
**Создать app/ai/tools.py:**

```python
"""4 инструмента AI для доступа к данным бюджета (AI-05).

Каждая tool-функция:
- принимает db: AsyncSession (+ опциональные параметры)
- возвращает dict с данными или {"error": "сообщение"} при ошибке
- никогда не бросает исключение (AI объясняет пользователю через error dict)

Деньги: всегда BIGINT kopecks — никаких float/Decimal.
"""
from __future__ import annotations

from typing import Any

from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import (
    ActualTransaction,
    BudgetPeriod,
    Category,
    CategoryKind,
    PeriodStatus,
    PlannedTransaction,
)


async def get_period_balance(db: AsyncSession) -> dict[str, Any]:
    """Tool: баланс (план/факт/дельта) активного периода (AI-05)."""
    try:
        q = select(BudgetPeriod).where(BudgetPeriod.status == PeriodStatus.active).limit(1)
        period = (await db.execute(q)).scalar_one_or_none()
        if period is None:
            return {"error": "Активный период не найден"}

        # Факт-расходы
        q_exp = select(func.sum(ActualTransaction.amount_cents)).where(
            ActualTransaction.period_id == period.id,
            ActualTransaction.kind == CategoryKind.expense,
        )
        actual_expense_cents = (await db.execute(q_exp)).scalar() or 0

        # Факт-доходы
        q_inc = select(func.sum(ActualTransaction.amount_cents)).where(
            ActualTransaction.period_id == period.id,
            ActualTransaction.kind == CategoryKind.income,
        )
        actual_income_cents = (await db.execute(q_inc)).scalar() or 0

        # Плановые расходы
        q_plan = select(func.sum(PlannedTransaction.amount_cents)).where(
            PlannedTransaction.period_id == period.id,
            PlannedTransaction.kind == CategoryKind.expense,
        )
        planned_expense_cents = (await db.execute(q_plan)).scalar() or 0

        balance_cents = period.starting_balance_cents + actual_income_cents - actual_expense_cents
        delta_cents = planned_expense_cents - actual_expense_cents  # положит. = хорошо

        return {
            "period_start": period.period_start.isoformat(),
            "period_end": period.period_end.isoformat(),
            "starting_balance_cents": period.starting_balance_cents,
            "actual_expense_cents": actual_expense_cents,
            "actual_income_cents": actual_income_cents,
            "planned_expense_cents": planned_expense_cents,
            "balance_cents": balance_cents,
            "expense_delta_cents": delta_cents,
        }
    except Exception as exc:
        return {"error": f"Ошибка получения баланса: {exc}"}


async def get_category_summary(
    db: AsyncSession, category_id: int | None = None
) -> dict[str, Any]:
    """Tool: сводка по категориям (план/факт/остаток) (AI-05)."""
    try:
        q = select(BudgetPeriod).where(BudgetPeriod.status == PeriodStatus.active).limit(1)
        period = (await db.execute(q)).scalar_one_or_none()
        if period is None:
            return {"error": "Активный период не найден"}

        # Получить категории
        cat_q = select(Category).where(Category.is_archived.is_(False))
        if category_id is not None:
            cat_q = cat_q.where(Category.id == category_id)
        categories = (await db.execute(cat_q)).scalars().all()

        if not categories:
            return {"categories": [], "period_start": period.period_start.isoformat()}

        cat_ids = [c.id for c in categories]

        # Агрегат факт по категории
        q_actual = (
            select(
                ActualTransaction.category_id,
                func.sum(ActualTransaction.amount_cents).label("total"),
            )
            .where(
                ActualTransaction.period_id == period.id,
                ActualTransaction.category_id.in_(cat_ids),
            )
            .group_by(ActualTransaction.category_id)
        )
        actual_by_cat = {
            r.category_id: r.total
            for r in (await db.execute(q_actual)).all()
        }

        # Агрегат план по категории
        q_plan = (
            select(
                PlannedTransaction.category_id,
                func.sum(PlannedTransaction.amount_cents).label("total"),
            )
            .where(
                PlannedTransaction.period_id == period.id,
                PlannedTransaction.category_id.in_(cat_ids),
            )
            .group_by(PlannedTransaction.category_id)
        )
        plan_by_cat = {
            r.category_id: r.total
            for r in (await db.execute(q_plan)).all()
        }

        result = []
        for cat in categories:
            actual = actual_by_cat.get(cat.id, 0) or 0
            plan = plan_by_cat.get(cat.id, 0) or 0
            # Дельта: расходы Plan−Fact, доходы Fact−Plan (положит. = хорошо)
            delta = (plan - actual) if cat.kind == CategoryKind.expense else (actual - plan)
            result.append({
                "id": cat.id,
                "name": cat.name,
                "kind": cat.kind.value,
                "planned_cents": plan,
                "actual_cents": actual,
                "delta_cents": delta,
            })

        return {
            "categories": result,
            "period_start": period.period_start.isoformat(),
            "period_end": period.period_end.isoformat(),
        }
    except Exception as exc:
        return {"error": f"Ошибка получения категорий: {exc}"}


async def query_transactions(
    db: AsyncSession,
    limit: int = 10,
    kind: str | None = None,
    category_id: int | None = None,
) -> dict[str, Any]:
    """Tool: список факт-транзакций с фильтрацией (AI-05)."""
    try:
        q = (
            select(
                ActualTransaction.id,
                ActualTransaction.amount_cents,
                ActualTransaction.kind,
                ActualTransaction.tx_date,
                ActualTransaction.description,
                Category.name.label("category_name"),
            )
            .join(Category, ActualTransaction.category_id == Category.id, isouter=True)
            .order_by(ActualTransaction.tx_date.desc())
            .limit(min(limit, 50))
        )
        if kind == "expense":
            q = q.where(ActualTransaction.kind == CategoryKind.expense)
        elif kind == "income":
            q = q.where(ActualTransaction.kind == CategoryKind.income)
        if category_id is not None:
            q = q.where(ActualTransaction.category_id == category_id)

        rows = (await db.execute(q)).all()
        transactions = [
            {
                "id": r.id,
                "amount_cents": r.amount_cents,
                "kind": r.kind.value,
                "date": r.tx_date.isoformat(),
                "description": r.description or "",
                "category": r.category_name or "—",
            }
            for r in rows
        ]
        return {"transactions": transactions, "count": len(transactions)}
    except Exception as exc:
        return {"error": f"Ошибка получения транзакций: {exc}"}


async def get_forecast(db: AsyncSession) -> dict[str, Any]:
    """Tool: прогноз остатка к концу периода (linear extrapolation) (AI-05)."""
    try:
        from datetime import date as date_type

        q = select(BudgetPeriod).where(BudgetPeriod.status == PeriodStatus.active).limit(1)
        period = (await db.execute(q)).scalar_one_or_none()
        if period is None:
            return {"error": "Активный период не найден"}

        today = date_type.today()
        period_start = period.period_start
        period_end = period.period_end
        total_days = (period_end - period_start).days + 1
        elapsed_days = max((today - period_start).days + 1, 1)
        remaining_days = max((period_end - today).days, 0)

        if elapsed_days < 3:
            return {
                "insufficient_data": True,
                "reason": "Период только начался — прогноз недоступен",
                "period_start": period_start.isoformat(),
                "period_end": period_end.isoformat(),
            }

        # Факт-расходы за период
        q_exp = select(func.sum(ActualTransaction.amount_cents)).where(
            ActualTransaction.period_id == period.id,
            ActualTransaction.kind == CategoryKind.expense,
        )
        actual_expense_cents = (await db.execute(q_exp)).scalar() or 0

        # Факт-доходы за период
        q_inc = select(func.sum(ActualTransaction.amount_cents)).where(
            ActualTransaction.period_id == period.id,
            ActualTransaction.kind == CategoryKind.income,
        )
        actual_income_cents = (await db.execute(q_inc)).scalar() or 0

        current_balance_cents = (
            period.starting_balance_cents + actual_income_cents - actual_expense_cents
        )

        daily_expense_rate = actual_expense_cents / elapsed_days
        forecasted_remaining_expense = int(daily_expense_rate * remaining_days)
        forecast_balance_cents = current_balance_cents - forecasted_remaining_expense

        return {
            "insufficient_data": False,
            "period_start": period_start.isoformat(),
            "period_end": period_end.isoformat(),
            "current_balance_cents": current_balance_cents,
            "elapsed_days": elapsed_days,
            "remaining_days": remaining_days,
            "total_days": total_days,
            "daily_expense_rate_cents": int(daily_expense_rate),
            "forecast_balance_cents": forecast_balance_cents,
        }
    except Exception as exc:
        return {"error": f"Ошибка прогноза: {exc}"}


# OpenAI function calling schema для 4 инструментов (AI-05)
TOOLS_SCHEMA: list[dict] = [
    {
        "type": "function",
        "function": {
            "name": "get_period_balance",
            "description": "Получить баланс (план/факт/дельта) активного бюджетного периода",
            "parameters": {"type": "object", "properties": {}, "required": []},
        },
    },
    {
        "type": "function",
        "function": {
            "name": "get_category_summary",
            "description": "Получить сводку по категориям бюджета (план/факт/остаток по каждой)",
            "parameters": {
                "type": "object",
                "properties": {
                    "category_id": {
                        "type": "integer",
                        "description": "ID конкретной категории. Без него — все категории.",
                    }
                },
                "required": [],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "query_transactions",
            "description": "Получить список факт-транзакций с фильтрацией по типу и категории",
            "parameters": {
                "type": "object",
                "properties": {
                    "limit": {
                        "type": "integer",
                        "description": "Максимальное количество транзакций (не более 50)",
                        "default": 10,
                    },
                    "kind": {
                        "type": "string",
                        "enum": ["expense", "income"],
                        "description": "Тип транзакции: expense (расход) или income (доход)",
                    },
                    "category_id": {
                        "type": "integer",
                        "description": "Фильтр по ID категории",
                    },
                },
                "required": [],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "get_forecast",
            "description": "Получить прогноз остатка бюджета к концу текущего периода",
            "parameters": {"type": "object", "properties": {}, "required": []},
        },
    },
]

# Маппинг имя tool → функция для вызова в route handler
TOOL_FUNCTIONS = {
    "get_period_balance": get_period_balance,
    "get_category_summary": get_category_summary,
    "query_transactions": query_transactions,
    "get_forecast": get_forecast,
}
```

**Создать app/ai/system_prompt.py:**

```python
"""Системный промпт и сборщик messages для OpenAI API (AI-07).

build_messages() включает:
1. Системный промпт с cache_control (prompt caching) — роль + правила
2. История из БД (последние AI_MAX_CONTEXT_MESSAGES)
3. Новое сообщение пользователя

Промпт на русском (по решению Claude's Discretion из CONTEXT.md).
"""
from __future__ import annotations

SYSTEM_PROMPT = """Ты — персональный бюджетный помощник в Telegram Mini App. \
Отвечаешь только на вопросы о бюджете пользователя: расходы, доходы, баланс, \
категории, прогнозы. Используй инструменты (functions) для получения актуальных \
данных из базы данных. Если данных недостаточно — скажи об этом честно. \
Отвечай кратко и по делу. Суммы всегда в рублях (данные хранятся в копейках — \
делить на 100 перед выводом). Положительная дельта означает, что бюджет в норме.
"""


def build_messages(
    history: list[dict],
    user_message: str,
) -> list[dict]:
    """Собрать messages list для OpenAI API с cache_control на системном промпте.

    history: список dict {role, content} из БД (уже обрезан до 20 сообщений).
    user_message: новое сообщение пользователя.

    Возвращает список в формате OpenAI messages с structured content
    для системного промпта (prompt caching через cache_control).
    """
    messages: list[dict] = [
        {
            "role": "system",
            "content": [
                {
                    "type": "text",
                    "text": SYSTEM_PROMPT,
                    # Сигнал для prompt caching (AI-07).
                    # OpenAI автоматически кэширует при ≥1024 токенов.
                    "cache_control": {"type": "ephemeral"},
                }
            ],
        }
    ]

    # История разговора из БД (уже отфильтрована сервисом до 20 сообщений)
    for msg in history:
        role = msg.get("role", "user")
        content = msg.get("content") or ""
        # tool messages: role="tool" → передаём как assistant (simplification)
        if role == "tool":
            messages.append({"role": "assistant", "content": content})
        else:
            messages.append({"role": role, "content": content})

    # Новое сообщение пользователя
    messages.append({"role": "user", "content": user_message})

    return messages
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "
from app.ai.tools import get_period_balance, get_category_summary, query_transactions, get_forecast, TOOLS_SCHEMA, TOOL_FUNCTIONS
from app.ai.system_prompt import build_messages, SYSTEM_PROMPT
assert len(TOOLS_SCHEMA) == 4
assert 'get_period_balance' in TOOL_FUNCTIONS
msgs = build_messages([], 'тест')
assert msgs[0]['role'] == 'system'
assert msgs[-1]['content'] == 'тест'
print('ALL OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def get_period_balance" app/ai/tools.py` возвращает 1
    - `grep -c "async def get_category_summary" app/ai/tools.py` возвращает 1
    - `grep -c "async def query_transactions" app/ai/tools.py` возвращает 1
    - `grep -c "async def get_forecast" app/ai/tools.py` возвращает 1
    - `grep "TOOLS_SCHEMA" app/ai/tools.py | grep -c "list\[dict\]"` возвращает 1
    - `grep "TOOL_FUNCTIONS" app/ai/tools.py` возвращает строку с dict
    - `grep "except Exception" app/ai/tools.py | wc -l` возвращает 4 (каждая tool защищена)
    - `grep "cache_control" app/ai/system_prompt.py` возвращает строку с "ephemeral"
    - `grep "build_messages" app/ai/system_prompt.py` возвращает строку def
    - `python -c "from app.ai.tools import TOOLS_SCHEMA; assert len(TOOLS_SCHEMA)==4"` — без ошибок
    - `grep -v "float\|Decimal" app/ai/tools.py | grep "amount" | wc -l` — нет float операций с деньгами
  </acceptance_criteria>
  <done>4 tool-функции + TOOLS_SCHEMA + TOOL_FUNCTIONS + build_messages() готовы</done>
</task>

<task type="auto">
  <name>Задача 2: AI conversation service + Pydantic схемы</name>
  <files>app/services/ai_conversation_service.py, app/api/schemas/ai.py</files>
  <read_first>
    - app/services/analytics.py (строки 1-40 — паттерн async service с db: AsyncSession)
    - app/api/schemas/analytics.py (паттерн BaseModel с ConfigDict(from_attributes=True))
    - app/db/models.py (AiConversation и AiMessage — только что добавленные классы)
  </read_first>
  <action>
**Создать app/services/ai_conversation_service.py:**

```python
"""Сервис управления AI-разговором (AI-06).

Одна глобальная conversation на пользователя (single-tenant).
Последние AI_MAX_CONTEXT_MESSAGES передаются в LLM (старше — хранятся в БД).
Clear = hard delete ai_message строк (conversation строка остаётся).
"""
from __future__ import annotations

from sqlalchemy import delete, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import AiConversation, AiMessage


async def get_or_create_conversation(db: AsyncSession) -> AiConversation:
    """Вернуть единственную conversation, создать если её нет (AI-06)."""
    q = select(AiConversation).limit(1)
    conv = (await db.execute(q)).scalar_one_or_none()
    if conv is None:
        conv = AiConversation()
        db.add(conv)
        await db.flush()  # получить id без commit (commit делает get_db)
    return conv


async def append_message(
    db: AsyncSession,
    conv_id: int,
    role: str,
    content: str | None = None,
    tool_name: str | None = None,
    tool_result: str | None = None,
) -> AiMessage:
    """Добавить сообщение в conversation (AI-06).

    role: 'user' | 'assistant' | 'tool'
    tool_name: имя инструмента (для role='tool')
    tool_result: JSON-строка результата tool (для role='tool')
    """
    msg = AiMessage(
        conversation_id=conv_id,
        role=role,
        content=content,
        tool_name=tool_name,
        tool_result=tool_result,
    )
    db.add(msg)
    await db.flush()
    return msg


async def get_recent_messages(
    db: AsyncSession,
    conv_id: int,
    limit: int = 20,
) -> list[AiMessage]:
    """Вернуть последние limit сообщений conversation (AI-06).

    Используется для формирования LLM-контекста.
    """
    q = (
        select(AiMessage)
        .where(AiMessage.conversation_id == conv_id)
        .order_by(AiMessage.id.desc())
        .limit(limit)
    )
    rows = (await db.execute(q)).scalars().all()
    # Вернуть в хронологическом порядке (oldest first)
    return list(reversed(rows))


async def clear_conversation(db: AsyncSession, conv_id: int) -> None:
    """Hard delete всех сообщений conversation (AI-06).

    Conversation-строка остаётся — только сообщения удаляются.
    """
    await db.execute(
        delete(AiMessage).where(AiMessage.conversation_id == conv_id)
    )
    # Flush без commit (commit делает get_db dependency)
    await db.flush()
```

**Создать app/api/schemas/ai.py:**

```python
"""Pydantic v2 схемы для AI Assistant endpoints (AI-03, AI-06)."""
from __future__ import annotations

from typing import Optional

from pydantic import BaseModel, ConfigDict


class ChatRequest(BaseModel):
    """Тело запроса POST /ai/chat."""

    model_config = ConfigDict(from_attributes=True)

    message: str


class ChatMessageRead(BaseModel):
    """Одно сообщение из истории (ответ GET /ai/history)."""

    model_config = ConfigDict(from_attributes=True)

    id: int
    role: str  # "user" | "assistant" | "tool"
    content: Optional[str] = None
    tool_name: Optional[str] = None
    created_at: str  # ISO datetime строка


class ChatHistoryResponse(BaseModel):
    """Ответ GET /ai/history."""

    model_config = ConfigDict(from_attributes=True)

    messages: list[ChatMessageRead]
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "
from app.services.ai_conversation_service import get_or_create_conversation, append_message, get_recent_messages, clear_conversation
from app.api.schemas.ai import ChatRequest, ChatMessageRead, ChatHistoryResponse
req = ChatRequest(message='тест')
assert req.message == 'тест'
print('ALL OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async def get_or_create_conversation" app/services/ai_conversation_service.py` возвращает 1
    - `grep -c "async def append_message" app/services/ai_conversation_service.py` возвращает 1
    - `grep -c "async def get_recent_messages" app/services/ai_conversation_service.py` возвращает 1
    - `grep -c "async def clear_conversation" app/services/ai_conversation_service.py` возвращает 1
    - `grep "db.commit" app/services/ai_conversation_service.py | wc -l` возвращает 0 (commit делает get_db)
    - `grep "class ChatRequest" app/api/schemas/ai.py` возвращает строку
    - `grep "class ChatMessageRead" app/api/schemas/ai.py` возвращает строку
    - `grep "class ChatHistoryResponse" app/api/schemas/ai.py` возвращает строку
    - `grep "from_attributes=True" app/api/schemas/ai.py | wc -l` возвращает 3
    - `python -c "from app.api.schemas.ai import ChatRequest, ChatHistoryResponse"` — без ошибок
  </acceptance_criteria>
  <done>conversation service и Pydantic схемы готовы; RED тесты test_ai_conversation_service.py переходят в GREEN</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → tools | Tool функции принимают параметры от LLM — нужна валидация inputs |
| tool results → LLM | Данные БД передаются обратно в LLM prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | query_transactions limit | mitigate | `min(limit, 50)` ограничение — LLM не может запросить >50 записей за раз |
| T-09-08 | Information | tool results | accept | Single-tenant, данные принадлежат владельцу — риск раскрытия минимален |
| T-09-09 | Repudiation | AiMessage | accept | Soft delete не используется (hard delete by design); audit через created_at |
| T-09-10 | Denial of Service | get_category_summary | mitigate | is_archived фильтр ограничивает набор категорий; category_id фильтр уменьшает нагрузку |
</threat_model>

<verification>
```bash
cd /Users/exy/pet_projects/tg-budget-planner
python -c "
from app.ai.tools import TOOLS_SCHEMA, TOOL_FUNCTIONS
assert len(TOOLS_SCHEMA) == 4
assert set(TOOL_FUNCTIONS.keys()) == {'get_period_balance', 'get_category_summary', 'query_transactions', 'get_forecast'}
print('tools OK')

from app.ai.system_prompt import build_messages
msgs = build_messages([{'role': 'user', 'content': 'hi'}], 'тест')
assert msgs[0]['role'] == 'system'
assert 'cache_control' in msgs[0]['content'][0]
print('system_prompt OK')

from app.services.ai_conversation_service import get_or_create_conversation
from app.api.schemas.ai import ChatRequest
print('service + schemas OK')
"
```
</verification>

<success_criteria>
- 4 tool-функции + TOOLS_SCHEMA + TOOL_FUNCTIONS экспортируются из app/ai/tools.py
- build_messages() возвращает список с cache_control на системном промпте
- ai_conversation_service.py: 4 функции, нет db.commit()
- app/api/schemas/ai.py: ChatRequest, ChatMessageRead, ChatHistoryResponse с from_attributes=True
- RED тесты из Plan 09-01 (test_tools.py, test_ai_conversation_service.py) переходят в GREEN при наличии DB
</success_criteria>

<output>
После завершения создать `.planning/phases/09-ai-assistant/09-04-SUMMARY.md`
</output>
