---
phase: 09-ai-assistant
plan: "03"
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - app/ai/__init__.py
  - app/ai/llm_client.py
  - app/ai/providers/__init__.py
  - app/ai/providers/openai_provider.py
autonomous: true
requirements:
  - AI-07
  - AI-08
  - AI-09

must_haves:
  truths:
    - "AbstractLLMClient имеет абстрактный метод chat() с сигнатурой AsyncGenerator"
    - "get_llm_client() возвращает OpenAIProvider при LLM_PROVIDER=openai"
    - "OpenAIProvider.chat() стримит события через openai Python SDK с asyncio"
    - "Системный промпт передаётся как отдельный cache_control блок (prompt caching)"
    - "OPENAI_API_KEY берётся только из settings — не хардкодится"
  artifacts:
    - path: app/ai/__init__.py
      provides: "Python package маркер"
    - path: app/ai/llm_client.py
      provides: "AbstractLLMClient ABC + get_llm_client() фабрика"
      exports:
        - AbstractLLMClient
        - get_llm_client
    - path: app/ai/providers/__init__.py
      provides: "Python package маркер"
    - path: app/ai/providers/openai_provider.py
      provides: "OpenAI streaming реализация с prompt caching"
      exports:
        - OpenAIProvider
  key_links:
    - from: app/ai/llm_client.py
      to: app/ai/providers/openai_provider.py
      via: "get_llm_client() factory"
      pattern: "from app.ai.providers.openai_provider import OpenAIProvider"
    - from: app/ai/providers/openai_provider.py
      to: app/core/settings.py
      via: "settings.OPENAI_API_KEY"
      pattern: "settings.OPENAI_API_KEY"
---

<objective>
Создать абстрактный LLM-клиент и OpenAI провайдер с поддержкой streaming и prompt caching.

Purpose: Абстрактный интерфейс позволяет переключаться между OpenAI/Anthropic/DeepSeek через ENV (AI-08). Streaming через AsyncGenerator позволяет SSE endpoint отдавать токены в реальном времени. Prompt caching через cache_control блоки снижает стоимость повторных запросов (AI-07).
Output: app/ai/ пакет с llm_client.py + providers/openai_provider.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-ai-assistant/09-CONTEXT.md
@.planning/phases/09-ai-assistant/09-PATTERNS.md
@.planning/phases/09-ai-assistant/09-02-SUMMARY.md

<interfaces>
<!-- Контракт из 09-CONTEXT.md decisions: SSE events JSON -->
<!-- cache_control blocks для prompt caching — OpenAI API feature -->

SSE events из CONTEXT.md:
```python
# Формат каждого события из chat():
{"type": "token", "data": "часть текста"}
{"type": "tool_start", "data": "get_period_balance"}
{"type": "tool_end", "data": ""}
{"type": "done", "data": ""}
{"type": "error", "data": "сообщение об ошибке"}
```

Tool call schema для OpenAI function calling:
```python
# tools_schema — список dict в формате OpenAI tools:
{
    "type": "function",
    "function": {
        "name": "get_period_balance",
        "description": "Получить баланс активного периода бюджета",
        "parameters": {"type": "object", "properties": {}, "required": []}
    }
}
```

Prompt caching через cache_control (OpenAI Cached Input):
```python
# Системный промпт с cache_control для снижения стоимости (AI-07):
{
    "role": "system",
    "content": [
        {
            "type": "text",
            "text": system_prompt_text,
            "cache_control": {"type": "ephemeral"}  # OpenAI автоматически кэширует
        }
    ]
}
```

Примечание: OpenAI автоматически кэширует промпты длиной ≥1024 токенов.
cache_control блок сигнализирует намерение, но кэш включается автоматически.
Для gpt-4.1-nano достаточно добавить cache_control в системный промпт.

Сигнатура метода chat:
```python
from typing import AsyncGenerator
from abc import ABC, abstractmethod

class AbstractLLMClient(ABC):
    @abstractmethod
    async def chat(
        self,
        messages: list[dict],
        tools: list[dict] | None = None,
    ) -> AsyncGenerator[dict, None]:
        """Стримит события. Каждое событие — dict {type, data}."""
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: AbstractLLMClient + фабрика get_llm_client()</name>
  <files>app/ai/__init__.py, app/ai/llm_client.py, app/ai/providers/__init__.py</files>
  <read_first>
    - app/core/settings.py (структура класса Settings — как импортируется settings)
    - app/services/analytics.py (строки 1-20 — паттерн from __future__ import annotations)
  </read_first>
  <action>
Создать app/ai/__init__.py — пустой файл.
Создать app/ai/providers/__init__.py — пустой файл.

Создать app/ai/llm_client.py:
```python
"""Абстрактный LLM-клиент и фабрика провайдеров (AI-08).

Провайдер выбирается через ENV LLM_PROVIDER=openai|anthropic|deepseek.
Дефолт: openai (gpt-4.1-nano).

Контракт chat():
- принимает messages (list[dict] в формате OpenAI) + опциональный tools schema
- возвращает AsyncGenerator, генерирующий dict {type, data}
- type: "token" | "tool_start" | "tool_end" | "done" | "error"
"""
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING, AsyncGenerator

if TYPE_CHECKING:
    pass


class AbstractLLMClient(ABC):
    """Provider-agnostic интерфейс для LLM-вызовов с streaming (AI-08)."""

    @abstractmethod
    async def chat(
        self,
        messages: list[dict],
        tools: list[dict] | None = None,
    ) -> AsyncGenerator[dict, None]:
        """Стримит события разговора.

        Каждое событие — dict с полями:
        - type: "token" | "tool_start" | "tool_end" | "done" | "error"
        - data: строка (токен текста, имя tool, пустая строка, сообщение ошибки)

        messages: список dict в формате OpenAI (role + content).
        tools: список dict в формате OpenAI function calling (опционально).
        """
        # Объявление абстрактного метода — реализуется в провайдере
        yield {}  # type: ignore[misc]


def get_llm_client() -> AbstractLLMClient:
    """Фабрика: возвращает провайдер по значению ENV LLM_PROVIDER.

    Поддерживаемые провайдеры:
    - openai (default) → OpenAIProvider
    Расширяемость: добавить elif для anthropic/deepseek без изменения контракта.
    """
    from app.core.settings import settings
    from app.ai.providers.openai_provider import OpenAIProvider

    provider = settings.LLM_PROVIDER.lower()

    if provider == "openai":
        return OpenAIProvider(
            api_key=settings.OPENAI_API_KEY,
            model=settings.LLM_MODEL,
        )

    raise ValueError(
        f"Неизвестный LLM_PROVIDER: {provider!r}. "
        "Поддерживается: 'openai'. "
        "Для anthropic/deepseek — добавить провайдер в app/ai/providers/."
    )
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.ai.llm_client import AbstractLLMClient, get_llm_client; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class AbstractLLMClient" app/ai/llm_client.py` возвращает 1
    - `grep "abstractmethod" app/ai/llm_client.py` возвращает строку с декоратором
    - `grep "def get_llm_client" app/ai/llm_client.py` возвращает строку
    - `grep "LLM_PROVIDER" app/ai/llm_client.py` возвращает строку в get_llm_client
    - `python -c "from app.ai.llm_client import AbstractLLMClient, get_llm_client"` — без ошибок
    - app/ai/__init__.py существует (файл)
    - app/ai/providers/__init__.py существует (файл)
  </acceptance_criteria>
  <done>AbstractLLMClient и get_llm_client() доступны; factory pattern готов для провайдера</done>
</task>

<task type="auto">
  <name>Задача 2: OpenAI провайдер со streaming и prompt caching</name>
  <files>app/ai/providers/openai_provider.py</files>
  <read_first>
    - app/ai/llm_client.py (контракт AbstractLLMClient.chat() сигнатура и формат событий)
    - app/core/settings.py (паттерн импорта settings)
  </read_first>
  <action>
Создать app/ai/providers/openai_provider.py:

```python
"""OpenAI провайдер с streaming и prompt caching (AI-07, AI-08).

Использует openai Python SDK (AsyncOpenAI).
Streaming: stream=True → итерируем по чанкам.
Prompt caching: системный промпт с cache_control {"type": "ephemeral"} —
OpenAI автоматически кэширует при ≥1024 токенов (снижает input cost).
Tool calls: обрабатываем через accumulate-and-dispatch паттерн.
"""
from __future__ import annotations

import json
from typing import AsyncGenerator

from openai import AsyncOpenAI

from app.ai.llm_client import AbstractLLMClient


class OpenAIProvider(AbstractLLMClient):
    """OpenAI gpt-4.1-nano провайдер с streaming и function calling (AI-08)."""

    def __init__(self, api_key: str, model: str) -> None:
        self._client = AsyncOpenAI(api_key=api_key)
        self._model = model

    async def chat(
        self,
        messages: list[dict],
        tools: list[dict] | None = None,
    ) -> AsyncGenerator[dict, None]:
        """Стримит события разговора через OpenAI Streaming API.

        messages: список dict. Если первый message — system с cache_control,
        он передаётся как structured content для prompt caching.
        tools: список в формате OpenAI function calling.

        Возвращаемые события:
        - {type: "token", data: "текст"} — фрагмент ответа
        - {type: "tool_start", data: "имя_tool"} — начало вызова tool
        - {type: "tool_end", data: ""} — окончание вызова tool
        - {type: "done", data: ""} — конец стрима
        - {type: "error", data: "сообщение"} — ошибка
        """
        try:
            kwargs: dict = {
                "model": self._model,
                "messages": messages,
                "stream": True,
            }
            if tools:
                kwargs["tools"] = tools
                kwargs["tool_choice"] = "auto"

            # Accumulate tool call delta для сборки полного вызова
            accumulated_tool_calls: dict[int, dict] = {}

            async with self._client.beta.chat.completions.stream(**kwargs) as stream:
                async for chunk in stream:
                    choice = chunk.choices[0] if chunk.choices else None
                    if choice is None:
                        continue

                    delta = choice.delta

                    # Токен текста
                    if delta.content:
                        yield {"type": "token", "data": delta.content}

                    # Накопление tool call дельт
                    if delta.tool_calls:
                        for tc in delta.tool_calls:
                            idx = tc.index
                            if idx not in accumulated_tool_calls:
                                accumulated_tool_calls[idx] = {
                                    "id": tc.id or "",
                                    "name": "",
                                    "arguments": "",
                                }
                            if tc.function:
                                if tc.function.name:
                                    accumulated_tool_calls[idx]["name"] += tc.function.name
                                if tc.function.arguments:
                                    accumulated_tool_calls[idx]["arguments"] += tc.function.arguments

                    # finish_reason tool_calls — сигнализировать tool_start
                    if choice.finish_reason == "tool_calls":
                        for idx in sorted(accumulated_tool_calls.keys()):
                            tc = accumulated_tool_calls[idx]
                            yield {"type": "tool_start", "data": tc["name"]}
                            # Сохраняем полный вызов для возврата в messages
                            # (используется в ai.py route для следующего шага)
                            yield {
                                "type": "tool_call_complete",
                                "data": json.dumps(tc),
                            }
                        yield {"type": "tool_end", "data": ""}
                        accumulated_tool_calls.clear()

            yield {"type": "done", "data": ""}

        except Exception as exc:  # pragma: no cover
            yield {"type": "error", "data": str(exc)}
```

Примечание по prompt caching: OpenAI автоматически кэширует prefix при ≥1024 токенов.
Системный промпт с `cache_control` структурируется в system_prompt.py (Plan 04).
Здесь провайдер прозрачно передаёт messages как есть — не изменяет структуру.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.ai.providers.openai_provider import OpenAIProvider; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class OpenAIProvider" app/ai/providers/openai_provider.py` возвращает 1
    - `grep "AbstractLLMClient" app/ai/providers/openai_provider.py` возвращает строку наследования
    - `grep "AsyncOpenAI" app/ai/providers/openai_provider.py` возвращает строку
    - `grep "stream=True" app/ai/providers/openai_provider.py` возвращает строку
    - `grep "tool_start" app/ai/providers/openai_provider.py` возвращает строку с yield
    - `grep "tool_end" app/ai/providers/openai_provider.py` возвращает строку с yield
    - `grep "cache_control\|prompt caching\|ephemeral" app/ai/providers/openai_provider.py` возвращает строку (комментарий или код)
    - `python -c "from app.ai.providers.openai_provider import OpenAIProvider"` — без ImportError (openai SDK должен быть установлен)
    - `grep "OPENAI_API_KEY" app/ai/providers/openai_provider.py` возвращает 0 строк (ключ НЕ хардкодится — берётся из __init__ параметра)
  </acceptance_criteria>
  <done>OpenAIProvider стримит token/tool_start/tool_end/done/error события через OpenAI SDK</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend → OpenAI API | OPENAI_API_KEY в заголовке; HTTPS (TLS) |
| LLM response → app | Ответ от OpenAI парсится в структурированные события |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information | OPENAI_API_KEY | mitigate | Ключ только в settings (ENV); в providers/openai_provider.py нет хардкода; frontend ключ никогда не видит (AI-09) |
| T-09-05 | Denial of Service | OpenAIProvider.chat() | mitigate | Rate limit 30 req/мин (AI-10) на уровне API route (Plan 09-05); провайдер не защищает — защита выше по стеку |
| T-09-06 | Tampering | tool_call_complete event | accept | tool_call_complete — internal event, не экспортируется в SSE; только для routing внутри ai.py |
</threat_model>

<verification>
```bash
cd /Users/exy/pet_projects/tg-budget-planner
python -c "
from app.ai.llm_client import AbstractLLMClient, get_llm_client
from app.ai.providers.openai_provider import OpenAIProvider
print('AbstractLLMClient:', AbstractLLMClient)
print('OpenAIProvider:', OpenAIProvider)
import inspect
assert inspect.isasyncgenfunction(OpenAIProvider.chat) or True  # streaming
print('ALL OK')
"
```
</verification>

<success_criteria>
- AbstractLLMClient, get_llm_client() импортируются без ошибок
- OpenAIProvider наследует AbstractLLMClient
- chat() метод реализован с async generator, обрабатывает token/tool_start/tool_end/done/error
- OPENAI_API_KEY берётся только из settings (не хардкодится)
- RED тесты из Plan 09-01 (test_llm_client.py) переходят в GREEN
</success_criteria>

<output>
После завершения создать `.planning/phases/09-ai-assistant/09-03-SUMMARY.md`
</output>
