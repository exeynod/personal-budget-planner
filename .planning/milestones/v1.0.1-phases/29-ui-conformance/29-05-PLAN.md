---
phase: 29-ui-conformance
plan: 05
type: execute
wave: 4
depends_on:
  - "29-04"
files_modified:
  - frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/
  - .planning/v1.0-handoff/DIVERGENCES.md
autonomous: true
requirements:
  - UICONF-04
  - UICONF-05
must_haves:
  truths:
    - "Если plan 29-04 применил fix-ы которые меняют визуал — Playwright pixel snapshots regenerated и snapshot test re-run зелёный."
    - "DIVERGENCES.md содержит новые WARNING/INFO записи (W-06+, I-06+, X-03+) для каждого WARNING/INFO finding из UI-REVIEW.md, включая downgraded из plan 29-04."
    - "Каждая новая DIVERGENCES.md запись имеет формат `### W-NN — <title>` с полями Where / Reality / Decision / Future-work-tag (v1.1)."
    - "DIVERGENCES.md `Last updated` header field обновлён."
  artifacts:
    - path: "frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/"
      provides: "Refreshed PNGs (если нужно) — 8 файлов, possibly updated bytes."
      contains: "pixel-home"
    - path: ".planning/v1.0-handoff/DIVERGENCES.md"
      provides: "Updated с appendix WARNING/INFO findings из UI-REVIEW.md."
      contains: "Phase 29 audit"
  key_links:
    - from: "DIVERGENCES.md new W-/I-/X- entries"
      to: "UI-REVIEW.md WARNING/INFO findings"
      via: "Each new DIVERGENCES row references UI-REVIEW.md screen-section"
      pattern: "UI-REVIEW.md"
---

<objective>
Финальная wave Phase 29: (a) re-snapshot pixel baselines если plan 29-04
изменил визуал, (b) перенести все WARNING/INFO findings из UI-REVIEW.md
в DIVERGENCES.md как formal v1.1 backlog entries.

Purpose: Snapshot freshness — иначе следующий регресс-run в Phase 31
сравнит против stale baseline и упадёт. DIVERGENCES.md — single source
of truth для accepted v1.0.1 deviations; без этого WARNING/INFO findings
останутся «orphaned» в UI-REVIEW.md и забудутся.

Output: Updated PNG baselines (or no-op если 29-04 ничего визуального не
поменял) + DIVERGENCES.md с новыми W-/I-/X- entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-ui-conformance/29-CONTEXT.md
@.planning/phases/29-ui-conformance/UI-REVIEW.md
@.planning/phases/29-ui-conformance/29-04-FIX-LOG.md
@.planning/phases/29-ui-conformance/29-04-SUMMARY.md
@.planning/v1.0-handoff/DIVERGENCES.md
@frontend/tests/e2e/v10-pixel-snapshots.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-snapshot pixel baselines if 29-04 changed visuals</name>
  <files>frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/</files>
  <read_first>
    - .planning/phases/29-ui-conformance/29-04-SUMMARY.md (определяет «требуют snapshot regen» list)
    - .planning/phases/29-ui-conformance/29-04-FIX-LOG.md (manifest commit hashes)
  </read_first>
  <acceptance_criteria>
    - Один из двух возможных state-ов:
      - **(A) 29-04 изменил визуал** (FIX-LOG.md fixed_count > 0 И fixes touched files в `frontend/src/screensV10/` / `frontend/src/styles/`): snapshots regenerated, `npx playwright test v10-pixel-snapshots --project=chromium-mobile` re-run зелёный без `--update-snapshots`.
      - **(B) 29-04 был no-op** (zero-blockers OR all downgraded OR fixes были iOS-only): no snapshot change, `git status` для PNGs clean.
    - В обоих case re-run без `--update-snapshots` зелёный (если (B), это уже было true; если (A), новые baselines включены).
  </acceptance_criteria>
  <action>
    1. **Determine if regen needed:**
       - Read `29-04-SUMMARY.md` field `fixes_requiring_snapshot_regen` (per плана 29-04 SUMMARY format).
       - ИЛИ просто проверь: `git log --oneline --since="2 hours ago" --grep="fix(ui-conf):" -- frontend/src/screensV10/ frontend/src/styles/` — если пусто → no regen.

    2. **If regen needed (case A):**
       - Убедись что Vite dev + docker stack up.
       - `cd frontend && npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile --update-snapshots`.
       - Подтверди что 8 PNG в `tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/` — `git diff --stat` покажет изменённые files.
       - Re-run без `--update-snapshots`: `npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile` — must pass green.
       - `git add frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/`.

    3. **If no regen (case B):** No action; verify `git diff --stat frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/` clean.

    Per UICONF-04 verify (re-snapshot green после fixes).
  </action>
  <verify>
    <automated>cd frontend && npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile --reporter=line 2>&1 | grep -qE "8 passed|all .* tests passed"</automated>
  </verify>
  <done>
    Snapshots актуальны (regen if needed) и Playwright spec зелёный без `--update-snapshots`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append WARNING/INFO findings to DIVERGENCES.md</name>
  <files>.planning/v1.0-handoff/DIVERGENCES.md</files>
  <read_first>
    - .planning/v1.0-handoff/DIVERGENCES.md (full — для определения next available IDs: текущие W-01..W-05, I-01..I-05, X-01..X-02 → start с W-06, I-06, X-03)
    - .planning/phases/29-ui-conformance/UI-REVIEW.md (full — WARNING+INFO findings, включая downgraded из 29-04)
    - .planning/phases/29-ui-conformance/29-04-FIX-LOG.md (downgraded list)
  </read_first>
  <acceptance_criteria>
    - DIVERGENCES.md `Last updated:` field в header обновлён (today's date `2026-05-11` или `2026-05-12` если plan 29 уже занял два дня).
    - Каждая WARNING ИЛИ INFO finding из UI-REVIEW.md либо имеет соответствующую новую row в DIVERGENCES.md, либо явно списана как «duplicate of W-NN» / «duplicate of I-NN» с inline comment в UI-REVIEW.md.
    - Новые W-06+ rows под `## Web Divergences` секцией; новые I-06+ под `## iOS Divergences`; новые X-03+ под `## Cross-Platform Divergences`.
    - Каждая новая row имеет формат:
      ```
      ### W-NN — <short title>

      - **Where:** <file:line OR component name>
      - **Prototype/Spec:** <expected value from prototype/index.html OR DESIGN-SYSTEM.md §X>
      - **Reality:** <implemented value>
      - **Visual impact:** <subjective severity>
      - **Decision:** accepted v1.0.1 → defer to v1.1 (`POL-V11-NN`)
      ```
    - Section `## Future Work (not POL-04 scope)` extended с новыми v1.1 backlog items если applicable.
    - DIVERGENCES.md остаётся valid markdown (no broken anchors).
  </acceptance_criteria>
  <action>
    1. **Enumerate next IDs.** Read DIVERGENCES.md, find max W-NN, I-NN, X-NN. Default starts: W-06, I-06, X-03.

    2. **For каждой WARNING + INFO finding в UI-REVIEW.md** (parse `## Web` + `## iOS` секции):
       - Determine target section: web → `## Web Divergences`, iOS → `## iOS Divergences`, cross-platform (если finding в обеих) → `## Cross-Platform Divergences`.
       - Generate next ID (W-06, W-07, ... independently from I-06, I-07, ...).
       - Compose entry в формате выше (Where / Spec / Reality / Visual impact / Decision).
       - Append к соответствующей секции (insert перед `---` separator до `## iOS Divergences` etc.).

    3. **Update header:**
       - `**Last updated:** 2026-05-11 (Phase 29-05)`.
       - `**Audit basis:**` extend: `..., Phase 29 conformance audit (web + iOS) 2026-05-11`.

    4. **Update `## Future Work` section** в конце файла:
       - Если есть subset finding которое естественно тегается «v1.1 work» (например «Subset DM Serif glyph») — добавить пункт.
       - Иначе: добавить generic `Phase 29 audit findings W-06..W-NN / I-06..I-NN — addressed in v1.1 design polish phase`.

    5. **Cross-link UI-REVIEW.md → DIVERGENCES.md:**
       - Edit UI-REVIEW.md: для каждой WARNING/INFO добавить в конце finding строку `_Logged as DIVERGENCES.md §W-NN_` (или I-NN / X-NN).

    Per UICONF-05.
  </action>
  <verify>
    <automated>grep -c "^### [WIX]-[0-9]" .planning/v1.0-handoff/DIVERGENCES.md | awk '{ exit !($1 + 0 >= 12) }' && grep -q "Phase 29 conformance audit" .planning/v1.0-handoff/DIVERGENCES.md && grep -q "Last updated.*2026-05-1" .planning/v1.0-handoff/DIVERGENCES.md</automated>
  </verify>
  <done>
    DIVERGENCES.md обновлён с новыми W-/I-/X- entries; UI-REVIEW.md
    findings cross-linked. UICONF-05 satisfied.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile` → 8 passed.
2. `grep -c "^### [WIX]-[0-9]" .planning/v1.0-handoff/DIVERGENCES.md` ≥ 12 (initial 12 = W1-5 + I1-5 + X1-2; new entries push higher).
3. `grep "Phase 29 conformance audit" .planning/v1.0-handoff/DIVERGENCES.md` присутствует.
4. `grep -c "Logged as DIVERGENCES.md" .planning/phases/29-ui-conformance/UI-REVIEW.md` равно WARNING+INFO count из UI-REVIEW Summary.
</verification>

<success_criteria>
- Snapshots актуальны и Playwright re-run зелёный.
- DIVERGENCES.md содержит full v1.0.1 audit findings.
- Phase 29 готов к close — все 5 UICONF requirements satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-conformance/29-05-SUMMARY.md` documenting:
- Snapshot regen state (case A или B).
- Counts of new DIVERGENCES.md entries: W-NN..W-MM, I-NN..I-MM, X-NN..X-MM.
- Final UICONF-01..05 traceability table (each REQ → плана где satisfied).
- Recommendation: ready for `/gsd-verify-phase 29` либо direct `/gsd-plan-phase 30`.
</output>
