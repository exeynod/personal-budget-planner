---
phase: 30-tech-debt
plan: 02
type: execute
wave: 2
depends_on:
  - "30-07"
files_modified:
  - frontend/src/screensV10/AddSheet/AddSheet.tsx
  - frontend/src/screensV10/Home/HomeMount.tsx
  - frontend/src/screensV10/Transactions/TransactionsMount.tsx
  - frontend/src/screensV10/AddSheet/AccountPickerSheet.tsx
  - frontend/src/screensV10/AddSheet/AccountPickerSheet.module.css
autonomous: true
requirements:
  - DEBT-02
  - DEBT-03
must_haves:
  truths:
    - "DEBT-02 (web): AddSheet onSubmitted callback fires → HomeMount + TransactionsMount refetch (refetchToken increments)."
    - "data-testid=\"parent-refetched\" updates after submit."
    - "DEBT-03 (web): Account selection — bottom-sheet с list of accounts (name + balance + ОСНОВНОЙ badge); replaces row-cycler."
    - "tsc clean, vitest green."
  artifacts:
    - path: ".planning/phases/30-tech-debt/30-02-SUMMARY.md"
      contains: "DEBT-02"
      min_lines: 12
---

<objective>
Two related web fixes: AddSheet refetch trigger + new AccountPickerSheet (replaces row-cycler).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tech-debt/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AddSheet refetch wiring (DEBT-02)</name>
  <files>
    frontend/src/screensV10/AddSheet/AddSheet.tsx
    frontend/src/screensV10/Home/HomeMount.tsx
    frontend/src/screensV10/Transactions/TransactionsMount.tsx
  </files>
  <action>
    1. AddSheet.tsx — existing `onSubmitted` callback prop. Найти после successful create.
    2. V10MainShell.tsx или AppV10.tsx — где AddSheet рендерится — пробросить state `[refetchToken, setRefetchToken]`. onSubmitted → setRefetchToken(t => t+1).
    3. HomeMount.tsx — add `refetchToken?: number` prop, include в useEffect deps.
    4. TransactionsMount.tsx — same.
    5. Add `data-testid="parent-refetched"` to root div with value `{refetchToken}`.
  </action>
  <done>AddSheet submit → parent Mounts refetch.</done>
</task>

<task type="auto">
  <name>Task 2: AccountPickerSheet (DEBT-03)</name>
  <files>
    frontend/src/screensV10/AddSheet/AccountPickerSheet.tsx (new)
    frontend/src/screensV10/AddSheet/AccountPickerSheet.module.css (new)
    frontend/src/screensV10/AddSheet/AddSheet.tsx
  </files>
  <action>
    1. Create `AccountPickerSheet.tsx` — uses `PosterSheet` from `../common`. Body: list of accounts (rows: name + balance + «ОСНОВНОЙ» badge if `is_default`).
    2. tap row → onSelect(accountId) + close.
    3. AddSheet.tsx — replace existing row-cycler с `<button onClick={openPicker}>{currentAccountName}</button>` + `<AccountPickerSheet ... />`.
    4. CSS: rows match существующий PosterSheet style; selected has subtle ✓ indicator.
  </action>
  <done>AccountPicker bottom-sheet replaces row-cycler.</done>
</task>

<task type="auto">
  <name>Task 3: Verify + commit + SUMMARY</name>
  <action>
    `npx vitest run frontend/src/screensV10/AddSheet/` + tsc. Commit `feat(30-02): AddSheet refetch + AccountPickerSheet (DEBT-02+03 web)`. SUMMARY.
  </action>
</task>

</tasks>

<verification>
1. Vitest AddSheet/Home/Transactions tests green.
2. tsc clean.
3. SUMMARY.
</verification>

<output>30-02-SUMMARY.md per template.</output>
