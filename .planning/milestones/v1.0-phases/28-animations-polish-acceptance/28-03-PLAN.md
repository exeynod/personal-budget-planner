---
phase: 28-animations-polish-acceptance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/v10-pixel-snapshots.spec.ts
  - frontend/tests/e2e/__screenshots__/v10-pixel/.gitkeep
  - .planning/v1.0-handoff/DIVERGENCES.md
autonomous: true
requirements: [POL-04]
tags: [polish, pixel-perfect, screenshots, divergences]

must_haves:
  truths:
    - "Playwright snapshot test покрывает 8 ключевых V10 экранов (Home, Transactions, AddSheet, CategoryDetail, PlanMonth, Subscriptions, Savings, AI initial) и ре-run проходит зелёным."
    - "DIVERGENCES.md существует, описывает known divergences от prototype/index.html (cyrillic dual-font fallback ADR-001, iOS safe-area, easing approximations) + iOS manual screenshot checklist для acceptance."
  artifacts:
    - path: "frontend/tests/e2e/v10-pixel-snapshots.spec.ts"
      provides: "8 toHaveScreenshot tests"
      min_lines: 60
    - path: "frontend/tests/e2e/__screenshots__/v10-pixel/.gitkeep"
      provides: "directory committed (baseline PNGs land on first run)"
    - path: ".planning/v1.0-handoff/DIVERGENCES.md"
      provides: "documented divergences + iOS visual QA checklist"
      min_lines: 80
  key_links:
    - from: "v10-pixel-snapshots.spec.ts"
      to: "frontend/playwright.config.ts"
      via: "uses existing webServer + Pixel 5 viewport"
      pattern: "expect\\(page\\)\\.toHaveScreenshot"
    - from: "DIVERGENCES.md"
      to: ".planning/v1.0-handoff/handoff/prototype/index.html"
      via: "explicit references to prototype line ranges + ADR-001/-002"
      pattern: "prototype/"
---

<objective>
Pixel-perfect side-by-side QA: web — Playwright `toHaveScreenshot()` baseline для 8 ключевых V10 экранов; iOS — DIVERGENCES.md с manual screenshot checklist для acceptance + документация известных расхождений (ADR-001 cyrillic dual-font, ADR-002 PosterNavStack vs UIKit nav, iOS safe-area, SwiftUI spring vs CSS cubic-bezier).

Purpose: POL-04 (web и iOS).
Output: один Playwright spec, baseline PNG-снапшоты (созданы при первом run), DIVERGENCES.md в handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-animations-polish-acceptance/28-CONTEXT.md
@.planning/phases/28-animations-polish-acceptance/28-must-haves.md
@frontend/playwright.config.ts

<interfaces>
<!-- Existing Playwright config: -->
- viewport: 393×851 (Pixel 5 device preset)
- baseURL: http://localhost:5173
- workers: 1, fullyParallel: false
- Snapshot output по умолчанию: tests/e2e/__screenshots__/{spec-name}/{test-name}-{platform}.png

<!-- Theme switch: -->
- localStorage.setItem('ui.theme', 'v10') OR import.meta.env.VITE_UI_THEME='v10' enables AppV10 root.
- Existing pattern в onboarding-v10.spec.ts / v10-phase25-acceptance.spec.ts.

<!-- iOS reference (для DIVERGENCES.md): -->
- ADR-001 location: PROJECT.md или handoff/DESIGN-SYSTEM.md (cyrillic fallback strategy).
- ADR-002 location: PROJECT.md (PosterNavStack manual edge-swipe).
- prototype: .planning/v1.0-handoff/handoff/prototype/index.html (single-page reference).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v10-pixel-snapshots.spec.ts (8 screens) + .gitkeep</name>
  <files>frontend/tests/e2e/v10-pixel-snapshots.spec.ts, frontend/tests/e2e/__screenshots__/v10-pixel/.gitkeep</files>
  <action>
1. Создай `__screenshots__/v10-pixel/.gitkeep` — пустой файл, чтобы коммитнуть директорию (baseline PNG-ки добавятся самим Playwright первый запуском под `--update-snapshots`).

2. Создай spec `v10-pixel-snapshots.spec.ts`. 8 экранов:

```ts
import { test, expect } from '@playwright/test';

// Phase 28 POL-04 — pixel-perfect baseline для 8 V10 экранов.
// Запускается НЕ параллельно (workers:1 в config). Первый run требует
// `--update-snapshots` для генерации baseline.
//
// Baseline хранится в __screenshots__/v10-pixel-snapshots.spec.ts/{name}-chromium-mobile-darwin.png.

const SCREENS: Array<{ name: string; setup: (page: any) => Promise<void> }> = [
  {
    name: 'home',
    setup: async (page) => {
      await page.goto('/');
      await page.waitForSelector('.poster-rise-in', { state: 'visible', timeout: 5000 });
      // Disable count-up для детерминизма snapshot.
      await page.addStyleTag({ content: `* { animation-duration: 0s !important; transition-duration: 0s !important; }` });
      await page.waitForTimeout(200);
    },
  },
  { name: 'transactions',     setup: async (page) => navigate(page, 'transactions') },
  { name: 'add-sheet',        setup: async (page) => navigate(page, 'add') },
  { name: 'category-detail',  setup: async (page) => navigate(page, 'category') },
  { name: 'plan-month',       setup: async (page) => navigate(page, 'plan') },
  { name: 'subscriptions',    setup: async (page) => navigate(page, 'subs') },
  { name: 'savings',          setup: async (page) => navigate(page, 'savings') },
  { name: 'ai-initial',       setup: async (page) => navigate(page, 'ai') },
];

async function navigate(page: any, target: string) {
  await page.goto('/');
  // ... минимальная навигационная последовательность для каждого target.
  // Если в проекте есть test-helper для V10 routes — используй его.
  // Если нет — добавь fallback через usePosterRouter dev hook ИЛИ через ?route=savings query param.
  await page.addStyleTag({ content: `* { animation-duration: 0s !important; transition-duration: 0s !important; }` });
  await page.waitForTimeout(300);
}

test.beforeEach(async ({ page }) => {
  await page.addInitScript(() => localStorage.setItem('ui.theme', 'v10'));
});

for (const screen of SCREENS) {
  test(`pixel: ${screen.name}`, async ({ page }) => {
    await screen.setup(page);
    await expect(page).toHaveScreenshot(`${screen.name}.png`, {
      maxDiffPixelRatio: 0.02, // 2% tolerance — for sub-pixel font AA differences across CI/dev.
      fullPage: true,
    });
  });
}
```

3. Запусти один раз с `--update-snapshots` чтобы создать baseline:
```bash
cd /Users/exy/pet_projects/tg-budget-planner/frontend && \
npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --update-snapshots
```
4. Закоммить baseline PNG-ки в `__screenshots__/v10-pixel-snapshots.spec.ts/`.

ВАЖНО:
- Если какой-то экран не маунтится без mocked данных (auth, API) — используй существующие fixtures pattern из `tests/e2e/v10-phase25-acceptance.spec.ts`. Если экран принципиально требует interactive setup которого нет — пропусти его И задокументируй пропуск в DIVERGENCES.md (Task 2). Лучше иметь 6 working snapshot tests чем 0 рабочих.
- Платформа в имени файла снапшота — `chromium-mobile-darwin` (или `linux` на CI). На разных хостах baseline разный — рассмотри `--ignore-snapshot-suffix` ИЛИ добавление в .gitignore platform-suffixed файлов с keep-one-platform правилом. Для solo-dev workflow на macOS: коммитим `darwin` baseline; на CI просто пропускаем тест (skip if process.platform !== 'darwin') ИЛИ добавляем otto-baseline через CI step. Зафиксируй выбор в SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend &amp;&amp; npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --reporter=list</automated>
  </verify>
  <done>
- Spec файл создан.
- `__screenshots__/v10-pixel-snapshots.spec.ts/` содержит ≥ 6 baseline PNG (8 ideal; задокументируй любой пропущенный экран в DIVERGENCES.md).
- Re-run без `--update-snapshots` проходит зелёным.
  </done>
</task>

<task type="auto">
  <name>Task 2: DIVERGENCES.md (web + iOS hybrid + iOS visual QA checklist)</name>
  <files>.planning/v1.0-handoff/DIVERGENCES.md</files>
  <action>
Создай файл `.planning/v1.0-handoff/DIVERGENCES.md` со следующей структурой:

```markdown
# DIVERGENCES.md — v1.0 Maximal Poster Implementation vs Prototype

**Last updated:** 2026-05-10 (Phase 28-03)
**Reference:** `.planning/v1.0-handoff/handoff/prototype/index.html`
**Audit basis:** Phase 28-03 pixel-perfect QA (web automated, iOS manual).

This document catalogs **known, accepted divergences** between the implemented v1.0 UI and the original prototype. These are NOT bugs — they're conscious tradeoffs documented at planning time (ADR-001/-002) or surfaced during pixel-QA in Phase 28.

---

## Web Divergences

### W-01 — DM Serif Italic Cyrillic Fallback (ADR-001)
- **Where:** any italic Mass header rendering Cyrillic text («Май», «Подписки.», «Копилка.», «Месяц.», «Счета.», «Реестр.»).
- **Prototype:** uses DM Serif Display Italic for both Latin and Cyrillic.
- **Reality:** DM Serif Display ships NO Cyrillic subset on Google Fonts. Solution: dual-font via `unicode-range` — DM Serif Italic for U+0020..U+024F (Latin), PT Serif Italic for U+0400..U+04FF (Cyrillic).
- **Visual impact:** glyph shapes слегка различаются между Latin/Cyrillic в одной строке. Most visible на mixed strings.
- **Decision:** accepted at v1.0 ADR-001. Designer review pre-shipping recommended for §14.7 («нет видимого FOUT»).

### W-02 — Snapshot tolerance 2% (sub-pixel AA)
- **Where:** all 8 baseline pixel-snapshot tests (Plan 28-03 task 1).
- **Reality:** `toHaveScreenshot({ maxDiffPixelRatio: 0.02 })` accepts ≤ 2% pixel differences. Sub-pixel font AA varies across machines / Chromium minor versions.
- **Decision:** acceptable for solo-dev workflow на macOS-only baseline; CI may regenerate or skip.

### W-03 — Animation duration zeroed in snapshots
- **Where:** all snapshot tests inject `* { animation-duration: 0s !important; transition-duration: 0s !important; }` для детерминизма.
- **Decision:** snapshot фиксирует FINAL state, не in-flight кадр. Это правильно — пиксельную верность для motion-frames мы не валидируем.

---

## iOS Divergences

### I-01 — DM Serif Cyrillic Fallback (ADR-001)
- Same root cause as W-01.
- **Reality:** iOS bundles a single PT Serif Italic for **all** italic Mass headers (no dual-font hybrid).
- **Visual impact:** Latin text в italic Mass header выглядит ChunkPT-Serif-style вместо DM-Serif-style; сравнение с web показывает очевидное смещение в weight + character spacing.
- **Decision:** pragmatic fallback per ADR-001; web vs iOS будут немного отличаться на acceptance §14.7 — это OK.

### I-02 — Custom PosterNavStack vs UIKit Nav (ADR-002)
- **Where:** push/pop transitions across all V10 screens.
- **Prototype:** N/A (web prototype uses CSS transitions).
- **Reality:** SwiftUI `NavigationStack` cannot be styled to match `posterSlideInFwd` (28px slide + 420ms easeOut). Custom 50-LOC `PosterNavStack` (ZStack + asymmetric transitions + @Observable router) replaces it. Edge-swipe-back is `UIScreenEdgePanGestureRecognizer` via UIViewRepresentable (minimumDistance 24, threshold 80px).
- **Visual impact:** ≈ matches web slide-in; edge-swipe behaves identically to UIKit native back gesture.
- **Decision:** accepted at ADR-002.

### I-03 — Easing curve approximation (SwiftUI spring vs CSS cubic-bezier)
- **Where:** `posterTabPop` uses `.spring(response: 0.45, dampingFraction: 0.55)` SwiftUI primitive — не точный pixel-perfect эквивалент CSS `cubic-bezier(0.34, 1.56, 0.64, 1)` overshoot.
- **Visual impact:** subtle — overshoot visible на iOS but timing slightly different.
- **Decision:** accepted; alternative `.timingCurve` would require iOS 17+ phaseAnimator gymnastics.

### I-04 — Safe-area padding
- **Where:** all V10 screens render top/bottom safe-area insets per device (iPhone 15 has 59pt top + 34pt bottom; iPhone SE has 20pt top + 0pt bottom).
- **Prototype:** flat web layout, no notch/Dynamic Island awareness.
- **Decision:** Apple HIG-mandated; accepted.

### I-05 — Bare `.animation()` callsites flagged for v1.1 (Plan 28-02 audit)
- **Where:** TBD per Plan 28-02 grep audit findings (will be appended below by Plan 28-02 SUMMARY).
- **Decision:** known minor — V10 components mostly use `.posterAnimation()` modifier; flagged callsites are edge-cases and don't break reduce-motion (`accessibilityReduceMotion` still affects them via SwiftUI's global reduce-motion behavior in many primitives).

---

## iOS Manual Screenshot Checklist (acceptance prep)

For acceptance §14, manually capture iOS screenshots via XcodeBuildMCP or `xcrun simctl io <device> screenshot` and side-by-side compare with `prototype/index.html` rendered at 393×851 viewport. Mark each ✓ / ✗ / divergence.

| # | Screen          | iPhone 15 simulator route      | Expect                                                                  | Status |
| - | --------------- | ------------------------------ | ----------------------------------------------------------------------- | ------ |
| 1 | Home            | Tap Home tab                  | coral bg, eyebrow VOL.NN, italic «Дневной темп —», count-up BigFig      | ☐      |
| 2 | Transactions    | Home → «ВСЕ ОПЕРАЦИИ →»        | cobalt push, day groups, chip filter, roundup/deposit spec-tags         | ☐      |
| 3 | AddSheet        | Tap FAB                        | black bg, BigFig 86px yellow, custom 3×4 keypad, system kb suppressed   | ☐      |
| 4 | CategoryDetail  | Tap category from Home          | cobalt/red bg per isOver, Mass UPPER name, BigFig + bar with break-tick | ☐      |
| 5 | PLAN мая        | Home → plan badge              | cobalt, surplus plate, 2 rollover plates, regulars block, 8 sliders     | ☐      |
| 6 | Subscriptions   | Mgmt? or push site             | coral, Mass italic «Подписки.», bottom-sheet menu w/ 3 ghost CTAs       | ☐      |
| 7 | Savings         | Tap КОПИЛКА tab                | black, jaune plate «НАКОПЛЕНО ВСЕГО», roundup toggle, goal cards        | ☐      |
| 8 | AI initial      | Tap AI tab                     | DM Serif italic 36px observation, 4 chip-suggestions, eyebrow ONLINE    | ☐      |
| 9 | Accounts list   | Mgmt → 02 СЧЕТА                | cream, Mass italic «Счета.», dark plate СУММАРНО                        | ☐      |
| 10 | Analytics       | Mgmt → 03 АНАЛИТИКА            | cream, Mass italic «Месяц.», 2 KPI plates, bar chart, top-5             | ☐      |

**Reduce-motion smoke (iOS):**
- Settings → Accessibility → Motion → Reduce Motion ON.
- Open app → swipe between tabs → assert no horizontal slide; entries fade-in only.
- Status: ☐

**Edge-swipe-back smoke (PosterEdgeSwipe accessibility):**
- VoiceOver ON → push 3 screens → swipe-rotate-finger near left edge → VoiceOver announces «Назад, кнопка».
- Status: ☐

---

## Cross-Platform Divergences

### X-01 — Tab content swap differs
- **Web:** `posterTabSwap` 0.35s easeOut + 8px translateY (CSS keyframe).
- **iOS:** SwiftUI tab swap goes through PosterRouter `popToRoot` или sibling root replace; transition: opacity-only fade.
- **Decision:** acceptable — both feel snappy.

### X-02 — Toast lifetime
- **Web:** `--poster-toast-life: 1700ms` CSS variable.
- **iOS:** `PosterAnimations.toastLifeMs = 1700` (test_PosterAnimationsAuditTests verifies).
- **Status:** ✓ symmetric.

---

## Future Work (not POL-04 scope)

- Bring iOS to use `.timingCurve` для posterTabPop overshoot match (iOS 17+ available).
- Subset DM Serif Italic with hand-built Cyrillic glyphs (~ 4h work; defer to v1.1 when designer available).
- Snapshot diff CI на multi-platform baselines (Linux+macOS).
```

ВАЖНО:
- Файл — markdown, ~150-200 LOC.
- Если Plan 28-02 уже завершён и зафиксировал bare `.animation()` callsites — добавь их в I-05.
- Если pixel-snapshots в Task 1 пропустили какой-то экран — добавь его как отдельный `W-XX` divergence «не покрыт snapshot baseline because {reason}».
  </action>
  <verify>
    <automated>test -f /Users/exy/pet_projects/tg-budget-planner/.planning/v1.0-handoff/DIVERGENCES.md &amp;&amp; wc -l /Users/exy/pet_projects/tg-budget-planner/.planning/v1.0-handoff/DIVERGENCES.md | awk '{ if ($1 &gt;= 80) print "PASS"; else print "FAIL: "$1" lines"; }'</automated>
  </verify>
  <done>
- DIVERGENCES.md существует, ≥ 80 LOC.
- Содержит W-01..W-03, I-01..I-05, X-01..X-02 + iOS manual checklist.
- ADR-001/-002 явно reference'd.
  </done>
</task>

</tasks>

<verification>
- `npx playwright test v10-pixel-snapshots.spec.ts` (без `--update-snapshots`) green.
- `__screenshots__/v10-pixel-snapshots.spec.ts/` содержит committed baseline PNG.
- DIVERGENCES.md ≥ 80 LOC, содержит iOS manual checklist.
</verification>

<success_criteria>
- POL-04 (web): pixel-perfect baseline + repeatable test.
- POL-04 (iOS): manual checklist + documented divergences shipped в handoff.
</success_criteria>

<output>
Create `.planning/phases/28-animations-polish-acceptance/28-03-SUMMARY.md` per template, including:
- list of screens with successful baseline (✓) vs skipped (× + reason)
- snapshot baseline platform note (darwin / linux / both)
- DIVERGENCES.md TOC
- pointer to iOS manual checklist as «human verification follow-up» for v1.0 acceptance
</output>
