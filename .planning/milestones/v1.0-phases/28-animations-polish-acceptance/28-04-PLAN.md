---
phase: 28-animations-polish-acceptance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .planning/phases/28-animations-polish-acceptance/28-perf-report.md
autonomous: false
requirements: [POL-05]
tags: [polish, performance, lighthouse, bundle-size]

must_haves:
  truths:
    - "`make perf-report` запускает `npm run build` и выводит bundle output (raw + gzipped per-asset)."
    - "28-perf-report.md содержит измеренные значения: bundle total, woff2 sum (≤ 200kB gzipped target), Lighthouse score (если CLI запустился), Home count-up wall-clock (manual measurement)."
    - "Pass/fail acceptance gate (3 пункта) явно отмечен ✓/✗."
  artifacts:
    - path: "Makefile"
      provides: "perf-report target"
      contains: "perf-report:"
    - path: ".planning/phases/28-animations-polish-acceptance/28-perf-report.md"
      provides: "performance audit results + acceptance gate"
      min_lines: 60
  key_links:
    - from: "make perf-report"
      to: "npm --prefix frontend run build"
      via: "Makefile shell"
      pattern: "perf-report:"
    - from: "28-perf-report.md"
      to: "POL-05 acceptance criteria"
      via: "explicit comparison: measured vs target"
      pattern: "Target.*Measured.*Status"
---

<objective>
Performance audit: bundle-size measurement (automated), Lighthouse score (CLI-dependent), Home count-up wall-clock (manual smoke). Все три зафиксированы в 28-perf-report.md.

Purpose: POL-05.
Output: Makefile target + дашборд-отчёт.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-animations-polish-acceptance/28-CONTEXT.md
@.planning/phases/28-animations-polish-acceptance/28-must-haves.md

@Makefile
@frontend/package.json

<interfaces>
<!-- POL-05 targets (per ROADMAP success_criteria 28.4): -->
- Home первая отрисовка с count-up завершается < 1.5s после launch (iPhone 11 / iPhone Pro target)
- Lighthouse mobile score > 90 / LCP < 2.5s
- Bundle добавка woff2 < 200kB gzipped

<!-- Existing build pipeline: -->
- `npm --prefix frontend run build` → vite production build, выводит per-chunk + per-asset gzipped sizes на stdout.
- Self-hosted шрифты: see frontend/src/stylesV10/fonts.css + @fontsource entries в package.json. Они идут в bundle как .woff2 в `dist/assets/`.

<!-- Lighthouse CLI: -->
- Если установлен `npx lighthouse <url> --only-categories=performance`. Локальный dev URL = http://localhost:5173.
- Если CLI недоступен (нет Chrome / нет lighthouse в node_modules) — fallback на bundle-size proxy + manual smoke.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile perf-report target + auto часть отчёта</name>
  <files>Makefile, .planning/phases/28-animations-polish-acceptance/28-perf-report.md</files>
  <action>
1. Расширь `Makefile` добавив `.PHONY: perf-report` (если ещё не в .PHONY) + target:

```makefile
# POL-05 — bundle-size + (best-effort) Lighthouse run.
# Output идёт в stdout; фиксируется в 28-perf-report.md вручную.
perf-report:
	@echo "=== Phase 28-04 perf-report ==="
	@echo "Step 1: production build"
	npm --prefix frontend run build 2>&1 | tee .perf-build.log
	@echo ""
	@echo "Step 2: woff2 size aggregation (target <= 200kB gzipped sum)"
	@find frontend/dist/assets -name '*.woff2' -exec ls -l {} \; 2>/dev/null \
	  | awk '{ sum += $$5 } END { printf "Total woff2 raw: %d bytes (%.1f kB)\n", sum, sum/1024 }' || echo "no woff2 found in dist"
	@echo ""
	@echo "Step 3: total dist size"
	@du -sh frontend/dist 2>/dev/null || echo "no frontend/dist"
	@echo ""
	@echo "Step 4 (optional, может fail): Lighthouse CLI"
	@(cd frontend && npx --yes lighthouse http://localhost:5173 --only-categories=performance --form-factor=mobile --output=json --output-path=../.perf-lighthouse.json --chrome-flags='--headless' 2>&1 | tail -5) \
	  || echo "Lighthouse CLI unavailable — manual smoke required (см. checkpoint)"
	@echo ""
	@echo "DONE. Заполни 28-perf-report.md секцию Measurements значениями."
```

2. Запусти target:
```bash
cd /Users/exy/pet_projects/tg-budget-planner && make perf-report
```
Output потечёт в stdout (build log + woff2 aggregation + lighthouse если повезло).

3. Создай `.planning/phases/28-animations-polish-acceptance/28-perf-report.md` со скелетом (Targets vs Measured / Bundle Breakdown / Lighthouse Result / Manual Measurements / Decisions / Acceptance Gate). Заполни автоматическую часть (Bundle Breakdown из `.perf-build.log`, woff2 sum из awk-вывода, Lighthouse Result если CLI отработал). Manual measurements и Acceptance Gate — оставь {fill} placeholders, они заполнятся в Task 2.

Скелет:
```markdown
# Phase 28-04 — Performance Audit Report (POL-05)

**Run date:** {date}
**Build:** vite production via `npm --prefix frontend run build`
**Reference targets:** ROADMAP §28 success_criteria 4

## Targets vs Measured

| Metric                                | Target                  | Measured        | Status       |
|---------------------------------------|-------------------------|-----------------|--------------|
| woff2 sum (gzipped, all v10 fonts)    | ≤ 200 kB                | {fill}          | {☐ ✓ ✗}      |
| Bundle total (dist/)                  | ≤ 1.5 MB raw            | {fill}          | {☐ ✓ ✗}      |
| Lighthouse mobile/performance score   | ≥ 90                    | {fill or N/A}   | {☐ ✓ ✗ N/A}  |
| Lighthouse LCP (mobile)               | < 2.5s                  | {fill or N/A}   | {☐ ✓ ✗ N/A}  |
| Home count-up wall-clock (iOS sim)    | < 1.5s after launch     | {fill}          | {☐ ✓ ✗}      |
| Home count-up wall-clock (web)        | < 1.5s after first paint| {fill}          | {☐ ✓ ✗}      |

## Bundle Breakdown
(Paste build-log output here.)

## Lighthouse Result
(Paste main scores OR write "CLI unavailable — fallback на bundle-size proxy.")

## Manual Measurements
### Home count-up wall-clock (web)
- Method: Chrome devtools / Safari mobile sim, cmd-R reload, stopwatch.
- Average 3 reloads: {fill ms}.

### Home count-up wall-clock (iOS sim)
- Simulator: iPhone 15.
- Method: `xcrun simctl launch booted ...` + stopwatch.
- Average 3 launches: {fill ms}.

## Decisions
- (если что-то fails — list follow-ups)

## Acceptance Gate
- [ ] woff2 ≤ 200 kB
- [ ] Lighthouse ≥ 90 OR documented fallback (proxy via bundle-size)
- [ ] Home count-up < 1.5s on at least one platform

3/3 ✓ → POL-05 SATISFIED.
Any ✗ → log в STATE.md blockers как "v1.0 Phase 28 perf gap".
```

4. Удали временные файлы `.perf-build.log` и `.perf-lighthouse.json` после копирования output. Добавь их в `.gitignore` если ещё нет.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner &amp;&amp; make perf-report 2>&amp;1 | tail -20 &amp;&amp; test -f .planning/phases/28-animations-polish-acceptance/28-perf-report.md &amp;&amp; awk 'END{exit ($1&gt;=60?0:1)}' .planning/phases/28-animations-polish-acceptance/28-perf-report.md</automated>
  </verify>
  <done>
- `make perf-report` exits 0 (build не падает; lighthouse fallback допустим).
- 28-perf-report.md существует, ≥ 60 LOC.
- Bundle breakdown заполнен.
- Manual sections содержат {fill} placeholders для Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — Home count-up wall-clock + Acceptance Gate</name>
  <files>.planning/phases/28-animations-polish-acceptance/28-perf-report.md</files>
  <action>
**This is a checkpoint task — Claude pauses for the human to perform manual measurements that cannot be reliably automated (subjective animation timing, Lighthouse manual run if CLI fails, count-up wall-clock on real device/simulator).**

Шаги (выполняет user, не Claude):

**Шаг 1 — Home count-up wall-clock на web (≈ 2 мин):**
1. `cd frontend && npm run dev` (если ещё не запущен).
2. Открой http://localhost:5173 в Chrome (DevTools → Network → throttling: Fast 3G для conservative measurement).
3. Hard reload (cmd-shift-R) → секундомер от пустой страницы до того, как BigFig «Дневной темп» закончил count-up animation.
4. Повтори 3 раза, посчитай среднее. **Цель: < 1500 ms.**
5. Запиши в `28-perf-report.md` секцию «Home count-up wall-clock (web)».

**Шаг 2 — Home count-up wall-clock на iOS Simulator (≈ 3 мин):**
1. Запусти iPhone 15 simulator: `xcrun simctl boot 'iPhone 15'` или из Xcode.
2. Build & install BudgetPlanner: `cd ios && make run` (или другой target).
3. Force-quit + relaunch 3 раза — секундомер от splash до count-up settle.
4. Запиши среднее в «Home count-up wall-clock (iOS sim)».

**Шаг 3 (только если Task 1 Lighthouse CLI failed) — manual Lighthouse:**
1. Chrome DevTools → Lighthouse tab.
2. Mode: Navigation, Device: Mobile, Categories: Performance only.
3. URL: http://localhost:5173.
4. Run analysis → запиши Performance score + LCP в Lighthouse Result секцию.

**Шаг 4 — Acceptance Gate:**
- Tick checkboxes для каждого пункта (✓ если pass / ✗ если fail).
- 3/3 ✓ → POL-05 SATISFIED.
- Любой ✗ → добавь в `.planning/STATE.md` под Blockers/Concerns: «v1.0 Phase 28 POL-05 perf gap: {description}» + next steps (например «defer LCP optimization to v1.1»).

**Шаг 5 — cleanup:**
- Удали `.perf-build.log` и `.perf-lighthouse.json`.

**Resume signal:** Type "approved" once `28-perf-report.md` заполнен и Acceptance Gate отмечен (даже с ✗ — это валидный исход с зафиксированной gap), либо опиши проблему.
  </action>
  <verify>
    <automated>! grep -c '{☐\|{fill' /Users/exy/pet_projects/tg-budget-planner/.planning/phases/28-animations-polish-acceptance/28-perf-report.md | grep -v '^0$'</automated>
  </verify>
  <done>
- 28-perf-report.md не содержит {☐} или {fill} placeholders.
- Acceptance Gate секция содержит [x] либо [ ] с явным ✗ (не ☐).
- Если есть perf gaps — соответствующая запись добавлена в STATE.md.
  </done>
</task>

</tasks>

<verification>
- `make perf-report` succeeds.
- 28-perf-report.md ≥ 60 LOC.
- All Acceptance Gate items have ✓ или ✗ (не {☐}).
</verification>

<success_criteria>
- POL-05: bundle-size measured + Lighthouse documented (real OR fallback) + Home count-up wall-clock measured. 3-item Acceptance Gate explicitly resolved.
</success_criteria>

<output>
Create `.planning/phases/28-animations-polish-acceptance/28-04-SUMMARY.md` per template, including:
- final Acceptance Gate state (3/3 ✓ или ровно сколько прошло)
- если Lighthouse CLI failed — fallback decision noted
- если есть perf gaps — pointer на STATE.md запись
</output>
