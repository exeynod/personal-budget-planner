---
phase: 26
plan: 07
type: execute
wave: 3
depends_on: [26-01, 26-05]
files_modified:
  - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsData.swift
  - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsView.swift
  - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionMenuSheet.swift
  - ios/BudgetPlannerTests/FeaturesV10/SubscriptionsDataTests.swift
autonomous: true
requirements: [SUBS-V10-01, SUBS-V10-02, SUBS-V10-03, SUBS-V10-04]
tags: [ios, swiftui, observable, subscriptions, posterSheet, confirmationDialog, tdd]

must_haves:
  truths:
    - "SubscriptionsView на coral-фоне, Mass italic «Подписки.», BigFig monthly_total ₽/мес, eyebrow «N АКТИВНЫХ · Y ₽ В ГОД»"
    - "Список subs: name UPPER · «каждое N число» (monthly) или «N мая» (yearly) · price · ··· tap-target"
    - "Tap ··· → posterSheet primary с 3 ghost-кнопками + destructive ОТМЕНИТЬ ПОДПИСКУ"
    - "СМЕНИТЬ ДЕНЬ → secondary posterSheet с Stepper(value, in: 1...28); save → SubscriptionsV10API.patch({day_of_month})"
    - "ИЗМЕНИТЬ ЦЕНУ → secondary posterSheet с numeric TextField; save → SubscriptionsV10API.patch({amount_cents})"
    - "ОТМЕНИТЬ ПОДПИСКУ → SwiftUI .confirmationDialog → SubscriptionsV10API.delete"
  artifacts:
    - path: "ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsData.swift"
      provides: "computeMonthlyTotal/computeYearlyTotalAnnualized/computeActiveCount/formatCadenceRu/sortForDisplay"
      min_lines: 100
    - path: "ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsView.swift"
      provides: "SwiftUI screen"
      min_lines: 200
    - path: "ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionMenuSheet.swift"
      provides: "Menu sheet + day/price editors + confirm-delete"
      min_lines: 250
  key_links:
    - from: "SubscriptionsViewModel.swift"
      to: "SubscriptionsV10API.swift (Plan 26-05)"
      via: "list/patch/delete calls"
      pattern: "SubscriptionsV10API\\."
---

<objective>
iOS Subscriptions screen (SUBS-V10-01..04) symmetric to web Plan 26-06. Coral bg, Mass italic «Подписки.», BigFig + eyebrow, list with «···» tap-target, posterSheet primary menu (ПАУЗА toggle, СМЕНИТЬ ДЕНЬ, ИЗМЕНИТЬ ЦЕНУ, destructive ОТМЕНИТЬ ПОДПИСКУ), secondary posterSheets для day/price editors, .confirmationDialog для delete.

API surfaces (SubscriptionsV10API + SubscriptionV10DTO + SubscriptionV10UpdateRequest) уже созданы в Plan 26-05 Task 1.

Reachable программно через `router?.push(SubscriptionsView())`. Phase 27 Mgmt добавит entry-point.

Output: 5 новых файлов в `FeaturesV10/Subscriptions/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-category-detail-plan-subscriptions/26-CONTEXT.md
@.planning/phases/26-category-detail-plan-subscriptions/26-must-haves.md

@ios/BudgetPlanner/Networking/DTO/SubscriptionV10DTO.swift
@ios/BudgetPlanner/Networking/Endpoints/SubscriptionsV10API.swift
@ios/BudgetPlanner/FeaturesV10/Common/PosterSheet.swift
@ios/BudgetPlanner/FeaturesV10/Common/V10Formatters.swift
@ios/BudgetPlanner/FeaturesV10/Common/Toast.swift

<interfaces>
<!-- API surfaces (Plan 26-05 Task 1 created these): -->
```swift
@MainActor enum SubscriptionsV10API {
    static func list() async throws -> [SubscriptionV10DTO]
    static func post(id: Int) async throws -> SubscriptionPostResponseDTO
    static func unpost(id: Int) async throws
    static func patch(id: Int, payload: SubscriptionV10UpdateRequest) async throws -> SubscriptionV10DTO
    static func delete(id: Int) async throws
}

struct SubscriptionV10DTO: Decodable, Identifiable, Equatable {
    let id, amountCents, categoryId, notifyDaysBefore: Int
    let name: String
    let cycle: SubCycle
    let nextChargeDate: Date
    let isActive: Bool
    let dayOfMonth: Int?
    let accountId: Int?
    let postedTxnId: Int?
}
```

<!-- PosterSheet ViewModifier (FeaturesV10/Common/PosterSheet.swift): -->
```swift
extension View {
    func posterSheet<Content: View>(isPresented: Binding<Bool>, @ViewBuilder content: @escaping () -> Content) -> some View
}
// usage: someView.posterSheet(isPresented: $showMenu) { menuContent }
```

<!-- For two stacked sheets: nest .posterSheet on inner content view. SwiftUI handles z-order via view hierarchy depth. -->

<!-- V10Formatters constants: -->
```swift
import enum V10Formatters
V10Formatters.monthsRuGenitive  // ["января", ..., "декабря"]
```

<!-- RubleFormatter (FeaturesV10/Onboarding/RubleFormatter.swift): -->
```swift
RubleFormatter.format(cents: Int) → String  // "12 345" с U+202F
```

<!-- SwiftUI .confirmationDialog signature: -->
```swift
.confirmationDialog("Title", isPresented: $isShown, titleVisibility: .visible) {
    Button("Удалить", role: .destructive) { ... }
    Button("Отмена", role: .cancel) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SubscriptionsData pure compute + RED/GREEN XCTests</name>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift (compute pattern)
    - ios/BudgetPlanner/Networking/DTO/SubscriptionV10DTO.swift (DTO shape from Plan 26-05)
    - ios/BudgetPlanner/FeaturesV10/Common/V10Formatters.swift (monthsRuGenitive)
  </read_first>
  <files>
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsData.swift
    - ios/BudgetPlannerTests/FeaturesV10/SubscriptionsDataTests.swift
  </files>
  <behavior>
    - 12+ XCTest cases mirror web Plan 26-06:
      - computeActiveCount counts is_active = true.
      - computeMonthlyTotal sums monthly+active amounts.
      - computeYearlyTotalAnnualized = monthly*12 + Σ yearly active amounts.
      - formatCadenceRu: monthly+dayOfMonth → «каждое N число»; monthly+nil → «ежемесячно»; yearly → «N {month_genitive}» from nextChargeDate.
      - sortForDisplay: active first, then by amount DESC, then by name ASC.
  </behavior>
  <action>
    1. **Create `SubscriptionsData.swift`** (~120 LOC):
       ```swift
       import Foundation

       enum SubscriptionsData {
           static func computeActiveCount(_ subs: [SubscriptionV10DTO]) -> Int {
               subs.filter { $0.isActive }.count
           }

           static func computeMonthlyTotal(_ subs: [SubscriptionV10DTO]) -> Int {
               subs.filter { $0.isActive && $0.cycle == .monthly }
                   .reduce(0) { $0 + $1.amountCents }
           }

           static func computeYearlyTotalAnnualized(_ subs: [SubscriptionV10DTO]) -> Int {
               let monthlyAnnual = computeMonthlyTotal(subs) * 12
               let yearlySum = subs.filter { $0.isActive && $0.cycle == .yearly }
                                   .reduce(0) { $0 + $1.amountCents }
               return monthlyAnnual + yearlySum
           }

           static func formatCadenceRu(_ sub: SubscriptionV10DTO, calendar: Calendar = .current) -> String {
               if sub.cycle == .monthly {
                   if let day = sub.dayOfMonth { return "каждое \(day) число" }
                   return "ежемесячно"
               }
               // yearly
               let day = calendar.component(.day, from: sub.nextChargeDate)
               let month = calendar.component(.month, from: sub.nextChargeDate)
               guard month >= 1 && month <= 12 else { return "ежегодно" }
               return "\(day) \(V10Formatters.monthsRuGenitive[month - 1])"
           }

           static func sortForDisplay(_ subs: [SubscriptionV10DTO]) -> [SubscriptionV10DTO] {
               subs.sorted { lhs, rhs in
                   if lhs.isActive != rhs.isActive { return lhs.isActive }
                   if lhs.amountCents != rhs.amountCents { return lhs.amountCents > rhs.amountCents }
                   return lhs.name.localizedCompare(rhs.name) == .orderedAscending
               }
           }
       }
       ```

    2. **Create `SubscriptionsDataTests.swift`** (~200 LOC) с 12+ XCTests covering behavior list. JSON fixture для DTO inputs.
    3. Build + RED → GREEN.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodebuild test -only-testing:BudgetPlannerTests/SubscriptionsDataTests -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -quiet 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "static func compute\|static func format\|static func sortForDisplay" ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsData.swift` ≥ 5.
    - 12+ XCTests pass.
    - Build clean.
  </acceptance_criteria>
  <done>Pure helpers tested.</done>
</task>

<task type="auto">
  <name>Task 2: SubscriptionsViewModel + SubscriptionsView SwiftUI screen</name>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10ViewModel.swift (Lines 1-end — Observable model + load() pattern)
    - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10View.swift (Lines 1-end — list rendering + tap → sheet)
    - ios/BudgetPlanner/FeaturesV10/Common/PosterTokens.swift (color tokens)
  </read_first>
  <files>
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsViewModel.swift
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsView.swift
  </files>
  <action>
    1. **Create `SubscriptionsViewModel.swift`** (~140 LOC):
       ```swift
       import Foundation
       import Observation

       @MainActor
       @Observable
       final class SubscriptionsViewModel {
           enum Status: Equatable { case idle, loading, ready, error(String) }
           private(set) var subs: [SubscriptionV10DTO] = []
           private(set) var status: Status = .idle
           var menuSub: SubscriptionV10DTO? = nil
           var pendingDeleteSub: SubscriptionV10DTO? = nil

           private var inFlight = false

           func load() async {
               guard !inFlight else { return }
               inFlight = true; defer { inFlight = false }
               status = .loading
               do {
                   self.subs = try await SubscriptionsV10API.list()
                   status = .ready
               } catch {
                   status = .error(error.localizedDescription)
               }
           }

           func togglePause(_ sub: SubscriptionV10DTO) async {
               do {
                   _ = try await SubscriptionsV10API.patch(
                       id: sub.id,
                       payload: SubscriptionV10UpdateRequest(isActive: !sub.isActive)
                   )
                   await load()
               } catch { /* polish */ }
           }

           func changeDay(_ sub: SubscriptionV10DTO, newDay: Int) async {
               do {
                   _ = try await SubscriptionsV10API.patch(
                       id: sub.id,
                       payload: SubscriptionV10UpdateRequest(dayOfMonth: newDay)
                   )
                   await load()
               } catch { /* polish */ }
           }

           func changePrice(_ sub: SubscriptionV10DTO, newCents: Int) async {
               do {
                   _ = try await SubscriptionsV10API.patch(
                       id: sub.id,
                       payload: SubscriptionV10UpdateRequest(amountCents: newCents)
                   )
                   await load()
               } catch { /* polish */ }
           }

           func deleteSub(_ sub: SubscriptionV10DTO) async {
               do {
                   try await SubscriptionsV10API.delete(id: sub.id)
                   await load()
               } catch { /* polish */ }
           }
       }
       ```

    2. **Create `SubscriptionsView.swift`** (~220 LOC):
       ```swift
       import SwiftUI

       struct SubscriptionsView: View {
           @State private var model = SubscriptionsViewModel()
           @Environment(\.posterRouter) private var router

           var body: some View {
               ZStack {
                   PosterTokens.Color.coral.ignoresSafeArea()
                   content
               }
               .task { await model.load() }
               // Primary menu sheet
               .posterSheet(isPresented: Binding(
                   get: { model.menuSub != nil && model.pendingDeleteSub == nil },
                   set: { if !$0 { model.menuSub = nil } }
               )) {
                   if let sub = model.menuSub {
                       SubscriptionMenuSheet(
                           sub: sub,
                           onTogglePause: { Task { await model.togglePause(sub); model.menuSub = nil } },
                           onChangeDay: { newDay in Task { await model.changeDay(sub, newDay: newDay); model.menuSub = nil } },
                           onChangePrice: { newCents in Task { await model.changePrice(sub, newCents: newCents); model.menuSub = nil } },
                           onRequestDelete: { model.pendingDeleteSub = sub }
                       )
                   }
               }
               // Confirm delete
               .confirmationDialog(
                   model.pendingDeleteSub.map { "Отменить подписку «\($0.name)»?" } ?? "",
                   isPresented: Binding(
                       get: { model.pendingDeleteSub != nil },
                       set: { if !$0 { model.pendingDeleteSub = nil } }
                   ),
                   titleVisibility: .visible
               ) {
                   Button("Удалить", role: .destructive) {
                       if let sub = model.pendingDeleteSub {
                           Task { await model.deleteSub(sub); model.pendingDeleteSub = nil; model.menuSub = nil }
                       }
                   }
                   Button("Отмена", role: .cancel) { model.pendingDeleteSub = nil }
               }
           }

           @ViewBuilder
           private var content: some View {
               switch model.status {
               case .idle, .loading:
                   ProgressView().tint(PosterTokens.Color.ink)
               case .error(let msg):
                   VStack { Eyebrow("ОШИБКА"); Text(msg).foregroundColor(.white)
                       PosterButton("ПОВТОРИТЬ →", variant: .yellow) { Task { await model.load() } } }
               case .ready:
                   readyView
               }
           }

           private var readyView: some View {
               let sorted = SubscriptionsData.sortForDisplay(model.subs)
               let active = SubscriptionsData.computeActiveCount(model.subs)
               let monthly = SubscriptionsData.computeMonthlyTotal(model.subs)
               let yearly = SubscriptionsData.computeYearlyTotalAnnualized(model.subs)

               return ScrollView {
                   VStack(alignment: .leading, spacing: 14) {
                       HStack {
                           if router?.canPop ?? false {
                               Button(action: { router?.pop() }) {
                                   Text("← НАЗАД").font(.posterMono(size: 11)).foregroundColor(PosterTokens.Color.ink)
                               }
                           }
                           Spacer()
                           Eyebrow("SUBSCRIPTIONS", color: PosterTokens.Color.ink)
                       }
                       Mass("Подписки.", italic: true, size: 70).foregroundColor(PosterTokens.Color.ink)
                       BigFig(value: monthly / 100, sup: "₽/мес", size: 86, color: PosterTokens.Color.ink)
                       Eyebrow("\(active) АКТИВНЫХ · \(RubleFormatter.format(cents: yearly)) ₽ В ГОД",
                               color: PosterTokens.Color.ink)
                       if sorted.isEmpty {
                           Text("Нет подписок").italic()
                               .font(.posterMono(size: 12))
                               .foregroundColor(PosterTokens.Color.ink.opacity(0.6))
                       } else {
                           ForEach(sorted) { sub in
                               subRow(sub)
                           }
                       }
                   }
                   .padding(.horizontal, 22)
                   .padding(.top, 56)
                   .padding(.bottom, 90)
               }
           }

           @ViewBuilder
           private func subRow(_ sub: SubscriptionV10DTO) -> some View {
               HStack {
                   VStack(alignment: .leading, spacing: 2) {
                       Text(sub.name.uppercased())
                           .font(.posterSans(size: 14, weight: .bold))
                           .foregroundColor(sub.isActive ? PosterTokens.Color.ink : PosterTokens.Color.ink.opacity(0.4))
                       Text(SubscriptionsData.formatCadenceRu(sub))
                           .font(.posterMono(size: 11))
                           .foregroundColor(PosterTokens.Color.ink.opacity(0.6))
                   }
                   Spacer()
                   Text("\(RubleFormatter.format(cents: sub.amountCents)) ₽")
                       .font(.posterMono(size: 13, weight: .semibold))
                       .foregroundColor(PosterTokens.Color.ink)
                   Button(action: { model.menuSub = sub }) {
                       Text("···")
                           .font(.posterMono(size: 18, weight: .bold))
                           .foregroundColor(PosterTokens.Color.ink)
                           .frame(width: 32, height: 32)
                   }
                   .accessibilityLabel("Меню для \(sub.name)")
               }
               .padding(.vertical, 8)
           }
       }
       ```

       **Note:** Eyebrow signature `Eyebrow(_ text: String, opacity: Double = 0.6, color: Color = .paper)` — adapt color: parameter to actual API. If no `color:` parameter exists in shipped Eyebrow component, wrap Text manually для ink color rendering on coral bg.

    3. Build гейт.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -quiet 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `SubscriptionsView.swift` exists; `grep -c "Подписки\|SUBSCRIPTIONS\|АКТИВНЫХ\|posterSheet\|confirmationDialog" ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsView.swift` ≥ 5.
    - Build succeeds.
  </acceptance_criteria>
  <done>iOS Subscriptions screen renders.</done>
</task>

<task type="auto">
  <name>Task 3: SubscriptionMenuSheet с day/price editors (nested posterSheets)</name>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetView.swift (numeric input pattern reference)
    - ios/BudgetPlanner/FeaturesV10/Common/PosterSheet.swift (verify nested posterSheet behavior)
  </read_first>
  <files>
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionMenuSheet.swift
  </files>
  <action>
    1. **Create `SubscriptionMenuSheet.swift`** (~280 LOC):
       ```swift
       import SwiftUI

       struct SubscriptionMenuSheet: View {
           let sub: SubscriptionV10DTO
           let onTogglePause: () -> Void
           let onChangeDay: (Int) -> Void
           let onChangePrice: (Int) -> Void
           let onRequestDelete: () -> Void

           @State private var dayEditorOpen = false
           @State private var priceEditorOpen = false
           @State private var dayValue: Int = 1
           @State private var priceRubles: String = ""

           var body: some View {
               VStack(alignment: .leading, spacing: 16) {
                   Text(sub.name.uppercased())
                       .font(.posterSans(size: 16, weight: .bold))
                       .foregroundColor(PosterTokens.Color.ink)
                   PosterButton(sub.isActive ? "ПАУЗА" : "ВКЛЮЧИТЬ", variant: .ghost) {
                       onTogglePause()
                   }
                   PosterButton("СМЕНИТЬ ДЕНЬ", variant: .ghost) {
                       dayValue = sub.dayOfMonth ?? 1
                       dayEditorOpen = true
                   }
                   PosterButton("ИЗМЕНИТЬ ЦЕНУ", variant: .ghost) {
                       priceRubles = String(sub.amountCents / 100)
                       priceEditorOpen = true
                   }
                   Button(action: onRequestDelete) {
                       Text("ОТМЕНИТЬ ПОДПИСКУ")
                           .font(.posterSans(size: 14, weight: .bold))
                           .foregroundColor(PosterTokens.Color.paper)
                           .frame(maxWidth: .infinity)
                           .padding(12)
                           .background(PosterTokens.Color.red)
                   }
               }
               .padding(20)
               .frame(maxWidth: .infinity, alignment: .leading)
               // Nested day editor
               .posterSheet(isPresented: $dayEditorOpen) {
                   VStack(alignment: .leading, spacing: 16) {
                       Eyebrow("СМЕНИТЬ ДЕНЬ")
                       Stepper(value: $dayValue, in: 1...28) {
                           Text("\(dayValue) число")
                               .font(.posterMono(size: 22, weight: .bold))
                               .foregroundColor(PosterTokens.Color.ink)
                       }
                       HStack(spacing: 10) {
                           PosterButton("ОТМЕНА", variant: .ghost) { dayEditorOpen = false }
                           PosterButton("СОХРАНИТЬ", variant: .yellow) {
                               onChangeDay(dayValue)
                               dayEditorOpen = false
                           }
                       }
                   }.padding(20)
               }
               // Nested price editor
               .posterSheet(isPresented: $priceEditorOpen) {
                   VStack(alignment: .leading, spacing: 16) {
                       Eyebrow("ИЗМЕНИТЬ ЦЕНУ")
                       TextField("0", text: $priceRubles)
                           .keyboardType(.numberPad)
                           .font(.posterMono(size: 28, weight: .bold))
                           .foregroundColor(PosterTokens.Color.ink)
                       Text("в рублях").font(.posterMono(size: 11)).foregroundColor(PosterTokens.Color.ink.opacity(0.6))
                       HStack(spacing: 10) {
                           PosterButton("ОТМЕНА", variant: .ghost) { priceEditorOpen = false }
                           PosterButton("СОХРАНИТЬ", variant: .yellow) {
                               let cleaned = priceRubles.filter { $0.isNumber }
                               if let rubles = Int(cleaned), rubles > 0 {
                                   onChangePrice(rubles * 100)
                                   priceEditorOpen = false
                               }
                           }
                       }
                   }.padding(20)
               }
           }
       }
       ```

       **Note re nested posterSheet behavior:** verify by reading PosterSheet.swift in Common — это ViewModifier; nesting должно работать так как inner sheet attaches к VStack's modifier chain. If problems surface (двойной backdrop, gesture conflict), переключиться на single sheet с ZStack switching content based on `editorMode` enum (как в web Plan 26-06 alternative pattern).

    2. Build + verify SubscriptionsView compose-flow works.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -quiet 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep -c "ПАУЗА\|ВКЛЮЧИТЬ\|СМЕНИТЬ ДЕНЬ\|ИЗМЕНИТЬ ЦЕНУ\|ОТМЕНИТЬ ПОДПИСКУ" ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionMenuSheet.swift` ≥ 5.
    - Build succeeds.
  </acceptance_criteria>
  <done>Menu sheet с nested editors готов.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS App → API | Untrusted PATCH/DELETE для subscriptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Repudiation | Accidental delete | mitigate | Two-step: menu → ОТМЕНИТЬ ПОДПИСКУ button → SwiftUI .confirmationDialog с destructive «Удалить» / cancel «Отмена». Only confirm fires deleteSub. |
| T-26-07-02 | Tampering | day_of_month out of range | mitigate | UI Stepper(value:in: 1...28) constrains; backend Pydantic Field(ge=1, le=28). |
| T-26-07-03 | Tampering | Negative price | mitigate | TextField filtering к isNumber; rubles > 0 gate; cents = rubles * 100 always positive. |
| T-26-07-04 | Information Disclosure | Cross-tenant sub | accept | listSubscriptionsV10 RLS-protected. |
</threat_model>

<verification>
- xcodebuild test SubscriptionsDataTests → 12+ pass
- xcodebuild build → clean
- HomeDataTests + V10MainShellTests + CategoryDetailDataTests + PlanDataTests no regressions
- Manual on Simulator: programmatically push SubscriptionsView() (через debug menu или test entry-point) → coral bg → ··· tap opens menu → пауза toggles is_active → день editor with stepper saves → цена editor saves → удалить confirms then removes
</verification>

<success_criteria>
- 4 REQs (SUBS-V10-01..04) iOS implementation complete
- Nested posterSheets для day/price editors (или single-sheet с editorMode switching как fallback)
- .confirmationDialog для destructive delete
- Build clean; tests pass; no regressions
</success_criteria>

<output>
После завершения создать `.planning/phases/26-category-detail-plan-subscriptions/26-07-SUMMARY.md` с requirements-completed: [SUBS-V10-01..04].
</output>
