---
phase: 05-dashboard-period-lifecycle
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_close_period_job.py
  - app/worker/__init__.py
  - app/worker/jobs/__init__.py
  - app/worker/jobs/close_period.py
  - main_worker.py
autonomous: true
requirements: [PER-04, PER-03]
tags: [backend, worker, apscheduler, periods, close-period]
must_haves:
  truths:
    - "Worker-job close_period запускается ежедневно в 00:01 Europe/Moscow"
    - "Истёкший active period (period_end < today_msk) закрывается: status=closed, ending_balance_cents = starting + Σ actual_income − Σ actual_expense, closed_at=now()"
    - "После закрытия создаётся следующий period с starting_balance_cents = ending_balance_cents предыдущего"
    - "Повторный запуск джобы в тот же день — no-op (нет active периода в expired состоянии)"
    - "Конкурентный запуск двух джоб — второй процесс получает pg_try_advisory_lock=false и выходит без ошибки"
    - "Ошибка в любой части (close или create) откатывает транзакцию полностью"
  artifacts:
    - path: tests/test_close_period_job.py
      provides: "RED-тесты для close_period_job"
      contains: "async def test_close_period_"
    - path: app/worker/jobs/close_period.py
      provides: "close_period_job async function + ADVISORY_LOCK_KEY"
      contains: "async def close_period_job"
    - path: main_worker.py
      provides: "scheduler.add_job(close_period_job, 'cron', hour=0, minute=1, ...)"
      contains: "close_period_job"
  key_links:
    - from: main_worker.py
      to: app/worker/jobs/close_period.py::close_period_job
      via: "import + scheduler.add_job"
      pattern: "from app.worker.jobs.close_period import close_period_job"
    - from: app/worker/jobs/close_period.py
      to: app/services/actual.py::compute_balance
      via: "вызов для расчёта ending_balance_cents"
      pattern: "compute_balance\\("
    - from: app/worker/jobs/close_period.py
      to: app/core/period.py::period_for
      via: "расчёт bounds следующего периода"
      pattern: "period_for\\("
---

<objective>
Реализовать **PER-04**: worker-job, который ежедневно в 00:01 Europe/Moscow:
1. Находит active period с `period_end < today_msk` (истёк).
2. Если найден — рассчитывает `ending_balance_cents = starting_balance_cents + Σ actual_income − Σ actual_expense`, ставит `status=closed`, `closed_at=now()`.
3. Создаёт следующий period с bounds `period_for(today_msk, cycle_start_day)` и `starting_balance_cents = ending_balance` закрытого (PER-03 наследование).
4. Защищается через `pg_try_advisory_lock` от двойного запуска. Идемпотентность: повторный запуск без истёкшего активного периода — no-op без ошибок. Атомарность: одна DB-транзакция на close+create.

Покрывает требования **PER-04** (auto close+create) и **PER-03** (`starting_balance` = `ending_balance` предыдущего, ранее deferred из Phase 2).

Output: новый модуль `app/worker/jobs/close_period.py` + регистрация джобы в `main_worker.py` + RED тесты.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md
@.planning/phases/05-dashboard-period-lifecycle/05-PATTERNS.md
@CLAUDE.md
@main_worker.py
@app/services/periods.py
@app/services/actual.py
@app/core/period.py
@app/db/models.py
@app/db/session.py
@app/core/settings.py
@tests/test_balance.py

<interfaces>
<!-- Существующая инфраструктура (НЕ переписывать!) -->

From `main_worker.py:33-87`:
```python
import pytz
from apscheduler.schedulers.asyncio import AsyncIOScheduler
from app.core.settings import settings
from app.db.session import AsyncSessionLocal

MOSCOW_TZ = pytz.timezone(settings.APP_TZ)  # 'Europe/Moscow'

# scheduler.add_job pattern:
scheduler.add_job(
    heartbeat_job, "interval", minutes=5,
    id="heartbeat", replace_existing=True,
    next_run_time=datetime.now(MOSCOW_TZ),
)

# В файле уже есть placeholder comment (lines 76-82) для close_period —
# его нужно заменить на реальный add_job + сохранить placeholders для Phase 6.
```

From `app/services/actual.py:323`:
```python
async def compute_balance(db: AsyncSession, period_id: int) -> dict
# Returns dict with key 'balance_now_cents' = starting + actual_income - actual_expense
# Raises PeriodNotFoundError if period missing
```

From `app/services/periods.py:14`:
```python
def _today_in_app_tz() -> date  # today in Europe/Moscow
```

From `app/core/period.py`:
```python
def period_for(d: date, cycle_start_day: int) -> tuple[date, date]
# Returns (period_start, period_end) for given date and cycle_start_day
```

From `app/services/settings.py`:
```python
async def get_cycle_start_day(db: AsyncSession, tg_user_id: int) -> int
class UserNotFoundError(Exception)  # raised on missing app_user row
```

From `app/db/models.py`:
```python
class BudgetPeriod(Base):
    __tablename__ = "budget_period"
    # period_start: UNIQUE
    # ending_balance_cents: Optional[int]  (NULL while active)
    # status: PeriodStatus  (active | closed)
    # closed_at: Optional[datetime]

class PeriodStatus(str, enum.Enum):
    active = "active"
    closed = "closed"
```

From `app/db/session.py`:
```python
AsyncSessionLocal: async_sessionmaker
# usage: async with AsyncSessionLocal() as session: ...
```

From `app/core/settings.py`:
```python
settings.APP_TZ          # 'Europe/Moscow'
settings.OWNER_TG_ID     # int — single-tenant ID
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — RED тесты для close_period_job (idempotency, balance, lock)</name>
  <files>tests/test_close_period_job.py</files>
  <read_first>
    - tests/test_balance.py (паттерн _require_db + db_setup TRUNCATE-fixture)
    - tests/conftest.py (async_client конфигурация)
    - app/services/actual.py:323-438 (compute_balance возвращает balance_now_cents)
    - main_worker.py (понять структуру AsyncSessionLocal)
    - app/db/models.py:99-123 (BudgetPeriod fields)
    - app/core/period.py (period_for сигнатура)
  </read_first>
  <behavior>
    - **test_close_period_noop_when_no_active_period**: пустая БД, без BudgetPeriod → close_period_job() выполняется без ошибки, БД остаётся пустой
    - **test_close_period_noop_when_active_not_expired**: создаём active period с period_end >= today (например period_end=today+5d) → after run: period.status остаётся active, новых периодов не создано
    - **test_close_period_closes_expired_period**: создаём active period с period_end=today-1d, starting_balance_cents=100000, без транзакций → after run: period.status=closed, period.ending_balance_cents=100000, period.closed_at != None
    - **test_close_period_balance_with_transactions**: active period (period_end=yesterday), starting_balance=100000, +actual income 50000, +actual expense 30000 → after run: ending_balance_cents = 100000 + 50000 - 30000 = 120000
    - **test_close_period_creates_next_period_with_inherited_balance** (PER-03): expired active period с ending_balance=120000 → after run: появился новый period (status=active), starting_balance_cents=120000
    - **test_close_period_idempotent_second_run**: запускаем close_period_job() дважды подряд → первый раз закрывает+создаёт, второй раз no-op (новый active period не expired)
    - **test_close_period_advisory_lock_prevents_concurrent**: захватываем advisory lock в отдельном connection (`SELECT pg_advisory_lock(<KEY>)`), потом вызываем close_period_job() → джоба выходит без изменений в БД (lock_acquired=False); после release — следующий вызов работает
    - **test_close_period_rollback_on_error**: monkey-patch внутреннюю функцию (например `period_for`) чтобы raise → после ошибки: исходный active period не закрыт (ничего не сохранено)
  </behavior>
  <action>
    Создать `tests/test_close_period_job.py`:

    1. Headers + `_require_db()` helper (точная копия из test_balance.py:21-23 — skip если DATABASE_URL не задан).
    2. Fixture `db_setup` — точная копия из tests/test_balance.py:32-65: TRUNCATE TABLE category, planned_transaction, actual_transaction, plan_template_item, subscription, budget_period, app_user RESTART IDENTITY CASCADE; затем `app.dependency_overrides[get_db] = real_get_db`. Возвращает `(async_client, SessionLocal)`. Можно factor в helper, но для простоты — копия.
    3. Импортировать целевую функцию (это упадёт ImportError на Wave 0 — RED state):
       ```python
       from app.worker.jobs.close_period import close_period_job, ADVISORY_LOCK_KEY
       ```
    4. Для подмены `today_msk` использовать `pytest.MonkeyPatch` (НЕ вводим freezegun):
       ```python
       def patch_today(monkeypatch, fake_today):
           monkeypatch.setattr(
               "app.services.periods._today_in_app_tz",
               lambda: fake_today,
           )
           # Также подменим в worker module если оно там reimported
           monkeypatch.setattr(
               "app.worker.jobs.close_period._today_in_app_tz",
               lambda: fake_today,
               raising=False,
           )
       ```
    5. В тестах создавать BudgetPeriod через прямую вставку в SessionLocal (НЕ через FastAPI client — джоба запускается без HTTP). Пример:
       ```python
       async with SessionLocal() as session:
           p = BudgetPeriod(
               period_start=date(2026, 4, 5),
               period_end=date(2026, 5, 4),  # expired if today=2026-05-05
               starting_balance_cents=100_000,
               status=PeriodStatus.active,
           )
           session.add(p); await session.commit()
       ```
    6. Импорт моделей: `from app.db.models import BudgetPeriod, PeriodStatus, Category, CategoryKind, ActualTransaction, ActualSource`.
    7. Для **test_close_period_advisory_lock_prevents_concurrent** использовать отдельный engine:
       ```python
       from sqlalchemy import text
       from sqlalchemy.ext.asyncio import create_async_engine
       lock_engine = create_async_engine(os.environ["DATABASE_URL"])
       async with lock_engine.connect() as lock_conn:
           await lock_conn.execute(text(f"SELECT pg_advisory_lock({ADVISORY_LOCK_KEY})"))
           # call job — should bail
           await close_period_job()
           # verify: original active period still active
           ...
           await lock_conn.execute(text(f"SELECT pg_advisory_unlock({ADVISORY_LOCK_KEY})"))
       await lock_engine.dispose()
       ```
    8. Каждый тест декорирован `@pytest.mark.asyncio`. Все тесты теряют DB через `db_setup` fixture, чтобы изоляция работала.
    9. Wave 0 RED: импорт упадёт ImportError для `app.worker.jobs.close_period` — приемлемо до Task 2.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test pytest tests/test_close_period_job.py -x --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_close_period_job.py`
    - Contains 8 test functions: `grep -c "^async def test_close_period_" tests/test_close_period_job.py` ≥ 7 (one test name might be `test_advisory_lock_*` без `close_period_` prefix — приемлемо)
    - Total async tests: `grep -cE "^async def test_" tests/test_close_period_job.py` == 8
    - Imports close_period_job: `grep -c "from app.worker.jobs.close_period import close_period_job" tests/test_close_period_job.py` == 1
    - Uses ADVISORY_LOCK_KEY: `grep -c "ADVISORY_LOCK_KEY" tests/test_close_period_job.py` ≥ 2
    - Has _require_db helper: `grep -c "_require_db" tests/test_close_period_job.py` ≥ 2
    - Uses TRUNCATE setup: `grep -c "TRUNCATE TABLE" tests/test_close_period_job.py` ≥ 1
    - Tests fail BEFORE Task 2 with ImportError (RED state, acceptable). After Task 2 — все 8 PASS.
  </acceptance_criteria>
  <done>
    `tests/test_close_period_job.py` существует с 8 async-тестами, использует тот же DB-pattern что и test_balance.py, импортирует close_period_job (RED state — ImportError ожидаем до Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Реализация app/worker/jobs/close_period.py — close_period_job</name>
  <files>app/worker/__init__.py, app/worker/jobs/__init__.py, app/worker/jobs/close_period.py</files>
  <read_first>
    - app/worker/__init__.py (текущее состояние — пустой пакет; убедиться)
    - main_worker.py (heartbeat_job pattern — try/except/rollback/structlog)
    - app/services/actual.py:323-438 (compute_balance — что возвращает)
    - app/services/actual.py:100-110 (_get_cycle_start_day с fallback на 5)
    - app/services/periods.py:14-32 (_today_in_app_tz, get_current_active_period)
    - app/core/period.py (period_for сигнатура)
    - app/db/models.py:99-123 (BudgetPeriod, PeriodStatus)
  </read_first>
  <behavior>
    - close_period_job() — coroutine без аргументов
    - Выполняется внутри `async with AsyncSessionLocal() as session:`
    - Сначала захватывает `pg_try_advisory_lock(ADVISORY_LOCK_KEY)`; если False — early return + log "close_period.skipped.lock_not_acquired"
    - Находит active period с `period_end < today_msk`. Если нет — log "close_period.skipped.no_expired_period" + return
    - Если нашёл — вызывает `compute_balance(session, expired.id)`, берёт `balance_now_cents` как `ending_balance`
    - Устанавливает `expired.status = PeriodStatus.closed`, `expired.ending_balance_cents = ending_balance`, `expired.closed_at = datetime.now(timezone.utc)`
    - Берёт cycle_start_day через `get_cycle_start_day(session, settings.OWNER_TG_ID)` с fallback 5 при `UserNotFoundError`
    - Создаёт `BudgetPeriod(period_start=p_start, period_end=p_end, starting_balance_cents=ending_balance, status=PeriodStatus.active)` где `(p_start, p_end) = period_for(today, cycle_start_day)`
    - session.add(new_period), await session.commit()
    - log "close_period.done" с period_id_closed/period_id_created/ending_balance_cents
    - Любая Exception → session.rollback() + log.exception("close_period.failed"); finally — pg_advisory_unlock + commit (отдельная попытка)
  </behavior>
  <action>
    1. **Проверить `app/worker/__init__.py`** через ls — он уже создан в Phase 1. Если есть — НЕ трогать. Если нет — создать пустой.

    2. **Создать `app/worker/jobs/__init__.py`** (новый файл) с одной строкой docstring:
       ```python
       """Worker job modules (close_period; future: notify_subscriptions, charge_subscriptions)."""
       ```

    3. **Создать `app/worker/jobs/close_period.py`** с следующим содержимым (точно):

    ```python
    """close_period worker job — PER-04 / PER-03.

    Runs daily at 00:01 Europe/Moscow via APScheduler (main_worker.py).

    Behaviour:
        1. Acquire pg_try_advisory_lock(ADVISORY_LOCK_KEY); bail if False.
        2. SELECT active period with period_end < today_msk (expired).
        3. If none — no-op (idempotency: subsequent runs in same day exit here).
        4. Otherwise: compute ending_balance via actual.compute_balance, mark
           old period closed, INSERT next period with starting_balance_cents
           = ending_balance (PER-03), all in single DB transaction.
        5. On any error — session.rollback() + log.exception.
        6. Always release advisory lock in finally.

    Idempotency: re-running same day is safe — no expired active period exists
    after a successful close+create (new period covers today).
    """
    from datetime import datetime, timezone

    import structlog
    from sqlalchemy import select, text

    from app.core.period import period_for
    from app.core.settings import settings
    from app.db.models import BudgetPeriod, PeriodStatus
    from app.db.session import AsyncSessionLocal
    from app.services.actual import compute_balance
    from app.services.periods import _today_in_app_tz
    from app.services.settings import UserNotFoundError, get_cycle_start_day

    logger = structlog.get_logger(__name__)

    # Unique advisory lock key for close_period coordination.
    # 8-digit yyyymmdd-style int — disjoint from future job keys (Phase 6
    # notify=20250502, charge=20250503).
    ADVISORY_LOCK_KEY = 20250501


    async def _resolve_cycle_start_day(session) -> int:
        """Resolve cycle_start_day for OWNER_TG_ID with fallback to 5."""
        try:
            return await get_cycle_start_day(session, settings.OWNER_TG_ID)
        except UserNotFoundError:
            return 5


    async def close_period_job() -> None:
        """PER-04: close expired active period + create next period.

        Single DB transaction. Idempotent: no-op if no expired active period.
        Coordinated via pg_try_advisory_lock to prevent concurrent runs.
        """
        async with AsyncSessionLocal() as session:
            lock_acquired = False
            try:
                # Step 1: try to acquire advisory lock (non-blocking).
                lock_result = await session.execute(
                    text("SELECT pg_try_advisory_lock(:key)"),
                    {"key": ADVISORY_LOCK_KEY},
                )
                lock_acquired = bool(lock_result.scalar())
                if not lock_acquired:
                    logger.info("close_period.skipped.lock_not_acquired")
                    return

                today = _today_in_app_tz()

                # Step 2: find expired active period.
                stmt = (
                    select(BudgetPeriod)
                    .where(
                        BudgetPeriod.status == PeriodStatus.active,
                        BudgetPeriod.period_end < today,
                    )
                    .order_by(BudgetPeriod.period_start.desc())
                    .limit(1)
                )
                expired = (await session.execute(stmt)).scalar_one_or_none()

                if expired is None:
                    logger.info("close_period.skipped.no_expired_period")
                    return

                # Step 3: compute ending_balance via shared service.
                bal = await compute_balance(session, expired.id)
                ending_balance = bal["balance_now_cents"]

                # Step 4: close expired period.
                expired.status = PeriodStatus.closed
                expired.ending_balance_cents = ending_balance
                expired.closed_at = datetime.now(timezone.utc)

                # Step 5: create next period (PER-03 inheritance).
                cycle_start_day = await _resolve_cycle_start_day(session)
                p_start, p_end = period_for(today, cycle_start_day)
                new_period = BudgetPeriod(
                    period_start=p_start,
                    period_end=p_end,
                    starting_balance_cents=ending_balance,
                    status=PeriodStatus.active,
                )
                session.add(new_period)

                await session.commit()
                logger.info(
                    "close_period.done",
                    period_id_closed=expired.id,
                    period_id_created=new_period.id,
                    ending_balance_cents=ending_balance,
                )
            except Exception:
                await session.rollback()
                logger.exception("close_period.failed")
            finally:
                if lock_acquired:
                    try:
                        await session.execute(
                            text("SELECT pg_advisory_unlock(:key)"),
                            {"key": ADVISORY_LOCK_KEY},
                        )
                        await session.commit()
                    except Exception:
                        logger.exception("close_period.unlock_failed")
    ```

    4. **Проверка имён**: убедиться, что `from app.services.settings import UserNotFoundError, get_cycle_start_day` совпадает с фактическим экспортом в `app/services/settings.py`. Если в actual.py:60 уже работает этот импорт — значит он валиден. Если UserNotFoundError имеет другое имя в settings.py — заменить.

    5. ADVISORY_LOCK_KEY = 20250501 — int меньше 2^31, валидный для PostgreSQL pg_advisory_lock(bigint).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.worker.jobs.close_period import close_period_job, ADVISORY_LOCK_KEY; print('import_ok', ADVISORY_LOCK_KEY)" && DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test pytest tests/test_close_period_job.py -x --no-header 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f app/worker/jobs/close_period.py`
    - File exists: `test -f app/worker/jobs/__init__.py`
    - `grep -c "ADVISORY_LOCK_KEY = 20250501" app/worker/jobs/close_period.py` == 1
    - `grep -c "async def close_period_job" app/worker/jobs/close_period.py` == 1
    - `grep -c "pg_try_advisory_lock" app/worker/jobs/close_period.py` ≥ 1
    - `grep -c "pg_advisory_unlock" app/worker/jobs/close_period.py` ≥ 1
    - `grep -c "compute_balance" app/worker/jobs/close_period.py` ≥ 1
    - `grep -c "PeriodStatus.closed" app/worker/jobs/close_period.py` ≥ 1
    - `grep -c "PeriodStatus.active" app/worker/jobs/close_period.py` ≥ 1
    - `grep -c "from app.services.actual import compute_balance" app/worker/jobs/close_period.py` == 1
    - Python import successful (no syntax/import errors)
    - All 8 tests in test_close_period_job.py PASS (`pytest tests/test_close_period_job.py -x` → 8 passed)
  </acceptance_criteria>
  <done>
    `app/worker/jobs/close_period.py` создан, импортируется без ошибок, ADVISORY_LOCK_KEY=20250501, тесты test_close_period_job.py все 8 PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Регистрация close_period_job в main_worker.py с CronTrigger 00:01 Europe/Moscow</name>
  <files>main_worker.py</files>
  <read_first>
    - main_worker.py (полностью — добавляем add_job в существующий main())
    - app/worker/jobs/close_period.py (после Task 2 — импортируем close_period_job)
  </read_first>
  <behavior>
    - main_worker.py импортирует `close_period_job` из `app.worker.jobs.close_period`
    - В main() после `scheduler.add_job(heartbeat_job, ...)` добавлен реальный `add_job` для close_period с `cron, hour=0, minute=1, id="close_period", replace_existing=True, timezone=MOSCOW_TZ`
    - Существующий placeholder-comment про close_period (lines 76-82 текущего main_worker.py) удалён или заменён на реальный код
    - heartbeat_job add_job не трогаем
    - При импорте main_worker.py в Python нет ImportError
  </behavior>
  <action>
    1. **Открыть `main_worker.py`** (root проекта, НЕ `app/worker/main_worker.py`).

    2. **Добавить импорт** в секцию `from app.*` (после `from app.db.session import AsyncSessionLocal` или в любом месте импорт-блока):
       ```python
       from app.worker.jobs.close_period import close_period_job
       ```

    3. **В функции `main()`** найти placeholder-comments (строки ~76-82):
       ```python
       # Placeholder cron jobs (will be wired in Phase 5/6 against MOSCOW_TZ):
       # scheduler.add_job(notify_subscriptions, "cron", hour=9, minute=0,
       #                   id="notify_subscriptions")
       # scheduler.add_job(charge_subscriptions, "cron", hour=0, minute=5,
       #                   id="charge_subscriptions")
       # scheduler.add_job(close_period, "cron", hour=0, minute=1,
       #                   id="close_period")
       ```

       **Заменить весь этот блок** на:
       ```python
       # Phase 5: close_period — daily at 00:01 Europe/Moscow (PER-04).
       scheduler.add_job(
           close_period_job,
           "cron",
           hour=0,
           minute=1,
           id="close_period",
           replace_existing=True,
           timezone=MOSCOW_TZ,
       )

       # Placeholder cron jobs — Phase 6 (notify/charge subscriptions):
       # scheduler.add_job(notify_subscriptions, "cron", hour=9, minute=0,
       #                   id="notify_subscriptions", timezone=MOSCOW_TZ)
       # scheduler.add_job(charge_subscriptions, "cron", hour=0, minute=5,
       #                   id="charge_subscriptions", timezone=MOSCOW_TZ)
       ```

       Удалить старый close_period placeholder-comment, оставив только notify/charge как Phase 6 placeholders.

    4. **Не менять** heartbeat_job add_job, MOSCOW_TZ, asyncio loop в конце main().

    5. **Опционально** обновить module docstring lines 6-9: убрать упоминание "close_period... — Phase 6", т.к. теперь это Phase 5. Discretionary — не критично.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import main_worker; assert 'close_period_job' in dir(main_worker), 'close_period_job not imported'; print('main_worker_ok')" && grep -n "close_period_job" main_worker.py | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from app.worker.jobs.close_period import close_period_job" main_worker.py` == 1
    - `grep -c 'scheduler.add_job(\s*close_period_job' main_worker.py` == 1 (с возможными пробелами/переводами строк)
    - `grep -c '"cron"' main_worker.py` ≥ 1 (был 0 — добавлен)
    - `grep -c 'hour=0' main_worker.py` ≥ 1
    - `grep -c 'minute=1' main_worker.py` ≥ 1
    - `grep -c 'id="close_period"' main_worker.py` == 1
    - `grep -c 'timezone=MOSCOW_TZ' main_worker.py` ≥ 1
    - `python -c "import main_worker"` успешно (no syntax/import errors)
    - heartbeat_job add_job не удалён: `grep -c "scheduler.add_job(\s*heartbeat_job" main_worker.py` == 1 (с возможными пробелами)
  </acceptance_criteria>
  <done>
    main_worker.py импортирует close_period_job, регистрирует cron-job в 00:01 МСК с timezone=MOSCOW_TZ; placeholder заменён реальным кодом; heartbeat_job сохранён; модуль импортируется без ошибок.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → DB | Worker имеет прямой доступ к Postgres через AsyncSessionLocal — нет HTTP/auth слоя; защита через docker network isolation (Phase 1 INF-03) |
| Worker → Worker (concurrent) | Два инстанса worker-контейнера могут запустить close_period одновременно (например, при rolling deploy) — защита через pg_try_advisory_lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | close_period_job race | mitigate | `pg_try_advisory_lock(ADVISORY_LOCK_KEY)` обеспечивает single-instance execution. Тест `test_close_period_advisory_lock_prevents_concurrent` подтверждает поведение |
| T-05-07 | Repudiation | close_period_job audit trail | mitigate | structlog.info("close_period.done") с period_id_closed/period_id_created/ending_balance_cents → запись в worker logs. Postgres-side `closed_at` timestamp на BudgetPeriod добавляет дополнительный audit |
| T-05-08 | DoS | duplicate runs / lock starvation | mitigate | `pg_try_advisory_lock` неблокирующий; failure → graceful exit (не висит). Lock освобождается в `finally`. Если процесс убит SIGKILL — Postgres автоматически освободит lock при разрыве connection (advisory_lock per-session) |
| T-05-09 | Information Disclosure | structlog logs | accept | Logs пишут period_id и amounts, но это не PII. ending_balance_cents — деньги владельца, видны только в worker logs (single-tenant) |
| T-05-10 | Tampering | partial close (close OK, create fails) | mitigate | Single `await session.commit()` в конце транзакции. Любой exception → `session.rollback()` → исходный period возвращается в active state (T-05-08 тест test_close_period_rollback_on_error) |
| T-05-11 | Spoofing | external trigger of close_period | accept | Worker — отдельный контейнер, нет HTTP endpoint для close_period. Только APScheduler cron triggers внутри процесса. Spoofing невозможен извне |
</threat_model>

<verification>
- Все 3 task'а выполнены
- pytest tests/test_close_period_job.py → 8/8 passed (с DATABASE_URL заданным)
- python -c "import main_worker" успешно
- Manual smoke (опционально): запустить worker через `docker compose up worker`, проверить логи на "worker.scheduler.started" + список jobs
- Существующие тесты (test_balance.py, test_periods.py, test_actual_*) не сломаны
</verification>

<success_criteria>
1. close_period_job() корректно закрывает истёкший active period с правильным ending_balance_cents (= compute_balance.balance_now_cents)
2. Создаёт следующий period с starting_balance_cents равным ending_balance закрытого (PER-03)
3. Повторный запуск в тот же день — no-op (нет expired active periods)
4. Concurrent run protection работает через pg_try_advisory_lock
5. Все 8 тестов test_close_period_job.py PASS
6. main_worker.py регистрирует close_period в APScheduler с CronTrigger hour=0 minute=1 timezone=MOSCOW_TZ
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-period-lifecycle/05-02-SUMMARY.md` containing:
- Files created/modified (close_period.py, main_worker.py, test_close_period_job.py)
- Test results (8 tests passed)
- Manual verification: `python -c "import main_worker"` output
- Job schedule: "Daily 00:01 Europe/Moscow"
- Notes for Phase 6: ADVISORY_LOCK_KEY=20250501 reserved; suggest 20250502 for notify, 20250503 for charge
</output>
