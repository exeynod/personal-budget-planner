---
phase: 05-dashboard-period-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/utils/format.ts
  - frontend/src/api/types.ts
  - frontend/src/api/periods.ts
  - frontend/src/hooks/usePeriods.ts
  - frontend/src/hooks/useDashboard.ts
autonomous: true
requirements: [DSH-01, DSH-02, DSH-06]
tags: [frontend, hooks, api-client, types, money-formatting]
must_haves:
  truths:
    - "usePeriods() возвращает массив всех периодов из GET /api/v1/periods, отсортированный по period_start desc"
    - "useDashboard(periodId, isActiveCurrent) возвращает BalanceResponse"
    - "useDashboard корректно переключается между активным (/actual/balance) и архивным (/periods/{id}/balance) endpoint'ами"
    - "formatKopecks(cents) и formatKopecksWithSign(cents) — общие утилиты, ru-RU locale"
    - "Хуки следуют паттерну useCurrentPeriod: cancellation flag, refetch(), data | null + loading + error"
  artifacts:
    - path: frontend/src/utils/format.ts
      provides: "formatKopecks, formatKopecksWithSign, formatKopecksWithCurrency, parseRublesToKopecks"
      contains: "export function formatKopecks"
    - path: frontend/src/api/types.ts
      provides: "PeriodListResponse type alias + Phase 5 comment-section"
      contains: "Phase 5: Dashboard"
    - path: frontend/src/api/periods.ts
      provides: "listPeriods(), getPeriodBalance(periodId)"
      contains: "export async function listPeriods"
    - path: frontend/src/hooks/usePeriods.ts
      provides: "usePeriods() — fetch list + refetch"
      contains: "export function usePeriods"
    - path: frontend/src/hooks/useDashboard.ts
      provides: "useDashboard(periodId, isActiveCurrent) — fetch balance"
      contains: "export function useDashboard"
  key_links:
    - from: frontend/src/hooks/usePeriods.ts
      to: frontend/src/api/periods.ts::listPeriods
      via: "import + await listPeriods()"
      pattern: "listPeriods\\("
    - from: frontend/src/hooks/useDashboard.ts
      to: frontend/src/api/periods.ts::getPeriodBalance
      via: "import + await getPeriodBalance(periodId)"
      pattern: "getPeriodBalance\\("
    - from: frontend/src/hooks/useDashboard.ts
      to: frontend/src/api/actual.ts::getBalance
      via: "import + await getBalance() для активного периода"
      pattern: "getBalance\\(\\)"
    - from: frontend/src/api/periods.ts
      to: frontend/src/api/client.ts::apiFetch
      via: "import + apiFetch<PeriodRead[]>('/periods')"
      pattern: "apiFetch<"
---

<objective>
Создать frontend data-layer для Phase 5 dashboard:
1. **Утилиты форматирования денег** — централизованные `formatKopecks`/`formatKopecksWithSign`/`formatKopecksWithCurrency`/`parseRublesToKopecks` в `frontend/src/utils/format.ts`. Новые компоненты Phase 5 импортируют отсюда; рефактор существующих PlanRow/PlanItemEditor/ActualEditor — out of scope (deferred).
2. **API-обёртки** в `frontend/src/api/periods.ts` для GET /periods и GET /periods/{id}/balance (endpoint'ы создаются в Plan 05-01).
3. **Хуки** `usePeriods()` и `useDashboard(periodId, isActiveCurrent)` — следуют паттерну `useCurrentPeriod`/`usePlanned`.

Все три части необходимы для DSH-01 (hero/aggr/categories), DSH-02 (delta sign rendering требует formatKopecksWithSign), DSH-06 (PeriodSwitcher).

Output: 5 файлов с готовыми контрактами для Plan 05-04 (компоненты) и 05-05 (HomeScreen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md
@.planning/phases/05-dashboard-period-lifecycle/05-PATTERNS.md
@.planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md
@CLAUDE.md
@frontend/src/api/types.ts
@frontend/src/api/client.ts
@frontend/src/api/actual.ts
@frontend/src/hooks/useCurrentPeriod.ts
@frontend/src/hooks/useActual.ts
@frontend/src/hooks/usePlanned.ts
@frontend/src/components/PlanRow.tsx
@frontend/src/components/ActualEditor.tsx

<interfaces>
<!-- Существующие в кодовой базе (НЕ переписывать!) -->

From `frontend/src/api/types.ts:34-45`:
```typescript
export type PeriodStatus = 'active' | 'closed';
export interface PeriodRead {
  id: number;
  period_start: string; // ISO date
  period_end: string;
  starting_balance_cents: number;
  ending_balance_cents: number | null;
  status: PeriodStatus;
  closed_at: string | null;
}
```

From `frontend/src/api/types.ts:172-193`:
```typescript
export interface BalanceCategoryRow {
  category_id: number; name: string; kind: CategoryKind;
  planned_cents: number; actual_cents: number; delta_cents: number;
}
export interface BalanceResponse {
  period_id: number;
  period_start: string; period_end: string;
  starting_balance_cents: number;
  planned_total_expense_cents: number; actual_total_expense_cents: number;
  planned_total_income_cents: number; actual_total_income_cents: number;
  balance_now_cents: number; delta_total_cents: number;
  by_category: BalanceCategoryRow[];
}
```

From `frontend/src/api/client.ts`:
```typescript
export class ApiError extends Error { status: number; body: string; ... }
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>
// Throws ApiError on non-2xx; injects X-Telegram-Init-Data automatically.
```

From `frontend/src/api/actual.ts:24-26`:
```typescript
export async function getBalance(): Promise<BalanceResponse> {
  return apiFetch<BalanceResponse>('/actual/balance');
}
```

From `frontend/src/hooks/useCurrentPeriod.ts` (reference pattern):
```typescript
export function useCurrentPeriod(): UseCurrentPeriodResult {
  const [period, setPeriod] = useState<PeriodRead | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const refetch = useCallback(async () => { ... }, []);
  useEffect(() => {
    let cancelled = false;
    setLoading(true); setError(null);
    apiFetch<PeriodRead>('/periods/current')
      .then((data) => { if (!cancelled) setPeriod(data); })
      .catch((e: unknown) => {
        if (cancelled) return;
        if (e instanceof ApiError && e.status === 404) setPeriod(null);
        else setError(e instanceof Error ? e.message : String(e));
      })
      .finally(() => { if (!cancelled) setLoading(false); });
    return () => { cancelled = true; };
  }, []);
  return { period, loading, error, refetch };
}
```

From `frontend/src/components/PlanRow.tsx:25-27` (existing local formatter — НЕ рефакторить здесь):
```typescript
function formatRubles(cents: number): string {
  return (cents / 100).toLocaleString('ru-RU', { maximumFractionDigits: 2 });
}
```

From Plan 05-01 contract (создаётся параллельно):
```
GET /api/v1/periods              → PeriodRead[]  (sorted period_start desc)
GET /api/v1/periods/{id}/balance → BalanceResponse  (404 if missing)
GET /api/v1/actual/balance       → BalanceResponse  (404 if no active period)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: frontend/src/utils/format.ts — центральные утилиты formatKopecks / formatKopecksWithSign</name>
  <files>frontend/src/utils/format.ts</files>
  <read_first>
    - frontend/src/components/PlanRow.tsx:17-27 (существующий formatRubles + parseRublesToKopecks — копируем алгоритм)
    - frontend/src/components/PlanItemEditor.tsx:50-65 (существующий formatKopecksToRubles)
    - .planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md строки 60-75 (Typography — все деньги через --font-mono, формат "4 200 / 10 000 ₽")
  </read_first>
  <behavior>
    - **formatKopecks(cents: number): string** — без знака валюты, без знака +/-, ru-RU thousand separator
      - 0 → "0"
      - 420000 → "4 200"
      - -150050 → "-1 500,5" (минус для отрицательных через toLocaleString)
      - 1234567 → "12 345,67"
    - **formatKopecksWithSign(cents: number): string** — всегда со знаком, кроме нуля
      - 0 → "0"
      - 420000 → "+4 200"
      - -150000 → "-1 500"
    - **formatKopecksWithCurrency(cents: number): string** — "4 200 ₽" (без знака; для hero card)
    - **parseRublesToKopecks(input: string): number | null** — обратный парсер
    - НЕ float-арифметика между копейками; деление на 100 только в финальном `toLocaleString`
  </behavior>
  <action>
    Создать `frontend/src/utils/format.ts` следующего содержимого:

    ```typescript
    /**
     * Money formatting helpers — central utility used by Phase 5 dashboard
     * components (HeroCard, AggrStrip, DashboardCategoryRow). New components
     * import from here; existing PlanRow/PlanItemEditor/ActualEditor inline
     * formatters are NOT refactored in this plan (deferred).
     *
     * All amounts are kopecks (BIGINT in DB, number in JS — within
     * Number.MAX_SAFE_INTEGER for amounts up to ~90 trillion rubles).
     * Formatting uses ru-RU locale: nbsp thousands separator, comma decimal.
     */

    /**
     * Format kopecks as a Russian-localised number string (no currency, no sign).
     * Examples: 0 → "0", 420000 → "4 200", -150050 → "-1 500,5", 1234567 → "12 345,67".
     */
    export function formatKopecks(cents: number): string {
      const rubles = cents / 100;
      return rubles.toLocaleString('ru-RU', { maximumFractionDigits: 2 });
    }

    /**
     * Format kopecks with leading sign (+ or −). Zero rendered as "0" without sign.
     * Examples: 0 → "0", 420000 → "+4 200", -150000 → "-1 500".
     * Used for delta values where sign carries semantic meaning (D-02 sign rule).
     */
    export function formatKopecksWithSign(cents: number): string {
      if (cents === 0) return '0';
      const sign = cents > 0 ? '+' : '';
      // toLocaleString already prefixes minus for negatives.
      return sign + formatKopecks(cents);
    }

    /**
     * Format kopecks with currency symbol. Examples: 420000 → "4 200 ₽".
     * Used in hero card and aggr strip totals where ₽ is part of the visual.
     */
    export function formatKopecksWithCurrency(cents: number): string {
      return formatKopecks(cents) + ' ₽';
    }

    /**
     * Parse user-typed rubles string into kopecks integer. Returns null on invalid.
     * - "1500" → 150000
     * - "1500,50" → 150050
     * - "1 500" → 150000
     * - "" / "abc" / "0" / "-50" → null (zero/negative not allowed)
     */
    export function parseRublesToKopecks(input: string): number | null {
      const cleaned = input.replace(/\s/g, '').replace(',', '.');
      if (cleaned === '') return null;
      const f = parseFloat(cleaned);
      if (isNaN(f) || !isFinite(f) || f <= 0) return null;
      return Math.round(f * 100);
    }
    ```

    Папка `frontend/src/utils/` будет создана автоматически Write tool.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit src/utils/format.ts 2>&1 | tail -10; echo "---"; grep -c "^export function" src/utils/format.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/utils/format.ts`
    - `grep -c "^export function formatKopecks(" frontend/src/utils/format.ts` == 1
    - `grep -c "^export function formatKopecksWithSign(" frontend/src/utils/format.ts` == 1
    - `grep -c "^export function formatKopecksWithCurrency(" frontend/src/utils/format.ts` == 1
    - `grep -c "^export function parseRublesToKopecks(" frontend/src/utils/format.ts` == 1
    - `grep -c "toLocaleString('ru-RU'" frontend/src/utils/format.ts` ≥ 1
    - TypeScript компилирует файл без ошибок
  </acceptance_criteria>
  <done>
    `frontend/src/utils/format.ts` экспортирует 4 функции, компилируется без TS-ошибок, использует ru-RU locale.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: frontend/src/api/periods.ts + types.ts (Phase 5 секция) + frontend/src/hooks/usePeriods.ts</name>
  <files>frontend/src/api/types.ts, frontend/src/api/periods.ts, frontend/src/hooks/usePeriods.ts</files>
  <read_first>
    - frontend/src/api/types.ts (полностью — добавить секцию Phase 5; PeriodRead/BalanceResponse уже есть)
    - frontend/src/api/actual.ts (точный паттерн apiFetch wrappers)
    - frontend/src/hooks/useCurrentPeriod.ts (полный паттерн копируем)
    - frontend/src/api/client.ts:73-101 (apiFetch + ApiError)
  </read_first>
  <behavior>
    - **frontend/src/api/types.ts**: добавлена секция-комментарий `// ---------- Phase 5: Dashboard & Period Lifecycle ----------` и `export type PeriodListResponse = PeriodRead[]`. PeriodRead и BalanceResponse уже определены — не дублируем.
    - **frontend/src/api/periods.ts**: новый файл с двумя экспортами:
      - `listPeriods(): Promise<PeriodRead[]>` — GET /api/v1/periods
      - `getPeriodBalance(periodId: number): Promise<BalanceResponse>` — GET /api/v1/periods/{id}/balance
    - **frontend/src/hooks/usePeriods.ts**: новый хук, mirror useCurrentPeriod:
      - state: `periods: PeriodRead[], loading: boolean, error: string | null`
      - returns: `{ periods, loading, error, refetch }`
      - на mount: fetch + cancellation flag
      - 404 НЕ обрабатывается специально (для усеList 404 — реальная ошибка; пустой список приходит как `[]` от backend)
  </behavior>
  <action>
    1. **`frontend/src/api/types.ts`** — добавить в КОНЕЦ файла (после существующей Phase 4 секции):

       ```typescript

       // ---------- Phase 5: Dashboard & Period Lifecycle ----------

       /**
        * GET /api/v1/periods response — list of all periods sorted by period_start desc.
        * Type alias for documentation; consumers can use PeriodRead[] directly.
        */
       export type PeriodListResponse = PeriodRead[];
       ```
       НЕ дублировать PeriodRead и BalanceResponse — они уже определены ранее (lines 36-45 и 181-193).

    2. **Создать `frontend/src/api/periods.ts`**:

       ```typescript
       import { apiFetch } from './client';
       import type { BalanceResponse, PeriodRead } from './types';

       /**
        * GET /api/v1/periods — list all budget periods, newest first.
        * Returns empty array if no periods exist (not 404). Used by PeriodSwitcher (DSH-06).
        */
       export async function listPeriods(): Promise<PeriodRead[]> {
         return apiFetch<PeriodRead[]>('/periods');
       }

       /**
        * GET /api/v1/periods/{period_id}/balance — balance for any period (active or closed).
        * Throws ApiError(404) if period does not exist. Used for archived period view (DSH-05/06).
        */
       export async function getPeriodBalance(periodId: number): Promise<BalanceResponse> {
         return apiFetch<BalanceResponse>(`/periods/${periodId}/balance`);
       }
       ```

    3. **Создать `frontend/src/hooks/usePeriods.ts`** (точный mirror useCurrentPeriod, без 404-special-case, для list):

       ```typescript
       import { useCallback, useEffect, useState } from 'react';
       import { listPeriods } from '../api/periods';
       import type { PeriodRead } from '../api/types';

       export interface UsePeriodsResult {
         periods: PeriodRead[];
         loading: boolean;
         error: string | null;
         refetch: () => Promise<void>;
       }

       /**
        * Fetches all budget periods (`GET /api/v1/periods`).
        *
        * Used by PeriodSwitcher (Phase 5, DSH-06) to populate navigation.
        * Returns empty array (not error) when no periods exist.
        *
        * Mirrors `useCurrentPeriod` mount-effect cancellation pattern: a local
        * `cancelled` flag prevents stale state writes on unmount.
        * `refetch()` is exposed for after-mutation reload (e.g. after the
        * close_period worker job creates a new period).
        */
       export function usePeriods(): UsePeriodsResult {
         const [periods, setPeriods] = useState<PeriodRead[]>([]);
         const [loading, setLoading] = useState(true);
         const [error, setError] = useState<string | null>(null);

         const refetch = useCallback(async () => {
           setLoading(true);
           setError(null);
           try {
             const data = await listPeriods();
             setPeriods(data);
           } catch (e) {
             setError(e instanceof Error ? e.message : String(e));
           } finally {
             setLoading(false);
           }
         }, []);

         useEffect(() => {
           let cancelled = false;
           setLoading(true);
           setError(null);
           listPeriods()
             .then((data) => {
               if (!cancelled) setPeriods(data);
             })
             .catch((e: unknown) => {
               if (cancelled) return;
               setError(e instanceof Error ? e.message : String(e));
             })
             .finally(() => {
               if (!cancelled) setLoading(false);
             });
           return () => {
             cancelled = true;
           };
         }, []);

         return { periods, loading, error, refetch };
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "(periods\.ts|usePeriods\.ts|types\.ts)" | head -10; echo "---grep---"; grep -c "Phase 5: Dashboard" src/api/types.ts; grep -c "export async function listPeriods" src/api/periods.ts; grep -c "export function usePeriods" src/hooks/usePeriods.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/api/periods.ts`
    - File exists: `test -f frontend/src/hooks/usePeriods.ts`
    - `grep -c "// ---------- Phase 5: Dashboard" frontend/src/api/types.ts` == 1
    - `grep -c "export type PeriodListResponse" frontend/src/api/types.ts` == 1
    - `grep -c "export async function listPeriods" frontend/src/api/periods.ts` == 1
    - `grep -c "export async function getPeriodBalance" frontend/src/api/periods.ts` == 1
    - `grep -cE "apiFetch<PeriodRead\[\]>\('/periods'\)" frontend/src/api/periods.ts` == 1
    - `grep -cE "apiFetch<BalanceResponse>" frontend/src/api/periods.ts` ≥ 1
    - `grep -c "export function usePeriods" frontend/src/hooks/usePeriods.ts` == 1
    - `grep -c "let cancelled = false" frontend/src/hooks/usePeriods.ts` == 1
    - TypeScript: `cd frontend && npx tsc --noEmit` exits 0 (или ошибки только в нерелевантных файлах)
    - НИКАКОГО дублирования: `grep -c "interface PeriodRead" frontend/src/api/types.ts` == 1 (не добавили дубль)
  </acceptance_criteria>
  <done>
    types.ts расширен Phase 5 секцией; api/periods.ts экспортирует listPeriods + getPeriodBalance; hooks/usePeriods.ts работает по паттерну useCurrentPeriod; TS-компиляция чистая.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: frontend/src/hooks/useDashboard.ts — fetch balance с переключением active/archive endpoint'ов</name>
  <files>frontend/src/hooks/useDashboard.ts</files>
  <read_first>
    - frontend/src/hooks/usePlanned.ts (точный паттерн hook с periodId: number | null)
    - frontend/src/hooks/useActual.ts (тот же паттерн)
    - frontend/src/api/actual.ts:24-26 (getBalance — для активного периода)
    - frontend/src/api/periods.ts (после Task 2 — getPeriodBalance для архива)
    - .planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md `<code_context>` секция (useDashboard принимает periodId)
  </read_first>
  <behavior>
    - **useDashboard(periodId: number | null, isActiveCurrent: boolean): UseDashboardResult**:
      - `periodId === null && !isActiveCurrent` → не делать fetch, balance: null
      - `isActiveCurrent === true` → вызвать `getBalance()` (`/actual/balance`)
      - `isActiveCurrent === false && periodId !== null` → вызвать `getPeriodBalance(periodId)`
      - Re-fetch при изменении `periodId` или `isActiveCurrent`
      - Cancellation flag pattern (как в useActual)
      - Returns: `{ balance: BalanceResponse | null, loading, error, refetch }`
    - **Reasoning**: `/actual/balance` существует с Phase 4 и используется ботом, гарантированно возвращает текущий active period. `/periods/{id}/balance` — новый Phase 5. HomeScreen передаёт `isActiveCurrent = (selectedPeriodId === null) || (selectedPeriodId === currentPeriod?.id && currentPeriod?.status === 'active')`.
  </behavior>
  <action>
    Создать `frontend/src/hooks/useDashboard.ts`:

    ```typescript
    import { useCallback, useEffect, useState } from 'react';
    import { getBalance } from '../api/actual';
    import { getPeriodBalance } from '../api/periods';
    import type { BalanceResponse } from '../api/types';

    export interface UseDashboardResult {
      balance: BalanceResponse | null;
      loading: boolean;
      error: string | null;
      refetch: () => Promise<void>;
    }

    /**
     * Fetches BalanceResponse for the dashboard.
     *
     * @param periodId  Specific period id (or null to skip fetching when isActiveCurrent=false).
     * @param isActiveCurrent  When true, uses GET /actual/balance — current active period.
     *                  When false, uses GET /periods/{id}/balance — archived period (DSH-05/06).
     *
     * Mirrors `usePlanned`/`useActual` cancellation pattern. Re-fetches when
     * `periodId` or `isActiveCurrent` changes.
     */
    export function useDashboard(
      periodId: number | null,
      isActiveCurrent: boolean,
    ): UseDashboardResult {
      const [balance, setBalance] = useState<BalanceResponse | null>(null);
      const [loading, setLoading] = useState(periodId !== null || isActiveCurrent);
      const [error, setError] = useState<string | null>(null);

      const fetchBalance = useCallback(async (): Promise<BalanceResponse> => {
        if (isActiveCurrent) {
          return await getBalance();
        }
        if (periodId === null) {
          throw new Error('useDashboard: periodId is null and isActiveCurrent is false');
        }
        return await getPeriodBalance(periodId);
      }, [periodId, isActiveCurrent]);

      const refetch = useCallback(async () => {
        if (periodId === null && !isActiveCurrent) {
          setBalance(null);
          setLoading(false);
          return;
        }
        setLoading(true);
        setError(null);
        try {
          const data = await fetchBalance();
          setBalance(data);
        } catch (e) {
          setError(e instanceof Error ? e.message : String(e));
        } finally {
          setLoading(false);
        }
      }, [fetchBalance, periodId, isActiveCurrent]);

      useEffect(() => {
        if (periodId === null && !isActiveCurrent) {
          setBalance(null);
          setLoading(false);
          setError(null);
          return;
        }
        let cancelled = false;
        setLoading(true);
        setError(null);
        fetchBalance()
          .then((data) => {
            if (!cancelled) setBalance(data);
          })
          .catch((e: unknown) => {
            if (!cancelled) setError(e instanceof Error ? e.message : String(e));
          })
          .finally(() => {
            if (!cancelled) setLoading(false);
          });
        return () => {
          cancelled = true;
        };
      }, [periodId, isActiveCurrent, fetchBalance]);

      return { balance, loading, error, refetch };
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "useDashboard" | head -10; echo "---grep---"; grep -c "export function useDashboard" src/hooks/useDashboard.ts; grep -c "isActiveCurrent" src/hooks/useDashboard.ts; grep -c "let cancelled = false" src/hooks/useDashboard.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/hooks/useDashboard.ts`
    - `grep -c "export function useDashboard" frontend/src/hooks/useDashboard.ts` == 1
    - `grep -c "periodId: number | null" frontend/src/hooks/useDashboard.ts` ≥ 1
    - `grep -c "isActiveCurrent" frontend/src/hooks/useDashboard.ts` ≥ 4 (param + branch + refetch guard + useEffect dep)
    - `grep -c "let cancelled = false" frontend/src/hooks/useDashboard.ts` == 1
    - `grep -c "from '../api/actual'" frontend/src/hooks/useDashboard.ts` == 1
    - `grep -c "from '../api/periods'" frontend/src/hooks/useDashboard.ts` == 1
    - `grep -c "getBalance()" frontend/src/hooks/useDashboard.ts` ≥ 1
    - `grep -c "getPeriodBalance(periodId)" frontend/src/hooks/useDashboard.ts` ≥ 1
    - TypeScript: `cd frontend && npx tsc --noEmit` exits 0 (or only pre-existing errors in unrelated files)
  </acceptance_criteria>
  <done>
    `useDashboard` хук создан, переключается между `/actual/balance` (active) и `/periods/{id}/balance` (archive), cancellation pattern работает, TS-компиляция чистая.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API | apiFetch автоматически инжектит X-Telegram-Init-Data header (Phase 1 client.ts) — нет risk утечки initData |
| Component → Hook | hooks возвращают immutable data (`balance`, `periods`) через React state — никаких записей в localStorage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-12 | Information Disclosure | usePeriods/useDashboard storage | accept | Хуки хранят данные только в React state. Перезагрузка страницы → fresh fetch с auth. БД OWNER_TG_ID whitelist гарантирует, что только OWNER может получить данные |
| T-05-13 | DoS | infinite refetch loop | mitigate | Cancellation flag (`let cancelled = false`) предотвращает stale renders. useCallback dependencies `[periodId, isActiveCurrent]` явные — нет accidental re-creations |
| T-05-14 | Spoofing | apiFetch без initData в browser | mitigate | client.ts:90-92 уже подхватывает `dev-mode-stub` в DEV; в prod всегда injects реальный initData. Backend validate_init_data блокирует invalid header |
| T-05-15 | Tampering | mock-injection in hooks | accept | DEV-only `window.__inject*__` pattern уже использован в PlannedScreen (D-37). Для Phase 5 hooks НЕ вводим аналогичный escape hatch (балансы read-only, mock не нужен) |
</threat_model>

<verification>
- Все 3 task'а выполнены
- TS-компиляция чистая: `cd frontend && npx tsc --noEmit` exits 0 (нет новых ошибок, кроме pre-existing в unrelated файлах)
- Manual smoke: запустить `npm run dev` (frontend) — usePeriods/useDashboard hooks present
- Нет regressions в существующих tests/test_periods_api.py — этот plan не трогает backend
</verification>

<success_criteria>
1. formatKopecks/formatKopecksWithSign/formatKopecksWithCurrency работают по ru-RU locale
2. listPeriods() возвращает PeriodRead[]; getPeriodBalance(id) → BalanceResponse или ApiError(404)
3. usePeriods() returns {periods, loading, error, refetch} — корректно реагирует на refetch()
4. useDashboard(null, true) → uses /actual/balance; useDashboard(id, false) → uses /periods/{id}/balance
5. Все хуки используют cancellation flag pattern (как useCurrentPeriod)
6. TypeScript компиляция чистая — нет новых ошибок типов
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-period-lifecycle/05-03-SUMMARY.md` containing:
- Files created (utils/format.ts, api/periods.ts, hooks/usePeriods.ts, hooks/useDashboard.ts)
- Files modified (api/types.ts — added Phase 5 section)
- TS compilation result
- Hook signatures for Plan 05-04 consumers (HeroCard, PeriodSwitcher, AggrStrip, DashboardCategoryRow)
- Note: existing PlanRow/PlanItemEditor/ActualEditor inline formatters NOT refactored (deferred — out of scope for Phase 5)
</output>
