---
phase: 05-dashboard-period-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - frontend/src/components/HeroCard.tsx
  - frontend/src/components/HeroCard.module.css
  - frontend/src/components/PeriodSwitcher.tsx
  - frontend/src/components/PeriodSwitcher.module.css
  - frontend/src/components/AggrStrip.tsx
  - frontend/src/components/AggrStrip.module.css
  - frontend/src/components/DashboardCategoryRow.tsx
  - frontend/src/components/DashboardCategoryRow.module.css
autonomous: true
requirements: [DSH-01, DSH-02, DSH-03, DSH-05, DSH-06]
tags: [frontend, components, dashboard, ui-spec]
must_haves:
  truths:
    - "HeroCard рендерит balance_now_cents (или ending_balance_cents для closed) с заголовком 'Баланс' / 'Итог периода', period dates и delta со знаком и цветом"
    - "PeriodSwitcher показывает 'Май 2026' с ‹/› navigation; ‹ disabled если нет previous; › disabled если на текущем active; бейдж 'Закрыт' для closed"
    - "AggrStrip отображает 3 колонки План/Факт/Δ для активного таба (expense или income); знак дельты следует D-02"
    - "DashboardCategoryRow рендерит amount/planned, прогресс-бар (4px), warn-стиль ≥80%, overspend-стиль >100% с бейджем '123%'; нет прогресс-бара при planned=0"
    - "Все компоненты используют formatKopecks/formatKopecksWithSign из utils/format.ts"
    - "Все стили соответствуют 05-UI-SPEC.md (Typography 4 sizes, Color tokens из tokens.css, Spacing scale)"
  artifacts:
    - path: frontend/src/components/HeroCard.tsx
      provides: "HeroCard component (DSH-01 hero, DSH-02 delta sign, DSH-05 closed)"
      contains: "export function HeroCard"
    - path: frontend/src/components/PeriodSwitcher.tsx
      provides: "PeriodSwitcher component (DSH-06)"
      contains: "export function PeriodSwitcher"
    - path: frontend/src/components/AggrStrip.tsx
      provides: "AggrStrip component (DSH-01 aggr, DSH-02 delta)"
      contains: "export function AggrStrip"
    - path: frontend/src/components/DashboardCategoryRow.tsx
      provides: "DashboardCategoryRow component (DSH-01 list, DSH-03 warn/overspend)"
      contains: "export function DashboardCategoryRow"
  key_links:
    - from: frontend/src/components/HeroCard.tsx
      to: frontend/src/utils/format.ts::formatKopecksWithSign
      via: "import + render delta"
      pattern: "formatKopecksWithSign"
    - from: frontend/src/components/AggrStrip.tsx
      to: frontend/src/utils/format.ts::formatKopecksWithSign
      via: "import + delta rendering with sign"
      pattern: "formatKopecksWithSign"
    - from: frontend/src/components/DashboardCategoryRow.tsx
      to: frontend/src/utils/format.ts::formatKopecks
      via: "import + amount rendering"
      pattern: "formatKopecks"
---

<objective>
Создать 4 dashboard-компонента согласно 05-UI-SPEC.md:
1. **HeroCard** — premium градиент-карточка с балансом, period dates, delta. Поддерживает active (`balance_now_cents` + label "Баланс") и closed (`ending_balance_cents` + label "Итог периода") режимы. Покрывает DSH-01 (hero), DSH-02 (delta sign), DSH-05 (closed read-only).
2. **PeriodSwitcher** — горизонтальная панель с ‹ Май 2026 › navigation, badge "Закрыт" для closed периодов. DSH-06.
3. **AggrStrip** — фиксированная полоска под TabBar с 3 колонками: План / Факт / Δ для активного kind. DSH-01 (aggr), DSH-02 (delta).
4. **DashboardCategoryRow** — плотная строка категории с прогресс-баром, warn (≥80%) / overspend (>100%) стилями, overspend бейджем "123%". DSH-01 (list), DSH-03 (warn/overspend).

Все компоненты pure presentational — receive data via props, no fetching. Wiring в HomeScreen — Plan 05-05.

Output: 4 компонента + 4 CSS-модуля, готовые к импорту в HomeScreen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md
@.planning/phases/05-dashboard-period-lifecycle/05-PATTERNS.md
@.planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md
@CLAUDE.md
@frontend/src/styles/tokens.css
@frontend/src/api/types.ts
@frontend/src/utils/format.ts
@frontend/src/components/CategoryRow.tsx
@frontend/src/components/CategoryRow.module.css
@frontend/src/components/Fab.module.css

<interfaces>
<!-- Готовое после Plan 05-03 -->

From `frontend/src/utils/format.ts`:
```typescript
export function formatKopecks(cents: number): string;             // "4 200"
export function formatKopecksWithSign(cents: number): string;     // "+4 200" | "-1 500" | "0"
export function formatKopecksWithCurrency(cents: number): string; // "4 200 ₽"
```

From `frontend/src/api/types.ts`:
```typescript
export type CategoryKind = 'expense' | 'income';
export type PeriodStatus = 'active' | 'closed';

export interface PeriodRead {
  id: number; period_start: string; period_end: string;
  starting_balance_cents: number; ending_balance_cents: number | null;
  status: PeriodStatus; closed_at: string | null;
}

export interface BalanceCategoryRow {
  category_id: number; name: string; kind: CategoryKind;
  planned_cents: number; actual_cents: number; delta_cents: number;
}

export interface BalanceResponse {
  period_id: number; period_start: string; period_end: string;
  starting_balance_cents: number;
  planned_total_expense_cents: number; actual_total_expense_cents: number;
  planned_total_income_cents: number; actual_total_income_cents: number;
  balance_now_cents: number; delta_total_cents: number;
  by_category: BalanceCategoryRow[];
}
```

<!-- Design tokens — все уже в tokens.css. Используем напрямую в .module.css через var(--token). -->
From `frontend/src/styles/tokens.css`:
```
Colors:    --color-bg #0e1116, --color-surface #1c2230, --color-surface-2 #232a3a,
           --color-text #f3f5f9, --color-text-muted #8e98ad, --color-text-dim #5d6577,
           --color-primary #4ea4ff, --color-success #2ecc71, --color-danger #ff5d5d,
           --color-warn #ffb547, --color-danger-soft rgba(255,93,93,0.14)
Gradients: --gradient-hero, --gradient-hero-glow
Typography: --font-mono, --text-sm 13px, --text-base 15px, --text-md 17px, --text-2xl 32px,
            --weight-regular 400, --weight-semibold 600
Spacing:    --space-1..12 (4..48px)
Radius:     --radius-md 14px, --radius-lg 20px, --radius-full 9999px
Shadows:    --shadow-md, --shadow-lg
Layout:     --safe-bottom 28px, --main-button-height 54px
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HeroCard.tsx + PeriodSwitcher.tsx (+ CSS)</name>
  <files>frontend/src/components/HeroCard.tsx, frontend/src/components/HeroCard.module.css, frontend/src/components/PeriodSwitcher.tsx, frontend/src/components/PeriodSwitcher.module.css</files>
  <read_first>
    - frontend/src/components/CategoryRow.tsx (структура tsx-компонента, CSS Modules import)
    - frontend/src/components/CategoryRow.module.css (паттерн использования tokens)
    - frontend/src/utils/format.ts (formatKopecks, formatKopecksWithSign, formatKopecksWithCurrency)
    - .planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md строки 110-160 (HeroCard и PeriodSwitcher specs)
    - frontend/src/styles/tokens.css (все --color-*, --space-*, --text-*, --gradient-*)
  </read_first>
  <behavior>
    - **HeroCard** props: `{ balance: BalanceResponse, period: PeriodRead, isClosed: boolean }`. Рендерит:
      - 3 строки: period range "5 апр – 4 мая 2026", balance amount (большая цифра), delta строку
      - Если `isClosed` — берёт `period.ending_balance_cents` (если null → 0) с label "Итог периода"
      - Если `!isClosed` — берёт `balance.balance_now_cents` с label "Баланс"
      - Delta всегда `balance.delta_total_cents`, цвет: positive → success, negative → danger, zero → muted; через formatKopecksWithSign
      - Background: `--gradient-hero` + overlay `--gradient-hero-glow`, border-radius `--radius-lg`, padding `--space-5 --space-6`, shadow `--shadow-md`, min-height 120px
    - **PeriodSwitcher** props: `{ periods: PeriodRead[], selectedId: number, onSelect: (id: number) => void }`. Рендерит:
      - `[‹]  [<Month Year> [Закрыт?]]  [›]` в одну строку, height 36px
      - `idx = periods.findIndex(p => p.id === selectedId)`; periods отсортированы DESC по period_start (id 0 = newest)
      - `hasPrev = idx < periods.length - 1` (есть более старый период)
      - `hasNext = idx > 0` (есть более новый период)
      - ‹ disabled если !hasPrev; › disabled если !hasNext
      - Бейдж "Закрыт" если `current.status === 'closed'`
      - Format month label: `new Date(period_start).toLocaleDateString('ru-RU', { month: 'long', year: 'numeric' })` → "май 2026" (с маленькой), capitalize первой буквы
  </behavior>
  <action>
    1. **`frontend/src/components/HeroCard.tsx`**:

       ```typescript
       import type { BalanceResponse, PeriodRead } from '../api/types';
       import { formatKopecks, formatKopecksWithSign } from '../utils/format';
       import styles from './HeroCard.module.css';

       export interface HeroCardProps {
         balance: BalanceResponse;
         period: PeriodRead;
         /** True when viewing a closed/archived period — switches label and amount source. */
         isClosed: boolean;
       }

       /**
        * HeroCard — premium gradient balance card.
        *
        * Active mode (DSH-01): shows balance.balance_now_cents with label "Баланс".
        * Closed mode (DSH-05): shows period.ending_balance_cents with label "Итог периода".
        * Delta (DSH-02): formatted with sign, coloured by sign (success/danger/muted).
        */
       export function HeroCard({ balance, period, isClosed }: HeroCardProps) {
         const amountCents = isClosed
           ? period.ending_balance_cents ?? 0
           : balance.balance_now_cents;
         const amountLabel = isClosed ? 'Итог периода' : 'Баланс';

         const periodRange = formatPeriodRange(period.period_start, period.period_end);

         const delta = balance.delta_total_cents;
         const deltaCls =
           delta > 0
             ? styles.deltaPositive
             : delta < 0
               ? styles.deltaNegative
               : styles.deltaZero;

         return (
           <div className={styles.card}>
             <div className={styles.glow} aria-hidden />
             <div className={styles.periodRange}>{periodRange}</div>
             <div className={styles.amountWrap}>
               <span className={styles.amountLabel}>{amountLabel}</span>
               <span className={styles.amount}>{formatKopecks(amountCents)} ₽</span>
             </div>
             <div className={styles.deltaWrap}>
               <span className={styles.deltaLabel}>Δ</span>
               <span className={deltaCls}>{formatKopecksWithSign(delta)} ₽</span>
             </div>
           </div>
         );
       }

       function formatPeriodRange(startISO: string, endISO: string): string {
         const start = new Date(startISO);
         const end = new Date(endISO);
         const startStr = start.toLocaleDateString('ru-RU', { day: 'numeric', month: 'short' });
         const endStr = end.toLocaleDateString('ru-RU', {
           day: 'numeric', month: 'short', year: 'numeric',
         });
         return `${startStr} – ${endStr}`;
       }
       ```

    2. **`frontend/src/components/HeroCard.module.css`**:

       ```css
       .card {
         position: relative;
         min-height: 120px;
         padding: var(--space-5) var(--space-6);
         border-radius: var(--radius-lg);
         background: var(--gradient-hero);
         box-shadow: var(--shadow-md);
         color: var(--color-text);
         overflow: hidden;
       }
       .glow {
         position: absolute; inset: 0;
         background: var(--gradient-hero-glow);
         pointer-events: none;
       }
       .periodRange {
         position: relative;
         font-size: var(--text-sm);
         color: var(--color-text-muted);
         margin-bottom: var(--space-3);
       }
       .amountWrap {
         position: relative;
         display: flex; flex-direction: column; gap: var(--space-1);
         margin-bottom: var(--space-3);
       }
       .amountLabel {
         font-size: var(--text-sm);
         color: var(--color-text-muted);
         font-weight: var(--weight-regular);
       }
       .amount {
         font-family: var(--font-mono);
         font-size: var(--text-2xl);
         font-weight: var(--weight-semibold);
         line-height: 1.1;
         color: var(--color-text);
       }
       .deltaWrap {
         position: relative;
         display: flex; align-items: baseline; gap: var(--space-2);
       }
       .deltaLabel {
         font-size: var(--text-sm);
         color: var(--color-text-muted);
       }
       .deltaPositive, .deltaNegative, .deltaZero {
         font-family: var(--font-mono);
         font-size: var(--text-base);
         font-weight: var(--weight-semibold);
       }
       .deltaPositive { color: var(--color-success); }
       .deltaNegative { color: var(--color-danger); }
       .deltaZero     { color: var(--color-text-muted); }
       ```

    3. **`frontend/src/components/PeriodSwitcher.tsx`**:

       ```typescript
       import type { PeriodRead } from '../api/types';
       import styles from './PeriodSwitcher.module.css';

       export interface PeriodSwitcherProps {
         /** Periods sorted period_start DESC (newest first). */
         periods: PeriodRead[];
         selectedId: number;
         onSelect: (id: number) => void;
       }

       /**
        * PeriodSwitcher (DSH-06): horizontal navigation across budget periods.
        *
        * periods are sorted DESC by period_start, so:
        *   - hasPrev = idx < periods.length - 1  (older period exists)
        *   - hasNext = idx > 0                    (newer period exists)
        *
        * Renders "Закрыт" pill badge when current period is closed.
        */
       export function PeriodSwitcher({ periods, selectedId, onSelect }: PeriodSwitcherProps) {
         const idx = periods.findIndex((p) => p.id === selectedId);
         const current = idx >= 0 ? periods[idx] : undefined;
         const hasPrev = idx >= 0 && idx < periods.length - 1;
         const hasNext = idx > 0;

         const handlePrev = () => {
           if (hasPrev) onSelect(periods[idx + 1].id);
         };
         const handleNext = () => {
           if (hasNext) onSelect(periods[idx - 1].id);
         };

         return (
           <div className={styles.row}>
             <button
               type="button"
               onClick={handlePrev}
               disabled={!hasPrev}
               className={styles.navBtn}
               aria-label="Предыдущий период"
             >
               ‹
             </button>
             <span className={styles.label}>
               {current ? formatPeriodLabel(current) : '—'}
               {current?.status === 'closed' && (
                 <span className={styles.badge}>Закрыт</span>
               )}
             </span>
             <button
               type="button"
               onClick={handleNext}
               disabled={!hasNext}
               className={styles.navBtn}
               aria-label="Следующий период"
             >
               ›
             </button>
           </div>
         );
       }

       function formatPeriodLabel(period: PeriodRead): string {
         const d = new Date(period.period_start);
         const raw = d.toLocaleDateString('ru-RU', { month: 'long', year: 'numeric' });
         // Capitalise first letter: "май 2026" -> "Май 2026".
         return raw.charAt(0).toUpperCase() + raw.slice(1);
       }
       ```

    4. **`frontend/src/components/PeriodSwitcher.module.css`**:

       ```css
       .row {
         display: flex; align-items: center; justify-content: center;
         gap: var(--space-3);
         height: 36px;
         padding: 0 var(--space-4);
         margin-bottom: var(--space-3);
       }
       .navBtn {
         background: transparent; border: 0; padding: var(--space-1) var(--space-2);
         color: var(--color-primary);
         font-size: var(--text-md);
         font-weight: var(--weight-semibold);
         line-height: 1;
         cursor: pointer;
         min-width: 32px;
       }
       .navBtn:disabled {
         color: var(--color-text-dim);
         cursor: default;
         pointer-events: none;
       }
       .label {
         display: inline-flex; align-items: center; gap: var(--space-2);
         font-size: var(--text-base);
         font-weight: var(--weight-semibold);
         color: var(--color-text);
       }
       .badge {
         padding: 2px var(--space-2);
         border-radius: var(--radius-full);
         background: var(--color-surface-2);
         color: var(--color-text-muted);
         font-size: var(--text-sm);
         font-weight: var(--weight-regular);
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "(HeroCard|PeriodSwitcher)" | head -20; echo "---grep---"; grep -c "export function HeroCard" src/components/HeroCard.tsx; grep -c "export function PeriodSwitcher" src/components/PeriodSwitcher.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist: `test -f frontend/src/components/HeroCard.tsx && test -f frontend/src/components/HeroCard.module.css && test -f frontend/src/components/PeriodSwitcher.tsx && test -f frontend/src/components/PeriodSwitcher.module.css`
    - `grep -c "export function HeroCard" frontend/src/components/HeroCard.tsx` == 1
    - `grep -c "export interface HeroCardProps" frontend/src/components/HeroCard.tsx` == 1
    - `grep -c "isClosed" frontend/src/components/HeroCard.tsx` ≥ 3 (param, branching, type)
    - `grep -c "ending_balance_cents" frontend/src/components/HeroCard.tsx` ≥ 1
    - `grep -c "balance_now_cents" frontend/src/components/HeroCard.tsx` ≥ 1
    - `grep -c "formatKopecksWithSign" frontend/src/components/HeroCard.tsx` ≥ 1
    - `grep -c "Итог периода" frontend/src/components/HeroCard.tsx` ≥ 1
    - `grep -c "Баланс" frontend/src/components/HeroCard.tsx` ≥ 1
    - `grep -c "var(--gradient-hero)" frontend/src/components/HeroCard.module.css` ≥ 1
    - `grep -c "export function PeriodSwitcher" frontend/src/components/PeriodSwitcher.tsx` == 1
    - `grep -c "Закрыт" frontend/src/components/PeriodSwitcher.tsx` ≥ 1
    - `grep -c "aria-label=\"Предыдущий период\"" frontend/src/components/PeriodSwitcher.tsx` == 1
    - `grep -c "aria-label=\"Следующий период\"" frontend/src/components/PeriodSwitcher.tsx` == 1
    - `grep -c "hasPrev" frontend/src/components/PeriodSwitcher.tsx` ≥ 2
    - `grep -c "hasNext" frontend/src/components/PeriodSwitcher.tsx` ≥ 2
    - TS компилируется: `cd frontend && npx tsc --noEmit` exits 0 (или ошибки только в нерелевантных файлах)
  </acceptance_criteria>
  <done>
    HeroCard и PeriodSwitcher компоненты + CSS-модули созданы; TS компиляция чистая; все DSH-01/02/05/06 truths рендерятся согласно UI-SPEC.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AggrStrip.tsx + DashboardCategoryRow.tsx (+ CSS)</name>
  <files>frontend/src/components/AggrStrip.tsx, frontend/src/components/AggrStrip.module.css, frontend/src/components/DashboardCategoryRow.tsx, frontend/src/components/DashboardCategoryRow.module.css</files>
  <read_first>
    - frontend/src/api/types.ts (BalanceResponse, BalanceCategoryRow, CategoryKind)
    - frontend/src/utils/format.ts (formatKopecks, formatKopecksWithSign)
    - .planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md строки 138-175 (AggrStrip и DashboardCategoryRow specs)
    - frontend/src/components/CategoryRow.module.css (паттерн border, padding, hover)
  </read_first>
  <behavior>
    - **AggrStrip** props: `{ balance: BalanceResponse, kind: CategoryKind }`. Рендерит:
      - 3 колонки: "План" (planned_total), "Факт" (actual_total), "Δ" (delta) для kind
      - planned/actual berу по kind: expense → planned_total_expense_cents/actual_total_expense_cents; income → income аналоги
      - **Знак delta** (D-02): expense → `planned − actual`, income → `actual − planned`
      - Цвет delta: positive → success, negative → danger, zero → muted
      - Стиль: padding `--space-3 --space-4`, background `--color-surface`, border-bottom 1px `--color-border-subtle`
    - **DashboardCategoryRow** props: `{ row: BalanceCategoryRow }`. Рендерит:
      - Название слева (truncate ellipsis)
      - Справа: amount/planned "4 200 / 10 000 ₽"
      - Прогресс-бар 4px под amounts, fill = `min(actual/planned * 100, 100)%`
      - Цвет fill: <80% primary, ≥80% и ≤100% warn, >100% danger
      - Если `planned_cents === 0` — НЕ рендерить прогресс-бар (но строку оставить)
      - Warn-стиль (≥80% и ≤100%): row border 1px `--color-warn`
      - Overspend-стиль (>100%): row border 1px `--color-danger` + overspend badge "123%" в правом углу
      - Layout: padding `--space-3 --space-4`, background `--color-surface`, border-radius `--radius-md`, margin-bottom `--space-2`
  </behavior>
  <action>
    1. **`frontend/src/components/AggrStrip.tsx`**:

       ```typescript
       import type { BalanceResponse, CategoryKind } from '../api/types';
       import { formatKopecks, formatKopecksWithSign } from '../utils/format';
       import styles from './AggrStrip.module.css';

       export interface AggrStripProps {
         balance: BalanceResponse;
         /** Active tab — determines which kind's totals to show. */
         kind: CategoryKind;
       }

       /**
        * AggrStrip (DSH-01 aggr block, DSH-02 delta sign): 3-column strip below tabs.
        *
        * For active kind:
        *   - План  = planned_total_{kind}_cents
        *   - Факт  = actual_total_{kind}_cents
        *   - Δ     = (D-02 sign rule)
        *             expense: plan - actual  (positive = under-budget = good)
        *             income:  actual - plan  (positive = above-target = good)
        */
       export function AggrStrip({ balance, kind }: AggrStripProps) {
         const planned = kind === 'expense'
           ? balance.planned_total_expense_cents
           : balance.planned_total_income_cents;
         const actual = kind === 'expense'
           ? balance.actual_total_expense_cents
           : balance.actual_total_income_cents;
         const delta = kind === 'expense'
           ? planned - actual
           : actual - planned;

         const deltaCls =
           delta > 0
             ? styles.deltaPositive
             : delta < 0
               ? styles.deltaNegative
               : styles.deltaZero;

         return (
           <div className={styles.strip}>
             <div className={styles.col}>
               <div className={styles.label}>План</div>
               <div className={styles.value}>{formatKopecks(planned)} ₽</div>
             </div>
             <div className={styles.col}>
               <div className={styles.label}>Факт</div>
               <div className={styles.value}>{formatKopecks(actual)} ₽</div>
             </div>
             <div className={styles.col}>
               <div className={styles.label}>Δ</div>
               <div className={`${styles.value} ${deltaCls}`}>
                 {formatKopecksWithSign(delta)} ₽
               </div>
             </div>
           </div>
         );
       }
       ```

    2. **`frontend/src/components/AggrStrip.module.css`**:

       ```css
       .strip {
         display: grid;
         grid-template-columns: 1fr 1fr 1fr;
         gap: var(--space-5);
         padding: var(--space-3) var(--space-4);
         background: var(--color-surface);
         border-bottom: 1px solid var(--color-border-subtle);
       }
       .col {
         display: flex; flex-direction: column; gap: var(--space-1);
       }
       .label {
         font-size: var(--text-sm);
         color: var(--color-text-dim);
         text-transform: uppercase;
         letter-spacing: 0.06em;
         font-weight: var(--weight-semibold);
       }
       .value {
         font-family: var(--font-mono);
         font-size: var(--text-md);
         font-weight: var(--weight-semibold);
         color: var(--color-text);
       }
       .deltaPositive { color: var(--color-success); }
       .deltaNegative { color: var(--color-danger); }
       .deltaZero     { color: var(--color-text-muted); }
       ```

    3. **`frontend/src/components/DashboardCategoryRow.tsx`**:

       ```typescript
       import type { BalanceCategoryRow } from '../api/types';
       import { formatKopecks } from '../utils/format';
       import styles from './DashboardCategoryRow.module.css';

       export interface DashboardCategoryRowProps {
         row: BalanceCategoryRow;
       }

       /**
        * DashboardCategoryRow (DSH-01 list, DSH-03 warn/overspend):
        * Single row in the dashboard category list.
        *
        * Progress bar fill width = min(actual/planned * 100, 100)%.
        * Style states (D-02 sign-agnostic; based on consumption ratio):
        *   - normal  (<80%):  primary blue progress bar, no row border highlight
        *   - warn    (≥80% and ≤100%): warn yellow progress + 1px warn border
        *   - overspend (>100%): danger red progress + 1px danger border + "123%" badge
        * If planned_cents === 0, no progress bar is rendered at all.
        */
       export function DashboardCategoryRow({ row }: DashboardCategoryRowProps) {
         const hasPlanned = row.planned_cents > 0;
         const pct = hasPlanned ? row.actual_cents / row.planned_cents : 0;
         const isWarn = hasPlanned && pct >= 0.8 && pct <= 1.0;
         const isOverspend = hasPlanned && pct > 1.0;

         const rowCls = [
           styles.row,
           isWarn ? styles.warn : '',
           isOverspend ? styles.overspend : '',
         ].filter(Boolean).join(' ');

         const barFillCls = [
           styles.barFill,
           isWarn ? styles.barWarn : '',
           isOverspend ? styles.barOverspend : '',
         ].filter(Boolean).join(' ');

         const fillWidth = hasPlanned
           ? `${Math.min(pct * 100, 100)}%`
           : '0%';

         const overspendPct = isOverspend ? `${Math.round(pct * 100)}%` : null;

         return (
           <div className={rowCls}>
             <div className={styles.topRow}>
               <span className={styles.name}>{row.name}</span>
               <span className={styles.amounts}>
                 <span className={styles.actual}>{formatKopecks(row.actual_cents)}</span>
                 {hasPlanned && (
                   <>
                     <span className={styles.slash}> / </span>
                     <span className={styles.planned}>{formatKopecks(row.planned_cents)}</span>
                   </>
                 )}
                 <span className={styles.currency}> ₽</span>
                 {overspendPct && <span className={styles.badge}>{overspendPct}</span>}
               </span>
             </div>
             {hasPlanned && (
               <div className={styles.bar} aria-hidden>
                 <div className={barFillCls} style={{ width: fillWidth }} />
               </div>
             )}
           </div>
         );
       }
       ```

    4. **`frontend/src/components/DashboardCategoryRow.module.css`**:

       ```css
       .row {
         display: flex; flex-direction: column; gap: var(--space-2);
         padding: var(--space-3) var(--space-4);
         min-height: 52px;
         background: var(--color-surface);
         border: 1px solid transparent;
         border-radius: var(--radius-md);
         margin-bottom: var(--space-2);
       }
       .row:hover {
         background: var(--color-surface-2);
       }
       .row.warn {
         border-color: var(--color-warn);
       }
       .row.overspend {
         border-color: var(--color-danger);
       }
       .topRow {
         display: flex; align-items: baseline; justify-content: space-between;
         gap: var(--space-3);
       }
       .name {
         font-size: var(--text-base);
         font-weight: var(--weight-regular);
         color: var(--color-text);
         flex: 1;
         overflow: hidden;
         text-overflow: ellipsis;
         white-space: nowrap;
       }
       .amounts {
         display: inline-flex; align-items: baseline; gap: var(--space-1);
         font-family: var(--font-mono);
         font-size: var(--text-sm);
         flex-shrink: 0;
       }
       .actual { color: var(--color-text); }
       .slash { color: var(--color-text-dim); }
       .planned { color: var(--color-text-muted); }
       .currency { color: var(--color-text-muted); margin-left: 2px; }
       .badge {
         margin-left: var(--space-2);
         padding: 2px var(--space-2);
         border-radius: var(--radius-full);
         background: var(--color-danger-soft);
         color: var(--color-danger);
         font-family: var(--font-sans);
         font-size: var(--text-sm);
         font-weight: var(--weight-semibold);
       }
       .bar {
         height: 4px;
         border-radius: 2px;
         background: var(--color-border);
         overflow: hidden;
       }
       .barFill {
         height: 100%;
         background: var(--color-primary);
         transition: width 200ms ease-out;
       }
       .barFill.barWarn { background: var(--color-warn); }
       .barFill.barOverspend { background: var(--color-danger); }
       ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "(AggrStrip|DashboardCategoryRow)" | head -20; echo "---grep---"; grep -c "export function AggrStrip" src/components/AggrStrip.tsx; grep -c "export function DashboardCategoryRow" src/components/DashboardCategoryRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist: `test -f frontend/src/components/AggrStrip.tsx && test -f frontend/src/components/AggrStrip.module.css && test -f frontend/src/components/DashboardCategoryRow.tsx && test -f frontend/src/components/DashboardCategoryRow.module.css`
    - `grep -c "export function AggrStrip" frontend/src/components/AggrStrip.tsx` == 1
    - `grep -c "export interface AggrStripProps" frontend/src/components/AggrStrip.tsx` == 1
    - `grep -c "kind === 'expense'" frontend/src/components/AggrStrip.tsx` ≥ 3 (planned, actual, delta branches)
    - `grep -c "planned - actual" frontend/src/components/AggrStrip.tsx` ≥ 1 (D-02 expense delta)
    - `grep -c "actual - planned" frontend/src/components/AggrStrip.tsx` ≥ 1 (D-02 income delta)
    - `grep -c "formatKopecksWithSign" frontend/src/components/AggrStrip.tsx` ≥ 1
    - `grep -c "План" frontend/src/components/AggrStrip.tsx` == 1
    - `grep -c "Факт" frontend/src/components/AggrStrip.tsx` == 1
    - `grep -c "export function DashboardCategoryRow" frontend/src/components/DashboardCategoryRow.tsx` == 1
    - `grep -c "isWarn" frontend/src/components/DashboardCategoryRow.tsx` ≥ 3
    - `grep -c "isOverspend" frontend/src/components/DashboardCategoryRow.tsx` ≥ 3
    - `grep -c "pct >= 0.8" frontend/src/components/DashboardCategoryRow.tsx` ≥ 1
    - `grep -c "pct > 1.0" frontend/src/components/DashboardCategoryRow.tsx` ≥ 1
    - `grep -c "Math.min(pct \* 100, 100)" frontend/src/components/DashboardCategoryRow.tsx` ≥ 1
    - `grep -c "hasPlanned" frontend/src/components/DashboardCategoryRow.tsx` ≥ 3
    - `grep -c "var(--color-warn)" frontend/src/components/DashboardCategoryRow.module.css` ≥ 1
    - `grep -c "var(--color-danger)" frontend/src/components/DashboardCategoryRow.module.css` ≥ 1
    - TS компилируется: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    AggrStrip и DashboardCategoryRow компоненты + CSS-модули созданы; TS компиляция чистая; все DSH-01/02/03 truths рендерятся; warn/overspend стили работают.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → DOM | React strict text rendering — XSS-safe; никаких dangerouslySetInnerHTML; aria-label для arrows + aria-hidden для glow/bar |
| Component → Caller | Props read-only через React TypeScript типы; `onSelect` callback contract — caller validates id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Tampering | XSS via category.name | mitigate | React escapes text content в `{row.name}` автоматически. Нет `dangerouslySetInnerHTML` ни в одном из 4 компонентов. Подтверждается grep'ом: `grep -r "dangerouslySetInnerHTML" frontend/src/components/HeroCard frontend/src/components/PeriodSwitcher frontend/src/components/AggrStrip frontend/src/components/DashboardCategoryRow` returns 0 |
| T-05-17 | Information Disclosure | balance отображается на screen | accept | Single-tenant — данные принадлежат OWNER, который и видит экран. Нет screenshot-API leakage в Mini App контексте |
| T-05-18 | Tampering | onSelect callback с invalid id | accept | PeriodSwitcher вычисляет periods[idx ± 1].id — id берётся из массива, не из user input. Caller (HomeScreen) валидирует в Plan 05-05 |
| T-05-19 | DoS | бесконечный re-render через Math.min | accept | Все вычисления pure, deterministic, O(1). Нет infinite loops |
| T-05-20 | Spoofing | accessibility | mitigate | aria-label на arrows ("Предыдущий период", "Следующий период"), aria-hidden на decorative glow/bar — screen reader не путается. Закрытый период чётко отмечен текстом "Закрыт" (не только цветом) |
</threat_model>

<verification>
- Все 2 task'а выполнены (4 компонента + 4 CSS-модуля)
- TS-компиляция чистая: `cd frontend && npx tsc --noEmit` exits 0
- Manual smoke: import каждый компонент в isolated Storybook-like тестовый файл (или сразу в Plan 05-05)
- CSS-токены используются только из существующего tokens.css (нет magic-numbers)
</verification>

<success_criteria>
1. HeroCard рендерит correct label ("Баланс" vs "Итог периода") в зависимости от isClosed
2. HeroCard delta цвет: success / danger / muted в зависимости от знака
3. PeriodSwitcher disabled-state корректен: ‹ disabled на самом старом, › disabled на самом новом
4. PeriodSwitcher показывает "Закрыт" badge только для closed периодов
5. AggrStrip: D-02 правило знака — expense (plan-act), income (act-plan); цвет соответствует
6. DashboardCategoryRow: warn (1px warn border + warn fill) при ≥80%, overspend (1px danger + danger fill + "123%" badge) при >100%
7. DashboardCategoryRow без прогресс-бара когда planned_cents === 0
8. TS компиляция чистая для всех 4 компонентов
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-period-lifecycle/05-04-SUMMARY.md` containing:
- 8 файлов создано (4 .tsx + 4 .module.css)
- TS compilation result
- Component prop signatures для Plan 05-05 HomeScreen consumer
- Notes на дизайн-токенах (всё через var(--*), нет hardcoded цветов)
</output>
