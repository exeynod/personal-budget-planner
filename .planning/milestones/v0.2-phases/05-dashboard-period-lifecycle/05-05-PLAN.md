---
phase: 05-dashboard-period-lifecycle
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - frontend/src/screens/HomeScreen.tsx
  - frontend/src/screens/HomeScreen.module.css
  - frontend/src/App.tsx
autonomous: true
requirements: [DSH-01, DSH-02, DSH-03, DSH-04, DSH-05, DSH-06]
tags: [frontend, screen, integration, dashboard, edge-states]
must_haves:
  truths:
    - "Главный экран показывает HeroCard, PeriodSwitcher, TabBar (Расходы/Доходы), AggrStrip и список DashboardCategoryRow"
    - "Tab toggle переключает фильтр категорий по kind и AggrStrip totals"
    - "Empty state (нет ни одной планируемой строки в активном периоде) показывает 2 CTA: 'Применить шаблон' + 'Добавить вручную'"
    - "Apply Template вызывает POST /periods/{id}/apply-template, показывает toast 'Шаблон применён', refetch'ит useDashboard"
    - "Closed period: FAB hidden; MainButton visible, disabled с label 'Период закрыт'; PeriodSwitcher показывает 'Закрыт' badge"
    - "Active current period: FAB visible (открывает BottomSheet с ActualEditor); MainButton hidden"
    - "Archived/closed period: FAB hidden — мутации заблокированы во UI"
    - "‹/› в PeriodSwitcher работают: при выборе другого period_id обновляется useDashboard и HomeScreen рендерит данные нового периода"
    - "После создания нового actual через FAB+ActualEditor+createActual — useDashboard.refetch() обновляет дашборд"
  artifacts:
    - path: frontend/src/screens/HomeScreen.tsx
      provides: "Полный dashboard screen — DSH-01..06"
      contains: "useDashboard"
    - path: frontend/src/screens/HomeScreen.module.css
      provides: "Layout стили: tabs, sticky tabBar, scrollable list, edge-state spacing"
      contains: "tabBar"
    - path: frontend/src/App.tsx
      provides: "Routing с selectedPeriodId state lifted up"
      contains: "selectedPeriodId"
  key_links:
    - from: frontend/src/screens/HomeScreen.tsx
      to: frontend/src/hooks/useDashboard.ts
      via: "useDashboard(selectedPeriodId, isActiveCurrent)"
      pattern: "useDashboard\\("
    - from: frontend/src/screens/HomeScreen.tsx
      to: frontend/src/hooks/usePeriods.ts
      via: "usePeriods()"
      pattern: "usePeriods\\(\\)"
    - from: frontend/src/screens/HomeScreen.tsx
      to: frontend/src/api/planned.ts::applyTemplate
      via: "POST /periods/{id}/apply-template вызов из empty-state CTA"
      pattern: "applyTemplate\\("
    - from: frontend/src/screens/HomeScreen.tsx
      to: frontend/src/components/HeroCard.tsx
      via: "import + JSX usage"
      pattern: "<HeroCard"
    - from: frontend/src/screens/HomeScreen.tsx
      to: frontend/src/components/MainButton.tsx
      via: "tg.MainButton API для closed period"
      pattern: "MainButton"
---

<objective>
Полностью заменить placeholder HomeScreen на реальный dashboard:
- Layout: HeroCard → PeriodSwitcher → sticky TabBar (Расходы/Доходы) → AggrStrip → scrollable список DashboardCategoryRow → FAB | MainButton (closed) → BottomSheet (FAB add)
- Все 4 edge-state (DSH-04 empty / DSH-03 in-progress + warn ≥80% + overspend >100% / DSH-05 closed read-only)
- DSH-06 PeriodSwitcher навигация: ← / → переключают `selectedPeriodId`; useDashboard re-fetches
- Сохранить FAB + ActualEditor pipeline из текущего HomeScreen (Phase 4) для активного периода

App.tsx поднимает `selectedPeriodId` state в App-уровень (на случай если другие экраны нуждаются — но здесь достаточно local в HomeScreen). По CONTEXT.md рекомендация: lift up. Я выбираю **локальный state в HomeScreen** — другие экраны не используют `selectedPeriodId`; lifting усложняет props без выгоды.

Output: новый HomeScreen + CSS + App.tsx (минимально изменён — добавлен RoutingScreen для actual после создания).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md
@.planning/phases/05-dashboard-period-lifecycle/05-PATTERNS.md
@.planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md
@CLAUDE.md
@frontend/src/screens/HomeScreen.tsx
@frontend/src/screens/HomeScreen.module.css
@frontend/src/screens/PlannedScreen.tsx
@frontend/src/App.tsx
@frontend/src/api/planned.ts
@frontend/src/api/actual.ts
@frontend/src/hooks/useCurrentPeriod.ts
@frontend/src/hooks/useCategories.ts
@frontend/src/components/MainButton.tsx
@frontend/src/components/Fab.tsx
@frontend/src/components/BottomSheet.tsx
@frontend/src/components/ActualEditor.tsx
@frontend/src/styles/tokens.css

<interfaces>
<!-- Готовое после Plans 05-03 + 05-04 + предыдущих phase -->

From `frontend/src/hooks/usePeriods.ts` (Plan 05-03):
```typescript
export function usePeriods(): {
  periods: PeriodRead[]; loading: boolean; error: string | null;
  refetch: () => Promise<void>;
}
```

From `frontend/src/hooks/useDashboard.ts` (Plan 05-03):
```typescript
export function useDashboard(periodId: number | null, isActiveCurrent: boolean): {
  balance: BalanceResponse | null; loading: boolean; error: string | null;
  refetch: () => Promise<void>;
}
```

From `frontend/src/hooks/useCurrentPeriod.ts` (Phase 2):
```typescript
export function useCurrentPeriod(): {
  period: PeriodRead | null; loading: boolean; error: string | null;
  refetch: () => Promise<void>;
}
```

From `frontend/src/hooks/useCategories.ts`:
```typescript
export function useCategories(includeArchived: boolean): {
  categories: CategoryRead[]; loading: boolean; ...;
}
```

From `frontend/src/components/HeroCard.tsx` (Plan 05-04):
```typescript
export interface HeroCardProps {
  balance: BalanceResponse;
  period: PeriodRead;
  isClosed: boolean;
}
export function HeroCard(props: HeroCardProps): JSX.Element
```

From `frontend/src/components/PeriodSwitcher.tsx` (Plan 05-04):
```typescript
export interface PeriodSwitcherProps {
  periods: PeriodRead[]; selectedId: number; onSelect: (id: number) => void;
}
```

From `frontend/src/components/AggrStrip.tsx` (Plan 05-04):
```typescript
export interface AggrStripProps {
  balance: BalanceResponse; kind: CategoryKind;
}
```

From `frontend/src/components/DashboardCategoryRow.tsx` (Plan 05-04):
```typescript
export interface DashboardCategoryRowProps {
  row: BalanceCategoryRow;
}
```

From `frontend/src/api/planned.ts`:
```typescript
export async function applyTemplate(periodId: number): Promise<ApplyTemplateResponse>;
// Returns { period_id, created, planned: PlannedRead[] } — idempotent (D-31)
```

From `frontend/src/api/actual.ts`:
```typescript
export async function createActual(payload: ActualCreatePayload): Promise<ActualRead>;
```

From `frontend/src/components/MainButton.tsx`:
```typescript
// Telegram MainButton API wrapper — props: { text, onClick, disabled, visible }
// Renders при visible=true; пропадает при visible=false.
// Existing API: see file.
```

From `frontend/src/components/Fab.tsx`:
```typescript
export interface FabProps { onClick: () => void; ariaLabel: string; label?: string; }
export function Fab(props: FabProps): JSX.Element
// Position: fixed bottom-right (CSS-controlled via Fab.module.css)
```

From `frontend/src/components/BottomSheet.tsx`:
```typescript
// props: { open: boolean; onClose: () => void; title: string; children: ReactNode }
```

From `frontend/src/components/ActualEditor.tsx`:
```typescript
// props: { categories, onSave, onCancel, maxTxDate, initial?: ActualEditorInitial }
// onSave receives { kind, category_id, amount_cents, description, tx_date }
```

From `frontend/src/App.tsx` (current — Plan 05-05 НЕ ломает):
```typescript
type Screen = 'onboarding' | 'home' | 'categories' | 'template' | 'planned' | 'actual' | 'settings';
// HomeScreen уже имеет props: { onNavigate: (s) => void }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HomeScreen.tsx full replacement — dashboard layout + edge states</name>
  <files>frontend/src/screens/HomeScreen.tsx, frontend/src/screens/HomeScreen.module.css</files>
  <read_first>
    - frontend/src/screens/HomeScreen.tsx (текущий — placeholder с FAB; ПОЛНОСТЬЮ переписываем, но сохраняем FAB+ActualEditor pipeline)
    - frontend/src/screens/HomeScreen.module.css (существующий — расширяем/переписываем)
    - frontend/src/screens/PlannedScreen.tsx (паттерн toast + busy guard + mutationError + applyTemplate handler)
    - frontend/src/components/MainButton.tsx (понять API для closed period)
    - .planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md строки 188-260 (Screen Layout, Interaction Contracts)
    - frontend/src/styles/tokens.css (--main-button-height, --safe-bottom для bottom padding в closed mode)
  </read_first>
  <behavior>
    - **State**:
      - `useCurrentPeriod()` → currentPeriod (для определения "active current")
      - `usePeriods()` → periods (для switcher)
      - `selectedPeriodId: number | null` — local state, инициализируется из `currentPeriod.id` когда оно загрузится (через useEffect)
      - `isActiveCurrent` = `selectedPeriodId === null` || (`selectedPeriodId === currentPeriod?.id` && `currentPeriod?.status === 'active'`)
      - `useDashboard(selectedPeriodId, isActiveCurrent)` → balance
      - `selectedPeriod` = `periods.find(p => p.id === selectedPeriodId)` или `currentPeriod`
      - `isClosed` = `selectedPeriod?.status === 'closed'`
      - `activeTab: 'expense' | 'income'` — local state, default 'expense'
      - `useCategories(false)` → для category lookup (sort_order)
      - `sheetOpen, toast, busy, mutationError` — стандартные state-ы (как в PlannedScreen)
    - **Layout** (top-to-bottom):
      1. HeroCard (если balance + selectedPeriod loaded)
      2. PeriodSwitcher (если periods.length > 0)
      3. Sticky TabBar [Расходы / Доходы]
      4. AggrStrip (если balance loaded)
      5. Если empty state: EmptyStateDashboard inline (heading + CTA Применить шаблон + Добавить вручную)
      6. Иначе: scrollable список DashboardCategoryRow filtered by activeTab kind, sorted by category.sort_order
      7. FAB (только если isActiveCurrent && !isClosed)
      8. MainButton (только если isClosed)
      9. BottomSheet с ActualEditor (открывается из FAB)
      10. Toast (при mutation success/error)
    - **Filtering and sorting** (DashboardCategoryRow rows):
      - Фильтр: `balance.by_category.filter(r => r.kind === activeTab)`
      - Sort: stable order — сначала те что есть в `useCategories.categories` order (по `sort_order`), потом orphan'ы (если есть). Простейшая реализация: `categories.filter(c => c.kind === activeTab && rowsByCatId.has(c.id)).map(c => rowsByCatId.get(c.id))` — сохранит DB-порядок.
      - Если `balance.by_category` для активного kind пусто — это empty state (нет ни одной строки plan/actual для kind в этом периоде)
    - **Empty state detection** (DSH-04):
      - Considers `balance.planned_total_expense_cents + balance.planned_total_income_cents === 0` AND `balance.actual_total_expense_cents + balance.actual_total_income_cents === 0` → "плана нет совсем"
      - Альтернативный critique: если есть actual но нет planned — это in-progress, не empty. Используем `balance.by_category.filter(r => r.planned_cents > 0).length === 0` как критерий empty
    - **Apply Template handler**: `applyTemplate(currentPeriod.id)` → `useDashboard.refetch()` + toast "Шаблон применён" + busy guard
    - **Add Manually**: `onNavigate('planned')` → переход на PlannedScreen
    - **Period switching**: `onSelect={setSelectedPeriodId}` — переключатель меняет state, useDashboard автоматически refetch'ит (см. effect deps в Plan 05-03)
    - **MainButton closed**: `<MainButton text="Период закрыт" disabled visible={isClosed} />` — закрытие показывается всегда disabled
    - **FAB**: только при `isActiveCurrent && !isClosed`. При этом condition должен быть `selectedPeriod && selectedPeriod.status === 'active' && (selectedPeriodId === null || selectedPeriodId === currentPeriod?.id)`. Если selectedPeriod — другой active period (теоретически невозможно в normal flow, но защита) — FAB hidden.
  </behavior>
  <action>
    1. **Полная замена `frontend/src/screens/HomeScreen.tsx`** — НЕ оставлять placeholder. Сохранить:
       - Все существующие импорты, кроме placeholder-разметки
       - FAB + ActualEditor + BottomSheet pipeline для quick-add (логика handleSave из текущего HomeScreen.tsx остаётся в новой версии)
       - `onNavigate` prop signature (App.tsx уже передаёт)

    Новое содержимое (полное):

    ```typescript
    import { useEffect, useMemo, useState } from 'react';
    import { createActual } from '../api/actual';
    import { applyTemplate } from '../api/planned';
    import { useCategories } from '../hooks/useCategories';
    import { useCurrentPeriod } from '../hooks/useCurrentPeriod';
    import { useDashboard } from '../hooks/useDashboard';
    import { usePeriods } from '../hooks/usePeriods';
    import type { CategoryKind } from '../api/types';
    import { ActualEditor } from '../components/ActualEditor';
    import { AggrStrip } from '../components/AggrStrip';
    import { BottomSheet } from '../components/BottomSheet';
    import { DashboardCategoryRow } from '../components/DashboardCategoryRow';
    import { Fab } from '../components/Fab';
    import { HeroCard } from '../components/HeroCard';
    import { MainButton } from '../components/MainButton';
    import { PeriodSwitcher } from '../components/PeriodSwitcher';
    import styles from './HomeScreen.module.css';

    export interface HomeScreenProps {
      onNavigate: (
        screen: 'categories' | 'template' | 'planned' | 'actual' | 'settings',
      ) => void;
    }

    /**
     * HomeScreen — Dashboard (Phase 5, DSH-01..06).
     *
     * Layout (top→bottom):
     *   HeroCard → PeriodSwitcher → TabBar (sticky) → AggrStrip
     *   → DashboardCategoryRow list  OR  EmptyState (DSH-04)
     *   → Fab (active current only)  OR  MainButton (closed read-only DSH-05)
     *   → BottomSheet (FAB-triggered ActualEditor)
     *
     * Period selection (DSH-06): selectedPeriodId starts null, syncs with
     * useCurrentPeriod once it loads. PeriodSwitcher mutates selectedPeriodId;
     * useDashboard re-fetches automatically.
     */
    export function HomeScreen({ onNavigate }: HomeScreenProps) {
      const { period: currentPeriod, loading: curLoading } = useCurrentPeriod();
      const { periods } = usePeriods();
      const { categories } = useCategories(false);

      const [selectedPeriodId, setSelectedPeriodId] = useState<number | null>(null);
      const [activeTab, setActiveTab] = useState<CategoryKind>('expense');
      const [sheetOpen, setSheetOpen] = useState(false);
      const [toast, setToast] = useState<string | null>(null);
      const [busy, setBusy] = useState(false);
      const [mutationError, setMutationError] = useState<string | null>(null);

      // Sync selectedPeriodId to current period once it resolves.
      useEffect(() => {
        if (selectedPeriodId === null && currentPeriod) {
          setSelectedPeriodId(currentPeriod.id);
        }
      }, [currentPeriod, selectedPeriodId]);

      // Resolve selectedPeriod object (prefer periods list; fallback to currentPeriod).
      const selectedPeriod =
        periods.find((p) => p.id === selectedPeriodId) ?? currentPeriod;

      const isActiveCurrent =
        selectedPeriod !== null &&
        selectedPeriod !== undefined &&
        selectedPeriod.status === 'active' &&
        currentPeriod !== null &&
        selectedPeriod.id === currentPeriod.id;

      const isClosed = selectedPeriod?.status === 'closed';

      const {
        balance,
        loading: balLoading,
        error: balError,
        refetch: refetchDashboard,
      } = useDashboard(selectedPeriodId, isActiveCurrent);

      const showToast = (msg: string) => {
        setToast(msg);
        window.setTimeout(() => setToast(null), 2200);
      };

      // Empty state: nothing planned at all (no template applied, no manual rows).
      const isEmpty =
        balance !== null &&
        balance.by_category.filter((r) => r.planned_cents > 0).length === 0;

      // Filtered + sorted rows for active tab.
      const visibleRows = useMemo(() => {
        if (!balance) return [];
        const rowsByCatId = new Map(
          balance.by_category
            .filter((r) => r.kind === activeTab)
            .map((r) => [r.category_id, r]),
        );
        const sorted = categories
          .filter((c) => c.kind === activeTab && rowsByCatId.has(c.id))
          .map((c) => rowsByCatId.get(c.id)!)
          .filter((r) => r !== undefined);
        // Append any orphan rows (category not in useCategories — shouldn't happen).
        const knownIds = new Set(sorted.map((r) => r.category_id));
        const orphans = balance.by_category
          .filter((r) => r.kind === activeTab && !knownIds.has(r.category_id));
        return [...sorted, ...orphans];
      }, [balance, categories, activeTab]);

      const handleApplyTemplate = async () => {
        if (!currentPeriod || busy) return;
        setBusy(true);
        setMutationError(null);
        try {
          const result = await applyTemplate(currentPeriod.id);
          if (result.created === 0 && result.planned.length === 0) {
            showToast('Шаблон пуст — нечего применять');
          } else if (result.created === 0) {
            showToast('Шаблон уже применён');
          } else {
            showToast('Шаблон применён');
          }
          await refetchDashboard();
        } catch (e) {
          setMutationError(e instanceof Error ? e.message : String(e));
        } finally {
          setBusy(false);
        }
      };

      const handleAddManual = () => onNavigate('planned');

      const maxTxDate = (() => {
        const d = new Date();
        d.setDate(d.getDate() + 7);
        return d.toISOString().slice(0, 10);
      })();

      const handleSaveActual = async (data: {
        kind: CategoryKind;
        category_id: number;
        amount_cents: number;
        description: string | null;
        tx_date: string;
      }) => {
        await createActual(data);
        setSheetOpen(false);
        showToast('Транзакция добавлена');
        await refetchDashboard();
      };

      // Loading / no period guard.
      if (curLoading) {
        return <div className={styles.muted}>Загрузка периода…</div>;
      }
      if (!currentPeriod) {
        return (
          <div className={styles.root}>
            <div className={styles.empty}>Сначала завершите onboarding.</div>
          </div>
        );
      }

      return (
        <div className={`${styles.root} ${isClosed ? styles.rootClosed : ''}`}>
          {selectedPeriod && balance && (
            <div className={styles.heroWrap}>
              <HeroCard balance={balance} period={selectedPeriod} isClosed={isClosed} />
            </div>
          )}

          {periods.length > 0 && selectedPeriodId !== null && (
            <PeriodSwitcher
              periods={periods}
              selectedId={selectedPeriodId}
              onSelect={setSelectedPeriodId}
            />
          )}

          <div className={styles.tabBar}>
            <button
              type="button"
              onClick={() => setActiveTab('expense')}
              className={
                activeTab === 'expense' ? styles.tabActive : styles.tabInactive
              }
            >
              Расходы
            </button>
            <button
              type="button"
              onClick={() => setActiveTab('income')}
              className={
                activeTab === 'income' ? styles.tabActive : styles.tabInactive
              }
            >
              Доходы
            </button>
          </div>

          {balance && <AggrStrip balance={balance} kind={activeTab} />}

          {balLoading && <div className={styles.muted}>Загрузка дашборда…</div>}
          {balError && (
            <div className={styles.error}>
              Не удалось загрузить данные. Попробуй ещё раз.
            </div>
          )}
          {mutationError && <div className={styles.error}>Ошибка: {mutationError}</div>}

          {!balLoading && balance && isEmpty && (
            <div className={styles.emptyState}>
              <div className={styles.emptyHeading}>Бюджет не запланирован</div>
              <div className={styles.emptyBody}>
                Примени шаблон или добавь строки вручную
              </div>
              <button
                type="button"
                onClick={handleApplyTemplate}
                disabled={busy || !isActiveCurrent}
                className={styles.ctaPrimary}
              >
                {busy ? '…' : 'Применить шаблон'}
              </button>
              <button
                type="button"
                onClick={handleAddManual}
                disabled={busy || !isActiveCurrent}
                className={styles.ctaSecondary}
              >
                Добавить вручную
              </button>
            </div>
          )}

          {!balLoading && balance && !isEmpty && (
            <div className={styles.list}>
              {visibleRows.map((row) => (
                <DashboardCategoryRow key={row.category_id} row={row} />
              ))}
              {visibleRows.length === 0 && (
                <div className={styles.muted}>
                  В этом периоде нет {activeTab === 'expense' ? 'расходов' : 'доходов'}.
                </div>
              )}
            </div>
          )}

          {toast && <div className={styles.toast}>{toast}</div>}

          {/* Active current period only — quick-add via FAB. */}
          {isActiveCurrent && !isClosed && (
            <Fab
              onClick={() => setSheetOpen(true)}
              ariaLabel="Добавить факт-трату"
            />
          )}

          {/* Closed period — disabled MainButton placeholder. */}
          {isClosed && (
            <MainButton
              text="Период закрыт"
              onClick={() => undefined}
              disabled
              visible
            />
          )}

          <BottomSheet
            open={sheetOpen}
            onClose={() => setSheetOpen(false)}
            title="Новая транзакция"
          >
            <ActualEditor
              categories={categories}
              onSave={handleSaveActual}
              onCancel={() => setSheetOpen(false)}
              maxTxDate={maxTxDate}
            />
          </BottomSheet>
        </div>
      );
    }
    ```

    2. **Замена `frontend/src/screens/HomeScreen.module.css`** на полный layout под UI-SPEC:

    ```css
    .root {
      max-width: 375px;
      margin: 0 auto;
      padding: var(--space-4) var(--space-4) var(--space-12);
      min-height: 100vh;
      background: var(--color-bg);
      color: var(--color-text);
      font-family: var(--font-sans);
      display: flex;
      flex-direction: column;
    }
    .rootClosed {
      padding-bottom: calc(var(--safe-bottom) + var(--main-button-height) + 8px);
    }

    .heroWrap {
      margin-bottom: var(--space-4);
    }

    .tabBar {
      position: sticky;
      top: 0;
      z-index: 20;
      display: flex;
      height: 44px;
      background: var(--color-bg-elevated);
      margin: 0 calc(-1 * var(--space-4)); /* full-bleed inside root padding */
      padding: 0 var(--space-4);
    }
    .tabActive,
    .tabInactive {
      flex: 1;
      background: transparent;
      border: 0;
      cursor: pointer;
      font-family: inherit;
      font-size: var(--text-base);
      padding: var(--space-3) var(--space-2);
    }
    .tabActive {
      color: var(--color-text);
      font-weight: var(--weight-semibold);
      border-bottom: 2px solid var(--color-primary);
    }
    .tabInactive {
      color: var(--color-text-muted);
      font-weight: var(--weight-regular);
    }

    .list {
      display: flex;
      flex-direction: column;
      padding-top: var(--space-3);
    }

    .emptyState {
      display: flex;
      flex-direction: column;
      align-items: stretch;
      gap: var(--space-3);
      padding: var(--space-8) var(--space-4);
      text-align: center;
    }
    .emptyHeading {
      font-size: var(--text-md);
      font-weight: var(--weight-semibold);
      color: var(--color-text);
    }
    .emptyBody {
      font-size: var(--text-sm);
      color: var(--color-text-muted);
      margin-bottom: var(--space-3);
    }
    .ctaPrimary {
      width: 100%; height: 44px;
      background: var(--color-primary);
      color: #fff;
      border: 0;
      border-radius: var(--radius-md);
      font-family: inherit;
      font-size: var(--text-base);
      font-weight: var(--weight-semibold);
      cursor: pointer;
    }
    .ctaPrimary:disabled {
      opacity: 0.5;
      cursor: default;
    }
    .ctaSecondary {
      width: 100%; height: 44px;
      background: transparent;
      color: var(--color-text);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-md);
      font-family: inherit;
      font-size: var(--text-base);
      cursor: pointer;
    }
    .ctaSecondary:disabled {
      opacity: 0.5;
      cursor: default;
    }

    .muted {
      color: var(--color-text-muted);
      text-align: center;
      padding: var(--space-4);
      font-size: var(--text-sm);
    }
    .empty {
      color: var(--color-text-muted);
      text-align: center;
      padding: var(--space-8);
    }
    .error {
      color: var(--color-danger);
      background: var(--color-danger-soft);
      padding: var(--space-3);
      border-radius: var(--radius-md);
      margin: var(--space-3) 0;
      font-size: var(--text-sm);
    }

    .toast {
      position: fixed;
      bottom: 100px;
      left: 50%;
      transform: translateX(-50%);
      padding: 10px 16px;
      background: var(--color-success-soft);
      color: var(--color-success);
      border: 1px solid var(--color-success);
      border-radius: var(--radius-full);
      font-size: var(--text-sm);
      box-shadow: var(--shadow-md);
      z-index: 110;
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "HomeScreen" | head -20; echo "---grep---"; grep -c "useDashboard" src/screens/HomeScreen.tsx; grep -c "usePeriods" src/screens/HomeScreen.tsx; grep -c "PeriodSwitcher" src/screens/HomeScreen.tsx; grep -c "HeroCard" src/screens/HomeScreen.tsx; grep -c "AggrStrip" src/screens/HomeScreen.tsx; grep -c "DashboardCategoryRow" src/screens/HomeScreen.tsx; grep -c "isClosed" src/screens/HomeScreen.tsx; grep -c "isActiveCurrent" src/screens/HomeScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f frontend/src/screens/HomeScreen.tsx`
    - Файл больше не placeholder: `grep -c "Дашборд будет в Phase 5" frontend/src/screens/HomeScreen.tsx` == 0
    - Импорты Phase 5 hooks: `grep -c "from '../hooks/useDashboard'" frontend/src/screens/HomeScreen.tsx` == 1
    - `grep -c "from '../hooks/usePeriods'" frontend/src/screens/HomeScreen.tsx` == 1
    - Импорты Phase 5 components: `grep -c "from '../components/HeroCard'" frontend/src/screens/HomeScreen.tsx` == 1
    - `grep -c "from '../components/PeriodSwitcher'" frontend/src/screens/HomeScreen.tsx` == 1
    - `grep -c "from '../components/AggrStrip'" frontend/src/screens/HomeScreen.tsx` == 1
    - `grep -c "from '../components/DashboardCategoryRow'" frontend/src/screens/HomeScreen.tsx` == 1
    - JSX usage: `grep -c "<HeroCard" frontend/src/screens/HomeScreen.tsx` ≥ 1
    - `grep -c "<PeriodSwitcher" frontend/src/screens/HomeScreen.tsx` ≥ 1
    - `grep -c "<AggrStrip" frontend/src/screens/HomeScreen.tsx` ≥ 1
    - `grep -c "<DashboardCategoryRow" frontend/src/screens/HomeScreen.tsx` ≥ 1
    - State variables: `grep -c "selectedPeriodId" frontend/src/screens/HomeScreen.tsx` ≥ 4
    - `grep -c "isActiveCurrent" frontend/src/screens/HomeScreen.tsx` ≥ 3
    - `grep -c "isClosed" frontend/src/screens/HomeScreen.tsx` ≥ 3
    - `grep -c "activeTab" frontend/src/screens/HomeScreen.tsx` ≥ 3
    - Apply Template wired: `grep -c "applyTemplate(currentPeriod.id)" frontend/src/screens/HomeScreen.tsx` == 1
    - Toast pattern: `grep -c "showToast('Шаблон применён')" frontend/src/screens/HomeScreen.tsx` ≥ 1
    - Empty state copy: `grep -c "Бюджет не запланирован" frontend/src/screens/HomeScreen.tsx` == 1
    - Empty CTA: `grep -c "Применить шаблон" frontend/src/screens/HomeScreen.tsx` == 1
    - `grep -c "Добавить вручную" frontend/src/screens/HomeScreen.tsx` == 1
    - Closed MainButton: `grep -c "Период закрыт" frontend/src/screens/HomeScreen.tsx` == 1
    - FAB conditional: `grep -cE "isActiveCurrent && !isClosed" frontend/src/screens/HomeScreen.tsx` ≥ 1
    - Tabs: `grep -c "Расходы" frontend/src/screens/HomeScreen.tsx` == 1
    - `grep -c "Доходы" frontend/src/screens/HomeScreen.tsx` == 1
    - CSS: `test -f frontend/src/screens/HomeScreen.module.css`
    - CSS contains tabBar: `grep -c "\.tabBar" frontend/src/screens/HomeScreen.module.css` ≥ 1
    - CSS contains emptyState: `grep -c "\.emptyState" frontend/src/screens/HomeScreen.module.css` ≥ 1
    - CSS contains rootClosed bottom-padding: `grep -c "rootClosed" frontend/src/screens/HomeScreen.module.css` ≥ 1
    - TS компилируется: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    HomeScreen.tsx полностью переписан как dashboard, все DSH-01..06 truths рендерятся, edge states работают, TS-компиляция чистая. Существующий FAB+ActualEditor pipeline сохранён для активного периода.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: App.tsx минимальная адаптация (если нужно) + manual smoke</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (текущий — уже передаёт `onNavigate` в HomeScreen, ничего больше не нужно)
    - frontend/src/screens/HomeScreen.tsx (после Task 1 — props signature должен совпадать)
  </read_first>
  <behavior>
    - **Anti-goal**: НЕ менять App.tsx если HomeScreen props совместимы. CONTEXT.md рекомендует поднимать selectedPeriodId в App, но это усложняет без выгоды (другие экраны не используют). Оставляем local state в HomeScreen.
    - **Goal**: убедиться, что App.tsx по-прежнему компилируется и роутинг работает; HomeScreen props не сломались.
  </behavior>
  <action>
    1. **Открыть `frontend/src/App.tsx`** и проверить, что строки:
       ```typescript
       if (screen === 'home') {
         return <HomeScreen onNavigate={(s) => setOverrideScreen(s)} />;
       }
       ```
       совпадают с props signature нового HomeScreen (`onNavigate: (screen: 'categories' | 'template' | 'planned' | 'actual' | 'settings') => void`).

    2. **Если совместимо** — НЕ редактировать App.tsx, просто verify через TS-компиляцию.

    3. **Если HomeScreen в Task 1 случайно поменял signature** (например, добавил required prop) — Task 1 был неправильным; вернуться и исправить Task 1, чтобы prop signature остался обратно-совместимым с App.tsx.

    4. **Manual smoke check** через TS-компиляцию ВСЕЙ frontend кодовой базы (включая App.tsx → HomeScreen):
       ```
       cd frontend && npx tsc --noEmit
       ```
       Должен exit 0.

    5. **Если App.tsx нужно править** (только если необходимо): ничего не должно ломать другие экраны. App.tsx изменения, если они нужны — минимальные:
       - Добавить опционально новый screen tag (НЕ нужно — все экраны уже в union)
       - Передать `selectedPeriodId` как prop (НЕ нужно — local state в HomeScreen)
       
       Если требуется правка — описать почему в SUMMARY.

    Этот task в большинстве случаев — no-op (verification only).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | tail -20; echo "---grep_app---"; grep -c "<HomeScreen" src/App.tsx; grep -c "onNavigate=" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.tsx` существует и не сломан
    - `grep -c "<HomeScreen" frontend/src/App.tsx` == 1
    - `grep -c "onNavigate=" frontend/src/App.tsx` ≥ 1 (для HomeScreen + других экранов)
    - TypeScript компиляция всей кодовой базы чистая: `cd frontend && npx tsc --noEmit` exits 0
    - Build проходит: `cd frontend && npx vite build` exits 0 (если vite настроен на ts errors - блокировать)
  </acceptance_criteria>
  <done>
    App.tsx работает с новым HomeScreen без правок (если правки нужны — минимальные); вся frontend кодовая база компилируется чисто.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API | apiFetch автоматически добавляет X-Telegram-Init-Data; backend validate_init_data (Phase 1) |
| User → UI mutation | Все мутации (apply-template, create-actual) через FAB или CTA — guarded by `busy` state и `isActiveCurrent` flag |
| Closed period → mutations | UI блокирует мутации (FAB hidden, CTA disabled) — backend НЕ блокирует (приемлемо, см. T-05-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-21 | Tampering | Закрытый период мутируется через DevTools | accept | UI прячет FAB и empty-CTA при isClosed/non-active; пользователь — owner (single-tenant) — может технически сделать прямой fetch, но это не attack vector. Phase 6 при необходимости добавит server-side period.status check |
| T-05-22 | Repudiation | Apply Template trigger без consent | mitigate | Showtoast при success/error даёт user feedback; busy=true блокирует double-click; mutationError выводит inline message — нет silent failure |
| T-05-23 | DoS | Race condition при rapid period switch | mitigate | useDashboard cancellation flag (Plan 05-03) предотвращает stale fetch; useEffect deps `[periodId, isActiveCurrent, fetchBalance]` re-creates effect при switch |
| T-05-24 | Information Disclosure | balance отображается на screen | accept | Single-tenant — данные принадлежат OWNER, который видит экран. Mini App context защищён Telegram сессией |
| T-05-25 | Spoofing | onNavigate(unknown) | mitigate | TypeScript signature ограничивает screen string union; runtime caller (CTA "Добавить вручную") передаёт literal `'planned'` |
| T-05-26 | Tampering | mock-state injection в DEV | accept | DEV-only PlannedScreen имеет `__injectMockPlanned__` — для HomeScreen аналог не вводим (не требуется по UI-SPEC) |
</threat_model>

<verification>
- Все 2 task'а выполнены
- TS-компиляция чистая: `cd frontend && npx tsc --noEmit` exits 0
- Vite build success: `cd frontend && npx vite build` exits 0
- Manual smoke (deferred to Plan 05-06 checkpoint): запустить `npm run dev`, открыть Mini App в Telegram (или в DEV browser), проверить layout
</verification>

<success_criteria>
1. HomeScreen рендерит HeroCard + PeriodSwitcher + TabBar + AggrStrip + DashboardCategoryRow list
2. Tab toggle (Расходы/Доходы) переключает фильтр и AggrStrip totals
3. PeriodSwitcher переключает selectedPeriodId; useDashboard refetch'ит данные нового period
4. Empty state (нет планируемых строк) показывает 2 CTA — Apply Template + Add Manually
5. Apply Template → toast + dashboard refetch
6. Closed period: FAB hidden, MainButton "Период закрыт" disabled, hero показывает ending_balance
7. Active current period: FAB visible, MainButton hidden, hero показывает balance_now
8. TS компиляция и Vite build проходят без ошибок
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-period-lifecycle/05-05-SUMMARY.md` containing:
- Files modified (HomeScreen.tsx full replacement, HomeScreen.module.css, App.tsx if any)
- TS compilation + Vite build results
- Manual UI walkthrough notes (если есть смысл — но в основном для Plan 05-06)
- Notes for Plan 05-06 checkpoint (что проверить визуально: hero gradient, period switcher disabled states, warn/overspend bars, closed period read-only)
</output>
