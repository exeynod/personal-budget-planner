---
phase: 04-actual-transactions-and-bot-commands
plan: 05
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/actual.ts
  - frontend/src/hooks/useActual.ts
  - frontend/src/components/ActualEditor.tsx
  - frontend/src/components/ActualEditor.module.css
  - frontend/src/components/Fab.tsx
  - frontend/src/components/Fab.module.css
autonomous: true
requirements:
  - ACT-01

must_haves:
  truths:
    - "frontend/src/api/types.ts содержит мirror'нные TS-типы для ActualSource, ActualRead, ActualCreatePayload, ActualUpdatePayload, BalanceResponse, BalanceCategoryRow"
    - "frontend/src/api/actual.ts экспортирует listActual, createActual, updateActual, deleteActual, getBalance — все обёртки над apiFetch"
    - "frontend/src/hooks/useActual.ts следует паттерну usePlanned (cancellation guard, refetch, busy/error)"
    - "frontend/src/components/ActualEditor.tsx — новый form-компонент с kind toggle, amount, category select (filtered by kind), description, tx_date input"
    - "ActualEditor сбрасывает category_id при смене kind если old category — другого kind"
    - "frontend/src/components/Fab.tsx — простой floating-action-button компонент (props: onClick, ariaLabel, label?)"
    - "TypeScript build (vite build / tsc) проходит без ошибок: новые типы экспортируются, нет implicit any"
  artifacts:
    - path: "frontend/src/api/types.ts"
      provides: "EXTENDED with Phase 4 actual + balance types"
    - path: "frontend/src/api/actual.ts"
      provides: "listActual, createActual, updateActual, deleteActual, getBalance"
    - path: "frontend/src/hooks/useActual.ts"
      provides: "useActual(periodId) hook (rows + loading + error + refetch)"
    - path: "frontend/src/components/ActualEditor.tsx"
      provides: "Form для add/edit factual transaction (kind toggle + amount + category + description + tx_date)"
    - path: "frontend/src/components/ActualEditor.module.css"
      provides: "Styles for ActualEditor (segmented kind toggle + form)"
    - path: "frontend/src/components/Fab.tsx"
      provides: "FAB component (floating + symbol)"
    - path: "frontend/src/components/Fab.module.css"
      provides: "FAB styles (position fixed, circular, accent color)"
  key_links:
    - from: "frontend/src/api/actual.ts"
      to: "POST/PATCH/DELETE /api/v1/actual + GET /periods/{id}/actual + GET /actual/balance"
      via: "apiFetch wrapper calls"
      pattern: "/actual"
    - from: "frontend/src/components/ActualEditor.tsx"
      to: "frontend/src/api/types.ts (CategoryRead, ActualCreatePayload, ActualUpdatePayload)"
      via: "imports types"
      pattern: "from.*api/types"

user_setup: []
---

<objective>
Wave 3 (один из двух параллельных путей): создать frontend infrastructure для Phase 4: TS-типы, API обёртки, React hook, ActualEditor form, FAB component.

Это ВСЕ frontend «реквизиты» — НЕ экраны. Plan 04-06 (depends on 04-05) собирает реквизиты в ActualScreen + расширяет HomeScreen/App.tsx. Разделение позволяет parallelism: Plan 04-04 (bot) работает параллельно (другие файлы), а Plan 04-06 ждёт Plan 04-05.

Реализуется 7 файлов:
- frontend/src/api/types.ts — РАСШИРЕНИЕ (добавить Phase 4 типы).
- frontend/src/api/actual.ts — НОВЫЙ.
- frontend/src/hooks/useActual.ts — НОВЫЙ.
- frontend/src/components/ActualEditor.tsx + .module.css — НОВЫЕ.
- frontend/src/components/Fab.tsx + .module.css — НОВЫЕ.

Purpose: подготовить все «строительные блоки» для ActualScreen + HomeScreen FAB integration в Plan 04-06. Plan 04-06 не должен изобретать API/types/components.
Output: 7 frontend модулей.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-CONTEXT.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-RESEARCH.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-UI-SPEC.md
@frontend/src/api/types.ts
@frontend/src/api/planned.ts
@frontend/src/api/client.ts
@frontend/src/hooks/usePlanned.ts
@frontend/src/hooks/useCategories.ts
@frontend/src/hooks/useCurrentPeriod.ts
@frontend/src/components/PlanItemEditor.tsx
@frontend/src/components/PlanItemEditor.module.css
@frontend/src/components/BottomSheet.tsx
@frontend/src/screens/PlannedScreen.tsx

<interfaces>
Контракты для Plan 04-05.

### frontend/src/api/types.ts — additions (НЕ менять existing types)

```typescript
// ---------- Phase 4: Actual Transactions & Balance ----------

export type ActualSource = 'mini_app' | 'bot';

/** Mirrors ActualRead from app/api/schemas/actual.py. */
export interface ActualRead {
  id: number;
  period_id: number;
  kind: CategoryKind;
  amount_cents: number;
  description: string | null;
  category_id: number;
  tx_date: string;       // ISO date
  source: ActualSource;
  created_at: string;    // ISO datetime
}

export interface ActualCreatePayload {
  kind: CategoryKind;
  amount_cents: number;
  description?: string | null;
  category_id: number;
  tx_date: string;       // ISO date — required
}

export interface ActualUpdatePayload {
  kind?: CategoryKind;
  amount_cents?: number;
  description?: string | null;
  category_id?: number;
  tx_date?: string;
}

export interface BalanceCategoryRow {
  category_id: number;
  name: string;
  kind: CategoryKind;
  planned_cents: number;
  actual_cents: number;
  delta_cents: number;
}

export interface BalanceResponse {
  period_id: number;
  period_start: string;
  period_end: string;
  starting_balance_cents: number;
  planned_total_expense_cents: number;
  actual_total_expense_cents: number;
  planned_total_income_cents: number;
  actual_total_income_cents: number;
  balance_now_cents: number;
  delta_total_cents: number;
  by_category: BalanceCategoryRow[];
}
```

### frontend/src/api/actual.ts

```typescript
import { apiFetch } from './client';
import type {
  ActualCreatePayload, ActualRead, ActualUpdatePayload, BalanceResponse,
} from './types';

/** GET /api/v1/periods/{period_id}/actual?kind=&category_id= */
export async function listActual(
  periodId: number,
  filters?: { kind?: 'expense' | 'income'; category_id?: number },
): Promise<ActualRead[]> {
  const qs = new URLSearchParams();
  if (filters?.kind) qs.set('kind', filters.kind);
  if (filters?.category_id !== undefined) qs.set('category_id', String(filters.category_id));
  const suffix = qs.toString() ? `?${qs.toString()}` : '';
  return apiFetch<ActualRead[]>(`/periods/${periodId}/actual${suffix}`);
}

/** POST /api/v1/actual — source='mini_app' set server-side */
export async function createActual(payload: ActualCreatePayload): Promise<ActualRead> {
  return apiFetch<ActualRead>('/actual', {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

/** PATCH /api/v1/actual/{id} — partial; tx_date change auto-recomputes period_id (ACT-05) */
export async function updateActual(id: number, patch: ActualUpdatePayload): Promise<ActualRead> {
  return apiFetch<ActualRead>(`/actual/${id}`, {
    method: 'PATCH',
    body: JSON.stringify(patch),
  });
}

/** DELETE /api/v1/actual/{id} — hard delete */
export async function deleteActual(id: number): Promise<ActualRead> {
  return apiFetch<ActualRead>(`/actual/${id}`, { method: 'DELETE' });
}

/** GET /api/v1/actual/balance — for active period */
export async function getBalance(): Promise<BalanceResponse> {
  return apiFetch<BalanceResponse>('/actual/balance');
}
```

### frontend/src/hooks/useActual.ts

Mirror frontend/src/hooks/usePlanned.ts полностью. Только разница — вызывает listActual (а не listPlanned):

```typescript
import { useCallback, useEffect, useState } from 'react';
import { listActual } from '../api/actual';
import type { ActualRead } from '../api/types';

export interface UseActualResult {
  rows: ActualRead[];
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

export function useActual(periodId: number | null): UseActualResult {
  // ... mirror usePlanned (same shape, listActual call)
}
```

### frontend/src/components/ActualEditor.tsx

```typescript
import { useEffect, useMemo, useState } from 'react';
import type { CategoryKind, CategoryRead } from '../api/types';
import styles from './ActualEditor.module.css';

export interface ActualEditorInitial {
  kind?: CategoryKind;
  amount_cents?: number;
  description?: string | null;
  category_id?: number;
  tx_date?: string;
}

export interface ActualEditorSavePayload {
  kind: CategoryKind;
  category_id: number;
  amount_cents: number;
  description: string | null;
  tx_date: string;
}

export interface ActualEditorProps {
  initial?: ActualEditorInitial;
  categories: CategoryRead[];
  /** Save handler. Throw to surface error inline. */
  onSave: (data: ActualEditorSavePayload) => Promise<void>;
  /** Provide for edit mode; omit для create. */
  onDelete?: () => Promise<void>;
  onCancel: () => void;
  /** Optional max date (default = today + 7d in local TZ). */
  maxTxDate?: string;
}

// Money helpers — copy из PlanItemEditor (D-63: НЕ refactor сейчас)
function parseRublesToKopecks(input: string): number | null {
  const cleaned = input.replace(/\s/g, '').replace(',', '.');
  if (cleaned === '') return null;
  const f = parseFloat(cleaned);
  if (isNaN(f) || !isFinite(f)) return null;
  return Math.round(f * 100);
}

function formatKopecksToRubles(cents: number | undefined | null): string {
  if (cents === undefined || cents === null) return '';
  return (cents / 100).toLocaleString('ru-RU', { maximumFractionDigits: 2 });
}

function todayISO(): string {
  return new Date().toISOString().slice(0, 10);
}

function maxTxDateDefault(): string {
  const d = new Date();
  d.setDate(d.getDate() + 7);
  return d.toISOString().slice(0, 10);
}

export function ActualEditor({
  initial, categories, onSave, onDelete, onCancel, maxTxDate,
}: ActualEditorProps) {
  const [kind, setKind] = useState<CategoryKind>(initial?.kind ?? 'expense');
  const [amountStr, setAmountStr] = useState<string>(formatKopecksToRubles(initial?.amount_cents));
  const [categoryId, setCategoryId] = useState<number | ''>(initial?.category_id ?? '');
  const [description, setDescription] = useState<string>(initial?.description ?? '');
  const [txDate, setTxDate] = useState<string>(initial?.tx_date ?? todayISO());
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const filteredCats = useMemo(
    () => categories.filter((c) => c.kind === kind && !c.is_archived),
    [categories, kind],
  );

  // Reset categoryId if switching kind invalidates current selection
  useEffect(() => {
    if (categoryId !== '' && !filteredCats.some((c) => c.id === categoryId)) {
      setCategoryId('');
    }
  }, [filteredCats, categoryId]);

  const amountCents = parseRublesToKopecks(amountStr);
  const canSubmit =
    categoryId !== '' && amountCents !== null && amountCents > 0 && txDate !== '' && !submitting;

  const handleSubmit = async () => {
    if (!canSubmit) return;
    setSubmitting(true);
    setError(null);
    try {
      await onSave({
        kind,
        category_id: Number(categoryId),
        amount_cents: amountCents!,
        description: description.trim() === '' ? null : description.trim(),
        tx_date: txDate,
      });
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setSubmitting(false);
    }
  };

  const handleDelete = async () => {
    if (!onDelete || submitting) return;
    if (!window.confirm('Удалить запись?')) return;
    setSubmitting(true);
    setError(null);
    try {
      await onDelete();
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setSubmitting(false);
    }
  };

  const isEdit = onDelete !== undefined;

  return (
    <div className={styles.form}>
      {/* Kind toggle (segmented control) */}
      <div className={styles.kindToggle}>
        <button
          type="button"
          className={`${styles.kindBtn} ${kind === 'expense' ? styles.kindBtnActive : ''}`}
          onClick={() => setKind('expense')}
          disabled={submitting}
        >
          Расход
        </button>
        <button
          type="button"
          className={`${styles.kindBtn} ${kind === 'income' ? styles.kindBtnActive : ''}`}
          onClick={() => setKind('income')}
          disabled={submitting}
        >
          Доход
        </button>
      </div>

      {/* Amount */}
      <label className={styles.field}>
        <span className={styles.label}>Сумма (₽)</span>
        <input
          type="text"
          inputMode="decimal"
          placeholder="1500"
          value={amountStr}
          onChange={(e) => setAmountStr(e.target.value)}
          disabled={submitting}
          className={styles.input}
          autoFocus={!isEdit}
        />
      </label>

      {/* Category select */}
      <label className={styles.field}>
        <span className={styles.label}>Категория</span>
        <select
          value={categoryId}
          onChange={(e) => setCategoryId(e.target.value === '' ? '' : Number(e.target.value))}
          disabled={submitting}
          className={styles.select}
        >
          <option value="">— выберите —</option>
          {filteredCats.map((c) => (
            <option key={c.id} value={c.id}>{c.name}</option>
          ))}
        </select>
      </label>

      {/* Description */}
      <label className={styles.field}>
        <span className={styles.label}>Описание</span>
        <textarea
          value={description}
          onChange={(e) => setDescription(e.target.value)}
          maxLength={500}
          rows={2}
          disabled={submitting}
          className={styles.textarea}
        />
      </label>

      {/* tx_date */}
      <label className={styles.field}>
        <span className={styles.label}>Дата</span>
        <input
          type="date"
          value={txDate}
          max={maxTxDate ?? maxTxDateDefault()}
          onChange={(e) => setTxDate(e.target.value)}
          disabled={submitting}
          className={styles.input}
        />
      </label>

      {error && <div className={styles.error}>Ошибка: {error}</div>}

      <div className={styles.actions}>
        {isEdit && onDelete && (
          <button type="button" onClick={handleDelete} disabled={submitting} className={styles.deleteBtn}>
            Удалить
          </button>
        )}
        <button type="button" onClick={onCancel} disabled={submitting} className={styles.cancelBtn}>
          Отмена
        </button>
        <button type="button" onClick={handleSubmit} disabled={!canSubmit} className={styles.saveBtn}>
          {submitting ? 'Сохранение…' : 'Сохранить'}
        </button>
      </div>
    </div>
  );
}
```

### frontend/src/components/ActualEditor.module.css

Mirror frontend/src/components/PlanItemEditor.module.css structure (form / field / label / input / textarea / actions / saveBtn / cancelBtn / deleteBtn / error). Add new:

```css
.kindToggle {
  display: flex;
  gap: 8px;
  margin-bottom: var(--space-3);
}
.kindBtn {
  flex: 1;
  padding: 10px;
  background: var(--color-surface-2, #2a2a2a);
  color: var(--color-text-muted);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  cursor: pointer;
  font-size: var(--text-sm);
}
.kindBtn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
.kindBtnActive {
  background: var(--color-accent);
  color: var(--color-on-accent, #fff);
  border-color: var(--color-accent);
}
```

Остальные стили — copy/paste из PlanItemEditor.module.css (form/field/label/input/etc.).

### frontend/src/components/Fab.tsx

```typescript
import styles from './Fab.module.css';

export interface FabProps {
  onClick: () => void;
  ariaLabel: string;
  /** Default '+' (plus sign). */
  label?: string;
}

/**
 * Floating Action Button — bottom-right, circular, accent-colored.
 * Used on HomeScreen («+ Трата») and ActualScreen (same).
 */
export function Fab({ onClick, ariaLabel, label = '+' }: FabProps) {
  return (
    <button
      type="button"
      onClick={onClick}
      className={styles.fab}
      aria-label={ariaLabel}
    >
      {label}
    </button>
  );
}
```

### frontend/src/components/Fab.module.css

```css
.fab {
  position: fixed;
  bottom: calc(24px + var(--safe-bottom, 0px));
  right: 24px;
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: var(--color-accent);
  color: var(--color-on-accent, #fff);
  border: 0;
  box-shadow: var(--shadow-lg);
  font-size: 28px;
  line-height: 1;
  cursor: pointer;
  z-index: 50;  /* below BottomSheet (100/101) */
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 100ms ease-out;
}
.fab:hover {
  transform: scale(1.05);
}
.fab:active {
  transform: scale(0.95);
}
```

### Pre-existing reuse
- frontend/src/api/client.ts::apiFetch + ApiError — reuse.
- frontend/src/hooks/useCategories.ts — reuse в ActualScreen (Plan 04-06).
- frontend/src/hooks/useCurrentPeriod.ts — reuse в ActualScreen (Plan 04-06).
- frontend/src/components/BottomSheet.tsx — reuse в ActualScreen (D-40 Phase 3).
- frontend/src/components/PlanItemEditor.tsx — reference для structure (НЕ override; НЕ extend; новый компонент).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend/src/api/types.ts and create frontend/src/api/actual.ts and frontend/src/hooks/useActual.ts</name>
  <files>frontend/src/api/types.ts, frontend/src/api/actual.ts, frontend/src/hooks/useActual.ts</files>
  <action>
**1. Extend frontend/src/api/types.ts:**

Открыть существующий файл, найти конец (после Phase 3 types, ~line 138). Добавить секцию Phase 4 в конец (ДО последнего `}` если файл заканчивается на закрывающую скобку, ИЛИ просто append).

Добавить экспорты строго по `<interfaces>`:
- ActualSource (type alias).
- ActualRead.
- ActualCreatePayload.
- ActualUpdatePayload.
- BalanceCategoryRow.
- BalanceResponse.

**Не модифицировать** существующие types Phase 1-3.

**2. Create frontend/src/api/actual.ts:**

Mirror frontend/src/api/planned.ts полностью (header docstring, exports). 5 функций:
- listActual(periodId, filters?).
- createActual(payload).
- updateActual(id, patch).
- deleteActual(id).
- getBalance().

Реализация — точно по `<interfaces>`. Header docstring каждой функции — пояснение endpoint + ACT-XX trace.

**3. Create frontend/src/hooks/useActual.ts:**

Mirror frontend/src/hooks/usePlanned.ts (с `cancelled` flag, refetch, periodId nullable).

```typescript
import { useCallback, useEffect, useState } from 'react';
import { listActual } from '../api/actual';
import type { ActualRead } from '../api/types';

export interface UseActualResult {
  rows: ActualRead[];
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

export function useActual(periodId: number | null): UseActualResult {
  const [rows, setRows] = useState<ActualRead[]>([]);
  const [loading, setLoading] = useState(periodId !== null);
  const [error, setError] = useState<string | null>(null);

  const refetch = useCallback(async () => {
    if (periodId === null) {
      setRows([]); setLoading(false); return;
    }
    setLoading(true); setError(null);
    try {
      const data = await listActual(periodId);
      setRows(data);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setLoading(false);
    }
  }, [periodId]);

  useEffect(() => {
    if (periodId === null) {
      setRows([]); setLoading(false); setError(null); return;
    }
    let cancelled = false;
    setLoading(true); setError(null);
    listActual(periodId)
      .then((data) => { if (!cancelled) setRows(data); })
      .catch((e: unknown) => { if (!cancelled) setError(e instanceof Error ? e.message : String(e)); })
      .finally(() => { if (!cancelled) setLoading(false); });
    return () => { cancelled = true; };
  }, [periodId]);

  return { rows, loading, error, refetch };
}
```

**Verification:**
- `cd frontend && npx tsc --noEmit -p .` (или `npm run build`) — должно пройти без ошибок.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit -p . 2>&1 | tail -20</automated>
  </verify>
  <done>
- frontend/src/api/types.ts расширен Phase 4 типами.
- frontend/src/api/actual.ts создан (5 функций).
- frontend/src/hooks/useActual.ts создан (mirror usePlanned).
- TypeScript build проходит без ошибок.
- Все exports type-checked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create frontend/src/components/ActualEditor.tsx + .module.css</name>
  <files>frontend/src/components/ActualEditor.tsx, frontend/src/components/ActualEditor.module.css</files>
  <action>
Создать новый компонент-форму строго по `<interfaces>` выше.

**ActualEditor.tsx:**
- 5 form fields: kind toggle (segmented), amount (text inputmode=decimal), category select (filtered by kind), description (textarea max 500), tx_date (input type=date).
- Helpers `parseRublesToKopecks` + `formatKopecksToRubles` + `todayISO` + `maxTxDateDefault` — copy из PlanItemEditor (D-63: НЕ extract в shared module).
- Logic:
  - State: kind, amountStr, categoryId, description, txDate, submitting, error.
  - filteredCats memo — категории текущего kind, не archived.
  - useEffect: если switch kind invalidates current categoryId — сбрасываем (UI-SPEC step 5).
  - handleSubmit: validate canSubmit → call onSave with normalized payload.
  - handleDelete: window.confirm → call onDelete.
- isEdit derived from `onDelete !== undefined`. В edit mode — кнопка «Удалить» отображается.
- autoFocus на amount input в create mode (ускоряет ввод).
- Threat T-04-21 mitigation (frontend defence-in-depth): client-side гарантирует amount > 0 + category выбрана + tx_date != "".

**ActualEditor.module.css:**

Скопировать стили из `frontend/src/components/PlanItemEditor.module.css`:
- `.form { display: flex; flex-direction: column; gap: var(--space-4); }`
- `.field { display: flex; flex-direction: column; gap: 4px; }`
- `.label { font-size: var(--text-sm); color: var(--color-text-muted); }`
- `.input { padding: 10px; ...; border: 1px solid var(--color-border); border-radius: var(--radius-md); background: var(--color-surface-2, #1f1f1f); color: var(--color-text); font-size: var(--text-md); }`
- `.select` — same as input.
- `.textarea` — same + resize: vertical.
- `.actions { display: flex; gap: 8px; justify-content: flex-end; align-items: center; margin-top: var(--space-2); }`
- `.deleteBtn` — danger styling, margin-right: auto (left-aligned).
- `.cancelBtn`, `.saveBtn` — neutral / primary styling.
- `.error { color: var(--color-danger, #f55); font-size: var(--text-sm); }`

Добавить **NEW** styles для kind toggle:
```css
.kindToggle { display: flex; gap: 8px; margin-bottom: var(--space-3); }
.kindBtn {
  flex: 1; padding: 10px; cursor: pointer;
  background: var(--color-surface-2, #2a2a2a);
  color: var(--color-text-muted);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: var(--text-sm);
  transition: background 100ms, color 100ms;
}
.kindBtn:disabled { opacity: 0.5; cursor: not-allowed; }
.kindBtnActive {
  background: var(--color-accent);
  color: var(--color-on-accent, #fff);
  border-color: var(--color-accent);
}
```

**Verification:**
- TypeScript build проходит.
- Импорт `import { ActualEditor } from '../components/ActualEditor';` работает.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit -p . 2>&1 | tail -10</automated>
  </verify>
  <done>
- frontend/src/components/ActualEditor.tsx создан.
- frontend/src/components/ActualEditor.module.css создан.
- 5 form fields реализованы (kind toggle, amount, category, description, tx_date).
- Kind toggle сбрасывает categoryId при invalidating switch.
- isEdit detected from onDelete prop.
- TypeScript build зелёный.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create frontend/src/components/Fab.tsx + .module.css</name>
  <files>frontend/src/components/Fab.tsx, frontend/src/components/Fab.module.css</files>
  <action>
Создать FAB компонент — простой stateless button с floating-position стилями.

**Fab.tsx:**
- Props: `{ onClick: () => void; ariaLabel: string; label?: string }`.
- Default `label='+'`.
- Stateless functional component.
- Класс `styles.fab` для всех visual.

**Fab.module.css:**
Полностью по `<interfaces>` выше:
- position fixed; bottom + right 24px.
- Размер 56x56 (стандартный Material FAB).
- border-radius 50% (круглый).
- background var(--color-accent); color var(--color-on-accent, #fff).
- Z-index 50 (ниже BottomSheet который 100/101).
- Hover/active scale animations.
- safe-bottom padding (для iOS notch).

**Verification:**
- TypeScript build проходит.
- Импорт `import { Fab } from '../components/Fab';` работает.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit -p . 2>&1 | tail -10</automated>
  </verify>
  <done>
- frontend/src/components/Fab.tsx создан.
- frontend/src/components/Fab.module.css создан.
- Stateless с props onClick + ariaLabel + optional label.
- Position fixed, circular, accent-colored.
- TypeScript build зелёный.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | apiFetch injects X-Telegram-Init-Data; ActualEditor валидирует client-side (defence-in-depth) |
| User input → ActualEditor | Текст в description рендерится через React (auto-escape) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-40 | Tampering | User шлёт amount=0 / negative | mitigate | Client canSubmit guard (amount > 0); server Pydantic gt=0 (defence-in-depth) |
| T-04-41 | Tampering | User шлёт kind='expense' с income category | mitigate | Filtered cats select только показывает matching kind; useEffect resets если switch; server-side KindMismatchError (defence-in-depth) |
| T-04-42 | XSS | description содержит script tag | mitigate | React auto-escape (нет dangerouslySetInnerHTML) |
| T-04-43 | Information Disclosure | error message раскрывает internal | accept | apiFetch ApiError содержит status + body — приемлемо для debug |
| T-04-44 | DoS | useActual cancellation race | mitigate | cancelled flag pattern (mirror Phase 2) |
| T-04-45 | Tampering | tx_date в далёком будущем (опечатка) | mitigate | Client max attribute (today + 7d); server FutureDateError 400 |
</threat_model>

<verification>
- TypeScript build проходит для всех файлов.
- Импорты actualEditor, Fab, useActual, listActual работают.
- ActualEditor mode behavior:
  - Без `onDelete` → нет кнопки «Удалить».
  - С `onDelete` → кнопка «Удалить» с window.confirm.
- Plan 04-06 имеет все нужные блоки для сборки ActualScreen.
</verification>

<success_criteria>
- 7 файлов созданы или расширены.
- TypeScript build green.
- ActualEditor реализует kind toggle с автоматическим сбросом categoryId.
- Fab — stateless, переиспользуемый.
- useActual — mirror usePlanned API.
- Plan 04-06 готов собрать ActualScreen + HomeScreen extension без необходимости создавать infra.
</success_criteria>

<output>
After completion, create `.planning/phases/04-actual-transactions-and-bot-commands/04-05-SUMMARY.md`.
</output>
