---
phase: 04-actual-transactions-and-bot-commands
plan: 04
type: execute
wave: 3
depends_on: [04-03]
files_modified:
  - app/bot/parsers.py
  - app/bot/disambiguation.py
  - app/bot/commands.py
  - app/bot/api_client.py
  - main_bot.py
autonomous: true
requirements:
  - ACT-03
  - ACT-04
  - ACT-05

must_haves:
  truths:
    - "Bot отвечает на /add <amount> <category_query> [description] — создаёт actual через POST /api/v1/internal/bot/actual; ответ форматируется по D-59"
    - "Bot отвечает на /income аналогично /add но с kind=income"
    - "Bot отвечает на /balance — вызывает POST /api/v1/internal/bot/balance, форматирует с эмодзи (D-60)"
    - "Bot отвечает на /today — вызывает POST /api/v1/internal/bot/today, форматирует список с итогами (D-61)"
    - "Bot отвечает на /app — InlineKeyboardButton с WebApp на MINI_APP_URL (D-62)"
    - "При ambiguous response от internal API: bot отправляет inline-keyboard с кнопкой на каждую категорию; callback_data формат act:TOKEN:CATEGORY_ID (D-48); pending state сохраняется в in-memory dict с TTL 5 мин (D-47)"
    - "Callback handler принимает callback, восстанавливает pending state, вызывает internal API с explicit category_id, отвечает в чат"
    - "Все командные handlers молча игнорируют не-OWNER (return без message.answer)"
    - "parsers.py содержит pure parse_amount + parse_add_command функции"
    - "api_client.py расширен 3 новыми методами: bot_create_actual, bot_get_balance, bot_get_today"
    - "main_bot.py подключает новый router с command handlers"
    - "Wave 0 bot тесты (test_bot_parsers, test_bot_handlers_phase4) переходят в GREEN"
  artifacts:
    - path: "app/bot/parsers.py"
      provides: "parse_amount + parse_add_command pure functions"
    - path: "app/bot/disambiguation.py"
      provides: "PendingActual dataclass + store_pending + pop_pending + TTL"
    - path: "app/bot/commands.py"
      provides: "Router + cmd_add/income/balance/today/app handlers + cb_disambiguation callback handler"
    - path: "app/bot/api_client.py"
      provides: "EXTENDED with bot_create_actual, bot_get_balance, bot_get_today"
    - path: "main_bot.py"
      provides: "EXTENDED with dp.include_router(commands.router)"
  key_links:
    - from: "app/bot/commands.py"
      to: "app/bot/api_client.py + app/bot/parsers.py + app/bot/disambiguation.py"
      via: "imports"
      pattern: "from app.bot"
    - from: "app/bot/api_client.py"
      to: "POST /api/v1/internal/bot/* via httpx"
      via: "HTTP POST with X-Internal-Token"
      pattern: "/api/v1/internal/bot/"
    - from: "main_bot.py"
      to: "app/bot/commands.py::router"
      via: "dp.include_router"
      pattern: "include_router"

user_setup: []
---

<objective>
Wave 3 (один из двух параллельных путей в этой волне): создать bot command handlers Phase 4. После этого пользователь в Telegram может использовать /add, /income, /balance, /today, /app, и disambiguation flow через inline-кнопки.

Реализуется 5 файлов:
- app/bot/parsers.py — pure parse_amount + parse_add_command.
- app/bot/disambiguation.py — in-memory pending state with TTL.
- app/bot/commands.py — все 5 command handlers + callback handler. Новый Router (НЕ модифицируем app/bot/handlers.py который содержит Phase 2 /start handler).
- app/bot/api_client.py — РАСШИРЕНИЕ с 3 новыми async функциями.
- main_bot.py — РАСШИРЕНИЕ: include новый router.

Purpose: завершить bot-сторону Phase 4. После этого Wave 0 bot-тесты (test_bot_parsers, test_bot_handlers_phase4) green.
Output: 3 новых bot модуля + 2 расширения существующих.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-CONTEXT.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-RESEARCH.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-VALIDATION.md
@app/bot/handlers.py
@app/bot/api_client.py
@main_bot.py
@app/core/settings.py
@docs/HLD.md

<interfaces>
Контракты для Plan 04-04 (используются Wave 0 bot тестами).

### app/bot/parsers.py

```python
import re

_AMOUNT_RE = re.compile(r"^\d+([.,]\d{1,2})?$")  # после чистки только digits + опц. 1-2 decimals

def parse_amount(s: str) -> int | None:
    """Parse 1500 / 1500.50 / 1 500 / 1500р / 1500₽ → kopecks; None on error.

    Поддержка:
    - integer: 1500 → 150000
    - decimal с точкой/запятой: 1500.50, 1500,50 → 150050 (max 2 знака)
    - thousand-spaces: 1 500 (regular space) и NBSP → 150000
    - суффиксы: р, руб, ₽ (case-insensitive trailing) → strip
    - комбинации: 1 500.50 ₽ → 150050

    Returns None для:
    - Некорректный формат (NaN, alpha, .50 без integer части)
    - amount <= 0
    - amount > 10^12 копеек (overflow guard)
    - 3+ знаков после запятой
    """
    if not s:
        return None
    s = s.strip()
    # Strip suffixes (case-insensitive, longest first to avoid 'руб' → cut 'р' + leftover 'уб')
    for suffix in ("₽", "руб", "р"):
        if s.lower().endswith(suffix):
            s = s[: -len(suffix)].rstrip()
    s = s.replace(",", ".")
    s = s.replace(" ", "").replace(" ", "")  # space + NBSP
    if not _AMOUNT_RE.match(s):
        return None
    try:
        f = float(s)
    except ValueError:
        return None
    if f <= 0:
        return None
    cents = round(f * 100)
    if cents > 10**12:
        return None
    return cents


def parse_add_command(args: str | None) -> tuple[int, str, str | None] | None:
    """Parse /add AMOUNT CATEGORY_QUERY [DESCRIPTION] args (без leading /add).

    Returns (amount_cents, category_query, description_or_None) or None on failure.
    """
    if not args:
        return None
    tokens = args.strip().split()
    if len(tokens) < 2:
        return None
    amount_cents = parse_amount(tokens[0])
    if amount_cents is None:
        return None
    category_query = tokens[1]
    description = " ".join(tokens[2:]) if len(tokens) > 2 else None
    return amount_cents, category_query, description
```

### app/bot/disambiguation.py

```python
from dataclasses import dataclass, field
from datetime import datetime, timedelta
from typing import Optional
from uuid import uuid4

TTL = timedelta(minutes=5)

@dataclass
class PendingActual:
    chat_id: int
    kind: str
    amount_cents: int
    description: Optional[str]
    tx_date: Optional[str]  # ISO date or None — bot не знает default, server use today
    candidates: list[dict]  # list of {id, name, kind} dicts
    created_at: datetime = field(default_factory=datetime.utcnow)

    @property
    def is_expired(self) -> bool:
        return datetime.utcnow() - self.created_at > TTL

# Module-level state (acceptable for single-tenant pet)
_PENDING: dict[str, PendingActual] = {}


def store_pending(p: PendingActual) -> str:
    """Store pending; returns 8-hex token for callback_data."""
    token = uuid4().hex[:8]
    _PENDING[token] = p
    _gc()
    return token


def pop_pending(token: str) -> Optional[PendingActual]:
    """Retrieve and remove. None if expired or missing."""
    p = _PENDING.pop(token, None)
    if p is None or p.is_expired:
        return None
    return p


def _gc() -> None:
    """Drop expired entries on each store_pending. O(n) — acceptable for low cardinality."""
    expired = [k for k, v in _PENDING.items() if v.is_expired]
    for k in expired:
        del _PENDING[k]
```

### app/bot/api_client.py — extension contract

```python
# Существующее: bind_chat_id (Phase 2). Не трогать.

async def bot_create_actual(
    *,
    tg_user_id: int,
    kind: str,
    amount_cents: int,
    category_query: str | None = None,
    category_id: int | None = None,
    description: str | None = None,
    tx_date: str | None = None,  # ISO date string OR None
) -> dict:
    """POST /api/v1/internal/bot/actual; returns parsed JSON dict (BotActualResponse shape).

    Raises InternalApiError on network/non-2xx.
    """
    payload = {
        "tg_user_id": tg_user_id,
        "kind": kind,
        "amount_cents": amount_cents,
    }
    if category_query is not None:
        payload["category_query"] = category_query
    if category_id is not None:
        payload["category_id"] = category_id
    if description is not None:
        payload["description"] = description
    if tx_date is not None:
        payload["tx_date"] = tx_date
    return await _post_internal("/api/v1/internal/bot/actual", payload)


async def bot_get_balance(tg_user_id: int) -> dict:
    """POST /api/v1/internal/bot/balance; returns BotBalanceResponse JSON."""
    return await _post_internal("/api/v1/internal/bot/balance", {"tg_user_id": tg_user_id})


async def bot_get_today(tg_user_id: int) -> dict:
    """POST /api/v1/internal/bot/today; returns BotTodayResponse JSON."""
    return await _post_internal("/api/v1/internal/bot/today", {"tg_user_id": tg_user_id})


async def _post_internal(path: str, payload: dict) -> dict:
    """Helper: POST with X-Internal-Token, raise InternalApiError on failure."""
    headers = {"X-Internal-Token": settings.INTERNAL_TOKEN}
    try:
        async with httpx.AsyncClient(base_url=settings.API_BASE_URL, timeout=5.0) as client:
            response = await client.post(path, json=payload, headers=headers)
            response.raise_for_status()
            return response.json()
    except httpx.HTTPError as exc:
        logger.warning("bot.internal_call.failed", path=path, error=str(exc))
        raise InternalApiError(f"{path} failed: {exc}") from exc
```

### app/bot/commands.py — public contract

```python
from aiogram import F, Router
from aiogram.filters import Command, CommandObject
from aiogram.types import (
    CallbackQuery,
    InlineKeyboardButton,
    InlineKeyboardMarkup,
    Message,
    WebAppInfo,
)

router = Router()  # Module-level. main_bot.py: dp.include_router(router)

@router.message(Command("add"))
async def cmd_add(message: Message, command: CommandObject) -> None: ...

@router.message(Command("income"))
async def cmd_income(message: Message, command: CommandObject) -> None: ...

@router.message(Command("balance"))
async def cmd_balance(message: Message) -> None: ...

@router.message(Command("today"))
async def cmd_today(message: Message) -> None: ...

@router.message(Command("app"))
async def cmd_app(message: Message) -> None: ...

@router.callback_query(F.data.startswith("act:"))
async def cb_disambiguation(callback: CallbackQuery) -> None: ...
```

Все command handlers сначала проверяют `message.from_user.id == settings.OWNER_TG_ID`; иначе `return` (silent). Затем call api_client + format reply.

### main_bot.py — extension

```python
# Existing imports
from app.bot.handlers import router as start_router  # rename для clarity
from app.bot.commands import router as commands_router  # NEW

# Existing dp = Dispatcher(); dp.include_router(start_router)
# Add:
dp.include_router(commands_router)
```

### Pre-existing infrastructure
- app/bot/handlers.py — /start handler (Phase 2). НЕ модифицируем.
- app/bot/api_client.py — bind_chat_id + InternalApiError. РАСШИРЯЕМ.
- main_bot.py — dp.include_router(router) уже подключён для start; ДОБАВЛЯЕМ commands_router.
- app/core/settings.py::settings — все нужные env vars (INTERNAL_TOKEN, API_BASE_URL, OWNER_TG_ID, MINI_APP_URL).
- app/api/schemas/internal_bot.py::BotActualResponse, BotBalanceResponse, BotTodayResponse — для shape reference в bot.

### Reply text formats (D-59, D-60, D-61, D-62)

**D-59 /add reply (status=created):**
```
✓ Записано: 1 500 ₽ — Продукты (пятёрочка)
Остаток по категории: 8 500 ₽ (план 10 000 ₽)
```
Если description=None: пропускаем `(...)` часть.

**D-59 /income reply (status=created):**
```
✓ Доход: 50 000 ₽ — Зарплата (аванс)
```

**D-59 /add reply (status=ambiguous):**
```
Уточните категорию:
```
+ InlineKeyboardMarkup с одной кнопкой на каждую категорию (см. Pattern 2 в RESEARCH.md).

**D-59 /add reply (status=not_found):**
```
Категория не найдена. Доступные категории: см. список в Mini App.
```

**D-60 /balance reply:**
```
💰 Баланс: 23 450 ₽
Δ периода: +5 200 ₽

Топ-5 категорий:
✓ Продукты: 8 500 / 10 000 ₽ (Δ +1 500)
⚠️ Кафе: 4 200 / 5 000 ₽ (Δ +800, 84%)
🔴 Развлечения: 6 500 / 5 000 ₽ (Δ -1 500, 130%)
✓ Транспорт: 1 200 / 3 000 ₽ (Δ +1 800)
✓ Подписки: 1 990 / 2 000 ₽ (Δ +10)

Период: 5 фев — 4 мар
```
- Эмодзи: для expense `actual/planned`: ≥80% и ≤100% → ⚠️; >100% → 🔴; <80% → ✓.
- Топ-5: by `abs(delta_cents) desc`, ограничение `[:5]`.
- Если активного периода нет → бот говорит «Сначала завершите onboarding в Mini App.» (от 404).

**D-61 /today reply:**
```
Сегодня (2 мая 2026):
• Продукты: 1 500 ₽ — пятёрочка
• Кафе: 850 ₽ — обед
Итого расходов: 2 350 ₽
```
Если transactions empty: «Сегодня нет факт-трат.».

**D-62 /app reply:**
```
Откройте Mini App для управления бюджетом:
```
+ InlineKeyboardMarkup с одной WebApp кнопкой (`text=Открыть бюджет`, `web_app=WebAppInfo(url=settings.MINI_APP_URL)`).
**Note:** Этот же паттерн уже есть в `app/bot/handlers.py::_open_app_keyboard()`. **МОЖНО** импортировать его, ИЛИ скопировать (минимальный код). Discretion executor.

### Money formatter helper

В bot reply нужно форматировать копейки → 1 500 ₽, 8 500 ₽ etc. Helper в `app/bot/commands.py` (или новый `app/bot/format.py`):

```python
def format_kopecks(cents: int) -> str:
    """Format kopecks as 1 500 (с разделителем тысяч, без копеек если round)."""
    if cents % 100 == 0:
        rubles = cents // 100
        return f"{rubles:,}".replace(",", " ")
    rubles, kop = divmod(cents, 100)
    return f"{rubles:,}".replace(",", " ") + f",{kop:02d}"

def format_kopecks_with_sign(cents: int) -> str:
    """+1 500 / -1 500 / 0 (для дельты)."""
    if cents == 0:
        return "0 ₽"
    sign = "+" if cents > 0 else "-"
    return f"{sign}{format_kopecks(abs(cents))} ₽"
```

`format_kopecks(150000)` → `1 500`, `format_kopecks(150050)` → `1 500,50`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/bot/parsers.py and app/bot/disambiguation.py</name>
  <files>app/bot/parsers.py, app/bot/disambiguation.py</files>
  <action>
Создать два pure-utility файла для bot.

**app/bot/parsers.py:**

Header docstring должен покрывать:
- Назначение: pure parsing helpers для bot commands.
- D-49: parse_amount поддержка форматов 1500/.50/_/р/₽; cap 10^12 копеек.
- D-50: parse_add_command split args; «первое слово после суммы — категория».

Реализация строго по `<interfaces>` выше. Особое внимание:

1. parse_amount:
   - Order of operations: strip → lowercase suffix check (longest first: руб перед р, иначе руб cut as р+уб) → replace , → . → replace spaces (regular + NBSP  ) → regex match → float → checks → cents.
   - suffix matching case-insensitive: `s.lower().endswith(suffix)` для проверки, но cut на `s[:-len(suffix)]` (оригинал не lowered).
   - NBSP: Python literal `" "` (NO-BREAK SPACE).
   - Regex: `^\d+([.,]\d{1,2})?$` после strip suffix + replace.

2. parse_add_command:
   - Принимает str | None (т.к. CommandObject.args может быть None).
   - Возвращает tuple | None.
   - Split via .split() (whitespace-agnostic).

**app/bot/disambiguation.py:**

Header docstring:
- Назначение: in-memory pending-state для disambiguation callback (D-47).
- TTL 5 минут; cleanup на каждом store_pending.
- НЕ persistent (рестарт бота → cache потеряется → пользователь повторит команду).

Реализация строго по `<interfaces>`. Дополнительно:
- _PENDING — module-level dict; thread-safe в asyncio (single event loop).
- _gc() — sync, дешёвая операция (~10 entries max).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.bot.parsers import parse_amount, parse_add_command; assert parse_amount('1500') == 150000; assert parse_amount('1500.50') == 150050; assert parse_amount('1 500') == 150000; assert parse_amount('1500p'.replace('p','р')) == 150000; assert parse_amount('0') is None; assert parse_amount('-100') is None; assert parse_add_command('1500 prod desc')[0] == 150000; assert parse_add_command('1500') is None; from app.bot.disambiguation import PendingActual, store_pending, pop_pending; p = PendingActual(chat_id=1, kind='expense', amount_cents=100, description=None, tx_date=None, candidates=[]); t = store_pending(p); assert len(t) == 8; r = pop_pending(t); assert r is not None and r.amount_cents == 100; assert pop_pending(t) is None; print('parsers + disambiguation OK')"</automated>
  </verify>
  <done>
- Файлы app/bot/parsers.py и app/bot/disambiguation.py созданы.
- parse_amount проходит все edge cases из теста.
- parse_add_command split tokens корректно.
- PendingActual + store/pop pending работают.
- TTL 5 мин определён.
- Smoke-тест через python -c проходит.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend app/bot/api_client.py with 3 new functions and helper</name>
  <files>app/bot/api_client.py</files>
  <action>
Расширить существующий `app/bot/api_client.py` тремя новыми async функциями + helper `_post_internal`.

**Шаги:**

1. НЕ изменять существующий `bind_chat_id` (Phase 2 функция).
2. НЕ изменять `InternalApiError` class.
3. Расширить header docstring (упомянуть Phase 4 endpoints).
4. Добавить helper `_post_internal` (private):
   ```python
   async def _post_internal(path: str, payload: dict) -> dict:
       """Generic POST to /api/v1/internal/* with X-Internal-Token; returns parsed JSON.

       Used by bot Phase 4 commands. Raises InternalApiError on network/non-2xx.
       Logs failures (without leaking token) via structlog.
       """
       headers = {"X-Internal-Token": settings.INTERNAL_TOKEN}
       try:
           async with httpx.AsyncClient(base_url=settings.API_BASE_URL, timeout=5.0) as client:
               response = await client.post(path, json=payload, headers=headers)
               response.raise_for_status()
               return response.json()
       except httpx.HTTPError as exc:
           logger.warning("bot.internal_call.failed", path=path, error=str(exc))
           raise InternalApiError(f"{path} failed: {exc}") from exc
   ```

5. Добавить `bot_create_actual` (см. interfaces):
   - Конструирует payload с обязательными tg_user_id, kind, amount_cents.
   - Опциональные: category_query, category_id, description, tx_date.
   - Не передаёт keys которые None (чтобы Pydantic v2 на server корректно обработал Optional + model_validator).
   - Делегирует в `_post_internal("/api/v1/internal/bot/actual", payload)`.

6. Добавить `bot_get_balance(tg_user_id)`:
   - Payload `{"tg_user_id": tg_user_id}`.
   - Делегирует в `_post_internal("/api/v1/internal/bot/balance", payload)`.

7. Добавить `bot_get_today(tg_user_id)`:
   - Payload `{"tg_user_id": tg_user_id}`.
   - Делегирует в `_post_internal("/api/v1/internal/bot/today", payload)`.

После рефактора `bind_chat_id` может (опц.) использовать `_post_internal`, но это уже refactor существующего рабочего кода — оставьте `bind_chat_id` как есть, иначе риск нерабочей Phase 2 функциональности.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.bot.api_client import bot_create_actual, bot_get_balance, bot_get_today, bind_chat_id, InternalApiError; import inspect; assert inspect.iscoroutinefunction(bot_create_actual); assert inspect.iscoroutinefunction(bot_get_balance); assert inspect.iscoroutinefunction(bot_get_today); print('api_client extended OK')" && uv run pytest tests/test_telegram_chat_bind.py -x --no-header 2>&1 | tail -10</automated>
  </verify>
  <done>
- app/bot/api_client.py extended.
- 3 новые async функции экспортированы: bot_create_actual, bot_get_balance, bot_get_today.
- _post_internal helper для DRY с X-Internal-Token.
- bind_chat_id не сломан (test_telegram_chat_bind.py зелёный).
- python -c проверка проходит.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create app/bot/commands.py with all 5 command handlers + callback handler</name>
  <files>app/bot/commands.py</files>
  <action>
Создать `app/bot/commands.py` — основной handler-модуль Phase 4. Использовать `app/bot/handlers.py` как референс для стиля (router declaration + structlog + OWNER check + reply_markup).

Header docstring:
- Назначение: aiogram command handlers Phase 4 (ACT-03, ACT-04, ACT-05).
- 5 commands: /add, /income, /balance, /today, /app.
- 1 callback handler: cb_disambiguation для inline-keyboard ответов.
- OWNER-only enforcement: silent return для не-OWNER.
- Disambiguation flow: /add → ambiguous → kbd → callback → второй internal call.
- ALL handlers wrapped в try/except InternalApiError для graceful degradation (D-59 /add reply на api_error).

**Module structure:**

```python
from __future__ import annotations
from datetime import datetime
from zoneinfo import ZoneInfo

import structlog
from aiogram import F, Router
from aiogram.filters import Command, CommandObject
from aiogram.types import (
    CallbackQuery, InlineKeyboardButton, InlineKeyboardMarkup, Message, WebAppInfo,
)

from app.bot.api_client import (
    InternalApiError, bot_create_actual, bot_get_balance, bot_get_today,
)
from app.bot.disambiguation import PendingActual, pop_pending, store_pending
from app.bot.parsers import parse_add_command, parse_amount
from app.core.settings import settings

logger = structlog.get_logger(__name__)
router = Router()


# ---------- Helpers ----------

def format_kopecks(cents: int) -> str:
    if cents % 100 == 0:
        rubles = cents // 100
        return f"{rubles:,}".replace(",", " ")
    rubles, kop = divmod(cents, 100)
    return f"{rubles:,}".replace(",", " ") + f",{kop:02d}"


def format_kopecks_with_sign(cents: int) -> str:
    if cents == 0:
        return "0 ₽"
    sign = "+" if cents > 0 else "-"
    return f"{sign}{format_kopecks(abs(cents))} ₽"


def _is_owner(message_or_callback) -> bool:
    user = getattr(message_or_callback, "from_user", None)
    return user is not None and user.id == settings.OWNER_TG_ID


def _build_disambiguation_kbd(token: str, candidates: list[dict]) -> InlineKeyboardMarkup:
    rows = [
        [InlineKeyboardButton(
            text=f"{c['name']} ({c['kind']})",
            callback_data=f"act:{token}:{c['id']}",
        )]
        for c in candidates
    ]
    return InlineKeyboardMarkup(inline_keyboard=rows)


def _build_app_kbd() -> InlineKeyboardMarkup:
    """WebApp button keyboard (mirror app/bot/handlers.py::_open_app_keyboard)."""
    btn = InlineKeyboardButton(
        text="Открыть бюджет",
        web_app=WebAppInfo(url=settings.MINI_APP_URL),
    )
    return InlineKeyboardMarkup(inline_keyboard=[[btn]])


def _format_created_actual(result: dict, *, kind: str) -> str:
    """D-59 reply formatter for status=created."""
    actual = result["actual"]
    cat = result["category"]
    balance_cents = result.get("category_balance_cents")
    desc = actual.get("description")
    head_kind = "Записано" if kind == "expense" else "Доход"
    head = f"✓ {head_kind}: {format_kopecks(actual['amount_cents'])} ₽ — {cat['name']}"
    if desc:
        head += f" ({desc})"
    if balance_cents is not None:
        # Compute план для category from balance + actual_cents — нет, balance_cents = плановый - actual для expense (см. internal_bot.py service).
        # Bot отображает: "Остаток по категории: X ₽" — где X = balance_cents для expense (положительный = есть бюджет, отрицательный = overspend).
        if kind == "expense":
            tail = f"\nОстаток по категории: {format_kopecks(balance_cents)} ₽"
        else:
            tail = f"\nДоходы периода (Δ): {format_kopecks_with_sign(balance_cents)}"
        return head + tail
    return head


def _balance_emoji(actual: int, planned: int, kind: str) -> str:
    """D-60 emoji logic. Mirror DSH-03."""
    if planned == 0:
        return "✓"  # no plan — neutral
    pct = actual / planned * 100
    if kind == "expense":
        if pct > 100:
            return "🔴"
        if pct >= 80:
            return "⚠️"
        return "✓"
    else:  # income
        # для дохода inverse: <80% of plan = недополучили = «нехорошо» (но в MVP не маркируем строгim emoji — оставляем ✓ если >100%, ⚠️ если 80-100%, 🔴 если <80%)
        if pct >= 100:
            return "✓"
        if pct >= 80:
            return "⚠️"
        return "🔴"


def _format_balance_reply(result: dict) -> str:
    """D-60 reply formatter."""
    balance_now = format_kopecks_with_sign(result["balance_now_cents"]) if result["balance_now_cents"] != 0 else f"{format_kopecks(result['balance_now_cents'])} ₽"
    delta_total = format_kopecks_with_sign(result["delta_total_cents"])
    period_start = datetime.fromisoformat(result["period_start"]).strftime("%d %b").lower() if isinstance(result["period_start"], str) else result["period_start"].strftime("%d %b").lower()
    period_end = datetime.fromisoformat(result["period_end"]).strftime("%d %b").lower() if isinstance(result["period_end"], str) else result["period_end"].strftime("%d %b").lower()
    by_cat = sorted(result["by_category"], key=lambda r: abs(r["delta_cents"]), reverse=True)[:5]

    lines = [
        f"💰 Баланс: {format_kopecks(result['balance_now_cents'])} ₽",
        f"Δ периода: {delta_total}",
        "",
        "Топ-5 категорий:",
    ]
    for r in by_cat:
        emoji = _balance_emoji(r["actual_cents"], r["planned_cents"], r["kind"])
        if r["planned_cents"] > 0:
            pct = round(r["actual_cents"] / r["planned_cents"] * 100)
            extra = f", {pct}%" if pct >= 80 else ""
        else:
            extra = ""
        lines.append(
            f"{emoji} {r['name']}: {format_kopecks(r['actual_cents'])} / {format_kopecks(r['planned_cents'])} ₽ "
            f"(Δ {format_kopecks_with_sign(r['delta_cents'])}{extra})"
        )
    lines.append("")
    lines.append(f"Период: {period_start} — {period_end}")
    return "\n".join(lines)


def _format_today_reply(result: dict) -> str:
    """D-61 reply formatter."""
    actuals = result.get("actuals", [])
    if not actuals:
        return "Сегодня нет факт-трат."
    today_str = datetime.now(ZoneInfo(settings.APP_TZ)).strftime("%d %B %Y")
    lines = [f"Сегодня ({today_str}):"]
    for a in actuals:
        line = f"• {a['category_name']}: {format_kopecks(a['amount_cents'])} ₽"
        if a.get("description"):
            line += f" — {a['description']}"
        lines.append(line)
    if result["total_expense_cents"] > 0:
        lines.append(f"Итого расходов: {format_kopecks(result['total_expense_cents'])} ₽")
    if result["total_income_cents"] > 0:
        lines.append(f"Итого доходов: {format_kopecks(result['total_income_cents'])} ₽")
    return "\n".join(lines)


# ---------- Handlers ----------

async def _handle_add_or_income(message: Message, command: CommandObject, *, kind: str) -> None:
    """Common logic for /add and /income."""
    if not _is_owner(message):
        return  # silent ignore non-OWNER

    parsed = parse_add_command(command.args)
    if parsed is None:
        verb = "трату" if kind == "expense" else "доход"
        await message.answer(
            f"Использование: /{ 'add' if kind=='expense' else 'income' } <сумма> <категория> [описание]\n"
            f"Например: /{ 'add' if kind=='expense' else 'income' } 1500 продукты пятёрочка"
        )
        return

    amount_cents, category_query, description = parsed

    try:
        result = await bot_create_actual(
            tg_user_id=message.from_user.id,
            kind=kind,
            amount_cents=amount_cents,
            category_query=category_query,
            description=description,
        )
    except InternalApiError as exc:
        logger.warning("bot.cmd.api_failed", cmd=kind, error=str(exc))
        await message.answer("Не удалось связаться с сервером. Попробуйте позже.")
        return

    status = result.get("status")
    if status == "created":
        await message.answer(_format_created_actual(result, kind=kind))
    elif status == "ambiguous":
        candidates = result.get("candidates", [])
        token = store_pending(PendingActual(
            chat_id=message.chat.id,
            kind=kind,
            amount_cents=amount_cents,
            description=description,
            tx_date=None,
            candidates=candidates,
        ))
        kbd = _build_disambiguation_kbd(token, candidates)
        await message.answer("Уточните категорию:", reply_markup=kbd)
    elif status == "not_found":
        await message.answer(
            "Категория не найдена. Доступные категории: см. список в Mini App."
        )
    else:
        logger.warning("bot.cmd.unknown_status", status=status)
        await message.answer("Неожиданный ответ от сервера.")


@router.message(Command("add"))
async def cmd_add(message: Message, command: CommandObject) -> None:
    await _handle_add_or_income(message, command, kind="expense")


@router.message(Command("income"))
async def cmd_income(message: Message, command: CommandObject) -> None:
    await _handle_add_or_income(message, command, kind="income")


@router.message(Command("balance"))
async def cmd_balance(message: Message) -> None:
    if not _is_owner(message):
        return
    try:
        result = await bot_get_balance(message.from_user.id)
    except InternalApiError as exc:
        logger.warning("bot.cmd.balance.api_failed", error=str(exc))
        # 404 для "no active period" попадает сюда тоже (raise_for_status)
        await message.answer("Не удалось получить баланс. Возможно, нужно завершить onboarding в Mini App.")
        return
    await message.answer(_format_balance_reply(result))


@router.message(Command("today"))
async def cmd_today(message: Message) -> None:
    if not _is_owner(message):
        return
    try:
        result = await bot_get_today(message.from_user.id)
    except InternalApiError as exc:
        logger.warning("bot.cmd.today.api_failed", error=str(exc))
        await message.answer("Не удалось получить список трат за сегодня.")
        return
    await message.answer(_format_today_reply(result))


@router.message(Command("app"))
async def cmd_app(message: Message) -> None:
    if not _is_owner(message):
        return
    await message.answer(
        "Откройте Mini App для управления бюджетом:",
        reply_markup=_build_app_kbd(),
    )


@router.callback_query(F.data.startswith("act:"))
async def cb_disambiguation(callback: CallbackQuery) -> None:
    """Disambiguation callback: extract token + category_id, re-call internal API."""
    if not _is_owner(callback):
        await callback.answer()  # dismiss silently
        return

    parts = (callback.data or "").split(":", 2)
    if len(parts) != 3 or parts[0] != "act":
        await callback.answer("Некорректный формат.", show_alert=False)
        return
    _, token, category_id_str = parts
    try:
        category_id = int(category_id_str)
    except ValueError:
        await callback.answer("Некорректный id категории.", show_alert=False)
        return

    pending = pop_pending(token)
    if pending is None:
        await callback.answer("Время ожидания истекло.", show_alert=True)
        if callback.message:
            try:
                await callback.message.edit_reply_markup(reply_markup=None)
            except Exception:
                pass  # message может быть слишком старым для edit
        return

    try:
        result = await bot_create_actual(
            tg_user_id=callback.from_user.id,
            kind=pending.kind,
            amount_cents=pending.amount_cents,
            category_id=category_id,
            description=pending.description,
            tx_date=pending.tx_date,
        )
    except InternalApiError as exc:
        logger.warning("bot.cb.api_failed", error=str(exc))
        await callback.answer("Сервер недоступен.", show_alert=True)
        return

    status = result.get("status")
    if status == "created":
        text = _format_created_actual(result, kind=pending.kind)
        if callback.message:
            try:
                await callback.message.edit_text(text)
            except Exception:
                # edit может fail (parse mode mismatch и т.п.) — отправим новое
                await callback.message.answer(text)
        await callback.answer()
    else:
        await callback.answer(f"Неожиданный статус: {status}", show_alert=True)
```

**Notes для executor:**
- format_balance_reply parses `period_start`/`period_end` strings — Pydantic v2 сериализует date в ISO string в JSON, поэтому в bot мы получаем strings. Helper handles either.
- `format_kopecks(0)` → `"0"` — для balance_now_cents=0 случая.
- `_handle_add_or_income` дедуплицирует cmd_add и cmd_income.
- ALL exception handlers НЕ raise — они отвечают пользователю + log warning.

**Verification:**
- `python -c "from app.bot.commands import router, cmd_add, cmd_income, cmd_balance, cmd_today, cmd_app, cb_disambiguation, format_kopecks, _is_owner; print('commands OK')"` — все экспортированные.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.bot.commands import router, format_kopecks, format_kopecks_with_sign; assert format_kopecks(150000) == '1 500'; assert format_kopecks(150050) == '1 500,50'; assert format_kopecks_with_sign(150000) == '+1 500 ₽'; assert format_kopecks_with_sign(-150000) == '-1 500 ₽'; assert format_kopecks_with_sign(0) == '0 ₽'; print('commands OK')"</automated>
  </verify>
  <done>
- Файл app/bot/commands.py создан.
- 5 command handlers (cmd_add, cmd_income, cmd_balance, cmd_today, cmd_app) + 1 callback (cb_disambiguation).
- _handle_add_or_income deduplicates /add и /income.
- format_kopecks + format_kopecks_with_sign helpers экспортированы.
- _is_owner check во всех handlers.
- Disambiguation flow: ambiguous → store_pending → kbd → callback → pop_pending → re-call API.
- All InternalApiError catches respond gracefully.
- Smoke-тест проходит.
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire commands router into main_bot.py</name>
  <files>main_bot.py</files>
  <action>
Расширить `main_bot.py` для подключения нового commands router.

**Шаги:**

1. Обновить header docstring — добавить «Phase 4 scope (ACT-03/04/05): /add, /income, /balance, /today, /app + disambiguation callback. Implementation in app.bot.commands.»

2. Добавить import (после `from app.bot.handlers import router`):
   ```python
   from app.bot.commands import router as commands_router
   ```
   ИЛИ переименовать существующий import для clarity:
   ```python
   from app.bot.handlers import router as start_router
   from app.bot.commands import router as commands_router
   ```
   (если переименовываете, обновите все references — один: `dp.include_router(router)`).

3. Добавить второй include_router (после существующего `dp.include_router(router)`):
   ```python
   dp.include_router(commands_router)
   ```

**НЕ изменять:**
- Health-check сервер (port 8001).
- Bot lifecycle (Bot/Dispatcher/start_polling).
- Configure_logging call.

**Verification:**
- `python -c "import asyncio; from main_bot import main; print('main_bot import OK')"` — должно работать без ошибок.
- Existing test `tests/test_main_bot_entry.py` всё ещё проходит.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from main_bot import main; print('main_bot OK')" && uv run pytest tests/test_main_bot_entry.py -x --no-header 2>&1 | tail -5</automated>
  </verify>
  <done>
- main_bot.py обновлён.
- Header docstring упоминает Phase 4 commands.
- commands_router imported и зарегистрирован через dp.include_router.
- Существующий test_main_bot_entry.py не сломан.
- main_bot import succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → bot (Update payload) | message.from_user.id может быть подделан только Telegram-side (сложно); все handlers делают _is_owner check |
| Bot → API (HTTPS internal) | X-Internal-Token обязателен; bot — единственный носитель токена |
| Disambiguation token storage | In-memory _PENDING dict; рестарт бота → потерян (acceptable per D-47) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-30 | Spoofing | /add от не-OWNER | mitigate | _is_owner check + silent return (no message.answer = no spam) |
| T-04-31 | Tampering | callback_data с инжектированным category_id | mitigate | int parse + FK constraint в БД (CategoryNotFoundError 404 → bot graceful) |
| T-04-32 | Tampering | callback_data с длинным token (брute force) | mitigate | UUID4 8-hex 32 bits; TTL 5 мин — окно слишком короткое |
| T-04-33 | Information Disclosure | Bot reply содержит INTERNAL_TOKEN или другие secrets | mitigate | format functions не упоминают settings; api_client log strips |
| T-04-34 | DoS | Bot принимает шквал /add | accept | Single-tenant; aiogram processes serially per chat_id; rate-limit не нужен |
| T-04-35 | Tampering | parse_amount принимает overflow | mitigate | cap 10^12 копеек; больше — None |
| T-04-36 | Spoofing | parse_add_command принимает кириллицу как category_query | accept | Это feature, не угроза; ILIKE безопасно к SQL injection (bound param) |
| T-04-37 | Tampering | Disambiguation callback от другого пользователя (не OWNER) | mitigate | _is_owner check в cb_disambiguation; silent dismiss |
| T-04-38 | Resource Exhaustion | _PENDING dict растёт без bound | mitigate | _gc на каждом store_pending; TTL 5 мин — практически impossible accumulation |
</threat_model>

<verification>
- python -c для каждого модуля проходит.
- format_kopecks(150000) == "1 500".
- parse_amount edge cases все правильные.
- store_pending → 8-hex token; pop_pending(twice) → None on second.
- Wave 0 bot тесты test_bot_parsers.py + test_bot_handlers_phase4.py зеленеют после Plan 04-04.
- Existing test_telegram_chat_bind.py + test_main_bot_entry.py не сломаны.
</verification>

<success_criteria>
- 3 новых bot модуля (parsers, disambiguation, commands) + 2 расширения (api_client, main_bot) созданы.
- 5 command handlers + 1 callback handler работают.
- All handlers OWNER-gated (silent для не-OWNER).
- Disambiguation flow end-to-end: ambiguous response → kbd → callback → second API call → reply.
- format_kopecks helper output matches D-60 examples (1 500 / +5 200 / -1 500).
- Wave 0 bot тесты test_bot_parsers + test_bot_handlers_phase4 переходят в GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-actual-transactions-and-bot-commands/04-04-SUMMARY.md`.
</output>
