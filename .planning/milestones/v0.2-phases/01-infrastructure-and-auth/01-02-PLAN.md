---
phase: 01-infrastructure-and-auth
plan: 02
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - pyproject.toml
  - uv.lock
  - app/__init__.py
  - app/core/__init__.py
  - app/core/settings.py
  - app/core/logging.py
  - app/db/__init__.py
  - app/db/base.py
  - app/db/session.py
  - app/db/models.py
  - app/api/__init__.py
  - app/bot/__init__.py
  - app/worker/__init__.py
autonomous: true
requirements:
  - INF-01
  - INF-02

must_haves:
  truths:
    - "uv sync создаёт venv с корректными зависимостями"
    - "from app.core.settings import settings работает без ошибок при наличии .env"
    - "from app.db.models import AppUser, Category работает"
    - "from app.core.logging import configure_logging работает"
    - "Все 6 ORM-моделей определены с типами BIGINT копейки, DATE/TIMESTAMPTZ"
  artifacts:
    - path: "pyproject.toml"
      provides: "зависимости проекта, pytest config, uv toolchain"
      contains: "fastapi"
    - path: "app/core/settings.py"
      provides: "pydantic-settings Settings с 11 полями"
      exports: ["Settings", "settings"]
    - path: "app/db/models.py"
      provides: "6 ORM-моделей + app_health"
      contains: "class AppUser"
    - path: "app/db/session.py"
      provides: "async engine + sessionmaker"
      exports: ["async_engine", "AsyncSessionLocal", "get_db"]
  key_links:
    - from: "app/core/settings.py"
      to: ".env"
      via: "pydantic-settings BaseSettings env_file"
      pattern: "env_file.*\\.env"
    - from: "app/db/models.py"
      to: "app/db/base.py"
      via: "Base = DeclarativeBase()"
      pattern: "class.*Base"
---

<objective>
Python skeleton: pyproject.toml с зависимостями, структура пакета app/, настройки через pydantic-settings, structlog конфигурация, SQLAlchemy 2.x модели всех 6 таблиц + app_health, async session factory.

Purpose: Создать общую кодовую базу, которую шарят api, bot, worker контейнеры.
Output: Полная структура app/ с моделями и настройками; pyproject.toml с uv lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-and-auth/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-auth/01-RESEARCH.md
@docs/HLD.md

<interfaces>
<!-- HLD §2: ERD, таблицы, enums, индексы -->

Таблицы и поля (из HLD §2):
```
app_user: id PK, tg_user_id BIGINT UNIQUE NOT NULL, tg_chat_id BIGINT NULL,
          cycle_start_day INT DEFAULT 5, onboarded_at TIMESTAMPTZ NULL, created_at TIMESTAMPTZ

category: id PK, name TEXT NOT NULL, kind CategoryKind, is_archived BOOL DEFAULT FALSE,
          sort_order INT DEFAULT 0, created_at TIMESTAMPTZ

budget_period: id PK, period_start DATE NOT NULL UNIQUE, period_end DATE NOT NULL,
               starting_balance_cents BIGINT DEFAULT 0, ending_balance_cents BIGINT NULL,
               status PeriodStatus DEFAULT 'active', closed_at TIMESTAMPTZ NULL

plan_template_item: id PK, category_id FK→category, amount_cents BIGINT NOT NULL,
                    description TEXT, day_of_period INT NULL, sort_order INT DEFAULT 0

planned_transaction: id PK, period_id FK→budget_period, kind CategoryKind NOT NULL,
                     amount_cents BIGINT NOT NULL, description TEXT, category_id FK→category,
                     planned_date DATE NULL, source PlanSource NOT NULL,
                     subscription_id INT NULL FK→subscription

actual_transaction: id PK, period_id FK→budget_period, kind CategoryKind NOT NULL,
                    amount_cents BIGINT NOT NULL, description TEXT, category_id FK→category,
                    tx_date DATE NOT NULL, source ActualSource NOT NULL, created_at TIMESTAMPTZ

subscription: id PK, name TEXT NOT NULL, amount_cents BIGINT NOT NULL,
              cycle SubCycle NOT NULL, next_charge_date DATE NOT NULL,
              category_id FK→category, notify_days_before INT DEFAULT 2,
              is_active BOOL DEFAULT TRUE

app_health: id PK, service TEXT NOT NULL, last_heartbeat_at TIMESTAMPTZ NOT NULL
```

Enums (из HLD §2.2):
```python
CategoryKind   = "expense" | "income"
PeriodStatus   = "active" | "closed"
PlanSource     = "template" | "manual" | "subscription_auto"
ActualSource   = "mini_app" | "bot"
SubCycle       = "monthly" | "yearly"
```

Индексы (из HLD §2.3):
```
actual_transaction: (period_id, kind), (category_id, tx_date)
planned_transaction: (period_id, kind)
subscription: (is_active, next_charge_date)
budget_period: period_start UNIQUE (уже в column definition)
planned_transaction: UNIQUE (subscription_id, original_charge_date) — добавить поле original_charge_date DATE NULL
```

Settings поля (из HLD §8 + RESEARCH.md Pattern pydantic-settings):
```python
DATABASE_URL: str          # postgresql+asyncpg://
DATABASE_URL_SYNC: str     # postgresql:// (без asyncpg, для APScheduler)
BOT_TOKEN: str
OWNER_TG_ID: int
INTERNAL_TOKEN: str
API_BASE_URL: str = "http://api:8000"
PUBLIC_DOMAIN: str
DEV_MODE: bool = False
LOG_LEVEL: str = "INFO"
LOG_FORMAT: str = "json"   # "json" | "console"
APP_TZ: str = "Europe/Moscow"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать pyproject.toml и инициализировать uv</name>
  <read_first>
    - .planning/phases/01-infrastructure-and-auth/01-RESEARCH.md (Standard Stack таблица — версии библиотек, Installation section)
    - CLAUDE.md (Technology Stack раздел)
  </read_first>
  <files>pyproject.toml</files>
  <action>
Создать `pyproject.toml` в корне проекта:

```toml
[project]
name = "tg-budget-planner"
version = "0.1.0"
description = "Telegram Mini App budget planner"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.128.8",
    "uvicorn[standard]==0.39.0",
    "sqlalchemy[asyncio]==2.0.49",
    "asyncpg==0.31.0",
    "alembic==1.16.5",
    "pydantic==2.13.3",
    "pydantic-settings==2.11.0",
    "aiogram==3.22.0",
    "apscheduler<4",
    "structlog==25.5.0",
    "aiohttp==3.13.5",
    "pytz",
]

[project.optional-dependencies]
dev = [
    "pytest==8.4.2",
    "pytest-asyncio==1.2.0",
    "httpx==0.28.1",
]

[tool.uv]
dev-dependencies = [
    "pytest==8.4.2",
    "pytest-asyncio==1.2.0",
    "httpx==0.28.1",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

ВАЖНО: После создания файла НЕ запускать `uv sync` автоматически — это делается через Docker или вручную разработчиком. `uv.lock` будет создан при первом `uv sync`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); deps=d['project']['dependencies']; assert any('fastapi' in x for x in deps), 'fastapi missing'; assert any('sqlalchemy' in x for x in deps), 'sqlalchemy missing'; assert any('aiogram' in x for x in deps), 'aiogram missing'; assert any('alembic' in x for x in deps), 'alembic missing'; print('pyproject.toml OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml существует: `test -f pyproject.toml`
    - Содержит requires-python = ">=3.12": `grep -c 'requires-python.*3.12' pyproject.toml` == 1
    - Содержит fastapi==0.128.8: `grep -c 'fastapi==0.128.8' pyproject.toml` == 1
    - Содержит aiogram==3.22.0: `grep -c 'aiogram==3.22.0' pyproject.toml` == 1
    - Содержит "apscheduler<4": `grep -c '"apscheduler<4"' pyproject.toml` == 1
    - Содержит asyncio_mode = "auto" в pytest config: `grep -c 'asyncio_mode.*auto' pyproject.toml` == 1
    - Содержит testpaths = ["tests"]: `grep -c 'testpaths.*tests' pyproject.toml` == 1
    - Синтаксис TOML корректен: `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` без ошибок
  </acceptance_criteria>
  <done>pyproject.toml создан с корректными зависимостями и pytest конфигурацией</done>
</task>

<task type="auto">
  <name>Task 2: Создать app/ пакет — settings, logging, DB models, session</name>
  <read_first>
    - .planning/phases/01-infrastructure-and-auth/01-RESEARCH.md (Pattern 11 structlog, Pattern 1 SQLAlchemy session, Code Examples SQLAlchemy model)
    - docs/HLD.md §2 (ERD — все таблицы, §2.2 enums, §2.3 индексы)
    - CLAUDE.md (Conventions — BIGINT копейки, DATE/TIMESTAMPTZ, soft delete только category, single-tenant NO user_id FK)
  </read_first>
  <files>
    app/__init__.py,
    app/core/__init__.py,
    app/core/settings.py,
    app/core/logging.py,
    app/db/__init__.py,
    app/db/base.py,
    app/db/session.py,
    app/db/models.py,
    app/api/__init__.py,
    app/bot/__init__.py,
    app/worker/__init__.py
  </files>
  <action>
Создать пустые `__init__.py` для: `app/`, `app/core/`, `app/db/`, `app/api/`, `app/bot/`, `app/worker/`.

**app/core/settings.py:**
```python
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    DATABASE_URL: str = "postgresql+asyncpg://budget:budget@db:5432/budget_db"
    DATABASE_URL_SYNC: str = "postgresql://budget:budget@db:5432/budget_db"
    BOT_TOKEN: str = "changeme"
    OWNER_TG_ID: int = 0
    INTERNAL_TOKEN: str = "changeme"
    API_BASE_URL: str = "http://api:8000"
    PUBLIC_DOMAIN: str = "localhost"
    DEV_MODE: bool = False
    LOG_LEVEL: str = "INFO"
    LOG_FORMAT: str = "json"
    APP_TZ: str = "Europe/Moscow"


settings = Settings()
```

**app/core/logging.py:**
```python
import logging
import structlog


def configure_logging(log_level: str = "INFO", log_format: str = "json") -> None:
    level = getattr(logging, log_level.upper(), logging.INFO)
    logging.basicConfig(level=level, format="%(message)s")

    if log_format == "json":
        processors = [
            structlog.contextvars.merge_contextvars,
            structlog.stdlib.add_log_level,
            structlog.stdlib.add_logger_name,
            structlog.processors.TimeStamper(fmt="iso", utc=True),
            structlog.processors.JSONRenderer(),
        ]
    else:
        processors = [
            structlog.contextvars.merge_contextvars,
            structlog.stdlib.add_log_level,
            structlog.processors.TimeStamper(fmt="%H:%M:%S"),
            structlog.dev.ConsoleRenderer(),
        ]

    structlog.configure(
        processors=processors,
        wrapper_class=structlog.stdlib.BoundLogger,
        logger_factory=structlog.stdlib.LoggerFactory(),
        cache_logger_on_first_use=True,
    )
```

**app/db/base.py:**
```python
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    pass
```

**app/db/session.py:**
```python
from typing import AsyncGenerator
from sqlalchemy.ext.asyncio import (
    AsyncSession,
    async_sessionmaker,
    create_async_engine,
)
from app.core.settings import settings

async_engine = create_async_engine(settings.DATABASE_URL, echo=False)
AsyncSessionLocal = async_sessionmaker(async_engine, expire_on_commit=False)


async def get_db() -> AsyncGenerator[AsyncSession, None]:
    async with AsyncSessionLocal() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

**app/db/models.py:**
```python
"""
SQLAlchemy 2.x ORM models for TG Budget Planner.

Conventions (CLAUDE.md):
- Money: BIGINT kopecks (*_cents fields), no Float
- Dates: DATE for business dates, TIMESTAMPTZ for audit timestamps (UTC in DB)
- Soft delete: only Category (is_archived). Transactions/subscriptions: hard delete.
- Single-tenant MVP: NO user_id FK on any table.
"""
from datetime import date, datetime
from typing import Optional
import enum

from sqlalchemy import (
    BigInteger,
    Boolean,
    Date,
    ForeignKey,
    Index,
    Integer,
    String,
    Text,
    UniqueConstraint,
)
from sqlalchemy.dialects.postgresql import ENUM as PgEnum
from sqlalchemy.orm import Mapped, mapped_column, relationship
from sqlalchemy.sql import func
from sqlalchemy.types import TIMESTAMP

from app.db.base import Base


# ---------- Enums ----------

class CategoryKind(str, enum.Enum):
    expense = "expense"
    income = "income"


class PeriodStatus(str, enum.Enum):
    active = "active"
    closed = "closed"


class PlanSource(str, enum.Enum):
    template = "template"
    manual = "manual"
    subscription_auto = "subscription_auto"


class ActualSource(str, enum.Enum):
    mini_app = "mini_app"
    bot = "bot"


class SubCycle(str, enum.Enum):
    monthly = "monthly"
    yearly = "yearly"


# ---------- Models ----------

class AppUser(Base):
    __tablename__ = "app_user"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    tg_user_id: Mapped[int] = mapped_column(BigInteger, unique=True, nullable=False)
    tg_chat_id: Mapped[Optional[int]] = mapped_column(BigInteger, nullable=True)
    cycle_start_day: Mapped[int] = mapped_column(Integer, default=5, nullable=False)
    onboarded_at: Mapped[Optional[datetime]] = mapped_column(TIMESTAMP(timezone=True), nullable=True)
    created_at: Mapped[datetime] = mapped_column(
        TIMESTAMP(timezone=True), server_default=func.now(), nullable=False
    )


class Category(Base):
    __tablename__ = "category"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    name: Mapped[str] = mapped_column(Text, nullable=False)
    kind: Mapped[CategoryKind] = mapped_column(
        PgEnum(CategoryKind, name="categorykind", create_type=True), nullable=False
    )
    is_archived: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    sort_order: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    created_at: Mapped[datetime] = mapped_column(
        TIMESTAMP(timezone=True), server_default=func.now(), nullable=False
    )


class BudgetPeriod(Base):
    __tablename__ = "budget_period"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    period_start: Mapped[date] = mapped_column(Date, nullable=False, unique=True)
    period_end: Mapped[date] = mapped_column(Date, nullable=False)
    starting_balance_cents: Mapped[int] = mapped_column(BigInteger, default=0, nullable=False)
    ending_balance_cents: Mapped[Optional[int]] = mapped_column(BigInteger, nullable=True)
    status: Mapped[PeriodStatus] = mapped_column(
        PgEnum(PeriodStatus, name="periodstatus", create_type=True),
        default=PeriodStatus.active,
        nullable=False,
    )
    closed_at: Mapped[Optional[datetime]] = mapped_column(TIMESTAMP(timezone=True), nullable=True)

    planned_transactions: Mapped[list["PlannedTransaction"]] = relationship(back_populates="period")
    actual_transactions: Mapped[list["ActualTransaction"]] = relationship(back_populates="period")


class PlanTemplateItem(Base):
    __tablename__ = "plan_template_item"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    category_id: Mapped[int] = mapped_column(ForeignKey("category.id"), nullable=False)
    amount_cents: Mapped[int] = mapped_column(BigInteger, nullable=False)
    description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    day_of_period: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
    sort_order: Mapped[int] = mapped_column(Integer, default=0, nullable=False)

    category: Mapped["Category"] = relationship()


class Subscription(Base):
    __tablename__ = "subscription"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    name: Mapped[str] = mapped_column(Text, nullable=False)
    amount_cents: Mapped[int] = mapped_column(BigInteger, nullable=False)
    cycle: Mapped[SubCycle] = mapped_column(
        PgEnum(SubCycle, name="subcycle", create_type=True), nullable=False
    )
    next_charge_date: Mapped[date] = mapped_column(Date, nullable=False)
    category_id: Mapped[int] = mapped_column(ForeignKey("category.id"), nullable=False)
    notify_days_before: Mapped[int] = mapped_column(Integer, default=2, nullable=False)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True, nullable=False)

    category: Mapped["Category"] = relationship()

    __table_args__ = (
        Index("ix_subscription_active_charge", "is_active", "next_charge_date"),
    )


class PlannedTransaction(Base):
    __tablename__ = "planned_transaction"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    period_id: Mapped[int] = mapped_column(ForeignKey("budget_period.id"), nullable=False)
    kind: Mapped[CategoryKind] = mapped_column(
        PgEnum(CategoryKind, name="categorykind", create_type=False), nullable=False
    )
    amount_cents: Mapped[int] = mapped_column(BigInteger, nullable=False)
    description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    category_id: Mapped[int] = mapped_column(ForeignKey("category.id"), nullable=False)
    planned_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)
    source: Mapped[PlanSource] = mapped_column(
        PgEnum(PlanSource, name="plansource", create_type=True), nullable=False
    )
    subscription_id: Mapped[Optional[int]] = mapped_column(ForeignKey("subscription.id"), nullable=True)
    original_charge_date: Mapped[Optional[date]] = mapped_column(Date, nullable=True)

    period: Mapped["BudgetPeriod"] = relationship(back_populates="planned_transactions")
    category: Mapped["Category"] = relationship()

    __table_args__ = (
        Index("ix_planned_period_kind", "period_id", "kind"),
        UniqueConstraint("subscription_id", "original_charge_date", name="uq_planned_sub_charge_date"),
    )


class ActualTransaction(Base):
    __tablename__ = "actual_transaction"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    period_id: Mapped[int] = mapped_column(ForeignKey("budget_period.id"), nullable=False)
    kind: Mapped[CategoryKind] = mapped_column(
        PgEnum(CategoryKind, name="categorykind", create_type=False), nullable=False
    )
    amount_cents: Mapped[int] = mapped_column(BigInteger, nullable=False)
    description: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    category_id: Mapped[int] = mapped_column(ForeignKey("category.id"), nullable=False)
    tx_date: Mapped[date] = mapped_column(Date, nullable=False)
    source: Mapped[ActualSource] = mapped_column(
        PgEnum(ActualSource, name="actualsource", create_type=True), nullable=False
    )
    created_at: Mapped[datetime] = mapped_column(
        TIMESTAMP(timezone=True), server_default=func.now(), nullable=False
    )

    period: Mapped["BudgetPeriod"] = relationship(back_populates="actual_transactions")
    category: Mapped["Category"] = relationship()

    __table_args__ = (
        Index("ix_actual_period_kind", "period_id", "kind"),
        Index("ix_actual_category_date", "category_id", "tx_date"),
    )


class AppHealth(Base):
    """Worker heartbeat table (D-12)."""
    __tablename__ = "app_health"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    service: Mapped[str] = mapped_column(String(50), nullable=False)
    last_heartbeat_at: Mapped[datetime] = mapped_column(
        TIMESTAMP(timezone=True), nullable=False
    )
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "
import ast, os
files = [
    'app/__init__.py', 'app/core/__init__.py', 'app/core/settings.py',
    'app/core/logging.py', 'app/db/__init__.py', 'app/db/base.py',
    'app/db/session.py', 'app/db/models.py',
    'app/api/__init__.py', 'app/bot/__init__.py', 'app/worker/__init__.py'
]
for f in files:
    assert os.path.exists(f), f'Missing: {f}'
    ast.parse(open(f).read())
print('All files exist and syntax OK')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/core/settings.py содержит 11 полей Settings: DATABASE_URL, DATABASE_URL_SYNC, BOT_TOKEN, OWNER_TG_ID, INTERNAL_TOKEN, API_BASE_URL, PUBLIC_DOMAIN, DEV_MODE, LOG_LEVEL, LOG_FORMAT, APP_TZ
    - `grep -c "DATABASE_URL_SYNC" app/core/settings.py` == 1 (APScheduler sync URL)
    - `grep -c "DEV_MODE" app/core/settings.py` == 1
    - app/db/models.py содержит 8 классов моделей: `grep -c "^class.*Base" app/db/models.py` == 8 (AppUser, Category, BudgetPeriod, PlanTemplateItem, Subscription, PlannedTransaction, ActualTransaction, AppHealth)
    - Нет ни одного Float в models.py: `grep -v "^#" app/db/models.py | grep -c "Float"` == 0
    - Все денежные поля — BigInteger: `grep -c "BigInteger" app/db/models.py` >= 5
    - Нет user_id FK (single-tenant): `grep -v "^#" app/db/models.py | grep -c "user_id"` == 0
    - app/core/logging.py содержит configure_logging функцию: `grep -c "def configure_logging" app/core/logging.py` == 1
    - Composite index на (period_id, kind) для actual_transaction: `grep -c "ix_actual_period_kind" app/db/models.py` == 1
    - Composite index на (is_active, next_charge_date) для subscription: `grep -c "ix_subscription_active_charge" app/db/models.py` == 1
    - UniqueConstraint на (subscription_id, original_charge_date): `grep -c "uq_planned_sub_charge_date" app/db/models.py` == 1
    - Soft delete поле только в Category: `grep -c "is_archived" app/db/models.py` == 1
    - compare_digest не используется в models (не нужен): безопасно
    - Все файлы имеют корректный синтаксис Python
  </acceptance_criteria>
  <done>Все модули app/ созданы: settings с 11 полями, 8 ORM-моделей с BIGINT копейки, без user_id FK, с нужными индексами и unique constraints</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ENV → Settings | Секреты поступают через ENV; .env файл не попадает в git |
| models → DB | ORM работает с параметризованными запросами (SQLAlchemy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-secrets | Info Disclosure | app/core/settings.py, .env | mitigate | .env добавлен в .gitignore (Plan 06); .env.example без реальных значений (D-06) |
| T-devmode | Spoofing | app/core/settings.py DEV_MODE | mitigate | DEV_MODE: bool = False по умолчанию (per D-05); переопределяется только в docker-compose.override.yml для dev |
| T-sqlinj | Tampering | app/db/session.py, models | accept | SQLAlchemy использует параметризованные запросы; raw SQL не используется в моделях |
</threat_model>

<verification>
Plan 02 завершён когда:
1. `python -c "import ast; ast.parse(open('app/db/models.py').read()); print('OK')"` — без ошибок
2. `python -c "import ast; ast.parse(open('app/core/settings.py').read()); print('OK')"` — без ошибок
3. `grep -v "^#" app/db/models.py | grep -c "Float"` == 0 (нет float денег)
4. `grep -c "BigInteger" app/db/models.py` >= 5 (денежные поля BIGINT)
5. `grep -v "^#" app/db/models.py | grep -c "user_id"` == 0 (single-tenant, нет user_id FK)
</verification>

<success_criteria>
- pyproject.toml содержит все 13 зависимостей + dev зависимости
- Структура app/ полная: core/, db/, api/, bot/, worker/ с __init__.py
- settings.py: все 11 ENV-полей с типами, DEV_MODE=False по умолчанию
- models.py: 8 классов, все деньги BIGINT, без Float, без user_id FK, с индексами HLD §2.3
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure-and-auth/01-02-SUMMARY.md`
</output>
