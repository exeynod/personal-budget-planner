---
phase: 06-subscriptions-worker-jobs
plan: 06
type: execute
wave: 4
depends_on: [06-05]
files_modified:
  - frontend/src/screens/SubscriptionsScreen.tsx
  - frontend/src/screens/SubscriptionsScreen.module.css
  - frontend/src/screens/SettingsScreen.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [SUB-01, SUB-02, SUB-04, SUB-05, SET-02]
must_haves:
  truths:
    - "Экран Подписки доступен через nav-bar в App.tsx"
    - "Hero-блок показывает count активных + monthly load (yearly /12)"
    - "Timeline-card отображает горизонтальную шкалу с dots по next_charge_date"
    - "Цветовая логика timeline/pills: ≤2 дня красный, ≤7 жёлтый"
    - "Today-line отображается на timeline"
    - "Список карточек подписок отсортирован ASC по next_charge_date"
    - "MainButton + tap на карточку открывают SubscriptionEditor"
    - "Settings экран содержит поле notify_days_before с PATCH"
    - "Visual UAT pass — пользователь видит работающий экран в Mini App"
  artifacts:
    - path: "frontend/src/screens/SubscriptionsScreen.tsx"
      provides: "Главный экран подписок (sketch 004-A)"
      exports: ["SubscriptionsScreen"]
    - path: "frontend/src/App.tsx"
      provides: "Screen union extended + nav кнопка Подписки"
      contains: "subscriptions"
    - path: "frontend/src/screens/SettingsScreen.tsx"
      provides: "Поле notify_days_before в Settings"
      contains: "notify_days_before"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "SubscriptionsScreen"
      via: "screen === 'subscriptions' ? <SubscriptionsScreen /> : ..."
      pattern: "SubscriptionsScreen"
    - from: "frontend/src/screens/SubscriptionsScreen.tsx"
      to: "useSubscriptions + SubscriptionEditor"
      via: "imports + render"
      pattern: "useSubscriptions"
---

<objective>
Wave 4: финальный UI слой — SubscriptionsScreen (sketch 004-A: hero + timeline + flat list),
расширение SettingsScreen для notify_days_before, регистрация экрана в App.tsx с nav-кнопкой.
Per D-82 / D-84.

Purpose: SUB-01/SUB-02/SUB-05/SET-02 видны в Mini App. После этого волна 06-07 верификации.

Output: пользователь может в Mini App создавать/редактировать подписки и менять глобальный notify_days_before.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-subscriptions-worker-jobs/06-CONTEXT.md
@.planning/phases/06-subscriptions-worker-jobs/06-UI-SPEC.md
@.planning/sketches/MANIFEST.md
@frontend/src/App.tsx
@frontend/src/screens/SettingsScreen.tsx
@frontend/src/screens/HomeScreen.tsx
@frontend/src/screens/PlannedScreen.tsx
@frontend/src/components/MainButton.tsx
@frontend/src/components/Fab.tsx
@frontend/src/components/SectionCard.tsx
@frontend/src/themes/default.css
@frontend/src/utils/format.ts

<interfaces>
# App.tsx Screen union (existing pattern):
type Screen = 'home' | 'planned' | 'actual' | 'categories' | 'settings';
// Add 'subscriptions' to union

# SettingsScreen pattern: input field + handle PATCH
const handleSave = async () => {
  await updateSettings({ cycle_start_day, notify_days_before });
  await refetch();
};

# Format util (frontend/src/utils/format.ts):
formatKopecks(cents) → '12 345,67' (без рубля)
formatKopecksWithRub(cents) → '12 345,67 ₽'

# Date math для timeline:
const daysInMonth = new Date(year, month + 1, 0).getDate();
const dayOfMonth = date.getDate();
const pct = ((dayOfMonth - 1) / (daysInMonth - 1)) * 100;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SubscriptionsScreen — hero + timeline + список (D-82)</name>
  <files>frontend/src/screens/SubscriptionsScreen.tsx, frontend/src/screens/SubscriptionsScreen.module.css</files>
  <behavior>
    - Hero block: count активных, monthly load (sum monthly + sum yearly/12)
    - Timeline card: горизонтальная CSS шкала, dots по дням, today-line, цвет по дням
    - Список карточек: name + cycle badge + amount + pill «через N дн.» + toggle is_active + tap → edit
    - MainButton «+ Добавить подписку»
    - Empty-state: «Нет подписок» + CTA
  </behavior>
  <action>
    Создать `frontend/src/screens/SubscriptionsScreen.tsx`:

    ```typescript
    import { useMemo, useState } from 'react';
    import { useSubscriptions } from '../hooks/useSubscriptions';
    import { SubscriptionEditor } from '../components/SubscriptionEditor';
    import { MainButton } from '../components/MainButton';
    import { SectionCard } from '../components/SectionCard';
    import { useSettings } from '../hooks/useSettings';
    import { formatKopecksWithRub } from '../utils/format';
    import { createSubscription, updateSubscription, deleteSubscription } from '../api/subscriptions';
    import type { SubscriptionRead } from '../api/types';
    import styles from './SubscriptionsScreen.module.css';

    function daysUntil(dateStr: string): number {
      const today = new Date(); today.setHours(0,0,0,0);
      const d = new Date(dateStr); d.setHours(0,0,0,0);
      return Math.round((d.getTime() - today.getTime()) / 86400000);
    }
    function colorClass(days: number): string {
      if (days <= 2) return styles.danger;
      if (days <= 7) return styles.warn;
      return styles.neutral;
    }

    export function SubscriptionsScreen() {
      const { subscriptions, loading, mutate } = useSubscriptions();
      const { settings } = useSettings();
      const [editor, setEditor] = useState<{mode:'create'|'edit', sub?:SubscriptionRead}|null>(null);

      const monthlyLoad = useMemo(() => subscriptions
        .filter(s => s.is_active)
        .reduce((acc, s) => acc + (s.cycle === 'monthly' ? s.amount_cents : Math.round(s.amount_cents / 12)), 0)
      , [subscriptions]);
      const activeCount = subscriptions.filter(s => s.is_active).length;

      const handleCreate = async (payload: any) => {
        await mutate(() => createSubscription(payload));
      };
      const handleUpdate = (id: number) => async (payload: any) => {
        await mutate(() => updateSubscription(id, payload));
      };
      const handleDelete = (id: number) => async () => {
        await mutate(() => deleteSubscription(id));
      };

      return (
        <div className={styles.screen}>
          <SectionCard className={styles.hero}>
            <div className={styles.heroTitle}>Подписки</div>
            <div className={styles.heroStats}>
              <div><span className={styles.stat}>{activeCount}</span><span className={styles.statLabel}>активных</span></div>
              <div><span className={styles.stat}>{formatKopecksWithRub(monthlyLoad)}</span><span className={styles.statLabel}>в месяц</span></div>
            </div>
          </SectionCard>

          <SectionCard title="Ближайшие списания">
            <Timeline subscriptions={subscriptions.filter(s => s.is_active)} />
          </SectionCard>

          <SectionCard title={`Все подписки (${subscriptions.length})`}>
            {subscriptions.length === 0 && <div className={styles.empty}>Подписок пока нет</div>}
            {subscriptions.map(s => {
              const days = daysUntil(s.next_charge_date);
              return (
                <button key={s.id} className={styles.card} onClick={() => setEditor({mode:'edit', sub:s})}>
                  <div className={styles.cardLeft}>
                    <div className={styles.cardName}>{s.name}</div>
                    <div className={styles.cardMeta}>
                      <span className={styles.cycleBadge}>{s.cycle === 'monthly' ? 'мес' : 'год'}</span>
                      <span className={styles.cardCat}>{s.category.name}</span>
                    </div>
                  </div>
                  <div className={styles.cardRight}>
                    <div className={styles.cardAmount}>{formatKopecksWithRub(s.amount_cents)}</div>
                    <div className={`${styles.pill} ${colorClass(days)}`}>через {days} дн.</div>
                  </div>
                </button>
              );
            })}
          </SectionCard>

          <MainButton onClick={() => setEditor({mode:'create'})}>+ Добавить подписку</MainButton>

          {editor && (
            <SubscriptionEditor
              mode={editor.mode}
              initial={editor.sub}
              defaultNotifyDays={settings?.notify_days_before ?? 2}
              onClose={() => setEditor(null)}
              onSubmit={editor.mode === 'create' ? handleCreate : handleUpdate(editor.sub!.id)}
              onDelete={editor.mode === 'edit' ? handleDelete(editor.sub!.id) : undefined}
            />
          )}
        </div>
      );
    }

    function Timeline({ subscriptions }: { subscriptions: SubscriptionRead[] }) {
      const today = new Date();
      const year = today.getFullYear(), month = today.getMonth();
      const daysInMonth = new Date(year, month + 1, 0).getDate();
      const todayDay = today.getDate();
      const todayPct = ((todayDay - 1) / (daysInMonth - 1)) * 100;

      const dotsThisMonth = subscriptions.filter(s => {
        const d = new Date(s.next_charge_date);
        return d.getFullYear() === year && d.getMonth() === month;
      });

      return (
        <div className={styles.timeline}>
          <div className={styles.timelineLine} />
          <div className={styles.todayLine} style={{ left: `${todayPct}%` }} />
          {dotsThisMonth.map(s => {
            const d = new Date(s.next_charge_date).getDate();
            const pct = ((d - 1) / (daysInMonth - 1)) * 100;
            const days = daysUntil(s.next_charge_date);
            return (
              <div
                key={s.id}
                className={`${styles.dot} ${colorClass(days)}`}
                style={{ left: `${pct}%` }}
                title={`${s.name}: ${s.next_charge_date}`}
              />
            );
          })}
        </div>
      );
    }
    ```

    CSS module: hero gradient, timeline (height 60px, position relative, line на top:50%, dots absolute с translateX(-50%)), pill цвета через CSS modules (.danger/.warn/.neutral используют var(--c-danger)/var(--c-warn)/var(--text-secondary)).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -15</automated>
  </verify>
  <done>
    Компилируется и собирается. Component imports работают.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Регистрация в App.tsx + расширение SettingsScreen (D-84, D-77)</name>
  <files>frontend/src/App.tsx, frontend/src/screens/SettingsScreen.tsx</files>
  <behavior>
    - Screen union включает 'subscriptions'
    - Nav-bar содержит кнопку «Подписки»
    - Routing: screen === 'subscriptions' → <SubscriptionsScreen />
    - SettingsScreen содержит секцию «Уведомления о подписках» с number input notify_days_before (0..30)
    - PATCH settings отправляет оба поля при изменении
  </behavior>
  <action>
    1. `frontend/src/App.tsx`:
       - Расширить Screen union: добавить `'subscriptions'`
       - Импорт: `import { SubscriptionsScreen } from './screens/SubscriptionsScreen';`
       - В рендере: `screen === 'subscriptions' && <SubscriptionsScreen />`
       - В nav-bar: добавить кнопку рядом с другими экранами (используя existing pattern button → setScreen('subscriptions'))

    2. `frontend/src/screens/SettingsScreen.tsx`:
       - Добавить state `notifyDays` инициализированный из settings.notify_days_before
       - Добавить секцию (SectionCard title="Уведомления о подписках"):
         ```tsx
         <label>Напоминать за (дней до списания)</label>
         <input type="number" min={0} max={30} value={notifyDays} onChange={e => setNotifyDays(Number(e.target.value))} />
         <small>Применяется только к новым подпискам. Существующие имеют свой настроенный override.</small>
         ```
       - В handleSave: добавить `notify_days_before: notifyDays` в payload
       - PATCH endpoint в API уже умеет partial-update обоих полей (06-03)
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    Build успешен. Nav-кнопка «Подписки» рендерится. Settings показывает поле notify_days_before.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual UAT — подписки и settings в Mini App</name>
  <what-built>
    Полный UI подписок (sketch 004-A) + расширение Settings. После этой проверки фаза готова к финальной верификации.
  </what-built>
  <how-to-verify>
    1. `docker-compose up --build` — собрать образы (frontend image включает новые exports)
    2. Открыть Mini App в Telegram
    3. **Nav-bar**: убедиться что появилась кнопка «Подписки», тап переключает экран
    4. **Empty state**: при нулевом списке видно «Подписок пока нет» + MainButton «+ Добавить подписку»
    5. **Создать подписку**: MainButton → форма открывается → заполнить (Spotify, 299, monthly, через 5 дней, любая категория) → Сохранить
    6. **Список**: подписка появилась с amount, cycle badge «мес», pill «через 5 дн.» (жёлтая, ≤7)
    7. **Hero**: «1 активных», «299,00 ₽ в месяц»
    8. **Timeline**: точка на правильной позиции в текущем месяце, today-line вертикально
    9. **Edit**: тап на карточку → SubscriptionEditor открывается в edit mode → менять amount, сохранить → обновлено
    10. **Delete**: edit mode → кнопка удалить → подтверждение → удалена
    11. **Settings**: открыть Settings → поле «Напоминать за (дней до списания)» → менять с 2 на 5 → Save → обновить экран → значение сохранилось (5)
    12. **Цвета pill**: создать подписку с next_charge_date через 1 день → pill красный; через 10 дней → нейтральный
  </how-to-verify>
  <resume-signal>Type "approved" если все 12 пунктов работают, иначе опиши проблему.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI inputs | parseFloat amount, number input range 0..30 для notify_days_before |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Tampering | client-side notify_days_before | mitigate | input min/max + Pydantic backend validation |
</threat_model>

<verification>
- `npm run build` без ошибок
- Visual UAT: 12 пунктов checkpoint
- В DOM присутствует timeline с правильным позиционированием dots
</verification>

<success_criteria>
SUB-01/SUB-02/SUB-05/SET-02 visible в Mini App. Пользователь может полноценно вести подписки.
</success_criteria>

<output>
Создать `.planning/phases/06-subscriptions-worker-jobs/06-06-SUMMARY.md`:
- Скриншоты/описание визуальных state'ов
- Результаты UAT
- Заметки для верификации (06-07)
</output>
