---
phase: 03-plan-template-and-planned-transactions
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/templates.ts
  - frontend/src/hooks/useTemplate.ts
  - frontend/src/components/BottomSheet.tsx
  - frontend/src/components/BottomSheet.module.css
  - frontend/src/components/PlanItemEditor.tsx
  - frontend/src/components/PlanItemEditor.module.css
  - frontend/src/components/PlanRow.tsx
  - frontend/src/components/PlanRow.module.css
  - frontend/src/screens/TemplateScreen.tsx
  - frontend/src/screens/TemplateScreen.module.css
  - frontend/src/screens/HomeScreen.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - TPL-01
  - TPL-02

must_haves:
  truths:
    - "TemplateScreen рендерит группировку Расходы/Доходы → категории → строки шаблона (sketch 005-B)"
    - "Inline-edit amount работает: Enter сохраняет, Esc отменяет, ✓/× кнопки"
    - "BottomSheet открывается на tap-non-amount; PlanItemEditor поля + validation"
    - "TS типы для PlanSource, TemplateItemRead, PlannedRead зеркалируют backend Pydantic schemas"
    - "Frontend type-check passes (npx tsc --noEmit exit=0)"
    - "Vite build passes"
  artifacts:
    - path: "frontend/src/api/types.ts"
      provides: "TS-типы PlanSource, TemplateItemRead, PlannedRead, TemplateItemCreatePayload, TemplateItemUpdatePayload, PlannedCreatePayload, PlannedUpdatePayload, ApplyTemplateResponse, SnapshotFromPeriodResponse"
    - path: "frontend/src/api/templates.ts"
      provides: "listTemplateItems, createTemplateItem, updateTemplateItem, deleteTemplateItem, snapshotFromPeriod"
    - path: "frontend/src/hooks/useTemplate.ts"
      provides: "useTemplate() hook with state + refetch (паттерн useCategories)"
    - path: "frontend/src/components/BottomSheet.tsx"
      provides: "Reusable bottom-sheet primitive (CSS-only, Telegram BackButton + Esc)"
    - path: "frontend/src/components/PlanItemEditor.tsx"
      provides: "Universal form for create/edit template-item or planned-row inside BottomSheet"
    - path: "frontend/src/components/PlanRow.tsx"
      provides: "Single row display + inline-edit amount + tap-to-open-editor"
    - path: "frontend/src/screens/TemplateScreen.tsx"
      provides: "Group-by-kind→category layout + inline-edit + BottomSheet integration"
  key_links:
    - from: "frontend/src/screens/TemplateScreen.tsx"
      to: "/api/v1/template/items (GET/POST/PATCH/DELETE)"
      via: "useTemplate hook → templates.ts → apiFetch"
      pattern: "apiFetch.*template"
    - from: "frontend/src/components/BottomSheet.tsx"
      to: "window.Telegram.WebApp.BackButton"
      via: "useEffect subscribe/unsubscribe"
      pattern: "BackButton"

user_setup: []
---

<objective>
Wave 3 (parallel with Plan 03-05): построить frontend для TemplateScreen — экран управления шаблоном плана. Также создать переиспользуемые компоненты BottomSheet, PlanItemEditor и PlanRow, которые будут использоваться обоими экранами (Template + Planned).

Purpose: Полностью реализовать TPL-02 (CRUD UI с inline-edit и BottomSheet). После этой задачи пользователь может создавать/редактировать/удалять строки шаблона через Telegram Mini App.
Output: 11 frontend файлов (5 новых компонентов + 1 экран + 1 hook + 1 api client + 1 types update + CSS modules).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-plan-template-and-planned-transactions/03-CONTEXT.md
@.planning/phases/03-plan-template-and-planned-transactions/03-RESEARCH.md
@.planning/phases/03-plan-template-and-planned-transactions/03-UI-SPEC.md
@frontend/src/api/client.ts
@frontend/src/api/categories.ts
@frontend/src/api/types.ts
@frontend/src/hooks/useCategories.ts
@frontend/src/screens/CategoriesScreen.tsx
@frontend/src/components/CategoryRow.tsx
@frontend/src/components/NewCategoryForm.tsx
@frontend/src/components/SectionCard.tsx
@frontend/src/components/MainButton.tsx
@frontend/src/styles/tokens.css

<interfaces>
<!-- Backend контракты из Plan 03-03 (готовые) -->

REST API endpoints:
- `GET /api/v1/template/items` → `TemplateItemRead[]`
- `POST /api/v1/template/items` body=TemplateItemCreatePayload → `TemplateItemRead` | 400 | 404 | 422
- `PATCH /api/v1/template/items/{id}` body=TemplateItemUpdatePayload → `TemplateItemRead` | 404 | 400 | 422
- `DELETE /api/v1/template/items/{id}` → `TemplateItemRead` | 404
- `POST /api/v1/template/snapshot-from-period/{period_id}` → `SnapshotFromPeriodResponse` | 404

TypeScript types (мирror backend Pydantic schemas):
```typescript
type PlanSource = 'template' | 'manual' | 'subscription_auto';
type Kind = 'expense' | 'income';

interface TemplateItemRead {
  id: number;
  category_id: number;
  amount_cents: number;
  description: string | null;
  day_of_period: number | null;
  sort_order: number;
}

interface TemplateItemCreatePayload {
  category_id: number;
  amount_cents: number;
  description?: string | null;
  day_of_period?: number | null;
  sort_order?: number;
}

interface TemplateItemUpdatePayload {
  category_id?: number;
  amount_cents?: number;
  description?: string | null;
  day_of_period?: number | null;
  sort_order?: number;
}

interface SnapshotFromPeriodResponse {
  template_items: TemplateItemRead[];
  replaced: number;
}

// Используется в Plan 03-05 (PlannedScreen) — определяем здесь, чтобы не дублировать
interface PlannedRead {
  id: number;
  period_id: number;
  kind: Kind;
  amount_cents: number;
  description: string | null;
  category_id: number;
  planned_date: string | null;  // ISO date
  source: PlanSource;
  subscription_id: number | null;
}

interface PlannedCreatePayload {
  kind: Kind;
  amount_cents: number;
  description?: string | null;
  category_id: number;
  planned_date?: string | null;
}

interface PlannedUpdatePayload {
  kind?: Kind;
  amount_cents?: number;
  description?: string | null;
  category_id?: number;
  planned_date?: string | null;
}

interface ApplyTemplateResponse {
  period_id: number;
  created: number;
  planned: PlannedRead[];
}
```

Pre-existing reuse:
- `frontend/src/api/client.ts::apiFetch<T>(path, init?)` — auto-inject X-Telegram-Init-Data, throws ApiError on non-2xx.
- `frontend/src/api/categories.ts` — паттерн: `export async function listCategories(): Promise<CategoryRead[]> { return apiFetch('/categories'); }`.
- `frontend/src/hooks/useCategories.ts` — паттерн: useState + useEffect + refetch + loading/error.
- `frontend/src/components/CategoryRow.tsx` — inline-edit pattern (Enter/Esc/✓/×).
- `frontend/src/styles/tokens.css` — CSS variables.

Telegram WebApp BackButton (для BottomSheet):
- `window.Telegram.WebApp.BackButton.show()` / `.hide()` / `.onClick(cb)` / `.offClick(cb)`.
- Доступен в Mini App; в browser dev — undefined (Esc как fallback).

Currency parsing (для amount_cents input):
- В `OnboardingScreen.tsx` есть `parseRubles`-подобная функция. Скопировать паттерн:
  ```typescript
  function parseRublesToKopecks(input: string): number | null {
    const cleaned = input.replace(/\s/g, '').replace(',', '.');
    const f = parseFloat(cleaned);
    if (isNaN(f) || !isFinite(f)) return null;
    return Math.round(f * 100);
  }

  function formatKopecksToRubles(cents: number): string {
    return (cents / 100).toLocaleString('ru-RU', { maximumFractionDigits: 2 });
  }
  ```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TS types + API client + useTemplate hook</name>
  <files>frontend/src/api/types.ts, frontend/src/api/templates.ts, frontend/src/hooks/useTemplate.ts</files>
  <behavior>
- types.ts расширен: PlanSource, Kind alias, TemplateItemRead/Create/Update, PlannedRead/Create/Update, ApplyTemplateResponse, SnapshotFromPeriodResponse.
- templates.ts экспортирует 5 функций: listTemplateItems, createTemplateItem, updateTemplateItem, deleteTemplateItem, snapshotFromPeriod.
- useTemplate hook возвращает `{ items, loading, error, refetch }` (паттерн useCategories).
- TS-тип-check проходит без ошибок.
  </behavior>
  <action>
**1. Расширить frontend/src/api/types.ts** (добавить в конец файла):

```typescript
// ---------- Phase 3: Plan Template & Planned Transactions ----------

export type PlanSource = 'template' | 'manual' | 'subscription_auto';

/** Mirrors `TemplateItemRead` from app/api/schemas/templates.py. */
export interface TemplateItemRead {
  id: number;
  category_id: number;
  amount_cents: number;
  description: string | null;
  day_of_period: number | null;
  sort_order: number;
}

export interface TemplateItemCreatePayload {
  category_id: number;
  amount_cents: number;
  description?: string | null;
  day_of_period?: number | null;
  sort_order?: number;
}

export interface TemplateItemUpdatePayload {
  category_id?: number;
  amount_cents?: number;
  description?: string | null;
  day_of_period?: number | null;
  sort_order?: number;
}

export interface SnapshotFromPeriodResponse {
  template_items: TemplateItemRead[];
  replaced: number;
}

/** Mirrors `PlannedRead` from app/api/schemas/planned.py. */
export interface PlannedRead {
  id: number;
  period_id: number;
  kind: CategoryKind;
  amount_cents: number;
  description: string | null;
  category_id: number;
  planned_date: string | null; // ISO date
  source: PlanSource;
  subscription_id: number | null;
}

export interface PlannedCreatePayload {
  kind: CategoryKind;
  amount_cents: number;
  description?: string | null;
  category_id: number;
  planned_date?: string | null;
}

export interface PlannedUpdatePayload {
  kind?: CategoryKind;
  amount_cents?: number;
  description?: string | null;
  category_id?: number;
  planned_date?: string | null;
}

export interface ApplyTemplateResponse {
  period_id: number;
  created: number;
  planned: PlannedRead[];
}
```

(Обратите внимание: `CategoryKind` уже экспортируется как `'expense' | 'income'` в существующем файле — переиспользуем.)

**2. Создать frontend/src/api/templates.ts**:

```typescript
import { apiFetch } from './client';
import type {
  SnapshotFromPeriodResponse,
  TemplateItemCreatePayload,
  TemplateItemRead,
  TemplateItemUpdatePayload,
} from './types';

/** GET /api/v1/template/items */
export async function listTemplateItems(): Promise<TemplateItemRead[]> {
  return apiFetch<TemplateItemRead[]>('/template/items');
}

/** POST /api/v1/template/items */
export async function createTemplateItem(
  payload: TemplateItemCreatePayload,
): Promise<TemplateItemRead> {
  return apiFetch<TemplateItemRead>('/template/items', {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

/** PATCH /api/v1/template/items/{id} — partial update. */
export async function updateTemplateItem(
  id: number,
  patch: TemplateItemUpdatePayload,
): Promise<TemplateItemRead> {
  return apiFetch<TemplateItemRead>(`/template/items/${id}`, {
    method: 'PATCH',
    body: JSON.stringify(patch),
  });
}

/** DELETE /api/v1/template/items/{id} — hard delete. */
export async function deleteTemplateItem(id: number): Promise<TemplateItemRead> {
  return apiFetch<TemplateItemRead>(`/template/items/${id}`, { method: 'DELETE' });
}

/** POST /api/v1/template/snapshot-from-period/{period_id} (TPL-03). */
export async function snapshotFromPeriod(
  periodId: number,
): Promise<SnapshotFromPeriodResponse> {
  return apiFetch<SnapshotFromPeriodResponse>(
    `/template/snapshot-from-period/${periodId}`,
    { method: 'POST' },
  );
}
```

**3. Создать frontend/src/hooks/useTemplate.ts** — копировать паттерн `useCategories.ts`:

```typescript
import { useCallback, useEffect, useState } from 'react';
import { listTemplateItems } from '../api/templates';
import type { TemplateItemRead } from '../api/types';

export interface UseTemplateResult {
  items: TemplateItemRead[];
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

/**
 * Fetches all template items on mount; exposes refetch for after-mutation reload.
 *
 * Single-tenant — no need for optimistic updates; refetch after mutation
 * keeps state consistent (T-fe-stale mitigation, carry-over from Phase 2).
 */
export function useTemplate(): UseTemplateResult {
  const [items, setItems] = useState<TemplateItemRead[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refetch = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await listTemplateItems();
      setItems(data);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setError(null);
    listTemplateItems()
      .then((data) => {
        if (!cancelled) setItems(data);
      })
      .catch((e: unknown) => {
        if (!cancelled) setError(e instanceof Error ? e.message : String(e));
      })
      .finally(() => {
        if (!cancelled) setLoading(false);
      });
    return () => {
      cancelled = true;
    };
  }, []);

  return { items, loading, error, refetch };
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -20 && echo "EXIT $?"</automated>
  </verify>
  <done>
- types.ts содержит 9 новых export'ов (PlanSource, TemplateItem*, Planned*, ApplyTemplateResponse, SnapshotFromPeriodResponse).
- templates.ts экспортирует 5 функций (listTemplateItems, createTemplateItem, updateTemplateItem, deleteTemplateItem, snapshotFromPeriod).
- useTemplate.ts экспортирует функцию-hook возвращающую {items, loading, error, refetch}.
- npx tsc --noEmit → exit=0 (zero TypeScript errors).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BottomSheet + PlanItemEditor + PlanRow components</name>
  <files>frontend/src/components/BottomSheet.tsx, frontend/src/components/BottomSheet.module.css, frontend/src/components/PlanItemEditor.tsx, frontend/src/components/PlanItemEditor.module.css, frontend/src/components/PlanRow.tsx, frontend/src/components/PlanRow.module.css</files>
  <behavior>
- BottomSheet — открывается/закрывается, animates slide-up, backdrop tap = close, Esc = close, Telegram BackButton = close.
- PlanItemEditor — универсальная форма: category select, amount input, description textarea, day_of_period numeric (template mode) или planned_date input (planned mode); submit/cancel/delete buttons; client-side validation.
- PlanRow — отображает строку с amount + description + day badge; tap на amount → inline edit; tap elsewhere → onOpenEditor() callback; subscription_auto rendering — read-only с 🔁 badge.
- TS check passes; компоненты компилируются.
  </behavior>
  <action>
**1. BottomSheet.tsx** — следовать spec из 03-RESEARCH.md Pattern 4 + 03-UI-SPEC.md «Component: BottomSheet»:

```tsx
import { useEffect, type ReactNode } from 'react';
import styles from './BottomSheet.module.css';

export interface BottomSheetProps {
  open: boolean;
  onClose: () => void;
  title: string;
  children: ReactNode;
}

interface TgBackButton {
  show: () => void;
  hide: () => void;
  onClick: (cb: () => void) => void;
  offClick: (cb: () => void) => void;
}

declare global {
  interface Window {
    Telegram?: {
      WebApp?: {
        BackButton?: TgBackButton;
        // (existing MainButton + others from client.ts)
      };
    };
  }
}

/**
 * Reusable bottom sheet (sketch 002-B style).
 *
 * - CSS-only animation (transform: translateY).
 * - Tap on backdrop → close.
 * - Esc → close (browser dev fallback).
 * - Telegram BackButton (Mini App) → close.
 *
 * Used in Phase 3 for full-edit of template/planned items; reused in Phase 4
 * for add-actual-transaction.
 */
export function BottomSheet({ open, onClose, title, children }: BottomSheetProps) {
  useEffect(() => {
    if (!open) return;

    const onKey = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose();
    };
    window.addEventListener('keydown', onKey);

    const tgBackBtn = window.Telegram?.WebApp?.BackButton;
    if (tgBackBtn) {
      tgBackBtn.show();
      tgBackBtn.onClick(onClose);
    }

    return () => {
      window.removeEventListener('keydown', onKey);
      if (tgBackBtn) {
        tgBackBtn.offClick(onClose);
        tgBackBtn.hide();
      }
    };
  }, [open, onClose]);

  return (
    <>
      <div
        className={`${styles.backdrop} ${open ? styles.backdropOpen : ''}`}
        onClick={onClose}
        aria-hidden="true"
      />
      <div
        className={`${styles.sheet} ${open ? styles.sheetOpen : ''}`}
        role="dialog"
        aria-modal="true"
        aria-label={title}
      >
        <div className={styles.handle} />
        <div className={styles.head}>
          <h3 className={styles.title}>{title}</h3>
          <button
            type="button"
            onClick={onClose}
            className={styles.closeBtn}
            aria-label="Закрыть"
          >
            ×
          </button>
        </div>
        <div className={styles.body}>{children}</div>
      </div>
    </>
  );
}
```

**BottomSheet.module.css**:
```css
.backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.5);
  opacity: 0;
  pointer-events: none;
  transition: opacity 250ms ease-out;
  z-index: 100;
}
.backdropOpen {
  opacity: 1;
  pointer-events: auto;
}

.sheet {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  max-height: 85vh;
  overflow: auto;
  background: var(--color-surface);
  color: var(--color-text);
  border-radius: var(--radius-lg) var(--radius-lg) 0 0;
  transform: translateY(100%);
  transition: transform 250ms ease-out;
  z-index: 101;
  padding-bottom: var(--safe-bottom);
  box-shadow: var(--shadow-lg);
}
.sheetOpen {
  transform: translateY(0);
}

.handle {
  width: 40px;
  height: 4px;
  background: var(--color-border);
  border-radius: var(--radius-full);
  margin: 12px auto 0;
}

.head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-4) var(--space-5) var(--space-3);
}

.title {
  margin: 0;
  font-size: var(--text-md);
  font-weight: var(--weight-semibold);
}

.closeBtn {
  background: transparent;
  color: var(--color-text-muted);
  border: 0;
  font-size: 28px;
  line-height: 1;
  cursor: pointer;
  padding: 0 8px;
}
.closeBtn:hover { color: var(--color-text); }

.body {
  padding: 0 var(--space-5) var(--space-5);
}
```

**2. PlanItemEditor.tsx** — универсальная форма. Discriminated по `mode` prop:

```tsx
import { useMemo, useState } from 'react';
import type { CategoryRead } from '../api/types';
import styles from './PlanItemEditor.module.css';

export type EditorMode = 'create-template' | 'edit-template' | 'create-planned' | 'edit-planned';

export interface EditorInitial {
  category_id?: number;
  amount_cents?: number;
  description?: string | null;
  day_of_period?: number | null;
  planned_date?: string | null;
  sort_order?: number;
}

export interface EditorSavePayload {
  category_id: number;
  amount_cents: number;
  description: string | null;
  day_of_period?: number | null;     // template modes
  planned_date?: string | null;       // planned modes
  sort_order?: number;
}

export interface PlanItemEditorProps {
  mode: EditorMode;
  initial?: EditorInitial;
  categories: CategoryRead[];
  periodBounds?: { start: string; end: string };  // for date input min/max (planned modes)
  onSave: (data: EditorSavePayload) => Promise<void>;
  onDelete?: () => Promise<void>;
  onCancel: () => void;
}

function parseRublesToKopecks(input: string): number | null {
  const cleaned = input.replace(/\s/g, '').replace(',', '.');
  if (cleaned === '') return null;
  const f = parseFloat(cleaned);
  if (isNaN(f) || !isFinite(f)) return null;
  return Math.round(f * 100);
}

function formatKopecksToRubles(cents: number | undefined): string {
  if (cents === undefined || cents === null) return '';
  return (cents / 100).toLocaleString('ru-RU', { maximumFractionDigits: 2 });
}

export function PlanItemEditor({
  mode,
  initial,
  categories,
  periodBounds,
  onSave,
  onDelete,
  onCancel,
}: PlanItemEditorProps) {
  const isTemplate = mode === 'create-template' || mode === 'edit-template';
  const isEdit = mode === 'edit-template' || mode === 'edit-planned';

  const [categoryId, setCategoryId] = useState<number | ''>(initial?.category_id ?? '');
  const [amountStr, setAmountStr] = useState<string>(formatKopecksToRubles(initial?.amount_cents));
  const [description, setDescription] = useState<string>(initial?.description ?? '');
  const [dayOfPeriod, setDayOfPeriod] = useState<string>(
    initial?.day_of_period !== undefined && initial?.day_of_period !== null
      ? String(initial.day_of_period)
      : '',
  );
  const [plannedDate, setPlannedDate] = useState<string>(initial?.planned_date ?? '');
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const expenseCats = useMemo(
    () => categories.filter((c) => c.kind === 'expense' && !c.is_archived),
    [categories],
  );
  const incomeCats = useMemo(
    () => categories.filter((c) => c.kind === 'income' && !c.is_archived),
    [categories],
  );

  const amountCents = parseRublesToKopecks(amountStr);
  const canSubmit =
    categoryId !== '' && amountCents !== null && amountCents > 0 && !submitting;

  const handleSubmit = async () => {
    if (!canSubmit) return;
    setSubmitting(true);
    setError(null);
    try {
      const payload: EditorSavePayload = {
        category_id: Number(categoryId),
        amount_cents: amountCents!,
        description: description.trim() === '' ? null : description.trim(),
      };
      if (isTemplate) {
        payload.day_of_period = dayOfPeriod === '' ? null : Number(dayOfPeriod);
      } else {
        payload.planned_date = plannedDate === '' ? null : plannedDate;
      }
      await onSave(payload);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setSubmitting(false);
    }
  };

  const handleDelete = async () => {
    if (!onDelete || submitting) return;
    if (!window.confirm('Удалить строку?')) return;
    setSubmitting(true);
    setError(null);
    try {
      await onDelete();
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className={styles.form}>
      <label className={styles.field}>
        <span className={styles.label}>Категория</span>
        <select
          value={categoryId}
          onChange={(e) => setCategoryId(e.target.value === '' ? '' : Number(e.target.value))}
          className={styles.select}
          disabled={submitting}
        >
          <option value="">— выберите —</option>
          {expenseCats.length > 0 && (
            <optgroup label="Расходы">
              {expenseCats.map((c) => (
                <option key={c.id} value={c.id}>
                  {c.name}
                </option>
              ))}
            </optgroup>
          )}
          {incomeCats.length > 0 && (
            <optgroup label="Доходы">
              {incomeCats.map((c) => (
                <option key={c.id} value={c.id}>
                  {c.name}
                </option>
              ))}
            </optgroup>
          )}
        </select>
      </label>

      <label className={styles.field}>
        <span className={styles.label}>Сумма (₽)</span>
        <input
          type="text"
          inputMode="decimal"
          placeholder="1500"
          value={amountStr}
          onChange={(e) => setAmountStr(e.target.value)}
          disabled={submitting}
          className={styles.input}
        />
      </label>

      <label className={styles.field}>
        <span className={styles.label}>Описание</span>
        <textarea
          value={description}
          onChange={(e) => setDescription(e.target.value)}
          maxLength={500}
          rows={2}
          disabled={submitting}
          className={styles.textarea}
        />
      </label>

      {isTemplate ? (
        <label className={styles.field}>
          <span className={styles.label}>День периода (опц.)</span>
          <input
            type="number"
            min={1}
            max={31}
            placeholder="напр. 5"
            value={dayOfPeriod}
            onChange={(e) => setDayOfPeriod(e.target.value)}
            disabled={submitting}
            className={styles.input}
          />
          <span className={styles.helper}>1..31. Пусто — без привязки.</span>
        </label>
      ) : (
        <label className={styles.field}>
          <span className={styles.label}>Дата (опц.)</span>
          <input
            type="date"
            value={plannedDate}
            min={periodBounds?.start}
            max={periodBounds?.end}
            onChange={(e) => setPlannedDate(e.target.value)}
            disabled={submitting}
            className={styles.input}
          />
        </label>
      )}

      {error && <div className={styles.error}>Ошибка: {error}</div>}

      <div className={styles.actions}>
        {isEdit && onDelete && (
          <button
            type="button"
            onClick={handleDelete}
            disabled={submitting}
            className={styles.deleteBtn}
          >
            Удалить
          </button>
        )}
        <button
          type="button"
          onClick={onCancel}
          disabled={submitting}
          className={styles.cancelBtn}
        >
          Отмена
        </button>
        <button
          type="button"
          onClick={handleSubmit}
          disabled={!canSubmit}
          className={styles.saveBtn}
        >
          {submitting ? 'Сохранение…' : 'Сохранить'}
        </button>
      </div>
    </div>
  );
}
```

**PlanItemEditor.module.css**:
```css
.form { display: flex; flex-direction: column; gap: var(--space-4); }

.field { display: flex; flex-direction: column; gap: var(--space-1); }

.label { font-size: var(--text-sm); color: var(--color-text-muted); }

.select, .input, .textarea {
  background: var(--color-surface-2);
  color: var(--color-text);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 10px 12px;
  font-size: var(--text-base);
  font-family: var(--font-sans);
  width: 100%;
  box-sizing: border-box;
}
.textarea { resize: vertical; min-height: 56px; }
.input:focus, .select:focus, .textarea:focus {
  outline: 2px solid var(--color-primary);
  border-color: transparent;
}

.helper { font-size: var(--text-xs); color: var(--color-text-dim); margin-top: 2px; }

.error {
  color: var(--color-danger);
  background: var(--color-danger-soft);
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-sm);
  font-size: var(--text-sm);
}

.actions {
  display: flex;
  gap: var(--space-2);
  justify-content: flex-end;
  margin-top: var(--space-2);
}

.deleteBtn {
  background: transparent;
  color: var(--color-danger);
  border: 1px solid var(--color-danger);
  border-radius: var(--radius-md);
  padding: 10px 16px;
  font-weight: var(--weight-medium);
  cursor: pointer;
  margin-right: auto;
}
.deleteBtn:disabled { opacity: 0.5; cursor: not-allowed; }

.cancelBtn {
  background: transparent;
  color: var(--color-text-muted);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 10px 16px;
  cursor: pointer;
}

.saveBtn {
  background: var(--color-primary);
  color: #fff;
  border: 0;
  border-radius: var(--radius-md);
  padding: 10px 18px;
  font-weight: var(--weight-semibold);
  cursor: pointer;
}
.saveBtn:disabled { opacity: 0.5; cursor: not-allowed; }
```

**3. PlanRow.tsx** — общий для template/planned:

```tsx
import { useState } from 'react';
import type { CategoryRead, TemplateItemRead, PlannedRead } from '../api/types';
import styles from './PlanRow.module.css';

export type PlanRowItem =
  | { kind: 'template'; row: TemplateItemRead }
  | { kind: 'planned'; row: PlannedRead };

export interface PlanRowProps {
  item: PlanRowItem;
  category: CategoryRead | undefined;  // looked-up by parent; may be undefined if archived/missing
  onAmountSave: (newAmountCents: number) => Promise<void>;
  onOpenEditor: () => void;
}

function parseRublesToKopecks(input: string): number | null {
  const cleaned = input.replace(/\s/g, '').replace(',', '.');
  if (cleaned === '') return null;
  const f = parseFloat(cleaned);
  if (isNaN(f) || !isFinite(f) || f <= 0) return null;
  return Math.round(f * 100);
}

function formatRubles(cents: number): string {
  return (cents / 100).toLocaleString('ru-RU', { maximumFractionDigits: 2 });
}

export function PlanRow({ item, category, onAmountSave, onOpenEditor }: PlanRowProps) {
  const isSubAuto = item.kind === 'planned' && item.row.source === 'subscription_auto';
  const readOnly = isSubAuto;

  const [editing, setEditing] = useState(false);
  const [draft, setDraft] = useState<string>(formatRubles(item.row.amount_cents));
  const [saving, setSaving] = useState(false);

  const handleAmountTap = (e: React.MouseEvent) => {
    if (readOnly) return;
    e.stopPropagation();
    setDraft(formatRubles(item.row.amount_cents));
    setEditing(true);
  };

  const handleSave = async () => {
    const cents = parseRublesToKopecks(draft);
    if (cents === null) {
      setEditing(false);
      setDraft(formatRubles(item.row.amount_cents));
      return;
    }
    if (cents === item.row.amount_cents) {
      setEditing(false);
      return;
    }
    setSaving(true);
    try {
      await onAmountSave(cents);
      setEditing(false);
    } finally {
      setSaving(false);
    }
  };

  const handleCancel = () => {
    setEditing(false);
    setDraft(formatRubles(item.row.amount_cents));
  };

  // Render badges
  const dayBadge =
    item.kind === 'template' && item.row.day_of_period !== null
      ? `День ${item.row.day_of_period}`
      : item.kind === 'planned' && item.row.planned_date !== null
        ? new Date(item.row.planned_date).toLocaleDateString('ru-RU', {
            day: '2-digit',
            month: 'short',
          })
        : null;

  const cls = [styles.row, readOnly ? styles.readOnly : ''].filter(Boolean).join(' ');

  return (
    <div className={cls} onClick={readOnly ? undefined : onOpenEditor} role="button">
      <div className={styles.amountZone} onClick={handleAmountTap}>
        {editing ? (
          <span className={styles.editGroup} onClick={(e) => e.stopPropagation()}>
            <input
              type="text"
              inputMode="decimal"
              value={draft}
              onChange={(e) => setDraft(e.target.value)}
              onKeyDown={(e) => {
                if (e.key === 'Enter') {
                  e.preventDefault();
                  void handleSave();
                }
                if (e.key === 'Escape') {
                  e.preventDefault();
                  handleCancel();
                }
              }}
              autoFocus
              disabled={saving}
              className={styles.amountInput}
            />
            <button
              type="button"
              onClick={(e) => {
                e.stopPropagation();
                void handleSave();
              }}
              disabled={saving}
              className={styles.iconBtn}
              aria-label="Сохранить"
            >
              {saving ? '…' : '✓'}
            </button>
            <button
              type="button"
              onClick={(e) => {
                e.stopPropagation();
                handleCancel();
              }}
              disabled={saving}
              className={styles.iconBtn}
              aria-label="Отмена"
            >
              ×
            </button>
          </span>
        ) : (
          <span className={styles.amount}>{formatRubles(item.row.amount_cents)} ₽</span>
        )}
      </div>
      <div className={styles.metaZone}>
        <div className={styles.description}>
          {item.row.description || category?.name || 'Без описания'}
        </div>
        <div className={styles.badges}>
          {isSubAuto && <span className={styles.subBadge}>🔁 Подписка</span>}
          {dayBadge && <span className={styles.dayBadge}>{dayBadge}</span>}
        </div>
      </div>
    </div>
  );
}
```

**PlanRow.module.css**:
```css
.row {
  display: flex;
  flex-direction: column;
  gap: var(--space-1);
  padding: var(--space-3) var(--space-4);
  background: var(--color-surface);
  border-radius: var(--radius-md);
  margin-bottom: var(--space-2);
  cursor: pointer;
}
.row:hover { background: var(--color-surface-2); }

.readOnly { opacity: 0.7; cursor: default; }
.readOnly:hover { background: var(--color-surface); }

.amountZone {
  display: flex;
  align-items: center;
  justify-content: flex-end;
}

.amount {
  font-family: var(--font-mono);
  font-size: var(--text-md);
  font-weight: var(--weight-semibold);
  color: var(--color-text);
}

.editGroup {
  display: flex;
  align-items: center;
  gap: var(--space-1);
}

.amountInput {
  background: var(--color-surface-2);
  color: var(--color-text);
  border: 1px solid var(--color-primary);
  border-radius: var(--radius-sm);
  padding: 4px 8px;
  width: 100px;
  text-align: right;
  font-family: var(--font-mono);
  font-size: var(--text-base);
}

.iconBtn {
  background: transparent;
  color: var(--color-text-muted);
  border: 0;
  font-size: 18px;
  cursor: pointer;
  padding: 4px 6px;
}
.iconBtn:hover { color: var(--color-text); }

.metaZone {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-2);
}

.description {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
}

.badges { display: flex; gap: var(--space-2); flex-shrink: 0; }

.subBadge {
  background: var(--color-primary-soft);
  color: var(--color-primary);
  border-radius: var(--radius-full);
  padding: 2px 8px;
  font-size: var(--text-xs);
}

.dayBadge {
  background: var(--color-surface-2);
  color: var(--color-text-dim);
  border-radius: var(--radius-full);
  padding: 2px 8px;
  font-size: var(--text-xs);
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -30 && echo "EXIT $?"</automated>
  </verify>
  <done>
- BottomSheet, PlanItemEditor, PlanRow .tsx + .module.css созданы.
- TS check passes (exit=0).
- BottomSheet корректно подписывается/отписывается на Telegram BackButton + Esc.
- PlanItemEditor поддерживает 4 mode (create/edit × template/planned), показывает delete только в edit mode.
- PlanRow корректно рендерит read-only branch для subscription_auto, badge «🔁 Подписка», inline edit amount с Enter/Esc.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TemplateScreen + integrate into App.tsx + HomeScreen</name>
  <files>frontend/src/screens/TemplateScreen.tsx, frontend/src/screens/TemplateScreen.module.css, frontend/src/screens/HomeScreen.tsx, frontend/src/App.tsx</files>
  <behavior>
- TemplateScreen рендерит группировку Расходы/Доходы → категории → строки шаблона; поддерживает create/update/delete через PlanItemEditor в BottomSheet; inline-edit amount.
- HomeScreen получает 4-ую кнопку «Шаблон» (или 4 кнопки в строку/wrap).
- App.tsx Screen union расширяется на `'template'`.
- Vite build проходит, TS check проходит.
  </behavior>
  <action>
**1. TemplateScreen.tsx** (примерная структура — детали можно варьировать в рамках UI-SPEC §Screen 1):

```tsx
import { useMemo, useState } from 'react';
import {
  createTemplateItem,
  deleteTemplateItem,
  updateTemplateItem,
} from '../api/templates';
import { useTemplate } from '../hooks/useTemplate';
import { useCategories } from '../hooks/useCategories';
import type { CategoryRead, CategoryKind, TemplateItemRead } from '../api/types';
import { PlanRow } from '../components/PlanRow';
import { BottomSheet } from '../components/BottomSheet';
import { PlanItemEditor, type EditorSavePayload } from '../components/PlanItemEditor';
import styles from './TemplateScreen.module.css';

export interface TemplateScreenProps {
  onBack: () => void;
}

interface SheetState {
  open: boolean;
  mode: 'create-template' | 'edit-template';
  item?: TemplateItemRead;
  presetCategoryId?: number;
}

interface CategoryGroup {
  kindTitle: 'Расходы' | 'Доходы';
  kind: CategoryKind;
  categories: { category: CategoryRead; items: TemplateItemRead[] }[];
}

export function TemplateScreen({ onBack }: TemplateScreenProps) {
  const { items, loading: tplLoading, error: tplError, refetch } = useTemplate();
  const { categories, loading: catLoading, error: catError } = useCategories(false);
  const [sheet, setSheet] = useState<SheetState>({ open: false, mode: 'create-template' });
  const [mutationError, setMutationError] = useState<string | null>(null);

  const groups = useMemo<CategoryGroup[]>(() => {
    const catById = new Map(categories.map((c) => [c.id, c]));
    const byKind: Record<CategoryKind, CategoryGroup> = {
      expense: { kindTitle: 'Расходы', kind: 'expense', categories: [] },
      income: { kindTitle: 'Доходы', kind: 'income', categories: [] },
    };
    // Group items by category_id
    const itemsByCat = new Map<number, TemplateItemRead[]>();
    for (const it of items) {
      const arr = itemsByCat.get(it.category_id) ?? [];
      arr.push(it);
      itemsByCat.set(it.category_id, arr);
    }
    for (const [catId, catItems] of itemsByCat.entries()) {
      const cat = catById.get(catId);
      if (!cat) continue;
      catItems.sort((a, b) => a.sort_order - b.sort_order || a.id - b.id);
      byKind[cat.kind].categories.push({ category: cat, items: catItems });
    }
    // Sort categories within each kind by (sort_order, name)
    for (const k of ['expense', 'income'] as CategoryKind[]) {
      byKind[k].categories.sort(
        (a, b) =>
          a.category.sort_order - b.category.sort_order ||
          a.category.name.localeCompare(b.category.name, 'ru'),
      );
    }
    return [byKind.expense, byKind.income].filter((g) => g.categories.length > 0);
  }, [items, categories]);

  const wrap = async (fn: () => Promise<unknown>) => {
    setMutationError(null);
    try {
      await fn();
      await refetch();
    } catch (e) {
      setMutationError(e instanceof Error ? e.message : String(e));
    }
  };

  const handleAmountSave = async (id: number, newAmountCents: number) => {
    await wrap(() => updateTemplateItem(id, { amount_cents: newAmountCents }));
  };

  const handleSave = async (data: EditorSavePayload) => {
    if (sheet.mode === 'create-template') {
      await createTemplateItem({
        category_id: data.category_id,
        amount_cents: data.amount_cents,
        description: data.description,
        day_of_period: data.day_of_period ?? null,
        sort_order: data.sort_order ?? 0,
      });
    } else if (sheet.item) {
      await updateTemplateItem(sheet.item.id, {
        category_id: data.category_id,
        amount_cents: data.amount_cents,
        description: data.description,
        day_of_period: data.day_of_period ?? null,
      });
    }
    setSheet({ open: false, mode: 'create-template' });
    await refetch();
  };

  const handleDelete = async () => {
    if (!sheet.item) return;
    await deleteTemplateItem(sheet.item.id);
    setSheet({ open: false, mode: 'create-template' });
    await refetch();
  };

  const openCreate = (categoryId?: number) =>
    setSheet({
      open: true,
      mode: 'create-template',
      item: undefined,
      presetCategoryId: categoryId,
    });
  const openEdit = (item: TemplateItemRead) =>
    setSheet({ open: true, mode: 'edit-template', item });

  const loading = tplLoading || catLoading;
  const loadError = tplError ?? catError;

  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <button
          type="button"
          onClick={onBack}
          className={styles.backBtn}
          aria-label="Назад"
        >
          ←
        </button>
        <div className={styles.title}>Шаблон плана</div>
        <button
          type="button"
          onClick={() => openCreate(undefined)}
          className={styles.addBtn}
        >
          + Строка
        </button>
      </header>

      {loading && <div className={styles.muted}>Загрузка…</div>}
      {loadError && <div className={styles.error}>Ошибка: {loadError}</div>}
      {mutationError && <div className={styles.error}>Ошибка: {mutationError}</div>}

      {!loading && !loadError && groups.length === 0 && items.length === 0 && (
        <div className={styles.empty}>
          Шаблон пуст. Добавьте первую строку.
        </div>
      )}

      {!loading && !loadError && categories.length === 0 && (
        <div className={styles.empty}>
          Сначала создайте категории в разделе «Категории».
        </div>
      )}

      {groups.map((g) => (
        <section key={g.kind} className={styles.kindGroup}>
          <h3 className={styles.kindTitle}>{g.kindTitle}</h3>
          {g.categories.map(({ category, items: catItems }) => (
            <div key={category.id} className={styles.categoryGroup}>
              <h4 className={styles.categoryTitle}>{category.name}</h4>
              {catItems.map((item) => (
                <PlanRow
                  key={item.id}
                  item={{ kind: 'template', row: item }}
                  category={category}
                  onAmountSave={(cents) => handleAmountSave(item.id, cents)}
                  onOpenEditor={() => openEdit(item)}
                />
              ))}
              <button
                type="button"
                onClick={() => openCreate(category.id)}
                className={styles.addInGroup}
              >
                + Добавить строку в {category.name}
              </button>
            </div>
          ))}
        </section>
      ))}

      <BottomSheet
        open={sheet.open}
        onClose={() => setSheet({ open: false, mode: 'create-template' })}
        title={sheet.mode === 'edit-template' ? 'Изменить строку шаблона' : 'Новая строка шаблона'}
      >
        <PlanItemEditor
          mode={sheet.mode}
          initial={
            sheet.item
              ? {
                  category_id: sheet.item.category_id,
                  amount_cents: sheet.item.amount_cents,
                  description: sheet.item.description,
                  day_of_period: sheet.item.day_of_period,
                  sort_order: sheet.item.sort_order,
                }
              : sheet.presetCategoryId
                ? { category_id: sheet.presetCategoryId }
                : undefined
          }
          categories={categories}
          onSave={handleSave}
          onDelete={sheet.mode === 'edit-template' ? handleDelete : undefined}
          onCancel={() => setSheet({ open: false, mode: 'create-template' })}
        />
      </BottomSheet>
    </div>
  );
}
```

**TemplateScreen.module.css**:
```css
.root {
  min-height: 100vh;
  padding: var(--space-4) var(--space-4) var(--space-12);
  background: var(--color-bg);
  color: var(--color-text);
}

.header {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}

.backBtn, .addBtn {
  background: transparent;
  color: var(--color-primary);
  border: 0;
  font-size: var(--text-md);
  cursor: pointer;
  padding: 4px 8px;
}

.title {
  font-size: var(--text-lg);
  font-weight: var(--weight-semibold);
  flex: 1;
  text-align: center;
}

.muted { color: var(--color-text-muted); padding: var(--space-3); }
.error {
  color: var(--color-danger);
  background: var(--color-danger-soft);
  padding: var(--space-3);
  border-radius: var(--radius-md);
  margin-bottom: var(--space-3);
}
.empty {
  color: var(--color-text-dim);
  text-align: center;
  padding: var(--space-8);
}

.kindGroup { margin-bottom: var(--space-6); }
.kindTitle {
  font-size: var(--text-sm);
  text-transform: uppercase;
  color: var(--color-text-muted);
  margin: var(--space-3) 0 var(--space-2);
  letter-spacing: 0.04em;
}

.categoryGroup { margin-bottom: var(--space-4); }
.categoryTitle {
  font-size: var(--text-base);
  font-weight: var(--weight-medium);
  color: var(--color-text);
  margin: var(--space-2) 0 var(--space-2);
}

.addInGroup {
  background: transparent;
  color: var(--color-primary);
  border: 1px dashed var(--color-border);
  border-radius: var(--radius-md);
  padding: 8px 12px;
  font-size: var(--text-sm);
  cursor: pointer;
  width: 100%;
  text-align: left;
}
.addInGroup:hover { background: var(--color-primary-soft); }
```

**2. Обновить frontend/src/screens/HomeScreen.tsx**:
```tsx
// Изменить тип и добавить 2 кнопки. Добавить 'template' и 'planned' в onNavigate union (даже если planned ещё не создан в этом плане — Plan 03-05 создаст; но для типа можно оставить только 'template' пока, и расширить в Plan 03-05).
//
// Решение: добавляем сразу обе ('template' | 'planned'), потому что HomeScreen — общий контракт, а App.tsx ниже добавит обработку.
```

Минимальный diff:
```tsx
import styles from './HomeScreen.module.css';

export interface HomeScreenProps {
  onNavigate: (screen: 'categories' | 'template' | 'planned' | 'settings') => void;
}

export function HomeScreen({ onNavigate }: HomeScreenProps) {
  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <div className={styles.title}>TG Budget</div>
      </header>
      <div className={styles.placeholder}>
        Дашборд будет в Phase 5.
        <br />
        Сейчас доступны категории, шаблон, план и настройки.
      </div>
      <div className={styles.nav}>
        <button type="button" className={styles.navBtn} onClick={() => onNavigate('categories')}>
          Категории
        </button>
        <button type="button" className={styles.navBtn} onClick={() => onNavigate('template')}>
          Шаблон
        </button>
        <button type="button" className={styles.navBtn} onClick={() => onNavigate('planned')}>
          План
        </button>
        <button type="button" className={styles.navBtn} onClick={() => onNavigate('settings')}>
          Настройки
        </button>
      </div>
    </div>
  );
}
```

(Возможно, придётся обновить HomeScreen.module.css для wrap-grid 4 кнопок. Минимально: `flex-wrap: wrap; gap: 8px;` на `.nav`.)

**3. Обновить frontend/src/App.tsx**:
```tsx
import { useState } from 'react';
import { useUser } from './hooks/useUser';
import { OnboardingScreen } from './screens/OnboardingScreen';
import { HomeScreen } from './screens/HomeScreen';
import { CategoriesScreen } from './screens/CategoriesScreen';
import { TemplateScreen } from './screens/TemplateScreen';
import { SettingsScreen } from './screens/SettingsScreen';
import styles from './App.module.css';

type Screen = 'onboarding' | 'home' | 'categories' | 'template' | 'planned' | 'settings';

export default function App() {
  const { user, loading, error, refetch } = useUser();
  const [overrideScreen, setOverrideScreen] = useState<Screen | null>(null);

  if (loading && !user) {
    return <div className={styles.loadingRoot}>Загрузка…</div>;
  }
  if (error || !user) {
    return (
      <div className={styles.errorRoot}>
        Не удалось загрузить пользователя.
        <br />
        {error && <code>{error}</code>}
      </div>
    );
  }

  const isOnboarded = user.onboarded_at !== null;
  const screen: Screen = overrideScreen ?? (isOnboarded ? 'home' : 'onboarding');

  if (screen === 'onboarding') {
    return (
      <OnboardingScreen
        user={user}
        onRefreshUser={refetch}
        onComplete={() => {
          setOverrideScreen('home');
          void refetch();
        }}
      />
    );
  }
  if (screen === 'home') {
    return <HomeScreen onNavigate={(s) => setOverrideScreen(s)} />;
  }
  if (screen === 'categories') {
    return <CategoriesScreen onBack={() => setOverrideScreen('home')} />;
  }
  if (screen === 'template') {
    return <TemplateScreen onBack={() => setOverrideScreen('home')} />;
  }
  if (screen === 'planned') {
    // Phase 3-05 will provide PlannedScreen; placeholder for now to avoid TS error
    return (
      <div style={{ padding: 16 }}>
        План — будет реализован в Plan 03-05.
        <br />
        <button onClick={() => setOverrideScreen('home')}>← Назад</button>
      </div>
    );
  }
  return <SettingsScreen onBack={() => setOverrideScreen('home')} />;
}
```

**Важно:** `'planned'` экран ещё не существует — оставляем placeholder, чтобы TypeScript не падал. Plan 03-05 заменит на реальный PlannedScreen. Это допустимый промежуточный stub в multi-plan фазе.

После всех изменений проверить:
```bash
cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit && npm run build
```

Если build проходит — задача завершена.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | tail -10 && echo "TS-EXIT $?" && npm run build 2>&1 | tail -10 && echo "BUILD-EXIT $?"</automated>
  </verify>
  <done>
- frontend/src/screens/TemplateScreen.tsx + .module.css созданы.
- HomeScreen.tsx обновлён: добавлены 2 кнопки (Шаблон, План) + onNavigate union расширен.
- App.tsx обновлён: Screen union расширен; routes для 'template' (full) и 'planned' (placeholder).
- npx tsc --noEmit → exit=0.
- npm run build → exit=0 (Vite build OK, бандл создан).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App ↔ API | apiFetch + initData header (Phase 2 carryover) |
| User input → amount/day_of_period | Frontend validation + server-side Pydantic 422 |
| BottomSheet open state ↔ Telegram BackButton | useEffect lifecycle subscribe/unsubscribe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | Frontend amount input принимает невалидное значение (NaN, negative) | mitigate | parseRublesToKopecks возвращает null; canSubmit blocks submit; backend Pydantic gt=0 finalizes |
| T-03-19 | XSS | description с `<script>...</script>` | mitigate | React escape по умолчанию; не используем dangerouslySetInnerHTML |
| T-03-20 | UI lifecycle | BottomSheet остаётся открытым после navigate | mitigate | useEffect cleanup в BottomSheet hides BackButton; setSheet(open: false) на screen change (App.tsx тоже сбрасывает overrideScreen) |
| T-03-21 | UX | Пользователь случайно удалил template-item | accept | window.confirm в PlanItemEditor.handleDelete |
| T-03-22 | Tampering | User editing с другим browser tab → stale state | accept | Single-tenant; refetch после mutation минимизирует |
</threat_model>

<verification>
- TS check exit=0.
- Vite build exit=0.
- Manual UI verification — отдельный checkpoint в Plan 03-06.
</verification>

<success_criteria>
- 11 файлов созданы / модифицированы.
- TemplateScreen полностью функционирует против реального backend (Plan 03-03).
- BottomSheet, PlanItemEditor, PlanRow готовы к переиспользованию в Plan 03-05 (PlannedScreen).
- App.tsx и HomeScreen обновлены с новыми экранами.
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-template-and-planned-transactions/03-04-SUMMARY.md`.
</output>
