---
phase: 02-domain-foundation-and-onboarding
plan: 04
type: execute
wave: 3
depends_on: [02, 03]
files_modified:
  - app/api/routes/__init__.py
  - app/api/routes/categories.py
  - app/api/routes/periods.py
  - app/api/routes/onboarding.py
  - app/api/routes/settings.py
  - app/api/routes/internal_telegram.py
  - app/api/router.py
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - PER-01
  - PER-02
  - PER-03
  - PER-05
  - ONB-01
  - ONB-03
  - SET-01

must_haves:
  truths:
    - "5 новых под-роутеров зарегистрированы в app/api/router.py"
    - "GET/POST/PATCH/DELETE /api/v1/categories — soft archive, include_archived flag"
    - "GET /api/v1/periods/current — 200 + период / 404 если нет активного"
    - "POST /api/v1/onboarding/complete — 200 / 409 (повтор) / 422 (cycle_start_day OOR)"
    - "GET/PATCH /api/v1/settings — cycle_start_day"
    - "POST /api/v1/internal/telegram/chat-bind — 200 (с X-Internal-Token) / 403 (без)"
    - "Все новые public routes под Depends(get_current_user); internal — под verify_internal_token"
    - "test_categories.py, test_periods.py, test_onboarding.py, test_settings.py, test_telegram_chat_bind.py становятся GREEN"
  artifacts:
    - path: "app/api/routes/categories.py"
      provides: "categories_router (GET/POST/PATCH/DELETE /categories)"
      exports: ["categories_router"]
    - path: "app/api/routes/periods.py"
      provides: "periods_router (GET /periods/current)"
      exports: ["periods_router"]
    - path: "app/api/routes/onboarding.py"
      provides: "onboarding_router (POST /onboarding/complete)"
      exports: ["onboarding_router"]
    - path: "app/api/routes/settings.py"
      provides: "settings_router (GET, PATCH /settings)"
      exports: ["settings_router"]
    - path: "app/api/routes/internal_telegram.py"
      provides: "internal_telegram_router (POST /internal/telegram/chat-bind)"
      exports: ["internal_telegram_router"]
  key_links:
    - from: "app/api/router.py"
      to: "app/api/routes/{categories,periods,onboarding,settings,internal_telegram}"
      via: "include_router"
      pattern: "include_router"
    - from: "app/api/routes/*.py"
      to: "app/services/*"
      via: "service function calls"
      pattern: "from app.services"
    - from: "app/api/routes/onboarding.py"
      to: "AlreadyOnboardedError → 409"
      via: "exception handler"
      pattern: "AlreadyOnboardedError"
---

<objective>
Wave 3: тонкие FastAPI-обёртки над сервисами Plan 02-03. Каждый под-роутер живёт в своём файле в `app/api/routes/`. Главный `app/api/router.py` (модифицируется минимально) включает их через `include_router`. После этого плана все integration-тесты Wave 0 (test_categories, test_periods, test_onboarding, test_settings, test_telegram_chat_bind) — GREEN при наличии тестовой БД.

Покрывает: CAT-01..03, PER-01..02, PER-05 (структурно), ONB-01, ONB-03 (backend часть), SET-01.
ONB-02 — frontend; ONB-03 bot-часть — Plan 02-05.

Output: 5 route-файлов + регистрация в главном router.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md
@app/api/router.py
@app/api/dependencies.py
@app/api/schemas/categories.py
@app/api/schemas/periods.py
@app/api/schemas/onboarding.py
@app/api/schemas/settings.py
@app/api/schemas/telegram.py
@app/services/categories.py
@app/services/periods.py
@app/services/onboarding.py
@app/services/settings.py
@app/services/telegram.py

<interfaces>
<!-- Существующие зависимости (Phase 1) — переиспользуем без изменений -->
from app.api.dependencies import get_current_user, verify_internal_token, get_db

<!-- Существующий router (Phase 1) — добавляем include_router вызовы -->
# app/api/router.py:
public_router = APIRouter()  # уже определён + GET /me
internal_router = APIRouter(prefix="/internal", dependencies=[Depends(verify_internal_token)])  # уже определён + GET /internal/health

<!-- Сервисы (Plan 02-03) -->
from app.services import categories as cat_svc
from app.services import periods as period_svc
from app.services import settings as settings_svc
from app.services import telegram as telegram_svc
from app.services import onboarding as onboarding_svc
from app.services.onboarding import AlreadyOnboardedError

<!-- Схемы (Plan 02-02) -->
from app.api.schemas.categories import CategoryCreate, CategoryUpdate, CategoryRead
from app.api.schemas.periods import PeriodRead
from app.api.schemas.onboarding import OnboardingCompleteRequest, OnboardingCompleteResponse
from app.api.schemas.settings import SettingsRead, SettingsUpdate
from app.api.schemas.telegram import ChatBindRequest

<!-- Контракты HTTP (создаются этим планом) -->

POST /api/v1/categories                  body: CategoryCreate         → 200 + CategoryRead
GET  /api/v1/categories?include_archived  → 200 + list[CategoryRead]
PATCH /api/v1/categories/{id}            body: CategoryUpdate         → 200 + CategoryRead
DELETE /api/v1/categories/{id}           → 200 + CategoryRead (with is_archived=true)
GET  /api/v1/periods/current              → 200 + PeriodRead | 404
POST /api/v1/onboarding/complete         body: OnboardingCompleteRequest → 200 + OnboardingCompleteResponse | 409 (AlreadyOnboardedError)
GET  /api/v1/settings                    → 200 + SettingsRead
PATCH /api/v1/settings                   body: SettingsUpdate          → 200 + SettingsRead
POST /api/v1/internal/telegram/chat-bind body: ChatBindRequest + X-Internal-Token → 200 / 403
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать 4 public route-модуля (categories, periods, onboarding, settings) и зарегистрировать в router.py</name>
  <read_first>
    - app/api/router.py (текущий public_router structure)
    - app/services/categories.py (signatures)
    - app/services/onboarding.py (AlreadyOnboardedError)
    - tests/test_categories.py, tests/test_periods.py, tests/test_onboarding.py, tests/test_settings.py (HTTP-контракт)
  </read_first>
  <files>app/api/routes/__init__.py, app/api/routes/categories.py, app/api/routes/periods.py, app/api/routes/onboarding.py, app/api/routes/settings.py, app/api/router.py</files>
  <behavior>
    - app/api/routes/__init__.py: пустой docstring
    - 4 public route-модуля, каждый экспортирует APIRouter
    - app/api/router.py: добавляет 4 include_router(prefix=...) к public_router (это уже существующий APIRouter, к которому добавим sub-routers)
    - Все routes под `Depends(get_current_user)` через router-level dependency
    - onboarding.py обрабатывает AlreadyOnboardedError → 409
    - PATCH/DELETE категорий валидируют существование (через get_or_404 в сервисе)
    - GET /periods/current → 404 если нет активного периода
  </behavior>
  <action>
**1. Создать `app/api/routes/__init__.py`:**

```python
"""Per-domain route modules. Each module exports an APIRouter for include_router."""
```

**2. Создать `app/api/routes/categories.py`:**

```python
"""Categories REST routes — CAT-01, CAT-02 (soft archive)."""
from typing import Annotated

from fastapi import APIRouter, Depends, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db
from app.api.schemas.categories import CategoryCreate, CategoryRead, CategoryUpdate
from app.services import categories as cat_svc


categories_router = APIRouter(
    prefix="/categories",
    tags=["categories"],
    dependencies=[Depends(get_current_user)],
)


@categories_router.get("", response_model=list[CategoryRead])
async def list_categories(
    db: Annotated[AsyncSession, Depends(get_db)],
    include_archived: bool = False,
) -> list[CategoryRead]:
    """GET /api/v1/categories?include_archived=<bool>

    Default: returns only active categories (CAT-02). Pass `include_archived=true`
    to include archived rows (for the archive-management UI).
    """
    cats = await cat_svc.list_categories(db, include_archived=include_archived)
    return [CategoryRead.model_validate(c) for c in cats]


@categories_router.post("", response_model=CategoryRead, status_code=status.HTTP_200_OK)
async def create_category(
    body: CategoryCreate,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> CategoryRead:
    """POST /api/v1/categories — create a new category (CAT-01)."""
    cat = await cat_svc.create_category(
        db,
        name=body.name,
        kind=body.kind,
        sort_order=body.sort_order,
    )
    return CategoryRead.model_validate(cat)


@categories_router.patch("/{category_id}", response_model=CategoryRead)
async def update_category(
    category_id: int,
    body: CategoryUpdate,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> CategoryRead:
    """PATCH /api/v1/categories/{id} — partial update including is_archived toggle."""
    cat = await cat_svc.update_category(db, category_id, body)
    return CategoryRead.model_validate(cat)


@categories_router.delete("/{category_id}", response_model=CategoryRead)
async def archive_category(
    category_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> CategoryRead:
    """DELETE /api/v1/categories/{id} — soft-archive (D-14, CAT-02).

    Sets is_archived=true rather than physically deleting; historical
    transactions referencing this category remain intact. Use PATCH with
    is_archived=false to unarchive.
    """
    cat = await cat_svc.archive_category(db, category_id)
    return CategoryRead.model_validate(cat)
```

**3. Создать `app/api/routes/periods.py`:**

```python
"""Periods REST routes — PER-01, PER-02 (read-only in Phase 2)."""
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db
from app.api.schemas.periods import PeriodRead
from app.services import periods as period_svc


periods_router = APIRouter(
    prefix="/periods",
    tags=["periods"],
    dependencies=[Depends(get_current_user)],
)


@periods_router.get("/current", response_model=PeriodRead)
async def get_current_period(
    db: Annotated[AsyncSession, Depends(get_db)],
) -> PeriodRead:
    """GET /api/v1/periods/current — returns the active budget period.

    Returns 404 if no active period exists (e.g., before onboarding).
    Phase 2 does NOT lazy-create a period here; that's Phase 5 worker job.
    """
    period = await period_svc.get_current_active_period(db)
    if period is None:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="No active budget period — complete onboarding first",
        )
    return PeriodRead.model_validate(period)
```

**4. Создать `app/api/routes/onboarding.py`:**

```python
"""Onboarding REST route — ONB-01, PER-02, CAT-03."""
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db
from app.api.schemas.onboarding import (
    OnboardingCompleteRequest,
    OnboardingCompleteResponse,
)
from app.services import onboarding as onb_svc
from app.services.onboarding import AlreadyOnboardedError


onboarding_router = APIRouter(
    prefix="/onboarding",
    tags=["onboarding"],
    dependencies=[Depends(get_current_user)],
)


@onboarding_router.post(
    "/complete",
    response_model=OnboardingCompleteResponse,
    status_code=status.HTTP_200_OK,
    responses={
        409: {"description": "Already onboarded"},
        422: {"description": "Validation error (e.g. cycle_start_day out of [1,28])"},
    },
)
async def complete_onboarding(
    body: OnboardingCompleteRequest,
    current_user: Annotated[dict, Depends(get_current_user)],
    db: Annotated[AsyncSession, Depends(get_db)],
) -> OnboardingCompleteResponse:
    """POST /api/v1/onboarding/complete — atomic seed + first period + flag user.

    Returns 409 if user already onboarded (D-10).
    Returns 422 if cycle_start_day not in [1, 28] (Pydantic Field).
    """
    try:
        result = await onb_svc.complete_onboarding(
            db,
            tg_user_id=current_user["id"],
            starting_balance_cents=body.starting_balance_cents,
            cycle_start_day=body.cycle_start_day,
            seed_default_categories=body.seed_default_categories,
        )
    except AlreadyOnboardedError as exc:
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail=str(exc),
        ) from exc
    except ValueError as exc:
        # AppUser not bootstrapped yet — must call /me first
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=str(exc),
        ) from exc

    return OnboardingCompleteResponse(**result)
```

**5. Создать `app/api/routes/settings.py`:**

```python
"""Settings REST routes — SET-01 (cycle_start_day)."""
from typing import Annotated

from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db
from app.api.schemas.settings import SettingsRead, SettingsUpdate
from app.services import settings as settings_svc


settings_router = APIRouter(
    prefix="/settings",
    tags=["settings"],
    dependencies=[Depends(get_current_user)],
)


@settings_router.get("", response_model=SettingsRead)
async def get_settings(
    current_user: Annotated[dict, Depends(get_current_user)],
    db: Annotated[AsyncSession, Depends(get_db)],
) -> SettingsRead:
    """GET /api/v1/settings — returns current user-level settings."""
    cycle = await settings_svc.get_cycle_start_day(db, current_user["id"])
    return SettingsRead(cycle_start_day=cycle)


@settings_router.patch("", response_model=SettingsRead)
async def update_settings(
    body: SettingsUpdate,
    current_user: Annotated[dict, Depends(get_current_user)],
    db: Annotated[AsyncSession, Depends(get_db)],
) -> SettingsRead:
    """PATCH /api/v1/settings — update cycle_start_day.

    Per SET-01: existing budget periods are NOT recomputed. Only future
    periods (Phase 5 worker job) will use the new value.
    """
    new_value = await settings_svc.update_cycle_start_day(
        db,
        tg_user_id=current_user["id"],
        cycle_start_day=body.cycle_start_day,
    )
    return SettingsRead(cycle_start_day=new_value)
```

**6. Обновить `app/api/router.py`:**

Добавить импорты и include_router вызовы. Текущий public_router (с GET /me) сохраняется; к нему добавляются 4 sub-routers через `public_router.include_router(...)`. Структура файла после правки:

```python
"""API routers for TG Budget Planner.

Phase 1 routes:
- ``/me`` (public_router) — returns current user info; protected by
  ``get_current_user`` (Telegram initData + OWNER_TG_ID whitelist).
- ``/internal/health`` (internal_router) — internal service-to-service health
  probe; protected by ``verify_internal_token`` (X-Internal-Token).

Phase 2 routes (added via include_router):
- ``/categories`` — CAT-01, CAT-02
- ``/periods/current`` — PER-01, PER-02
- ``/onboarding/complete`` — ONB-01, PER-02, CAT-03
- ``/settings`` — SET-01

D-11: ``app_user`` row is upserted on the first valid ``GET /me`` request.
"""
from typing import Annotated

from fastapi import APIRouter, Depends
from pydantic import BaseModel
from sqlalchemy import select
from sqlalchemy.dialects.postgresql import insert
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db, verify_internal_token
from app.api.routes.categories import categories_router
from app.api.routes.onboarding import onboarding_router
from app.api.routes.periods import periods_router
from app.api.routes.settings import settings_router
from app.db.models import AppUser


# ---- Public router (requires initData auth) ----
public_router = APIRouter()


class MeResponse(BaseModel):
    tg_user_id: int
    tg_chat_id: int | None
    cycle_start_day: int
    onboarded_at: str | None
    chat_id_known: bool


@public_router.get("/me", response_model=MeResponse)
async def get_me(
    current_user: Annotated[dict, Depends(get_current_user)],
    db: Annotated[AsyncSession, Depends(get_db)],
) -> MeResponse:
    """Return current user info; upsert ``app_user`` on first request (D-11)."""
    tg_user_id = current_user["id"]

    stmt = (
        insert(AppUser)
        .values(tg_user_id=tg_user_id)
        .on_conflict_do_nothing(index_elements=["tg_user_id"])
    )
    await db.execute(stmt)

    result = await db.execute(
        select(AppUser).where(AppUser.tg_user_id == tg_user_id)
    )
    user = result.scalar_one()

    return MeResponse(
        tg_user_id=user.tg_user_id,
        tg_chat_id=user.tg_chat_id,
        cycle_start_day=user.cycle_start_day,
        onboarded_at=user.onboarded_at.isoformat() if user.onboarded_at else None,
        chat_id_known=user.tg_chat_id is not None,
    )


# Register Phase 2 sub-routers under the same /api/v1 prefix
public_router.include_router(categories_router)
public_router.include_router(periods_router)
public_router.include_router(onboarding_router)
public_router.include_router(settings_router)


# ---- Internal router (requires X-Internal-Token) ----
internal_router = APIRouter(
    prefix="/internal",
    dependencies=[Depends(verify_internal_token)],
)


@internal_router.get("/health")
async def internal_health() -> dict:
    """Health check for internal service-to-service communication (bot↔api)."""
    return {"status": "ok", "service": "api-internal"}


# (internal_telegram_router is added in the same file by Task 2 below)
```

**Validation steps:**

```bash
# Schema-сборка FastAPI app не падает
uv run python -c "
from app.main_api import app
routes = [r.path for r in app.routes if hasattr(r, 'path')]
expected = ['/api/v1/categories', '/api/v1/categories/{category_id}', '/api/v1/periods/current', '/api/v1/onboarding/complete', '/api/v1/settings', '/api/v1/me', '/api/v1/internal/health', '/healthz']
for e in expected:
    assert e in routes, f'missing route: {e} (have: {routes})'
print('all expected routes registered:', expected)
"

# Если есть тестовая БД — запустить integration-тесты
DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test uv run pytest tests/test_categories.py tests/test_periods.py tests/test_onboarding.py tests/test_settings.py -v
```

  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && for f in app/api/routes/__init__.py app/api/routes/categories.py app/api/routes/periods.py app/api/routes/onboarding.py app/api/routes/settings.py app/api/router.py; do python3 -c "import ast; ast.parse(open('$f').read()); print('$f OK')"; done && cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.main_api import app; routes={r.path for r in app.routes if hasattr(r,'path')}; assert '/api/v1/categories' in routes and '/api/v1/onboarding/complete' in routes and '/api/v1/settings' in routes and '/api/v1/periods/current' in routes; print('routes registered OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 5 файлов в app/api/routes/ + обновлённый app/api/router.py — синтаксически корректны
    - app/api/router.py содержит 4 include_router вызова: `grep -c "include_router(categories\\|include_router(periods\\|include_router(onboarding\\|include_router(settings" app/api/router.py` >= 4
    - Routes имеют router-level Depends(get_current_user): `grep -c "dependencies=\[Depends(get_current_user)\]" app/api/routes/`*.py >= 4 (исключая internal_telegram, см. Task 2)
    - onboarding.py обрабатывает AlreadyOnboardedError → 409: `grep -c "409\|HTTP_409_CONFLICT" app/api/routes/onboarding.py` >= 1
    - **Application boots:** `uv run python -c "from app.main_api import app; routes={r.path for r in app.routes if hasattr(r,'path')}; assert '/api/v1/categories' in routes and '/api/v1/onboarding/complete' in routes and '/api/v1/settings' in routes and '/api/v1/periods/current' in routes; print('OK')"`
    - **Integration tests pass at least at collection:** `uv run pytest tests/test_categories.py tests/test_periods.py tests/test_onboarding.py tests/test_settings.py --collect-only` без ImportError. (Полный pytest зелёный — требует тестовой БД, проверяется отдельно)
  </acceptance_criteria>
  <done>4 public route-модуля созданы и зарегистрированы в public_router; FastAPI app загружается; все ожидаемые routes присутствуют; integration tests collect без ошибок</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать internal_telegram_router (POST /internal/telegram/chat-bind) и зарегистрировать</name>
  <read_first>
    - app/api/router.py (только что обновлённый — internal_router structure)
    - app/services/telegram.py (bind_chat_id signature)
    - app/api/schemas/telegram.py (ChatBindRequest)
    - tests/test_telegram_chat_bind.py (контракт endpoint)
  </read_first>
  <files>app/api/routes/internal_telegram.py, app/api/router.py</files>
  <behavior>
    - Создать internal_telegram_router с prefix="/telegram" (под существующим /internal)
    - Endpoint POST /internal/telegram/chat-bind принимает ChatBindRequest, вызывает bind_chat_id
    - Защита X-Internal-Token наследуется от router-level dependency на internal_router
    - В app/api/router.py: добавить include_router(internal_telegram_router) к internal_router
    - test_telegram_chat_bind.py становится GREEN (при наличии тестовой БД)
  </behavior>
  <action>
**1. Создать `app/api/routes/internal_telegram.py`:**

```python
"""Internal endpoint for bot → api chat-bind (ONB-03).

Protected by X-Internal-Token (inherited from internal_router via include_router
with dependencies=[Depends(verify_internal_token)] applied at parent level).

The bot service calls this endpoint after receiving /start from the OWNER —
it persists tg_chat_id so the worker (Phase 5/6) can send push notifications.
"""
from typing import Annotated

from fastapi import APIRouter, Depends, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_db
from app.api.schemas.telegram import ChatBindRequest
from app.services import telegram as telegram_svc


# Note: no router-level dependencies here — they are inherited from the
# parent internal_router (verify_internal_token) when included.
internal_telegram_router = APIRouter(
    prefix="/telegram",
    tags=["internal-telegram"],
)


@internal_telegram_router.post("/chat-bind", status_code=status.HTTP_200_OK)
async def chat_bind(
    body: ChatBindRequest,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> dict:
    """POST /api/v1/internal/telegram/chat-bind — upsert AppUser.tg_chat_id.

    Idempotent: re-issuing with the same (tg_user_id, tg_chat_id) is a no-op.
    With a different tg_chat_id, updates the row.
    """
    await telegram_svc.bind_chat_id(
        db,
        tg_user_id=body.tg_user_id,
        tg_chat_id=body.tg_chat_id,
    )
    return {"status": "ok", "tg_user_id": body.tg_user_id}
```

**2. Обновить `app/api/router.py` — добавить регистрацию:**

В блоке после `internal_health` функции добавить:

```python
from app.api.routes.internal_telegram import internal_telegram_router

internal_router.include_router(internal_telegram_router)
```

(импорт можно поставить вверху со всеми остальными from app.api.routes; include — после определения internal_router и его existing routes).

Финальная структура `app/api/router.py` имеет в импортах:
```python
from app.api.routes.categories import categories_router
from app.api.routes.internal_telegram import internal_telegram_router
from app.api.routes.onboarding import onboarding_router
from app.api.routes.periods import periods_router
from app.api.routes.settings import settings_router
```

И registers:
```python
public_router.include_router(categories_router)
public_router.include_router(periods_router)
public_router.include_router(onboarding_router)
public_router.include_router(settings_router)

internal_router.include_router(internal_telegram_router)
```

**Validation:**

```bash
uv run python -c "
from app.main_api import app
routes = {r.path for r in app.routes if hasattr(r,'path')}
expected = '/api/v1/internal/telegram/chat-bind'
assert expected in routes, f'missing: {expected}; have: {sorted(routes)}'
print('chat-bind route registered OK')
"

# Если есть тестовая БД:
DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test uv run pytest tests/test_telegram_chat_bind.py -v
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python3 -c "import ast; ast.parse(open('app/api/routes/internal_telegram.py').read()); print('OK')" && cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.main_api import app; routes={r.path for r in app.routes if hasattr(r,'path')}; assert '/api/v1/internal/telegram/chat-bind' in routes; print('chat-bind registered')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/api/routes/internal_telegram.py создан и синтаксически корректен (ast.parse)
    - Endpoint POST /chat-bind определён: `grep -c "chat-bind\|chat_bind" app/api/routes/internal_telegram.py` >= 2
    - Использует ChatBindRequest schema: `grep -c "ChatBindRequest" app/api/routes/internal_telegram.py` >= 1
    - Вызывает telegram_svc.bind_chat_id: `grep -c "bind_chat_id" app/api/routes/internal_telegram.py` >= 1
    - app/api/router.py регистрирует internal_telegram_router: `grep -c "internal_router.include_router(internal_telegram_router)" app/api/router.py` == 1
    - **Endpoint registered:** `uv run python -c "from app.main_api import app; routes={r.path for r in app.routes if hasattr(r,'path')}; assert '/api/v1/internal/telegram/chat-bind' in routes; print('OK')"`
    - **Inheritance защиты X-Internal-Token подтверждена:** Когда вызывается без header, FastAPI делегирует на verify_internal_token (ранее в Phase 1 проверено для /internal/health) — тот же механизм
  </acceptance_criteria>
  <done>internal_telegram_router создан и зарегистрирован под internal_router; endpoint вызывает telegram service; inherited dependency защищает endpoint от внешних запросов</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy → FastAPI | `/api/v1/internal/*` блокируется на Caddy edge (Phase 1 verified); сюда не доходят внешние запросы |
| FastAPI → service | Pydantic-валидация request body; routes доверяют валидированным данным |
| Bot → API internal | Только bot имеет INTERNAL_TOKEN; X-Internal-Token проверяется router-level dep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cat-archive | Tampering | DELETE /categories/{id} | mitigate | Endpoint вызывает archive_category (soft), не физическое удаление; CategoryRead в response показывает is_archived=true для UI confirmation |
| T-cycle-validation | Tampering | POST /onboarding/complete, PATCH /settings | mitigate | Pydantic Field(ge=1, le=28) — 422 на out-of-range до достижения сервиса |
| T-double-onboard | Tampering | POST /onboarding/complete | mitigate | Service raises AlreadyOnboardedError → route → 409 |
| T-internal-token | Spoofing | POST /internal/telegram/chat-bind | mitigate | router-level Depends(verify_internal_token) на parent internal_router; Caddy edge блокирует /internal/* снаружи (Phase 1 verified) |
| T-chatbind-spoof | Tampering | POST /internal/telegram/chat-bind | accept | Endpoint доверяет body.tg_user_id (без проверки == OWNER_TG_ID). Single-tenant: только bot имеет токен; bot уже фильтрует OWNER_TG_ID в /start handler (Plan 02-05). Если когда-нибудь multi-tenant — добавить проверку |
| T-onboarding-leak | Info Disclosure | POST /onboarding/complete response | accept | Возвращает period_id + onboarded_at — нет sensitive данных |
| T-archive-historical-break | Tampering | DELETE /categories/{id} | mitigate | Soft archive не удаляет ID, FK в historical transactions сохраняются (CAT-02) |
</threat_model>

<verification>
План завершён когда:
1. Все route-файлы существуют и синтаксически корректны
2. `from app.main_api import app` успешно загружается
3. Все 9 ожидаемых routes зарегистрированы (см. acceptance):
   - /api/v1/me (Phase 1 — должен сохраниться)
   - /api/v1/categories, /api/v1/categories/{category_id}
   - /api/v1/periods/current
   - /api/v1/onboarding/complete
   - /api/v1/settings
   - /api/v1/internal/health (Phase 1 — должен сохраниться)
   - /api/v1/internal/telegram/chat-bind
   - /healthz (Phase 1)
4. При наличии тестовой БД: `uv run pytest tests/test_categories.py tests/test_periods.py tests/test_onboarding.py tests/test_settings.py tests/test_telegram_chat_bind.py -v` — GREEN
5. Phase 1 тесты не сломаны: `uv run pytest tests/test_auth.py tests/test_health.py tests/test_internal_auth.py -x` — GREEN
</verification>

<success_criteria>
- 6 файлов в app/api/routes/ + обновлённый app/api/router.py
- Все Phase 2 endpoints отвечают на правильные HTTP-методы и пути
- Public routes защищены initData auth (router-level)
- Internal route защищён X-Internal-Token (inherited)
- AlreadyOnboardedError → 409, Pydantic Field violations → 422, missing categories → 404
- Все integration-тесты Wave 0 проходят при наличии тестовой БД
- /me, /healthz, /internal/health не сломаны
</success_criteria>

<output>
После завершения создать `.planning/phases/02-domain-foundation-and-onboarding/02-04-SUMMARY.md`
</output>
