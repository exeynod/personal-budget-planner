---
phase: 02-domain-foundation-and-onboarding
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - app/services/__init__.py
  - app/services/categories.py
  - app/services/periods.py
  - app/services/settings.py
  - app/services/telegram.py
  - app/services/onboarding.py
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - PER-01
  - PER-02
  - PER-03
  - PER-05
  - ONB-01
  - ONB-03
  - SET-01

must_haves:
  truths:
    - "app/services/ — новый пакет с 5 сервисными модулями"
    - "categories.py: SEED_CATEGORIES (14), CRUD функции, soft-archive, idempotent seed"
    - "periods.py: create_first_period(), get_current_active_period()"
    - "onboarding.py: complete_onboarding() — atomic seed + period + user-flag в одной транзакции"
    - "settings.py: get_cycle_start_day(), update_cycle_start_day() — без перепресчёта прошлых периодов"
    - "telegram.py: bind_chat_id() — upsert AppUser.tg_chat_id"
    - "Все сервисы async и принимают AsyncSession; никакого прямого engine connect"
  artifacts:
    - path: "app/services/categories.py"
      provides: "CRUD категорий, SEED_CATEGORIES, soft-archive"
      exports: ["SEED_CATEGORIES", "list_categories", "create_category", "update_category", "archive_category", "seed_default_categories", "get_or_404"]
    - path: "app/services/periods.py"
      provides: "create_first_period, get_current_active_period"
      exports: ["create_first_period", "get_current_active_period"]
    - path: "app/services/onboarding.py"
      provides: "complete_onboarding (atomic), AlreadyOnboardedError"
      exports: ["complete_onboarding", "AlreadyOnboardedError"]
    - path: "app/services/settings.py"
      provides: "get_cycle_start_day, update_cycle_start_day"
      exports: ["get_cycle_start_day", "update_cycle_start_day"]
    - path: "app/services/telegram.py"
      provides: "bind_chat_id (upsert)"
      exports: ["bind_chat_id"]
  key_links:
    - from: "app/services/onboarding.py"
      to: "app/services/categories.py"
      via: "import seed_default_categories"
      pattern: "from app.services.categories"
    - from: "app/services/onboarding.py"
      to: "app/core/period.py"
      via: "period_for"
      pattern: "from app.core.period"
    - from: "app/services/telegram.py"
      to: "PostgreSQL upsert"
      via: "sqlalchemy.dialects.postgresql.insert + on_conflict_do_update"
      pattern: "on_conflict_do_update"
---

<objective>
Wave 2: реализация полного сервисного слоя Phase 2. Все сервисы — pure async functions, принимают AsyncSession как первый аргумент. Этот плана НЕ создаёт routes (тонкие HTTP-обёртки в Plan 02-04). Тестировать сервисы можно даже без HTTP-слоя — через прямой вызов с тестовой сессией. План включает все 5 модулей одним пакетом, потому что они тесно связаны (onboarding оркестрирует categories + periods + user-flags + settings).

Purpose: Изолированная бизнес-логика, легко переиспользуемая (Phase 5 worker `close_period` будет вызывать `periods.create_next_period()` — структура для этого готовится).

Output: app/services/ пакет с 5 модулями + __init__.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md
@app/db/models.py
@app/api/schemas/categories.py
@app/api/schemas/onboarding.py
@app/api/schemas/settings.py
@app/api/schemas/telegram.py
@app/core/period.py

<interfaces>
<!-- Импортируем из ORM -->
from app.db.models import (
    AppUser, Category, BudgetPeriod, CategoryKind, PeriodStatus,
)

<!-- Импортируем созданные в Plan 02-02 -->
from app.core.period import period_for
from app.api.schemas.categories import CategoryUpdate

<!-- SQLAlchemy 2.x async -->
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, func, update
from sqlalchemy.dialects.postgresql import insert as pg_insert  # для upsert

<!-- Создаваемые контракты -->

categories.py exports:
- SEED_CATEGORIES: list[tuple[str, str, int]]  # (name, kind_str, sort_order), 14 элементов
- async list_categories(db, *, include_archived: bool = False) -> list[Category]
- async create_category(db, *, name: str, kind: str, sort_order: int = 0) -> Category
- async update_category(db, category_id: int, patch: CategoryUpdate) -> Category
- async archive_category(db, category_id: int) -> Category  # synonym: update is_archived=True
- async seed_default_categories(db) -> list[Category]  # idempotent
- async get_or_404(db, category_id: int) -> Category  # raises HTTPException(404)

periods.py exports:
- async create_first_period(db, *, starting_balance_cents: int, cycle_start_day: int) -> BudgetPeriod
- async get_current_active_period(db) -> BudgetPeriod | None

settings.py exports:
- async get_cycle_start_day(db, tg_user_id: int) -> int
- async update_cycle_start_day(db, tg_user_id: int, cycle_start_day: int) -> int

telegram.py exports:
- async bind_chat_id(db, *, tg_user_id: int, tg_chat_id: int) -> None  # upsert

onboarding.py exports:
- class AlreadyOnboardedError(Exception)
- async complete_onboarding(db, *, tg_user_id: int, starting_balance_cents: int, cycle_start_day: int, seed_default_categories: bool) -> dict[str, Any]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать app/services/categories.py + periods.py + settings.py + telegram.py</name>
  <read_first>
    - app/db/models.py (Category, BudgetPeriod, AppUser, enum классы)
    - app/api/schemas/categories.py (CategoryUpdate)
    - .planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md (D-16: SEED_CATEGORIES)
    - .planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md (Pattern 7: idempotency)
    - tests/test_categories.py (ожидаемое поведение API)
    - tests/test_settings.py (ожидаемое поведение)
    - tests/test_telegram_chat_bind.py (ожидаемое поведение upsert)
  </read_first>
  <files>app/services/__init__.py, app/services/categories.py, app/services/periods.py, app/services/settings.py, app/services/telegram.py</files>
  <behavior>
    - app/services/__init__.py: пустой docstring
    - categories.py: SEED_CATEGORIES + 5 async функций + get_or_404
    - periods.py: create_first_period + get_current_active_period; вызывает period_for из app/core/period.py с datetime.now(MOSCOW_TZ).date()
    - settings.py: read/write `app_user.cycle_start_day`; не пересчитывает прошлые периоды
    - telegram.py: PostgreSQL UPSERT через `insert().on_conflict_do_update`
    - Все функции: type-hinted, async, принимают AsyncSession как первый позиционный аргумент
  </behavior>
  <action>
**1. Создать `app/services/__init__.py`:**

```python
"""Service layer: async business logic, DB-aware, framework-agnostic.

Each service module owns one domain and exports async functions that take
an AsyncSession as the first argument. Services are reusable from FastAPI
routes (Plan 02-04) and from worker jobs (Phase 5/6).
"""
```

**2. Создать `app/services/categories.py`:**

```python
"""Categories CRUD + soft-archive + seed (CAT-01, CAT-02, CAT-03)."""
from typing import Optional

from fastapi import HTTPException, status
from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.schemas.categories import CategoryUpdate
from app.db.models import Category, CategoryKind


# CAT-03: 14 default categories proposed during onboarding (D-16).
# Schema: (name, kind_str, sort_order)
SEED_CATEGORIES: list[tuple[str, str, int]] = [
    # expense (12)
    ("Продукты", "expense", 10),
    ("Дом", "expense", 20),
    ("Машина", "expense", 30),
    ("Кафе и рестораны", "expense", 40),
    ("Здоровье", "expense", 50),
    ("Подарки", "expense", 60),
    ("Развлечения", "expense", 70),
    ("Одежда", "expense", 80),
    ("Транспорт", "expense", 90),
    ("Подписки", "expense", 100),
    ("Связь и интернет", "expense", 110),
    ("Прочее", "expense", 120),
    # income (2)
    ("Зарплата", "income", 10),
    ("Прочие доходы", "income", 20),
]


async def list_categories(
    db: AsyncSession, *, include_archived: bool = False
) -> list[Category]:
    """Return categories ordered by (kind ASC: expense first, sort_order ASC, name ASC).

    CAT-02: by default skips archived (is_archived=true).
    """
    stmt = select(Category)
    if not include_archived:
        stmt = stmt.where(Category.is_archived.is_(False))
    stmt = stmt.order_by(Category.kind, Category.sort_order, Category.name)
    result = await db.execute(stmt)
    return list(result.scalars().all())


async def create_category(
    db: AsyncSession,
    *,
    name: str,
    kind: str,
    sort_order: int = 0,
) -> Category:
    """Create a new category. `kind` must be a CategoryKind value ('expense' or 'income')."""
    cat = Category(
        name=name,
        kind=CategoryKind(kind),
        sort_order=sort_order,
        is_archived=False,
    )
    db.add(cat)
    await db.flush()
    await db.refresh(cat)
    return cat


async def get_or_404(db: AsyncSession, category_id: int) -> Category:
    """Fetch a category or raise 404."""
    result = await db.execute(select(Category).where(Category.id == category_id))
    cat = result.scalar_one_or_none()
    if cat is None:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail=f"Category {category_id} not found",
        )
    return cat


async def update_category(
    db: AsyncSession,
    category_id: int,
    patch: CategoryUpdate,
) -> Category:
    """Apply non-None fields from `patch`. Allows un-archive via is_archived=False."""
    cat = await get_or_404(db, category_id)
    data = patch.model_dump(exclude_unset=True)
    for field, value in data.items():
        setattr(cat, field, value)
    await db.flush()
    await db.refresh(cat)
    return cat


async def archive_category(db: AsyncSession, category_id: int) -> Category:
    """CAT-02: soft-archive — set is_archived=True. DELETE handler delegates here."""
    cat = await get_or_404(db, category_id)
    cat.is_archived = True
    await db.flush()
    await db.refresh(cat)
    return cat


async def seed_default_categories(db: AsyncSession) -> list[Category]:
    """CAT-03: insert SEED_CATEGORIES. Idempotent — skip if any category exists.

    Returns the list of newly created categories (empty list if skipped).
    """
    existing_count = await db.scalar(select(func.count()).select_from(Category))
    if existing_count and existing_count > 0:
        return []  # idempotent: skip seed entirely

    rows = [
        Category(
            name=name,
            kind=CategoryKind(kind),
            sort_order=sort_order,
            is_archived=False,
        )
        for name, kind, sort_order in SEED_CATEGORIES
    ]
    db.add_all(rows)
    await db.flush()
    for cat in rows:
        await db.refresh(cat)
    return rows
```

**3. Создать `app/services/periods.py`:**

```python
"""Budget period creation and retrieval (PER-01, PER-02)."""
from datetime import datetime
from typing import Optional
from zoneinfo import ZoneInfo

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.period import period_for
from app.core.settings import settings
from app.db.models import BudgetPeriod, PeriodStatus


def _today_in_app_tz() -> "datetime.date":  # noqa: F821 - forward ref for inline date
    """Return today's date in the configured APP_TZ (Europe/Moscow)."""
    return datetime.now(ZoneInfo(settings.APP_TZ)).date()


async def get_current_active_period(db: AsyncSession) -> Optional[BudgetPeriod]:
    """Return the active period (status=active), or None if none exists."""
    result = await db.execute(
        select(BudgetPeriod)
        .where(BudgetPeriod.status == PeriodStatus.active)
        .order_by(BudgetPeriod.period_start.desc())
        .limit(1)
    )
    return result.scalar_one_or_none()


async def create_first_period(
    db: AsyncSession,
    *,
    starting_balance_cents: int,
    cycle_start_day: int,
) -> BudgetPeriod:
    """PER-02: create the first period covering today, with given starting_balance.

    Period bounds are computed by `period_for(today_msk, cycle_start_day)`.
    Status is `active`. Caller (onboarding service) is responsible for ensuring
    no other active period exists.
    """
    today = _today_in_app_tz()
    p_start, p_end = period_for(today, cycle_start_day)
    period = BudgetPeriod(
        period_start=p_start,
        period_end=p_end,
        starting_balance_cents=starting_balance_cents,
        status=PeriodStatus.active,
    )
    db.add(period)
    await db.flush()
    await db.refresh(period)
    return period
```

**4. Создать `app/services/settings.py`:**

```python
"""User-level settings (cycle_start_day) — SET-01.

Note (SET-01): updating cycle_start_day does NOT recompute existing periods.
Only newly-created periods (next period boundary, Phase 5 worker) will use
the new value.
"""
from fastapi import HTTPException, status
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import AppUser


async def _get_user_or_404(db: AsyncSession, tg_user_id: int) -> AppUser:
    result = await db.execute(
        select(AppUser).where(AppUser.tg_user_id == tg_user_id)
    )
    user = result.scalar_one_or_none()
    if user is None:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="App user not found — call /me first to bootstrap",
        )
    return user


async def get_cycle_start_day(db: AsyncSession, tg_user_id: int) -> int:
    user = await _get_user_or_404(db, tg_user_id)
    return user.cycle_start_day


async def update_cycle_start_day(
    db: AsyncSession,
    tg_user_id: int,
    cycle_start_day: int,
) -> int:
    """Persist the new cycle_start_day on the user row.

    Pydantic validates 1..28 upstream (SettingsUpdate.cycle_start_day). This
    function trusts the caller and does not re-validate.

    Existing budget_period rows are NOT modified — they retain the
    period_start/period_end values computed at their creation time.
    """
    user = await _get_user_or_404(db, tg_user_id)
    user.cycle_start_day = cycle_start_day
    await db.flush()
    return user.cycle_start_day
```

**5. Создать `app/services/telegram.py`:**

```python
"""Telegram chat-bind service (ONB-03).

Called by the bot service when /start is received from OWNER. Stores
tg_chat_id so the worker (Phase 5/6) can send push notifications.
"""
from sqlalchemy.dialects.postgresql import insert as pg_insert
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import AppUser


async def bind_chat_id(
    db: AsyncSession,
    *,
    tg_user_id: int,
    tg_chat_id: int,
) -> None:
    """Upsert AppUser.tg_chat_id by tg_user_id (idempotent).

    Two cases handled atomically:
    - User row already exists (created by /me upsert per Phase 1 D-11):
      UPDATE tg_chat_id.
    - User row does not exist yet (bot received /start before user opened
      Mini App): INSERT new row with tg_chat_id set.
    """
    stmt = (
        pg_insert(AppUser)
        .values(tg_user_id=tg_user_id, tg_chat_id=tg_chat_id)
        .on_conflict_do_update(
            index_elements=["tg_user_id"],
            set_={"tg_chat_id": tg_chat_id},
        )
    )
    await db.execute(stmt)
    # commit happens via get_db dependency on successful handler exit
```

**Validation steps after writing:**

```bash
# Modules import without error
uv run python -c "
from app.services.categories import SEED_CATEGORIES, list_categories, create_category, seed_default_categories
from app.services.periods import create_first_period, get_current_active_period
from app.services.settings import get_cycle_start_day, update_cycle_start_day
from app.services.telegram import bind_chat_id
assert len(SEED_CATEGORIES) == 14, f'expected 14 seed cats, got {len(SEED_CATEGORIES)}'
print('services import OK; SEED_CATEGORIES has 14 entries')
"
```

**Why no DB-tests run yet:** routes ещё не существуют (создаются в Plan 02-04). Сервис-уровень будет проверен косвенно через integration-тесты в Plan 02-04.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && for f in app/services/__init__.py app/services/categories.py app/services/periods.py app/services/settings.py app/services/telegram.py; do python3 -c "import ast; ast.parse(open('$f').read()); print('$f OK')"; done && cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.services.categories import SEED_CATEGORIES; assert len(SEED_CATEGORIES)==14; print('14 seed cats OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 5 файлов в app/services/ созданы и синтаксически корректны (ast.parse)
    - SEED_CATEGORIES содержит ровно 14 элементов (12 expense + 2 income): `uv run python -c "from app.services.categories import SEED_CATEGORIES; assert len(SEED_CATEGORIES)==14; expense=sum(1 for n,k,o in SEED_CATEGORIES if k=='expense'); income=sum(1 for n,k,o in SEED_CATEGORIES if k=='income'); assert expense==12 and income==2"`
    - categories.py определяет 6 публичных async функций: `grep -c "^async def" app/services/categories.py` == 6
    - periods.py использует period_for: `grep -c "from app.core.period import period_for" app/services/periods.py` == 1
    - periods.py использует APP_TZ через ZoneInfo: `grep -c "ZoneInfo" app/services/periods.py` == 1
    - telegram.py использует on_conflict_do_update: `grep -c "on_conflict_do_update" app/services/telegram.py` == 1
    - settings.py не делает recompute периодов: `grep -c "BudgetPeriod\|period" app/services/settings.py` == 0 (модуль не импортирует BudgetPeriod вообще)
    - **Импорты проходят:** `uv run python -c "from app.services.categories import list_categories, seed_default_categories; from app.services.periods import create_first_period; from app.services.settings import update_cycle_start_day; from app.services.telegram import bind_chat_id; print('OK')"`
  </acceptance_criteria>
  <done>4 сервисных модуля + __init__.py созданы; SEED_CATEGORIES — 14 элементов (12 exp + 2 inc); все импорты проходят; никакой связи settings.py с периодами (SET-01 compliance)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать app/services/onboarding.py — atomic orchestration</name>
  <read_first>
    - app/services/categories.py (только что созданный — seed_default_categories signature)
    - app/services/periods.py (create_first_period signature)
    - app/db/models.py (AppUser.onboarded_at)
    - tests/test_onboarding.py (ожидаемое поведение complete_onboarding, 409, atomicity)
    - .planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md (Pitfall 4)
  </read_first>
  <files>app/services/onboarding.py</files>
  <behavior>
    - Определяет AlreadyOnboardedError (наследует Exception)
    - complete_onboarding() — проверяет user.onboarded_at, если != None → raises AlreadyOnboardedError
    - Иначе атомарно: seed (если seed_default_categories=true) + create_first_period + update user (cycle_start_day, onboarded_at)
    - Atomicity достигается через `get_db` auto-commit/rollback (один transaction); если что-то упало в середине — rollback всей транзакции через except в get_db
    - Возвращает dict с period_id, seeded_categories (count), onboarded_at (isoformat str)
  </behavior>
  <action>
Создать `app/services/onboarding.py`:

```python
"""Onboarding orchestration (ONB-01, PER-02, CAT-03).

Performs four steps atomically (all-or-nothing via the request-scoped DB
transaction held by `get_db`):

  1. Verify user not already onboarded (AlreadyOnboardedError → 409).
  2. Optionally seed default categories (idempotent).
  3. Create the first budget period using `period_for(today_msk, cycle_start_day)`.
  4. Set user.cycle_start_day + user.onboarded_at = now().

If any step raises, the surrounding transaction is rolled back by the
`get_db` dependency — no partial onboarding state is persisted.
"""
from datetime import datetime, timezone
from typing import Any

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import AppUser
from app.services import categories as cat_svc
from app.services import periods as period_svc


class AlreadyOnboardedError(Exception):
    """User has already completed onboarding (D-10)."""


async def complete_onboarding(
    db: AsyncSession,
    *,
    tg_user_id: int,
    starting_balance_cents: int,
    cycle_start_day: int,
    seed_default_categories: bool,
) -> dict[str, Any]:
    """Run all 4 onboarding steps atomically.

    Args:
        db: AsyncSession injected by FastAPI dependency.
        tg_user_id: identifies the AppUser row (single-tenant — must match
            OWNER_TG_ID per upstream auth).
        starting_balance_cents: signed kopecks (negative allowed).
        cycle_start_day: 1..28 (validated by Pydantic upstream).
        seed_default_categories: if True, attempt to seed (skipped silently
            by the service if any category already exists).

    Returns:
        {"period_id": int, "seeded_categories": int, "onboarded_at": iso-str}

    Raises:
        AlreadyOnboardedError: when user.onboarded_at is not None.
        ValueError: when no AppUser row exists for tg_user_id (caller must
            trigger /me first to upsert the row per Phase 1 D-11).
    """
    # 1. Locate user
    result = await db.execute(
        select(AppUser).where(AppUser.tg_user_id == tg_user_id)
    )
    user = result.scalar_one_or_none()
    if user is None:
        raise ValueError(
            f"AppUser with tg_user_id={tg_user_id} not found; "
            "call GET /me first to upsert the row"
        )

    # D-10: idempotency / repeat-protection
    if user.onboarded_at is not None:
        raise AlreadyOnboardedError(
            f"User {tg_user_id} is already onboarded at {user.onboarded_at.isoformat()}"
        )

    # 2. Optional seed (idempotent inside service)
    seeded: list = []
    if seed_default_categories:
        seeded = await cat_svc.seed_default_categories(db)

    # 3. Create first period (uses period_for + today_msk inside the service)
    period = await period_svc.create_first_period(
        db,
        starting_balance_cents=starting_balance_cents,
        cycle_start_day=cycle_start_day,
    )

    # 4. Update user
    user.cycle_start_day = cycle_start_day
    user.onboarded_at = datetime.now(timezone.utc)
    await db.flush()

    return {
        "period_id": period.id,
        "seeded_categories": len(seeded),
        "onboarded_at": user.onboarded_at.isoformat(),
    }
```

**Atomicity note:** `get_db` (Phase 1, app/api/dependencies.py) уже оборачивает session в try/except: при ошибке делает rollback. Поскольку все четыре наших шага выполняются в рамках одного хендлера и одной session, любая ошибка в середине → откат seeded категорий + create period + user update. Это покрывает T-onboarding-atomicity угрозу из 02-VALIDATION.md.

**Validation:**

```bash
uv run python -c "
from app.services.onboarding import complete_onboarding, AlreadyOnboardedError
print('onboarding service imports OK')
print('AlreadyOnboardedError is Exception:', issubclass(AlreadyOnboardedError, Exception))
"
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python3 -c "import ast; ast.parse(open('app/services/onboarding.py').read()); print('onboarding.py syntax OK')" && cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.services.onboarding import complete_onboarding, AlreadyOnboardedError; assert issubclass(AlreadyOnboardedError, Exception); print('OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/services/onboarding.py существует и синтаксически корректен (ast.parse)
    - Определяет AlreadyOnboardedError class: `grep -c "^class AlreadyOnboardedError" app/services/onboarding.py` == 1
    - Импортирует cat_svc + period_svc: `grep -c "from app.services" app/services/onboarding.py` >= 2
    - Проверяет user.onboarded_at: `grep -c "onboarded_at is not None\|user.onboarded_at" app/services/onboarding.py` >= 2
    - Использует datetime.now(timezone.utc) (не naive datetime): `grep -c "timezone.utc" app/services/onboarding.py` >= 1
    - **Импорты проходят:** `uv run python -c "from app.services.onboarding import complete_onboarding, AlreadyOnboardedError; assert issubclass(AlreadyOnboardedError, Exception)"`
  </acceptance_criteria>
  <done>onboarding.py создан; complete_onboarding оркестрирует 4 шага atomically через единую транзакцию; AlreadyOnboardedError класс определён</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → service | Routes передают валидированные Pydantic модели; сервисы доверяют validated input |
| service → DB | Используется session, переданная через `Depends(get_db)`; commits/rollback в зависимости |
| service → service | onboarding вызывает categories+periods в той же session (одна транзакция) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-double-onboard | Tampering | onboarding.complete_onboarding | mitigate | Проверка `user.onboarded_at is not None` → AlreadyOnboardedError → route переводит в 409 (D-10) |
| T-onboarding-atomicity | Tampering | onboarding.complete_onboarding | mitigate | Все 4 шага в одной session; на исключении get_db делает rollback (Phase 1 verified) |
| T-cat-archive | Tampering | categories.list_categories | mitigate | Default `include_archived=False`; параметр явный, нельзя случайно показать архив |
| T-seed-double-fire | Tampering | categories.seed_default_categories | mitigate | Idempotent: skip if `count(Category) > 0` |
| T-chatbind-spoof | Tampering | telegram.bind_chat_id | mitigate (delegated) | Сервис принимает (tg_user_id, tg_chat_id) и доверяет caller'у. Routes layer (Plan 02-04) проверяет X-Internal-Token; сам сервис не имеет защиты — это by design (используется и из bot, и потенциально из tests) |
| T-settings-side-effect | Tampering | settings.update_cycle_start_day | mitigate | Не трогает BudgetPeriod (грep == 0); не пересчитывает прошлые периоды (SET-01) |
</threat_model>

<verification>
План завершён когда:
1. Все 6 файлов app/services/ существуют и синтаксически корректны
2. SEED_CATEGORIES имеет ровно 14 элементов (12 expense + 2 income)
3. Все import-цепочки работают: `uv run python -c "from app.services.onboarding import complete_onboarding"` без ошибок
4. AlreadyOnboardedError — подкласс Exception
5. settings.py не импортирует BudgetPeriod (граница SET-01)
6. Phase 1 тесты не сломаны: `uv run pytest tests/test_auth.py tests/test_health.py -x` GREEN
7. test_period_engine.py всё ещё GREEN (Plan 02-02 артефакт не сломан)
</verification>

<success_criteria>
- 6 модулей в app/services/ созданы (`__init__.py`, `categories.py`, `periods.py`, `settings.py`, `telegram.py`, `onboarding.py`)
- Все async functions используют AsyncSession как первый аргумент (не engine, не Session)
- categories.SEED_CATEGORIES — 14 элементов, kind ∈ {expense, income}
- onboarding.complete_onboarding атомарен через get_db transaction
- telegram.bind_chat_id — UPSERT через on_conflict_do_update
- settings.update_cycle_start_day не пересчитывает существующие periods (impotent на BudgetPeriod)
- Никаких routes пока (это Plan 02-04)
</success_criteria>

<output>
После завершения создать `.planning/phases/02-domain-foundation-and-onboarding/02-03-SUMMARY.md`
</output>
