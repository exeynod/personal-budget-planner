---
phase: 51-liquid-glass-system
plan: 01
type: execute
wave: 1
depends_on: [50-02]
files_modified:
  - design/tokens.json (LG motion/typography/radius tokens)
  - frontend/src/stylesV10/tokens.css (regenerated)
  - ios/BudgetPlanner/FeaturesV10/Common/PosterTokens.swift (regenerated)
autonomous: true
requirements: [LG-SYS-01, LG-SYS-02, LG-SYS-03, LG-SYS-04]
status: complete
commit: a2dc43a
---

# Plan 51-01 — LG palette/material/typography/motion/radius tokens

Extend `design/tokens.json` `themes.liquid_glass` секцию с финальными LG tokens:
palette (system Light/Dark adaptive), materials (4 blur intensities),
typography (SF Pro Display / SF Pro Text, iOS 26 HIG sizes), motion (springs + durations),
radius (card 14pt / sheet 16pt / button 12pt / pill).

Codegen re-emits `tokens.css` + `PosterTokens.swift` — both `LiquidGlassTokens` namespace
получает finalized values (FontSize / Radius / Motion sub-enums).

**Commit:** a2dc43a — tokens tune, zero regression on Maximal Poster baselines.
