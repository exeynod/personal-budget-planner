# Requirements Archive: TG Budget Planner — v0.3

**Archived:** 2026-05-06 at v0.3 milestone close
**Original defined:** 2026-05-01
**Core Value:** В один тап записать факт-трату и видеть актуальную дельту план/факт по категориям бюджета — быстрее, чем открывать Google-таблицу.

> **Note:** Это снапшот REQUIREMENTS.md на момент закрытия v0.3. Все v1 requirements (mapping на phases 1-6, milestone v0.2) и v0.3 requirements (NAV/TXN/MGT/ANL/AI/AICAT/SET-03) были реализованы в момент закрытия v0.3. v0.2 не закрывался formally через `/gsd-complete-milestone`, поэтому traceability ниже включает full coverage обоих milestones.

## v1 Requirements (v0.2 + v0.3)

### Auth & Onboarding (v0.2)

- [x] **AUTH-01**: Telegram `initData` валидируется HMAC-SHA256 с `bot_token`, `auth_date` ≤ 24ч — Phase 1
- [x] **AUTH-02**: Whitelist через ENV `OWNER_TG_ID`, всё остальное → 403 — Phase 1
- [x] **ONB-01**: Onboarding-экран — single-page scrollable с нумерованными секциями (1 bot bind / 2 starting balance / 3 cycle_start_day / 4 seed категории) — Phase 2
- [x] **ONB-02**: Если `chat_id` неизвестен — секция bot bind активна с кнопкой `tg.openTelegramLink(...?start=onboard)` — Phase 2
- [x] **ONB-03**: Бот при `/start` сохраняет `tg_chat_id` в БД для push-уведомлений — Phase 2

### Categories (v0.2)

- [x] **CAT-01**: CRUD категорий через REST API + UI (kind expense/income, name, sort_order) — Phase 2
- [x] **CAT-02**: Мягкая архивация — `is_archived=true` скрывает категорию из выбора, исторические записи остаются — Phase 2
- [x] **CAT-03**: Дефолтный seed-набор из 14 категорий (как в исходной xlsx) предлагается в onboarding шаге 4 — Phase 2

### Budget Period (v0.2)

- [x] **PER-01**: Период определяется глобальной настройкой `cycle_start_day` (1..28, default = 5) — Phase 2
- [x] **PER-02**: При onboarding пользователь вводит `starting_balance` для первого периода — Phase 2
- [x] **PER-03**: Каждый последующий период автоматически наследует `starting_balance` = `ending_balance` предыдущего — Phase 5
- [x] **PER-04**: Worker-job ежедневно 00:01 МСК закрывает истёкший активный период и создаёт следующий — Phase 5
- [x] **PER-05**: При создании нового периода развёртывается `PlanTemplate` (idempotent) — Phase 3

### Plan Template (v0.2)

- [x] **TPL-01**: Один `PlanTemplate` на пользователя, каждый item: category, amount, description, day_of_period (опц.) — Phase 3
- [x] **TPL-02**: CRUD строк шаблона через UI (Grouped by category + inline-редактирование суммы + bottom-sheet для полного редактора) — Phase 3
- [x] **TPL-03**: Кнопка «Перенести текущий план в шаблон» — snapshot активного периода в шаблон — Phase 3
- [x] **TPL-04**: Endpoint `/periods/{id}/apply-template` идемпотентен — повторный вызов не создаёт дубли — Phase 3

### Planned Transactions (v0.2)

- [x] **PLN-01**: CRUD строк плана текущего периода (group by category + inline edit + bottom-sheet) — Phase 3
- [x] **PLN-02**: Источник создания (`source` enum): template / manual / subscription_auto — Phase 3
- [x] **PLN-03**: Строка от подписки маркируется визуально («🔁 from subscription») — Phase 3

### Actual Transactions (v0.2)

- [x] **ACT-01**: Bottom-sheet форма добавления факт-транзакции (Mini App): сумма, kind, категория, описание, дата (default — сегодня) — Phase 4
- [x] **ACT-02**: Период факт-транзакции вычисляется по `tx_date` + текущий `cycle_start_day` — Phase 4
- [x] **ACT-03**: Бот-команды `/add <сумма> <category_query> [описание]` и `/income <...>` создают факт-транзакции — Phase 4
- [x] **ACT-04**: Бот-команды `/balance`, `/today`, `/app` выводят соответствующие данные — Phase 4
- [x] **ACT-05**: При неоднозначном `category_query` (≥2 совпадения) бот показывает inline-кнопки выбора — Phase 4

### Dashboard / Summary (v0.2)

- [x] **DSH-01**: Главный экран Mini App — tabs Расходы/Доходы + hero-карточка баланса + aggr-блок План/Факт/Δ + плотный список категорий с прогресс-барами факт/план — Phase 5
- [x] **DSH-02**: Знак дельты «положительная = хорошо»: расходы `План−Факт`, доходы `Факт−План`. Зелёный для положительной, красный для отрицательной — Phase 5
- [x] **DSH-03**: Состояние in-progress: warn-стили (≥80% = жёлтая обводка), danger-стили (>100% = красная обводка + бейдж процента) — Phase 5
- [x] **DSH-04**: Состояние empty (нет плана): empty-state с кнопками «Применить шаблон» / «Добавить вручную» — Phase 5
- [x] **DSH-05**: Состояние closed: read-only, MainButton дизейблен с надписью «Период закрыт», бейдж «Закрыт» на period-switcher — Phase 5
- [x] **DSH-06**: Переключатель периодов (← / →); навигация в архивные периоды только для просмотра — Phase 5

### Subscriptions (v0.2)

- [x] **SUB-01**: CRUD подписок через UI (name, amount, cycle ∈ {monthly, yearly}, next_charge_date, category, notify_days_before, is_active) — Phase 6
- [x] **SUB-02**: Список подписок с горизонтальным таймлайном на месяц (today-line, цветовая логика: ≤2 дня = красный, ≤7 = жёлтый) — Phase 6
- [x] **SUB-03**: Worker-job ежедневно 09:00 МСК отправляет push через бота за `notify_days_before` дней до списания — Phase 6
- [x] **SUB-04**: Worker-job ежедневно 00:05 МСК создаёт `PlannedTransaction` (source=subscription_auto) и сдвигает `next_charge_date` (+1 mo / +1 yr) — Phase 6
- [x] **SUB-05**: Unique constraint `(subscription_id, original_charge_date)` в `planned_transaction` для защиты от дублей при повторных запусках шедулера — Phase 6

### Settings (v0.2 + v0.3)

- [x] **SET-01**: Настройка `cycle_start_day` через UI, применяется только к будущим периодам — Phase 2
- [x] **SET-02**: Настройка `notify_days_before` для подписок (default = 2) — Phase 6
- [x] **SET-03** (v0.3): Toggle `enable_ai_categorization` (default = on) — отключает AI-предложение категории в форме новой транзакции — Phase 10

### Navigation v0.3 (Phase 7)

- [x] **NAV-01**: Bottom nav содержит ровно 5 функциональных табов: Главная / Транзакции / Аналитика / AI / Управление
- [x] **NAV-02**: Активный таб «AI» окрашен в фиолетовый (`#a78bfa`); остальные активные — primary blue
- [x] **NAV-03**: Phosphor line-icons: House / ArrowsLeftRight / ChartBar / Sparkle / SquaresFour
- [x] **NAV-04**: Существующие топ-уровневые экраны (HistoryScreen, PlannedScreen, SubscriptionsScreen, MoreScreen) реорганизуются под новую nav без потери функциональности

### Transactions tab v0.3 (Phase 7)

- [x] **TXN-01**: Таб «Транзакции» содержит 2 под-таба (underline sticky TabBar): История / План
- [x] **TXN-02**: Под-таб «История» — факт-транзакции сгруппированы по дням, в day-header — total за день
- [x] **TXN-03**: Под-таб «План» — плановые строки сгруппированы по категориям, у каждой строки source-badge (template / manual / subscription)
- [x] **TXN-04**: Под-таб kind (SubTabBar Расходы / Доходы) + чипсы категорий, отфильтрованные по активному kind. Чипса «Все» отсутствует — kindFilter намеренно exclusive (один тип за раз), чипсы кликабельны для category-фильтра.
- [x] **TXN-05**: FAB добавляет факт-транзакцию (под-таб История) или плановую строку (под-таб План)

### Management tab v0.3 (Phase 7)

- [x] **MGT-01**: Таб «Управление» = меню-список из 4 пунктов: Подписки / Шаблон / Категории / Настройки
- [x] **MGT-02**: Каждый пункт меню — surface card с иконкой 36×36, title + контекстная desc + chevron
- [x] **MGT-03**: Контекстные desc: «3 активные · 1 097 ₽/мес», «14 активных категорий», и т.п.
- [x] **MGT-04**: Саб-скрины (SubscriptionsScreen, TemplateScreen, CategoriesScreen, SettingsScreen) переиспользуются как есть; меняется только entry point

### Analytics v0.3 (Phase 8)

- [x] **ANL-01**: Экран Аналитика — top-level таб с PageTitle (без back-button)
- [x] **ANL-02**: Period chips (1 мес / 3 мес / 6 мес / Год)
- [x] **ANL-03**: Топ перерасходов текущего периода — карточки с лево-бордером danger/warn
- [x] **ANL-04**: Тренд расходов по месяцам — SVG line chart за последние N месяцев
- [x] **ANL-05**: Топ категорий по расходам — горизонтальные bars с цветами из chart-палитры
- [x] **ANL-06**: Прогноз остатка к концу текущего периода (linear extrapolation по дневному темпу)
- [x] **ANL-07**: API endpoints `GET /api/v1/analytics/trend`, `/top-overspend`, `/top-categories`, `/forecast`
- [x] **ANL-08**: Все агрегаты считаются на backend (не frontend)

### AI Assistant v0.3 (Phase 9 + 10.1 + 10.2)

- [x] **AI-01**: Экран AI — top-level таб с PageTitle «Budget AI» + аватар
- [x] **AI-02**: Empty-state с suggestion chips (Топ расходов / Сравни месяцы / Где экономить?)
- [x] **AI-03**: Streaming через SSE: `POST /api/v1/ai/chat` инициирует, frontend получает chunks через EventSource
- [x] **AI-04**: Tool-use indicator в bubble во время вызова tool («Смотрю март...» с pulse-точкой)
- [x] **AI-05**: Tools (function calling): `query_transactions`, `get_period_balance`, `get_category_summary`, `compare_periods`, `get_subscriptions`, `get_forecast`
- [x] **AI-06**: Conversation persistence в БД (`ai_conversation`, `ai_message`) — одна активная conversation на пользователя, можно очистить
- [x] **AI-07**: Prompt caching системного промпта + контекста бюджета — снижает input cost
- [x] **AI-08**: Provider-agnostic LLM client `app/ai/llm_client.py`, дефолт `openai/gpt-4.1-mini` (upgraded from -nano in 10.2), переключение через ENV `LLM_PROVIDER`/`LLM_MODEL`
- [x] **AI-09**: `OPENAI_API_KEY` только в backend ENV; frontend никогда не получает ключ
- [x] **AI-10**: Rate limit ≤10 req/мин на пользователя (lowered from 30 in Phase 10.1)

### AI Categorization v0.3 (Phase 10 + 10.1 + 10.2)

- [x] **AICAT-01**: При вводе описания в форме «Новая транзакция» AI предлагает категорию через cosine similarity
- [x] **AICAT-02**: AI-suggestion box заменяет `<select>` категории — имя + confidence-bar; кнопка «Сменить» возвращает стандартный select (confidence-bar убран в Phase 10.2 после UAT — только имя)
- [x] **AICAT-03**: Эмбеддинги категорий хранятся в `category_embedding(category_id PK, vector(1536), updated_at)`
- [x] **AICAT-04**: Если top-1 cosine similarity < 0.35 — показывать обычный select, AI не навязывает (lowered from 0.5 in Phase 10.2)
- [x] **AICAT-05**: Embedding для description вычисляется через OpenAI `text-embedding-3-small`
- [x] **AICAT-06**: Toggle через `SET-03` (`enable_ai_categorization`)

### Infrastructure (v0.2)

- [x] **INF-01**: docker-compose с 5 сервисами: caddy / api / bot / worker / db; все сервисы шарят один Python-codebase — Phase 1
- [x] **INF-02**: PostgreSQL 16 + Alembic-миграции; автомиграции при старте `api` — Phase 1
- [x] **INF-03**: Caddy с автоматическим Let's Encrypt; внутренний docker network `bot ↔ api` не проксируется наружу — Phase 1
- [x] **INF-04**: Internal API endpoints `/api/v1/internal/*` защищены `X-Internal-Token` — Phase 1
- [x] **INF-05**: Health-check эндпоинты `/healthz` для api/bot, heartbeat для worker — Phase 1

## v2 Requirements (Deferred — see new REQUIREMENTS.md after milestone close)

### Push alerts

- **ALERT-01**: Push-алерт при достижении 90% бюджета по критичной категории (deferred — не критично для personal pet)

### Import / Export

- **IMP-01**: Импорт CSV выписки банка с маппингом в категории
- **IMP-02**: Импорт исходного xlsx (one-shot seed)
- **EXP-01**: Экспорт периода в xlsx для бэкапа

### Multi-tenant (PROMOTED to v0.4)

- **MUL-01**: Регистрация любого TG-пользователя
- **MUL-02**: Изоляция данных по `user_id`
- **MUL-03**: Биллинг / тарифы (deferred — out of scope для v0.4)

### Other

- **OTH-01**: Мультивалютность (RUB + USD/EUR с курсом-снапшотом на дату)
- **OTH-02**: Семейный учёт с раздельными подбюджетами
- **OTH-03**: Веб-версия вне Telegram (через email/Google auth)
- **OTH-04**: Привязка ActualTransaction к конкретной строке плана
- **OTH-05**: Дедупликация подписок (фактическая транзакция → автосдвиг next_charge_date)

## Out of Scope at v0.3 close

| Feature | Reason |
|---------|--------|
| Семейный учёт / роли | «Здоровье Наташи» и т.п. — обычные категории, не отдельные сущности |
| Мультивалютность | Только RUB; курс-снапшоты усложнят без пользы |
| Импорт CSV/выписок банка | Ручной ввод и бот-команды покрывают daily-use |
| Импорт исходного xlsx | Старт с нуля, исторические данные не нужны |
| Push-алерты перерасхода | Только визуальная индикация в UI, без push |
| Веб-версия вне TG | Только Mini App |
| Backup через xlsx-выгрузку | pg_dump покрывает |
| Привязка ActualTransaction к строке плана | Агрегация на уровне категории достаточна |
| Дедупликация подписок | Маркер «🔁» + ручное удаление дубля; автологика хрупкая |
| Оффлайн-режим / локальный кэш | Серверная БД, online-only |

> **Removed from Out of Scope:** «Multi-tenant / SaaS» — promoted to v0.4 milestone goal.
> **Removed from Out of Scope:** «Графики трендов» — реализованы в Phase 8 (Analytics).

## Traceability (Final at v0.3 close)

**v1 Coverage:**
- v0.2 requirements (Phases 1-6): 43 — all Complete
- v0.3 requirements (Phases 7-10.2): 38 — all Complete
- **Total: 81 requirements, all Complete**

**Per-phase distribution:**
- Phase 1 (Infrastructure & Auth): 7 — AUTH-01, AUTH-02, INF-01..05
- Phase 2 (Domain Foundation & Onboarding): 11 — CAT-01..03, PER-01..02, PER-05, ONB-01..03, SET-01
- Phase 3 (Plan Template & Planned Transactions): 7 — TPL-01..04, PLN-01..03
- Phase 4 (Actual Transactions & Bot Commands): 5 — ACT-01..05
- Phase 5 (Dashboard & Period Lifecycle): 7 — DSH-01..06, PER-03, PER-04
- Phase 6 (Subscriptions & Worker Jobs): 6 — SUB-01..05, SET-02
- Phase 7 (Nav Refactor): 13 — NAV-01..04, TXN-01..05, MGT-01..04
- Phase 8 (Analytics): 8 — ANL-01..08
- Phase 9 (AI Assistant): 10 — AI-01..10
- Phase 10 (AI Categorization): 7 — AICAT-01..06, SET-03
- Phases 10.1, 10.2 (INSERTED): no new requirements (cost optimization + hardening derived from audit/UAT findings)

## Notes on Changed Requirements (post-close)

- **AI-08**: Default model upgraded from `gpt-4.1-nano` to `gpt-4.1-mini` in Phase 10.2 — nano выдумывал категории и плохо считал.
- **AI-10**: Rate limit lowered from 30 req/мин to 10 req/мин in Phase 10.1 — cost-review optimization.
- **AICAT-02**: Confidence-bar removed from suggestion box in Phase 10.2 — UAT feedback (visual noise without value).
- **AICAT-04**: Confidence threshold lowered from 0.5 to 0.35 in Phase 10.2 — typical short Russian descriptions need lower bar.

---
*Requirements archived: 2026-05-06 at v0.3 milestone close*
*Original defined: 2026-05-01*
