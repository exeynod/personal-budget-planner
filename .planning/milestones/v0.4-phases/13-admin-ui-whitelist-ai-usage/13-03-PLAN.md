---
phase: 13-admin-ui-whitelist-ai-usage
plan: "03"
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - app/api/routes/ai.py
  - tests/test_ai_usage_log_hook.py
autonomous: true
requirements: [AIUSE-02]
tags: [ai, usage-tracking, persistence, hook]
must_haves:
  truths:
    - "Каждый завершённый /ai/chat вызов записывает 1 строку в `ai_usage_log` с правильным user_id, model, prompt/completion/cached/total tokens, est_cost_usd"
    - "Запись в ai_usage_log происходит inside SSE event loop сразу после ring-buffer record (existing behaviour preserved); failure записи в БД НЕ ломает SSE стрим — wrap в try/except + structured log"
    - "_record_usage signature расширена: `_record_usage(usage: dict, *, user_id: int, db_session_factory: async_sessionmaker)` — необходимо потому что hook нужен async DB session, а sync ring-buffer record был sync function"
    - "Test test_ai_usage_log_hook_writes_row проверяет: после mock /ai/chat call с usage event → 1 row в ai_usage_log с правильным user_id и tokens"
    - "Test test_ai_usage_log_hook_db_failure_swallowed — если DB INSERT падает (mock SQLAlchemy raise), SSE стрим не падает, structured log содержит error"
    - "Test test_ai_usage_log_hook_skips_when_user_id_missing — defensive: если user_id is None или 0, INSERT skipped без crash"
  artifacts:
    - path: app/api/routes/ai.py
      provides: "_record_usage расширена hook'ом который пишет в ai_usage_log"
      contains: "ai_usage_log"
      contains_2: "AiUsageLog"
    - path: tests/test_ai_usage_log_hook.py
      provides: "Tests для DB persistence hook (3 теста)"
      contains: "AiUsageLog"
      contains_2: "test_ai_usage_log_hook_writes_row"
  key_links:
    - from: app/api/routes/ai.py::_record_usage
      to: app/db/models.py::AiUsageLog
      via: "import + session.add(AiUsageLog(...))"
      pattern: "from app.db.models import AiUsageLog"
    - from: app/api/routes/ai.py::_event_stream
      to: app/api/routes/ai.py::_record_usage
      via: "passes user_id + AsyncSessionLocal через kwargs (existing signature wrap)"
      pattern: "_record_usage.*user_id"
---

<objective>
Plan 13-03 — wire `_record_usage` чтобы каждый AI usage event дополнительно записывался в `ai_usage_log` (создан в Plan 13-02). Это даёт persistent per-user breakdown который Plan 13-05 (`GET /admin/ai-usage`) агрегирует.

Текущее поведение: `_record_usage(usage: dict)` — sync, append в in-memory deque + log line. Нет user_id, нет DB.

Новое поведение: signature расширяется — `_record_usage(usage, *, user_id, session_factory)`. После append в ring buffer (preserved) + log line, открывается short-lived AsyncSession через session_factory и INSERT'ится строка AiUsageLog. Failure не должен ломать SSE — wrap try/except.

Purpose: Persistent storage AI usage с user_id — необходимо для AIUSE-02 admin breakdown.

Output: модификация `app/api/routes/ai.py` (~30 строк изменений) + 1 новый test file. tests/test_admin_ai_usage_api.py из 13-01 пока ещё RED (потому что admin endpoint не написан — это 13-05), но `ai_usage_log` теперь содержит данные после реальных /ai/chat calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-CONTEXT.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-02-PLAN.md
@CLAUDE.md
@app/api/routes/ai.py
@app/db/session.py
@app/db/models.py
@tests/conftest.py
@tests/helpers/seed.py

<interfaces>
<!-- Существующая _record_usage сигнатура (current state на BEFORE этого плана) -->

```python
# app/api/routes/ai.py — current
_USAGE_BUFFER_MAX = 1000
_usage_buffer: deque[dict] = deque(maxlen=_USAGE_BUFFER_MAX)


def _record_usage(usage: dict) -> None:
    """Append a usage record (with UTC timestamp) to the ring buffer
    and emit a structured log line. Called from the SSE event loop."""
    record = {**usage, "ts": datetime.now(timezone.utc).isoformat()}
    _usage_buffer.append(record)
    logger.info(
        "ai.usage model=%s prompt=%d cached=%d completion=%d total=%d est_usd=%.6f",
        usage.get("model"),
        usage.get("prompt_tokens", 0),
        ...
    )

# Single call site (line 207):
elif etype == "usage":
    _record_usage(event["data"])
```

```python
# Новый контракт — после изменений в этом плане
async def _record_usage(
    usage: dict,
    *,
    user_id: int | None = None,
    session_factory: async_sessionmaker[AsyncSession] | None = None,
) -> None:
    """Append usage to ring buffer + log line + persist to ai_usage_log.

    Backwards compatible: existing call sites без kwargs продолжают работать
    (только ring buffer + log; DB insert skip).

    Args:
        usage: dict с keys model, prompt_tokens, cached_tokens,
               completion_tokens, total_tokens, est_cost_usd (как было)
        user_id: app_user.id юзера-инициатора /ai/chat. Если None или 0,
                 DB insert skipped (defensive, log warning).
        session_factory: async_sessionmaker для open краткой DB transaction.
                         Если None, DB insert skipped.
    """
```

В `_event_stream` (line 104+) единственный call site — нужно передать `user_id` и `session_factory`.

`AsyncSessionLocal` уже импортирован в `app/api/routes/ai.py`? Проверка:
```bash
grep "AsyncSessionLocal\|async_sessionmaker" app/api/routes/ai.py
```
Если нет — добавить импорт `from app.db.session import AsyncSessionLocal`.

```python
# В _event_stream (line ~127):
# user_id уже доступен как параметр функции (line 106)
# Изменить call site (line 207):
elif etype == "usage":
    await _record_usage(
        event["data"],
        user_id=user_id,
        session_factory=AsyncSessionLocal,
    )
```

NB: `_record_usage` становится async — call site уже в async generator, await работает. Существующая sync sig signaturу нужно перевести в async (всё равно: ring buffer append + log call синхронны, но добавился `await` на DB).

NB: AsyncSessionLocal в app/db/session.py — существующий global; идентичный использованию в `get_db()` (см. app/api/dependencies.py).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_ai_usage_log_hook.py — RED → GREEN тесты на DB-hook (3 теста)</name>
  <files>tests/test_ai_usage_log_hook.py</files>
  <read_first>
    - app/api/routes/ai.py (current _record_usage signature, _event_stream layout)
    - app/db/models.py (AiUsageLog после Plan 13-02)
    - app/db/session.py (AsyncSessionLocal)
    - tests/conftest.py (db_client pattern, _PHASE13_TRUNCATE_TABLES)
    - tests/helpers/seed.py (seed_user; не trying to use seed_ai_usage_log — этот тест проверяет именно hook'у, не helper)
  </read_first>
  <behavior>
    Файл tests/test_ai_usage_log_hook.py — 3 теста, ВСЕ DB-backed (skip when DATABASE_URL unset):

    - test_ai_usage_log_hook_writes_row: seed AppUser; вызов `await _record_usage(usage_dict, user_id=user.id, session_factory=AsyncSessionLocal)` напрямую (не через HTTP — изолируем hook). After: SELECT count(*) FROM ai_usage_log WHERE user_id = user.id → 1; SELECT model, total_tokens, est_cost_usd FROM ai_usage_log → matches input.
    - test_ai_usage_log_hook_db_failure_swallowed: monkey-patch session_factory чтобы выбрасывал OperationalError; вызов `await _record_usage(usage_dict, user_id=user.id, session_factory=broken_factory)` НЕ raise (no exception propagated); structured log содержит "ai.usage_log_persist_failed".
    - test_ai_usage_log_hook_skips_when_user_id_missing: вызов `await _record_usage(usage_dict, user_id=None)` НЕ выбрасывает, БД rows count = 0.

    После Plan 13-03 implementation все 3 теста становятся GREEN. На текущем коде (без изменений) они RED либо ImportError (если await не сработает на sync function), либо AttributeError (если signature не имеет user_id kwarg).
  </behavior>
  <action>
    Создать tests/test_ai_usage_log_hook.py:

    ```python
    """Tests for Plan 13-03 — _record_usage hook persists to ai_usage_log table.

    The hook is called from the SSE event loop in app/api/routes/ai.py
    when an LLM 'usage' event arrives. It must:
      1. Continue to append to the in-memory ring buffer (existing behaviour)
      2. Persist a row in ai_usage_log table with user_id + tokens + cost
      3. Swallow DB failures (do not break the SSE stream)

    Tests skip when DATABASE_URL is unset.
    """
    from __future__ import annotations

    import logging
    import os
    from datetime import datetime, timezone

    import pytest
    import pytest_asyncio
    from sqlalchemy import text


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set")


    @pytest_asyncio.fixture
    async def fresh_db():
        _require_db()
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
        from tests.helpers.seed import _PHASE13_TRUNCATE_TABLES

        admin_url = os.environ.get("ADMIN_DATABASE_URL") or os.environ["DATABASE_URL"]
        admin_engine = create_async_engine(admin_url, echo=False)
        async with admin_engine.begin() as conn:
            await conn.execute(
                text(f"TRUNCATE TABLE {_PHASE13_TRUNCATE_TABLES} RESTART IDENTITY CASCADE")
            )
        await admin_engine.dispose()

        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)
        yield SessionLocal
        await engine.dispose()


    @pytest.mark.asyncio
    async def test_ai_usage_log_hook_writes_row(fresh_db):
        """Happy path: hook persists 1 row to ai_usage_log."""
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole
        from app.api.routes.ai import _record_usage

        async with fresh_db() as session:
            user = await seed_user(session, tg_user_id=9_777_777_001, role=UserRole.owner)
            await session.commit()
            user_id = user.id

        usage_event = {
            "model": "gpt-4o-mini",
            "prompt_tokens": 100,
            "completion_tokens": 50,
            "cached_tokens": 10,
            "total_tokens": 150,
            "est_cost_usd": 0.0012,
        }
        await _record_usage(usage_event, user_id=user_id, session_factory=fresh_db)

        async with fresh_db() as session:
            result = await session.execute(
                text(
                    "SELECT user_id, model, prompt_tokens, completion_tokens, "
                    "cached_tokens, total_tokens, est_cost_usd "
                    "FROM ai_usage_log WHERE user_id = :uid"
                ),
                {"uid": user_id},
            )
            rows = result.all()
        assert len(rows) == 1, f"expected 1 row, got {len(rows)}"
        row = rows[0]
        assert row[0] == user_id
        assert row[1] == "gpt-4o-mini"
        assert row[2] == 100
        assert row[3] == 50
        assert row[4] == 10
        assert row[5] == 150
        assert abs(row[6] - 0.0012) < 1e-9


    @pytest.mark.asyncio
    async def test_ai_usage_log_hook_db_failure_swallowed(fresh_db, caplog):
        """If DB insert fails, hook MUST NOT raise — SSE stream continues."""
        from app.api.routes.ai import _record_usage

        class BrokenFactory:
            def __call__(self):
                from sqlalchemy.exc import OperationalError
                raise OperationalError("simulated", {}, Exception("conn lost"))

        usage_event = {
            "model": "gpt-4o-mini", "prompt_tokens": 1, "completion_tokens": 1,
            "cached_tokens": 0, "total_tokens": 2, "est_cost_usd": 0.00001,
        }

        # Should NOT raise.
        with caplog.at_level(logging.WARNING, logger="app.api.routes.ai"):
            await _record_usage(usage_event, user_id=999, session_factory=BrokenFactory())

        # Structured log line should mention the persist failure.
        assert any(
            "ai.usage_log_persist_failed" in rec.message
            for rec in caplog.records
        ), f"expected log line 'ai.usage_log_persist_failed', got: {[r.message for r in caplog.records]}"


    @pytest.mark.asyncio
    async def test_ai_usage_log_hook_skips_when_user_id_missing(fresh_db):
        """Defensive: user_id=None or 0 → skip insert, do not raise."""
        from app.api.routes.ai import _record_usage

        usage_event = {
            "model": "gpt-4o-mini", "prompt_tokens": 1, "completion_tokens": 1,
            "cached_tokens": 0, "total_tokens": 2, "est_cost_usd": 0.00001,
        }

        # None case.
        await _record_usage(usage_event, user_id=None, session_factory=fresh_db)
        # Zero case (defensive).
        await _record_usage(usage_event, user_id=0, session_factory=fresh_db)

        async with fresh_db() as session:
            count = (await session.execute(text("SELECT count(*) FROM ai_usage_log"))).scalar_one()
        assert count == 0, f"no rows should persist when user_id missing, got {count}"
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_ai_usage_log_hook.py --collect-only 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File created; pytest --collect-only shows 3 tests collected
    - `grep -c "AiUsageLog\|ai_usage_log" tests/test_ai_usage_log_hook.py` → ≥ 4
    - `grep -c "_record_usage" tests/test_ai_usage_log_hook.py` → ≥ 3 (один в каждом тесте)
    - `grep -c "ai.usage_log_persist_failed" tests/test_ai_usage_log_hook.py` → 1 (assertion на log line)
    - При запуске на коде ДО Task 2: тесты RED — TypeError "got unexpected keyword argument 'user_id'" или TypeError "object NoneType can't be used in 'await'" (sync fn).
  </acceptance_criteria>
  <done>
    Файл создан; 3 теста собираются; RED на текущем коде. После Task 2 они станут GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Расширить app/api/routes/ai.py — _record_usage пишет в ai_usage_log + обновить call site в _event_stream</name>
  <files>app/api/routes/ai.py</files>
  <read_first>
    - app/api/routes/ai.py (текущая _record_usage функция, _event_stream call site, импорты)
    - app/db/models.py::AiUsageLog (добавлен в Plan 13-02)
    - app/db/session.py (AsyncSessionLocal экспорт)
  </read_first>
  <action>
    Изменения в `app/api/routes/ai.py`:

    1) Импорты — в шапке файла (после существующих SQLAlchemy импортов) добавить:
    ```python
    from sqlalchemy.ext.asyncio import async_sessionmaker
    from app.db.session import AsyncSessionLocal
    from app.db.models import AiUsageLog
    ```
    NB: проверить что `from app.db.session import AsyncSessionLocal` ещё не импортирован — если уже есть, не дублировать.

    2) Заменить функцию `_record_usage` целиком. Текущая версия (около строки 68):

    ```python
    def _record_usage(usage: dict) -> None:
        """Append a usage record (with UTC timestamp) to the ring buffer
        and emit a structured log line. Called from the SSE event loop."""
        record = {**usage, "ts": datetime.now(timezone.utc).isoformat()}
        _usage_buffer.append(record)
        logger.info(
            "ai.usage model=%s prompt=%d cached=%d completion=%d total=%d est_usd=%.6f",
            usage.get("model"),
            usage.get("prompt_tokens", 0),
            usage.get("cached_tokens", 0),
            usage.get("completion_tokens", 0),
            usage.get("total_tokens", 0),
            usage.get("est_cost_usd", 0.0),
        )
    ```

    Новая версия:

    ```python
    async def _record_usage(
        usage: dict,
        *,
        user_id: int | None = None,
        session_factory: "async_sessionmaker[AsyncSession] | None" = None,
    ) -> None:
        """Append a usage record + log line + persist to ai_usage_log table.

        Phase 13: persistent per-user storage (AIUSE-02). Existing ring buffer
        behaviour preserved for backwards compat with /ai/usage endpoint.

        Args:
            usage: dict with model + token counts + est_cost_usd (LLM event).
            user_id: app_user.id of the requestor. None/0 → skip DB insert.
            session_factory: async_sessionmaker for short-lived insert. None
                → skip DB insert (used by tests / sites не имеющие сессии).

        DB failures are caught + logged ('ai.usage_log_persist_failed') so a
        broken DB does NOT break the SSE event stream.
        """
        record = {**usage, "ts": datetime.now(timezone.utc).isoformat()}
        _usage_buffer.append(record)
        logger.info(
            "ai.usage model=%s prompt=%d cached=%d completion=%d total=%d est_usd=%.6f",
            usage.get("model"),
            usage.get("prompt_tokens", 0),
            usage.get("cached_tokens", 0),
            usage.get("completion_tokens", 0),
            usage.get("total_tokens", 0),
            usage.get("est_cost_usd", 0.0),
        )

        # ---- Phase 13 AIUSE-02: persist to ai_usage_log ----
        if not user_id or session_factory is None:
            return
        try:
            async with session_factory() as session:
                row = AiUsageLog(
                    user_id=user_id,
                    model=str(usage.get("model") or "unknown"),
                    prompt_tokens=int(usage.get("prompt_tokens", 0) or 0),
                    completion_tokens=int(usage.get("completion_tokens", 0) or 0),
                    cached_tokens=int(usage.get("cached_tokens", 0) or 0),
                    total_tokens=int(usage.get("total_tokens", 0) or 0),
                    est_cost_usd=float(usage.get("est_cost_usd", 0.0) or 0.0),
                )
                session.add(row)
                await session.commit()
        except Exception as exc:  # noqa: BLE001 — telemetry must not break SSE
            logger.warning(
                "ai.usage_log_persist_failed user_id=%s model=%s err=%s",
                user_id, usage.get("model"), exc,
            )
    ```

    Важно: `from sqlalchemy.ext.asyncio import AsyncSession` уже импортирован (line 29). Тип `async_sessionmaker[AsyncSession]` — для type hint используем строковый алиас (forward ref) чтобы не требовать generic-evaluation на module load: `"async_sessionmaker[AsyncSession] | None"`.

    3) Обновить call site в `_event_stream` (около строки 207):

    Найти:
    ```python
    elif etype == "usage":
        _record_usage(event["data"])
    ```

    Заменить на:
    ```python
    elif etype == "usage":
        # Phase 13: pass user_id + session factory so the hook can persist
        # to ai_usage_log (Plan 13-03). Failure is swallowed inside the
        # hook — telemetry must not break the SSE stream.
        await _record_usage(
            event["data"],
            user_id=user_id,
            session_factory=AsyncSessionLocal,
        )
    ```

    `user_id` доступен в области `_event_stream` (declared as parameter, line 106). `AsyncSessionLocal` — после добавления import в шаге 1.

    4) Self-check после изменений:
    - `grep -c "async def _record_usage" app/api/routes/ai.py` → 1 (sync version replaced)
    - `grep -c "def _record_usage" app/api/routes/ai.py` → 1 (только async)
    - `grep -c "ai_usage_log_persist_failed" app/api/routes/ai.py` → 1
    - `grep -c "AiUsageLog" app/api/routes/ai.py` → ≥ 2 (import + usage)
    - `grep -c "AsyncSessionLocal" app/api/routes/ai.py` → ≥ 2 (import + call site)
    - `grep -c "await _record_usage" app/api/routes/ai.py` → 1 (event loop call site)
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.routes.ai import _record_usage; import inspect; assert inspect.iscoroutinefunction(_record_usage), 'must be async'; sig = inspect.signature(_record_usage); assert 'user_id' in sig.parameters; assert 'session_factory' in sig.parameters; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `_record_usage` is async (`inspect.iscoroutinefunction(_record_usage)` returns True)
    - Signature has `user_id` and `session_factory` keyword-only parameters
    - `grep -c "async def _record_usage" app/api/routes/ai.py` → 1
    - `grep -c "ai.usage_log_persist_failed" app/api/routes/ai.py` → 1
    - `grep -c "AiUsageLog" app/api/routes/ai.py` → ≥ 2
    - `grep -c "AsyncSessionLocal" app/api/routes/ai.py` → ≥ 2
    - `grep -c "await _record_usage" app/api/routes/ai.py` → 1 (call site updated)
    - `pytest tests/test_ai_usage_log_hook.py -x` (если DB доступна) → 3/3 passed; если DB недоступна — 3/3 skipped.
    - `pytest tests/ai/ tests/api/ -k "not test_postgres_role_runtime" --collect-only 2>&1 | grep -c "error"` → 0 (нет regression в коллекции)
  </acceptance_criteria>
  <done>
    `_record_usage` extended; call site updated; tests pass on DB-enabled run; SSE stream remains stable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM client → _record_usage | Trusted (internal); usage dict shape contracted by LLM provider |
| _record_usage → ai_usage_log INSERT | budget_app role inside SET LOCAL app.current_user_id (RLS-permitted INSERT — user_id matches GUC since hook runs in user's request scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | T (Tampering) | Malformed usage dict (missing keys) crashes _record_usage | mitigate | All field accesses use `.get(key, default)`; explicit `int(...)` / `float(...)` coercion handles None; falsy fallbacks prevent ValueError |
| T-13-03-02 | D (Denial of Service) | DB outage cascades to SSE failure (user-facing chat breaks) | mitigate | try/except around DB insert; logger.warning records the failure; SSE stream continues |
| T-13-03-03 | I (Info Disclosure) | Hook writes wrong user_id (cross-tenant leak) | mitigate | user_id sourced from `_event_stream` parameter (Depends(get_current_user_id)); not from client-supplied data; RLS policy on ai_usage_log adds defence-in-depth |
| T-13-03-04 | E (Elevation) | Hook tries to commit a long transaction holding locks | mitigate | session_factory() opens fresh AsyncSession; commit immediately after add; no shared transaction with parent SSE handler |

</threat_model>

<verification>
- `_record_usage` is async, accepts user_id + session_factory kwargs (Task 2 acceptance)
- Tests in tests/test_ai_usage_log_hook.py — 3/3 GREEN on DB-enabled CI
- Existing tests/test_ai_usage.py / tests/test_ai_chat.py НЕ должны regression (mock test code НЕ передаёт user_id; signature backwards-compat)
- Manual smoke (deferred to Plan 13-08): запустить /ai/chat в dev, проверить `SELECT count(*) FROM ai_usage_log` → ≥ 1 row
</verification>

<success_criteria>
- 1 модифицированный файл: app/api/routes/ai.py — _record_usage стала async, signature расширена, call site обновлён
- 1 новый файл: tests/test_ai_usage_log_hook.py — 3 DB-tests
- pytest tests/test_ai_usage_log_hook.py — 3 passed (если DB доступна)
- pytest --collect-only весь suite — без regressions
- Готовность к Plan 13-05 (admin /ai-usage endpoint читает из persistent ai_usage_log)
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ui-whitelist-ai-usage/13-03-SUMMARY.md`
</output>
