---
phase: 13-admin-ui-whitelist-ai-usage
plan: "08"
type: execute
wave: 6
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05", "13-06", "13-07"]
files_modified:
  - .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md
autonomous: false
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, AIUSE-01, AIUSE-02, AIUSE-03]
tags: [verification, integration, uat, human-checkpoint, threat-model-attestation]
must_haves:
  truths:
    - "13-VERIFICATION.md создан с frontmatter (phase, status, verified-on, verifier, requirements) — формат соответствует 12-VERIFICATION.md"
    - "Раздел Requirements Verification покрывает все 9 ID (ADM-01..06 + AIUSE-01..03) — каждое с PASS/FAIL/⚠ + evidence (test names, grep results, manual checkpoints)"
    - "Раздел Manual Checkpoints покрывает: alembic 0008 upgrade/downgrade cycle, frontend tsc + npm build, live owner→member→revoke→cascade-purge end-to-end (live TG MiniApp UAT)"
    - "Threat model attestation block — STRIDE register из плана 13-01..13-07 aggregated; каждый threat либо mitigated (ссылка на code/test) либо accepted (rationale)"
    - "Live TG MiniApp UAT — listed как `human_needed` checkpoint (analogous Phase 11 / Phase 12 pattern)"
    - "Status frontmatter: либо `passed` (full automated GREEN + human signed off) либо `human_needed` (automated GREEN; live MiniApp UAT pending)"
    - "Если migration apply failed (alembic 0008) — статус FAIL и rollback инструкции"
  artifacts:
    - path: .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md
      provides: "Phase 13 verification report"
      contains: "ADM-01"
      contains_2: "AIUSE-03"
      contains_3: "alembic 0008"
  key_links:
    - from: 13-VERIFICATION.md
      to: tests/test_admin_users_api.py + tests/test_admin_ai_usage_api.py + tests/test_ai_usage_log_hook.py
      via: "evidence cites each test file with pass/fail count"
      pattern: "test_admin_"
    - from: 13-VERIFICATION.md
      to: alembic/versions/0008_admin_phase13.py
      via: "Manual checkpoint: alembic upgrade head + downgrade -1 cycle on dev DB"
      pattern: "0008"
---

<objective>
Wave 6 verification gate. После Plans 13-01..13-07 implementation complete. Этот план:

1. Запускает full automated suite — собирает evidence
2. Per-requirement evidence (ADM-01..06 + AIUSE-01..03)
3. Verifies alembic 0008 apply (spending_cap_cents + ai_usage_log + last_seen_at) — full upgrade/downgrade cycle
4. Verifies frontend build clean (vite + tsc)
5. Записывает remaining manual UAT items (live TG MiniApp smoke — analogous Phase 11/12)
6. Создаёт `.planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md` (Phase 12 format)

2 blocking checkpoints:
- **Checkpoint 1 (human-verify)**: docker rebuild + alembic 0008 apply on dev DB
- **Checkpoint 2 (human-verify)**: live TG MiniApp end-to-end (owner invite member → /start by member → owner revokes → cascade purge verified в psql). Может быть deferred to milestone close, как Phase 11/12.

Output: 13-VERIFICATION.md с status `passed` или `human_needed`. Phase 13 functionally complete либо awaiting live smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-CONTEXT.md
@.planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md
@.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-01-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-02-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-03-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-04-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-05-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-06-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-07-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated tests — собрать evidence для всех requirements</name>
  <files></files>
  <read_first>
    - tests/test_admin_users_api.py
    - tests/test_admin_ai_usage_api.py
    - tests/test_ai_usage_log_hook.py
  </read_first>
  <behavior>
    Запустить pytest на Phase 13 own files + Phase 11/12 regression smoke. Сохранить логи в /tmp/phase13_*.log для Task 4 (доказательства).
  </behavior>
  <action>
    Выполнить sequentially:

    1. Phase 13 own tests:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest \
        tests/test_admin_users_api.py \
        tests/test_admin_ai_usage_api.py \
        tests/test_ai_usage_log_hook.py \
        -v 2>&1 | tee /tmp/phase13_own_tests.log | tail -40
    ```
    Expected: 12 + 5 + 3 = 20 passed (with DB available). Если skipped — записать причину в 13-VERIFICATION.md (DATABASE_URL not set).

    2. Phase 12 regression check:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest \
        tests/test_role_based_auth.py \
        tests/test_require_owner.py \
        tests/test_me_returns_role.py \
        tests/test_bot_role_resolution.py \
        -v 2>&1 | tee /tmp/phase13_phase12_regression.log | tail -25
    ```
    Expected: 15/15 passing (no Phase 13 regressions on auth surface).

    3. Phase 11 regression check:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest \
        tests/test_multitenancy_isolation.py \
        tests/test_rls_policy.py \
        tests/test_migration_backfill.py \
        -v 2>&1 | tee /tmp/phase13_phase11_regression.log | tail -25
    ```
    Expected: 14/14 passing (RLS still enforces, no schema regression).

    4. Full suite pass rate:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest 2>&1 | tee /tmp/phase13_full_suite.log | tail -15
    ```
    Expected: ≥ 90% pass rate (target threshold from D-11-07-01 measure). Phase 13 adds ~20 tests, total should be ~295/315 with skips.

    5. Записать pass/fail counts в файлы /tmp/* для Task 4 incorporation.
  </action>
  <verify>
    <automated>ls -la /tmp/phase13_own_tests.log /tmp/phase13_phase12_regression.log /tmp/phase13_phase11_regression.log /tmp/phase13_full_suite.log 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - 4 log файла существуют в /tmp/
    - Phase 13 own log содержит "20 passed" или "20 skipped" (DB-dependent)
    - Phase 12 regression: "15 passed" в last 10 lines
    - Phase 11 regression: "14 passed" в last 10 lines
    - Full suite: pass rate ≥ 90% (count passed / (passed + failed))
  </acceptance_criteria>
  <done>
    Logs собраны; counts известны; готов к Task 4 написанию verification.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual checkpoint — alembic 0008 apply + downgrade cycle on dev DB</name>
  <what-built>
    - alembic 0008 миграция (Plan 13-02): adds spending_cap_cents + ai_usage_log + last_seen_at + RLS + grants
    - app/db/models.py: AppUser.spending_cap_cents + AppUser.last_seen_at + AiUsageLog model
    - app/api/routes/ai.py: _record_usage hook теперь персистит в ai_usage_log
    - app/api/routes/admin.py: 4 admin endpoints (/admin/users CRUD + /admin/ai-usage)
    - frontend AccessScreen + 4 components + 2 hooks + types
  </what-built>
  <how-to-verify>
    Operator выполняет в локальном dev окружении (см. memory infra-deploy.md):

    1. Rebuild api container с новой migration code:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         docker compose -f docker-compose.yml -f docker-compose.dev.yml build api worker bot
       ```
       Expected: build succeeds without Python ImportError (models.py loads).

    2. Apply alembic 0008:
       ```bash
       docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d db && \
         sleep 3 && \
         docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm api alembic upgrade head
       ```
       Expected: output contains `Running upgrade 0007_postgres_role_split -> 0008_admin_phase13`. No errors.

    3. Inspect schema:
       ```bash
       docker compose -f docker-compose.yml -f docker-compose.dev.yml exec db \
         psql -U budget budget_db -c '\d app_user' -c '\d ai_usage_log' -c "SELECT version_num FROM alembic_version"
       ```
       Expected:
       - `app_user` shows `spending_cap_cents | bigint | not null | default 46500` and `last_seen_at | timestamp with time zone`
       - `ai_usage_log` table exists with FK CASCADE on user_id, RLS policy `ai_usage_log_user_isolation`
       - `version_num` = `0008_admin_phase13`

    4. Round-trip downgrade/upgrade:
       ```bash
       docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm api alembic downgrade -1 && \
         docker compose -f docker-compose.yml -f docker-compose.dev.yml run --rm api alembic upgrade head
       ```
       Expected:
       - downgrade reverts to `0007_postgres_role_split` (drops ai_usage_log + spending_cap_cents + last_seen_at)
       - upgrade re-applies 0008 cleanly

    5. Verify GRANT on ai_usage_log:
       ```bash
       docker compose -f docker-compose.yml -f docker-compose.dev.yml exec db \
         psql -U budget budget_db -c "\dp ai_usage_log"
       ```
       Expected: budget_app has SELECT, INSERT, UPDATE, DELETE privileges.

    6. Bring up full stack:
       ```bash
       docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
       ```
       Expected: api/bot/worker started, no startup errors in logs.

    7. (Optional smoke) Hit admin endpoint via curl с DEV_MODE:
       ```bash
       curl -s -H 'X-Telegram-Init-Data: dev-mode-stub' http://localhost/api/v1/admin/users | jq
       ```
       Expected: returns array containing owner row.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 6 steps succeeded (alembic clean, schema correct, GRANTs present, stack starts, /admin/users responds).
    Otherwise describe the failure (e.g. "alembic 0008 fails on grant — sequence ai_usage_log_id_seq missing").
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual checkpoint — frontend build + live TG MiniApp end-to-end smoke</name>
  <what-built>
    - AccessScreen (Plan 13-07) routed via App.tsx managementView='access'
    - ManagementScreen «Доступ» menu item — owner-only via useUser().role
    - Invite + Revoke flows wired to /admin/users endpoints
    - AI Usage sub-tab wired to /admin/ai-usage
  </what-built>
  <how-to-verify>
    1. Frontend build:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && \
         npm install --no-audit --no-fund 2>&1 | tail -3 && \
         npm run build 2>&1 | tail -15
       ```
       Expected: build succeeds; assets in `frontend/dist/`. No TypeScript errors.

    2. (Optional) Frontend dev preview если бэкенд запущен:
       ```bash
       cd frontend && npm run dev
       ```
       Open http://localhost:5173 — должна загружаться SPA.

    3. **Live TG MiniApp UAT** — analogous Phase 11 U-1 / Phase 12 Step 9. Может быть deferred to milestone close.

       Сценарий end-to-end (через реальный Telegram client):

       a. Open MiniApp как owner (your tg_user_id = OWNER_TG_ID).
          - Tab «Управление» → пункт «Доступ» виден ✓
          - AccessScreen → SubTabBar visible с «Пользователи» / «AI Usage»
          - Sub-tab «Пользователи» — owner row виден без revoke кнопки ✓

       b. FAB «+» → InviteSheet → ввести test tg_user_id (например, друга или второй аккаунт): 5+ цифр.
          - Submit → toast «Приглашение создано» ✓
          - Список обновился, новая строка появилась с badge «участник» ✓

       c. (Optional) Member пользователь open MiniApp:
          - Открывает /start в боте → бот говорит «Добро пожаловать» (Phase 14 покроет полностью; в Phase 13 enough если bot не падает).
          - Member заходит в MiniApp → tab «Управление» → пункт «Доступ» НЕ виден ✓

       d. Owner возвращается, sub-tab «AI Usage»:
          - Видит owner row + member row, обе с $0.00 / $5.00 (no usage yet) ✓
          - (Optional) Owner открывает AI chat и спрашивает что-то → возвращается в Доступ → AI Usage → owner row показывает > $0.00 ✓

       e. Owner отзывает member:
          - Тап на trash icon у member row → RevokeConfirmDialog с warning
          - Tap «Удалить» → row исчезает + toast «Пользователь отозван» ✓

       f. Verify cascade в psql (в dev compose db):
          ```bash
          docker compose ... exec db psql -U budget budget_db \
            -c "SELECT count(*) FROM category WHERE user_id = <revoked_id>" \
            -c "SELECT count(*) FROM app_user WHERE id = <revoked_id>"
          ```
          Expected: оба возвращают 0.

       g. Member пытается /start заново:
          - Backend получает 403 (role unknown) — bot reply «Бот приватный» ✓

    4. Sign-off:
       - All UI flows работают без visual artifacts
       - No console errors в browser devtools
       - Backend logs показывают `audit.user_invited` и `audit.user_revoked` lines
       - Cascade purge работает (psql verifies)
  </how-to-verify>
  <resume-signal>
    Type "approved" if all flows pass (build clean + UI flows green + cascade verified в psql).
    Type "deferred" if frontend build clean но live TG smoke оставлен for milestone close (analogous Phase 11/12 pattern).
    Otherwise describe issues.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Создать .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md</name>
  <files>.planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md (format reference)
    - .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md (format reference)
    - /tmp/phase13_*.log (logs from Task 1)
    - All 7 plans 13-01..13-07 (frontmatter + threat_model blocks для STRIDE aggregation)
  </read_first>
  <action>
    Создать `.planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md` со следующей структурой (заполнить реальными counts из /tmp/phase13_*.log):

    ```markdown
    ---
    phase: 13-admin-ui-whitelist-ai-usage
    status: <passed|human_needed>  # set based on Task 3 resume signal
    verified-on: 2026-05-07
    verifier: Claude executor (automated) + human (live TG MiniApp smoke)
    requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, AIUSE-01, AIUSE-02, AIUSE-03]
    ---

    # Phase 13 Verification — Admin UI: Whitelist & AI Usage

    **Phase:** 13 — Admin UI — Whitelist & AI Usage
    **Verified:** 2026-05-07
    **Verifier:** Claude executor (automated steps) + human (live TG smoke)
    **Status:** `<passed|human_needed>` — automated checks GREEN; live MiniApp UAT <signed off|deferred>.

    ## Status Routing

    - <✅|⚠> Phase 13 own automated tests: <X/Y> passed (test_admin_users_api 12 + test_admin_ai_usage_api 5 + test_ai_usage_log_hook 3 = 20 expected)
    - <✅|⚠> Phase 12 regression: <X/15> passed (auth surface intact)
    - <✅|⚠> Phase 11 regression: <X/14> passed (RLS + multitenancy intact)
    - <✅|⚠> Full pytest suite: <X passed> / <Y failed> / <Z skipped> — pass rate <N>%
    - <✅|⚠> Alembic 0008 migration cycle: <PASS|FAIL> (upgrade head ✓ / downgrade -1 ✓ / upgrade head ✓)
    - <✅|⚠> Frontend build (vite): <PASS|FAIL> (npm run build clean)
    - <✅|⚠|—> Live TG MiniApp smoke (Checkpoint 3): <PASSED|deferred>

    ## Requirements Verification

    ### ADM-01: «Доступ» visible only для owner
    - [<x|>] PASS — `frontend/src/screens/ManagementScreen.tsx`: `ITEMS.filter((it) => !it.ownerOnly || isOwner)` gating; isOwner from useUser().role
    - Evidence — Plan 13-07 Task 3 acceptance criteria

    ### ADM-02: 2 саб-таба (Пользователи / AI Usage)
    - [<x|>] PASS — `frontend/src/screens/AccessScreen.tsx` SubTabBar with TABS array
    - Evidence — Plan 13-07

    ### ADM-03: Список с last_seen_at, owner first
    - [<x|>] PASS — backend `app/services/admin_users.py::list_users` orders by (role != owner, nulls_last(desc(last_seen_at)), id); frontend `UsersList` rendering
    - Evidence — test_admin_list_users_returns_owner_and_member, test_admin_list_users_includes_required_fields

    ### ADM-04: FAB «Пригласить» → tg_user_id form
    - [<x|>] PASS — frontend InviteSheet (numeric input + 409/422 inline errors); backend POST /admin/users (Pydantic Field ge=10_000)
    - Evidence — test_admin_create_user_returns_201_with_member_role, test_admin_create_user_409_on_duplicate, test_admin_create_user_422_on_short_id

    ### ADM-05: Revoke confirm + cascade purge
    - [<x|>] PASS — frontend RevokeConfirmDialog (explicit cascade list); backend purge_user (10 tables + AppUser); self-revoke 403
    - Evidence — test_admin_delete_user_204, test_admin_delete_user_self_403, test_admin_delete_user_cascade_purges_data, test_admin_delete_user_404_unknown

    ### ADM-06: API endpoints (GET / POST / DELETE /admin/users)
    - [<x|>] PASS — `app/api/routes/admin.py` — 3 endpoints behind `Depends(require_owner)`
    - Evidence — все 12 tests из test_admin_users_api.py

    ### AIUSE-01: AI Usage sub-tab
    - [<x|>] PASS — frontend AiUsageList component with linear progress bar
    - Evidence — Plan 13-07 Task 2

    ### AIUSE-02: GET /admin/ai-usage with user-grouping
    - [<x|>] PASS — `app/services/admin_ai_usage.py::build_admin_ai_usage_breakdown` aggregates from ai_usage_log; admin DSN bypass for cross-tenant aggregation
    - Evidence — test_admin_ai_usage_returns_per_user_breakdown, test_admin_ai_usage_current_month_excludes_old_data, test_admin_ai_usage_sort_by_est_cost_desc

    ### AIUSE-03: pct_of_cap indicator (≥80% warn, ≥100% danger)
    - [<x|>] PASS — backend pct_of_cap calculation; frontend AiUsageList renders barWarn / barDanger styles based on pct
    - Evidence — test_admin_ai_usage_pct_of_cap_warns_at_80_pct, AiUsageList.module.css has .barWarn + .barDanger classes

    ## Manual Checkpoints

    - [<x|>] Step 1 — alembic 0008 upgrade head clean (Task 2 step 2)
    - [<x|>] Step 2 — schema inspection: app_user.spending_cap_cents present, app_user.last_seen_at present, ai_usage_log table + RLS policy + GRANTs (Task 2 step 3)
    - [<x|>] Step 3 — alembic 0008 downgrade/upgrade round-trip (Task 2 step 4)
    - [<x|>] Step 4 — GRANTS verified for budget_app on ai_usage_log (Task 2 step 5)
    - [<x|>] Step 5 — Full stack startup clean (Task 2 step 6)
    - [<x|>] Step 6 — frontend npm build clean (Task 3 step 1)
    - [< |x|>] Step 7 — Live TG MiniApp end-to-end (Task 3 step 3) — <PASSED|deferred to milestone close>

    ## Threat Model Attestation

    Aggregated STRIDE register from Plans 13-01..13-07.

    | Threat ID | Category | Component | Disposition | Evidence |
    |-----------|----------|-----------|-------------|----------|
    | T-13-01-04 | E | Member calls admin endpoint | mitigate | test_admin_*_403_for_member (4 tests) |
    | T-13-01-05 | E | Owner self-revoke locks admin | mitigate | test_admin_delete_user_self_403; route guard `if user_id == current_user.id` |
    | T-13-02-01 | T | Migration backfill loses existing rows | mitigate | server_default fills retroactively; alembic upgrade verified clean |
    | T-13-02-02 | I | RLS leaks cross-user via aggregation | mitigate | RLS policy with coalesce-NULLIF -1 sentinel; admin breakdown uses ADMIN_DATABASE_URL bypass |
    | T-13-02-03 | E | budget_app excess privileges | mitigate | Grants restricted to SELECT/INSERT/UPDATE/DELETE on ai_usage_log; no ALTER/TRIGGER |
    | T-13-03-01 | T | Malformed usage dict crashes hook | mitigate | .get() with defaults; int/float coercion handles None |
    | T-13-03-02 | D | DB outage cascades to SSE failure | mitigate | try/except в _record_usage; logger.warning; SSE continues |
    | T-13-03-03 | I | Hook writes wrong user_id | mitigate | user_id from get_current_user_id dependency, not client-supplied |
    | T-13-04-01 | E | Member calls /admin/* | mitigate | router-level Depends(require_owner) → 403 |
    | T-13-04-02 | E | Owner self-revoke | mitigate | Route check before service call |
    | T-13-04-03 | T | Race: duplicate invite | mitigate | invite_user catches IntegrityError → 409 |
    | T-13-04-04 | I | Cascade leaks other users' data | mitigate | DELETE WHERE user_id = :uid; test_cascade_purges_data verifies owner intact |
    | T-13-04-06 | T | Service RLS bypass without auth | mitigate | Service called only from /admin/* routes |
    | T-13-05-01 | E | Member sees AI usage | mitigate | router-level require_owner |
    | T-13-05-02 | I | SUPERUSER engine leak | mitigate | engine.dispose in finally; response only to owner |
    | T-13-05-03 | T | Time window boundary errors | mitigate | UTC+3 fixed offset; test_current_month_excludes_old_data |
    | T-13-05-07 | S | Engine pool collision | mitigate | New engine per call + dispose |
    | T-13-06-02 | T | Optimistic revoke not rolled back | mitigate | snapshot taken pre-setState; restored in catch |
    | T-13-07-01 | E | Stale role caches admin menu | mitigate | useUser refetch; ManagementScreen reads .role on render |
    | T-13-07-04 | E | Self-revoke via devtools | mitigate | Backend 403 (T-13-04-02) — UI gate is convenience |

    Threats accepted (rationale):

    - T-13-02-04 (D — ai_usage_log unbounded growth) — Phase 15 will add retention; volume manageable until then
    - T-13-02-05 (R — spending_cap edits not audit-logged) — Phase 15 introduces PATCH cap with audit
    - T-13-04-05 (I — audit log contains tg_user_id) — tg_user_id not PII per CONTEXT closed whitelist
    - T-13-04-07 (D — cascade DELETE on huge user) — MVP scale 5-50 users
    - T-13-04-08 (R — only stderr audit) — full audit_log table deferred (AUD-01)
    - T-13-05-04 (D — admin breakdown query time) — MVP scale acceptable
    - T-13-05-05 (I — cap_cents in admin response) — admin needs visibility
    - T-13-05-06 (E — ADMIN_DATABASE_URL fallback to DATABASE_URL in dev) — dev runs as superuser already
    - T-13-06-01 (I — types.ts drift from backend) — manual mirror process; tsc fails on response shape mismatch
    - T-13-07-03 (I — toast shows raw error) — single-tenant owner

    ## Resolved Items

    - Phase 12 D-11-07-01 (legacy fixture sweep) — already RESOLVED (carry-over check)
    - Phase 12 D-11-07-02 (Postgres role split) — already RESOLVED (carry-over check)

    ## Out of Scope (deferred to later phases)

    - Phase 14: Onboarding flow для приглашённых member юзеров (bot bind → starting_balance → cycle_start_day → seed categories + embeddings)
    - Phase 15: spending_cap_cents enforcement → 429 (currently shipped как stub default 46500); PATCH /admin/users/{id}/cap для редактирования
    - Phase 15+: Audit log table (AUD-01) — currently structured log lines only
    - Phase 14: name field из tg_chat_id — currently `name=null` для всех users в admin breakdown

    ## Notes / Issues found

    <Записать любые обнаруженные deviations from plan здесь. Если ничего — "None.">

    ## Sign-off

    - Date: 2026-05-07
    - Status: `<passed|human_needed>`
    - Outstanding: <none|live TG MiniApp smoke (Step 7) deferred to milestone close>
    - Phase 13 functionally complete; admin whitelist + AI usage breakdown shipped end-to-end через Mini App; cascade purge verified в psql.
    - Ready for Phase 14 (Multi-Tenant Onboarding) — invitee flow реализуется поверх /admin/users invite endpoint shipped here.

    ---
    *Verification completed: 2026-05-07*
    ```

    Заполнить плейсхолдеры `<X>`, `<status>`, `<x|>` фактическими значениями из Task 1 logs + Task 2/3 checkpoint resume signals.

    Self-checks:
    - File exists с frontmatter
    - All 9 requirements (ADM-01..06 + AIUSE-01..03) present в Requirements Verification section
    - alembic 0008 mention в Manual Checkpoints
    - Threat model table содержит ≥ 15 STRIDE entries
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && grep -c "ADM-0\|AIUSE-0" .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md` exists
    - frontmatter содержит phase, status, verified-on, verifier, requirements
    - `grep -c "ADM-0\|AIUSE-0" 13-VERIFICATION.md` → ≥ 18 (each ID minimum дважды — req section + summary)
    - `grep -c "alembic 0008\|0008_admin_phase13" 13-VERIFICATION.md` → ≥ 2
    - `grep -c "T-13-" 13-VERIFICATION.md` → ≥ 15 (STRIDE register)
    - Status set explicitly to `passed` либо `human_needed` based on resume signals
  </acceptance_criteria>
  <done>
    13-VERIFICATION.md создан; status set; STRIDE attestation complete; ready for git commit.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md + ROADMAP.md + commit</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md</files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/ROADMAP.md (Phase 13 entry)
    - .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md (Task 4)
  </read_first>
  <action>
    1. Update `.planning/STATE.md`:
       - frontmatter: `stopped_at: Phase 13 complete — 13-VERIFICATION.md status=<passed|human_needed>`; `last_activity: 2026-05-07 -- Phase 13 verification complete`; `last_updated: <ISO datetime>`
       - progress: completed_phases 3 (was 2); completed_plans 22 (was 14, +8 from Phase 13)
       - Section "Current Position": Phase 14 (multi-tenant-onboarding) — NOT STARTED
       - Section "Performance Metrics": add Phase 13 row to By Phase table; total_plans 22, by phase row "13 | 8 | est ~120 min | ~15 min/plan"
       - Section "Accumulated Context > Decisions": add v0.4 (2026-05-07) decision entries from Phase 13 (e.g. "ai_usage_log persistent table; ADMIN_DATABASE_URL bypass для cross-tenant aggregation; UI cascade-list explicit; self-revoke forbidden by route guard")
       - Section "Session Continuity": Stopped at Phase 13 complete; resume Phase 14 next.

    2. Update `.planning/ROADMAP.md`:
       - В section "🚧 v0.4 Multi-Tenant & Admin (Active)": изменить `[ ] **Phase 13: ...` на `[x] **Phase 13: ... — 8/8 plans complete; status=<passed|human_needed>`
       - В section "Phase Details > Phase 13": заполнить `**Plans**: 8 plans` и список плеинов:
         ```
         **Plans**: 8 plans
         - [x] 13-01-PLAN.md — RED tests + helper extension (Wave 1, parallel with 13-02)
         - [x] 13-02-PLAN.md — Alembic 0008 (spending_cap_cents + ai_usage_log + RLS + grants) (Wave 1)
         - [x] 13-03-PLAN.md — _record_usage hook → ai_usage_log persistence (Wave 2)
         - [x] 13-04-PLAN.md — Admin users CRUD service+routes + cascade purge (Wave 2, parallel with 13-03)
         - [x] 13-05-PLAN.md — Admin AI usage breakdown service+endpoint (Wave 3)
         - [x] 13-06-PLAN.md — Frontend admin types + API + hooks (Wave 4)
         - [x] 13-07-PLAN.md — Frontend AccessScreen + components + integration (Wave 5)
         - [x] 13-08-PLAN.md — Verification: pytest + alembic + frontend build + 13-VERIFICATION.md (Wave 6, has human checkpoints)
         ```
       - В section "Progress > Milestone v0.4 (Active)": Phase 13 row → `8/8 | <Complete|In Progress>`

    3. Git commit:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         git add .planning/phases/13-admin-ui-whitelist-ai-usage/13-VERIFICATION.md \
                 .planning/STATE.md \
                 .planning/ROADMAP.md && \
         git commit -m "docs(phase-13): verification + STATE/ROADMAP update for Phase 13 close"
       ```

    Commit message reflects status (passed vs human_needed).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && git log --oneline -1 && grep -c "Phase 13" .planning/STATE.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md обновлён (completed_phases 3, last_activity 2026-05-07 Phase 13)
    - ROADMAP.md Phase 13 → `[x]` checked, plans list 8 items, progress table reflects
    - 13-VERIFICATION.md committed
    - Git log shows commit for Phase 13 close
  </acceptance_criteria>
  <done>
    STATE/ROADMAP synced; verification artifact persisted in git; Phase 13 closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration apply (alembic) | Operator runs upgrade in dev; production-grade test deferred to staging/prod stack |
| Live TG smoke | Real Telegram client + dev stack via Cloudflare Tunnel + Tailscale (memory infra-deploy.md) |
| Test logs in /tmp/ | Local-only, not committed; provides Task 4 evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08-01 | T (Tampering) | 13-VERIFICATION.md says PASS but tests are failing | mitigate | Task 4 reads /tmp/phase13_*.log directly; counts must match acceptance criteria |
| T-13-08-02 | R (Repudiation) | No git evidence of verification artifact | mitigate | Task 5 commits 13-VERIFICATION.md + STATE.md + ROADMAP.md atomically |
| T-13-08-03 | I (Info Disclosure) | Verification log accidentally includes BOT_TOKEN | accept | /tmp/ logs не committed; if pytest captures token in error message, dev can scrub |

(Wave 6 — verification + documentation only. Production threats already mitigated by Plans 13-02..13-07.)
</threat_model>

<verification>
- 13-VERIFICATION.md создан с frontmatter
- Task 1 logs показывают actual pass counts
- Task 2 alembic migration applied + downgrade/upgrade verified
- Task 3 frontend build clean + (optional) live TG smoke
- Task 5 STATE/ROADMAP updated + commit pushed
</verification>

<success_criteria>
- 13-VERIFICATION.md exists с frontmatter status=passed|human_needed
- All 9 requirement IDs traced (ADM-01..06 + AIUSE-01..03)
- Threat model attestation block aggregated from Plans 13-01..13-07
- alembic 0008 fully applied + round-trip verified
- Frontend build clean (vite + tsc)
- Manual checkpoints (2 blocking) reached
- STATE.md + ROADMAP.md updated
- Git commit closes Phase 13
- Phase 13 closed, ready для Phase 14 onboarding
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ui-whitelist-ai-usage/13-08-SUMMARY.md`
</output>
