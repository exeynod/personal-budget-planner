---
phase: 13-admin-ui-whitelist-ai-usage
plan: "04"
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - app/api/schemas/admin.py
  - app/services/admin_users.py
  - app/api/routes/admin.py
  - app/api/router.py
  - app/db/models.py
autonomous: true
requirements: [ADM-03, ADM-04, ADM-05, ADM-06]
tags: [admin, routes, service-layer, cascade-purge, rbac]
must_haves:
  truths:
    - "GET /api/v1/admin/users возвращает 200 + JSON list для owner; owner-row first, member-rows sorted by last_seen_at desc; каждая строка содержит id, tg_user_id, tg_chat_id, role, last_seen_at, onboarded_at, created_at"
    - "POST /api/v1/admin/users {tg_user_id: int} → 201 с AdminUserResponse при успехе (создаёт AppUser role=member); 409 при duplicate tg_user_id; 422 при tg_user_id < 10000 (FastAPI validation)"
    - "DELETE /api/v1/admin/users/{user_id} → 204 при успехе; 403 если user_id == current_user.id (self-revoke); 404 при unknown user_id; cascade purge всех 9 доменных таблиц + ai_usage_log + AppUser"
    - "Все 3 endpoint'а защищены `Depends(require_owner)` → member/revoked получают 403"
    - "Cascade purge service `purge_user(db, user_id)` удаляет в правильном порядке (FK-aware): ai_message → ai_conversation → category_embedding → planned_transaction → actual_transaction → plan_template_item → subscription → budget_period → category → ai_usage_log (CASCADE безопасно но explicit) → app_user; возвращает dict с counts по таблицам"
    - "Audit log: каждое DELETE логирует строку `audit.user_revoked uid=<deleted_id> by_owner=<owner_id> purged_rows={...}` через logger.info"
    - "AppUser получает колонку last_seen_at: Mapped[Optional[datetime]] (TIMESTAMPTZ nullable). Phase 13 НЕ обновляет last_seen_at автоматически (это Phase 14 будет — bot bind / first /me); пока для всех existing rows last_seen_at = None"
    - "Tests из Plan 13-01 (test_admin_users_api.py) — 12/12 GREEN после этого плана"
  artifacts:
    - path: app/api/schemas/admin.py
      provides: "Pydantic schemas: AdminUserResponse, AdminUserCreateRequest"
      contains: "class AdminUserResponse"
      contains_2: "class AdminUserCreateRequest"
      contains_3: "ge=10_000"
    - path: app/services/admin_users.py
      provides: "Service-layer: list_users, invite_user, purge_user (cascade)"
      contains: "async def purge_user"
      contains_2: "async def invite_user"
    - path: app/api/routes/admin.py
      provides: "FastAPI router /admin/users CRUD with require_owner dep"
      contains: "admin_router"
      contains_2: "Depends(require_owner)"
      contains_3: "self-revoke"
    - path: app/api/router.py
      provides: "Mount admin_router в public_router"
      contains: "admin_router"
    - path: app/db/models.py
      provides: "AppUser.last_seen_at column added"
      contains: "last_seen_at"
  key_links:
    - from: app/api/routes/admin.py
      to: app/api/dependencies.py::require_owner
      via: "router-level Depends(require_owner) — RBAC gate for ALL endpoints in admin_router"
      pattern: "dependencies=\\[Depends\\(require_owner\\)\\]"
    - from: app/api/routes/admin.py::revoke_user
      to: app/services/admin_users.py::purge_user
      via: "Service-layer cascade delete, route only orchestrates HTTP semantics + self-revoke check"
      pattern: "await purge_user"
    - from: app/api/router.py
      to: app/api/routes/admin.py
      via: "include_router(admin_router) — admin endpoints become available at /api/v1/admin/*"
      pattern: "from app.api.routes.admin import admin_router"
    - from: app/api/schemas/admin.py::AdminUserCreateRequest
      to: pydantic.Field(ge=10_000)
      via: "Min 5-digit tg_user_id validation per CONTEXT (no @username support)"
      pattern: "ge=10_000"
---

<objective>
Wave 2 backend: реализация admin users CRUD + cascade purge сервиса. Этот план переводит 12 RED тестов из Plan 13-01 (`tests/test_admin_users_api.py`) в GREEN.

Core artifacts:
1. `app/api/schemas/admin.py` — Pydantic v2 schemas (response + request)
2. `app/services/admin_users.py` — service layer: list / invite / cascade purge
3. `app/api/routes/admin.py` — FastAPI router с `Depends(require_owner)` на router-уровне
4. `app/api/router.py` — mount admin_router в public_router
5. `app/db/models.py` — добавить `AppUser.last_seen_at` колонку

Cascade purge — критическая часть ADM-05. Не используем DB ON DELETE CASCADE (per Plan 11-02 D-NOTE: domain tables используют RESTRICT для defensive depth), кроме `ai_usage_log` который CASCADE per Plan 13-02. Service-layer удаляет в правильном FK-aware порядке.

Self-revoke check на route-уровне (НЕ на service-уровне) — это HTTP semantic concern (хотим вернуть 403 а не 500). Реализуется через сравнение `user_id == current_user.id` в роуте.

Audit log — пока structured log line (полная audit_log table — deferred per CONTEXT.md).

Output: 5 файлов модифицированы; pytest tests/test_admin_users_api.py — 12/12 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-CONTEXT.md
@.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
@CLAUDE.md
@app/api/dependencies.py
@app/api/router.py
@app/db/models.py
@app/api/schemas/categories.py
@app/api/routes/categories.py
@app/services/onboarding.py
@tests/test_admin_users_api.py

<interfaces>
<!-- Pydantic v2 schema pattern (см. app/api/schemas/categories.py) -->

```python
# app/api/schemas/admin.py — NEW

from __future__ import annotations
from datetime import datetime
from typing import Literal, Optional
from pydantic import BaseModel, ConfigDict, Field


class AdminUserResponse(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: int
    tg_user_id: int
    tg_chat_id: Optional[int] = None
    role: Literal["owner", "member", "revoked"]
    last_seen_at: Optional[datetime] = None  # ISO datetime
    onboarded_at: Optional[datetime] = None
    created_at: datetime


class AdminUserCreateRequest(BaseModel):
    model_config = ConfigDict(extra="forbid")

    # ge=10_000 → minimum 5 digits per CONTEXT decision (no @username support).
    # FastAPI returns 422 if tg_user_id < 10000 or non-int.
    tg_user_id: int = Field(..., ge=10_000)
```

```python
# app/services/admin_users.py — NEW

from __future__ import annotations
import logging
from datetime import datetime
from typing import Optional

from sqlalchemy import desc, nulls_last, select, text
from sqlalchemy.exc import IntegrityError
from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import AppUser, UserRole

logger = logging.getLogger(__name__)


class UserAlreadyExistsError(Exception):
    """tg_user_id уже в whitelist (409)."""


class UserNotFoundError(Exception):
    """user_id не существует (404)."""


async def list_users(db: AsyncSession) -> list[AppUser]:
    """ADM-03: список whitelist'а — owner first, members by last_seen_at desc.

    NB: admin endpoint обходит per-user RLS scope (читает все строки app_user).
    app_user — НЕ scoped по user_id (нет такой колонки), так что прямой
    SELECT работает вне зависимости от GUC.
    """
    # owner first via CASE; затем last_seen_at desc NULLS LAST; затем id asc.
    stmt = (
        select(AppUser)
        .order_by(
            (AppUser.role != UserRole.owner),  # False (owner) sorts before True (member/revoked)
            nulls_last(desc(AppUser.last_seen_at)),
            AppUser.id,
        )
    )
    result = await db.execute(stmt)
    return list(result.scalars().all())


async def invite_user(db: AsyncSession, *, tg_user_id: int) -> AppUser:
    """ADM-04: создать AppUser с role=member, onboarded_at=NULL.

    Phase 14 заполнит onboarded_at + tg_chat_id когда юзер сам пройдёт
    onboarding flow в Mini App.

    Raises UserAlreadyExistsError если tg_user_id уже существует (409).
    """
    existing = await db.execute(
        select(AppUser).where(AppUser.tg_user_id == tg_user_id)
    )
    if existing.scalar_one_or_none() is not None:
        raise UserAlreadyExistsError(f"tg_user_id={tg_user_id} already in whitelist")

    new_user = AppUser(
        tg_user_id=tg_user_id,
        role=UserRole.member,
        cycle_start_day=5,  # default; Phase 14 onboarding позволит юзеру изменить
    )
    db.add(new_user)
    try:
        await db.flush()
    except IntegrityError as exc:
        # Race condition защита (один tg_user_id вставлен между select и insert).
        await db.rollback()
        raise UserAlreadyExistsError(
            f"tg_user_id={tg_user_id} already exists (race)"
        ) from exc
    await db.refresh(new_user)
    return new_user


# Domain tables to purge before app_user. ORDER MATTERS — children first.
# ai_message → ai_conversation: ai_message.conversation_id FK
# planned/actual → budget_period (budget_period.id FK), category (category.id FK)
# subscription, plan_template_item → category (category.id FK)
# category_embedding → category (category.id FK CASCADE — но явно для clarity)
# subscription, plan_template_item, planned_transaction, actual_transaction,
#   budget_period, category — domain tables (user_id RESTRICT FK)
_PURGE_TABLES_ORDERED = (
    "ai_message",
    "ai_conversation",
    "category_embedding",  # FK to category (CASCADE) — но удаляем явно для счёта
    "planned_transaction",
    "actual_transaction",
    "plan_template_item",
    "subscription",
    "budget_period",
    "category",
    "ai_usage_log",  # FK to app_user CASCADE — explicit для счёта
)


async def purge_user(db: AsyncSession, *, user_id: int) -> dict[str, int]:
    """ADM-05: cascade-удалить все данные юзера + AppUser строку.

    Returns: dict {table_name: rows_deleted, "app_user": 1}.

    Raises UserNotFoundError если user_id не существует.

    Implementation: explicit DELETE per table в FK-safe порядке. Это даёт
    нам count rows для audit log + работает независимо от ON DELETE настройки.

    NB: handler уже под Depends(require_owner) → caller гарантированно owner.
    Self-revoke проверяется на route-уровне, не здесь.
    """
    # 1. Verify exists.
    user_exists = await db.execute(
        select(AppUser.id).where(AppUser.id == user_id)
    )
    if user_exists.scalar_one_or_none() is None:
        raise UserNotFoundError(f"user_id={user_id} not found")

    counts: dict[str, int] = {}
    for table in _PURGE_TABLES_ORDERED:
        result = await db.execute(
            text(f"DELETE FROM {table} WHERE user_id = :uid"),
            {"uid": user_id},
        )
        counts[table] = result.rowcount or 0

    # Last: app_user itself.
    final = await db.execute(
        text("DELETE FROM app_user WHERE id = :uid"),
        {"uid": user_id},
    )
    counts["app_user"] = final.rowcount or 0
    await db.flush()
    return counts
```

```python
# app/api/routes/admin.py — NEW

"""Admin endpoints (Phase 13 ADM-03..06).

Owner-only via Depends(require_owner). All endpoints under /api/v1/admin/*.
"""
from __future__ import annotations
import logging
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_db, require_owner
from app.api.schemas.admin import AdminUserCreateRequest, AdminUserResponse
from app.db.models import AppUser
from app.services import admin_users as admin_svc

logger = logging.getLogger(__name__)

admin_router = APIRouter(
    prefix="/admin",
    tags=["admin"],
    dependencies=[Depends(require_owner)],  # router-level RBAC
)


@admin_router.get("/users", response_model=list[AdminUserResponse])
async def list_admin_users(
    db: Annotated[AsyncSession, Depends(get_db)],
    _owner: Annotated[AppUser, Depends(require_owner)],
) -> list[AppUser]:
    """ADM-03 + ADM-06: список whitelist'а с last_seen_at."""
    return await admin_svc.list_users(db)


@admin_router.post(
    "/users",
    response_model=AdminUserResponse,
    status_code=status.HTTP_201_CREATED,
)
async def create_admin_user(
    payload: AdminUserCreateRequest,
    db: Annotated[AsyncSession, Depends(get_db)],
    current_user: Annotated[AppUser, Depends(require_owner)],
) -> AppUser:
    """ADM-04 + ADM-06: invite по tg_user_id; 409 если duplicate."""
    try:
        new_user = await admin_svc.invite_user(db, tg_user_id=payload.tg_user_id)
    except admin_svc.UserAlreadyExistsError as exc:
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail="tg_user_id already in whitelist",
        ) from exc
    logger.info(
        "audit.user_invited tg_user_id=%s new_id=%s by_owner=%s",
        payload.tg_user_id, new_user.id, current_user.id,
    )
    return new_user


@admin_router.delete(
    "/users/{user_id}",
    status_code=status.HTTP_204_NO_CONTENT,
)
async def delete_admin_user(
    user_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
    current_user: Annotated[AppUser, Depends(require_owner)],
) -> None:
    """ADM-05 + ADM-06: revoke + cascade purge.

    Self-revoke forbidden — owner cannot delete own account (would lock
    admin access permanently). 403 returned with explicit message.
    """
    if user_id == current_user.id:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="Owner cannot revoke own account",
        )
    try:
        counts = await admin_svc.purge_user(db, user_id=user_id)
    except admin_svc.UserNotFoundError as exc:
        raise HTTPException(
            status_code=status.HTTP_404_NOT_FOUND,
            detail="user_id not found",
        ) from exc
    logger.info(
        "audit.user_revoked uid=%s by_owner=%s purged_rows=%s",
        user_id, current_user.id, counts,
    )
```

```python
# app/api/router.py — change

# After existing include_router calls:
from app.api.routes.admin import admin_router  # ADD this import
public_router.include_router(admin_router)  # ADD this line
```

`get_db` (not `get_db_with_tenant_scope`) для admin endpoints — потому что:
1. `app_user` table не имеет user_id, RLS не применима (нет policy)
2. Domain tables (для purge) — Plan 13-04 обходит RLS через explicit `WHERE user_id = :uid` в DELETE; не нужен SET LOCAL потому что admin читает rows нескольких юзеров
3. ai_usage_log — RLS scoped по user_id; в purge мы используем `text("DELETE FROM ai_usage_log WHERE user_id = :uid")` — это DELETE будет blocked by RLS если current_setting('app.current_user_id') не выставлен. Нужно либо bypass через privileged role, либо использовать explicit `set_config(...)` перед DELETE.

ВАРИАНТ A — обойти RLS через ALTER ROLE:
- Не годится: budget_app не bypass RLS by design.

ВАРИАНТ B — установить app.current_user_id перед каждым DELETE:
- В service `purge_user`, перед циклом DELETE, выполнить `SELECT set_config('app.current_user_id', :uid, true)` (transaction-scoped). Это разрешит RLS для tenant `:uid`. Этот pattern уже использовали в worker/AI scoping (Plan 11-06).

Чтобы избежать confusion, делаем: в `purge_user` ставим scope перед DELETE:
```python
await db.execute(
    text("SELECT set_config('app.current_user_id', :uid, true)"),
    {"uid": str(user_id)},
)
```
Это работает для domain tables + ai_usage_log (все они user_id RLS-scoped). После DELETE GUC сбрасывается на commit (transaction-scoped, `is_local=true`).

`app_user` DELETE — последний шаг, не имеет RLS на himself, gets through.

Final purge_user implementation замечание (добавить в скелет выше):
```python
async def purge_user(db: AsyncSession, *, user_id: int) -> dict[str, int]:
    # ... verify exists ...

    # Set tenant scope for RLS (transaction-scoped GUC).
    await db.execute(
        text("SELECT set_config('app.current_user_id', :uid, true)"),
        {"uid": str(user_id)},
    )

    counts: dict[str, int] = {}
    for table in _PURGE_TABLES_ORDERED:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать app/api/schemas/admin.py + app/services/admin_users.py + добавить last_seen_at в models.py</name>
  <files>app/api/schemas/admin.py, app/services/admin_users.py, app/db/models.py</files>
  <read_first>
    - app/api/schemas/categories.py (Pydantic v2 pattern: model_config, ConfigDict)
    - app/services/onboarding.py (service exception pattern: AlreadyOnboardedError, OnboardingUserNotFoundError)
    - app/db/models.py (AppUser current shape — куда вставить last_seen_at)
    - alembic/versions/0008_admin_phase13.py (Plan 13-02 миграция — нет last_seen_at там; нужно добавить колонку либо в 0008 либо в 0009)
  </read_first>
  <action>
    Изменения в трёх файлах:

    A) `app/db/models.py` — добавить колонку `last_seen_at` в `AppUser`. Местоположение: ПОСЛЕ `onboarded_at` и ПЕРЕД `created_at`.

    Текущий блок (около строки 104):
    ```python
        onboarded_at: Mapped[Optional[datetime]] = mapped_column(
            TIMESTAMP(timezone=True), nullable=True
        )
        created_at: Mapped[datetime] = mapped_column(
            TIMESTAMP(timezone=True), server_default=func.now(), nullable=False
        )
    ```

    Новая версия:
    ```python
        onboarded_at: Mapped[Optional[datetime]] = mapped_column(
            TIMESTAMP(timezone=True), nullable=True
        )
        last_seen_at: Mapped[Optional[datetime]] = mapped_column(
            TIMESTAMP(timezone=True), nullable=True
        )
        created_at: Mapped[datetime] = mapped_column(
            TIMESTAMP(timezone=True), server_default=func.now(), nullable=False
        )
    ```

    NB: alembic 0008 (Plan 13-02) НЕ добавляет last_seen_at колонку. Поэтому нужно расширить 0008 миграцию, ИЛИ добавить отдельную 0009. Чтобы не мутировать 0008 (его уже могли применить в dev) — выбираем расширение 0008 ВНУТРИ Plan 13-04 ТОЛЬКО ЕСЛИ Plan 13-02 ещё не применён в dev. На практике Plans Wave 1 и Wave 2 применяются в одном execution sweep — после revision возможно. Безопаснее: добавить ALTER в 0008.

    РЕШЕНИЕ: расширить `alembic/versions/0008_admin_phase13.py` upgrade функцию — добавить `op.add_column("app_user", sa.Column("last_seen_at", sa.TIMESTAMP(timezone=True), nullable=True))` ПЕРЕД add_column для spending_cap_cents (порядок не критичен, но logical: last_seen_at рядом с onboarded_at). Зеркально в downgrade — drop_column("app_user", "last_seen_at") ДО drop spending_cap_cents.

    Корректировка для 13-04: Этот план модифицирует `alembic/versions/0008_admin_phase13.py` (вернее уведомляет executor что это допустимое расширение). Альтернатива — отдельный 0009 миграция; но это создаёт лишний rollback. Идём с расширением 0008.

    Изменения в `alembic/versions/0008_admin_phase13.py` (был создан Plan 13-02):
    В функции `upgrade()` ПЕРВОЙ строкой (до spending_cap_cents add_column) добавить:
    ```python
        # AppUser.last_seen_at — Phase 13 admin breakdown (last login indicator).
        # Phase 14 will populate via /me handler; for now NULL for existing rows.
        op.add_column(
            "app_user",
            sa.Column("last_seen_at", sa.TIMESTAMP(timezone=True), nullable=True),
        )
    ```

    В функции `downgrade()` ПОСЛЕДНЕЙ строкой (после drop spending_cap_cents) добавить:
    ```python
        op.drop_column("app_user", "last_seen_at")
    ```

    B) Создать `app/api/schemas/admin.py` (новый файл):
    ```python
    """Pydantic schemas для admin endpoints (Phase 13 ADM-03..06)."""
    from __future__ import annotations

    from datetime import datetime
    from typing import Literal, Optional

    from pydantic import BaseModel, ConfigDict, Field


    class AdminUserResponse(BaseModel):
        """One whitelist row для GET /admin/users."""

        model_config = ConfigDict(from_attributes=True)

        id: int
        tg_user_id: int
        tg_chat_id: Optional[int] = None
        role: Literal["owner", "member", "revoked"]
        last_seen_at: Optional[datetime] = None
        onboarded_at: Optional[datetime] = None
        created_at: datetime


    class AdminUserCreateRequest(BaseModel):
        """Body для POST /admin/users — invite by tg_user_id.

        ge=10_000 enforces "min 5 digits" per CONTEXT decision; @username
        not supported in Phase 13. FastAPI returns 422 on validation fail.
        """

        model_config = ConfigDict(extra="forbid")

        tg_user_id: int = Field(..., ge=10_000)
    ```

    C) Создать `app/services/admin_users.py` (новый файл — full text per <interfaces> блок выше):

    Полный текст файла:

    ```python
    """Admin users service-layer (Phase 13 ADM-03..06).

    Service operations:
      - list_users(db) — owner-first sort, then last_seen_at desc NULLS LAST.
      - invite_user(db, tg_user_id) — create AppUser(role=member); 409 on dup.
      - purge_user(db, user_id) — cascade delete all 9 domain tables +
                                  ai_usage_log + AppUser. RLS scope set
                                  via set_config inside the transaction.

    Self-revoke check is performed at route layer (HTTP-semantic concern).

    Exceptions:
      UserAlreadyExistsError → mapped to 409 in routes
      UserNotFoundError → mapped to 404 in routes
    """
    from __future__ import annotations

    import logging

    from sqlalchemy import desc, nulls_last, select, text
    from sqlalchemy.exc import IntegrityError
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.db.models import AppUser, UserRole

    logger = logging.getLogger(__name__)


    class UserAlreadyExistsError(Exception):
        """tg_user_id уже в whitelist (409)."""


    class UserNotFoundError(Exception):
        """user_id не существует (404)."""


    async def list_users(db: AsyncSession) -> list[AppUser]:
        stmt = (
            select(AppUser)
            .order_by(
                (AppUser.role != UserRole.owner),  # owner-first
                nulls_last(desc(AppUser.last_seen_at)),
                AppUser.id,
            )
        )
        result = await db.execute(stmt)
        return list(result.scalars().all())


    async def invite_user(db: AsyncSession, *, tg_user_id: int) -> AppUser:
        existing = await db.execute(
            select(AppUser).where(AppUser.tg_user_id == tg_user_id)
        )
        if existing.scalar_one_or_none() is not None:
            raise UserAlreadyExistsError(
                f"tg_user_id={tg_user_id} already in whitelist"
            )

        new_user = AppUser(
            tg_user_id=tg_user_id,
            role=UserRole.member,
            cycle_start_day=5,
        )
        db.add(new_user)
        try:
            await db.flush()
        except IntegrityError as exc:
            await db.rollback()
            raise UserAlreadyExistsError(
                f"tg_user_id={tg_user_id} already exists (race)"
            ) from exc
        await db.refresh(new_user)
        return new_user


    # FK-safe order: children first, parents last. Each table is user_id-scoped.
    _PURGE_TABLES_ORDERED = (
        "ai_message",
        "ai_conversation",
        "category_embedding",
        "planned_transaction",
        "actual_transaction",
        "plan_template_item",
        "subscription",
        "budget_period",
        "category",
        "ai_usage_log",
    )


    async def purge_user(db: AsyncSession, *, user_id: int) -> dict[str, int]:
        """Cascade-delete all rows owned by user_id, then AppUser itself.

        Sets app.current_user_id GUC for the active transaction so RLS
        policies on each domain table allow the DELETE. Returns row counts
        per table (for audit logging).
        """
        # Verify the user exists before doing any work.
        existing = await db.execute(
            select(AppUser.id).where(AppUser.id == user_id)
        )
        if existing.scalar_one_or_none() is None:
            raise UserNotFoundError(f"user_id={user_id} not found")

        # Set transaction-scoped tenant GUC so RLS policies allow our DELETE.
        # (Plan 11-06 pattern; SET LOCAL via set_config function form.)
        await db.execute(
            text("SELECT set_config('app.current_user_id', :uid, true)"),
            {"uid": str(user_id)},
        )

        counts: dict[str, int] = {}
        for table in _PURGE_TABLES_ORDERED:
            result = await db.execute(
                text(f"DELETE FROM {table} WHERE user_id = :uid"),
                {"uid": user_id},
            )
            counts[table] = result.rowcount or 0

        # AppUser table itself — no RLS, no scope dependency.
        final = await db.execute(
            text("DELETE FROM app_user WHERE id = :uid"),
            {"uid": user_id},
        )
        counts["app_user"] = final.rowcount or 0
        await db.flush()
        return counts
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.schemas.admin import AdminUserResponse, AdminUserCreateRequest; from app.services.admin_users import list_users, invite_user, purge_user, UserAlreadyExistsError, UserNotFoundError; from app.db.models import AppUser; assert hasattr(AppUser, 'last_seen_at'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/schemas/admin.py exists, imports work, both classes present
    - app/services/admin_users.py exists, exports list_users / invite_user / purge_user / UserAlreadyExistsError / UserNotFoundError
    - app/db/models.py: `grep -c "last_seen_at" app/db/models.py` → 1
    - alembic 0008 модифицирован: `grep -c "last_seen_at" alembic/versions/0008_admin_phase13.py` → 2 (upgrade + downgrade)
    - `grep -c "ge=10_000" app/api/schemas/admin.py` → 1
    - `grep -c "set_config" app/services/admin_users.py` → 1 (set tenant scope для purge)
    - `grep -c "_PURGE_TABLES_ORDERED" app/services/admin_users.py` → ≥ 2 (definition + iteration)
    - Python import test passes (verify automated above)
  </acceptance_criteria>
  <done>
    Schemas + service layer + last_seen_at column готовы; module importable; alembic 0008 расширен.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать app/api/routes/admin.py + mount в app/api/router.py</name>
  <files>app/api/routes/admin.py, app/api/router.py</files>
  <read_first>
    - app/api/routes/categories.py (FastAPI route pattern: prefix, dependencies, schema response_model)
    - app/api/router.py (existing include_router calls — где добавить admin_router)
    - app/api/dependencies.py (require_owner signature)
    - app/api/schemas/admin.py (создан в Task 1)
    - app/services/admin_users.py (создан в Task 1)
  </read_first>
  <action>
    A) Создать `app/api/routes/admin.py` (полный файл):

    ```python
    """Admin endpoints (Phase 13 ADM-03..06).

    Owner-only via Depends(require_owner) at router level.
    All endpoints under /api/v1/admin/* (mounted in app/api/router.py).

    Endpoints:
      GET    /admin/users          list whitelist
      POST   /admin/users          invite by tg_user_id (creates role=member)
      DELETE /admin/users/{id}     revoke + cascade purge

    Self-revoke (owner deleting own account) is forbidden — returns 403
    with explicit detail. This guards against accidental admin lockout.

    Plan 13-05 will extend this router with /admin/ai-usage breakdown.
    """
    from __future__ import annotations

    import logging
    from typing import Annotated

    from fastapi import APIRouter, Depends, HTTPException, status
    from sqlalchemy.ext.asyncio import AsyncSession

    from app.api.dependencies import get_db, require_owner
    from app.api.schemas.admin import AdminUserCreateRequest, AdminUserResponse
    from app.db.models import AppUser
    from app.services import admin_users as admin_svc

    logger = logging.getLogger(__name__)

    admin_router = APIRouter(
        prefix="/admin",
        tags=["admin"],
        dependencies=[Depends(require_owner)],
    )


    @admin_router.get("/users", response_model=list[AdminUserResponse])
    async def list_admin_users(
        db: Annotated[AsyncSession, Depends(get_db)],
    ) -> list[AppUser]:
        """ADM-03 + ADM-06: список whitelist'а с last_seen_at."""
        return await admin_svc.list_users(db)


    @admin_router.post(
        "/users",
        response_model=AdminUserResponse,
        status_code=status.HTTP_201_CREATED,
    )
    async def create_admin_user(
        payload: AdminUserCreateRequest,
        db: Annotated[AsyncSession, Depends(get_db)],
        current_user: Annotated[AppUser, Depends(require_owner)],
    ) -> AppUser:
        """ADM-04 + ADM-06: invite by tg_user_id. 409 on duplicate.

        New row: role=member, onboarded_at=NULL, tg_chat_id=NULL.
        Phase 14 fills onboarded_at + tg_chat_id when invitee runs /start.
        """
        try:
            new_user = await admin_svc.invite_user(
                db, tg_user_id=payload.tg_user_id
            )
        except admin_svc.UserAlreadyExistsError as exc:
            raise HTTPException(
                status_code=status.HTTP_409_CONFLICT,
                detail="tg_user_id already in whitelist",
            ) from exc
        logger.info(
            "audit.user_invited tg_user_id=%s new_id=%s by_owner=%s",
            payload.tg_user_id, new_user.id, current_user.id,
        )
        return new_user


    @admin_router.delete(
        "/users/{user_id}",
        status_code=status.HTTP_204_NO_CONTENT,
    )
    async def delete_admin_user(
        user_id: int,
        db: Annotated[AsyncSession, Depends(get_db)],
        current_user: Annotated[AppUser, Depends(require_owner)],
    ) -> None:
        """ADM-05 + ADM-06: revoke + cascade purge.

        - Self-revoke forbidden (403) — guards against admin lockout.
        - Unknown user_id → 404.
        - Cascade purges all 9 domain tables + ai_usage_log + AppUser.
        - Logs structured `audit.user_revoked` line с counts.
        """
        if user_id == current_user.id:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="Owner cannot revoke own account",
            )
        try:
            counts = await admin_svc.purge_user(db, user_id=user_id)
        except admin_svc.UserNotFoundError as exc:
            raise HTTPException(
                status_code=status.HTTP_404_NOT_FOUND,
                detail="user_id not found",
            ) from exc
        logger.info(
            "audit.user_revoked uid=%s by_owner=%s purged_rows=%s",
            user_id, current_user.id, counts,
        )
    ```

    B) Изменения в `app/api/router.py`:

    1. Добавить импорт после существующих route-импортов (около строки 56-60, рядом с `from app.api.routes.subscriptions import router as subscriptions_router`):
    ```python
    from app.api.routes.admin import admin_router
    ```

    2. После `public_router.include_router(ai_suggest_router, prefix="/ai")` (около строки 128) добавить:
    ```python
    # Phase 13 sub-router — admin whitelist + AI usage (require_owner gated).
    public_router.include_router(admin_router)
    ```

    3. Обновить docstring в шапке router.py (опционально но рекомендуется — добавить "Phase 13 routes" блок аналогично существующим Phase 9 / Phase 12 блокам). Минимум — не критично.

    Self-checks:
    - `grep -c "admin_router" app/api/router.py` → ≥ 2 (import + include_router)
    - `grep -c "/admin/" app/api/routes/admin.py` или `prefix="/admin"` → 1
    - `grep -c "Owner cannot revoke own account" app/api/routes/admin.py` → 1
    - `grep -c "audit.user_revoked\|audit.user_invited" app/api/routes/admin.py` → 2
    - Python import: `from app.api.routes.admin import admin_router` exit 0
    - `from app.main_api import app` загружается без ошибки (полная registration test)
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.routes.admin import admin_router; from app.main_api import app; routes = [r.path for r in app.router.routes if hasattr(r, 'path')]; print([p for p in routes if '/admin/' in p]); assert any('/api/v1/admin/users' in p for p in routes), 'admin/users missing'"</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/routes/admin.py` exists (≥ 70 lines)
    - admin_router has 3 endpoints: GET /users, POST /users, DELETE /users/{user_id}
    - Each endpoint Annotated[AppUser, Depends(require_owner)] либо relies on router-level dep
    - app/api/router.py includes admin_router under public_router
    - `grep -c "admin_router" app/api/router.py` → ≥ 2
    - `grep -c "Depends(require_owner)" app/api/routes/admin.py` → ≥ 2 (router-level + route-level on POST/DELETE for current_user resolution)
    - `pytest tests/test_admin_users_api.py` (with DB) — 12/12 passing (PROVIDED Plan 13-02 alembic 0008 applied + Task 1 of this plan done)
    - Python full app load: `from app.main_api import app` exit 0
    - FastAPI route registry includes `/api/v1/admin/users` path
  </acceptance_criteria>
  <done>
    admin_router зарегистрирован; 12 RED тестов из Plan 13-01 переходят в GREEN; Python import full-app clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → /admin/* | initData HMAC validates tg_user_id; require_owner reads role from DB |
| Route → service layer | Self-revoke check at route; service layer doesn't know caller identity |
| Service → DB (cascade purge) | set_config sets RLS scope per-transaction; explicit DELETE WHERE user_id |
| Service → audit log | Stdout/stderr structured log line; ops monitor via container logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | E (Elevation) | Member calls /admin/* | mitigate | router-level Depends(require_owner) returns 403; tested test_admin_list_users_403_for_member, test_admin_create_user_403_for_member, test_admin_delete_user_403_for_member |
| T-13-04-02 | E | Owner self-revoke locks admin | mitigate | Route checks `user_id == current_user.id` → 403 BEFORE calling service; tested test_admin_delete_user_self_403 |
| T-13-04-03 | T (Tampering) | Race: two invites for same tg_user_id at once | mitigate | invite_user catches IntegrityError, rolls back, raises UserAlreadyExistsError → 409 |
| T-13-04-04 | I (Info Disclosure) | Cascade purge leaks other users' data | mitigate | DELETE explicitly scoped via `WHERE user_id = :uid`; test_admin_delete_user_cascade_purges_data verifies owner data intact |
| T-13-04-05 | I | DELETE rowcount logged but contains user_id (PII?) | accept | tg_user_id is not PII per CONTEXT (closed whitelist 5-50 users); audit log essential for compliance |
| T-13-04-06 | T | RLS bypass: service sets app.current_user_id without authorization check | mitigate | Service is called only from /admin/* routes which require_owner already; service layer не предполагается direct caller |
| T-13-04-07 | D (Denial of Service) | Cascade DELETE на юзере с миллионами записей блокирует request | accept | MVP scale 5-50 users × few thousand transactions each; not a current concern. Phase 15+ might add async background purge if scale grows |
| T-13-04-08 | R (Repudiation) | No persisted audit trail (only stderr) | accept | Phase 13 ships structured log line; full audit_log table deferred per CONTEXT (Future Requirements AUD-01) |

</threat_model>

<verification>
- pytest tests/test_admin_users_api.py — 12/12 GREEN (with DB available)
- pytest --collect-only весь suite — 0 errors (regression check)
- Python import: `from app.main_api import app` exit 0; route registry includes 3 admin endpoints
- alembic 0008 (расширенный с last_seen_at) applies cleanly via upgrade head
</verification>

<success_criteria>
- 5 модифицированных/созданных файлов:
  - NEW: app/api/schemas/admin.py
  - NEW: app/services/admin_users.py
  - NEW: app/api/routes/admin.py
  - MOD: app/api/router.py (admin_router include)
  - MOD: app/db/models.py (AppUser.last_seen_at)
  - MOD: alembic/versions/0008_admin_phase13.py (last_seen_at column add/drop)
- 12 RED tests из Plan 13-01 → GREEN
- 3 admin endpoints доступны под /api/v1/admin/users[/{id}]
- Cascade purge удаляет данные из 10 таблиц (9 domain + ai_usage_log) + AppUser
- Audit log lines: audit.user_invited, audit.user_revoked
- Готовность к Plan 13-05 (admin /ai-usage extends admin_router)
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ui-whitelist-ai-usage/13-04-SUMMARY.md`
</output>
