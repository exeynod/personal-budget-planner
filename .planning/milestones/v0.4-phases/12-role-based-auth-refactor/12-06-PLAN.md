---
phase: 12-role-based-auth-refactor
plan: 06
type: execute
wave: 4
depends_on: ["12-02", "12-03", "12-04"]
files_modified:
  - tests/helpers/__init__.py
  - tests/helpers/seed.py
  - tests/conftest.py
  - tests/test_subscriptions.py
  - tests/test_planned.py
  - tests/test_actual_crud.py
  - tests/test_actual_period.py
  - tests/test_apply_template.py
  - tests/test_templates.py
  - tests/test_snapshot.py
  - tests/test_balance.py
  - tests/test_periods_api.py
  - tests/test_internal_bot.py
  - tests/test_close_period_job.py
  - tests/test_worker_charge.py
  - tests/test_categories.py
  - tests/test_periods.py
  - tests/test_settings.py
  - tests/test_onboarding.py
  - tests/test_auth.py
  - tests/test_bot_handlers.py
  - tests/test_bot_handlers_phase4.py
autonomous: true
requirements: [ROLE-02, ROLE-03]
tags: [tests, fixtures, fixture-sweep, d-11-07-01, refactor]
must_haves:
  truths:
    - "Создан tests/helpers/seed.py с helper-функциями: seed_user(session, *, tg_user_id, role=UserRole.owner) -> AppUser; seed_category(session, *, user_id, name, kind, ...) -> Category; seed_subscription(session, *, user_id, ...) -> Subscription; seed_budget_period(session, *, user_id, ...) -> BudgetPeriod; seed_plan_template_item(...); seed_planned_transaction(...); seed_actual_transaction(...). Все принимают user_id явно."
    - "tests/helpers/__init__.py existing (empty или re-exports seed helpers)"
    - "В tests/conftest.py добавлена `single_user` pytest fixture — async fixture создаёт single AppUser(tg_user_id=conftest owner_tg_id, role=UserRole.owner) после TRUNCATE; yields user.id (PK) для использования в legacy tests"
    - "tests/conftest.py — `_rls_test_role` fixture помечена deprecated в docstring (после Plan 12-05 не нужна) — НЕ удаляем чтобы не сломать существующие tests которые её используют explicitly (test_rls_policy.py)"
    - "Existing test fixtures которые seed напрямую через session.add(Category(...)) обновлены чтобы передавать user_id (либо через single_user, либо через seed_user helper). Список tests подтвержденно затронуты: test_subscriptions.py, test_planned.py, test_actual_crud.py, test_actual_period.py, test_apply_template.py, test_templates.py, test_snapshot.py, test_balance.py, test_periods_api.py, test_internal_bot.py, test_close_period_job.py, test_worker_charge.py — 12 файлов."
    - "test_categories.py / test_periods.py / test_settings.py / test_onboarding.py / test_auth.py — already do GET /me first which upserts AppUser; могут потребовать минимальный fix (e.g., в db_setup fixture seed AppUser явно с role=owner перед /me call поскольку /me больше не upsert'ит после Plan 12-03)"
    - "test_bot_handlers.py / test_bot_handlers_phase4.py — fixtures обновлены: seed AppUser в DB для каждого test scenario (раньше тесты предполагали OWNER_TG_ID-eq без DB lookup; теперь bot_resolve_user_role требует DB row)"
    - "После Plan'а: pytest полная suite (`pytest tests/`) показывает резкое улучшение pass rate: цель — ≥ 90% (≥ 250/276 tests pass) или хотя бы все Phase 11 + Phase 12 own tests + всё что было passing до Phase 11 schema change. Нерешённые failures documented как acceptable carryover (e.g., AI test fixtures depending on real OpenAI calls — out of scope)"
    - "GOLDEN: D-11-07-01 RESOLVED — все ~63 NotNullViolationError tests теперь pass с user_id-aware fixtures"
  artifacts:
    - path: tests/helpers/seed.py
      provides: "user_id-aware factory functions для domain objects"
      contains: "seed_user"
      contains_2: "seed_category"
      min_lines: 60
    - path: tests/helpers/__init__.py
      provides: "marker package для helpers"
    - path: tests/conftest.py
      provides: "single_user fixture + updated существующие fixtures"
      contains: "single_user"
  key_links:
    - from: tests/helpers/seed.py
      to: app/db/models.py
      via: "Imports AppUser, UserRole, Category, Subscription, BudgetPeriod, PlanTemplateItem, PlannedTransaction, ActualTransaction"
      pattern: "from app.db.models import"
    - from: tests/conftest.py::single_user
      to: tests/helpers/seed.py::seed_user
      via: "fixture wraps seed_user call с TRUNCATE prelude"
      pattern: "seed_user"
    - from: tests/test_subscriptions.py (and others)
      to: tests/helpers/seed.py
      via: "from tests.helpers.seed import seed_user, seed_category, seed_subscription; replace inline session.add with helpers"
      pattern: "from tests.helpers.seed"
---

<objective>
Resolve D-11-07-01 (legacy test fixtures missing user_id). После Phase 11 schema change, ~63 tests failед с `NotNullViolationError: null value in column "user_id"`. Этот план:
1. Создаёт `tests/helpers/seed.py` — central factory module с user_id-aware helpers
2. Добавляет `single_user` pytest fixture в `tests/conftest.py` для simple legacy tests
3. Massively обновляет 12+ test файлов чтобы использовать helpers / single_user / two_tenants
4. Обновляет bot test fixtures (test_bot_handlers*) для работы с role-based check (Plan 12-04)
5. Обновляет API test fixtures (test_auth, test_settings, test_onboarding) которые depended on old /me upsert behaviour (Plan 12-03 убрал upsert)

Цель: pytest полная suite — переходит из "63 failed + 66 errors" в "≥ 250/276 pass". Все Phase 11/12 own tests остаются GREEN (не регрессировать).

Этот Plan — самый объёмный механический refactor в Phase 12. Объём context cost — на верхней границе (~30-35% per task). Поэтому Tasks 2-4 разбиты по группам: subscriptions+planned, actuals, templates+balance+misc.

Output: 22 файла обновлены/созданы; pytest pass rate ≥ 90%; D-11-07-01 RESOLVED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-role-based-auth-refactor/12-CONTEXT.md
@.planning/phases/11-multi-tenancy-db-migration/deferred-items.md
@CLAUDE.md
@tests/conftest.py
@app/db/models.py
@app/db/session.py

<interfaces>
**Helper module shape (`tests/helpers/seed.py`)** — central factories:

```python
"""Factory helpers for tests — user_id-aware seeds (Phase 12 D-11-07-01).

After Phase 11, all domain tables require user_id NOT NULL FK. Tests must
seed AppUser first, then pass user_id to every domain row constructor.

Usage:
    async with SessionLocal() as session:
        user = await seed_user(session, tg_user_id=123456789)
        await session.commit()
        cat = await seed_category(session, user_id=user.id, name="Food",
                                   kind=CategoryKind.expense)
        await session.commit()
"""
from __future__ import annotations
from datetime import date, datetime, timezone
from typing import Optional

from sqlalchemy.ext.asyncio import AsyncSession

from app.db.models import (
    ActualSource,
    ActualTransaction,
    AppUser,
    BudgetPeriod,
    Category,
    CategoryKind,
    PeriodStatus,
    PlanSource,
    PlanTemplateItem,
    PlannedTransaction,
    SubCycle,
    Subscription,
    UserRole,
)


async def seed_user(
    session: AsyncSession,
    *,
    tg_user_id: int,
    tg_chat_id: Optional[int] = None,
    role: UserRole = UserRole.owner,
    cycle_start_day: int = 5,
    onboarded_at: Optional[datetime] = None,
) -> AppUser:
    user = AppUser(
        tg_user_id=tg_user_id,
        tg_chat_id=tg_chat_id,
        role=role,
        cycle_start_day=cycle_start_day,
        onboarded_at=onboarded_at,
    )
    session.add(user)
    await session.flush()
    return user


async def seed_category(
    session: AsyncSession,
    *,
    user_id: int,
    name: str = "Тест",
    kind: CategoryKind = CategoryKind.expense,
    is_archived: bool = False,
    sort_order: int = 0,
) -> Category:
    c = Category(
        user_id=user_id, name=name, kind=kind,
        is_archived=is_archived, sort_order=sort_order,
    )
    session.add(c)
    await session.flush()
    return c


async def seed_budget_period(
    session: AsyncSession,
    *,
    user_id: int,
    period_start: date,
    period_end: date,
    starting_balance_cents: int = 0,
    status: PeriodStatus = PeriodStatus.active,
) -> BudgetPeriod:
    p = BudgetPeriod(
        user_id=user_id, period_start=period_start, period_end=period_end,
        starting_balance_cents=starting_balance_cents, status=status,
    )
    session.add(p)
    await session.flush()
    return p


async def seed_subscription(
    session: AsyncSession,
    *,
    user_id: int,
    name: str,
    amount_cents: int,
    cycle: SubCycle,
    next_charge_date: date,
    category_id: int,
    notify_days_before: int = 2,
    is_active: bool = True,
) -> Subscription:
    s = Subscription(
        user_id=user_id, name=name, amount_cents=amount_cents, cycle=cycle,
        next_charge_date=next_charge_date, category_id=category_id,
        notify_days_before=notify_days_before, is_active=is_active,
    )
    session.add(s)
    await session.flush()
    return s


async def seed_plan_template_item(
    session: AsyncSession,
    *,
    user_id: int,
    category_id: int,
    amount_cents: int,
    description: Optional[str] = None,
    day_of_period: Optional[int] = None,
    sort_order: int = 0,
) -> PlanTemplateItem:
    t = PlanTemplateItem(
        user_id=user_id, category_id=category_id, amount_cents=amount_cents,
        description=description, day_of_period=day_of_period, sort_order=sort_order,
    )
    session.add(t)
    await session.flush()
    return t


async def seed_planned_transaction(
    session: AsyncSession,
    *,
    user_id: int,
    period_id: int,
    kind: CategoryKind,
    amount_cents: int,
    category_id: int,
    source: PlanSource = PlanSource.manual,
    description: Optional[str] = None,
    planned_date: Optional[date] = None,
    subscription_id: Optional[int] = None,
    original_charge_date: Optional[date] = None,
) -> PlannedTransaction:
    p = PlannedTransaction(
        user_id=user_id, period_id=period_id, kind=kind, amount_cents=amount_cents,
        category_id=category_id, source=source, description=description,
        planned_date=planned_date, subscription_id=subscription_id,
        original_charge_date=original_charge_date,
    )
    session.add(p)
    await session.flush()
    return p


async def seed_actual_transaction(
    session: AsyncSession,
    *,
    user_id: int,
    period_id: int,
    kind: CategoryKind,
    amount_cents: int,
    category_id: int,
    tx_date: date,
    source: ActualSource = ActualSource.mini_app,
    description: Optional[str] = None,
) -> ActualTransaction:
    a = ActualTransaction(
        user_id=user_id, period_id=period_id, kind=kind, amount_cents=amount_cents,
        category_id=category_id, tx_date=tx_date, source=source, description=description,
    )
    session.add(a)
    await session.flush()
    return a
```

**Pytest fixture (`single_user` в conftest.py)**:

```python
@pytest_asyncio.fixture
async def single_user(db_session, owner_tg_id):
    """Single AppUser fixture для простых legacy tests (Phase 12 D-11-07-01).

    Returns:
        dict with 'id' (PK) and 'tg_user_id' so tests can seed dependent
        rows with user_id=fixture['id'] and POST against /api/v1/* with
        initData(tg_user_id=fixture['tg_user_id']).
    """
    from sqlalchemy import text
    from app.db.models import AppUser, UserRole

    # Cleanup pre-test (мирится с _rls_test_role residue, RLS bypass).
    await db_session.execute(text("RESET ROLE"))
    await db_session.execute(text("SET LOCAL row_security = off"))
    # Сначала domain rows (FK RESTRICT)
    for tbl in (
        "ai_message", "ai_conversation", "category_embedding",
        "actual_transaction", "planned_transaction", "subscription",
        "plan_template_item", "budget_period", "category",
    ):
        await db_session.execute(text(f"DELETE FROM {tbl}"))
    await db_session.execute(text("DELETE FROM app_user"))
    await db_session.commit()

    user = AppUser(tg_user_id=owner_tg_id, role=UserRole.owner, cycle_start_day=5)
    db_session.add(user)
    await db_session.commit()
    await db_session.refresh(user)

    yield {"id": user.id, "tg_user_id": owner_tg_id}

    # Pre-test cleanup will run для следующего теста.
```

**Existing tests pattern that needs update** (example test_subscriptions.py:99-116):

```python
# OLD:
expense_cat = Category(
    name="Подписки", kind=CategoryKind.expense,
    is_archived=False, sort_order=10,
)

# NEW:
expense_cat = await seed_category(
    session, user_id=single_user["id"], name="Подписки",
    kind=CategoryKind.expense, sort_order=10,
)
```

OR test fixtures `db_setup` могут seed AppUser явно перед `GET /me` (потому что Plan 12-03 убрал /me upsert):

```python
@pytest_asyncio.fixture
async def db_setup(async_client, bot_token, owner_tg_id):
    # ... TRUNCATE ...
    # NEW: explicitly seed owner because /me no longer upserts (Plan 12-03).
    async with SessionLocal() as session:
        from app.db.models import AppUser, UserRole
        session.add(AppUser(tg_user_id=owner_tg_id, role=UserRole.owner, cycle_start_day=5))
        await session.commit()
    # ... rest of fixture (GET /me etc.) ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/helpers/{__init__.py, seed.py} + add single_user fixture в conftest.py</name>
  <files>tests/helpers/__init__.py, tests/helpers/seed.py, tests/conftest.py</files>
  <read_first>
    - app/db/models.py (все 8 model constructors — AppUser, Category, BudgetPeriod, Subscription, PlanTemplateItem, PlannedTransaction, ActualTransaction, и enums)
    - tests/conftest.py (existing fixtures — bot_token, owner_tg_id, db_session, two_tenants pattern)
  </read_first>
  <behavior>
    После Task 1:
    - `from tests.helpers.seed import seed_user, seed_category, seed_subscription, seed_budget_period, seed_plan_template_item, seed_planned_transaction, seed_actual_transaction; print('OK')` → "OK"
    - pytest вызов `single_user` fixture: создаёт AppUser(tg_user_id=owner_tg_id, role=owner), yields {'id': PK, 'tg_user_id': owner_tg_id}
  </behavior>
  <action>
    **A. Создать `tests/helpers/__init__.py`** — пустой файл (marker для python package).

    **B. Создать `tests/helpers/seed.py`** — содержимое exactly как в `<interfaces>` секции выше (full seed factories).

    **C. Update `tests/conftest.py`** — добавить `single_user` fixture в КОНЦЕ файла (после `two_tenants`):

    ```python
    @pytest_asyncio.fixture
    async def single_user(db_session, owner_tg_id):
        """Single AppUser fixture для простых legacy tests (Phase 12 D-11-07-01).

        Создаёт AppUser(tg_user_id=owner_tg_id, role=owner) после полного
        TRUNCATE доменных таблиц. Используется legacy tests (test_subscriptions,
        test_planned etc.) которые ранее seed'или Category/Subscription без user_id.

        Yields dict {'id': PK, 'tg_user_id': owner_tg_id} — first для seed
        helper'ов (`user_id=single_user['id']`), второй для initData
        (`make_init_data(single_user['tg_user_id'], bot_token)`).
        """
        from sqlalchemy import text
        from app.db.models import AppUser, UserRole

        # Cleanup pre-test: bypass RLS для admin operations.
        await db_session.execute(text("RESET ROLE"))
        await db_session.execute(text("SET LOCAL row_security = off"))
        for tbl in (
            "ai_message", "ai_conversation", "category_embedding",
            "actual_transaction", "planned_transaction", "subscription",
            "plan_template_item", "budget_period", "category",
        ):
            await db_session.execute(text(f"DELETE FROM {tbl}"))
        await db_session.execute(text("DELETE FROM app_user"))
        await db_session.commit()

        user = AppUser(
            tg_user_id=owner_tg_id,
            role=UserRole.owner,
            cycle_start_day=5,
        )
        db_session.add(user)
        await db_session.commit()
        await db_session.refresh(user)

        yield {"id": user.id, "tg_user_id": owner_tg_id}
    ```

    Verify imports:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run python -c "from tests.helpers.seed import seed_user, seed_category, seed_subscription, seed_budget_period, seed_plan_template_item, seed_planned_transaction, seed_actual_transaction; print('OK')"
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from tests.helpers.seed import seed_user, seed_category, seed_subscription, seed_budget_period, seed_plan_template_item, seed_planned_transaction, seed_actual_transaction; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Import line выше → "OK"
    - `tests/helpers/__init__.py` существует (empty file OK)
    - `grep -c 'def single_user' tests/conftest.py` → 1
  </acceptance_criteria>
  <done>
    Helper-модуль и фикстура готовы для использования в Tasks 2-4.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sweep group A — test_subscriptions.py + test_planned.py + test_actual_crud.py + test_actual_period.py</name>
  <files>tests/test_subscriptions.py, tests/test_planned.py, tests/test_actual_crud.py, tests/test_actual_period.py</files>
  <read_first>
    - tests/test_subscriptions.py (full — fixtures db_setup, seed_categories на lines 95-116)
    - tests/test_planned.py (full — fixture db_setup, seed)
    - tests/test_actual_crud.py (lines 1-120 — db_setup pattern)
    - tests/test_actual_period.py (skim main fixture)
  </read_first>
  <behavior>
    После Task 2:
    - pytest tests/test_subscriptions.py: ≥ 80% pass (12 tests; D-11-07-01 fix)
    - pytest tests/test_planned.py: ≥ 80% pass (14 tests)
    - pytest tests/test_actual_crud.py + tests/test_actual_period.py: ≥ 80% pass
  </behavior>
  <action>
    Generic pattern для каждого файла в group A:

    1. **Импорт helpers** в top of file:
       ```python
       from tests.helpers.seed import seed_user, seed_category, seed_subscription, seed_budget_period, seed_planned_transaction, seed_actual_transaction
       from app.db.models import AppUser, UserRole
       ```
       (выбрать только те seed_* которые используются файлом)

    2. **Fixture db_setup**: после TRUNCATE block добавить explicit AppUser seed:
       ```python
       # Seed AppUser explicitly — /me no longer upserts after Phase 12 (Plan 12-03).
       async with SessionLocal() as session:
           session.add(AppUser(tg_user_id=owner_tg_id, role=UserRole.owner, cycle_start_day=5))
           await session.commit()
       ```
       Поместить ПОСЛЕ TRUNCATE и ПЕРЕД `app.dependency_overrides[get_db]` setup.

       Если fixture в test_subscriptions.py делает `await async_client.get('/api/v1/me', ...)` для bootstrap — оставить (теперь /me просто читает существующего AppUser, не upsert'ит — что нам нужно).

    3. **Fixtures `seed_categories` etc.**: каждый Category(name=..., kind=...) → передать user_id. Получить user_id из `single_user` fixture или из недавно-созданного AppUser. Pattern:
       ```python
       # OLD:
       expense_cat = Category(name="Подписки", kind=CategoryKind.expense, sort_order=10)

       # NEW: (если fixture получает single_user или резолвит user_id)
       expense_cat = Category(
           user_id=user_id, name="Подписки", kind=CategoryKind.expense, sort_order=10
       )
       ```
       Где `user_id` — берётся либо из `single_user['id']` параметра fixture, либо из `(SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id)`.

       Простейший подход для существующих fixtures — добавить `single_user` зависимостью:
       ```python
       @pytest_asyncio.fixture
       async def seed_categories(db_setup, single_user):
           _, SessionLocal = db_setup
           ...
           expense_cat = Category(user_id=single_user["id"], ...)
       ```
       Но `single_user` fixture использует `db_session` который тоже creates engine — может конфликтовать с `db_setup`. → Простой fix: внутри `seed_categories` сделать `SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id` через SessionLocal:
       ```python
       async with SessionLocal() as session:
           result = await session.execute(
               text("SELECT id FROM app_user WHERE tg_user_id = :tg"),
               {"tg": owner_tg_id},
           )
           user_id = result.scalar_one()
       ```

       Это minimal change, без overhauling fixtures.

    4. **Inline session.add(Category|Subscription|BudgetPeriod|...)** в test bodies: добавить user_id parameter. Where user_id come from — same pattern (SELECT app_user.id by tg_user_id), или fixture ref.

    5. **Run tests**:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         uv run pytest tests/test_subscriptions.py tests/test_planned.py tests/test_actual_crud.py tests/test_actual_period.py --tb=short 2>&1 | tail -30
       ```

    6. Если какой-то тест падает по причине НЕ связанной с user_id (e.g., service-layer signature change) — отметить в SUMMARY как "carryover for Plan 12-07 verification" и продолжить.

    **Strategy**: если context cost для одного файла превышает 30%, splittified Task — но эти 4 файла должны combined fit within ~35% если refactor mechanical (find-replace). Если нет — Task 2 splittable на 2 sub-tasks (subscriptions+planned, actual_crud+actual_period).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_subscriptions.py tests/test_planned.py tests/test_actual_crud.py tests/test_actual_period.py --tb=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Combined 4 файла: pytest pass rate ≥ 80% (was 0% — все падали NotNullViolationError)
    - Никаких NotNullViolationError на user_id в этих файлах (через grep `pytest -v output | grep NotNull` → 0 matches related к user_id)
    - Для нерешённых fail (не related к D-11-07-01) — задокументировать в SUMMARY
  </acceptance_criteria>
  <done>
    Group A tests могут seed данные с user_id; D-11-07-01 fixed для этих 4 файлов.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sweep group B — test_apply_template + test_templates + test_snapshot + test_balance + test_periods_api + test_internal_bot</name>
  <files>tests/test_apply_template.py, tests/test_templates.py, tests/test_snapshot.py, tests/test_balance.py, tests/test_periods_api.py, tests/test_internal_bot.py</files>
  <read_first>
    - Каждый файл: skim db_setup fixtures, seed patterns (lines где Category/BudgetPeriod/PlanTemplateItem/etc. constructed without user_id)
  </read_first>
  <behavior>
    Аналогично Task 2 — pass rate ≥ 80% для каждого файла после refactor.
  </behavior>
  <action>
    Apply same pattern as Task 2 для каждого файла:
    1. Import seed helpers + AppUser/UserRole
    2. db_setup fixture: explicit AppUser seed (если ещё нет)
    3. Inline session.add(Model(...)) → добавить user_id
    4. Run pytest для группы; verify ≥ 80% pass rate

    **NOTE для test_internal_bot.py**: internal_bot endpoints используют X-Internal-Token (не initData). User-context резолвится через bot routes service-layer. Тестовые fixtures должны seed AppUser явно (auth не требует через initData, но domain rows нужен FK на app_user).

    **NOTE для test_templates.py**: PlanTemplateItem constructor — must include user_id; same pattern.

    **NOTE для test_balance.py**: balance test seeds Categories + BudgetPeriod + PlannedTransaction + ActualTransaction; обширный refactor — но всё через одну fixture.

    Run tests:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest tests/test_apply_template.py tests/test_templates.py tests/test_snapshot.py tests/test_balance.py tests/test_periods_api.py tests/test_internal_bot.py --tb=line 2>&1 | tail -15
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_apply_template.py tests/test_templates.py tests/test_snapshot.py tests/test_balance.py tests/test_periods_api.py tests/test_internal_bot.py --tb=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Combined 6 файлов: pass rate ≥ 80%
    - Никаких NotNullViolationError связанных с user_id
  </acceptance_criteria>
  <done>
    Group B tests work with user_id; D-11-07-01 fixed для этих 6 файлов.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Sweep group C — auth + categories + periods + settings + onboarding + close_period_job + worker_charge + bot_handlers</name>
  <files>tests/test_auth.py, tests/test_categories.py, tests/test_periods.py, tests/test_settings.py, tests/test_onboarding.py, tests/test_close_period_job.py, tests/test_worker_charge.py, tests/test_bot_handlers.py, tests/test_bot_handlers_phase4.py</files>
  <read_first>
    - tests/test_auth.py (db_client fixture lines 14-48 — depends on /me upsert which Plan 12-03 removed)
    - tests/test_settings.py (similar pattern — fixture relies on /me bootstrap)
    - tests/test_onboarding.py
    - tests/test_close_period_job.py / test_worker_charge.py (worker tests — seed AppUser per-tenant)
    - tests/test_bot_handlers.py / test_bot_handlers_phase4.py (Plan 12-04 changed _is_owner → _check_user_role_async; tests must seed AppUser в DB)
  </read_first>
  <behavior>
    Этот Task закрывает remaining test files. Особенно важно test_bot_handlers (Plan 12-04 broke them) и test_auth (Plan 12-03 removed /me upsert).
  </behavior>
  <action>
    Для каждого файла:

    **A. test_auth.py — db_client fixture**:
    ```python
    # In db_client (after TRUNCATE block):
    # Seed OWNER explicitly because /me no longer upserts (Plan 12-03).
    async with engine.begin() as conn:
        await conn.execute(
            text(
                "INSERT INTO app_user (tg_user_id, role, cycle_start_day) "
                "VALUES (:tg, 'owner', 5) ON CONFLICT DO NOTHING"
            ),
            {"tg": int(os.environ.get("OWNER_TG_ID", "123456789"))},
        )
    ```

    **B. test_settings.py / test_onboarding.py / test_categories.py / test_periods.py** (которые не были в Task 2/3 списке, но имеют db_setup-like fixture):
    - Для test_categories.py / test_periods.py — verify не trigger'ят user_id NotNull (могут не seed Category напрямую, всё через API). Если pass — leave alone. Если fail — apply pattern.
    - Для test_settings.py / test_onboarding.py — добавить explicit AppUser seed before /me call.

    **C. test_close_period_job.py / test_worker_charge.py** — worker tests:
    - Seed AppUser per-tenant (если test scenario multi-tenant) или single AppUser (если single-tenant test)
    - Add user_id ко всем domain seeds

    **D. test_bot_handlers.py / test_bot_handlers_phase4.py** — bot test fixtures:
    - В каждом тесте который simulate'ит OWNER user (e.g., user_id=settings.OWNER_TG_ID): seed AppUser(tg_user_id=that_id, role=UserRole.owner) в DB
    - В тестах которые simulate'ят non-owner (rejection scenarios): seed AppUser с role=revoked OR не seed'ить вообще (None → silent reject)
    - Bot tests могут использовать `single_user` fixture для simple cases

    **Strategy**: каждый bot test handler-call сейчас expects `_is_owner(message) == True/False`. После Plan 12-04 это `await bot_resolve_user_role(tg_user_id)`. Tests могут mock'ать DB lookup ИЛИ seed AppUser в DB. Cleaner — seed in DB чтобы test path mirror runtime path.

    Run tests:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest tests/test_auth.py tests/test_categories.py tests/test_periods.py tests/test_settings.py tests/test_onboarding.py tests/test_close_period_job.py tests/test_worker_charge.py tests/test_bot_handlers.py tests/test_bot_handlers_phase4.py --tb=line 2>&1 | tail -20
    ```

    **Acceptable carryover**: bot tests могут потребовать значительного refactor (превращение sync mock в async DB seeds). Если context cost становится excessive — пометить остаток как "deferred to Plan 13-XX bot refactor" и оставить tests skipped с pytest.mark.skip("D-12-06-01 carryover").

    GOAL: combined pytest pass rate ≥ 90% (≥ 250/276 tests).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/ --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest output: ≥ 250 passed (out of ~276 total)
    - Никаких regressions в Phase 12 own tests (test_role_based_auth, test_require_owner, test_me_returns_role, test_postgres_role_runtime [collect-only], test_bot_role_resolution)
    - Никаких regressions в Phase 11 own tests (test_multitenancy_isolation, test_rls_policy, test_migration_backfill)
    - D-11-07-01 RESOLVED: 0 NotNullViolationError на user_id columns в любом тесте
  </acceptance_criteria>
  <done>
    Полная test suite at ≥ 90% pass rate; legacy fixtures work с user_id; D-11-07-01 RESOLVED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → DB | Fixtures bypass RLS via SET LOCAL row_security = off; documented in single_user fixture |
| Test fixture → seed helper | Helpers enforce user_id parameter — caller cannot accidentally omit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06-01 | T (Tampering) | Test fixture forgets to seed AppUser → 403 в API tests | mitigate | Pattern explicit: all db_setup fixtures seed AppUser via SQL INSERT (or via single_user fixture); pytest output sera show 403 если skip'нут — easy to debug |
| T-12-06-02 | I (Info Disclosure) | Test data leaks между tests (pre-test cleanup misses table) | mitigate | single_user fixture explicitly TRUNCATEs all 9 domain tables + app_user before each test; two_tenants similar |
| T-12-06-03 | E | Test runs as superuser, hides RLS bugs | accept | После Plan 12-05 runtime DATABASE_URL = budget_app (NOSUPERUSER); db_session fixture connects via DATABASE_URL → tests now run under non-super by default. _rls_test_role fixture for explicit RLS tests still works |
| T-12-06-04 | D | Massive fixture sweep introduces flakiness | mitigate | Cleanup is idempotent (DELETE FROM with WHERE clause); per-fixture explicit setup → no shared state |
</threat_model>

<verification>
- `python -c "from tests.helpers.seed import *; print('OK')"` → "OK"
- `grep -c 'def single_user' tests/conftest.py` → 1
- pytest полная suite: ≥ 250 passed (90%)
- 0 NotNullViolationError relating к user_id в test output
- Phase 11/12 own tests все GREEN (no regression)
</verification>

<success_criteria>
- D-11-07-01 RESOLVED: ~63 tests fixed
- pytest полная suite pass rate ≥ 90%
- tests/helpers/seed.py provides reusable factory module
- single_user fixture used widely в legacy tests
- Bot tests (test_bot_handlers*) work с new role-based check
- Никаких regressions в Phase 11/12 own tests
</success_criteria>

<output>
After completion, create `.planning/phases/12-role-based-auth-refactor/12-06-SUMMARY.md`
</output>
