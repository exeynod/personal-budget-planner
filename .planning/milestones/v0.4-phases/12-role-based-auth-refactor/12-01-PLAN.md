---
phase: 12-role-based-auth-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_role_based_auth.py
  - tests/test_require_owner.py
  - tests/test_me_returns_role.py
  - tests/test_postgres_role_runtime.py
  - tests/test_bot_role_resolution.py
autonomous: true
requirements: [ROLE-02, ROLE-03, ROLE-04, ROLE-05]
tags: [tdd, red-tests, auth, role, postgres-role, threat-model-evidence]
must_haves:
  truths:
    - "test_role_based_auth.py existing на disk, импортирует app.main_api и AppUser/UserRole без ModuleNotFoundError, RED ожидаемые failures: get_current_user не возвращает AppUser ORM, требуется refactor (Plan 12-02)"
    - "test_require_owner.py existing, RED — require_owner dependency пока не существует в app/api/dependencies.py"
    - "test_me_returns_role.py existing, RED — MeResponse.role поле отсутствует (router.py возвращает без role) → KeyError или Pydantic validation error"
    - "test_postgres_role_runtime.py existing, RED — alembic миграция 0007 (создание budget_app role) пока не существует, fixture проверяет рантайм-роль = NOSUPERUSER NOBYPASSRLS"
    - "test_bot_role_resolution.py existing, RED — функция bot_resolve_user_role не существует в app/bot/auth.py (Plan 12-04 создаст)"
    - "Каждый тестовый файл импортирует только тех модулей, которые УЖЕ existing (app.main_api, app.api.dependencies, app.db.models, app.db.session, tests.conftest helpers); RED проявляется на assertion-level или AttributeError, НЕ на import-collect"
    - "Все 5 файлов добавляют свои тесты в pytest collection (5 collected items minimum) — pytest --collect-only не падает"
  artifacts:
    - path: tests/test_role_based_auth.py
      provides: "RED tests for ROLE-02 + ROLE-03 (revoked → 403, member → 200, owner → 200, unknown tg_user_id → 403)"
      contains: "UserRole.revoked"
      contains_2: "X-Telegram-Init-Data"
    - path: tests/test_require_owner.py
      provides: "RED tests for ROLE-04 (member на admin endpoint → 403, owner → 200) с stub admin endpoint inside test"
      contains: "require_owner"
    - path: tests/test_me_returns_role.py
      provides: "RED test for ROLE-05 (GET /api/v1/me возвращает role field)"
      contains: '"role"'
    - path: tests/test_postgres_role_runtime.py
      provides: "RED tests for D-11-07-02 fix (runtime DATABASE_URL connects as NOSUPERUSER NOBYPASSRLS role)"
      contains: "rolsuper"
    - path: tests/test_bot_role_resolution.py
      provides: "RED tests for bot helper bot_resolve_user_role(tg_user_id) → UserRole | None"
      contains: "bot_resolve_user_role"
  key_links:
    - from: tests/test_role_based_auth.py
      to: app/api/dependencies.py::get_current_user (current — DOES NOT YET return AppUser ORM)
      via: "Test asserts get_current_user возвращает AppUser instance с .role attribute; current returns dict — RED"
      pattern: "from app.api.dependencies import get_current_user"
    - from: tests/test_require_owner.py
      to: app/api/dependencies.py::require_owner (current — does not exist)
      via: "ImportError or AttributeError on import → RED"
      pattern: "from app.api.dependencies import require_owner"
    - from: tests/test_me_returns_role.py
      to: app/api/router.py::MeResponse
      via: "GET /api/v1/me возвращает JSON с role; current schema не содержит role field — KeyError/missing field"
      pattern: '"role"'
    - from: tests/test_postgres_role_runtime.py
      to: PostgreSQL pg_roles (runtime DATABASE_URL должен resolve to budget_app)
      via: "SELECT current_user → 'budget_app'; SELECT rolsuper FROM pg_roles WHERE rolname=current_user → false"
      pattern: "current_user"
    - from: tests/test_bot_role_resolution.py
      to: app/bot/auth.py::bot_resolve_user_role (does not exist)
      via: "ImportError → RED"
      pattern: "from app.bot.auth import bot_resolve_user_role"
---

<objective>
Wave 0 RED gate для Phase 12. Создаём 5 файлов тестов, каждый покрывает одну подзадачу (auth refactor / require_owner / /me role / Postgres role split / bot resolve). Каждый тест либо падает с ImportError (target код не существует), либо с AssertionError (поведение пока совпадает с Phase 11 OWNER_TG_ID-eq).

Цель: фиксировать ожидаемое поведение ДО написания кода. Plans 12-02..12-06 будут переводить эти тесты в GREEN incrementally.

Purpose: Test-driven evidence для requirements ROLE-02..05 + D-11-07-02 fix + bot helper. Threat model evidence: privilege escalation (revoked → 403) and bypass (unknown tg_user_id → 403) verified via test before implementation.

Output: 5 новых файлов в `tests/` с pytest async/integration tests, ВСЕ ожидаемо RED при текущем коде.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-role-based-auth-refactor/12-CONTEXT.md
@.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
@.planning/phases/11-multi-tenancy-db-migration/deferred-items.md
@CLAUDE.md
@app/api/dependencies.py
@app/api/router.py
@app/db/models.py
@app/db/session.py
@app/bot/handlers.py
@tests/conftest.py
@tests/test_auth.py

<interfaces>
<!-- Существующие контракты, которые тесты будут импортировать. -->

From app/api/dependencies.py (CURRENT — Phase 11 state, BEFORE Plan 12-02):
```python
async def get_db() -> AsyncGenerator[AsyncSession, None]: ...
async def get_current_user(x_telegram_init_data: str | None = Header(default=None)) -> dict:
    """Returns dict {id, first_name, ...} — id == tg_user_id. NO ORM AppUser, NO role."""
async def get_current_user_id(current_user, db) -> int:
    """Returns app_user.id (PK). Raises 403 if AppUser not found."""
async def get_db_with_tenant_scope(user_id) -> AsyncGenerator[AsyncSession, None]: ...
# require_owner НЕ СУЩЕСТВУЕТ — тест должен импортировать и поймать ImportError или
# зафиксировать что после Plan 12-02 функция должна экспортироваться.
```

From app/db/models.py:
```python
class UserRole(str, enum.Enum):
    owner = "owner"
    member = "member"
    revoked = "revoked"

class AppUser(Base):
    id: Mapped[int]
    tg_user_id: Mapped[int]  # unique
    role: Mapped[UserRole]   # default member, OWNER backfilled at migration 0006
    onboarded_at: Mapped[Optional[datetime]]
    # ... cycle_start_day, notify_days_before, enable_ai_categorization, tg_chat_id
```

From app/api/router.py (CURRENT — Phase 11 state, BEFORE Plan 12-03):
```python
class MeResponse(BaseModel):
    tg_user_id: int
    tg_chat_id: int | None
    cycle_start_day: int
    onboarded_at: str | None
    chat_id_known: bool
    # role — ОТСУТСТВУЕТ; Plan 12-03 добавит
```

From tests/conftest.py (доступные fixtures):
- bot_token, owner_tg_id, internal_token (string/int values)
- async_client (httpx AsyncClient + dependency_overrides[get_db] = stub yielding None)
- db_session (real AsyncSession; SKIP if no DATABASE_URL)
- two_tenants(db_session) — creates user_a, user_b с role=member каждый, plus categories+subscription, returns dict with ids
- _rls_test_role — fixture provisions budget_rls_test NOSUPERUSER NOBYPASSRLS role (test-only Phase 11 workaround)
- make_init_data(tg_user_id, bot_token) → str — generates valid HMAC-SHA256 initData

Established pattern для DB-backed tests (см. tests/test_auth.py db_client fixture):
```python
@pytest_asyncio.fixture
async def db_client(async_client):
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set")
    from sqlalchemy import text
    from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
    from app.api.dependencies import get_db
    from app.main_api import app

    engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
    SessionLocal = async_sessionmaker(engine, expire_on_commit=False)
    async with engine.begin() as conn:
        await conn.execute(text("TRUNCATE TABLE category, ..., app_user RESTART IDENTITY CASCADE"))

    async def real_get_db():
        async with SessionLocal() as session:
            try: yield session; await session.commit()
            except: await session.rollback(); raise

    app.dependency_overrides[get_db] = real_get_db
    yield async_client
    await engine.dispose()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/test_role_based_auth.py + tests/test_require_owner.py — RED tests for ROLE-02/03/04 auth-dep refactor</name>
  <files>tests/test_role_based_auth.py, tests/test_require_owner.py</files>
  <read_first>
    - app/api/dependencies.py (current Phase 11 contract — get_current_user returns dict, NOT AppUser; no require_owner)
    - app/db/models.py (UserRole enum, AppUser.role)
    - tests/conftest.py (async_client, db_session, owner_tg_id, bot_token, make_init_data)
    - tests/test_auth.py (pattern для db_client fixture с TRUNCATE + dependency_overrides)
  </read_first>
  <behavior>
    test_role_based_auth.py — 4 integration tests + 2 unit tests (per ROLE-02 + ROLE-03):
    - test_revoked_user_gets_403 — seed AppUser(tg_user_id=tg_X, role=UserRole.revoked); GET /api/v1/me с initData(tg_X) → 403 status
    - test_member_user_gets_200 — seed AppUser(tg_user_id=tg_X, role=UserRole.member); GET /api/v1/me с initData(tg_X) → 200
    - test_owner_user_gets_200 — seed AppUser(tg_user_id=OWNER_TG_ID, role=UserRole.owner); GET /api/v1/me с initData(OWNER) → 200
    - test_unknown_tg_user_id_gets_403 — НЕ seed AppUser; GET /api/v1/me с initData(tg_unknown_99999) → 403 (unknown tg_user_id, NOT auto-create)
    - test_get_current_user_returns_app_user_orm — после refactor (Plan 12-02), вызов get_current_user в dep chain должен возвращать AppUser ORM instance с .role attribute (НЕ dict). RED: текущая реализация returns dict.
    - test_owner_tg_id_eq_no_longer_in_get_current_user — `grep -c "settings.OWNER_TG_ID" app/api/dependencies.py` должно быть 0 после Plan 12-02 (исключая doc-комментарии, поэтому используем `grep -v '^#' | grep -c`).

    test_require_owner.py — 3 integration tests (per ROLE-04):
    - test_require_owner_blocks_member — seed member; вызов stub admin endpoint (зарегистрированный inside test через app.dependency_overrides или route registration с Depends(require_owner)) → 403
    - test_require_owner_allows_owner — seed owner; same endpoint → 200
    - test_require_owner_blocks_revoked — seed revoked; → 403 (на этом уровне get_current_user уже даёт 403, но тест явный)

    Все DB-backed тесты используют db_client fixture pattern (TRUNCATE + dependency_overrides[get_db]). Auth header: X-Telegram-Init-Data via make_init_data(tg_X, bot_token) для каждого тестового tg_user_id.

    RED expectation: каждый assertion будет fail на текущем коде:
    - revoked / member / unknown тесты → текущий dependency возвращает 403 ТОЛЬКО при tg_user_id != OWNER_TG_ID; revoked-юзер с tg_user_id == OWNER_TG_ID получит 200 (BUG что мы фиксим). Member юзер с tg_user_id != OWNER → текущая реализация даёт 403 (но не по role-причине).
    - test_get_current_user_returns_app_user_orm → RED, текущее возвращает dict.
    - test_owner_tg_id_eq_no_longer_in_get_current_user → RED, settings.OWNER_TG_ID встречается на строке 46 + 63.
    - test_require_owner_* → ImportError при `from app.api.dependencies import require_owner`.
  </behavior>
  <action>
    Создать tests/test_role_based_auth.py:

    ```python
    """RED tests for Phase 12 ROLE-02 / ROLE-03 — get_current_user role-based auth.

    Tests fail under current Phase-11 code:
    - Plan 12-02 refactors get_current_user to return AppUser ORM (not dict),
      check role IN ('owner', 'member'), reject 'revoked' + unknown.

    All DB-backed tests skip if DATABASE_URL not set (CI-safe).
    """
    from __future__ import annotations

    import os
    import re
    from pathlib import Path

    import pytest
    import pytest_asyncio


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set — integration test requires DB")


    @pytest_asyncio.fixture
    async def db_client(async_client):
        _require_db()
        from sqlalchemy import text
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

        from app.api.dependencies import get_db
        from app.main_api import app

        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

        async with engine.begin() as conn:
            await conn.execute(
                text(
                    "TRUNCATE TABLE category, planned_transaction, "
                    "actual_transaction, plan_template_item, subscription, "
                    "budget_period, ai_message, ai_conversation, "
                    "category_embedding, app_user RESTART IDENTITY CASCADE"
                )
            )

        async def real_get_db():
            async with SessionLocal() as session:
                try:
                    yield session
                    await session.commit()
                except Exception:
                    await session.rollback()
                    raise

        app.dependency_overrides[get_db] = real_get_db
        yield async_client, SessionLocal
        await engine.dispose()


    async def _seed_user(SessionLocal, *, tg_user_id, role):
        """Insert AppUser with explicit role; commit."""
        from app.db.models import AppUser, UserRole
        async with SessionLocal() as session:
            user = AppUser(tg_user_id=tg_user_id, role=role, cycle_start_day=5)
            session.add(user)
            await session.commit()
            await session.refresh(user)
            return user.id


    @pytest.mark.asyncio
    async def test_revoked_user_gets_403(db_client, bot_token):
        """ROLE-03: role=revoked → 403 на любом public endpoint."""
        from tests.conftest import make_init_data
        client, SessionLocal = db_client
        from app.db.models import UserRole
        await _seed_user(SessionLocal, tg_user_id=9_111_111_001, role=UserRole.revoked)
        init_data = make_init_data(9_111_111_001, bot_token)
        resp = await client.get(
            "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 403, (
            f"revoked must get 403, got {resp.status_code} body={resp.text}"
        )


    @pytest.mark.asyncio
    async def test_member_user_gets_200(db_client, bot_token):
        """ROLE-03: role=member → 200 (member проходит в get_current_user)."""
        from tests.conftest import make_init_data
        client, SessionLocal = db_client
        from app.db.models import UserRole
        await _seed_user(SessionLocal, tg_user_id=9_111_111_002, role=UserRole.member)
        init_data = make_init_data(9_111_111_002, bot_token)
        resp = await client.get(
            "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, (
            f"member must get 200, got {resp.status_code} body={resp.text}"
        )


    @pytest.mark.asyncio
    async def test_owner_user_gets_200(db_client, bot_token, owner_tg_id):
        """ROLE-02: OWNER_TG_ID юзер с role=owner проходит."""
        from tests.conftest import make_init_data
        client, SessionLocal = db_client
        from app.db.models import UserRole
        await _seed_user(SessionLocal, tg_user_id=owner_tg_id, role=UserRole.owner)
        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, (
            f"owner must get 200, got {resp.status_code} body={resp.text}"
        )


    @pytest.mark.asyncio
    async def test_unknown_tg_user_id_gets_403(db_client, bot_token):
        """ROLE-03: неизвестный tg_user_id (нет AppUser строки) → 403."""
        from tests.conftest import make_init_data
        client, _ = db_client  # NO seeding
        init_data = make_init_data(9_999_999_999, bot_token)
        resp = await client.get(
            "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 403, (
            f"unknown tg_user_id must get 403, got {resp.status_code} body={resp.text}"
        )


    @pytest.mark.asyncio
    async def test_get_current_user_returns_app_user_orm(db_client, bot_token, owner_tg_id):
        """ROLE-02 design: get_current_user должна возвращать AppUser ORM (не dict).

        RED until Plan 12-02. Тест проверяет через FastAPI dep override:
        регистрирует stub endpoint, который читает Depends(get_current_user)
        и asserts isinstance(...).
        """
        from fastapi import Depends
        from tests.conftest import make_init_data
        from app.api.dependencies import get_current_user
        from app.db.models import AppUser, UserRole
        from app.main_api import app

        client, SessionLocal = db_client
        await _seed_user(SessionLocal, tg_user_id=owner_tg_id, role=UserRole.owner)

        # Регистрируем stub endpoint только для теста.
        @app.get("/_test/current_user_type")
        async def _stub(user=Depends(get_current_user)):
            return {
                "type": type(user).__name__,
                "is_app_user": isinstance(user, AppUser),
                "has_role": hasattr(user, "role"),
            }

        try:
            init_data = make_init_data(owner_tg_id, bot_token)
            resp = await client.get(
                "/_test/current_user_type",
                headers={"X-Telegram-Init-Data": init_data},
            )
            assert resp.status_code == 200, resp.text
            body = resp.json()
            assert body["is_app_user"], (
                f"get_current_user must return AppUser ORM, got {body['type']}"
            )
            assert body["has_role"], "AppUser ORM must have .role attribute"
        finally:
            # Cleanup: remove stub route to keep app pristine.
            app.router.routes = [
                r for r in app.router.routes
                if getattr(r, "path", None) != "/_test/current_user_type"
            ]


    def test_owner_tg_id_eq_no_longer_in_get_current_user():
        """ROLE-02: после Plan 12-02 в get_current_user не остаётся OWNER_TG_ID-eq.

        Reads app/api/dependencies.py, считает строки (без комментариев и docstrings)
        что содержат 'settings.OWNER_TG_ID'. Допускается 0.

        Self-invalidating-grep mitigation: исключаем '^\\s*#' (line comments)
        + строки внутри triple-quoted блоков (грубая эвристика — флаг in_doc).
        """
        path = Path("app/api/dependencies.py")
        assert path.exists(), f"missing {path}"
        in_doc = False
        offending: list[str] = []
        for lineno, line in enumerate(path.read_text(encoding="utf-8").splitlines(), 1):
            stripped = line.strip()
            triple_count = stripped.count('"""') + stripped.count("'''")
            if triple_count % 2 == 1:
                in_doc = not in_doc
                continue
            if in_doc:
                continue
            if stripped.startswith("#"):
                continue
            if "settings.OWNER_TG_ID" in line:
                offending.append(f"{lineno}: {stripped}")
        assert offending == [], (
            "settings.OWNER_TG_ID must not appear in app/api/dependencies.py "
            f"after Plan 12-02 (auth refactor). Found:\n" + "\n".join(offending)
        )
    ```

    Создать tests/test_require_owner.py:

    ```python
    """RED tests for Phase 12 ROLE-04 — require_owner FastAPI dependency.

    Tests fail under Phase-11 code: require_owner does not exist in
    app/api/dependencies.py. Plan 12-02 will export it.

    Pattern: register a stub admin endpoint inside the test using
    Depends(require_owner) and a stub admin handler that returns 200.
    """
    from __future__ import annotations

    import os

    import pytest
    import pytest_asyncio


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set")


    @pytest_asyncio.fixture
    async def db_client(async_client):
        _require_db()
        from sqlalchemy import text
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
        from app.api.dependencies import get_db
        from app.main_api import app

        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)
        async with engine.begin() as conn:
            await conn.execute(
                text(
                    "TRUNCATE TABLE category, planned_transaction, "
                    "actual_transaction, plan_template_item, subscription, "
                    "budget_period, ai_message, ai_conversation, "
                    "category_embedding, app_user RESTART IDENTITY CASCADE"
                )
            )

        async def real_get_db():
            async with SessionLocal() as session:
                try:
                    yield session
                    await session.commit()
                except Exception:
                    await session.rollback()
                    raise

        app.dependency_overrides[get_db] = real_get_db
        yield async_client, SessionLocal
        await engine.dispose()


    async def _seed(SessionLocal, *, tg_user_id, role):
        from app.db.models import AppUser
        async with SessionLocal() as session:
            user = AppUser(tg_user_id=tg_user_id, role=role, cycle_start_day=5)
            session.add(user)
            await session.commit()


    def _register_stub_admin_route():
        """Register a /api/v1/_test/admin endpoint with Depends(require_owner)."""
        from fastapi import Depends
        from app.api.dependencies import require_owner  # RED: ImportError until 12-02
        from app.main_api import app

        @app.get("/api/v1/_test/admin")
        async def stub(_=Depends(require_owner)):
            return {"ok": True}

        return app


    @pytest.mark.asyncio
    async def test_require_owner_allows_owner(db_client, bot_token, owner_tg_id):
        from tests.conftest import make_init_data
        from app.db.models import UserRole

        client, SessionLocal = db_client
        await _seed(SessionLocal, tg_user_id=owner_tg_id, role=UserRole.owner)
        app = _register_stub_admin_route()

        init_data = make_init_data(owner_tg_id, bot_token)
        try:
            resp = await client.get(
                "/api/v1/_test/admin",
                headers={"X-Telegram-Init-Data": init_data},
            )
            assert resp.status_code == 200, resp.text
        finally:
            app.router.routes = [
                r for r in app.router.routes
                if getattr(r, "path", None) != "/api/v1/_test/admin"
            ]


    @pytest.mark.asyncio
    async def test_require_owner_blocks_member(db_client, bot_token):
        from tests.conftest import make_init_data
        from app.db.models import UserRole

        client, SessionLocal = db_client
        tg = 9_222_222_001
        await _seed(SessionLocal, tg_user_id=tg, role=UserRole.member)
        app = _register_stub_admin_route()

        init_data = make_init_data(tg, bot_token)
        try:
            resp = await client.get(
                "/api/v1/_test/admin",
                headers={"X-Telegram-Init-Data": init_data},
            )
            assert resp.status_code == 403, (
                f"member must be blocked by require_owner, got {resp.status_code}"
            )
        finally:
            app.router.routes = [
                r for r in app.router.routes
                if getattr(r, "path", None) != "/api/v1/_test/admin"
            ]


    @pytest.mark.asyncio
    async def test_require_owner_blocks_revoked(db_client, bot_token):
        from tests.conftest import make_init_data
        from app.db.models import UserRole

        client, SessionLocal = db_client
        tg = 9_222_222_002
        await _seed(SessionLocal, tg_user_id=tg, role=UserRole.revoked)
        app = _register_stub_admin_route()

        init_data = make_init_data(tg, bot_token)
        try:
            resp = await client.get(
                "/api/v1/_test/admin",
                headers={"X-Telegram-Init-Data": init_data},
            )
            assert resp.status_code == 403
        finally:
            app.router.routes = [
                r for r in app.router.routes
                if getattr(r, "path", None) != "/api/v1/_test/admin"
            ]
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_role_based_auth.py tests/test_require_owner.py --collect-only 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist на disk; pytest --collect-only показывает 9 tests collected (6 + 3) без collection errors
    - Запуск pytest без -x — ВСЕ tests RED (ожидаемо): expectations: 6 of 6 from test_role_based_auth fail with assertion error, 3 of 3 from test_require_owner fail with ImportError(require_owner) или 200/expected mismatch.
  </acceptance_criteria>
  <done>
    Файлы существуют, импортируются без syntax errors, pytest --collect-only собирает все тесты.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_me_returns_role.py + tests/test_postgres_role_runtime.py + tests/test_bot_role_resolution.py — RED tests for ROLE-05 + D-11-07-02 + bot helper</name>
  <files>tests/test_me_returns_role.py, tests/test_postgres_role_runtime.py, tests/test_bot_role_resolution.py</files>
  <read_first>
    - app/api/router.py (current MeResponse — no role field)
    - app/db/models.py (AppUser, UserRole)
    - app/db/session.py (async_engine, set_tenant_scope)
    - tests/conftest.py (db_session, _rls_test_role, two_tenants, make_init_data)
    - frontend/src/api/types.ts (MeResponse interface)
  </read_first>
  <behavior>
    test_me_returns_role.py — 2 tests (per ROLE-05):
    - test_me_includes_role_for_owner — seed AppUser(tg_user_id=OWNER, role=owner); GET /api/v1/me → JSON body has "role": "owner". RED: текущий MeResponse не содержит role field.
    - test_me_includes_role_for_member — seed member; GET /api/v1/me → "role": "member".

    test_postgres_role_runtime.py — 3 tests (per D-11-07-02 fix):
    - test_runtime_database_url_uses_nosuperuser_role — connect через DATABASE_URL, run `SELECT current_user, rolsuper, rolbypassrls FROM pg_roles WHERE rolname = current_user`. After Plan 12-05 fix → current_user == 'budget_app', rolsuper == False, rolbypassrls == False. RED: сейчас current_user='budget' с rolsuper=True.
    - test_admin_database_url_can_create_objects — connect через ADMIN_DATABASE_URL (env var, must be set after Plan 12-05); run `CREATE TEMP TABLE _t(...); DROP TABLE _t;` — успех значит admin role имеет нужные привилегии. RED: ADMIN_DATABASE_URL ENV не установлен.
    - test_rls_enforces_at_runtime_without_test_role — открыть session через DATABASE_URL (runtime), вставить категорию для user_a, без SET LOCAL app.current_user_id выполнить SELECT FROM category — должно вернуть 0 rows (RLS enforce'ятся БЕЗ _rls_test_role workaround). RED: будет видеть всё (superuser bypass).

    test_bot_role_resolution.py — 4 tests (per Plan 12-04 helper):
    - test_bot_resolve_user_role_owner — seed owner, вызов bot_resolve_user_role(tg_owner) → UserRole.owner
    - test_bot_resolve_user_role_member — seed member → UserRole.member
    - test_bot_resolve_user_role_revoked — seed revoked → UserRole.revoked
    - test_bot_resolve_user_role_unknown_returns_none — без seed → None
    RED: модуль app/bot/auth.py (или функция) не существует — ImportError.

    Все DB-tests skip if no DATABASE_URL.
  </behavior>
  <action>
    Создать tests/test_me_returns_role.py:

    ```python
    """RED tests for Phase 12 ROLE-05 — GET /api/v1/me возвращает role field.

    Tests fail until Plan 12-03 adds role to MeResponse + router.
    """
    from __future__ import annotations

    import os

    import pytest
    import pytest_asyncio


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set")


    @pytest_asyncio.fixture
    async def db_client(async_client):
        _require_db()
        from sqlalchemy import text
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
        from app.api.dependencies import get_db
        from app.main_api import app

        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)
        async with engine.begin() as conn:
            await conn.execute(
                text(
                    "TRUNCATE TABLE category, planned_transaction, "
                    "actual_transaction, plan_template_item, subscription, "
                    "budget_period, ai_message, ai_conversation, "
                    "category_embedding, app_user RESTART IDENTITY CASCADE"
                )
            )

        async def real_get_db():
            async with SessionLocal() as session:
                try:
                    yield session
                    await session.commit()
                except Exception:
                    await session.rollback()
                    raise

        app.dependency_overrides[get_db] = real_get_db
        yield async_client, SessionLocal
        await engine.dispose()


    async def _seed(SessionLocal, *, tg_user_id, role):
        from app.db.models import AppUser
        async with SessionLocal() as session:
            session.add(AppUser(tg_user_id=tg_user_id, role=role, cycle_start_day=5))
            await session.commit()


    @pytest.mark.asyncio
    async def test_me_includes_role_for_owner(db_client, bot_token, owner_tg_id):
        from tests.conftest import make_init_data
        from app.db.models import UserRole

        client, SessionLocal = db_client
        await _seed(SessionLocal, tg_user_id=owner_tg_id, role=UserRole.owner)
        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert "role" in body, f"MeResponse must include 'role' field, got keys={list(body.keys())}"
        assert body["role"] == "owner", f"expected role=owner, got {body['role']!r}"


    @pytest.mark.asyncio
    async def test_me_includes_role_for_member(db_client, bot_token):
        from tests.conftest import make_init_data
        from app.db.models import UserRole

        client, SessionLocal = db_client
        tg = 9_333_333_001
        await _seed(SessionLocal, tg_user_id=tg, role=UserRole.member)
        init_data = make_init_data(tg, bot_token)
        resp = await client.get(
            "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert body.get("role") == "member"
    ```

    Создать tests/test_postgres_role_runtime.py:

    ```python
    """RED tests for D-11-07-02 fix (Plan 12-05) — runtime Postgres role split.

    After Plan 12-05:
      - DATABASE_URL connects as `budget_app` (NOSUPERUSER NOBYPASSRLS)
      - ADMIN_DATABASE_URL env var connects as privileged role for migrations
      - RLS enforces at runtime WITHOUT the _rls_test_role workaround.

    Currently RED:
      - DATABASE_URL connects as `budget` (SUPERUSER → bypasses RLS)
      - ADMIN_DATABASE_URL is not set
    """
    from __future__ import annotations

    import os

    import pytest
    import pytest_asyncio
    from sqlalchemy import text
    from sqlalchemy.ext.asyncio import create_async_engine


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set")


    @pytest.mark.asyncio
    async def test_runtime_database_url_uses_nosuperuser_role():
        """DATABASE_URL должен подключать как budget_app NOSUPERUSER NOBYPASSRLS."""
        _require_db()
        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        try:
            async with engine.connect() as conn:
                result = await conn.execute(
                    text(
                        "SELECT current_user, rolsuper, rolbypassrls "
                        "FROM pg_roles WHERE rolname = current_user"
                    )
                )
                row = result.one()
            assert row[0] == "budget_app", (
                f"DATABASE_URL must connect as 'budget_app', got {row[0]!r}"
            )
            assert row[1] is False, f"runtime role must be NOSUPERUSER, rolsuper={row[1]}"
            assert row[2] is False, f"runtime role must be NOBYPASSRLS, rolbypassrls={row[2]}"
        finally:
            await engine.dispose()


    @pytest.mark.asyncio
    async def test_admin_database_url_present_and_privileged():
        """ADMIN_DATABASE_URL должен существовать и подключать админ-роль."""
        admin_url = os.environ.get("ADMIN_DATABASE_URL")
        assert admin_url, (
            "ADMIN_DATABASE_URL env var must be set after Plan 12-05 "
            "(used by alembic migrations + admin tasks)"
        )
        engine = create_async_engine(admin_url, echo=False)
        try:
            async with engine.connect() as conn:
                # Privilege check: admin может создавать объекты в public schema.
                await conn.execute(text("CREATE TEMP TABLE _phase12_t (x int)"))
                await conn.execute(text("DROP TABLE _phase12_t"))
                # Admin role либо superuser либо имеет CREATE ON SCHEMA public.
                result = await conn.execute(
                    text(
                        "SELECT current_user, rolsuper FROM pg_roles "
                        "WHERE rolname = current_user"
                    )
                )
                row = result.one()
            assert row[0] != "budget_app", (
                "ADMIN_DATABASE_URL must NOT use the runtime app role"
            )
            # Можно либо superuser ('budget'), либо отдельный 'budget_admin'.
            assert row[0] in ("budget", "budget_admin", "postgres"), (
                f"unexpected admin role: {row[0]!r}"
            )
        finally:
            await engine.dispose()


    @pytest.mark.asyncio
    async def test_rls_enforces_at_runtime_without_test_role(two_tenants, db_session):
        """RLS должна enforce'иться при использовании DATABASE_URL — без SET LOCAL ROLE.

        Phase 11 caveat: тесты использовали fixture _rls_test_role чтобы
        переключиться на NOSUPERUSER NOBYPASSRLS role и обойти superuser bypass.
        После Plan 12-05 рантайм уже не super → workaround не нужен.

        Тест: открываем session БЕЗ SET LOCAL app.current_user_id
        и БЕЗ SET LOCAL ROLE → SELECT FROM category должен вернуть 0 rows.
        Под Phase 11 fixture db_session использует connection с superuser
        ('budget') → видит все строки → тест RED.
        """
        # Чистая trx, без app.current_user_id, без ROLE switch.
        await db_session.commit()
        result = await db_session.execute(text("SELECT count(*) FROM category"))
        count = result.scalar_one()
        assert count == 0, (
            f"RLS must enforce at runtime (after Plan 12-05 D-11-07-02 fix); "
            f"expected 0 visible rows but got {count} — runtime role likely "
            f"still bypasses RLS"
        )
    ```

    Создать tests/test_bot_role_resolution.py:

    ```python
    """RED tests for Phase 12 Plan 12-04 — bot helper bot_resolve_user_role.

    Until Plan 12-04 lands, app/bot/auth.py (or wherever the helper lives)
    does not export bot_resolve_user_role → ImportError.

    Function contract (after 12-04):
        async def bot_resolve_user_role(tg_user_id: int) -> UserRole | None:
            '''Look up app_user.role by tg_user_id; None if не существует.'''
    """
    from __future__ import annotations

    import os

    import pytest
    import pytest_asyncio


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set")


    @pytest_asyncio.fixture
    async def fresh_db():
        _require_db()
        from sqlalchemy import text
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine

        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)
        async with engine.begin() as conn:
            await conn.execute(
                text(
                    "TRUNCATE TABLE category, planned_transaction, "
                    "actual_transaction, plan_template_item, subscription, "
                    "budget_period, ai_message, ai_conversation, "
                    "category_embedding, app_user RESTART IDENTITY CASCADE"
                )
            )
        yield SessionLocal
        await engine.dispose()


    async def _seed(SessionLocal, *, tg_user_id, role):
        from app.db.models import AppUser
        async with SessionLocal() as session:
            session.add(AppUser(tg_user_id=tg_user_id, role=role, cycle_start_day=5))
            await session.commit()


    @pytest.mark.asyncio
    async def test_bot_resolve_user_role_owner(fresh_db):
        from app.bot.auth import bot_resolve_user_role  # ImportError until 12-04
        from app.db.models import UserRole

        await _seed(fresh_db, tg_user_id=9_444_444_001, role=UserRole.owner)
        result = await bot_resolve_user_role(9_444_444_001)
        assert result is UserRole.owner


    @pytest.mark.asyncio
    async def test_bot_resolve_user_role_member(fresh_db):
        from app.bot.auth import bot_resolve_user_role
        from app.db.models import UserRole

        await _seed(fresh_db, tg_user_id=9_444_444_002, role=UserRole.member)
        result = await bot_resolve_user_role(9_444_444_002)
        assert result is UserRole.member


    @pytest.mark.asyncio
    async def test_bot_resolve_user_role_revoked(fresh_db):
        from app.bot.auth import bot_resolve_user_role
        from app.db.models import UserRole

        await _seed(fresh_db, tg_user_id=9_444_444_003, role=UserRole.revoked)
        result = await bot_resolve_user_role(9_444_444_003)
        assert result is UserRole.revoked


    @pytest.mark.asyncio
    async def test_bot_resolve_user_role_unknown_returns_none(fresh_db):
        from app.bot.auth import bot_resolve_user_role

        result = await bot_resolve_user_role(9_444_444_999)
        assert result is None
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_me_returns_role.py tests/test_postgres_role_runtime.py tests/test_bot_role_resolution.py --collect-only 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Все 3 файла созданы; pytest --collect-only собирает 9 tests (2 + 3 + 4) без syntax/collection errors
    - При запуске на Phase-11 коде ВСЕ tests RED:
      - test_me_returns_role: KeyError или missing field assertion
      - test_postgres_role_runtime: AssertionError (current_user='budget', rolsuper=True) + ADMIN_DATABASE_URL not set + RLS не enforce'ятся
      - test_bot_role_resolution: ImportError на `from app.bot.auth import bot_resolve_user_role`
  </acceptance_criteria>
  <done>
    Все 5 файлов из Plan 12-01 (включая 2 файла из Task 1) существуют, pytest collects всех 18 tests; commit готов.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → API | initData crosses boundary; HMAC validation already established (Phase 1) |
| API → PostgreSQL (runtime) | DATABASE_URL credentials; currently superuser, must become NOSUPERUSER (D-11-07-02) |
| API → PostgreSQL (admin) | Migration runs as privileged role; introduced in Plan 12-05 |
| Bot → DB (shared engine) | Bot reads role from DB to authorise commands; no internal API hop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | S | RED tests must seed users with explicit role | mitigate | Helpers `_seed_user(role=UserRole.owner|member|revoked)` enforce role in fixture; missing role would silently default to 'member' and mask failures |
| T-12-01-02 | T | Test fixtures могут быть неаккуратно скопированы и пропустить TRUNCATE | mitigate | Каждый db_client/fresh_db fixture явно TRUNCATE'ит app_user CASCADE до seed; isolation guaranteed |
| T-12-01-03 | I | Stub admin endpoint в test_require_owner может остаться зарегистрированным после теста | mitigate | `try/finally` removes stub route; assertion на cleanup в done criteria |

(Wave 0 — RED tests only. Production threats are mitigated in Plans 12-02..12-05.)
</threat_model>

<verification>
- pytest --collect-only собирает все 5 файлов без ошибок (no syntax/import errors at collection)
- Запуск pytest на Phase-11 коде (без Plan 12-02..06): все 18 tests RED (некоторые via assert, некоторые via ImportError)
- Никакой production код не модифицирован в этом плане (только tests/* added)
</verification>

<success_criteria>
- 5 файлов в tests/: test_role_based_auth.py, test_require_owner.py, test_me_returns_role.py, test_postgres_role_runtime.py, test_bot_role_resolution.py
- pytest --collect-only --quiet: ≥ 18 items collected from these files
- Запуск этих файлов: 18 tests fail (expected RED) при текущем codebase
- Готовность к Plans 12-02..12-06 (которые делают tests GREEN incrementally)
</success_criteria>

<output>
After completion, create `.planning/phases/12-role-based-auth-refactor/12-01-SUMMARY.md`
</output>
