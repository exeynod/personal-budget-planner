---
phase: 12-role-based-auth-refactor
plan: 07
type: execute
wave: 5
depends_on: ["12-02", "12-03", "12-04", "12-05", "12-06"]
files_modified:
  - .planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md
autonomous: false
requirements: [ROLE-02, ROLE-03, ROLE-04, ROLE-05]
tags: [verification, integration, uat, human-checkpoint, threat-model-attestation]
must_haves:
  truths:
    - "12-VERIFICATION.md создан с frontmatter (phase, status, verified-on, verifier, requirements) — соответствует Phase 11 verification format"
    - "Раздел Requirements Verification покрывает ROLE-02, ROLE-03, ROLE-04, ROLE-05 — каждый с PASS/FAIL/⚠ статусом + evidence (test names, grep results, manual checkpoints)"
    - "Раздел Manual Checkpoints покрывает: alembic 0007 upgrade/downgrade cycle, Postgres role privileges (psql \\du), runtime RLS enforcement (без _rls_test_role), bot manual smoke (живой /start, /add от owner)"
    - "D-11-07-01 + D-11-07-02 явно отмечены RESOLVED со ссылкой на artifacts (test_postgres_role_runtime + tests/helpers/seed.py)"
    - "Threat model attestation block — STRIDE table из всех 6 plans aggregated; каждый threat либо mitigated (со ссылкой на code/test) либо accepted (с rationale)"
    - "Live TG MiniApp UAT — listed как `human_needed` checkpoint (similar Phase 11; reasonable не automate)"
    - "Status frontmatter: либо `passed` (если все automated checks GREEN + human signed off) либо `human_needed` (automated GREEN; live MiniApp UAT pending)"
  artifacts:
    - path: .planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md
      provides: "Phase 12 verification report"
      contains: "ROLE-02"
      contains_2: "D-11-07-02"
  key_links:
    - from: 12-VERIFICATION.md
      to: tests/test_role_based_auth.py + test_require_owner.py + test_me_returns_role.py + test_postgres_role_runtime.py + test_bot_role_resolution.py
      via: "evidence section cites each test by name + count of pass/fail"
      pattern: "test_role_based_auth"
    - from: 12-VERIFICATION.md
      to: alembic/versions/0007_postgres_role_split.py
      via: "Manual checkpoint: alembic upgrade/downgrade verifies migration"
      pattern: "0007"
---

<objective>
Wave 5 verification gate. После Plans 12-01..12-06 всё implementation сделано. Этот план:
1. Запускает full automated test suite — собирает evidence
2. Сlowдит per-requirement evidence (ROLE-02..05)
3. Verifies Postgres runtime role split (D-11-07-02) — actually applies migration on dev DB + checks RLS enforcement
4. Verifies legacy fixture sweep (D-11-07-01) — full pytest pass rate ≥ 90%
5. Records remaining manual UAT items (live TG MiniApp smoke — analogous Phase 11)
6. Создаёт `.planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md` (Phase 11 format)

Verification has 2 blocking checkpoints:
- **Checkpoint 1 (human-verify)**: docker compose rebuild + alembic 0007 apply on dev DB. Operator runs the commands per project memory `infra-deploy.md`/`feedback-restart-services.md`.
- **Checkpoint 2 (human-verify)**: live TG MiniApp smoke (open as OWNER, /add via bot, role visible в /me response, member juser denied admin endpoint stub). Optional — может быть deferred to milestone close.

Output: 12-VERIFICATION.md с status либо `passed` либо `human_needed`. Если `human_needed` — Phase 12 functionally complete; проф-смок отложен.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-role-based-auth-refactor/12-CONTEXT.md
@.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
@.planning/phases/11-multi-tenancy-db-migration/deferred-items.md
@.planning/phases/12-role-based-auth-refactor/12-01-PLAN.md
@.planning/phases/12-role-based-auth-refactor/12-02-PLAN.md
@.planning/phases/12-role-based-auth-refactor/12-03-PLAN.md
@.planning/phases/12-role-based-auth-refactor/12-04-PLAN.md
@.planning/phases/12-role-based-auth-refactor/12-05-PLAN.md
@.planning/phases/12-role-based-auth-refactor/12-06-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite — collect evidence для requirements</name>
  <files></files>
  <read_first>
    - tests/test_role_based_auth.py
    - tests/test_require_owner.py
    - tests/test_me_returns_role.py
    - tests/test_postgres_role_runtime.py
    - tests/test_bot_role_resolution.py
  </read_first>
  <behavior>
    Запустить pytest на всех Phase 12 own test files + smoke run полной suite. Записать результаты в текстовый buffer для Task 4.
  </behavior>
  <action>
    1. **Phase 12 own tests** (созданные Plan 12-01):
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         uv run pytest \
           tests/test_role_based_auth.py \
           tests/test_require_owner.py \
           tests/test_me_returns_role.py \
           tests/test_bot_role_resolution.py \
           -v 2>&1 | tee /tmp/phase12_own_tests.log | tail -30
       ```
       Expected: 15+ passed (6 + 3 + 2 + 4)
       test_postgres_role_runtime.py — execute отдельно поскольку зависит от applied migration (Task 2 ниже).

    2. **Phase 11 regression check**:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         uv run pytest \
           tests/test_multitenancy_isolation.py \
           tests/test_rls_policy.py \
           tests/test_migration_backfill.py \
           -v 2>&1 | tee /tmp/phase11_regression.log | tail -20
       ```
       Expected: 14/14 passed (no regression).

    3. **Full suite snapshot** (для D-11-07-01 evidence):
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         uv run pytest tests/ --tb=no -q 2>&1 | tee /tmp/full_suite.log | tail -10
       ```
       Expected: ≥ 250 passed (90%+).

    Сохранить эти 3 logs для Task 4 evidence.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_role_based_auth.py tests/test_require_owner.py tests/test_me_returns_role.py tests/test_bot_role_resolution.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Phase 12 own tests (4 файла, без test_postgres_role_runtime): ≥ 15 passed
    - Phase 11 own tests: 14/14 passed (no regression)
    - Full suite pass rate ≥ 90% (≥ 250 / 276)
  </acceptance_criteria>
  <done>
    Evidence собран в /tmp/phase12_own_tests.log + /tmp/phase11_regression.log + /tmp/full_suite.log.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 1: Apply alembic 0007 + verify runtime Postgres role split (D-11-07-02)</name>
  <what-built>
    Plan 12-05 created alembic 0007 + ADMIN_DATABASE_URL split + docker-compose updates. Operator must rebuild docker stack so entrypoint applies the migration with budget_app role.
  </what-built>
  <how-to-verify>
    Operator (you) выполните в shell:

    1. **Подготовить .env**: убедитесь что `BUDGET_APP_PASSWORD` и `ADMIN_DATABASE_URL` set. Если ещё нет:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner
       grep -q '^BUDGET_APP_PASSWORD=' .env || \
         echo "BUDGET_APP_PASSWORD=$(uv run python -c 'import secrets; print(secrets.token_urlsafe(32))')" >> .env
       # Verify ADMIN_DATABASE_URL exists с budget user; если нет — добавить:
       # ADMIN_DATABASE_URL=postgresql+asyncpg://budget:$(grep ^DB_PASSWORD .env | cut -d= -f2)@localhost:5432/budget_db
       grep -E '^(BUDGET_APP_PASSWORD|ADMIN_DATABASE_URL|DATABASE_URL)=' .env
       ```
       Также verify DATABASE_URL обновлён на budget_app: `^DATABASE_URL=postgresql+asyncpg://budget_app:`

    2. **Rebuild docker stack** (per project memory feedback-restart-services.md):
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         docker compose -f docker-compose.yml -f docker-compose.dev.yml down && \
         docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
       ```
       This triggers entrypoint.sh which runs alembic upgrade head (0006 → 0007).

    3. **Verify migration applied**:
       ```bash
       docker compose exec -T db psql -U budget -d budget_db -c "SELECT version_num FROM alembic_version"
       ```
       Expected: `0007_postgres_role_split` (head).

    4. **Verify role split**:
       ```bash
       docker compose exec -T db psql -U budget -d budget_db -c "\du budget_app"
       ```
       Expected: role exists, attributes show `Cannot login` is FALSE (i.e., LOGIN), and absence of `Superuser`, `Bypass RLS`.

    5. **Verify runtime connects as budget_app**:
       ```bash
       docker compose exec -T api python -c "
       import asyncio, os
       from sqlalchemy.ext.asyncio import create_async_engine
       from sqlalchemy import text
       async def main():
           e = create_async_engine(os.environ['DATABASE_URL'])
           async with e.connect() as c:
               r = await c.execute(text('SELECT current_user, rolsuper FROM pg_roles WHERE rolname=current_user'))
               print(r.one())
           await e.dispose()
       asyncio.run(main())
       "
       ```
       Expected output: `('budget_app', False)`.

    6. **Run runtime tests** (выполняется внутри api контейнера чтобы DATABASE_URL = budget_app):
       ```bash
       docker compose exec -T api /app/.venv/bin/python -m pytest \
         tests/test_postgres_role_runtime.py -v 2>&1 | tail -15
       ```
       Expected: 3/3 passed.

    7. **Verify alembic downgrade/upgrade cycle** (round-trip):
       ```bash
       docker compose exec -T api /app/.venv/bin/python -m alembic downgrade -1
       docker compose exec -T api /app/.venv/bin/python -m alembic upgrade head
       ```
       Expected: clean round-trip; budget_app role dropped then recreated.

    Если ALL 7 шагов PASS — type "approved checkpoint-1". Если что-то fail — describe what failed.
  </how-to-verify>
  <resume-signal>Type "approved checkpoint-1" or describe issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Live TG MiniApp + Bot smoke (optional, can defer to milestone close)</name>
  <what-built>
    Phase 12 changes auth surface: get_current_user role-based, /me returns role, bot uses role lookup. Live verification ensures end-to-end works in real TG environment.
  </what-built>
  <how-to-verify>
    Эти проверки требуют живого TG client + bot/MiniApp running с .env settings. Если не уверены / не хотите рисковать — можно отложить до milestone close (как Phase 11 U-1).

    Если делаете сейчас:

    **Smoke 1 — /me возвращает role**:
    1. Откройте Mini App из бота как OWNER user
    2. В DevTools → Network → запрос на `/api/v1/me` → response body должен содержать `"role": "owner"`

    **Smoke 2 — bot allows OWNER**:
    1. В TG send `/start` к боту
    2. Бот отвечает greeting + WebApp button (НЕ "Бот приватный")
    3. Send `/balance` — бот возвращает баланс текущего периода

    **Smoke 3 — bot rejects revoked**:
    1. SQL update на dev DB: `UPDATE app_user SET role='revoked' WHERE tg_user_id=$OWNER_TG_ID;`
    2. В TG send `/start` к боту
    3. Бот отвечает "Бот приватный."
    4. Send `/balance` — бот silent (нет ответа)
    5. Restore: `UPDATE app_user SET role='owner' WHERE tg_user_id=$OWNER_TG_ID;`

    **Smoke 4 — admin endpoint stub denies member**:
    1. Зарегистрируйте временный stub admin endpoint (например, в `app/api/router.py` после `internal_router`):
       ```python
       from app.api.dependencies import require_owner
       admin_router = APIRouter(prefix="/admin", dependencies=[Depends(require_owner)])
       @admin_router.get("/health")
       async def admin_health(): return {"ok": True}
       app.include_router(admin_router, prefix="/api/v1")
       ```
    2. Restart api: `docker compose restart api`
    3. SQL: `UPDATE app_user SET role='member' WHERE tg_user_id=$OWNER_TG_ID;`
    4. В Mini App curl/devtools: `GET /api/v1/admin/health` → 403
    5. SQL restore: `UPDATE app_user SET role='owner' WHERE tg_user_id=$OWNER_TG_ID;`
    6. `GET /api/v1/admin/health` → 200
    7. Удалите stub — он временный (Phase 13 предоставит реальные admin endpoints)

    Smokes 3 + 4 — risky (требуют SQL манипуляций); можно skip и доверять automated tests + Checkpoint 1.

    Type "approved checkpoint-2" если ALL 4 smokes PASS, "deferred checkpoint-2" если откладываете до milestone close, или describe что fail'нуло.
  </how-to-verify>
  <resume-signal>Type "approved checkpoint-2" / "deferred checkpoint-2" / describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Create 12-VERIFICATION.md from collected evidence</name>
  <files>.planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md (как format reference)
    - /tmp/phase12_own_tests.log (Task 1 output)
    - /tmp/phase11_regression.log (Task 1 output)
    - /tmp/full_suite.log (Task 1 output)
    - Outputs Checkpoint 1 (если operator predоставил)
  </read_first>
  <behavior>
    Создать `.planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md` с frontmatter + Requirements Verification + Manual Checkpoints + Threat Model Attestation + Sign-off.
  </behavior>
  <action>
    Создать файл с таким template (заполнить placeholders реальными данными из logs/checkpoint outputs):

    ```markdown
    ---
    phase: 12-role-based-auth-refactor
    status: <passed|human_needed>   # passed если оба checkpoints approved; human_needed если smoke deferred
    verified-on: <YYYY-MM-DD сегодня>
    verifier: Claude executor (automated) + human (live TG smoke)
    requirements: [ROLE-02, ROLE-03, ROLE-04, ROLE-05]
    resolves: [D-11-07-01, D-11-07-02]
    ---

    # Phase 12 Verification — Role-Based Auth Refactor

    **Phase:** 12 — Role-Based Auth Refactor
    **Verified:** <YYYY-MM-DD>
    **Verifier:** Claude executor (automated steps) + human (live TG MiniApp/bot smoke)
    **Status:** `<status>` — <description: passed → all green; human_needed → smoke deferred>

    ## Status Routing

    - <status emoji> Automated tests (Phase 12 own): <count>/<count> passing
    - <emoji> Phase 11 regression check: 14/14 passing (no regression)
    - <emoji> Full pytest suite: <X>/<Y> passing (<%>) — D-11-07-01 measure
    - <emoji> Alembic 0007 migration cycle: <PASS|FAIL>
    - <emoji> Runtime Postgres role: budget_app NOSUPERUSER NOBYPASSRLS — <PASS|FAIL>
    - <emoji> Live TG MiniApp/bot smoke: <PASS|deferred|FAIL>

    ## Requirements Verification

    ### ROLE-02: OWNER_TG_ID-eq removed from request-pipeline

    - [<x>] PASS — `grep -v '^\s*#' app/api/dependencies.py | grep -E '== settings.OWNER_TG_ID|!= settings.OWNER_TG_ID' | wc -l` → 0; only DEV_MODE upsert path uses settings.OWNER_TG_ID для bootstrap
    - [<x>] PASS — `grep -v '^\s*#' app/bot/handlers.py app/bot/commands.py | grep settings.OWNER_TG_ID | wc -l` → 0
    - [<x>] Evidence — test_owner_tg_id_eq_no_longer_in_get_current_user passing (verifies via ast.parse)
    - [<x>] Evidence — Plan 12-04 grep gate в acceptance_criteria

    ### ROLE-03: get_current_user role-based whitelist

    - [<x>] PASS — get_current_user returns AppUser ORM; raises 403 на role==revoked OR unknown tg_user_id
    - [<x>] Evidence — test_revoked_user_gets_403, test_member_user_gets_200, test_owner_user_gets_200, test_unknown_tg_user_id_gets_403, test_get_current_user_returns_app_user_orm — все passing
    - [<x>] Evidence — bot path: bot_resolve_user_role + _check_user_role_async; test_bot_role_resolution.py 4/4 passing

    ### ROLE-04: require_owner dependency

    - [<x>] PASS — require_owner экспортирован из app/api/dependencies.py
    - [<x>] Evidence — test_require_owner_allows_owner, test_require_owner_blocks_member, test_require_owner_blocks_revoked — все passing
    - [<x>] Phase 13 admin endpoints will use `Depends(require_owner)` (out of scope для Phase 12 — но dep ready)

    ### ROLE-05: GET /api/v1/me returns role

    - [<x>] PASS — MeResponse pydantic model содержит `role: Literal["owner","member","revoked"]`
    - [<x>] PASS — frontend MeResponse interface содержит `role: UserRole` ('owner' | 'member' | 'revoked')
    - [<x>] Evidence — test_me_includes_role_for_owner, test_me_includes_role_for_member — passing
    - [<x>] Frontend tsc --noEmit clean (no MeResponse-related errors)

    ## Manual Checkpoints (Plan 12-07)

    - [<x>] Step 1 — Phase 12 own tests pytest: <X>/<Y> passing
    - [<x>] Step 2 — Phase 11 regression: 14/14 passing
    - [<x>] Step 3 — Alembic 0007 upgrade applied; budget_app role exists с NOSUPERUSER NOBYPASSRLS
    - [<x>] Step 4 — Runtime DATABASE_URL connects as budget_app (current_user check)
    - [<x>] Step 5 — Alembic downgrade/upgrade cycle clean
    - [<x>] Step 6 — Full pytest suite ≥ 90% pass rate (D-11-07-01 fix)
    - [<x or deferred>] Step 7 — Live TG MiniApp/bot smoke (Checkpoint 2)

    ## Resolved Deferred Items

    ### D-11-07-01 — Legacy test fixtures (Phase 11 carryover)

    Status: ✅ RESOLVED in Plan 12-06.

    - 22 test files updated (либо direct edit либо через single_user fixture / seed helpers)
    - tests/helpers/seed.py central factory module создан
    - tests/conftest.py single_user fixture added
    - Pass rate улучшилась с ~213/276 (Phase 11 broken) до <X>/<Y> (Phase 12 sweep)
    - Никаких NotNullViolationError на user_id columns

    ### D-11-07-02 — Runtime Postgres role split

    Status: ✅ RESOLVED in Plan 12-05.

    - Alembic 0007 creates budget_app NOSUPERUSER NOBYPASSRLS role
    - DATABASE_URL splits: runtime → budget_app, admin (alembic) → budget via ADMIN_DATABASE_URL
    - entrypoint.sh + docker-compose.yml + docker-compose.dev.yml + .env.example updated
    - Runtime tests verify: current_user='budget_app', rolsuper=False, rolbypassrls=False
    - RLS now enforces при runtime queries без _rls_test_role workaround

    ## Threat Model Attestation

    Aggregated STRIDE register from Plans 12-01..12-07. Each threat либо mitigated (со ссылкой на code/test) либо accepted (с rationale).

    | Threat ID | Category | Component | Disposition | Evidence |
    |-----------|----------|-----------|-------------|----------|
    | T-12-02-01 | E (Elevation) | Spoofed role via initData manipulation | mitigate | initData HMAC ensures tg_user_id authenticity (app/core/auth.py); role read from DB only |
    | T-12-02-02 | E | Member accesses admin endpoint | mitigate | require_owner dep raises 403; tested test_require_owner_blocks_member |
    | T-12-02-03 | T (Tampering) | Token replay after role revoked | mitigate | get_current_user reads role on every request, no caching |
    | T-12-02-04 | I (Info Disclosure) | Error detail distinguishes revoked/unknown | mitigate | All paths return generic "Not authorized" |
    | T-12-04-01 | E | Revoked user retains bot access until restart (cache stale) | mitigate | bot_resolve_user_role does fresh SELECT; tested test_bot_role_resolution_revoked |
    | T-12-04-02 | I | Bot replies different для non-allowed | mitigate | All non-allowed → "Бот приватный"; structured log differentiates for ops |
    | T-12-05-01 | E | Runtime app uses super role | mitigate | docker-compose enforces budget_app для DATABASE_URL; verified via test_runtime_database_url_uses_nosuperuser_role |
    | T-12-05-04 | E | budget_app excess privileges | mitigate | Migration grants ONLY SELECT/INSERT/UPDATE/DELETE; no CREATE/ALTER/TRIGGER |
    | T-12-05-06 | S | Operator forgets BUDGET_APP_PASSWORD | mitigate | Migration raises RuntimeError loudly с instructions |

    Threats accepted (rationale):
    - T-12-02-05 (DEV_MODE in production) — existing mitigation: validate_production_settings refuses to start
    - T-12-02-06 (extra SELECT per request) — single indexed lookup, <1ms
    - T-12-04-05 (DB call per bot command) — human-paced bot, negligible

    ## Out of Scope (deferred to later phases)

    - Phase 13: Admin endpoints implementation (`/api/v1/admin/users`, `/api/v1/admin/ai-usage` etc.)
    - Phase 13: Frontend admin tab visibility (uses /me role)
    - Phase 14: Onboarding flow для приглашённых members (включая bot bind for non-owners)
    - Phase 15: AI cost cap per user (`spending_cap_cents`)

    ## Notes / Issues found

    <Заполнить если что-то найдено в Tasks 1-3>

    ## Sign-off

    - Date: <YYYY-MM-DD>
    - Status: <passed | human_needed>
    - <Если human_needed>: Awaiting Checkpoint 2 (live TG smoke) — может быть deferred до milestone v0.4 close
    - <Если passed>: Phase 12 functionally complete; ready for Phase 13 (Admin UI)

    ---
    *Verification completed: <YYYY-MM-DD>*
    *Resolves: D-11-07-01 (legacy fixture sweep) + D-11-07-02 (Postgres role split)*
    ```

    После создания файла — `git add` + commit с message:
    ```
    docs(12-07): create 12-VERIFICATION.md (Phase 12 Role-Based Auth Refactor)

    - Resolves D-11-07-01 + D-11-07-02
    - Status: <passed|human_needed>
    - Phase 12 functionally complete; ready for Phase 13
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && test -f .planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md && head -10 .planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 12-VERIFICATION.md существует
    - Frontmatter содержит phase, status, verified-on, verifier, requirements, resolves
    - Requirements Verification section покрывает ROLE-02..05 (4 sub-sections)
    - Manual Checkpoints section отражает все шаги Tasks 1-3
    - Threat Model Attestation section существует
    - Sign-off block заполнен
  </acceptance_criteria>
  <done>
    Phase 12 verification artifact created; status documented; ready для Phase 13.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(Aggregated from Plans 12-01..12-06; no new boundaries introduced by verification phase itself.)

## STRIDE Threat Register

(Aggregated table уже в Task 4 12-VERIFICATION.md content above. No verification-specific threats — этот план только записывает observations.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | T (Tampering) | Verification artifact misrepresents test results | mitigate | Evidence сохраняется в /tmp/*.log; verifier — automated pytest output; human checkpoints required для smoke claims |
</threat_model>

<verification>
- 12-VERIFICATION.md создан с full structure
- Все Phase 12 requirements (ROLE-02..05) имеют evidence
- D-11-07-01 + D-11-07-02 explicitly marked RESOLVED
- Either both checkpoints approved → status=passed, OR Checkpoint 2 deferred → status=human_needed
- File committed to git
</verification>

<success_criteria>
- Phase 12 verified end-to-end (automated + manual)
- 12-VERIFICATION.md committed
- D-11-07-01 + D-11-07-02 closed (linked from this verification)
- Phase 12 ready for Phase 13 hand-off
- Threat model attested with evidence per threat
</success_criteria>

<output>
After completion, create `.planning/phases/12-role-based-auth-refactor/12-07-SUMMARY.md` (1-2 paragraph summary linking to 12-VERIFICATION.md)
</output>
