---
phase: 12-role-based-auth-refactor
plan: 04
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - app/bot/auth.py
  - app/bot/handlers.py
  - app/bot/commands.py
autonomous: true
requirements: [ROLE-02, ROLE-03]
tags: [bot, role-based, owner-removal, db-lookup, threat-model-mitigation]
must_haves:
  truths:
    - "Новый модуль app/bot/auth.py создаётся с async функцией bot_resolve_user_role(tg_user_id: int) -> UserRole | None"
    - "bot_resolve_user_role открывает свежий AsyncSession через AsyncSessionLocal, делает SELECT role FROM app_user WHERE tg_user_id = :tg_user_id, возвращает UserRole или None если не найден"
    - "app/bot/handlers.py::cmd_start больше НЕ сравнивает user_id != settings.OWNER_TG_ID — теперь резолвит role через bot_resolve_user_role; пропускает только role IN (UserRole.owner, UserRole.member); revoked + None → silent reject (старая семантика 'Бот приватный')"
    - "app/bot/commands.py::_is_owner заменена на _check_user_role_async(message_or_callback, allowed_roles: tuple[UserRole, ...]) async helper — выполняет тот же DB lookup; все 5 callsites (cmd_add line 233, cmd_balance line 307, cmd_today line 324, cmd_app line 338, cb_disambiguation line 358) обновлены на await call"
    - "settings.OWNER_TG_ID не сравнивается на runtime в app/bot/handlers.py + app/bot/commands.py (grep по этим двум файлам — 0 occurrences вне docstrings/comments)"
    - "Все existing bot tests (test_bot_handlers.py, test_bot_handlers_phase4.py, test_bot_parsers.py) либо pass либо имеют tracked обновления fixtures (если регрессия — handover Plan 12-06)"
    - "4 RED tests из Plan 12-01/test_bot_role_resolution.py становятся GREEN"
  artifacts:
    - path: app/bot/auth.py
      provides: "bot_resolve_user_role async helper для бот-side role lookup"
      contains: "bot_resolve_user_role"
      contains_2: "AsyncSessionLocal"
      min_lines: 20
    - path: app/bot/handlers.py
      provides: "cmd_start использует role-based check вместо OWNER_TG_ID-eq"
      contains: "bot_resolve_user_role"
    - path: app/bot/commands.py
      provides: "5 commands + 1 callback используют role-based check"
      contains: "bot_resolve_user_role"
  key_links:
    - from: app/bot/handlers.py::cmd_start
      to: app/bot/auth.py::bot_resolve_user_role
      via: "await bot_resolve_user_role(tg_user_id) → UserRole | None; allow if owner/member, silent reject otherwise"
      pattern: "bot_resolve_user_role"
    - from: app/bot/commands.py::cmd_add (и 4 других)
      to: app/bot/auth.py::bot_resolve_user_role (через _check_user_role_async helper)
      via: "проверка role IN (owner, member) перед internal API call; silent reject для revoked/unknown"
      pattern: "_check_user_role_async\\|bot_resolve_user_role"
    - from: app/bot/auth.py::bot_resolve_user_role
      to: app/db/session.py::AsyncSessionLocal
      via: "shared async engine; новая session per call; commit/rollback"
      pattern: "AsyncSessionLocal"
---

<objective>
Удалить OWNER_TG_ID-eq проверки из бот-кода (`app/bot/handlers.py` + `app/bot/commands.py`); заменить на role-based DB lookup через новый helper `bot_resolve_user_role`.

Конкретно:
1. **Создать `app/bot/auth.py`** — новый модуль с async helper:
   ```python
   async def bot_resolve_user_role(tg_user_id: int) -> UserRole | None:
       """Look up app_user.role by tg_user_id; return None if user not found."""
   ```
2. **`app/bot/handlers.py::cmd_start`** — заменить line 58 (`if user_id != settings.OWNER_TG_ID`) на role-based check
3. **`app/bot/commands.py`** — refactor `_is_owner` → `_check_user_role_async`:
   - Принимает `(message_or_callback, allowed_roles=(UserRole.owner, UserRole.member))` и возвращает bool
   - 5 callsites (cmd_add, cmd_income, cmd_balance, cmd_today, cmd_app, cb_disambiguation) обновляются на `if not await _check_user_role_async(...)`
   - Удалить старую `_is_owner` функцию

После этого Plan'а: 4 RED tests из Plan 12-01/test_bot_role_resolution.py → GREEN.

Output: 3 файла обновлены/созданы; 0 occurrences settings.OWNER_TG_ID-eq в bot runtime path; 4 tests GREEN.

**Зависимость от 12-02**: bot use case фактически не использует FastAPI dependencies, но bot_resolve_user_role читает то же AppUser ORM что и Phase 12-02 — так что concept'но share с 12-02 changes; runtime — independent (separate process, shared DB engine).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-role-based-auth-refactor/12-CONTEXT.md
@CLAUDE.md
@app/bot/handlers.py
@app/bot/commands.py
@app/bot/api_client.py
@app/db/session.py
@app/db/models.py
@app/core/settings.py
@tests/test_bot_role_resolution.py
@tests/test_bot_handlers.py

<interfaces>
CURRENT app/bot/handlers.py:55-62:
```python
user_id = message.from_user.id
# OWNER_TG_ID whitelist (HLD §5, AUTH-02 carry-over from Phase 1)
if user_id != settings.OWNER_TG_ID:
    await message.answer("Бот приватный.")
    logger.info("bot.start.rejected_non_owner", tg_user_id=user_id)
    return
```

CURRENT app/bot/commands.py:82-86:
```python
def _is_owner(message_or_callback: Message | CallbackQuery) -> bool:
    """Return True if the sender is the configured OWNER_TG_ID."""
    user = getattr(message_or_callback, "from_user", None)
    return user is not None and user.id == settings.OWNER_TG_ID
```

CURRENT callsites of `_is_owner` в commands.py:
- Line 233: `if not user or not _is_owner(message):` (cmd_add/cmd_income via _handle_add_or_income)
- Line 307: `if not user or not _is_owner(message):` (cmd_balance)
- Line 324: `if not user or not _is_owner(message):` (cmd_today)
- Line 338: `if not _is_owner(message):` (cmd_app)
- Line 358: `if not _is_owner(callback):` (cb_disambiguation)

TARGET app/bot/auth.py (NEW FILE):
```python
"""Bot-side authorization helpers (Phase 12 ROLE-02/03).

Bot shares the async engine with api/worker (см. app/db/session.py) — so it
can perform direct DB lookups for user role without going through internal
API. Adding role-aware checks at handler entry preserves "silent reject" UX
для non-whitelisted users (no information disclosure).
"""
from __future__ import annotations

from sqlalchemy import select

from app.db.models import AppUser, UserRole
from app.db.session import AsyncSessionLocal


async def bot_resolve_user_role(tg_user_id: int) -> UserRole | None:
    """Return AppUser.role for tg_user_id; None if no AppUser row exists.

    Opens a fresh AsyncSession (bot is a separate process; cannot reuse
    FastAPI request-scoped session). Read-only — no commit needed.
    """
    async with AsyncSessionLocal() as session:
        result = await session.execute(
            select(AppUser.role).where(AppUser.tg_user_id == tg_user_id)
        )
        return result.scalar_one_or_none()
```

TARGET app/bot/handlers.py (cmd_start change, line ~55-62):
```python
from app.bot.auth import bot_resolve_user_role
from app.db.models import UserRole

# inside cmd_start:
user_id = message.from_user.id
role = await bot_resolve_user_role(user_id)
if role not in (UserRole.owner, UserRole.member):
    # revoked, unknown, or DB unreachable → silent reject (carry-over UX)
    await message.answer("Бот приватный.")
    logger.info("bot.start.rejected", tg_user_id=user_id, role=role)
    return
```

TARGET app/bot/commands.py (helper rewrite + 5 callsite updates):
```python
from app.bot.auth import bot_resolve_user_role
from app.db.models import UserRole

async def _check_user_role_async(
    message_or_callback: Message | CallbackQuery,
    *,
    allowed_roles: tuple[UserRole, ...] = (UserRole.owner, UserRole.member),
) -> bool:
    """Role-based whitelist check (replaces _is_owner from Phase 1)."""
    user = getattr(message_or_callback, "from_user", None)
    if user is None:
        return False
    role = await bot_resolve_user_role(user.id)
    return role in allowed_roles

# Then в _handle_add_or_income / cmd_balance / cmd_today / cmd_app / cb_disambiguation:
# OLD: if not user or not _is_owner(message): return
# NEW: if not await _check_user_role_async(message): return
```

Note for `_handle_add_or_income`: текущий код проверяет `if not user or not _is_owner(message)` — `_check_user_role_async` уже handle'ит `user is None` через `if user is None: return False`. → Можно убрать internal `if not user` гард или сохранить (defensive). → Сохранить both для clarity (user может быть None в caller для отдельной semantic — silent ignore service messages). Конкретно: `if not user: return` (преrserved); затем `if not await _check_user_role_async(message): return` (silent reject для non-whitelisted).

CURRENT test_bot_role_resolution.py expectations (Plan 12-01):
```python
from app.bot.auth import bot_resolve_user_role
from app.db.models import UserRole

# Test seeded owner with tg_user_id=9_444_444_001
result = await bot_resolve_user_role(9_444_444_001)
assert result is UserRole.owner  # for owner test
# member test: assert result is UserRole.member
# revoked test: assert result is UserRole.revoked
# unknown test: assert result is None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/bot/auth.py — bot_resolve_user_role helper</name>
  <files>app/bot/auth.py</files>
  <read_first>
    - app/db/session.py (AsyncSessionLocal, async_engine)
    - app/db/models.py (AppUser, UserRole)
    - tests/test_bot_role_resolution.py (4 tests, contract)
  </read_first>
  <behavior>
    После Task 1 + DB seed:
    - bot_resolve_user_role(tg_owner) с seed AppUser(tg_user_id=tg_owner, role=owner) → UserRole.owner
    - bot_resolve_user_role(tg_member) → UserRole.member
    - bot_resolve_user_role(tg_revoked) → UserRole.revoked
    - bot_resolve_user_role(tg_unknown) → None
  </behavior>
  <action>
    Создать `app/bot/auth.py`:

    ```python
    """Bot-side authorization helpers (Phase 12 ROLE-02 / ROLE-03).

    Bot is a separate process from api but shares the async SQLAlchemy engine
    via app/db/session.py (single Postgres pool, multiple consumers). For
    role-aware command handlers we perform a direct AppUser lookup rather
    than calling the api over HTTP — keeps latency low (single SELECT) and
    avoids a network round-trip + initData synthesis just to read a column.

    Trust boundary note: bot already trusts settings.BOT_TOKEN to authenticate
    Telegram updates. Role enforcement here is the second gate (after Telegram
    sender identity) — ensures revoked / non-whitelisted Telegram accounts
    cannot send /add etc. via the bot even if they reach the long-poll stream.
    """
    from __future__ import annotations

    from sqlalchemy import select

    from app.db.models import AppUser, UserRole
    from app.db.session import AsyncSessionLocal


    async def bot_resolve_user_role(tg_user_id: int) -> UserRole | None:
        """Return AppUser.role for tg_user_id; None if no AppUser row exists.

        Opens a fresh AsyncSession (bot is its own asyncio loop; cannot reuse
        FastAPI request-scoped session). Read-only query — no commit needed,
        session closes via context manager.

        Args:
            tg_user_id: Telegram user id from message.from_user.id /
                callback.from_user.id.

        Returns:
            UserRole enum if the user is whitelisted; None for unknown
            tg_user_id (Phase 14 onboarding will pre-create rows for invitees).
        """
        async with AsyncSessionLocal() as session:
            result = await session.execute(
                select(AppUser.role).where(AppUser.tg_user_id == tg_user_id)
            )
            return result.scalar_one_or_none()
    ```

    Verify import:
    ```bash
    python -c "from app.bot.auth import bot_resolve_user_role; print('OK')"
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.bot.auth import bot_resolve_user_role; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Файл существует; import OK
    - `grep -c 'AsyncSessionLocal' app/bot/auth.py` → 1
    - `grep -c 'AppUser.role' app/bot/auth.py` → 1
  </acceptance_criteria>
  <done>
    Helper готов; bot_resolve_user_role экспортирован.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor app/bot/handlers.py + app/bot/commands.py — заменить OWNER_TG_ID-eq на role check</name>
  <files>app/bot/handlers.py, app/bot/commands.py</files>
  <read_first>
    - app/bot/handlers.py (полный — line 1-103)
    - app/bot/commands.py (lines 1-100 + lines 220-400 — все callsites _is_owner)
    - tests/test_bot_handlers.py (existing tests — verify which fixtures need update)
    - tests/test_bot_handlers_phase4.py
  </read_first>
  <behavior>
    После refactor:
    - cmd_start с tg_user=OWNER (role=owner в DB) → бот связывает chat + отвечает greeting
    - cmd_start с tg_user=member → бот связывает chat + greeting (теперь members разрешены — это новая семантика; Phase 14 onboarding flow добавит более полное greeting для members, тут просто generic accept)
    - cmd_start с tg_user=revoked → "Бот приватный" + log
    - cmd_start с tg_user=unknown (нет AppUser строки) → "Бот приватный" + log
    - /add от member → команда работает (раньше member не существовал — теперь да)
    - /add от revoked → silent return
    - /add от unknown → silent return

    Existing bot tests могут регрессировать (они ожидают OWNER_TG_ID-eq behavior). Если так — отметить в SUMMARY как known regression. Минимально: tests которые тестируют positive owner path должны pass; tests которые тестируют "non-owner получает silent reject" — могут потребовать обновления fixture (seed AppUser(tg_user_id=test_id, role=revoked) если они хотят 'rejected' behavior). Этот план НЕ обновляет existing bot tests — Plan 12-06 (fixture sweep) handle.
  </behavior>
  <action>
    **Часть A: app/bot/handlers.py**

    1. Импорты — добавить:
       ```python
       from app.bot.auth import bot_resolve_user_role
       from app.db.models import UserRole
       ```
       Удалить, если становится unused: `from app.core.settings import settings` — НО `settings.MINI_APP_URL` всё ещё используется в `_open_app_keyboard` (line 45) → settings keep imported.

    2. В `cmd_start` (line ~52-62) заменить блок:
       ```python
       # OLD:
       if user_id != settings.OWNER_TG_ID:
           await message.answer("Бот приватный.")
           logger.info("bot.start.rejected_non_owner", tg_user_id=user_id)
           return
       ```
       На:
       ```python
       # NEW (Phase 12 ROLE-02/03):
       role = await bot_resolve_user_role(user_id)
       if role not in (UserRole.owner, UserRole.member):
           await message.answer("Бот приватный.")
           logger.info(
               "bot.start.rejected",
               tg_user_id=user_id,
               role=role.value if role else None,
           )
           return
       ```

    3. Обновить module docstring (top of file lines 5-12) — заменить:
       ```
       - ``/start`` from non-OWNER → ``"Бот приватный"``. Defence-in-depth: ...
       ```
       На:
       ```
       - ``/start`` from non-whitelisted (revoked/unknown role) → ``"Бот приватный"``.
         Phase 12: role check via bot_resolve_user_role replaces OWNER_TG_ID-eq.
         Defence-in-depth: Telegram delivers messages from chats the user opens,
         we re-check role per HLD §5 + Phase 12 CONTEXT.
       ```

    **Часть B: app/bot/commands.py**

    1. Импорты — добавить:
       ```python
       from app.bot.auth import bot_resolve_user_role
       from app.db.models import UserRole
       ```
       Сохранить existing `from app.core.settings import settings` — `settings.MINI_APP_URL` используется в `_build_app_kbd` (line 109).

    2. **Удалить** функцию `_is_owner` (lines 82-86) полностью.

    3. **Добавить** новую async helper в её место:
       ```python
       async def _check_user_role_async(
           message_or_callback: Message | CallbackQuery,
           *,
           allowed_roles: tuple[UserRole, ...] = (UserRole.owner, UserRole.member),
       ) -> bool:
           """Phase 12 role-based whitelist check (replaces _is_owner).

           Single SELECT roundtrip (cached at FastAPI dep level not applicable
           here — bot has no request-scope cache; one DB call per command is
           negligible cost).
           """
           user = getattr(message_or_callback, "from_user", None)
           if user is None:
               return False
           role = await bot_resolve_user_role(user.id)
           return role in allowed_roles
       ```

    4. **Обновить 5 callsites** (заменить `_is_owner(...)` на `await _check_user_role_async(...)`):
       - Line ~233 в `_handle_add_or_income`:
         ```python
         # OLD: if not user or not _is_owner(message):
         # NEW: if not await _check_user_role_async(message):
         ```
         (`_check_user_role_async` уже handle'ит user=None, так что `if not user or` — redundant, но keep для defensive readability оптимально удалить).

         **Решение**: использовать просто `if not await _check_user_role_async(message): return`.

         Однако `_handle_add_or_income` ниже использует `user.id` (line ~249 `tg_user_id=user.id`). Если убираем check `if not user`, нужно гарантировать `user is not None` ДО use. `_check_user_role_async` returns False для user=None — handler return'нул, ОК. Но typing: `user: User | None`. Вариант: keep `if not user: return` отдельно, потом `if not await _check_user_role_async(message): return`.

         **Final**:
         ```python
         user = message.from_user
         if not user:
             return  # silent — service message
         if not await _check_user_role_async(message):
             return  # silent — non-whitelisted (T-04-30)
         ```

       - Line ~307 в `cmd_balance`:
         ```python
         user = message.from_user
         if not user or not await _check_user_role_async(message):
             return
         ```

       - Line ~324 в `cmd_today`: аналогично cmd_balance.

       - Line ~338 в `cmd_app` (нет `user` variable — handler immediately checks):
         ```python
         if not await _check_user_role_async(message):
             return
         ```

       - Line ~358 в `cb_disambiguation`:
         ```python
         if not await _check_user_role_async(callback):
             await callback.answer()  # silent dismiss (T-04-37)
             return
         ```

    5. Module docstring (top of file lines 1-16) — обновить упоминание OWNER:
       ```
       OWNER-only enforcement (Phase 1 → Phase 12 refactored): all handlers
       silently return (no message.answer) for non-whitelisted users (role NOT
       IN (owner, member)) to avoid spam and information disclosure (T-04-30,
       T-04-37). Phase 12 introduced role-based check via bot_resolve_user_role.
       ```

    **Verify обоих файлов**:
    ```bash
    grep -n '_is_owner\|settings.OWNER_TG_ID' app/bot/handlers.py app/bot/commands.py
    ```
    Ожидаемое: 0 matches (вне docstrings/comments).

    ```bash
    grep -n 'bot_resolve_user_role\|_check_user_role_async' app/bot/handlers.py app/bot/commands.py
    ```
    Ожидаемое: handlers.py — 1+ matches; commands.py — 6+ matches (1 import + 1 def + 5 callsites in commands).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && grep -v '^\s*#\|^\s*"""' app/bot/handlers.py app/bot/commands.py | grep 'settings\.OWNER_TG_ID\|_is_owner' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Команда выше → 0 (никаких OWNER_TG_ID-eq и _is_owner ссылок в коде)
    - `python -c "import app.bot.handlers; import app.bot.commands; print('OK')"` → "OK"
    - `grep -c 'bot_resolve_user_role' app/bot/handlers.py app/bot/commands.py` → ≥ 2 (handlers + commands import + use)
    - 5 callsites обновлены (cmd_add/income, cmd_balance, cmd_today, cmd_app, cb_disambiguation)
  </acceptance_criteria>
  <done>
    Bot runtime больше не сравнивает OWNER_TG_ID-eq; role-based check во всех handlers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run pytest для test_bot_role_resolution.py — 4 tests GREEN; check existing bot tests</name>
  <files></files>
  <read_first>
    - tests/test_bot_role_resolution.py
    - tests/test_bot_handlers.py
    - tests/test_bot_handlers_phase4.py
  </read_first>
  <behavior>
    После Tasks 1-2 4 RED tests из 12-01 → GREEN. Existing bot tests могут регрессировать (старая ожидаемая семантика "non-OWNER reject"); they нормально fixed Plan 12-06.
  </behavior>
  <action>
    Запустить:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest \
        tests/test_bot_role_resolution.py \
        tests/test_bot_handlers.py \
        tests/test_bot_handlers_phase4.py \
        -v --tb=short 2>&1 | tail -40
    ```

    Expected:
    - test_bot_role_resolution.py: 4 passed (GREEN)
    - test_bot_handlers.py: 0 passed | существующие могут падать (Phase 1 tests assumed sync _is_owner). Если падают — отметить в SUMMARY как regression to fix in Plan 12-06.
    - test_bot_handlers_phase4.py: возможные regressions (silent_non_owner check assumes _is_owner — могут падать)

    **Acceptance**:
    - test_bot_role_resolution.py — must be 4/4 GREEN (HARD requirement, blocks Plan)
    - test_bot_handlers + test_bot_handlers_phase4 — soft requirement; if some regressions, document them in 12-04-SUMMARY.md as "carryover for Plan 12-06" — Plan 12-06 (fixture sweep) is the natural place to update bot tests anyway because old fixtures don't seed AppUser.

    Если test_bot_role_resolution не зелёный — debug:
    - Может быть Plan 12-01 fixture `fresh_db` не TRUNCATE'ит ai_message/ai_conversation/category_embedding (зависит от FK — tests могут падать на second run). Verify TRUNCATE list в fixture покрывает все 9 доменных таблиц + app_user.
    - Если fixture скипается — DATABASE_URL не установлен. Запустить с `DATABASE_URL=$(grep '^DATABASE_URL=' .env | cut -d= -f2-)`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_bot_role_resolution.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test_bot_role_resolution.py: 4 passed
    - bot_resolve_user_role работает для owner / member / revoked / unknown сценариев
  </acceptance_criteria>
  <done>
    4 RED tests из Plan 12-01 → GREEN. Любые regressions в test_bot_handlers documented в SUMMARY как Plan 12-06 carryover.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram Updates → Bot long-poll | Bot trusts BOT_TOKEN-authenticated updates from Telegram |
| Bot → DB (shared engine) | bot_resolve_user_role reads role; trust boundary identical to api process |
| Bot command handler → internal API | Existing INTERNAL_TOKEN-protected calls (cb_disambiguation → bot_create_actual etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-01 | E (Elevation) | Revoked user retains bot access until restart (cache stale) | mitigate | bot_resolve_user_role does fresh SELECT every command — no caching; revoked propagates within one command turnaround |
| T-12-04-02 | I (Info Disclosure) | Bot replies different message to non-whitelisted vs revoked vs unknown | mitigate | Same "Бот приватный" reply for ALL non-allowed roles (revoked + unknown + None); structured log distinguishes for ops, but external behaviour identical |
| T-12-04-03 | E | Member sends /add command (was OWNER-only previously) | accept | Phase 12 design opens /add to members (per ROLE-03) — they have their own data partition (Phase 11); /add будет писать в их own actual_transaction rows. Phase 14 onboarding ensures member has BudgetPeriod before /add works. До Phase 14 — first /add для member может вернуть error если онbording не пройден; this is acceptable interim. |
| T-12-04-04 | T (Tampering) | settings.OWNER_TG_ID stale in code path bypasses role check | mitigate | grep gate (verify Task 2 acceptance) — 0 OWNER_TG_ID-eq в bot runtime files; CI can repeat grep |
| T-12-04-05 | D (DoS) | Each bot command = extra SELECT | accept | Single indexed lookup (uq tg_user_id), <1ms; bot throughput is human-paced (1 cmd/few-seconds), DB unaffected |
| T-12-04-06 | S | Bot calls bot_resolve_user_role with crafted tg_user_id (bypass) | mitigate | tg_user_id comes from message.from_user.id which is signed by Telegram (BOT_TOKEN); if attacker forges Telegram update, BOT_TOKEN already compromised — bigger problem |
</threat_model>

<verification>
- `grep -v '^\s*#\|^\s*"""' app/bot/handlers.py app/bot/commands.py | grep -E 'settings\.OWNER_TG_ID|_is_owner\(' | wc -l` → 0
- `grep -c 'bot_resolve_user_role' app/bot/auth.py app/bot/handlers.py app/bot/commands.py` → ≥ 4
- pytest tests/test_bot_role_resolution.py: 4 passed
- `python -c "import app.bot.auth; import app.bot.handlers; import app.bot.commands; print('OK')"` → "OK"
</verification>

<success_criteria>
- ROLE-02 satisfied (bot path): settings.OWNER_TG_ID-eq removed from bot runtime
- ROLE-03 satisfied (bot path): role-based whitelist applied at every command entry
- 4 RED tests из Plan 12-01 (test_bot_role_resolution) → GREEN
- bot_resolve_user_role helper создан и documented
- Existing bot test regressions (test_bot_handlers) documented для Plan 12-06 fixture sweep
</success_criteria>

<output>
After completion, create `.planning/phases/12-role-based-auth-refactor/12-04-SUMMARY.md`
</output>
