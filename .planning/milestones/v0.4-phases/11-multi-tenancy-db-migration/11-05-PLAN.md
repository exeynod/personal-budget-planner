---
phase: 11-multi-tenancy-db-migration
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - app/services/categories.py
  - app/services/periods.py
  - app/services/templates.py
  - app/services/planned.py
  - app/services/onboarding.py
  - app/services/settings.py
  - app/api/routes/categories.py
  - app/api/routes/periods.py
  - app/api/routes/templates.py
  - app/api/routes/planned.py
  - app/api/routes/onboarding.py
  - app/api/routes/settings.py
autonomous: true
requirements: [MUL-03, MUL-04]
tags: [services, routes, refactor, multitenancy, user-id-param]
must_haves:
  truths:
    - "Все public функции в 6 service modules (categories, periods, templates, planned, onboarding, settings) принимают `user_id: int` параметр и используют его в queries (.where(Model.user_id == user_id) или INSERT user_id=user_id)"
    - "Все 6 routes (categories, periods, templates, planned, onboarding, settings) используют Depends(get_db_with_tenant_scope) и Depends(get_current_user_id) — передают user_id в service-вызовы"
    - "Существующие service-функции, которые принимали tg_user_id (settings, onboarding) — продолжают работать через tg_user_id как раньше (изменение в этих функциях — добавляется user_id PK для записи в Mapped column)"
    - "Каждое INSERT новой строки доменной таблицы (Category, BudgetPeriod, PlanTemplateItem, PlannedTransaction) явно устанавливает user_id=user_id"
    - "list/get queries имеют .where(Model.user_id == user_id) — RLS — backstop, app-side filtering — primary"
    - "Существующие тесты pytest tests/test_categories.py, test_periods.py, test_planned.py, test_templates.py, test_onboarding.py, test_settings.py не сломаны (могут быть локально исправлены — проброс user_id где раньше передавался tg_user_id; не должны деградировать)"
  artifacts:
    - path: app/services/categories.py
      provides: "Все CAT функции принимают user_id, scoped queries"
    - path: app/services/periods.py
      provides: "Все PER функции принимают user_id"
    - path: app/services/templates.py
      provides: "Все template функции принимают user_id"
    - path: app/services/planned.py
      provides: "Все planned txn функции принимают user_id"
    - path: app/services/onboarding.py
      provides: "Onboarding функции получают user_id PK + tg_user_id (для backwards compat)"
    - path: app/services/settings.py
      provides: "Settings функции — резолв через user_id PK или tg_user_id (обе сигнатуры допустимы)"
  key_links:
    - from: app/api/routes/categories.py (list_categories handler)
      to: app/services/categories.py (list_categories)
      via: "user_id передаётся через Depends(get_current_user_id) в route, далее в service kwargs"
      pattern: "user_id=user_id"
    - from: app/services/categories.py (list_categories)
      to: app/db/models.py (Category.user_id)
      via: "select(Category).where(Category.user_id == user_id)"
      pattern: "Category.user_id ==.*user_id"
---

<objective>
Refactor service layer + route layer (часть A: 6 модулей) для явного user_id scoping. Каждая функция, которая запрашивает или модифицирует данные доменных таблиц (category, budget_period, plan_template_item, planned_transaction), получает `user_id: int` параметр и применяет его в WHERE clause или INSERT field.

Принципы (MUL-03):
- App-side filtering — primary защита: `select(Model).where(Model.user_id == user_id)` явно
- RLS — defense-in-depth backstop: SET LOCAL уже стоит через get_db_with_tenant_scope
- INSERT новых строк — всегда с `user_id=user_id`
- Существующие сигнатуры не сломаны без необходимости — где раньше принимался `tg_user_id`, оставляем (settings.py, onboarding.py — функции операют над AppUser строкой по tg_user_id)

Этот план — часть A: categories, periods, templates, planned, onboarding, settings (~ половина файлов). Часть B (actuals, subscriptions, analytics, AI, internal_bot, worker) — Plan 11-06, выполняется параллельно но не пересекается по файлам.

Output: 12 файлов обновлены; локальные тесты не падают (или адаптированы).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md
@CLAUDE.md
@app/db/models.py
@app/api/dependencies.py
@app/services/categories.py
@app/services/periods.py
@app/services/templates.py
@app/services/planned.py
@app/services/onboarding.py
@app/services/settings.py
@app/api/routes/categories.py
@app/api/routes/periods.py
@app/api/routes/templates.py
@app/api/routes/planned.py
@app/api/routes/onboarding.py
@app/api/routes/settings.py

<interfaces>
Pattern для route handler (target):
```python
from app.api.dependencies import get_current_user_id, get_db_with_tenant_scope

@categories_router.get("", response_model=list[CategoryRead])
async def list_categories(
    db: Annotated[AsyncSession, Depends(get_db_with_tenant_scope)],
    user_id: Annotated[int, Depends(get_current_user_id)],
    include_archived: bool = False,
) -> list[CategoryRead]:
    cats = await cat_svc.list_categories(
        db, user_id=user_id, include_archived=include_archived
    )
    return [CategoryRead.model_validate(c) for c in cats]
```

Pattern для service function (target):
```python
async def list_categories(
    db: AsyncSession,
    *,
    user_id: int,
    include_archived: bool = False,
) -> list[Category]:
    stmt = select(Category).where(Category.user_id == user_id)
    if not include_archived:
        stmt = stmt.where(Category.is_archived.is_(False))
    stmt = stmt.order_by(Category.kind, Category.sort_order, Category.name)
    result = await db.execute(stmt)
    return list(result.scalars().all())


async def create_category(
    db: AsyncSession, *, user_id: int, name: str, kind: str, sort_order: int = 0
) -> Category:
    cat = Category(
        user_id=user_id,
        name=name,
        kind=CategoryKind(kind),
        sort_order=sort_order,
        is_archived=False,
    )
    db.add(cat)
    await db.flush()
    await db.refresh(cat)
    return cat


async def get_or_404(
    db: AsyncSession, category_id: int, *, user_id: int
) -> Category:
    result = await db.execute(
        select(Category).where(
            Category.id == category_id,
            Category.user_id == user_id,
        )
    )
    cat = result.scalar_one_or_none()
    if cat is None:
        raise CategoryNotFoundError(category_id)
    return cat
```

В service signature предпочитаем `*, user_id: int` (keyword-only) для безопасности — caller не может перепутать позиционными аргументами.

Существующая dependency граф (call sites):
- categories.py — вызывается из routes/categories.py + ai_conversation_service (вкладывает в conversation messages — Plan 11-06)
- periods.py — routes/periods.py + worker close_period.py (Plan 11-06)
- templates.py — routes/templates.py + planned.py (apply template)
- planned.py — routes/planned.py + ai/tools.py (chat tools)
- onboarding.py — routes/onboarding.py
- settings.py — routes/settings.py + worker (resolve cycle_start_day)

Подход к onboarding и settings: они operate over AppUser row by tg_user_id (legacy contract). Сохраняем существующую сигнатуру `tg_user_id: int`, но если функция CREATE'ит/UPDATE'ит доменные строки (например seed_default_categories внутри onboarding) — добавляем internal user_id resolution через AppUser lookup. Чисто read функции на AppUser таблице (settings get/set) — НЕ трогаем (AppUser не имеет RLS, доступен напрямую). Phase 12 уберёт OWNER_TG_ID eq и сделает auth role-based.

Обработка seed_default_categories (categories.py):
```python
async def seed_default_categories(
    db: AsyncSession, *, user_id: int
) -> list[Category]:
    """Insert SEED_CATEGORIES для конкретного user_id. Идемпотентно: skip если у этого user уже есть категории."""
    existing_count = await db.scalar(
        select(func.count())
        .select_from(Category)
        .where(Category.user_id == user_id)
    )
    if existing_count and existing_count > 0:
        return []
    rows = [
        Category(
            user_id=user_id,
            name=name,
            kind=CategoryKind(kind),
            sort_order=sort_order,
            is_archived=False,
        )
        for name, kind, sort_order in SEED_CATEGORIES
    ]
    db.add_all(rows)
    await db.flush()
    for cat in rows:
        await db.refresh(cat)
    return rows
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor app/services/categories.py + app/api/routes/categories.py</name>
  <files>app/services/categories.py, app/api/routes/categories.py</files>
  <read_first>
    - app/services/categories.py (целиком)
    - app/api/routes/categories.py (целиком)
    - app/db/models.py (Category model)
    - app/api/dependencies.py (get_current_user_id, get_db_with_tenant_scope сигнатуры)
  </read_first>
  <action>
**В app/services/categories.py:**

Изменить сигнатуры всех async функций добавив `*, user_id: int` keyword-only:

1. `list_categories(db, *, user_id: int, include_archived: bool = False)`:
   - Старт `stmt = select(Category)` → ЗАМЕНИТЬ на `stmt = select(Category).where(Category.user_id == user_id)`
   - Остальное без изменений.

2. `create_category(db, *, user_id: int, name: str, kind: str, sort_order: int = 0)`:
   - Заменить `cat = Category(name=..., kind=..., sort_order=..., is_archived=False)` на `cat = Category(user_id=user_id, name=..., kind=..., sort_order=..., is_archived=False)`.

3. `get_or_404(db, category_id, *, user_id: int)`:
   - `select(Category).where(Category.id == category_id)` → `select(Category).where(Category.id == category_id, Category.user_id == user_id)`

4. `update_category(db, category_id, patch, *, user_id: int)`:
   - Внутри вызывает `get_or_404` — теперь нужно передать user_id.

5. `archive_category(db, category_id, *, user_id: int)`:
   - Та же замена — пробросить user_id в get_or_404.

6. `seed_default_categories(db, *, user_id: int)`:
   - existing_count: добавить `.where(Category.user_id == user_id)` к запросу.
   - Каждый Category(...) создаётся с `user_id=user_id`.

**В app/api/routes/categories.py:**

1. Обновить импорт:
   ```python
   from app.api.dependencies import (
       get_current_user,
       get_current_user_id,
       get_db,
       get_db_with_tenant_scope,
   )
   ```

2. В каждом handler:
   - Заменить `db: Annotated[AsyncSession, Depends(get_db)]` на `db: Annotated[AsyncSession, Depends(get_db_with_tenant_scope)]`
   - Добавить параметр `user_id: Annotated[int, Depends(get_current_user_id)]`
   - В вызове service-функции передать `user_id=user_id`

3. `_refresh_embedding(category_id, name)` background task — НЕ трогаем здесь (relates к AI categorization, embeddings — touched в Plan 11-06).

4. POST /categories handler create_category — service get'ит `user_id=user_id`; existing flow сохраняется (background tasks работают как раньше).

5. PATCH/DELETE handlers — пробрасывают user_id в `get_or_404` через service.update_category / service.archive_category.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.services.categories import list_categories, create_category, get_or_404, update_category, archive_category, seed_default_categories; from app.api.routes.categories import categories_router" && grep -c 'user_id' app/services/categories.py app/api/routes/categories.py</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse: `python -c "import ast; ast.parse(open('app/services/categories.py').read()); ast.parse(open('app/api/routes/categories.py').read())"` exits 0
    - Imports work: `python -c "from app.services.categories import list_categories, create_category, get_or_404, update_category, archive_category, seed_default_categories; from app.api.routes.categories import categories_router"` exits 0
    - `grep -c "user_id" app/services/categories.py` >= 8 (use sites)
    - `grep -c "Category.user_id == user_id" app/services/categories.py` >= 3
    - `grep -c "user_id=user_id" app/services/categories.py` >= 2 (Category INSERT)
    - Routes use new deps: `grep -c "get_db_with_tenant_scope" app/api/routes/categories.py` >= 1, `grep -c "get_current_user_id" app/api/routes/categories.py` >= 1
    - Все 4 handler функции (list, create, update, delete) принимают user_id: `grep -c "user_id: Annotated\[int, Depends(get_current_user_id)\]" app/api/routes/categories.py` >= 4
  </acceptance_criteria>
  <done>app/services/categories.py: 6 функций принимают user_id keyword-only; все queries и INSERT scoped по user_id. app/api/routes/categories.py: 4 handlers используют get_db_with_tenant_scope + get_current_user_id, передают user_id в service</done>
</task>

<task type="auto">
  <name>Task 2: Refactor app/services/periods.py + app/services/templates.py + соответствующие routes</name>
  <files>app/services/periods.py, app/services/templates.py, app/api/routes/periods.py, app/api/routes/templates.py</files>
  <read_first>
    - app/services/periods.py (целиком)
    - app/services/templates.py (целиком)
    - app/api/routes/periods.py (целиком)
    - app/api/routes/templates.py (целиком)
    - app/db/models.py (BudgetPeriod, PlanTemplateItem)
    - app/api/dependencies.py
  </read_first>
  <action>
**В app/services/periods.py (~73 строк, 4-5 функций):**

Найти все async-функции (примеры: get_active_period, list_periods, create_period_if_missing, _today_in_app_tz):
- Каждая функция, читающая или мутирующая BudgetPeriod, получает `*, user_id: int`.
- В каждом `select(BudgetPeriod)` добавить `.where(BudgetPeriod.user_id == user_id)`.
- В create логике BudgetPeriod(...) — добавить `user_id=user_id`.
- Helper `_today_in_app_tz()` (без user-scope) — НЕ трогаем (системная утилита, не запрос).

**В app/services/templates.py (~174 строк):**

Каждая async-функция, оперирующая PlanTemplateItem или PlannedTransaction:
- Сигнатура: `*, user_id: int`
- `select(PlanTemplateItem)` → `.where(PlanTemplateItem.user_id == user_id)`
- `select(PlannedTransaction)` → `.where(PlannedTransaction.user_id == user_id)`
- Любой `PlanTemplateItem(...)` или `PlannedTransaction(...)` create — добавить `user_id=user_id`.

**В app/api/routes/periods.py (~83 строк):**

Все handlers:
- Заменить `Depends(get_db)` → `Depends(get_db_with_tenant_scope)`
- Добавить `user_id: Annotated[int, Depends(get_current_user_id)]`
- Передавать `user_id=user_id` в service вызовы
- Импорт `get_current_user_id, get_db_with_tenant_scope` из dependencies

**В app/api/routes/templates.py (~175 строк):**

То же самое для всех handlers.

**Важно:** не трогать `_today_in_app_tz` и подобные системные helpers без user-scope.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.services import periods, templates; from app.api.routes import periods as r_periods, templates as r_templates" && grep -c 'user_id' app/services/periods.py app/services/templates.py app/api/routes/periods.py app/api/routes/templates.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files parse: `python -c "import ast; [ast.parse(open(f).read()) for f in ['app/services/periods.py','app/services/templates.py','app/api/routes/periods.py','app/api/routes/templates.py']]"` exits 0
    - Imports succeed: `python -c "from app.services import periods, templates; from app.api.routes import periods as rp, templates as rt"` exits 0
    - `grep -c "BudgetPeriod.user_id == user_id" app/services/periods.py` >= 1
    - `grep -c "user_id=user_id" app/services/periods.py` >= 1 (BudgetPeriod INSERT)
    - `grep -c "PlanTemplateItem.user_id == user_id" app/services/templates.py` >= 1
    - `grep -c "get_db_with_tenant_scope" app/api/routes/periods.py app/api/routes/templates.py` >= 2 (один на файл)
    - `grep -c "get_current_user_id" app/api/routes/periods.py app/api/routes/templates.py` >= 2
  </acceptance_criteria>
  <done>periods + templates services и routes refactored: все доменные queries/INSERT scoped по user_id; routes используют новые dependencies</done>
</task>

<task type="auto">
  <name>Task 3: Refactor app/services/planned.py + соответствующий route + onboarding + settings</name>
  <files>app/services/planned.py, app/api/routes/planned.py, app/services/onboarding.py, app/api/routes/onboarding.py, app/services/settings.py, app/api/routes/settings.py</files>
  <read_first>
    - app/services/planned.py (целиком — ~335 строк)
    - app/api/routes/planned.py (целиком — ~216 строк)
    - app/services/onboarding.py (целиком — ~122 строки)
    - app/api/routes/onboarding.py (целиком — ~74 строки)
    - app/services/settings.py (целиком — ~119 строк)
    - app/api/routes/settings.py (целиком — ~99 строк)
    - app/db/models.py (PlannedTransaction, AppUser)
  </read_first>
  <action>
**В app/services/planned.py:**

Все async-функции, читающие или мутирующие PlannedTransaction или PlanTemplateItem:
- Добавить `*, user_id: int`
- `select(PlannedTransaction).where(PlannedTransaction.period_id == period_id)` → добавить `.where(PlannedTransaction.user_id == user_id)`
- `select(PlanTemplateItem)` → `.where(PlanTemplateItem.user_id == user_id)`
- Любой `PlannedTransaction(...)` create — `user_id=user_id`
- Apply template logic — пробрасывает user_id в каждый созданный PlannedTransaction

**В app/api/routes/planned.py:**

Все handlers — заменить get_db → get_db_with_tenant_scope, добавить user_id Depends, передать в service.

**В app/services/onboarding.py:**

Существующая сигнатура `complete_onboarding(db, *, tg_user_id: int, ...)` — в ней:
1. Резолвит AppUser строку через `select(AppUser).where(AppUser.tg_user_id == tg_user_id)`.
2. Создаёт BudgetPeriod, начальные категории.
3. Сейчас вызывает `seed_default_categories(db)` — после Plan 11-05 Task 1 эта функция требует user_id.

ИЗМЕНЕНИЯ:
- Сохранить сигнатуру `tg_user_id: int` в complete_onboarding (входная точка из routes).
- Внутри — после lookup `user = (await session.execute(select(AppUser).where(AppUser.tg_user_id == tg_user_id))).scalar_one()`, использовать `user.id` (PK) для всех downstream calls:
  - `await seed_default_categories(db, user_id=user.id)`
  - При создании BudgetPeriod — `BudgetPeriod(user_id=user.id, period_start=..., ...)`.

**В app/api/routes/onboarding.py:**

Handlers упрощаются — onboarding роуты могут продолжать использовать обычный `get_db` (НЕ get_db_with_tenant_scope), потому что в момент onboarding юзер только что создан и user_id PK lookup сделан внутри service. ОДНАКО RLS требует SET LOCAL — пред-onboarding state (юзер уже в app_user) → user_id уже есть → можно использовать get_db_with_tenant_scope. Пока сохраняем `get_db` для onboarding routes (consistency с Phase 14 где onboarding flow поменяется).

Проверить что роут вызывает `await onb_svc.complete_onboarding(db, tg_user_id=current_user["id"], ...)` — в принципе уже так работает.

Action: НЕ менять onboarding routes/services сигнатуры наружу; внутренние вызовы service-функций (например `seed_default_categories`) обновить на новые сигнатуры с user_id=user.id.

**В app/services/settings.py:**

Существующая `_get_user_or_404(db, tg_user_id)` — оставить.
Существующие функции get_settings/update_settings оперируют только AppUser row — НЕ доменные таблицы. AppUser таблица НЕ имеет RLS (только 9 доменных) — НЕ нужен SET LOCAL. Сигнатуры могут оставаться tg_user_id-based.

Action: settings.py service — НЕ менять (никаких queries на user-scoped таблицы — только AppUser).

**В app/api/routes/settings.py:**

Routes используют `get_db` (не нужен tenant scope) — НЕ менять. Существующие handlers оперируют над AppUser напрямую.

Если какой-то handler в settings DOES query доменную таблицу (например, balance, что-то связанное с категориями) — switch на get_db_with_tenant_scope.

ПРОВЕРИТЬ: пробежаться grep'ом — `grep -E 'select\(.*Category|select\(.*BudgetPeriod|select\(.*PlannedTransaction|select\(.*ActualTransaction|select\(.*Subscription' app/services/settings.py app/api/routes/settings.py`. Если есть matches — добавить user_id scoping и tenant_scope в эти места. Если matches нет — оставить как есть.

**Финальная верификация для всех 6 файлов:** запустить `python -c "from app.services import planned, onboarding, settings; from app.api.routes import planned as rp, onboarding as ro, settings as rs"` — exits 0.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.services import planned, onboarding, settings; from app.api.routes import planned as rp, onboarding as ro, settings as rs" && grep -c 'user_id=user.id\|PlannedTransaction.user_id == user_id' app/services/planned.py app/services/onboarding.py</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files parse: `python -c "import ast; [ast.parse(open(f).read()) for f in ['app/services/planned.py','app/services/onboarding.py','app/services/settings.py','app/api/routes/planned.py','app/api/routes/onboarding.py','app/api/routes/settings.py']]"` exits 0
    - Imports work: `python -c "from app.services import planned, onboarding, settings; from app.api.routes import planned as rp, onboarding as ro, settings as rs"` exits 0
    - planned.py service: `grep -c "PlannedTransaction.user_id == user_id\|user_id=user_id" app/services/planned.py` >= 2
    - planned.py route: `grep -c "get_db_with_tenant_scope" app/api/routes/planned.py` >= 1
    - onboarding service: `grep -c "user_id=user.id\|user.id" app/services/onboarding.py` >= 1
    - onboarding service вызывает seed_default_categories с user_id: `grep -c "seed_default_categories(.*user_id=" app/services/onboarding.py` >= 1
    - settings.py service: AppUser-only (no domain table queries needed): `grep -E 'select\(.*Category|select\(.*BudgetPeriod|select\(.*Planned|select\(.*Actual|select\(.*Subscription' app/services/settings.py | wc -l` returns either 0 (no matches, no change needed) OR if non-zero, those queries scoped by user_id.
    - settings.py route — sanity check: domain queries scoped, либо отсутствуют
  </acceptance_criteria>
  <done>planned + onboarding + settings services/routes consistent: planned scoped по user_id; onboarding пробрасывает user.id internally; settings.py не нуждается в tenant scope (AppUser-only)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route handler → service | user_id передаётся explicit kwarg; route не может «забыть» — Pylance/mypy ловит missing required argument |
| Service → DB query | Каждый query на доменную таблицу имеет .where(Model.user_id == user_id) — primary defense (RLS — backstop) |
| Onboarding flow → seed_default_categories | tg_user_id → AppUser lookup → user.id передаётся в service (никогда tg_user_id не утекает в Category INSERT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | Cross-tenant data leak — забытый WHERE clause в одной из service функций | mitigate | Каждая функция имеет explicit `*, user_id: int` keyword-only; mypy/runtime ловит missing arg. RLS — backstop (Plan 11-02 уже включил). Plan 11-07 добавит integration test multi-tenant isolation. |
| T-11-05-02 | Tampering | INSERT новой строки без user_id → IntegrityError NOT NULL после миграции | mitigate | ORM model (Plan 11-03) объявил user_id NOT NULL; SQLAlchemy сам потребует. Service explicitly создаёт с `user_id=user_id`. |
| T-11-05-03 | Spoofing | user_id «правится» атакующим | accept | user_id резолвится из validated tg_user_id (HMAC initData); never из request body. Get_current_user_id — only path. |
| T-11-05-04 | Tampering | Apply template создаёт PlannedTransaction с чужим user_id | mitigate | apply_template service принимает user_id и передаёт в каждый создаваемый PlannedTransaction; template_item filtered по user_id. Sanity test в Plan 11-07. |
| T-11-05-05 | Information Disclosure | get_or_404(category_id) для чужой категории возвращает данные | mitigate | get_or_404 теперь имеет `Category.user_id == user_id` фильтр; чужая категория → CategoryNotFoundError → 404 (не 403, не 200). |
| T-11-05-06 | Information Disclosure | RLS не активен (отсутствует SET LOCAL) → query видит все строки | mitigate | get_db_with_tenant_scope dep ставит SET LOCAL ДО первого query; routes не должны использовать `get_db` где требуется user-scope. Audit в Plan 11-07. |
</threat_model>

<verification>
1. `python -c "from app.services import categories, periods, templates, planned, onboarding, settings; from app.api.routes import categories as rc, periods as rp, templates as rt, planned as rpl, onboarding as ro, settings as rs"` — exits 0.
2. `grep -c "get_db_with_tenant_scope" app/api/routes/categories.py app/api/routes/periods.py app/api/routes/templates.py app/api/routes/planned.py` >= 4 (по одному на файл).
3. `grep -c "user_id=user_id\|user_id: int" app/services/categories.py app/services/periods.py app/services/templates.py app/services/planned.py` >= 8.
4. Существующие тесты (test_categories.py, test_periods_api.py, test_planned.py, test_templates.py, test_onboarding.py, test_settings.py) — могут локально нуждаться в правке (передача user_id вместо tg_user_id), но не должны деградировать функционально. Эти правки — часть acceptance этого таска ИЛИ откладываются на Plan 11-07 (где integration suite перезапускается).
5. Audit grep `grep -rE 'select\(.*Category[^E]' app/services/ | grep -v 'user_id'` — должен быть пустым (все queries на Category имеют user_id фильтр или это select(Category.user_id) специфический).
</verification>

<success_criteria>
- 6 service files refactored: каждая public async function принимает user_id: int (keyword-only) и применяет в queries/INSERT
- 6 route files refactored: handlers используют get_db_with_tenant_scope + get_current_user_id, передают user_id в service
- onboarding service внутренне резолвит user_id PK из tg_user_id, пробрасывает в seed_default_categories и BudgetPeriod create
- settings service — без изменений (AppUser-only, не доменная таблица)
- Все файлы парсятся, импорт без ошибок
- Существующие тесты не сломаны (могут потребовать локальной правки сигнатур fixture-callees — это OK)
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-tenancy-db-migration/11-05-SUMMARY.md` documenting:
- Список 12 файлов с описанием изменений (service: добавлен user_id keyword-only; route: новые deps)
- Особенность onboarding (резолв user_id PK внутри service из tg_user_id input)
- Особенность settings (AppUser-only — не нуждается в tenant scope)
- Pointer на Plan 11-06 для actuals/subs/analytics/AI/internal_bot/worker
</output>
