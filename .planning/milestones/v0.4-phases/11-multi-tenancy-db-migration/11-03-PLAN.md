---
phase: 11-multi-tenancy-db-migration
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - app/db/models.py
autonomous: true
requirements: [MUL-01, ROLE-01]
tags: [orm, models, multitenancy, role-enum]
must_haves:
  truths:
    - "Все 9 ORM моделей доменных таблиц имеют Mapped[int] user_id колонку с ForeignKey('app_user.id', ondelete='RESTRICT'), nullable=False"
    - "AppUser ORM модель имеет Mapped[UserRole] role колонку с PgEnum(UserRole, name='user_role', create_type=False), nullable=False, default=UserRole.member"
    - "Класс UserRole(str, enum.Enum) добавлен в models.py с тремя значениями owner/member/revoked"
    - "Существующие unique constraints в __table_args__ обновлены: budget_period получает UniqueConstraint('user_id', 'period_start', name='uq_budget_period_user_id_period_start'); category получает UniqueConstraint('user_id', 'name', name='uq_category_user_id_name'); subscription получает UniqueConstraint('user_id', 'name', name='uq_subscription_user_id_name')"
    - "Старый комментарий в docstring 'Single-tenant MVP: NO user_id FK on any table' удалён или заменён на 'Multi-tenant since Phase 11: user_id FK on 9 domain tables'"
    - "from typing import Optional не сломан; новые импорты добавлены если нужны"
    - "Файл парсится как валидный Python, mypy/ruff не валится при базовом import"
  artifacts:
    - path: app/db/models.py
      provides: "9 доменных моделей с user_id Mapped column + AppUser.role + UserRole enum"
      contains: "class UserRole"
      contains_2: "user_id"
  key_links:
    - from: app/db/models.py (Category, BudgetPeriod, Subscription, ...)
      to: app/db/models.py (AppUser.id)
      via: "ForeignKey('app_user.id', ondelete='RESTRICT')"
      pattern: "ForeignKey..app_user.id"
    - from: app/db/models.py (AppUser.role)
      to: alembic/versions/0006_*.py (user_role enum)
      via: "PgEnum(..., name='user_role', create_type=False) — type создан миграцией"
      pattern: "name=.user_role"
---

<objective>
Обновить SQLAlchemy ORM модели в `app/db/models.py` чтобы они отражали schema после миграции 0006:
1. Добавить класс `UserRole(str, enum.Enum)` с values `owner`, `member`, `revoked`
2. На `AppUser` добавить `role: Mapped[UserRole]` колонку
3. На каждой из 9 доменных моделей добавить `user_id: Mapped[int]` с FK→app_user.id ON DELETE RESTRICT
4. Обновить `__table_args__` где есть scoped unique constraints (Category, BudgetPeriod, Subscription)
5. Удалить устаревший комментарий «Single-tenant MVP: NO user_id FK on any table» из module docstring

Этот план НЕ меняет миграции, services, routes, deps. Только декларативные ORM mappings — чтобы код Python соответствовал schema после `alembic upgrade head` (применённой Plan 11-02).

Output: обновлённый app/db/models.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md
@CLAUDE.md
@app/db/models.py
@app/db/base.py

<interfaces>
Текущая структура app/db/models.py (важные классы и где они):

```python
import enum
from datetime import date, datetime
from typing import Optional
from pgvector.sqlalchemy import Vector
from sqlalchemy import (BigInteger, Boolean, Date, ForeignKey, Index, Integer, String, Text, UniqueConstraint)
from sqlalchemy.dialects.postgresql import ENUM as PgEnum
from sqlalchemy.orm import Mapped, mapped_column, relationship
from sqlalchemy.sql import func
from sqlalchemy.types import TIMESTAMP

# Enums (уже есть): CategoryKind, PeriodStatus, PlanSource, ActualSource, SubCycle

# Доменные модели:
class AppUser(Base):
    __tablename__ = "app_user"
    id, tg_user_id, tg_chat_id, cycle_start_day, notify_days_before,
    enable_ai_categorization, onboarded_at, created_at
    # role будет ДОБАВЛЕНА здесь.

class Category(Base):
    __tablename__ = "category"
    id, name, kind, is_archived, sort_order, created_at
    # user_id ДОБАВИТЬ + __table_args__ с UniqueConstraint(user_id, name)

class BudgetPeriod(Base):
    __tablename__ = "budget_period"
    id, period_start (unique=True), period_end, starting_balance_cents, ending_balance_cents, status, closed_at
    # period_start unique=True УДАЛИТЬ из колонки + ДОБАВИТЬ __table_args__ UniqueConstraint(user_id, period_start)
    relationships: planned_transactions, actual_transactions

class PlanTemplateItem(Base):
    __tablename__ = "plan_template_item"
    id, category_id (FK), amount_cents, description, day_of_period, sort_order
    # user_id ДОБАВИТЬ. Никакого scoped unique не требовалось CONTEXT'ом — пропускаем.
    relationships: category

class Subscription(Base):
    __tablename__ = "subscription"
    id, name, amount_cents, cycle, next_charge_date, category_id (FK), notify_days_before, is_active
    __table_args__ = (Index("ix_subscription_active_charge", ...),)
    # user_id ДОБАВИТЬ. ОБНОВИТЬ __table_args__ для добавления UniqueConstraint(user_id, name)
    relationships: category

class PlannedTransaction(Base):
    __tablename__ = "planned_transaction"
    id, period_id (FK), kind, amount_cents, description, category_id (FK), planned_date, source, subscription_id (FK), original_charge_date
    __table_args__ = (Index("ix_planned_period_kind", ...), UniqueConstraint("subscription_id", "original_charge_date", name="uq_planned_sub_charge_date"))
    # user_id ДОБАВИТЬ. __table_args__ остаётся, новые НЕ добавляем.
    relationships: period, category

class ActualTransaction(Base):
    __tablename__ = "actual_transaction"
    id, period_id (FK), kind, amount_cents, description, category_id (FK), tx_date, source, created_at
    __table_args__ = (Index("ix_actual_period_kind", ...), Index("ix_actual_category_date", ...))
    # user_id ДОБАВИТЬ.
    relationships: period, category

class AppHealth(Base):
    # Не доменная — НЕ трогаем.

class AiConversation(Base):
    __tablename__ = "ai_conversation"
    id, created_at
    # user_id ДОБАВИТЬ.
    relationships: messages

class AiMessage(Base):
    __tablename__ = "ai_message"
    id, conversation_id (FK), role, content, tool_name, tool_result, created_at
    __table_args__ = (Index("ix_ai_message_conversation", ...),)
    # user_id ДОБАВИТЬ. (CONTEXT уточняет: direct user_id FK для query simplicity, не через conversation join)

class CategoryEmbedding(Base):
    __tablename__ = "category_embedding"
    category_id (PK FK ondelete=CASCADE), embedding (Vector), updated_at
    # user_id ДОБАВИТЬ — отдельная колонка с FK на app_user.id RESTRICT.
```

Pattern для Mapped column:
```python
user_id: Mapped[int] = mapped_column(
    BigInteger,
    ForeignKey("app_user.id", ondelete="RESTRICT"),
    nullable=False,
)
```

Pattern для UserRole enum mapping:
```python
class UserRole(str, enum.Enum):
    owner = "owner"
    member = "member"
    revoked = "revoked"

# В AppUser:
role: Mapped[UserRole] = mapped_column(
    PgEnum(UserRole, name="user_role", create_type=False),
    nullable=False,
    server_default="member",
    default=UserRole.member,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Обновить app/db/models.py — добавить UserRole enum + AppUser.role + user_id на 9 моделей + scoped unique constraints</name>
  <files>app/db/models.py</files>
  <read_first>
    - app/db/models.py (целиком — нужно понимать каждый существующий класс)
    - app/db/base.py (целиком — чтобы убедиться что Base импортируется правильно)
    - alembic/versions/0006_multitenancy_user_id_rls_role.py (только-что созданный — для reference имён constraints, должны совпадать с миграцией)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (Schema Decisions)
  </read_first>
  <action>
Через Edit tool сделать следующие изменения в `app/db/models.py`:

**Изменение 1: модульный docstring**

Найти строки docstring, которые сейчас содержат:
```
- Single-tenant MVP: NO user_id FK on any table.
```
Заменить на:
```
- Multi-tenant since Phase 11: user_id BIGINT NOT NULL FK → app_user.id ON DELETE RESTRICT
  присутствует на всех 9 доменных таблицах. RLS policies (см. alembic 0006).
```

**Изменение 2: добавить UserRole enum**

После существующих enum'ов (после `class SubCycle(str, enum.Enum): ...`) и перед `# ---------- Models ----------`, ДОБАВИТЬ:

```python
class UserRole(str, enum.Enum):
    """Роль пользователя (Phase 11 ROLE-01).

    owner   — единственный администратор; backfill для существующего OWNER_TG_ID.
    member  — приглашённый юзер (default для новых через invite в Phase 13).
    revoked — отозванный доступ; auth-слой блокирует (полная семантика в Phase 12).
    """

    owner = "owner"
    member = "member"
    revoked = "revoked"
```

**Изменение 3: AppUser — добавить role колонку**

В классе `AppUser`, после колонки `enable_ai_categorization` и ПЕРЕД `onboarded_at`, ДОБАВИТЬ:

```python
    role: Mapped[UserRole] = mapped_column(
        PgEnum(UserRole, name="user_role", create_type=False),
        nullable=False,
        server_default="member",
        default=UserRole.member,
    )
```

**Изменение 4: Category — добавить user_id + scoped unique**

В классе `Category`, ПОСЛЕ колонки `created_at`, ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )

    __table_args__ = (
        UniqueConstraint("user_id", "name", name="uq_category_user_id_name"),
    )
```

**Изменение 5: BudgetPeriod — переместить unique с period_start на (user_id, period_start)**

В классе `BudgetPeriod`:
- Найти строку `period_start: Mapped[date] = mapped_column(Date, nullable=False, unique=True)` и УБРАТЬ `unique=True` — оставить только `mapped_column(Date, nullable=False)`
- ПОСЛЕ всех колонок (после `closed_at`) и ПЕРЕД relationships (`planned_transactions: Mapped[list...`), ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

- ПОСЛЕ relationships блока, ДОБАВИТЬ `__table_args__`:

```python
    __table_args__ = (
        UniqueConstraint(
            "user_id",
            "period_start",
            name="uq_budget_period_user_id_period_start",
        ),
    )
```

**Изменение 6: PlanTemplateItem — добавить user_id (без scoped unique по CONTEXT — пропускаем unique)**

В классе `PlanTemplateItem`, ПОСЛЕ колонки `sort_order` и ПЕРЕД relationships (`category: Mapped["Category"]`), ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

**Изменение 7: Subscription — добавить user_id + добавить scoped unique в существующий __table_args__**

В классе `Subscription`:
- ПОСЛЕ колонки `is_active` и ПЕРЕД relationships, ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

- В существующем `__table_args__` (`(Index("ix_subscription_active_charge", "is_active", "next_charge_date"),)`) ДОБАВИТЬ дополнительный элемент UniqueConstraint:

```python
    __table_args__ = (
        Index("ix_subscription_active_charge", "is_active", "next_charge_date"),
        UniqueConstraint("user_id", "name", name="uq_subscription_user_id_name"),
    )
```

**Изменение 8: PlannedTransaction — добавить user_id (НЕ менять __table_args__)**

В классе `PlannedTransaction`, ПОСЛЕ колонки `original_charge_date` и ПЕРЕД relationships, ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

(существующий __table_args__ с ix_planned_period_kind и uq_planned_sub_charge_date — не менять.)

**Изменение 9: ActualTransaction — добавить user_id (НЕ менять __table_args__)**

В классе `ActualTransaction`, ПОСЛЕ колонки `created_at` и ПЕРЕД relationships, ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

**Изменение 10: AiConversation — добавить user_id**

В классе `AiConversation`, ПОСЛЕ `created_at` и ПЕРЕД relationships (`messages: Mapped[list...`), ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

**Изменение 11: AiMessage — добавить user_id (НЕ менять __table_args__)**

В классе `AiMessage`, ПОСЛЕ `created_at` и ПЕРЕД relationships (`conversation: Mapped...`), ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

**Изменение 12: CategoryEmbedding — добавить user_id**

В классе `CategoryEmbedding`, ПОСЛЕ `updated_at` и ПЕРЕД relationships (`category: Mapped...`), ДОБАВИТЬ:

```python
    user_id: Mapped[int] = mapped_column(
        BigInteger,
        ForeignKey("app_user.id", ondelete="RESTRICT"),
        nullable=False,
    )
```

**НЕ менять:** AppHealth (не доменная), импорты (BigInteger, ForeignKey, UniqueConstraint, Index, PgEnum уже импортированы — проверить grep), enum-классы CategoryKind/PeriodStatus/PlanSource/ActualSource/SubCycle.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.db import models; assert hasattr(models, 'UserRole'); assert hasattr(models.AppUser, 'role'); for cls_name in ['Category','BudgetPeriod','PlanTemplateItem','Subscription','PlannedTransaction','ActualTransaction','AiConversation','AiMessage','CategoryEmbedding']: cls=getattr(models, cls_name); assert hasattr(cls, 'user_id'), cls_name+' missing user_id'; print('OK', cls_name)"</automated>
  </verify>
  <acceptance_criteria>
    - File parses: `python -c "import ast; ast.parse(open('app/db/models.py').read())"` exits 0
    - Import works: `python -c "from app.db import models"` exits 0
    - `grep -c "class UserRole" app/db/models.py` returns `1`
    - Все 9 доменных классов имеют user_id: `python -c "from app.db import models as m; [getattr(getattr(m,c),'user_id') for c in ['Category','BudgetPeriod','PlanTemplateItem','Subscription','PlannedTransaction','ActualTransaction','AiConversation','AiMessage','CategoryEmbedding']]"` exits 0
    - AppUser имеет role: `python -c "from app.db.models import AppUser, UserRole; assert hasattr(AppUser, 'role')"` exits 0
    - `grep -c "uq_category_user_id_name" app/db/models.py` >= 1
    - `grep -c "uq_subscription_user_id_name" app/db/models.py` >= 1
    - `grep -c "uq_budget_period_user_id_period_start" app/db/models.py` >= 1
    - `grep -c "ondelete=.RESTRICT" app/db/models.py` >= 9 (минимум 9 доменных таблиц)
    - `grep -c "name=.user_role" app/db/models.py` >= 1
    - Старый комментарий НЕ присутствует: `grep -c "Single-tenant MVP: NO user_id" app/db/models.py` returns `0`
    - period_start без unique=True: `grep "period_start.*unique=True" app/db/models.py` exits non-zero (no match)
  </acceptance_criteria>
  <done>app/db/models.py обновлён: UserRole enum добавлен; AppUser.role с PgEnum mapping; 9 доменных моделей имеют Mapped[int] user_id с FK ondelete=RESTRICT; scoped unique constraints на Category/BudgetPeriod/Subscription; импорт чистый</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM models → DB schema | После миграции 0006 schema имеет user_id NOT NULL FK; ORM models ДОЛЖНЫ это отражать иначе SQLAlchemy будет генерировать INSERT без user_id — IntegrityError на каждом write |
| ORM models → app code | Service layer (Plan 11-05/06) опирается на доступность атрибута `Model.user_id` для `.where(Model.user_id == user_id)` — без него код не скомпилируется |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | ORM модель забыла user_id, INSERT пропускает колонку, IntegrityError NOT NULL | mitigate | Pythonic check в acceptance_criteria — все 9 классов имеют user_id атрибут (проверяется через getattr). |
| T-11-03-02 | Tampering | Wrong FK ondelete (CASCADE вместо RESTRICT) — revoke кроет соседних юзеров | mitigate | Все ondelete='RESTRICT' проверяются grep'ом ≥9 раз; revoke flow в Phase 13 — explicit purge через service layer. |
| T-11-03-03 | Tampering | UserRole values mismatch с DB enum (например 'admin' вместо 'owner') | mitigate | Enum values записаны через ascii literals 'owner','member','revoked'; миграция 0006 создаёт точно те же значения. |
| T-11-03-04 | Information Disclosure | Старая unique(period_start) не удалена в ORM, SQLAlchemy при autogenerate миграции попытается её снова создать | mitigate | Изменение 5 явно убирает unique=True с period_start колонки + добавляет new UniqueConstraint в __table_args__. |
| T-11-03-05 | Tampering | create_type=False забыли — SQLAlchemy DDL пытается заново создать enum, конфликт с миграцией | mitigate | UserRole enum mapping использует name='user_role', create_type=False (явно — миграция уже создала тип). |
</threat_model>

<verification>
1. `python -c "from app.db import models"` — exits 0 (импорт без ошибок).
2. `python -c "from app.db.models import UserRole; assert UserRole.owner.value == 'owner'"` — exits 0.
3. `python -c "from app.db.models import Category, AppUser; from sqlalchemy import inspect; ins=inspect(Category); cols={c.name for c in ins.columns}; assert 'user_id' in cols, cols"` — exits 0.
4. `grep -c "ondelete=.RESTRICT." app/db/models.py` — returns ≥9 (минимум 9 доменных таблиц).
5. `grep -E "class (Category|BudgetPeriod|PlanTemplateItem|Subscription|PlannedTransaction|ActualTransaction|AiConversation|AiMessage|CategoryEmbedding)\(Base\)" app/db/models.py | wc -l` — returns 9.
</verification>

<success_criteria>
- app/db/models.py парсится и импортируется
- UserRole enum существует с ровно тремя values
- AppUser.role атрибут существует, mapped к user_role pg enum (create_type=False)
- 9 доменных моделей (Category, BudgetPeriod, PlanTemplateItem, Subscription, PlannedTransaction, ActualTransaction, AiConversation, AiMessage, CategoryEmbedding) имеют user_id атрибут
- 3 scoped unique constraints с правильными именами
- BudgetPeriod.period_start больше не имеет inline unique=True
- AppHealth не модифицирован
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-tenancy-db-migration/11-03-SUMMARY.md` documenting:
- Список 12 изменений в models.py (1 enum, 1 role на AppUser, 9 user_id на доменных моделях, 3 scoped uniques, перемещение period_start unique)
- Подтверждение что AppHealth не тронут (system table)
- Pointer для Plan 11-04+: «теперь Service layer может использовать Model.user_id для фильтрации»
</output>
