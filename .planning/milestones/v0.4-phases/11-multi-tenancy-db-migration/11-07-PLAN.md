---
phase: 11-multi-tenancy-db-migration
plan: 07
type: execute
wave: 5
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05", "11-06"]
files_modified:
  - tests/conftest.py
  - tests/test_multitenancy_isolation.py
  - tests/test_rls_policy.py
  - tests/test_migration_backfill.py
  - .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
autonomous: false
requirements: [MUL-01, MUL-02, MUL-03, MUL-04, MUL-05, ROLE-01]
tags: [verification, integration-test, uat, multitenancy, rls, manual-check]
must_haves:
  truths:
    - "two_tenants fixture создаёт реальные user_a, user_b строки в app_user + по 2-3 категории/транзакции/подписки для каждого"
    - "Все 14 RED тестов из Plan 11-01 заполнены реальными assertions и passing"
    - "test_user_id_backfilled_to_owner проверяет что после миграции все 9 таблиц имеют user_id == OWNER.id (и не NULL)"
    - "test_rls_blocks_query_without_setting verified — без SET LOCAL query возвращает 0 rows"
    - "test_unique_category_name_scoped_per_user verified — оба tenant создают 'Продукты' без IntegrityError"
    - "Manual verification на dev DB: alembic upgrade/downgrade/upgrade cycle работает; psql -c '\\d category' показывает user_id NOT NULL FK; SELECT relrowsecurity FROM pg_class даёт true для всех 9 таблиц"
    - "Existing pytest suite passes (или explicitly помечен failing tests как нужно адаптировать в follow-up — никаких regression в семантике)"
    - "11-VERIFICATION.md создан с pass/fail-checkbox по каждому из 6 requirements (MUL-01..05, ROLE-01) + manual UAT items"
    - "Все 6 requirements (MUL-01..05, ROLE-01) — verified PASS"
  artifacts:
    - path: tests/conftest.py
      provides: "two_tenants fixture с реальной seed логикой (заменяет 11-01 placeholder)"
      contains: "AppUser(tg_user_id="
    - path: tests/test_multitenancy_isolation.py
      provides: "6 passing integration тестов изоляции"
    - path: tests/test_rls_policy.py
      provides: "4 passing RLS-policy тестов"
    - path: tests/test_migration_backfill.py
      provides: "4 passing backfill verification тестов"
    - path: .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
      provides: "UAT checklist signed off — все requirements pass"
      contains: "MUL-01"
  key_links:
    - from: tests/conftest.py (two_tenants)
      to: tests/test_multitenancy_isolation.py + test_rls_policy.py
      via: "Каждый test использует two_tenants → реальные user_a_id, user_b_id с seed-данными"
      pattern: "two_tenants"
    - from: tests/test_migration_backfill.py
      to: alembic/versions/0006_*.py
      via: "Тест применяется на DB с применённой миграцией; проверяет post-migration state"
      pattern: "select.*app_user.*tg_user_id"
---

<objective>
Финальный verification checkpoint Phase 11. Этапы:
1. Заменить RED placeholder в two_tenants fixture (Plan 11-01) на реальную seed логику.
2. Заполнить 14 RED тестов реальными assertions, чтобы они passing.
3. Запустить полный pytest suite — verify никаких regressions от refactor 11-04..06.
4. Manual checkpoint: на dev DB проверить migration upgrade/downgrade/upgrade cycle, psql инспекцию schema, RLS state.
5. Создать `.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md` со status PASS/FAIL по каждому requirement.

Этот план не autonomous — содержит человеческий checkpoint в Task 4 (manual UAT на dev DB). Остальные tasks автоматизированы.

Output: green pytest, signed-off VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md
@CLAUDE.md
@tests/conftest.py
@tests/test_multitenancy_isolation.py
@tests/test_rls_policy.py
@tests/test_migration_backfill.py
@app/db/models.py
@app/db/session.py
@app/api/dependencies.py
@app/services/categories.py
@app/services/subscriptions.py
@app/services/ai_conversation_service.py
@alembic/versions/0006_multitenancy_user_id_rls_role.py

<interfaces>
**Целевой two_tenants fixture (заменяет placeholder):**

```python
@pytest_asyncio.fixture
async def two_tenants(db_session) -> dict:
    """Создаёт двух юзеров с независимыми наборами данных.

    Returns dict:
        {
            'user_a': {
                'id': int,           # PK app_user.id
                'tg_user_id': int,
                'category_ids': [int, int],   # 2 seeded categories
                'sub_id': int,                # 1 seeded subscription
            },
            'user_b': {
                'id': int,
                'tg_user_id': int,
                'category_ids': [int, int],   # 2 категории с теми же именами что у user_a (тест unique scope)
                'sub_id': int,
            }
        }
    """
    from datetime import date
    from sqlalchemy import text
    from app.db.models import (
        AppUser, UserRole, Category, CategoryKind,
        Subscription, SubCycle,
    )

    # Используем уникальные tg_user_id-ы вне диапазона OWNER, не пересекающиеся
    # с другими тестами. Range 9_000_000_000+ — pet-проектная конвенция «test users».
    tg_a, tg_b = 9_000_000_001, 9_000_000_002

    # CLEANUP — на случай если предыдущий test упал и оставил данные.
    # Cascade delete через FK ON DELETE RESTRICT не сработает; используем
    # explicit purge в reverse order через raw SQL под BYPASSRLS-сценарием:
    # делаем SET LOCAL session_replication_role = 'replica' для bypass policies.
    # ПРОЩЕ: закомментировать RLS на время cleanup через SET row_security = off
    # (требует superuser; в dev OK).
    await db_session.execute(text("SET LOCAL row_security = off"))
    await db_session.execute(
        text("DELETE FROM app_user WHERE tg_user_id IN (:a, :b)"),
        {"a": tg_a, "b": tg_b},
    )
    await db_session.commit()

    # CREATE user_a + user_b
    user_a = AppUser(tg_user_id=tg_a, role=UserRole.member, cycle_start_day=5)
    user_b = AppUser(tg_user_id=tg_b, role=UserRole.member, cycle_start_day=5)
    db_session.add_all([user_a, user_b])
    await db_session.flush()

    # Seed категории — обе с одинаковым именем 'Продукты' для теста unique scope.
    cat_a1 = Category(user_id=user_a.id, name="Продукты", kind=CategoryKind.expense, sort_order=10)
    cat_a2 = Category(user_id=user_a.id, name="Транспорт", kind=CategoryKind.expense, sort_order=20)
    cat_b1 = Category(user_id=user_b.id, name="Продукты", kind=CategoryKind.expense, sort_order=10)
    cat_b2 = Category(user_id=user_b.id, name="Транспорт", kind=CategoryKind.expense, sort_order=20)
    db_session.add_all([cat_a1, cat_a2, cat_b1, cat_b2])
    await db_session.flush()

    # Seed по 1 подписке у каждого с одинаковым именем (тест scoped unique)
    sub_a = Subscription(
        user_id=user_a.id, name="Netflix",
        amount_cents=99900, cycle=SubCycle.monthly,
        next_charge_date=date(2026, 6, 1),
        category_id=cat_a1.id, notify_days_before=2, is_active=True,
    )
    sub_b = Subscription(
        user_id=user_b.id, name="Netflix",
        amount_cents=149900, cycle=SubCycle.monthly,
        next_charge_date=date(2026, 6, 1),
        category_id=cat_b1.id, notify_days_before=2, is_active=True,
    )
    db_session.add_all([sub_a, sub_b])
    await db_session.flush()
    await db_session.commit()

    yield {
        "user_a": {
            "id": user_a.id, "tg_user_id": tg_a,
            "category_ids": [cat_a1.id, cat_a2.id], "sub_id": sub_a.id,
        },
        "user_b": {
            "id": user_b.id, "tg_user_id": tg_b,
            "category_ids": [cat_b1.id, cat_b2.id], "sub_id": sub_b.id,
        },
    }

    # Teardown: hard delete (RLS off для admin-cleanup)
    await db_session.execute(text("SET LOCAL row_security = off"))
    await db_session.execute(
        text("DELETE FROM app_user WHERE tg_user_id IN (:a, :b)"),
        {"a": tg_a, "b": tg_b},
    )
    await db_session.commit()
```

⚠ Note: ON DELETE RESTRICT означает что DELETE FROM app_user fail'ит если есть зависимые строки. Поэтому в teardown сначала удалить domain rows, потом app_user. Альтернатива — в dev test ENV `TRUNCATE` с `CASCADE`. ИЛИ: использовать `SET session_replication_role = 'replica'` который отключает FK checks. Выбор подходящего подхода — в Task 1 ниже.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Заполнить two_tenants fixture реальной seed логикой</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (целиком — placeholder из Plan 11-01)
    - app/db/models.py (для импорта моделей)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (раздел specifics — 2-tenant fixture pattern)
  </read_first>
  <action>
В `tests/conftest.py` ЗАМЕНИТЬ существующую placeholder fixture two_tenants (с pytest.skip) на ниже-указанную реальную реализацию.

Найти:
```python
@pytest_asyncio.fixture
async def two_tenants(db_session) -> tuple[int, int]:
    """Two-tenant fixture — возвращает (user_a_id, user_b_id).
    ...
    """
    import pytest as _pytest
    _pytest.skip(
        "two_tenants fixture is a Plan 11-01 RED placeholder — "
        "real implementation lands in Plan 11-07"
    )
    return (1, 2)
```

Заменить на:
```python
@pytest_asyncio.fixture
async def two_tenants(db_session):
    """Создаёт двух юзеров (user_a, user_b) с собственными seed-данными.

    Returns dict:
        {
            'user_a': {'id': int, 'tg_user_id': int,
                       'category_ids': list[int], 'sub_id': int},
            'user_b': {'id': int, 'tg_user_id': int,
                       'category_ids': list[int], 'sub_id': int},
        }

    Использует tg_user_id-ы 9_000_000_001 / 9_000_000_002 — не пересекаются
    с OWNER_TG_ID test default (123456789) и production диапазонами.

    Cleanup: до и после теста удаляет тестовых юзеров с RLS bypass'ом через
    SET LOCAL row_security = off (требует database role с привилегиями;
    в test env обычно есть). Сначала domain data (FK RESTRICT), потом app_user.
    """
    from datetime import date
    from sqlalchemy import text, delete

    from app.db.models import (
        AppUser, UserRole, Category, CategoryKind,
        Subscription, SubCycle,
    )

    tg_a, tg_b = 9_000_000_001, 9_000_000_002

    async def _cleanup():
        # Bypass RLS для administrative cleanup.
        await db_session.execute(text("SET LOCAL row_security = off"))
        # Получить id-ы юзеров если есть.
        result = await db_session.execute(
            text(
                "SELECT id FROM app_user WHERE tg_user_id = ANY(:tgs)"
            ),
            {"tgs": [tg_a, tg_b]},
        )
        user_ids = [row[0] for row in result.all()]
        if user_ids:
            # Сначала domain rows (FK RESTRICT) — order соответствует FK depth.
            for tbl in (
                "ai_message",
                "ai_conversation",
                "category_embedding",
                "actual_transaction",
                "planned_transaction",
                "subscription",
                "plan_template_item",
                "budget_period",
                "category",
            ):
                await db_session.execute(
                    text(f"DELETE FROM {tbl} WHERE user_id = ANY(:uids)"),
                    {"uids": user_ids},
                )
            await db_session.execute(
                text("DELETE FROM app_user WHERE id = ANY(:uids)"),
                {"uids": user_ids},
            )
        await db_session.commit()

    # Pre-test cleanup
    await _cleanup()

    # Create users
    user_a = AppUser(tg_user_id=tg_a, role=UserRole.member, cycle_start_day=5)
    user_b = AppUser(tg_user_id=tg_b, role=UserRole.member, cycle_start_day=5)
    db_session.add_all([user_a, user_b])
    await db_session.flush()

    # Categories — обе с одинаковыми именами (тест scoped unique)
    cat_a1 = Category(user_id=user_a.id, name="Продукты", kind=CategoryKind.expense, sort_order=10)
    cat_a2 = Category(user_id=user_a.id, name="Транспорт", kind=CategoryKind.expense, sort_order=20)
    cat_b1 = Category(user_id=user_b.id, name="Продукты", kind=CategoryKind.expense, sort_order=10)
    cat_b2 = Category(user_id=user_b.id, name="Транспорт", kind=CategoryKind.expense, sort_order=20)
    db_session.add_all([cat_a1, cat_a2, cat_b1, cat_b2])
    await db_session.flush()

    # Subscriptions — обе с одинаковыми именами (test scoped unique)
    sub_a = Subscription(
        user_id=user_a.id, name="Netflix",
        amount_cents=99900, cycle=SubCycle.monthly,
        next_charge_date=date(2026, 6, 1),
        category_id=cat_a1.id, notify_days_before=2, is_active=True,
    )
    sub_b = Subscription(
        user_id=user_b.id, name="Netflix",
        amount_cents=149900, cycle=SubCycle.monthly,
        next_charge_date=date(2026, 6, 1),
        category_id=cat_b1.id, notify_days_before=2, is_active=True,
    )
    db_session.add_all([sub_a, sub_b])
    await db_session.flush()
    await db_session.commit()

    try:
        yield {
            "user_a": {
                "id": user_a.id, "tg_user_id": tg_a,
                "category_ids": [cat_a1.id, cat_a2.id],
                "sub_id": sub_a.id,
            },
            "user_b": {
                "id": user_b.id, "tg_user_id": tg_b,
                "category_ids": [cat_b1.id, cat_b2.id],
                "sub_id": sub_b.id,
            },
        }
    finally:
        await _cleanup()
```

Эта fixture требует database с `row_security = off` permissions у test role — обычно в DEV постгрес у `budget` юзера эти права есть (он создатель таблиц). Если нет — fixture fail'ит при cleanup; в этом случае альтернатива — настроить test DB с superuser креденшелами или сделать tests хорошо изолированными чтобы избежать cleanup.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('tests/conftest.py').read())" && grep -c 'AppUser(tg_user_id=tg_a' tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - File parses: `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` exits 0
    - Placeholder skip удалён: `grep -c "Plan 11-01 RED placeholder" tests/conftest.py` returns `0`
    - Real fixture present: `grep -c "AppUser(tg_user_id=tg_a" tests/conftest.py` returns `1`, `grep -c "AppUser(tg_user_id=tg_b" tests/conftest.py` returns `1`
    - Cleanup помощник присутствует: `grep -c "row_security = off" tests/conftest.py` >= 1
    - Optional integration: `pytest tests/test_multitenancy_isolation.py::test_unique_category_name_scoped_per_user -x --tb=short` — runs without fixture-level errors (test body может упасть на NotImplementedError pока не пройдёт Task 2/3)
  </acceptance_criteria>
  <done>two_tenants fixture создаёт реальные user_a/user_b с seed данными; cleanup ставится pre-test и post-test; placeholder из Plan 11-01 удалён</done>
</task>

<task type="auto">
  <name>Task 2: Заполнить tests/test_multitenancy_isolation.py + test_rls_policy.py реальными assertions</name>
  <files>tests/test_multitenancy_isolation.py, tests/test_rls_policy.py</files>
  <read_first>
    - tests/test_multitenancy_isolation.py (из Plan 11-01 — 6 RED тестов)
    - tests/test_rls_policy.py (из Plan 11-01 — 4 RED тестов)
    - tests/conftest.py (только что обновлённый — two_tenants fixture)
    - app/services/categories.py, subscriptions.py, planned.py, actual.py (только что refactored — для service signatures)
    - app/db/session.py (set_tenant_scope helper)
  </read_first>
  <action>
**В tests/test_multitenancy_isolation.py — заменить каждый `raise NotImplementedError(...)` на реальную assertion logic:**

```python
from sqlalchemy import select, text
from sqlalchemy.exc import IntegrityError

from app.db.models import Category, Subscription
from app.db.session import set_tenant_scope


pytestmark = pytest.mark.asyncio


async def test_user_a_does_not_see_user_b_categories(two_tenants, db_session):
    """MUL-03: list_categories для user_a не должен возвращать категории user_b."""
    user_a = two_tenants["user_a"]
    user_b = two_tenants["user_b"]

    # Set tenant scope для user_a
    await set_tenant_scope(db_session, user_a["id"])

    # Service-level: app-side filtering
    from app.services.categories import list_categories
    cats = await list_categories(db_session, user_id=user_a["id"])
    cat_ids = [c.id for c in cats]

    # Должны быть только user_a категории
    assert set(cat_ids) == set(user_a["category_ids"]), (
        f"user_a sees {cat_ids}, expected only {user_a['category_ids']}; "
        f"user_b's categories are {user_b['category_ids']}"
    )
    for cat_b_id in user_b["category_ids"]:
        assert cat_b_id not in cat_ids


async def test_user_a_cannot_get_user_b_category_by_id(two_tenants, db_session):
    """MUL-03: прямой get_or_404 user_a с category_id user_b → CategoryNotFoundError."""
    from app.services.categories import get_or_404, CategoryNotFoundError

    user_a = two_tenants["user_a"]
    user_b = two_tenants["user_b"]

    await set_tenant_scope(db_session, user_a["id"])

    foreign_cat_id = user_b["category_ids"][0]
    with pytest.raises(CategoryNotFoundError):
        await get_or_404(db_session, foreign_cat_id, user_id=user_a["id"])


async def test_user_a_cannot_get_user_b_subscription_by_id(two_tenants, db_session):
    """MUL-03: subscription с чужим user_id невидим."""
    user_a = two_tenants["user_a"]
    user_b = two_tenants["user_b"]

    await set_tenant_scope(db_session, user_a["id"])

    # Direct ORM query (test isolation at lowest layer)
    result = await db_session.execute(
        select(Subscription).where(Subscription.id == user_b["sub_id"])
    )
    sub = result.scalar_one_or_none()
    # RLS должен блокировать row → None. ИЛИ если app-side filter применяется
    # дополнительно (через service), то service вернёт None / raise.
    assert sub is None, (
        f"Expected None (RLS blocked) but got Subscription for user_b sub_id={user_b['sub_id']}"
    )


async def test_user_a_cannot_see_user_b_planned_transactions(two_tenants, db_session):
    """MUL-03: select(PlannedTransaction) под user_a scope не видит rows user_b.

    Note: two_tenants fixture не seed'ит planned транзакции. Этот тест
    проверяет на пустоте — что user_a при scope (даже если бы у user_b
    были planned, RLS блокирует). Если хочешь стать строже — расширь
    fixture с seed planned_transaction для user_b.
    """
    from app.db.models import PlannedTransaction

    user_a = two_tenants["user_a"]
    await set_tenant_scope(db_session, user_a["id"])

    result = await db_session.execute(select(PlannedTransaction))
    rows = result.scalars().all()
    # Все returned rows должны быть user_a (если есть)
    for row in rows:
        assert row.user_id == user_a["id"], (
            f"Cross-tenant leak: row.user_id={row.user_id}, expected {user_a['id']}"
        )


async def test_user_a_cannot_see_user_b_actual_transactions(two_tenants, db_session):
    """MUL-03: то же для actual_transaction."""
    from app.db.models import ActualTransaction

    user_a = two_tenants["user_a"]
    await set_tenant_scope(db_session, user_a["id"])

    result = await db_session.execute(select(ActualTransaction))
    rows = result.scalars().all()
    for row in rows:
        assert row.user_id == user_a["id"]


async def test_unique_category_name_scoped_per_user(two_tenants, db_session):
    """MUL-04: оба tenant имеют категорию 'Продукты' (already seeded в fixture).

    Если глобальный unique существовал бы — fixture упал бы на second INSERT.
    Тест проверяет что оба row существуют.
    """
    from app.db.models import Category

    # Bypass RLS чтобы видеть оба row из admin-perspective.
    await db_session.execute(text("SET LOCAL row_security = off"))

    result = await db_session.execute(
        select(Category).where(Category.name == "Продукты")
    )
    cats = result.scalars().all()
    user_ids_with_produkty = {c.user_id for c in cats}
    assert len(user_ids_with_produkty) >= 2, (
        f"Ожидалось 2+ юзера с 'Продукты' (per-user scoped unique). "
        f"Получено: {user_ids_with_produkty}"
    )
```

**В tests/test_rls_policy.py — заменить каждый NotImplementedError:**

```python
from sqlalchemy import select, text
from app.db.models import Category
from app.db.session import set_tenant_scope


pytestmark = pytest.mark.asyncio


async def test_rls_blocks_query_without_setting(two_tenants, db_session):
    """MUL-02: SELECT без app.current_user_id → 0 rows (coalesce -1 не матчит)."""
    # Гарантировать что setting не stale: открываем НОВУЮ transaction.
    await db_session.commit()  # close current trx (Plan 11-01 fixture committed seed)

    # Without SET LOCAL — RLS blocks all rows.
    result = await db_session.execute(
        text("SELECT count(*) FROM category")
    )
    count = result.scalar_one()
    assert count == 0, (
        f"Expected 0 rows without SET LOCAL (RLS coalesce → -1) but got {count}"
    )


async def test_rls_filters_by_app_current_user_id(two_tenants, db_session):
    """MUL-02: SET LOCAL → видим только данные конкретного юзера."""
    user_a = two_tenants["user_a"]
    user_b = two_tenants["user_b"]

    await set_tenant_scope(db_session, user_a["id"])

    result = await db_session.execute(
        text("SELECT user_id FROM category")
    )
    user_ids = {row[0] for row in result.all()}
    assert user_ids == {user_a["id"]}, (
        f"Expected only user_a categories ({user_a['id']}), got {user_ids}"
    )


async def test_rls_setting_resets_after_commit(two_tenants, db_session):
    """MUL-02: SET LOCAL — transaction scope, на COMMIT сбрасывается."""
    user_a = two_tenants["user_a"]
    await set_tenant_scope(db_session, user_a["id"])
    await db_session.commit()  # SET LOCAL должен сброситься

    # Новая transaction (implicit при следующем execute)
    result = await db_session.execute(
        text("SELECT count(*) FROM category")
    )
    count = result.scalar_one()
    assert count == 0, (
        "Expected 0 rows after COMMIT (setting reset) but got {count}"
    )


async def test_rls_enabled_on_all_nine_tables(db_session):
    """MUL-02: pg_class.relrowsecurity = true на всех 9 доменных таблицах."""
    domain_tables = (
        "category", "budget_period", "plan_template_item",
        "planned_transaction", "actual_transaction", "subscription",
        "category_embedding", "ai_conversation", "ai_message",
    )

    result = await db_session.execute(
        text(
            "SELECT relname, relrowsecurity, relforcerowsecurity "
            "FROM pg_class "
            "WHERE relname = ANY(:tables) AND relkind = 'r'"
        ),
        {"tables": list(domain_tables)},
    )
    rows = {row[0]: (row[1], row[2]) for row in result.all()}
    assert set(rows.keys()) == set(domain_tables), (
        f"Missing tables in pg_class: {set(domain_tables) - set(rows.keys())}"
    )
    for table, (relrowsecurity, relforcerowsecurity) in rows.items():
        assert relrowsecurity, f"{table}: ROW LEVEL SECURITY not enabled"
        assert relforcerowsecurity, f"{table}: FORCE ROW LEVEL SECURITY not set"
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('tests/test_multitenancy_isolation.py').read()); ast.parse(open('tests/test_rls_policy.py').read())" && grep -c 'NotImplementedError' tests/test_multitenancy_isolation.py tests/test_rls_policy.py</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse: `python -c "import ast; ast.parse(open('tests/test_multitenancy_isolation.py').read()); ast.parse(open('tests/test_rls_policy.py').read())"` exits 0
    - NotImplementedError placeholders REMOVED: `grep -c "NotImplementedError" tests/test_multitenancy_isolation.py tests/test_rls_policy.py` returns `0`
    - Real assertions exist: `grep -c "assert " tests/test_multitenancy_isolation.py tests/test_rls_policy.py` >= 14 (минимум 14 assertions через 10 тестов)
    - Imports valid: `python -c "from tests.test_multitenancy_isolation import *; from tests.test_rls_policy import *"` exits 0 OR equivalent (модуль импортируется без ошибок)
    - Tests use two_tenants fixture: `grep -c "two_tenants" tests/test_multitenancy_isolation.py tests/test_rls_policy.py` >= 9
    - Optional integration (если есть DB): `pytest tests/test_multitenancy_isolation.py tests/test_rls_policy.py -x --tb=short -q` — все тесты passing (или skip-with-message если DATABASE_URL отсутствует)
  </acceptance_criteria>
  <done>10 тестов в test_multitenancy_isolation.py + test_rls_policy.py заполнены реальными assertions; NotImplementedError убран; assertions используют two_tenants fixture и set_tenant_scope</done>
</task>

<task type="auto">
  <name>Task 3: Заполнить tests/test_migration_backfill.py реальными assertions</name>
  <files>tests/test_migration_backfill.py</files>
  <read_first>
    - tests/test_migration_backfill.py (из Plan 11-01 — 4 RED тестов)
    - alembic/versions/0006_multitenancy_user_id_rls_role.py (для reference имён constraints/enum)
    - app/db/models.py (UserRole)
  </read_first>
  <action>
В `tests/test_migration_backfill.py` ЗАМЕНИТЬ все NotImplementedError на реальные assertions:

```python
"""Migration backfill integration test — Phase 11 (MUL-05, ROLE-01).

Запускается на БД с применённой миграцией 0006. db_session fixture skip'нет
если DATABASE_URL не доступен.
"""
from __future__ import annotations

import os

import pytest
from sqlalchemy import text


pytestmark = pytest.mark.asyncio


DOMAIN_TABLES = (
    "category", "budget_period", "plan_template_item",
    "planned_transaction", "actual_transaction", "subscription",
    "category_embedding", "ai_conversation", "ai_message",
)


async def test_user_id_backfilled_to_owner(db_session):
    """MUL-05: после миграции все user_id колонки заполнены и равны OWNER.id."""
    owner_tg_id = int(os.environ.get("OWNER_TG_ID", "123456789"))

    # Bypass RLS for inspection
    await db_session.execute(text("SET LOCAL row_security = off"))

    # Резолвить OWNER PK
    owner_id = (await db_session.execute(
        text("SELECT id FROM app_user WHERE tg_user_id = :tg"),
        {"tg": owner_tg_id},
    )).scalar_one_or_none()
    if owner_id is None:
        pytest.skip(f"OWNER юзер с tg_user_id={owner_tg_id} не найден в БД — фикстура dev_seed не запустилась?")

    # Verify: для каждой таблицы count(*) WHERE user_id IS NULL == 0
    for tbl in DOMAIN_TABLES:
        null_count = (await db_session.execute(
            text(f"SELECT count(*) FROM {tbl} WHERE user_id IS NULL")
        )).scalar_one()
        assert null_count == 0, f"{tbl}: {null_count} rows have user_id IS NULL"

    # Verify: existing rows на момент миграции принадлежат OWNER
    # (новые tenant rows могут существовать после fixture two_tenants — фильтруем
    # по тому что мы сидил'и в dev_seed: OWNER's data)
    for tbl in DOMAIN_TABLES:
        owner_rows = (await db_session.execute(
            text(f"SELECT count(*) FROM {tbl} WHERE user_id = :uid"),
            {"uid": owner_id},
        )).scalar_one()
        # owner_rows может быть 0 если dev_seed не сидил эту таблицу;
        # ключевая проверка — null_count == 0 (выше).
        assert owner_rows >= 0


async def test_role_owner_assigned_to_owner_tg_id(db_session):
    """ROLE-01: app_user.role = 'owner' для OWNER_TG_ID."""
    owner_tg_id = int(os.environ.get("OWNER_TG_ID", "123456789"))
    role = (await db_session.execute(
        text("SELECT role FROM app_user WHERE tg_user_id = :tg"),
        {"tg": owner_tg_id},
    )).scalar_one_or_none()
    if role is None:
        pytest.skip(f"OWNER юзер не существует — dev_seed не сработал?")
    assert role == "owner", f"Expected role='owner', got {role!r}"


async def test_user_role_enum_type_exists(db_session):
    """ROLE-01: postgres enum user_role существует с тремя значениями."""
    rows = (await db_session.execute(
        text(
            "SELECT enumlabel FROM pg_enum e "
            "JOIN pg_type t ON e.enumtypid = t.oid "
            "WHERE t.typname = 'user_role' "
            "ORDER BY e.enumsortorder"
        )
    )).all()
    labels = [row[0] for row in rows]
    assert labels == ["owner", "member", "revoked"], (
        f"Expected user_role enum labels in order [owner, member, revoked], got {labels}"
    )


async def test_category_unique_scoped_per_user(db_session):
    """MUL-04: scoped unique constraint exists; old global unique НЕ существует."""
    # Имена scoped uniques (из Plan 11-02 миграции).
    expected_scoped = {
        "uq_category_user_id_name",
        "uq_subscription_user_id_name",
        "uq_budget_period_user_id_period_start",
    }
    rows = (await db_session.execute(
        text(
            "SELECT con.conname FROM pg_constraint con "
            "JOIN pg_class cl ON con.conrelid = cl.oid "
            "WHERE con.contype = 'u' AND cl.relname IN ('category', 'subscription', 'budget_period')"
        )
    )).all()
    constraint_names = {row[0] for row in rows}

    missing = expected_scoped - constraint_names
    assert not missing, f"Missing scoped uniques: {missing}; have {constraint_names}"

    # Старый глобальный uq_budget_period_start не должен существовать
    assert "uq_budget_period_start" not in constraint_names, (
        "Old global unique uq_budget_period_start should be dropped"
    )
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('tests/test_migration_backfill.py').read())" && grep -c 'NotImplementedError' tests/test_migration_backfill.py</automated>
  </verify>
  <acceptance_criteria>
    - File parses: `python -c "import ast; ast.parse(open('tests/test_migration_backfill.py').read())"` exits 0
    - NotImplementedError placeholders REMOVED: `grep -c "NotImplementedError" tests/test_migration_backfill.py` returns `0`
    - Real assertions: `grep -c "assert " tests/test_migration_backfill.py` >= 6
    - Tests use db_session fixture: `grep -c "db_session" tests/test_migration_backfill.py` >= 4
    - Optional integration (with DB): `pytest tests/test_migration_backfill.py -x --tb=short -q` — passing
  </acceptance_criteria>
  <done>4 backfill verification теста заполнены реальными assertions; pytest.skip только для случая отсутствия OWNER_TG_ID юзера в БД; все assertions проверяют post-migration state</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual UAT — alembic migration cycle + psql inspection + integration suite</name>
  <what-built>
Plan 11-02..06 применили миграцию + ORM + service/route/worker refactor; Plans 11-01 + 11-07 Tasks 1-3 создали integration tests. Теперь human-verifier проверяет работоспособность миграционного цикла и schema state на dev DB.
  </what-built>
  <how-to-verify>
ПЕРЕД manual проверкой убедись что:
- Все Plans 11-01..06 имеют SUMMARY.md (executor совершил коммиты)
- Локальный docker-compose stack запущен с dev override (`docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build`)

**Шаг 1: Migration cycle test (на dev DB)**

```bash
# В отдельном терминале — заходим в api-контейнер чтобы alembic был доступен
docker compose exec api bash

# Внутри контейнера:
export OWNER_TG_ID=<твой-реальный-tg-id>  # должен совпадать с переданным compose

# Apply migration (если ещё не применена)
alembic upgrade head

# Проверить что миграция в alembic_version
alembic current
# Ожидаем: 0006_multitenancy_user_id_rls_role (head)

# Test downgrade
alembic downgrade -1
alembic current
# Ожидаем: 0005_enable_ai_categorization

# Test re-apply
alembic upgrade head
alembic current
# Ожидаем: 0006_multitenancy_user_id_rls_role (head)
```

Ожидаем: все 3 команды exit 0, нет ошибок.

**Шаг 2: psql schema inspection**

```bash
# Из api-контейнера или из хост через docker compose exec db psql -U budget budget_db
docker compose exec db psql -U budget -d budget_db

# В psql:
\d app_user      # role колонка должна быть с типом user_role NOT NULL DEFAULT 'member'::user_role
\d category      # user_id BIGINT NOT NULL FK app_user(id) ON DELETE RESTRICT
\d subscription  # тоже user_id; uq_subscription_user_id_name unique constraint
\d budget_period # uq_budget_period_user_id_period_start; старого uq_budget_period_start нет

-- Verify enum
\dT user_role
-- Ожидаем 3 значения

-- Verify RLS state
SELECT relname, relrowsecurity, relforcerowsecurity
FROM pg_class
WHERE relname IN ('category','budget_period','plan_template_item','planned_transaction',
                  'actual_transaction','subscription','category_embedding','ai_conversation','ai_message');
-- Все 9 строк: оба boolean = t

-- Verify policies
SELECT tablename, policyname FROM pg_policies WHERE tablename IN ('category','budget_period','plan_template_item','planned_transaction','actual_transaction','subscription','category_embedding','ai_conversation','ai_message');
-- Каждая таблица должна иметь policy <table>_user_isolation

-- Manual RLS test
\c budget_db budget  -- non-superuser role (если используется)
SELECT count(*) FROM category;  -- ожидаем: 0 (нет SET LOCAL)
BEGIN;
SET LOCAL app.current_user_id = (SELECT id FROM app_user LIMIT 1);
SELECT count(*) FROM category;  -- ожидаем: > 0 если у юзера есть категории
COMMIT;
SELECT count(*) FROM category;  -- ожидаем: 0 (setting сбросилось)
```

Ожидаем: schema inspection corresponds к Plan 11-02 (user_role enum, role на app_user, user_id NOT NULL FK на 9 таблицах, scoped uniques на category/subscription/budget_period, RLS policies на 9 таблицах).

**Шаг 3: Запустить полный pytest integration suite**

```bash
docker compose exec api pytest -x --tb=short -q
```

Или, если есть `scripts/run-integration-tests.sh`:
```bash
./scripts/run-integration-tests.sh
```

Ожидаем:
- Новые тесты (test_multitenancy_isolation, test_rls_policy, test_migration_backfill) — passing (10 + 4 = 14 ассертов)
- Existing тесты — passing OR explicitly помечены как требующие фикстуру-обновления (комментарий в коде «Phase 11: needs user_id arg»). Регрессий в семантике быть не должно.

**Шаг 4: Дополнить .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md (Task 5 ниже автогенерирует)**

После успешных шагов 1-3 — переходим в Task 5 для финальной фиксации.
  </how-to-verify>
  <resume-signal>Type "approved" если все 3 шага зелёные, ИЛИ перечисли фейлы (например "alembic downgrade fails — drop_constraint не может найти uq_budget_period_start", "RLS test показал 5 rows вместо 0", и т.п.) — executor скорректирует отзовёт revision feedback.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Создать 11-VERIFICATION.md с pass/fail-checkbox по requirements</name>
  <files>.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 11 success criteria)
    - .planning/REQUIREMENTS.md (MUL-01..05, ROLE-01)
    - tests/test_multitenancy_isolation.py, test_rls_policy.py, test_migration_backfill.py (passing tests как evidence)
    - Все 11-NN-SUMMARY.md (что было сделано в каждом плане)
  </read_first>
  <action>
Создать новый файл `.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md` с UAT-чек-листом:

```markdown
# Phase 11 Verification — Multi-Tenancy DB Migration & RLS

**Phase:** 11 — Multi-Tenancy DB Migration & RLS
**Verified:** YYYY-MM-DD (заменить на дату)
**Verifier:** Claude executor + human (Task 4 manual checkpoint)
**Status:** PASS / FAIL (заменить на актуальный)

## Requirements Verification

### MUL-01: user_id BIGINT NOT NULL FK на 9 доменных таблицах
- [ ] PASS — alembic 0006 добавил колонку (Plan 11-02 Task 1-2)
- [ ] PASS — ORM models имеют Mapped[int] user_id (Plan 11-03)
- [ ] Evidence: `psql -c "\d category"` показывает user_id BIGINT NOT NULL FK
- [ ] Evidence: test_user_id_backfilled_to_owner — passing

### MUL-02: Postgres RLS policies на 9 таблицах
- [ ] PASS — миграция включает ENABLE/FORCE ROW LEVEL SECURITY + CREATE POLICY (Plan 11-02 Task 3)
- [ ] Evidence: test_rls_enabled_on_all_nine_tables — passing
- [ ] Evidence: test_rls_blocks_query_without_setting — passing
- [ ] Evidence: test_rls_filters_by_app_current_user_id — passing
- [ ] Evidence: test_rls_setting_resets_after_commit — passing

### MUL-03: App-side filtering — все queries scoped по user_id
- [ ] PASS — service layer refactor (Plan 11-05, 11-06)
- [ ] Evidence: test_user_a_does_not_see_user_b_categories — passing
- [ ] Evidence: test_user_a_cannot_get_user_b_category_by_id — passing
- [ ] Evidence: test_user_a_cannot_see_user_b_planned_transactions — passing
- [ ] Evidence: test_user_a_cannot_see_user_b_actual_transactions — passing

### MUL-04: Unique constraints scoped per user
- [ ] PASS — `uq_category_user_id_name`, `uq_subscription_user_id_name`, `uq_budget_period_user_id_period_start` (Plan 11-02)
- [ ] Evidence: test_unique_category_name_scoped_per_user — passing
- [ ] Evidence: test_category_unique_scoped_per_user (backfill suite) — passing

### MUL-05: Backfill миграция работает
- [ ] PASS — Plan 11-02 _resolve_owner_tg_id + UPDATE subqueries + sanity check
- [ ] Evidence: test_user_id_backfilled_to_owner — passing
- [ ] Manual: `alembic upgrade head` работает без ошибок (Task 4 step 1)
- [ ] Manual: `alembic downgrade -1; alembic upgrade head` reapply works

### ROLE-01: app_user.role enum + backfill для OWNER
- [ ] PASS — Plan 11-02 enum + Plan 11-03 ORM + Plan 11-04 dev_seed update
- [ ] Evidence: test_user_role_enum_type_exists — passing
- [ ] Evidence: test_role_owner_assigned_to_owner_tg_id — passing
- [ ] Manual: `psql -c "SELECT role FROM app_user WHERE tg_user_id=<OWNER>"` returns 'owner'

## Manual Checkpoints

- [ ] Шаг 1 (Task 4): alembic migration cycle (upgrade/downgrade/upgrade) — PASS / FAIL
- [ ] Шаг 2 (Task 4): psql schema inspection — PASS / FAIL
- [ ] Шаг 3 (Task 4): pytest integration suite — PASS / FAIL (количество failing: ___)

## Out of Scope (deferred to later phases)

- ROLE-02..05 (auth refactor) — Phase 12
- Admin UI — Phase 13
- Multi-tenant onboarding — Phase 14
- AI cost cap — Phase 15

## Notes / Issues found

(Заполнить при наличии — например регрессии в test_close_period_job, требующие follow-up)

## Sign-off

- Date: ____
- Status: ____ (PASS — Phase 11 complete, готовы к Phase 12)
```

Заполнить чек-боксами и реальными результатами Task 4 manual run + новых auto тестов.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && test -f .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md && grep -c 'MUL-01\|MUL-02\|MUL-03\|MUL-04\|MUL-05\|ROLE-01' .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md`
    - Все 6 requirement IDs присутствуют: `grep -c "MUL-01\|MUL-02\|MUL-03\|MUL-04\|MUL-05\|ROLE-01" .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md` >= 6
    - Manual checkpoints упомянуты: `grep -c "Шаг 1\|Шаг 2\|Шаг 3\|Manual" .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md` >= 4
    - Status line присутствует: `grep -c "Status:" .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md` >= 2
    - Out of scope раздел упомянут: `grep -c "Phase 12\|Phase 13\|Phase 14\|Phase 15" .planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md` >= 4
  </acceptance_criteria>
  <done>11-VERIFICATION.md содержит UAT checklist с 6 requirements (по 2-4 evidence-line на каждый); manual checkpoints отмечены; sign-off section присутствует</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pytest fixtures → DB | two_tenants использует tg_user_id-ы вне production диапазона; cleanup до и после теста; используется только в test ENV |
| Manual UAT (human) → production decision | Шаг 4 — gate перед merge; если RLS test fails, executor НЕ продолжает к Phase 12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-01 | Tampering | Test fixture seeds данные на production | mitigate | tg_user_id 9_000_000_001/9_000_000_002 — за пределами реальных Telegram ID range; conftest.py db_session fixture skip'ает если DATABASE_URL не указан или unreachable; тесты по умолчанию НЕ запускаются на production. |
| T-11-07-02 | Information Disclosure | Cleanup в two_tenants использует SET row_security = off — может leak в другие тесты | mitigate | SET LOCAL — transaction scope; reset на db_session.commit() в pre-test cleanup. После yield новый rollback в db_session fixture (already in conftest.py). |
| T-11-07-03 | Denial of Service | RED тесты висят в CI если DB недоступен | mitigate | db_session fixture pytest.skip если DATABASE_URL не set или DB unreachable. CI без DB пропускает integration тесты явно. |
| T-11-07-04 | Repudiation | Manual UAT confirmation без записи | mitigate | 11-VERIFICATION.md — formal sign-off с date + status; git commit с этим файлом — audit trail. |
| T-11-07-05 | Information Disclosure | DELETE в test cleanup промахивается mismo prevent rows и затрагивает OWNER data | mitigate | Cleanup только удаляет rows WHERE user_id = ANY([test_user_a, test_user_b]) — explicit filter. OWNER user (tg_user_id=123456789 в test ENV) не затронут. |
</threat_model>

<verification>
1. После Task 1: `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` — exits 0; `grep -c "Plan 11-01 RED placeholder" tests/conftest.py` returns 0.
2. После Task 2: `grep -c "NotImplementedError" tests/test_multitenancy_isolation.py tests/test_rls_policy.py` — returns 0.
3. После Task 3: `grep -c "NotImplementedError" tests/test_migration_backfill.py` — returns 0.
4. После Task 4 (manual): human подтвердил что migration cycle работает; psql инспекция показала ожидаемое; pytest integration suite passing.
5. После Task 5: 11-VERIFICATION.md существует с MUL-01..05, ROLE-01, manual checkpoint results, sign-off.
6. (Final): `pytest tests/test_multitenancy_isolation.py tests/test_rls_policy.py tests/test_migration_backfill.py -x` — все 14 тестов passing.
</verification>

<success_criteria>
- two_tenants fixture создаёт реальные user_a, user_b с seed (без placeholder skip)
- 14 тестов в 3 файлах passing (или skip-with-reason для DB-less env)
- pytest полный suite зелёный (или explicit list of phase-11-related test failures которые требуют follow-up)
- Manual UAT (Task 4) успешно завершён — alembic cycle, psql inspection, integration suite
- 11-VERIFICATION.md создан с pass/fail-checkbox по 6 requirements
- Phase 11 готова к hand-off для Phase 12
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-tenancy-db-migration/11-07-SUMMARY.md` documenting:
- Что было заполнено в conftest.py + 3 test файлах
- Результаты Task 4 manual UAT (вкратце)
- Pointer на 11-VERIFICATION.md как single source of truth
- Если есть failing tests которые нужно адаптировать в follow-up — список с пометкой «Phase 12 backlog»
</output>
