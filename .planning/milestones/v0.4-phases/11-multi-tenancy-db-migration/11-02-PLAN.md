---
phase: 11-multi-tenancy-db-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0006_multitenancy_user_id_rls_role.py
autonomous: true
requirements: [MUL-01, MUL-02, MUL-04, MUL-05, ROLE-01]
tags: [migration, alembic, multitenancy, rls, role, ddl]
must_haves:
  truths:
    - "Single Alembic revision файл существует с revision='0006_multitenancy_user_id_rls_role' и down_revision='0005_enable_ai_categorization'"
    - "Upgrade добавляет user_role enum + app_user.role + user_id BIGINT NOT NULL FK на 9 доменных таблиц"
    - "Backfill через UPDATE устанавливает user_id = (SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id) для всех 9 таблиц"
    - "Backfill читает OWNER_TG_ID из os.environ — fail loud (RuntimeError) если ENV не установлен"
    - "Sanity check после backfill — count(*) WHERE user_id IS NULL == 0 на каждой таблице, иначе RuntimeError"
    - "После backfill ALTER COLUMN user_id SET NOT NULL применён ко всем 9 таблицам"
    - "FK constraints user_id → app_user.id ON DELETE RESTRICT добавлены на 9 таблицах"
    - "Unique constraints scoped: category(user_id, name), subscription(user_id, name), budget_period(user_id, period_start) — старый uq_budget_period_start удалён"
    - "RLS включён на 9 доменных таблицах: ENABLE ROW LEVEL SECURITY + FORCE ROW LEVEL SECURITY"
    - "RLS policy на каждой из 9 таблиц: USING (user_id = coalesce(current_setting('app.current_user_id', true)::bigint, -1))"
    - "Индексы ix_<table>_user_id созданы на 9 таблицах для query performance"
    - "Downgrade функция симметрична upgrade — drops в обратном порядке"
  artifacts:
    - path: alembic/versions/0006_multitenancy_user_id_rls_role.py
      provides: "Single Alembic revision: enum + role + user_id + backfill + RLS + uniques + indexes"
      contains: "ENABLE ROW LEVEL SECURITY"
  key_links:
    - from: alembic/versions/0006_multitenancy_user_id_rls_role.py
      to: app_user.tg_user_id
      via: "UPDATE ... SET user_id = (SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id)"
      pattern: "WHERE tg_user_id"
    - from: alembic/versions/0006_multitenancy_user_id_rls_role.py
      to: PostgreSQL session GUC
      via: "current_setting('app.current_user_id', true) — runtime parameter set per request"
      pattern: "app.current_user_id"
---

<objective>
Single Alembic revision реализующая всю schema-уровень часть миграции multi-tenancy:
1. Enum `user_role` (`'owner', 'member', 'revoked'`)
2. `app_user.role user_role NOT NULL DEFAULT 'member'` + backfill `'owner'` для существующего OWNER_TG_ID-юзера
3. `user_id BIGINT FK → app_user.id ON DELETE RESTRICT` (изначально nullable) на 9 доменных таблицах
4. Backfill `user_id` через subquery `(SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id)` для всех существующих строк
5. `ALTER COLUMN user_id SET NOT NULL` на 9 таблицах
6. Unique constraints scoped по `(user_id, ...)`: `category(user_id, name)`, `subscription(user_id, name)`, `budget_period(user_id, period_start)` (заменяя старый `uq_budget_period_start`)
7. Индексы `(user_id)` на 9 таблицах
8. RLS: `ENABLE ROW LEVEL SECURITY` + `FORCE ROW LEVEL SECURITY` + `CREATE POLICY ... USING (user_id = coalesce(current_setting('app.current_user_id', true)::bigint, -1))`

Downgrade — симметричный teardown.

Output: alembic/versions/0006_multitenancy_user_id_rls_role.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md
@CLAUDE.md
@alembic/versions/0001_initial.py
@alembic/versions/0003_ai_tables.py
@alembic/versions/0004_pgvector_embeddings.py
@alembic/versions/0005_enable_ai_categorization.py

<interfaces>
9 ДОМЕННЫХ ТАБЛИЦ (multi-tenant scope):
1. category — id PK, name TEXT, kind enum, is_archived bool, sort_order int, created_at
2. budget_period — id PK, period_start DATE UNIQUE (заменим на (user_id, period_start)), period_end, starting_balance_cents, ending_balance_cents, status, closed_at
3. plan_template_item — id PK, category_id FK, amount_cents, description, day_of_period, sort_order
4. planned_transaction — id PK, period_id FK, kind, amount_cents, description, category_id FK, planned_date, source, subscription_id FK, original_charge_date
5. actual_transaction — id PK, period_id FK, kind, amount_cents, description, category_id FK, tx_date, source, created_at
6. subscription — id PK, name, amount_cents, cycle, next_charge_date, category_id FK, notify_days_before, is_active
7. category_embedding — category_id PK FK→category, embedding vector(1536), updated_at
8. ai_conversation — id PK, created_at
9. ai_message — id PK, conversation_id FK→ai_conversation, role, content, tool_name, tool_result, created_at

НЕ доменные (out of scope): app_user (root tenant, добавляем role), app_health (system shared).

Reference Alembic patterns:
- op.execute("RAW SQL") для DDL без ORM (CREATE EXTENSION, RLS)
- op.add_column / op.drop_column / op.alter_column
- op.create_foreign_key / op.drop_constraint
- op.create_index / op.drop_index
- sa.Enum(..., create_type=False) если enum уже создан через op.execute
- op.get_bind() для получения connection (для conn.execute(text(...)).scalar_one() в sanity check)

Current revision chain: 0001 → 0002 → 0003 → 0004 → 0005 → [НАШ 0006]

Existing constraint имена (из 0001_initial.py):
- uq_app_user_tg_user_id — оставляем
- uq_budget_period_start — DROP, заменяем на uq_budget_period_user_id_period_start
- uq_planned_sub_charge_date — оставляем
- ix_subscription_active_charge — оставляем
- ix_planned_period_kind — оставляем
- ix_actual_period_kind, ix_actual_category_date — оставляем
- ix_ai_message_conversation — оставляем (из 0003)
- ix_category_embedding_hnsw — оставляем (из 0004)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать revision file со skeleton + Phase 1 (enum + role) + Phase 2 (user_id nullable columns)</name>
  <files>alembic/versions/0006_multitenancy_user_id_rls_role.py</files>
  <read_first>
    - alembic/versions/0001_initial.py (целиком — все 9 таблиц с FK)
    - alembic/versions/0003_ai_tables.py (целиком — паттерн ai_conversation/ai_message)
    - alembic/versions/0004_pgvector_embeddings.py (целиком — паттерн raw SQL через op.execute)
    - alembic/versions/0005_enable_ai_categorization.py (целиком — паттерн add_column на app_user)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (DB Migration Strategy + Schema Decisions + RLS Strategy)
  </read_first>
  <action>
Создать НОВЫЙ файл `alembic/versions/0006_multitenancy_user_id_rls_role.py` с содержимым:

```python
"""multitenancy: user_id FK + RLS + app_user.role + backfill

Revision ID: 0006_multitenancy_user_id_rls_role
Revises: 0005_enable_ai_categorization
Create Date: 2026-05-07

Phase 11: Multi-Tenancy DB Migration & RLS (MUL-01..05, ROLE-01).

What this revision does (single atomic Alembic transaction):
  1. CREATE TYPE user_role AS ENUM ('owner', 'member', 'revoked')
  2. ALTER TABLE app_user ADD COLUMN role user_role NOT NULL DEFAULT 'member'
     + UPDATE app_user SET role = 'owner' WHERE tg_user_id = :owner_tg_id
  3. На 9 доменных таблицах: ADD COLUMN user_id BIGINT NULL (изначально)
  4. Backfill user_id = (SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id)
  5. Sanity check: RAISE если в любой таблице остались user_id IS NULL
  6. На 9 доменных таблицах: ALTER COLUMN user_id SET NOT NULL
  7. ADD FOREIGN KEY user_id → app_user(id) ON DELETE RESTRICT (9 таблиц)
  8. Drop старого UNIQUE budget_period(period_start), CREATE NEW scoped uniques
  9. CREATE INDEX ix_<table>_user_id на 9 таблицах
  10. ENABLE/FORCE ROW LEVEL SECURITY + CREATE POLICY на 9 таблицах

Operational note: миграция требует downtime (~30 сек) — `docker compose stop
api bot worker; alembic upgrade head; docker compose start`.
"""
from __future__ import annotations

import os
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op


revision: str = "0006_multitenancy_user_id_rls_role"
down_revision: Union[str, None] = "0005_enable_ai_categorization"
branch_labels: Union[str, Sequence[str], None] = None
depends_on: Union[str, Sequence[str], None] = None


# 9 доменных таблиц (scope multi-tenant). app_user, app_health — out of scope.
DOMAIN_TABLES: tuple[str, ...] = (
    "category",
    "budget_period",
    "plan_template_item",
    "planned_transaction",
    "actual_transaction",
    "subscription",
    "category_embedding",
    "ai_conversation",
    "ai_message",
)


def _resolve_owner_tg_id() -> int:
    """Read OWNER_TG_ID from env; fail loud if missing or zero."""
    raw = os.environ.get("OWNER_TG_ID", "").strip()
    if not raw:
        raise RuntimeError(
            "0006_multitenancy: OWNER_TG_ID env var must be set for backfill. "
            "Aborting migration to avoid leaving DB in inconsistent state."
        )
    try:
        owner_id = int(raw)
    except ValueError as exc:
        raise RuntimeError(
            f"0006_multitenancy: OWNER_TG_ID={raw!r} is not an integer."
        ) from exc
    if owner_id == 0:
        raise RuntimeError(
            "0006_multitenancy: OWNER_TG_ID=0 (placeholder) — set real id."
        )
    return owner_id


def upgrade() -> None:
    owner_tg_id = _resolve_owner_tg_id()

    # ─── Phase 1: enum user_role + app_user.role + backfill owner role ───
    op.execute("CREATE TYPE user_role AS ENUM ('owner', 'member', 'revoked')")
    op.add_column(
        "app_user",
        sa.Column(
            "role",
            sa.Enum(
                "owner", "member", "revoked",
                name="user_role",
                create_type=False,
            ),
            nullable=False,
            server_default=sa.text("'member'::user_role"),
        ),
    )
    op.execute(
        sa.text(
            "UPDATE app_user SET role = 'owner' WHERE tg_user_id = :owner_tg_id"
        ).bindparams(owner_tg_id=owner_tg_id)
    )

    # ─── Phase 2: ADD COLUMN user_id BIGINT NULL на 9 доменных таблицах ───
    for table in DOMAIN_TABLES:
        op.add_column(
            table,
            sa.Column("user_id", sa.BigInteger(), nullable=True),
        )

    # Phases 3-8 добавятся в Tasks 2-3 (backfill, NOT NULL, FK, uniques, indexes, RLS).


def downgrade() -> None:
    # Заполнится в Task 3 целиком (симметричный teardown).
    pass
```

Это именно тот текст что нужно записать. Проверить что revision ID точный, down_revision указывает на 0005, DOMAIN_TABLES — кортеж из 9 строк в указанном порядке.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('alembic/versions/0006_multitenancy_user_id_rls_role.py').read())" && grep -c 'CREATE TYPE user_role' alembic/versions/0006_multitenancy_user_id_rls_role.py</automated>
  </verify>
  <acceptance_criteria>
    - File `alembic/versions/0006_multitenancy_user_id_rls_role.py` exists
    - Parses as valid Python: `python -c "import ast; ast.parse(open('alembic/versions/0006_multitenancy_user_id_rls_role.py').read())"` exits 0
    - `grep -c "revision: str = \"0006_multitenancy_user_id_rls_role\"" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns `1`
    - `grep -c "down_revision: Union\[str, None\] = \"0005_enable_ai_categorization\"" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns `1`
    - `grep -c "DOMAIN_TABLES" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 2
    - DOMAIN_TABLES contains all 9 tables: `python -c "import ast,sys; tree=ast.parse(open('alembic/versions/0006_multitenancy_user_id_rls_role.py').read()); names=[n.value for assign in tree.body if isinstance(assign,ast.Assign) and getattr(assign.targets[0],'id',None)=='DOMAIN_TABLES' for n in assign.value.elts]; assert set(names) == {'category','budget_period','plan_template_item','planned_transaction','actual_transaction','subscription','category_embedding','ai_conversation','ai_message'}, names"` exits 0
    - `grep -c "_resolve_owner_tg_id" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 2
    - `grep -c "CREATE TYPE user_role AS ENUM" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns `1`
    - `grep -c "RuntimeError" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 3
  </acceptance_criteria>
  <done>Revision file создан со skeleton, импортами, DOMAIN_TABLES (9 таблиц), helper _resolve_owner_tg_id (fail-loud), и Phase 1+2 в upgrade()</done>
</task>

<task type="auto">
  <name>Task 2: Добавить Phases 3-7 в upgrade() — backfill, NOT NULL, FK, uniques, indexes</name>
  <files>alembic/versions/0006_multitenancy_user_id_rls_role.py</files>
  <read_first>
    - alembic/versions/0006_multitenancy_user_id_rls_role.py (только-что созданный — добавляем код)
    - alembic/versions/0001_initial.py (имена существующих constraints — uq_budget_period_start)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (Schema Decisions: ON DELETE RESTRICT, unique scoped)
  </read_first>
  <action>
В файле `alembic/versions/0006_multitenancy_user_id_rls_role.py` найти комментарий "# Phases 3-8 добавятся в Tasks 2-3" внутри функции upgrade() и ЗАМЕНИТЬ его на следующий блок (продолжение функции upgrade, не отдельная функция):

```python
    # ─── Phase 3: backfill user_id = id юзера с tg_user_id == OWNER_TG_ID ───
    for table in DOMAIN_TABLES:
        op.execute(
            sa.text(
                f"UPDATE {table} SET user_id = "
                f"(SELECT id FROM app_user WHERE tg_user_id = :owner_tg_id) "
                f"WHERE user_id IS NULL"
            ).bindparams(owner_tg_id=owner_tg_id)
        )

    # ─── Phase 3.5: sanity check — никаких NULL user_id остаться не должно ───
    conn = op.get_bind()
    for table in DOMAIN_TABLES:
        count = conn.execute(
            sa.text(f"SELECT count(*) FROM {table} WHERE user_id IS NULL")
        ).scalar_one()
        if count and count > 0:
            raise RuntimeError(
                f"0006_multitenancy: backfill failed for {table}: "
                f"{count} rows still have user_id IS NULL. Check that "
                f"app_user has a row with tg_user_id={owner_tg_id}."
            )

    # ─── Phase 4: ALTER COLUMN user_id SET NOT NULL на 9 таблицах ───
    for table in DOMAIN_TABLES:
        op.alter_column(table, "user_id", nullable=False)

    # ─── Phase 5: FK constraints user_id → app_user.id ON DELETE RESTRICT ───
    for table in DOMAIN_TABLES:
        op.create_foreign_key(
            f"fk_{table}_user_id_app_user",
            source_table=table,
            referent_table="app_user",
            local_cols=["user_id"],
            remote_cols=["id"],
            ondelete="RESTRICT",
        )

    # ─── Phase 6: unique constraints scoped по user_id ───
    # 6a) budget_period: drop старого глобального unique(period_start),
    #     create scoped(user_id, period_start)
    op.drop_constraint("uq_budget_period_start", "budget_period", type_="unique")
    op.create_unique_constraint(
        "uq_budget_period_user_id_period_start",
        "budget_period",
        ["user_id", "period_start"],
    )
    # 6b) category: новый unique(user_id, name)
    op.create_unique_constraint(
        "uq_category_user_id_name",
        "category",
        ["user_id", "name"],
    )
    # 6c) subscription: новый unique(user_id, name)
    op.create_unique_constraint(
        "uq_subscription_user_id_name",
        "subscription",
        ["user_id", "name"],
    )

    # ─── Phase 7: индексы (user_id) на 9 таблицах ───
    for table in DOMAIN_TABLES:
        op.create_index(
            f"ix_{table}_user_id",
            table,
            ["user_id"],
        )

    # Phase 8 (RLS) добавится в Task 3.
```

Не трогать `def downgrade()` — он остаётся pass.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('alembic/versions/0006_multitenancy_user_id_rls_role.py').read())" && grep -c 'create_foreign_key\|create_unique_constraint\|create_index\|alter_column' alembic/versions/0006_multitenancy_user_id_rls_role.py</automated>
  </verify>
  <acceptance_criteria>
    - File still parses as valid Python
    - `grep -c "create_foreign_key" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns at least `1`
    - `grep -c "ON DELETE RESTRICT\|ondelete=.RESTRICT" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns at least `1`
    - `grep -c "uq_budget_period_user_id_period_start" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "uq_category_user_id_name" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "uq_subscription_user_id_name" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "drop_constraint(.uq_budget_period_start." alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "alter_column" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "ix_.*_user_id" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "SELECT count(\*) FROM" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
  </acceptance_criteria>
  <done>Phases 3-7 добавлены в upgrade(): backfill через UPDATE на каждой таблице, sanity check, NOT NULL, FK ON DELETE RESTRICT, scoped uniques (3 штуки), индексы (9 штук)</done>
</task>

<task type="auto">
  <name>Task 3: Добавить Phase 8 (RLS) в upgrade() + полный downgrade()</name>
  <files>alembic/versions/0006_multitenancy_user_id_rls_role.py</files>
  <read_first>
    - alembic/versions/0006_multitenancy_user_id_rls_role.py (текущее состояние после Task 2)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (раздел RLS Strategy — coalesce trick, FORCE ROW LEVEL SECURITY)
  </read_first>
  <action>
В файле `alembic/versions/0006_multitenancy_user_id_rls_role.py` найти комментарий "# Phase 8 (RLS) добавится в Task 3." внутри функции upgrade() и ЗАМЕНИТЬ его на:

```python
    # ─── Phase 8: RLS — ENABLE + FORCE + POLICY на каждой из 9 таблиц ───
    # Policy: разрешает row если user_id == app.current_user_id (GUC).
    # Без выставленного GUC current_setting('app.current_user_id', true) → NULL,
    # coalesce → -1, не матчит ни одну строку — query возвращает 0 rows
    # (defense-in-depth: app-side filter + RLS backstop).
    # FORCE ROW LEVEL SECURITY — table owner тоже подчиняется policy
    # (в production app-user не su; миграции через alembic_user могут получить
    # BYPASSRLS отдельно).
    for table in DOMAIN_TABLES:
        op.execute(f"ALTER TABLE {table} ENABLE ROW LEVEL SECURITY")
        op.execute(f"ALTER TABLE {table} FORCE ROW LEVEL SECURITY")
        op.execute(
            f"CREATE POLICY {table}_user_isolation ON {table} "
            f"USING (user_id = coalesce(current_setting('app.current_user_id', true)::bigint, -1)) "
            f"WITH CHECK (user_id = coalesce(current_setting('app.current_user_id', true)::bigint, -1))"
        )
```

Затем ПОЛНОСТЬЮ ЗАМЕНИТЬ функцию `def downgrade() -> None:` (которая сейчас просто `pass`) на симметричный teardown:

```python
def downgrade() -> None:
    # Симметрия upgrade — откат в обратном порядке.

    # ─── Drop RLS policies + disable RLS ───
    for table in DOMAIN_TABLES:
        op.execute(f"DROP POLICY IF EXISTS {table}_user_isolation ON {table}")
        op.execute(f"ALTER TABLE {table} NO FORCE ROW LEVEL SECURITY")
        op.execute(f"ALTER TABLE {table} DISABLE ROW LEVEL SECURITY")

    # ─── Drop indexes ix_<table>_user_id ───
    for table in DOMAIN_TABLES:
        op.drop_index(f"ix_{table}_user_id", table_name=table)

    # ─── Drop scoped unique constraints ───
    op.drop_constraint(
        "uq_subscription_user_id_name", "subscription", type_="unique"
    )
    op.drop_constraint(
        "uq_category_user_id_name", "category", type_="unique"
    )
    op.drop_constraint(
        "uq_budget_period_user_id_period_start",
        "budget_period",
        type_="unique",
    )
    # Restore старого глобального unique(period_start)
    op.create_unique_constraint(
        "uq_budget_period_start",
        "budget_period",
        ["period_start"],
    )

    # ─── Drop FK constraints ───
    for table in DOMAIN_TABLES:
        op.drop_constraint(
            f"fk_{table}_user_id_app_user", table, type_="foreignkey"
        )

    # ─── Drop user_id columns ───
    for table in DOMAIN_TABLES:
        op.drop_column(table, "user_id")

    # ─── Drop app_user.role + user_role enum ───
    op.drop_column("app_user", "role")
    op.execute("DROP TYPE user_role")
```

После замены — НЕ менять revision/down_revision/DOMAIN_TABLES/_resolve_owner_tg_id.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('alembic/versions/0006_multitenancy_user_id_rls_role.py').read())" && grep -c 'ENABLE ROW LEVEL SECURITY\|FORCE ROW LEVEL SECURITY\|CREATE POLICY\|current_setting.*app.current_user_id\|DROP POLICY\|DROP TYPE user_role' alembic/versions/0006_multitenancy_user_id_rls_role.py</automated>
  </verify>
  <acceptance_criteria>
    - File parses as valid Python
    - `grep -c "ENABLE ROW LEVEL SECURITY" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "FORCE ROW LEVEL SECURITY" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "CREATE POLICY" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "current_setting..app.current_user_id" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 2 (USING + WITH CHECK or USING + downgrade reference)
    - `grep -c "coalesce" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "WITH CHECK" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "DROP POLICY IF EXISTS" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "DROP TYPE user_role" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1
    - `grep -c "def downgrade" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns `1`
    - downgrade body не пустой: `grep -A 50 "def downgrade" alembic/versions/0006_multitenancy_user_id_rls_role.py | grep -c "drop_column"` >= 1
    - Optional check (manual integration on dev DB): `OWNER_TG_ID=<real-id> alembic upgrade head` succeeds; `alembic downgrade -1` succeeds; `alembic upgrade head` succeeds (reapply работает) — этот шаг manual в Plan 11-07 verification.
  </acceptance_criteria>
  <done>Phase 8 (RLS) добавлен в upgrade(); полный симметричный downgrade() реализован; revision file complete и парсится</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runtime → DB schema | alembic upgrade выполняется с DBA-уровнем доступом; migration code ДОЛЖЕН fail-loud при нарушении инварианта (отсутствие OWNER юзера) — иначе оставит БД в inconsistent state |
| os.environ → migration | OWNER_TG_ID читается из ENV; если ENV не установлен — миграция должна остановиться ДО первого DDL, а не оставить partial schema |
| User SQL injection через GUC | current_setting('app.current_user_id', true) — value ставится из app code; угроза — атакующий контролирует значение GUC |
| RLS bypass через BYPASSRLS role | App-user НЕ должен иметь BYPASSRLS; миграции выполняются отдельной ролью (alembic_user) с CREATEDB но не BYPASSRLS на app-tables (или с BYPASSRLS — это OK для миграции, важно что api/bot/worker роли не имеют его) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | Backfill оставляет NULL user_id, потом SET NOT NULL делает миграцию failed на середине | mitigate | Phase 3.5 sanity check — `SELECT count(*) WHERE user_id IS NULL` для каждой таблицы; RuntimeError если > 0. Останавливает миграцию ДО ALTER NOT NULL. |
| T-11-02-02 | Tampering | Wrong tg_user_id resolution в backfill (SELECT subquery возвращает NULL) | mitigate | _resolve_owner_tg_id fail-loud: пустой ENV → RuntimeError; "0" placeholder → RuntimeError; non-int → RuntimeError. ENV requirement задокументирован в operational note. |
| T-11-02-03 | Tampering | Race condition: два alembic upgrade процесса параллельно | mitigate | Alembic держит advisory lock на alembic_version table. CONTEXT.md уточняет: pg_try_advisory_lock не нужен в самой миграции (alembic это покрывает). |
| T-11-02-04 | Tampering | Partial migration на error (10 of 14 phases применилось) | mitigate | Alembic выполняет revision в одной transaction по умолчанию (Postgres DDL transactional). Любой raise в любом phase → ROLLBACK всей revision. |
| T-11-02-05 | Information Disclosure | Cross-tenant data leak через missing WHERE clause при future query | mitigate | RLS на 9 таблицах с FORCE ROW LEVEL SECURITY — даже table owner подчиняется policy. Defense-in-depth когда app code забывает WHERE. |
| T-11-02-06 | Tampering | SQL injection через app.current_user_id GUC | mitigate | `::bigint` cast в RLS policy — non-numeric value вызывает type cast error при evaluation. App code ставит SET LOCAL через bind parameter (не string concat). |
| T-11-02-07 | Elevation of Privilege | App-user с BYPASSRLS видит чужие данные | accept | Production deploy ручной — DBA assigns роль api/bot/worker без BYPASSRLS. Миграция не управляет ролями (out of scope, ручной step в deploy doc). Phase 12 verification может проверить через `\du`. |
| T-11-02-08 | Repudiation | Downgrade не восстанавливает данные | accept | Downgrade — DDL only (drop columns/policies). Данные user_id теряются необратимо. Это явная семантика downgrade, документирована. Production downgrade не предполагается; если нужен — restore из бэкапа. |
| T-11-02-09 | DoS | Migration зависает на большой таблице (lock acquisition) | accept | Pet-проект с 1 юзером и десятками-сотнями строк; миграция миллисекунды. CLAUDE.md scope: «Single-tenant в MVP». Worker-job advisory lock не нужен. |
</threat_model>

<verification>
1. `python -c "import ast; ast.parse(open('alembic/versions/0006_multitenancy_user_id_rls_role.py').read())"` — exits 0.
2. `grep -E "DOMAIN_TABLES|create_type=False" alembic/versions/0006_multitenancy_user_id_rls_role.py | head -5` — DOMAIN_TABLES tuple defined and used.
3. `grep -c "for table in DOMAIN_TABLES" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 6 (loops для phases 2,3,3.5,4,5,7,8 в upgrade и 1+ в downgrade).
4. `grep -c "WITH CHECK" alembic/versions/0006_multitenancy_user_id_rls_role.py` >= 1 — POLICY имеет и USING и WITH CHECK clauses.
5. `grep -c "DROP TYPE user_role" alembic/versions/0006_multitenancy_user_id_rls_role.py` returns 1 — downgrade удаляет enum.
6. Manual (на dev DB, выполнится в Plan 11-07): `OWNER_TG_ID=<real> alembic upgrade head` → no error; `psql -c "\d app_user"` показывает role колонку; `psql -c "\d category"` показывает user_id NOT NULL FK; `psql -c "SELECT relname, relrowsecurity, relforcerowsecurity FROM pg_class WHERE relname = 'category'"` — оба true.
</verification>

<success_criteria>
- alembic/versions/0006_multitenancy_user_id_rls_role.py существует, парсится как Python
- Revision ID: 0006_multitenancy_user_id_rls_role; down_revision: 0005_enable_ai_categorization
- DOMAIN_TABLES — tuple из 9 правильных имён таблиц
- _resolve_owner_tg_id fail-loud на пустой/zero/non-int ENV
- upgrade() имеет 8 logical phases: enum, role+backfill, user_id columns, backfill+sanity, NOT NULL, FK ON DELETE RESTRICT, uniques scoped + index, RLS ENABLE+FORCE+POLICY с WITH CHECK
- downgrade() симметричен — drops в обратном порядке
- Никаких других файлов не изменено в этом плане
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-tenancy-db-migration/11-02-SUMMARY.md` documenting:
- Имя revision file и положение в chain (после 0005)
- Полный список phases в upgrade (1-8) с одной строкой описания каждой
- Список новых constraint и index имён (для reference из 11-07 verify тестов)
- Запись об operational note: OWNER_TG_ID должен быть в ENV; downtime ~30 сек
</output>
