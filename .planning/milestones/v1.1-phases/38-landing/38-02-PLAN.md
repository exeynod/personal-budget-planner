# Plan 38-02: Analytics Event Log + Endpoint (REQ-38-02)

**Status:** Complete (commit `621cd76`)
**Owner:** autonomous
**Scope:** Backend event log + service + endpoint + frontend helper.

## Deliverables

- `alembic/versions/0024_analytics_event.py` — `analytics_event(id,
  user_id, event_name, event_props JSONB, created_at)` + 3 indexes; no
  RLS; GRANT SELECT/INSERT для `budget_app` role.
- `app/services/analytics.py` extended:
  - `track_event(db, event_name, user_id=None, props=None)` —
    fire-and-forget с swallow-all error semantics.
  - 12 `EVENT_*` константы (landing.hit, tg.oauth.start,
    onboarding.started/completed, txn.first_created, ai.chat.used,
    paywall.shown, payment.initiated/succeeded, trial.started/expired,
    subscription.canceled).
- `app/api/routes/analytics.py` — новый `event_router` (без
  `require_onboarded`) + `POST /api/v1/analytics/event`.
- `app/api/router.py` — регистрация `event_router` в `public_router`.
- `frontend/src/api/analytics.ts` — `trackEvent(eventName, props)` +
  `EVENT` enum constant.
- `tests/test_analytics_event.py` — 2 теста (insert happy-path +
  silent-error swallow).

## Acceptance

- `alembic upgrade head` + `downgrade -1` + `upgrade head` round-trip
  clean (verified).
- `pytest tests/test_analytics_event.py -v` — 2 passed.
- Существующий `tests/test_analytics.py` (Phase 8 aggregates) — без
  регрессий (10 passed, 4 skipped — DEV_MODE auth skip).

## Out of scope

- UTM-attribution capture в `app_user.acquisition_source` — deferred к
  Phase 39 (Habr launch).
- Welcome survey table + endpoint — deferred (REQ-38-04 → v1.2 UX).
- PostHog / Plausible self-host — pet-scale ad-hoc query'и поверх
  собственной таблицы достаточны до Month-3 gate.
- Funnel dashboard — query templates → ad-hoc для Month-3 review.
