---
phase: 34-yookassa
plan: 06
type: execute
wave: 4
depends_on: [34-05]
requirements:
  - REQ-34-04
commit: 5fbdd7c
---

<objective>
Frontend slice: typed API helpers (`frontend/src/api/billing.ts`) + `<PaymentButton />` component (`frontend/src/components/PaymentButton.tsx`) — минимальный UI-trigger для запуска ЮKassa-checkout.
</objective>

## Task

1. `frontend/src/api/billing.ts` — три async helpers: `createPayment`, `getMySubscription`, `cancelMySubscription`; типы `PaymentCreateRequest/Response`, `SubscriptionRead`. Использует существующий `apiFetch` из `./client` (auto-prefixes `/api/v1`).
2. `frontend/src/components/PaymentButton.tsx` — single button с loading/error state, on-click → `createPayment(...)` → `window.location.assign(confirmation_url)`. `data-testid="pay-via-yookassa"` для Playwright.

## Acceptance

- `npx tsc --noEmit` — без новых ошибок в `billing.ts` / `PaymentButton.tsx`.
- Files committed.

## Commit

`5fbdd7c` — feat(34-06): minimal frontend billing API + PaymentButton component (REQ-34-04 frontend slice)
