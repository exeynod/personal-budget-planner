---
phase: 34-yookassa
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - REQ-34-01
commit: c9b4fbf
---

<objective>
Schema foundation: alembic migration для `payment` + `subscription_billing` таблиц + RLS policies. Без этого никакой webhook / API endpoint не может писать данные.
</objective>

## Task

1 task: создать alembic migration `0021_yookassa_payment_schema.py`:
- `CREATE TABLE payment` (id, user_id FK app_user, yookassa_payment_id UNIQUE, amount_cents, status enum, created_at, updated_at).
- `CREATE TABLE subscription_billing` (id, user_id FK, tier enum free/pro, period_start, period_end, status enum, last_payment_id FK).
- RLS policies на обе таблицы (current_user_id GUC filter).
- alembic upgrade head → downgrade -1 → upgrade head round-trip clean.

## Acceptance

- Migration applies cleanly.
- RLS policies активны (verified via `pg_policies`).

## Commit

`c9b4fbf` — feat(34-01): payment + subscription_billing schema + RLS (REQ-34-01)
