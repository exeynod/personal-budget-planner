---
phase: 36-persona-e
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - REQ-36-01
commit: 10aa998
---

<objective>
Business/personal tag schema на category + actual_transaction для tax-deductible
classification (Persona E, самозанятые НПД). Schema + index, без UI surface.
</objective>

## Task

1. Alembic migration `0023_business_personal_tag.py` — adds:
   - `category.tag VARCHAR(16) NOT NULL DEFAULT 'personal'` + CHECK
     constraint `tag IN ('personal','business','mixed')`.
   - `actual_transaction.tag VARCHAR(16) NULL` + same CHECK (NULL = inherit).
   - Partial index `ix_actual_transaction_tag` WHERE `tag='business'` для
     tax-deductible queries.
2. SQLAlchemy ORM mapping — `Category.tag` (Mapped[str], default `"personal"`,
   server_default `"personal"`); `ActualTransaction.tag` (Mapped[Optional[str]],
   nullable).
3. Tests `tests/test_business_personal_tag.py` — 2 sceanarios: explicit insert
   round-trip (personal + business), DB DEFAULT fallback when column omitted.

## Acceptance

- `pytest tests/test_business_personal_tag.py -v` → 2 passed.
- Alembic round-trip clean.

## Commit

`10aa998` — feat(36-01): business/personal tag on category + actual_transaction (REQ-36-01)
