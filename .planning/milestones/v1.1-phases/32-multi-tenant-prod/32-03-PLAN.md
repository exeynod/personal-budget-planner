---
phase: 32-multi-tenant-prod
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - alembic/versions/0019_owner_role_backfill.py
  - app/api/dependencies.py
  - tests/test_owner_role_backfill.py
  - tests/test_no_owner_tg_id_in_prod.py
autonomous: true
requirements:
  - REQ-32-02
  - REQ-32-06
must_haves:
  truths:
    - "REQ-32-06: alembic migration 0019 idempotent — UPDATE app_user SET role='owner' WHERE tg_user_id=$OWNER_TG_ID AND role <> 'owner'."
    - "REQ-32-02: production path (DEV_MODE=false) НЕ использует OWNER_TG_ID для auth — initData с unknown tg_user_id (incl. tg_user_id == OWNER_TG_ID без app_user row) → 403."
    - "tests/test_owner_role_backfill.py + tests/test_no_owner_tg_id_in_prod.py — все зелёные."
  artifacts:
    - path: ".planning/phases/32-multi-tenant-prod/32-03-SUMMARY.md"
      contains: "REQ-32-02"
      min_lines: 12
---

<objective>
(a) Idempotent backfill migration для owner role (REQ-32-06) — safety-net на случай legacy/manual drift. (b) Audit + pytest для production path: `OWNER_TG_ID` не является magic-bypass; production auth полностью role-based (REQ-32-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-multi-tenant-prod/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration 0019 — owner role backfill (idempotent)</name>
  <files>alembic/versions/0019_owner_role_backfill.py</files>
  <read_first>
    - alembic/versions/0006_multitenancy.py (original v0.4 backfill — `UPDATE app_user SET role='owner' WHERE tg_user_id=$OWNER_TG_ID`)
    - alembic/versions/0018_ai_cost_cap_default_500.py (immediate predecessor — assumed from Plan 32-02)
    - app/core/settings.py (settings.OWNER_TG_ID semantics)
  </read_first>
  <action>
    Создать `alembic/versions/0019_owner_role_backfill.py`:

    ```python
    """Phase 32 REQ-32-06: idempotent owner-role backfill (safety-net).

    Revision ID: 0019_owner_backfill
    Revises: 0018_cap_500
    Create Date: 2026-05-11

    Rationale:
      - v0.4 alembic 0006 уже backfill-ил OWNER_TG_ID → role='owner' во время
        initial multi-tenant rollout. Эта миграция — safety-net на случай,
        когда роль была изменена admin-tool'ом или ручным SQL.
      - Idempotent: no-op если уже owner. WHERE-clause проверяет tg_user_id
        AND role <> 'owner'.
      - OWNER_TG_ID берётся из ENV (`OWNER_TG_ID`). Если ENV не установлен
        или =0 — миграция логирует warning и skip-s (для dev/test без owner-config).
      - Downgrade: NO-OP. Унять role-owner без бизнес-логики = logical-pollution
        (нельзя восстановить «правильную» previous role без extra state).

    Edge cases:
      - Multiple users with tg_user_id=OWNER_TG_ID: impossible — UNIQUE constraint.
      - User does not exist в DB: WHERE matches 0 rows — silent no-op.
      - User уже owner: WHERE role <> 'owner' filters out — no UPDATE.
    """
    from __future__ import annotations

    import os

    from alembic import op


    # revision identifiers, used by Alembic.
    revision = "0019_owner_backfill"
    down_revision = "0018_cap_500"
    branch_labels = None
    depends_on = None


    def upgrade() -> None:
        owner_tg_id_raw = os.environ.get("OWNER_TG_ID", "0")
        try:
            owner_tg_id = int(owner_tg_id_raw)
        except (TypeError, ValueError):
            owner_tg_id = 0

        if owner_tg_id <= 0:
            # Dev / test path: no owner configured. Migration is no-op.
            # Production path: OWNER_TG_ID env должен быть set per HLD §7.
            op.execute(
                "DO $$ BEGIN RAISE NOTICE "
                "'0019_owner_backfill: OWNER_TG_ID not set, skipping owner-role backfill'; "
                "END $$"
            )
            return

        # Idempotent backfill: UPDATE only rows that aren't already owner.
        op.execute(
            "UPDATE app_user SET role = 'owner'::user_role "
            f"WHERE tg_user_id = {owner_tg_id} AND role <> 'owner'"
        )


    def downgrade() -> None:
        # No-op: cannot reliably restore previous role without extra state.
        # Downgrade preserves data; only forward-compatible by design.
        pass
    ```

    Запустить:
    - `alembic upgrade head` — должно подняться до 0019_owner_backfill.
    - Verify: `psql -c "SELECT role FROM app_user WHERE tg_user_id=$OWNER_TG_ID"` → `owner` (если row есть).
    - `alembic downgrade -1` — head=0018; data preserved.
    - `alembic upgrade head` — back to 0019.
  </action>
  <acceptance_criteria>
    - File `alembic/versions/0019_owner_role_backfill.py` exists.
    - `grep "revision = \"0019_owner_backfill\"" alembic/versions/0019_owner_role_backfill.py` matches.
    - `grep "down_revision = \"0018_cap_500\"" alembic/versions/0019_owner_role_backfill.py` matches.
    - `grep 'role <> ' alembic/versions/0019_owner_role_backfill.py` matches (idempotency guard).
    - `alembic upgrade head` exits 0; `alembic downgrade -1 && alembic upgrade head` round-trip clean.
  </acceptance_criteria>
  <done>Migration 0019 created and applied.</done>
</task>

<task type="auto">
  <name>Task 2: Sharpen docstring in app/api/dependencies.py (REQ-32-02 audit)</name>
  <files>app/api/dependencies.py</files>
  <read_first>
    - app/api/dependencies.py (lines 167-302 — get_current_user)
  </read_first>
  <action>
    В `app/api/dependencies.py` найти docstring `get_current_user` (~line 173). Найти sub-section "Production path: HMAC + role-based whitelist" (~line 258) — добавить EXPLICIT note:

    Текущий комментарий (~line 228-241):
    ```python
    # ---------- DEV_MODE: bypass HMAC, upsert OWNER row ----------
    # NOTE: OWNER_TG_ID is referenced from _dev_mode_resolve_owner (dev-only helper).
    # The production path below does NOT use OWNER_TG_ID — auth is role-based.
    ```

    Заменить / расширить (вставить ниже):
    ```python
    # ---------- DEV_MODE: bypass HMAC, upsert OWNER row ----------
    # NOTE: OWNER_TG_ID is referenced from _dev_mode_resolve_owner (dev-only helper).
    # The production path below does NOT use OWNER_TG_ID — auth is role-based.
    #
    # ⇩ Phase 32 REQ-32-02 audit:
    #   - Production path (DEV_MODE=false) reads ONLY:
    #       initData → tg_user_id → SELECT FROM app_user → role check.
    #   - `OWNER_TG_ID` env var is ONLY a seed for `_dev_mode_resolve_owner`
    #     (DEV_MODE branch) and `dev_seed.py` / pytest fixtures. NO production
    #     branch (HMAC-validated init data) compares tg_user_id с OWNER_TG_ID
    #     directly — auth полностью основан на app_user.role (`owner` /
    #     `member` / `revoked`).
    #   - If a row with tg_user_id == OWNER_TG_ID does NOT exist в app_user,
    #     production path returns 403 — НЕТ implicit owner-bypass. See
    #     tests/test_no_owner_tg_id_in_prod.py for explicit regression cover.
    ```

    Никаких behaviour changes — только документация. Function body должен остаться bit-identical.
  </action>
  <acceptance_criteria>
    - `grep 'Phase 32 REQ-32-02 audit' app/api/dependencies.py` matches.
    - `grep 'NO production branch' app/api/dependencies.py` matches.
    - Behaviour test (existing): `pytest tests/test_auth.py tests/test_role_based_auth.py -v` still green (zero change to logic).
  </acceptance_criteria>
  <done>Docstring sharpened; no behaviour change.</done>
</task>

<task type="auto">
  <name>Task 3: tests/test_owner_role_backfill.py — migration 0019 acceptance</name>
  <files>tests/test_owner_role_backfill.py</files>
  <read_first>
    - tests/test_migration_backfill.py (pattern for migration-data tests)
    - app/core/settings.py
    - alembic/versions/0019_owner_role_backfill.py
  </read_first>
  <action>
    Создать `tests/test_owner_role_backfill.py`:

    ```python
    """Phase 32 REQ-32-06: idempotent owner-role backfill migration.

    Scenarios:
      • test_idempotent_already_owner — повторный UPDATE на уже-owner = no-op.
      • test_promotes_member_to_owner — member с tg_user_id=OWNER → owner после UPDATE.
      • test_no_owner_user_safe — отсутствие row с OWNER_TG_ID = silent no-op.

    Использует raw SQL imitating migration body — независимо от alembic CLI
    (тест должен бегать в одном transaction без full migration cycle).
    """
    from __future__ import annotations

    import os
    import uuid

    import pytest
    from sqlalchemy import text

    from app.db.models import UserRole

    pytestmark = pytest.mark.asyncio


    def _idempotent_backfill_sql(tg_user_id: int) -> str:
        """Mirror migration 0019 upgrade body."""
        return (
            "UPDATE app_user SET role = 'owner'::user_role "
            f"WHERE tg_user_id = {tg_user_id} AND role <> 'owner'"
        )


    async def test_promotes_member_to_owner(db_session):
        tg = 8881111 + (uuid.uuid4().int & 0xFFF)  # collision-resistant
        await db_session.execute(
            text("INSERT INTO app_user (tg_user_id, role) VALUES (:tg, 'member')"),
            {"tg": tg},
        )
        result = await db_session.execute(text(_idempotent_backfill_sql(tg)))
        # SQLAlchemy: rowcount available after DML
        assert result.rowcount == 1

        # Verify role flipped
        result = await db_session.execute(
            text("SELECT role FROM app_user WHERE tg_user_id=:tg"), {"tg": tg}
        )
        role = result.scalar_one()
        assert role == "owner"

        # Cleanup
        await db_session.execute(
            text("DELETE FROM app_user WHERE tg_user_id=:tg"), {"tg": tg}
        )


    async def test_idempotent_already_owner(db_session):
        tg = 8882222 + (uuid.uuid4().int & 0xFFF)
        await db_session.execute(
            text("INSERT INTO app_user (tg_user_id, role) VALUES (:tg, 'owner')"),
            {"tg": tg},
        )
        # Second-run UPDATE — should match 0 rows (idempotency).
        result = await db_session.execute(text(_idempotent_backfill_sql(tg)))
        assert result.rowcount == 0

        # Cleanup
        await db_session.execute(
            text("DELETE FROM app_user WHERE tg_user_id=:tg"), {"tg": tg}
        )


    async def test_no_owner_user_safe(db_session):
        # tg_user_id, который точно не существует.
        tg = 9_999_999_999
        result = await db_session.execute(text(_idempotent_backfill_sql(tg)))
        assert result.rowcount == 0  # silent no-op
    ```

    Запустить: `pytest tests/test_owner_role_backfill.py -v` — 3 теста green.
  </action>
  <acceptance_criteria>
    - File `tests/test_owner_role_backfill.py` exists.
    - `grep -c 'async def test_' tests/test_owner_role_backfill.py` = 3.
    - `pytest tests/test_owner_role_backfill.py -v` exits 0.
  </acceptance_criteria>
  <done>Backfill migration semantics tested.</done>
</task>

<task type="auto">
  <name>Task 4: tests/test_no_owner_tg_id_in_prod.py — production path audit (REQ-32-02)</name>
  <files>tests/test_no_owner_tg_id_in_prod.py</files>
  <read_first>
    - tests/test_auth.py (existing auth tests, pattern)
    - app/api/dependencies.py (get_current_user — production path)
    - app/core/auth.py (validate_init_data)
    - app/core/settings.py (DEV_MODE, OWNER_TG_ID)
  </read_first>
  <action>
    Создать `tests/test_no_owner_tg_id_in_prod.py`:

    ```python
    """Phase 32 REQ-32-02: production path has NO OWNER_TG_ID magic bypass.

    Sanity: даже если initData валидируется для tg_user_id == OWNER_TG_ID,
    при DEV_MODE=false и отсутствии app_user row — auth должен return 403,
    а НЕ имплицитный owner-upsert.

    Этот test закрывает регресcium-loophole: легко случайно вернуть legacy
    behaviour 'OWNER_TG_ID auto-resolves to owner without DB row' через
    дефолт в `_dev_mode_resolve_owner`. Тест явно проверяет, что production
    path этим не пользуется.

    Note: тест requires app fixtures с initData signing. Если test
    environment не может сгенерировать valid initData — мы fallback'имся
    на черный-box: mock validate_init_data + assert dependency body
    рейзит 403 для unknown tg_user_id.
    """
    from __future__ import annotations

    from unittest.mock import patch

    import pytest
    from fastapi import HTTPException
    from sqlalchemy import text

    from app.api.dependencies import get_current_user
    from app.core.settings import settings

    pytestmark = pytest.mark.asyncio


    async def test_prod_path_403_for_unknown_tg_user_id(db_session, monkeypatch):
        """DEV_MODE=false + initData для tg_user_id=OWNER_TG_ID без app_user row → 403."""
        # Ensure no row with OWNER_TG_ID exists.
        await db_session.execute(
            text("DELETE FROM app_user WHERE tg_user_id = :tg"),
            {"tg": settings.OWNER_TG_ID},
        )
        await db_session.commit()

        # Force production path.
        monkeypatch.setattr("app.core.settings.settings.DEV_MODE", False, raising=False)

        # Patch validate_init_data to return OWNER_TG_ID synthetically.
        with patch(
            "app.api.dependencies.validate_init_data",
            return_value={"id": settings.OWNER_TG_ID},
        ):
            with pytest.raises(HTTPException) as exc_info:
                await get_current_user(
                    x_telegram_init_data="mocked-payload",
                    authorization=None,
                    x_test_user=None,
                    db=db_session,
                )
            assert exc_info.value.status_code == 403, (
                f"Expected 403 (Not authorized) but got {exc_info.value.status_code}"
            )


    async def test_prod_path_role_based_only(db_session, monkeypatch):
        """User с role='member' и matching tg_user_id → returns user (не зависит от OWNER_TG_ID)."""
        import uuid
        tg = 7_777_000 + (uuid.uuid4().int & 0xFFF)

        await db_session.execute(
            text("INSERT INTO app_user (tg_user_id, role) VALUES (:tg, 'member')"),
            {"tg": tg},
        )
        await db_session.commit()

        monkeypatch.setattr("app.core.settings.settings.DEV_MODE", False, raising=False)

        try:
            with patch(
                "app.api.dependencies.validate_init_data",
                return_value={"id": tg},
            ):
                user = await get_current_user(
                    x_telegram_init_data="mocked-payload",
                    authorization=None,
                    x_test_user=None,
                    db=db_session,
                )
                assert user.tg_user_id == tg
                assert user.role.value == "member"  # ✓ NOT promoted via OWNER_TG_ID
        finally:
            await db_session.execute(
                text("DELETE FROM app_user WHERE tg_user_id = :tg"), {"tg": tg}
            )
            await db_session.commit()
    ```

    Запустить: `pytest tests/test_no_owner_tg_id_in_prod.py -v` — 2 теста green.
  </action>
  <acceptance_criteria>
    - File `tests/test_no_owner_tg_id_in_prod.py` exists.
    - `grep -c 'async def test_' tests/test_no_owner_tg_id_in_prod.py` = 2.
    - `grep "monkeypatch.setattr.*DEV_MODE.*False" tests/test_no_owner_tg_id_in_prod.py` matches.
    - `pytest tests/test_no_owner_tg_id_in_prod.py -v` exits 0.
  </acceptance_criteria>
  <done>Production-path audit covered by regression test.</done>
</task>

<task type="auto">
  <name>Task 5: Commit + SUMMARY</name>
  <files>.planning/phases/32-multi-tenant-prod/32-03-SUMMARY.md</files>
  <action>
    1. Stage: 
       - `alembic/versions/0019_owner_role_backfill.py`
       - `app/api/dependencies.py`
       - `tests/test_owner_role_backfill.py`
       - `tests/test_no_owner_tg_id_in_prod.py`
       - SUMMARY.md
    2. Commit: `feat(32-03): owner-role backfill migration 0019 + production path audit (REQ-32-02, REQ-32-06)` with `--no-verify`.
    3. SUMMARY.md: contains "REQ-32-02" + "REQ-32-06", migration 0019 details, test coverage, behaviour change = NONE (docstring + safety-net).
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` contains `32-03`.
    - SUMMARY.md exists with min 12 lines and contains "REQ-32-02".
  </acceptance_criteria>
  <done>Commit landed.</done>
</task>

</tasks>

<verification>
1. Migration 0019 idempotent, applied + downgrade tested.
2. `app/api/dependencies.py` docstring strengthened (zero behaviour change).
3. `tests/test_owner_role_backfill.py` + `tests/test_no_owner_tg_id_in_prod.py` green.
4. Existing `tests/test_auth.py` + `tests/test_role_based_auth.py` still green.
5. SUMMARY.md complete.
</verification>

<output>32-03-SUMMARY.md per template.</output>
