---
phase: 35-paywall
plan: 03
type: execute
wave: 3
depends_on: [35-02]
requirements:
  - REQ-35-05
commit: 698d3e7
---

<objective>
Frontend PaywallSheet — bottom-sheet с pricing tiers (monthly 299 ₽ /
annual 1990 ₽ -44%), feature bullets, ЮKassa CTA. `ProTierRequiredError`
class extends `ApiError` для 402 detection в UI.
</objective>

## Task

1. `frontend/src/lib/api/errors.ts` — `ProTierRequiredError` class, парсит
   detail `{error: "PRO_TIER_REQUIRED", current_tier, upgrade_url}`.
2. `frontend/src/lib/api/me.ts` — `getMyTier()` helper.
3. `frontend/src/components/PaywallSheet.tsx` — bottom-sheet UI, monthly
   299₽ / annual 1990₽ (annual highlight "-44%"), 5 feature bullets, CTA
   "Оплатить через ЮKassa" → POST /billing/create-payment.
4. Tests `frontend/src/components/__tests__/PaywallSheet.test.tsx` —
   render + interaction (5 cases).

## Acceptance

- `npm test -- PaywallSheet` → 5 passed.
- `ProTierRequiredError` thrown by API client when response is 402 with
  expected detail shape.

## Commit

`698d3e7` — feat(35-03): PaywallSheet UI + tier API + 402 error class (REQ-35-03)
