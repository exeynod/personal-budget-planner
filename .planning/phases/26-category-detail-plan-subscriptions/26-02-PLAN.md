---
phase: 26
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/api/v10/categories.ts
  - frontend/src/api/v10/index.ts
  - frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts
  - frontend/src/screensV10/CategoryDetail/CategoryDetailView.tsx
  - frontend/src/screensV10/CategoryDetail/CategoryDetailView.module.css
  - frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx
  - frontend/src/screensV10/CategoryDetail/index.ts
  - frontend/src/screensV10/CategoryDetail/__tests__/computeCategoryDetail.test.ts
  - frontend/src/screensV10/CategoryDetail/__tests__/CategoryDetailView.test.tsx
  - frontend/src/screensV10/Home/HomeMount.tsx
autonomous: true
requirements: [CAT-V10-01, CAT-V10-02, CAT-V10-03, CAT-V10-04, CAT-V10-05, CAT-V10-06]
tags: [react, typescript, vitest, posterRouter, category-detail, rollover-toggle, paused-toggle]

must_haves:
  truths:
    - "Push CategoryDetail из Home (через router.push после row tap) → cobalt-фон когда fact ≤ plan, red когда fact > plan"
    - "BigFig faktcents/100 + count-up; italic подзаголовок «— превышено на N%» / «— на N% плана»"
    - "Progress bar 6px высотой с разрывом-tick на отметке plan/fact когда isOver"
    - "Toggle plate «ОСТАТОК → НАКОПЛЕНИЯ / ПРОЧЕЕ» меняет category.rollover через PATCH /categories/:id"
    - "CTA «+ ПОДНЯТЬ ЛИМИТ» pushes PlanView с focus={categoryId}; «ПАУЗА» toggle category.paused через PATCH"
    - "Список операций по категории (filter actuals where category_id === id) day-grouped"
  artifacts:
    - path: "frontend/src/api/v10/categories.ts"
      provides: "updateCategoryV10 typed wrapper"
      contains: "export async function updateCategoryV10"
    - path: "frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts"
      provides: "computeOverPercent, computeUnderPercent, computeBarSegments, filterActualsForCategory"
      min_lines: 100
    - path: "frontend/src/screensV10/CategoryDetail/CategoryDetailView.tsx"
      provides: "CategoryDetailView presentational"
      min_lines: 200
    - path: "frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx"
      provides: "Data fetcher + router-push glue + PATCH calls"
      min_lines: 150
    - path: "frontend/src/screensV10/Home/HomeMount.tsx"
      provides: "MODIFIED — onCategoryTap pushes CategoryDetailMount instead of placeholder"
      contains: "CategoryDetailMount"
  key_links:
    - from: "frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx"
      to: "frontend/src/api/v10/categories.ts"
      via: "updateCategoryV10(id, {rollover}) and updateCategoryV10(id, {paused})"
      pattern: "updateCategoryV10"
    - from: "frontend/src/screensV10/Home/HomeMount.tsx"
      to: "frontend/src/screensV10/CategoryDetail/index.ts"
      via: "import { CategoryDetailMount }"
      pattern: "CategoryDetailMount"
---

<objective>
Web Category Detail screen (CAT-V10-01..06): push-stack экран на cobalt-фоне (red если isOver) с Mass UPPERCASE именем категории, italic подзаголовком, BigFig фактом + count-up, progress bar 6px с tick на отметке плана, rollover-toggle plate, CTA-row «+ ПОДНЯТЬ ЛИМИТ» / «ПАУЗА», список операций.

Replaces CategoryDetailPlaceholder (`screensV10/_placeholders.tsx`) которое HomeMount.tsx сейчас pushes на row tap (Phase 25-04 line ~placeholder import).

Purpose: User видит drill-down на конкретную категорию с возможностью toggle rollover policy и paused-флага без выхода в settings.
Output: 7 новых файлов в `screensV10/CategoryDetail/` (compute + view + mount + tests + barrel) + 1 modified (HomeMount.tsx swap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-category-detail-plan-subscriptions/26-CONTEXT.md
@.planning/phases/26-category-detail-plan-subscriptions/26-must-haves.md
@.planning/phases/25-home-transactions-add-sheet/25-04-web-home-view-SUMMARY.md
@.planning/phases/25-home-transactions-add-sheet/25-08-web-transactions-SUMMARY.md

@frontend/src/api/v10/categories.ts
@frontend/src/api/v10/index.ts
@frontend/src/screensV10/Home/HomeMount.tsx
@frontend/src/screensV10/Transactions/computeTransactions.ts
@frontend/src/screensV10/common/format.ts

<interfaces>
<!-- Existing CategoryV10 type (frontend/src/api/types.ts:613): -->
```typescript
export interface CategoryV10 {
  id: number;
  name: string;
  kind: CategoryKind;
  is_archived: boolean;
  sort_order: number;
  created_at: string;
  code?: string | null;
  plan_cents?: number;
  ord?: string;
  rollover?: CategoryRollover;     // 'misc' | 'savings'
  paused?: boolean;
  parent_id?: number | null;
}
```

<!-- Existing API wrappers (frontend/src/api/v10/categories.ts): -->
```typescript
export async function listCategoriesV10(includeArchived = false): Promise<CategoryV10[]>;
// Need to ADD:
// export async function updateCategoryV10(id: number, payload: {...}): Promise<CategoryV10>;
```

<!-- Existing components (componentsV10/index.ts barrel): -->
```typescript
import { Eyebrow, Mass, BigFig, Plate, Chip, PosterButton } from '../../componentsV10';
// BigFig: <BigFig value={number} sup={string} size={number} color={string} animate?={boolean} />
// Plate: <Plate tone="paper" | "yellow" | "dark" | "red">{children}</Plate>
// Mass: <Mass italic={boolean} size={number}>{text}</Mass>
```

<!-- Existing PosterRouter (screensV10/common/PosterRouter.tsx): -->
```typescript
const router = usePosterRouter();
router.push(<SomeView />);
router.pop();
router.canPop;  // boolean
```

<!-- Existing helpers from Transactions plan (re-use): -->
```typescript
import { groupByDay, formatTxAmount, applyFilterChip } from '../Transactions/computeTransactions';
// groupByDay(actuals, today) → TxDayGroup[]
// formatTxAmount(cents, kind) → string with U+2212 minus
```

<!-- Existing format helpers (screensV10/common/format.ts): -->
```typescript
import { formatDay, formatTimeHM } from '../common';
```

<!-- Filter pattern from HomeMount (Plan 25-04): handle category tap callback -->
<!-- Currently HomeMount.tsx pushes <CategoryDetailPlaceholder categoryId={id} />.
     Plan 25-04 SUMMARY confirms placeholder import at top of HomeMount; swap to:
     import { CategoryDetailMount } from '../CategoryDetail';
     ... onCategoryTap={(id) => router.push(<CategoryDetailMount categoryId={id} />)} -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure compute helpers + RED/GREEN tests</name>
  <read_first>
    - frontend/src/screensV10/Home/computeHomeData.ts (Lines 1-end — pattern reference)
    - frontend/src/screensV10/Transactions/computeTransactions.ts (groupByDay + formatTxAmount export signatures)
    - frontend/src/api/types.ts (Lines 510-633 — ActualV10Read + CategoryV10 + AccountResponse shapes)
  </read_first>
  <files>
    - frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts
    - frontend/src/screensV10/CategoryDetail/__tests__/computeCategoryDetail.test.ts
  </files>
  <behavior>
    - Test 1: computeOverPercent(15000, 10000) → 50 (15000/10000=1.5, over by 50%); rounded.
    - Test 2: computeOverPercent(10000, 10000) → 0 (no over); but caller should not invoke when isOver=false.
    - Test 3: computeOverPercent(11500, 10000) → 15 (round((11500-10000)/10000*100)=15).
    - Test 4: computeUnderPercent(7500, 10000) → 75; computeUnderPercent(0, 10000) → 0; computeUnderPercent(10000, 10000) → 100.
    - Test 5: computeUnderPercent(5000, 0) → 0 (avoid div-by-zero).
    - Test 6: computeBarSegments(7500, 10000) → {fillRatio: 0.75, tickAt: undefined} (under-budget; no tick).
    - Test 7: computeBarSegments(15000, 10000) → {fillRatio: 1.0, tickAt: 0.6667} (over-budget; capped fill, tick at plan/fact).
    - Test 8: computeBarSegments(10000, 0) → {fillRatio: 1.0, tickAt: 0} (any spend without plan = full + tick at start).
    - Test 9: filterActualsForCategory(actuals, 5) → only rows where category_id === 5; preserves array order.
    - Test 10: filterActualsForCategory(actuals, 999) → empty array (no match).
  </behavior>
  <action>
    1. Создать `frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts` (~120 LOC):
       ```typescript
       import type { ActualV10Read } from '../../api/v10';

       /** Returns rounded percent over plan when fact > plan, else 0. */
       export function computeOverPercent(factCents: number, planCents: number): number {
         if (planCents <= 0) return 0;  // edge — caller shouldn't display when no plan
         if (factCents <= planCents) return 0;
         return Math.round(((factCents - planCents) / planCents) * 100);
       }

       /** Returns rounded percent of plan used when fact ≤ plan. 0 if planCents=0. */
       export function computeUnderPercent(factCents: number, planCents: number): number {
         if (planCents <= 0) return 0;
         return Math.round((factCents / planCents) * 100);
       }

       export interface BarSegments {
         /** 0..1 — width of filled bar (capped at 1 for over-budget). */
         fillRatio: number;
         /** 0..1 position of break-tick (only set when over-budget; equals planCents/factCents). */
         tickAt?: number;
       }

       export function computeBarSegments(factCents: number, planCents: number): BarSegments {
         if (factCents <= 0) return { fillRatio: 0 };
         if (planCents <= 0) return { fillRatio: 1, tickAt: 0 };  // any spend w/o plan
         if (factCents <= planCents) return { fillRatio: factCents / planCents };
         // Over-budget — cap fill at 1, mark threshold inside the bar
         return { fillRatio: 1, tickAt: planCents / factCents };
       }

       /** Filter actuals to a single category (preserves input order). */
       export function filterActualsForCategory(
         actuals: ActualV10Read[],
         categoryId: number,
       ): ActualV10Read[] {
         return actuals.filter((a) => a.category_id === categoryId);
       }

       /** Sum of expense cents for a category (positive number). */
       export function computeFactForCategory(
         actuals: ActualV10Read[],
         categoryId: number,
       ): number {
         return actuals
           .filter((a) => a.category_id === categoryId && a.kind === 'expense')
           .reduce((sum, a) => sum + Math.abs(a.amount_cents), 0);
       }
       ```
    2. Создать `frontend/src/screensV10/CategoryDetail/__tests__/computeCategoryDetail.test.ts` (~120 LOC) с 10 тестами выше + 2 дополнительных для computeFactForCategory (sum of multiple expenses; ignores income/roundup/deposit kinds).
    3. RED: `cd frontend && npm test -- screensV10/CategoryDetail --run` — fails (file не создан).
    4. GREEN: создаётся файл → tests pass.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm test -- screensV10/CategoryDetail/__tests__/computeCategoryDetail.test.ts --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts` exists.
    - `grep -c "export function" frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts` ≥ 5.
    - All compute tests pass (12+).
  </acceptance_criteria>
  <done>5 pure helpers exported + tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategoryDetailView presenter + RED/GREEN tests + CSS module</name>
  <read_first>
    - frontend/src/screensV10/Home/HomeView.tsx (Lines 1-end — pattern для bar-fill, OVER plate, eyebrow row, back button)
    - frontend/src/screensV10/Home/HomeView.module.css
    - frontend/src/screensV10/Transactions/TransactionsView.tsx (day-grouped row pattern для оп. списка)
    - frontend/src/componentsV10/index.ts (барраль re-exports)
  </read_first>
  <files>
    - frontend/src/screensV10/CategoryDetail/CategoryDetailView.tsx
    - frontend/src/screensV10/CategoryDetail/CategoryDetailView.module.css
    - frontend/src/screensV10/CategoryDetail/__tests__/CategoryDetailView.test.tsx
  </files>
  <behavior>
    - Test 1: Renders Mass UPPERCASE имя категории (e.g. "ПРОДУКТЫ").
    - Test 2: Renders italic подзаголовок «— на 75% плана» когда fact ≤ plan; renders «— превышено на 50%» когда fact > plan.
    - Test 3: Background = cobalt CSS class когда !isOver; = red class когда isOver.
    - Test 4: BigFig визуально показывает fact/100 + suffix «₽» (animation в jsdom через `bigFigAnimate?: boolean = true` prop).
    - Test 5: Progress bar element имеет `style="width: 75%"` для under-budget; `style="width: 100%"` + tick child для over-budget.
    - Test 6: Plate «ОСТАТОК → НАКОПЛЕНИЯ» при rollover='savings'; «ОСТАТОК → ПРОЧЕЕ» при rollover='misc'; click → calls onToggleRollover.
    - Test 7: Кнопка «+ ПОДНЯТЬ ЛИМИТ» click → calls onPushPlan(categoryId).
    - Test 8: Кнопка «ПАУЗА» (или «ВКЛЮЧИТЬ» когда paused=true) click → calls onTogglePause.
    - Test 9: Список операций — для actuals length=3, рендерится 3 row (re-use pattern). Empty actuals → empty-state «Операций пока нет».
    - Test 10: ← НАЗАД button → calls onBack.
  </behavior>
  <action>
    1. Создать `CategoryDetailView.tsx` (~250 LOC) — presentational с props:
       ```typescript
       import { Eyebrow, Mass, BigFig, PosterButton } from '../../componentsV10';
       import type { CategoryV10, ActualV10Read } from '../../api/v10';
       import { computeOverPercent, computeUnderPercent, computeBarSegments, computeFactForCategory } from './computeCategoryDetail';
       import { groupByDay, formatTxAmount } from '../Transactions/computeTransactions';
       import { formatDay, formatTimeHM } from '../common';
       import styles from './CategoryDetailView.module.css';

       export interface CategoryDetailViewProps {
         category: CategoryV10;
         actuals: ActualV10Read[];
         today?: Date;
         bigFigAnimate?: boolean;
         onPushPlan: (categoryId: number) => void;
         onTogglePause: () => void;
         onToggleRollover: () => void;
         onBack: () => void;
       }

       export function CategoryDetailView({...props}: CategoryDetailViewProps) {
         const today = props.today ?? new Date();
         const planCents = props.category.plan_cents ?? 0;
         const factCents = computeFactForCategory(props.actuals, props.category.id);
         const isOver = factCents > planCents;
         const subtitle = isOver
           ? `— превышено на ${computeOverPercent(factCents, planCents)}%`
           : `— на ${computeUnderPercent(factCents, planCents)}% плана`;
         const segments = computeBarSegments(factCents, planCents);
         const rollover = props.category.rollover ?? 'misc';
         const paused = props.category.paused ?? false;
         const dayGroups = groupByDay(
           props.actuals.filter((a) => a.category_id === props.category.id),
           today,
         );

         return (
           <div className={`${styles.root} ${isOver ? styles.bgRed : styles.bgCobalt}`}>
             {/* Header row: ← НАЗАД + Eyebrow «CATEGORY · {ord}» */}
             <div className={styles.headerRow}>
               <button onClick={props.onBack} aria-label="Назад" className={styles.backBtn}>← НАЗАД</button>
               <Eyebrow>{`CATEGORY · ${props.category.ord ?? '00'}`}</Eyebrow>
             </div>
             {/* Mass UPPERCASE name + italic subtitle */}
             <Mass size={70}>{props.category.name.toUpperCase()}</Mass>
             <Mass italic size={28} className={styles.subtitle}>{subtitle}</Mass>
             {/* BigFig factCents/100 with count-up */}
             <BigFig value={Math.floor(factCents / 100)} sup="₽" size={88} color="paper" animate={props.bigFigAnimate ?? true} />
             {/* Progress bar 6px with tick */}
             <div className={styles.barTrack}>
               <div className={styles.barFill} style={{width: `${segments.fillRatio * 100}%`}} />
               {segments.tickAt !== undefined && (
                 <div className={styles.barTick} style={{left: `${segments.tickAt * 100}%`}} />
               )}
             </div>
             <div className={styles.barLabel}>{`из ${(planCents/100).toLocaleString('ru-RU')} ₽`}</div>
             {/* Rollover toggle plate */}
             <button
               type="button"
               onClick={props.onToggleRollover}
               className={styles.rolloverPlate}
               data-testid="rollover-plate"
             >
               {rollover === 'savings' ? 'ОСТАТОК → НАКОПЛЕНИЯ' : 'ОСТАТОК → ПРОЧЕЕ'}
             </button>
             {/* CTA row: «+ ПОДНЯТЬ ЛИМИТ» / «ПАУЗА» */}
             <div className={styles.ctaRow}>
               <PosterButton onClick={() => props.onPushPlan(props.category.id)} variant="ghost">+ ПОДНЯТЬ ЛИМИТ</PosterButton>
               <PosterButton onClick={props.onTogglePause} variant="ghost">{paused ? 'ВКЛЮЧИТЬ' : 'ПАУЗА'}</PosterButton>
             </div>
             {/* Operations list */}
             <Eyebrow>ОПЕРАЦИИ ПО КАТЕГОРИИ</Eyebrow>
             {dayGroups.length === 0 ? (
               <div className={styles.emptyState}>Операций пока нет</div>
             ) : (
               dayGroups.map((g) => (
                 <section key={g.dateKey} className={styles.daySection}>
                   <header className={styles.dayHeader}>
                     <span className={styles.dayLabel}>{g.dateLabel}</span>
                     <span className={styles.daySum}>{formatTxAmount(g.sumCents, 'expense')}</span>
                   </header>
                   {g.rows.map((tx) => (
                     <div key={tx.id} className={styles.row}>
                       <span className={styles.time}>{formatTimeHM(new Date(tx.created_at))}</span>
                       <span className={styles.desc}>{tx.description ?? '—'}</span>
                       <span className={styles.amount}>{formatTxAmount(tx.amount_cents, tx.kind)}</span>
                     </div>
                   ))}
                 </section>
               ))
             )}
           </div>
         );
       }
       ```
    2. Создать `CategoryDetailView.module.css` (~150 LOC) — определить `.root`, `.bgCobalt` (background-color: var(--poster-cobalt)), `.bgRed` (var(--poster-red)), `.barTrack` (height: 6px, position: relative), `.barFill` (height: 100%, background: var(--poster-paper)), `.barTick` (position:absolute, top:-2px, bottom:-2px, width:1px, background: var(--poster-paper)), `.rolloverPlate`, `.ctaRow`, `.daySection`, etc.
    3. Создать `__tests__/CategoryDetailView.test.tsx` (~250 LOC) с 10 тестами behavior + дополнительный test для empty actuals.
    4. RED → GREEN.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm test -- screensV10/CategoryDetail/__tests__/CategoryDetailView.test.tsx --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `CategoryDetailView.tsx` exists; rendered DOM contains `Mass`, `BigFig`, `PosterButton` элементы.
    - `grep -c "ПОДНЯТЬ ЛИМИТ\|ПАУЗА\|ВКЛЮЧИТЬ\|НАКОПЛЕНИЯ\|ПРОЧЕЕ" frontend/src/screensV10/CategoryDetail/CategoryDetailView.tsx` ≥ 5.
    - 10+ tests pass.
    - tsc strict clean.
  </acceptance_criteria>
  <done>CategoryDetailView renders все CAT-V10-01..06 элементы; presenter pure (без fetch); 10+ tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: API extension + CategoryDetailMount + barrel + HomeMount swap</name>
  <read_first>
    - frontend/src/api/v10/categories.ts (Lines 1-35)
    - frontend/src/api/v10/index.ts (barrel)
    - frontend/src/screensV10/Home/HomeMount.tsx (find onCategoryTap handler + CategoryDetailPlaceholder import)
  </read_first>
  <files>
    - frontend/src/api/v10/categories.ts
    - frontend/src/api/v10/index.ts
    - frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx
    - frontend/src/screensV10/CategoryDetail/index.ts
    - frontend/src/screensV10/Home/HomeMount.tsx
  </files>
  <action>
    1. **Extend `frontend/src/api/v10/categories.ts`** — добавить функцию ПОСЛЕ существующей `listCategoriesV10`:
       ```typescript
       export interface CategoryV10UpdatePayload {
         name?: string;
         sort_order?: number;
         is_archived?: boolean;
         /** Phase 26 — v1.0 fields per CAT-V10-04 / PLAN-V10-05 (backend Phase 26-01 BE-04 ext). */
         plan_cents?: number;
         rollover?: 'misc' | 'savings';
         paused?: boolean;
         parent_id?: number | null;
       }

       /**
        * PATCH /api/v1/categories/{id}
        * Phase 26: backend now accepts plan_cents/rollover/paused/parent_id.
        * Returns updated CategoryV10 row.
        */
       export async function updateCategoryV10(
         id: number,
         payload: CategoryV10UpdatePayload,
       ): Promise<CategoryV10> {
         return apiFetch<CategoryV10>(`/categories/${id}`, {
           method: 'PATCH',
           body: JSON.stringify(payload),
         });
       }
       ```
    2. **Extend `frontend/src/api/v10/index.ts`** — re-export:
       ```typescript
       export { listCategoriesV10, updateCategoryV10 } from './categories';
       export type { CategoryV10, CategoryRollover, CategoryV10UpdatePayload } from './categories';
       ```
    3. **Создать `frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx`** (~180 LOC):
       ```typescript
       import { useCallback, useEffect, useState } from 'react';
       import { usePosterRouter } from '../common';
       import { listCategoriesV10, listAccounts, updateCategoryV10, listActualV10 } from '../../api/v10';
       import { getCurrentPeriod } from '../../api/periods';
       import type { CategoryV10, ActualV10Read } from '../../api/v10';
       import { CategoryDetailView } from './CategoryDetailView';
       import { PlanViewPlaceholder } from '../_placeholders';

       export interface CategoryDetailMountProps {
         categoryId: number;
       }

       export function CategoryDetailMount({categoryId}: CategoryDetailMountProps) {
         const router = usePosterRouter();
         const [category, setCategory] = useState<CategoryV10 | null>(null);
         const [actuals, setActuals] = useState<ActualV10Read[]>([]);
         const [status, setStatus] = useState<'loading'|'ready'|'error'>('loading');
         const [error, setError] = useState<string>('');
         const [reloadToken, setReloadToken] = useState(0);

         useEffect(() => {
           let cancelled = false;
           (async () => {
             try {
               setStatus('loading');
               const [cats, period] = await Promise.all([
                 listCategoriesV10(),
                 getCurrentPeriod(),
               ]);
               if (cancelled) return;
               const cat = cats.find((c) => c.id === categoryId);
               if (!cat) { setError('Категория не найдена'); setStatus('error'); return; }
               setCategory(cat);
               if (period !== null) {
                 const acts = await listActualV10(period.id);
                 if (cancelled) return;
                 setActuals(acts);
               }
               setStatus('ready');
             } catch (e) {
               if (cancelled) return;
               setError(e instanceof Error ? e.message : 'Ошибка загрузки');
               setStatus('error');
             }
           })();
           return () => { cancelled = true; };
         }, [categoryId, reloadToken]);

         const handleToggleRollover = useCallback(async () => {
           if (!category) return;
           const next = (category.rollover ?? 'misc') === 'misc' ? 'savings' : 'misc';
           try {
             const updated = await updateCategoryV10(category.id, {rollover: next});
             setCategory(updated);
           } catch (e) {
             window.alert('Не удалось обновить rollover');
           }
         }, [category]);

         const handleTogglePause = useCallback(async () => {
           if (!category) return;
           try {
             const updated = await updateCategoryV10(category.id, {paused: !(category.paused ?? false)});
             setCategory(updated);
           } catch (e) {
             window.alert('Не удалось обновить pause');
           }
         }, [category]);

         const handlePushPlan = useCallback((catId: number) => {
           // For Phase 26 — push placeholder which Plan 26-04 will swap for real PlanMount with focus prop.
           // Plan 26-04 import path will replace this with `<PlanMount focusCategoryId={catId} />`.
           router.push(<PlanViewPlaceholder />);
         }, [router]);

         if (status === 'loading') {
           return <div data-testid="cat-loading">Загрузка…</div>;
         }
         if (status === 'error' || !category) {
           return (
             <div data-testid="cat-error">
               <div>{error}</div>
               <button onClick={() => setReloadToken((n) => n+1)}>Попробовать ещё раз</button>
             </div>
           );
         }
         return (
           <CategoryDetailView
             category={category}
             actuals={actuals}
             onPushPlan={handlePushPlan}
             onTogglePause={handleTogglePause}
             onToggleRollover={handleToggleRollover}
             onBack={() => router.pop()}
           />
         );
       }
       ```

       **NOTE for Plan 26-04 wiring:** В Task 3 of Plan 26-04, после создания PlanMount, исполнитель должен заменить `import { PlanViewPlaceholder } from '../_placeholders'` на `import { PlanMount } from '../Plan'` и `router.push(<PlanViewPlaceholder />)` на `router.push(<PlanMount focusCategoryId={catId} />)`. Это документируется здесь и в Plan 26-04 must_haves.

    4. **Создать `frontend/src/screensV10/CategoryDetail/index.ts`** — barrel:
       ```typescript
       export { CategoryDetailMount } from './CategoryDetailMount';
       export type { CategoryDetailMountProps } from './CategoryDetailMount';
       export { CategoryDetailView } from './CategoryDetailView';
       export type { CategoryDetailViewProps } from './CategoryDetailView';
       export * from './computeCategoryDetail';
       ```
    5. **Modify `frontend/src/screensV10/Home/HomeMount.tsx`** — найти `import { ... CategoryDetailPlaceholder ... } from '../_placeholders';`, удалить `CategoryDetailPlaceholder` из этого импорта; добавить `import { CategoryDetailMount } from '../CategoryDetail';`. Найти `onCategoryTap` callback / push (look for line containing `<CategoryDetailPlaceholder`); заменить на `<CategoryDetailMount categoryId={id} />`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit && npm test -- screensV10/CategoryDetail screensV10/Home --run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "updateCategoryV10" frontend/src/api/v10/categories.ts` ≥ 2 (declaration + maybe internal note).
    - `grep -c "updateCategoryV10" frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx` ≥ 2.
    - `grep -c "CategoryDetailMount" frontend/src/screensV10/Home/HomeMount.tsx` ≥ 2 (import + push).
    - `grep -v '^//' frontend/src/screensV10/Home/HomeMount.tsx | grep -c "CategoryDetailPlaceholder"` == 0 (placeholder removed from non-comment code).
    - tsc strict clean; vite build succeeds.
    - No regressions in HomeMount tests.
  </acceptance_criteria>
  <done>
    Real CategoryDetail mounted в HomeMount; rollover-toggle и paused-toggle работают через PATCH; «+ ПОДНЯТЬ ЛИМИТ» pushes (placeholder for now, Plan 26-04 swaps).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | Untrusted PATCH body for category updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | updateCategoryV10 payload | mitigate | Backend Pydantic validation (Phase 26-01 BE plan); UI uses typed CategoryV10UpdatePayload — TS prevents passing arbitrary keys at compile time. |
| T-26-02-02 | DoS | Re-entrant rollover toggle clicks | accept | Single-tenant; spam clicks = N PATCH calls, server idempotent (each call sets new value); UI updates optimistically; no debounce needed for v1.0. |
| T-26-02-03 | Information Disclosure | CategoryDetail для другой категории | mitigate | listCategoriesV10 returns only authenticated user's categories (RLS server-side); `cats.find(c => c.id === categoryId)` returns undefined для cross-tenant ID → "Категория не найдена" error state, no leak. |
| T-26-02-04 | Repudiation | rollover/paused toggle silent fail | mitigate | catch block window.alert на любую API failure (минимально жизнеспособное; Plan 28 polish заменит на toast). |
</threat_model>

<verification>
- npm test -- screensV10/CategoryDetail --run → all pass
- npm test -- --run → no regressions in 334+ tests
- npx tsc --noEmit → clean
- npm run build → succeeds
- Manual: Open http://localhost:5173 (или локальная dev URL), tap on a category from Home → CategoryDetail loads with cobalt/red bg, BigFig animates count-up, rollover plate toggles on click (network tab shows PATCH /categories/X with {rollover}), «+ ПОДНЯТЬ ЛИМИТ» pushes placeholder, ← НАЗАД pops back to Home.
</verification>

<success_criteria>
- 6 REQs (CAT-V10-01..06) satisfied by CategoryDetail component + mount wiring
- HomeMount.tsx swap from placeholder to real CategoryDetailMount complete
- All compute helpers + view tested; mount component tsc-clean
- updateCategoryV10 API wrapper used for both rollover and paused toggles
</success_criteria>

<output>
После завершения создать `.planning/phases/26-category-detail-plan-subscriptions/26-02-SUMMARY.md` с requirements-completed: [CAT-V10-01..06].
</output>
