---
phase: 61
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Features/Management/ManagementView.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift
autonomous: true
requirements:
  - v1.1.2-61-CONTEXT-area-1-navigation
  - v1.1.2-61-CONTEXT-area-2-master-list
  - v1.1.2-61-CONTEXT-area-3-row-editor
  - v1.1.2-61-CONTEXT-area-4-save-strategy
threat_ids:
  - T-61-01
must_haves:
  truths:
    - "ManagementItem.planEditor добавлен в ManagementItem.ID enum + ManagementItem.all (icon slider.horizontal.3, label «План месяца», ownerOnly false), inserted перед .subscriptions per CONTEXT D-1."
    - "ManagementView.destination(for:) обрабатывает case .planEditor → PlanEditorView()."
    - "Новый файл PlanEditorRoute.swift определяет typed-route enum (PlanEditorRoute.row(categoryId: Int)) — distinct от Int.self чтобы избежать конфликта с AccountsView.navigationDestination(for: Int.self) в shared ManagementView NavigationStack."
    - "Features/PlanEditor/ каталог создан с 4 scaffold-файлами (PlanEditorView, PlanEditorViewModel, PlanRowEditorView, PlanRowEditorViewModel) — surfaces only, load/save/body empty."
    - "PlanEditorData (pure-compute enum, no SwiftUI imports) определён со scaffold функциями computeSurplus, sortCategoriesForDisplay, planByCategoryId, applyOptimisticUpdate (signatures only — bodies в 61-02)."
    - "PlanRowEditorViewModel определяет onSaved closure (var onSaved: ((CategoryV10DTO) -> Void)?) как interface contract — 61-02 будет инжектить closure для optimistic update parent VM."
    - "Coexistence: FeaturesV10/Plan/* (PlanView.swift, PlanViewModel.swift, PlanData.swift), MainShell.swift, TemplateView.swift, AccountsView.swift, AccountDetailView.swift — untouched."
    - "Build clean (cd ios && make build) — все 4 stub-файла компилируются."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Management/ManagementView.swift"
      provides: "ManagementItem.planEditor registration + destination switch case"
      contains: "case planEditor"
      contains: "planEditor"
      contains: "slider.horizontal.3"
      contains: "План месяца"
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift"
      provides: "typed-route enum для shared NavigationStack disambiguation"
      contains: "enum PlanEditorRoute"
      contains: "case row(categoryId: Int)"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift"
      provides: "pure-compute helper enum (signatures — bodies в 61-02)"
      contains: "enum PlanEditorData"
      contains: "computeSurplus"
      contains: "sortCategoriesForDisplay"
      min_lines: 30
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift"
      provides: "scaffold struct PlanEditorView: View с State viewModel + empty body (60-01 pattern)"
      contains: "struct PlanEditorView"
      contains: "PlanEditorViewModel"
      min_lines: 15
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift"
      provides: "scaffold @Observable VM с surface (status enum, properties, init, empty load())"
      contains: "@Observable"
      contains: "final class PlanEditorViewModel"
      contains: "func load() async"
      min_lines: 40
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift"
      provides: "scaffold struct PlanRowEditorView: View (init + State viewModel + empty body)"
      contains: "struct PlanRowEditorView"
      contains: "categoryId: Int"
      min_lines: 15
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift"
      provides: "scaffold @Observable VM с surface (status, dirty/inFlight, onSaved closure, init, empty save())"
      contains: "@Observable"
      contains: "final class PlanRowEditorViewModel"
      contains: "onSaved"
      contains: "func save() async"
      min_lines: 50
  key_links:
    - from: "ManagementView.destination(for:)"
      to: "PlanEditorView()"
      via: "switch case .planEditor"
      pattern: "case \\.planEditor:"
    - from: "PlanRowEditorViewModel"
      to: "Parent VM optimistic update"
      via: "onSaved: ((CategoryV10DTO) -> Void)? closure"
      pattern: "onSaved.*CategoryV10DTO"
---

<objective>
Scaffold Phase 61 (Plan Editor v06 native). Зарегистрировать новый `ManagementItem.planEditor` в `ManagementView` (новый row «План месяца» перед «Подписки» per CONTEXT D-1) + создать каталог `ios/BudgetPlanner/Features/PlanEditor/` с 7 файлами: PlanEditorRoute (typed-route enum для disambiguation), PlanEditorData (pure-compute helpers — bodies в 61-02), PlanEditorView (scaffold), PlanEditorViewModel (scaffold с empty load), PlanRowEditorView (scaffold), PlanRowEditorViewModel (scaffold с onSaved closure contract + empty save).

Purpose: дать parallel wave 2 (61-02 и 61-03) готовый каркас + четкий contract (`onSaved: (CategoryV10DTO) -> Void`). PlanEditorRoute распознает push на детальный view через `navigationDestination(for: PlanEditorRoute.self)` — это избавляет от конфликта с `Int.self` который уже использует AccountsView в той же ManagementView NavigationStack.

Output:
- ManagementItem.planEditor виден в Management list (icon slider.horizontal.3, label «План месяца»).
- Tap → push PlanEditorView (пустой body — реализация в 61-02).
- 4 ViewModel/View scaffold готовы к параллельному заполнению в waves 2.
- PlanEditorData enum surface готов (signatures) для bodies в 61-02.
- PlanRowEditorViewModel.onSaved closure contract зафиксирован для 61-03.
- Build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-plan-editor-v06/61-CONTEXT.md
@.planning/phases/60-accounts-v06/60-01-SUMMARY.md
@ios/BudgetPlanner/Features/Management/ManagementView.swift
@ios/BudgetPlanner/Features/Accounts/AccountsView.swift
@ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift
@ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift
@ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift
@ios/BudgetPlanner/Networking/Endpoints/CategoriesV10API.swift
@ios/BudgetPlanner/FeaturesV10/Plan/PlanData.swift

<interfaces>
<!-- Key contracts for 61-02 / 61-03 consumers. -->

From ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift:
```swift
struct CategoryV10DTO: Decodable, Identifiable, Equatable {
    let id: Int
    let name: String
    let kind: CategoryKind       // .expense | .income
    let isArchived: Bool
    let sortOrder: Int
    let createdAt: Date?
    let code: String?            // "savings" — system category
    let planCents: Int           // ≥ 0
    let ord: String?             // '01'..'99' display ordinal
    let rollover: CategoryRollover  // .misc | .savings (default .misc)
    let paused: Bool             // default false
    let parentId: Int?
}

enum CategoryRollover: String, Codable {
    case misc
    case savings
}

struct CategoryV10UpdateRequest: Encodable {
    let name: String?
    let sortOrder: Int?
    let isArchived: Bool?
    let planCents: Int?
    let rollover: CategoryRollover?
    let paused: Bool?
    let parentId: Int?
}
```

From ios/BudgetPlanner/Networking/Endpoints/CategoriesV10API.swift:
```swift
enum CategoriesV10API {
    static func list(includeArchived: Bool = false) async throws -> [CategoryV10DTO]
    static func update(id: Int, payload: CategoryV10UpdateRequest) async throws -> CategoryV10DTO
}
```

Current ManagementItem.ID enum (existing — extend):
```swift
enum ID: String, Hashable {
    case analytics, subscriptions, template, accounts, categories, settings, access
    // 61-01 ADDS: planEditor (inserted BEFORE subscriptions in .all array)
}
```

Existing AccountsView already binds:
```swift
.navigationDestination(for: Int.self) { id in
    AccountDetailView(accountId: id)
}
```
→ В shared ManagementView NavigationStack `for: Int.self` уже занят AccountsView когда user находится на /accounts. Если бы PlanEditor использовал `for: Int.self` тоже, оба binding'а в shared stack привели бы к ambiguity для tap-row на /plan-editor. Решение — typed enum PlanEditorRoute.

61-01 определяет contract:
```swift
enum PlanEditorRoute: Hashable {
    case row(categoryId: Int)
}
```
→ 61-02 в PlanEditorView body: `.navigationDestination(for: PlanEditorRoute.self) { route in ... }`.

61-01 определяет PlanRowEditorViewModel onSaved closure contract:
```swift
var onSaved: ((CategoryV10DTO) -> Void)?
```
→ 61-02 в PlanEditorView push: `PlanRowEditorView(categoryId: id, onSaved: { updated in viewModel.applyOptimisticUpdate(updated) })`.
→ 61-03 в PlanRowEditorViewModel.save(): после successful CategoriesV10API.update → self.onSaved?(updated).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ManagementItem.planEditor в ManagementView</name>
  <files>ios/BudgetPlanner/Features/Management/ManagementView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Management/ManagementView.swift (full — extend ID enum + all array + destination switch)
  </read_first>
  <action>
    Modify `ManagementView.swift` (no other changes to file structure):

    1. **Extend ManagementItem.ID enum** (line ~134) — добавить case `planEditor`:
       ```swift
       enum ID: String, Hashable {
           case analytics, subscriptions, template, accounts, categories, settings, access, planEditor
       }
       ```

    2. **Insert ManagementItem entry в .all array** ПЕРЕД `.subscriptions` (per CONTEXT D-1):
       ```swift
       static let all: [ManagementItem] = [
           .init(id: .analytics, ... ),
           .init(id: .planEditor, label: "План месяца",
                 description: "Лимиты категорий и rollover",
                 icon: "slider.horizontal.3", ownerOnly: false),
           .init(id: .subscriptions, ... ),
           // ... rest unchanged
       ]
       ```

    3. **Extend destination(for:) switch** (line ~108) — добавить case `.planEditor`:
       ```swift
       @ViewBuilder
       private func destination(for id: ManagementItem.ID) -> some View {
           switch id {
           case .analytics: AnalyticsView()
           case .subscriptions: SubscriptionsView()
           case .template: TemplateView()
           case .accounts: AccountsView()
           case .categories: CategoriesView()
           case .settings: SettingsView()
           case .access: AccessView()
           case .planEditor: PlanEditorView()
           }
       }
       ```

    NO other modifications. Profile row / icon styling / EditButton — untouched.
  </action>
  <verify>
    <automated>grep -c "case planEditor" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 2 (ID enum + destination switch) -->
    <automated>grep -c "План месяца" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (label) -->
    <automated>grep -c "slider.horizontal.3" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "PlanEditorView()" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -v '^//' ios/BudgetPlanner/Features/Management/ManagementView.swift | grep -c "case .planEditor"</automated>
    <!-- Expected: 1 (destination switch — case in enum decl doesn't have leading dot) -->
  </verify>
  <acceptance_criteria>
    - ManagementItem.ID has case planEditor.
    - ManagementItem.all contains planEditor entry inserted BEFORE subscriptions.
    - destination(for:) returns PlanEditorView() для .planEditor.
    - No other modifications к existing rows.
  </acceptance_criteria>
  <done>
    All grep gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PlanEditorRoute + PlanEditorData scaffold files</name>
  <files>ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift, ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift</files>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/Plan/PlanData.swift (pattern для pure-compute enum — Foundation only, no SwiftUI)
    - ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift (DTO surface)
    - ios/BudgetPlanner/Features/Accounts/AccountsView.swift (line 59 — confirm Int.self уже занят в shared stack)
  </read_first>
  <action>
    Создать каталог `ios/BudgetPlanner/Features/PlanEditor/` и в нём 2 файла.

    **File 1: `PlanEditorRoute.swift`** — typed-route enum для NavigationStack disambiguation:
    ```swift
    import Foundation

    /// Phase 61 — typed-route enum для PlanEditor NavigationLinks.
    ///
    /// Используется вместо `Int` чтобы избежать collision с
    /// `AccountsView.navigationDestination(for: Int.self)` в shared
    /// ManagementView NavigationStack. Когда user находится на /accounts,
    /// Int-binding уже занят AccountDetailView push'ем — поэтому PlanEditor
    /// использует свой typed enum.
    ///
    /// Hashable conformance обязательна для NavigationLink(value:) API.
    enum PlanEditorRoute: Hashable {
        /// Push на детальный editor для одной категории.
        case row(categoryId: Int)
    }
    ```

    **File 2: `PlanEditorData.swift`** — pure-compute helpers (signatures only; bodies в 61-02). Mirrors FeaturesV10/Plan/PlanData.swift pattern (enum, no SwiftUI imports):
    ```swift
    // Phase 61: PlanEditorData — pure-compute helpers для PlanEditor master view.
    //
    // Symmetric to FeaturesV10/Plan/PlanData.swift (reusable где collision-free).
    // Все helpers — stateless static functions на `enum PlanEditorData`. No SwiftUI
    // imports, no state — unit-testable cheaply (PlanEditorDataTests in 61-04).
    //
    // Bodies заполняются в 61-02 (Task 1 — extend signatures with implementations
    // before PlanEditorView body construction). Scaffold gives 61-02 / 61-03 stable
    // contracts to import.

    import Foundation

    enum PlanEditorData {
        // MARK: - Surplus

        /// Остаток к распределению: incomeCents − Σ(planCents) over non-paused
        /// expense categories (income категории не вычитаются — они приносят
        /// дополнительный план-доход к месячному total).
        /// Body заполняется в 61-02.
        static func computeSurplus(
            incomeCents: Int,
            categories: [CategoryV10DTO]
        ) -> Int {
            // 61-02: реализация
            return 0
        }

        // MARK: - Display sorting

        /// Отдаёт две сортированные группы для master view:
        /// - expense categories (kind == .expense), sorted by `ord ?? "99"` ASC
        ///   с tie-break по name; paused-категории идут в конец каждой группы.
        /// - income categories (kind == .income), sorted same.
        /// Excludes archived categories (isArchived == true).
        /// Body заполняется в 61-02.
        static func sortCategoriesForDisplay(
            _ categories: [CategoryV10DTO]
        ) -> (expense: [CategoryV10DTO], income: [CategoryV10DTO]) {
            // 61-02: реализация
            return (expense: [], income: [])
        }

        // MARK: - Fact lookup

        /// Сумма фактических трат на категорию в текущем периоде
        /// (`actuals` filtered by kind == .expense ИЛИ .income в зависимости
        /// от категории; используется `abs(amountCents)`).
        /// Body заполняется в 61-02.
        static func factCentsByCategory(
            _ actuals: [ActualV10DTO],
            categoryId: Int
        ) -> Int {
            // 61-02: реализация
            return 0
        }

        // MARK: - Rollover aggregates

        /// Aggregated leftover by rollover destination (`.misc` / `.savings`)
        /// для master Aggregates section. Excludes paused / archived / savings-code.
        /// Body заполняется в 61-02 (можно reuse FeaturesV10/Plan/PlanData
        /// если no name-collision — иначе reimplement здесь).
        struct RolloverAggregates: Equatable {
            let miscCents: Int
            let savingsCents: Int
        }

        static func computeRolloverAggregates(
            categories: [CategoryV10DTO],
            actuals: [ActualV10DTO]
        ) -> RolloverAggregates {
            // 61-02: реализация
            return RolloverAggregates(miscCents: 0, savingsCents: 0)
        }

        // MARK: - Optimistic update

        /// Заменяет CategoryV10DTO в списке по id (для post-save optimistic
        /// refresh из child editor). Returns new array (immutable mutation).
        /// Body заполняется в 61-02.
        static func applyOptimisticUpdate(
            _ categories: [CategoryV10DTO],
            updated: CategoryV10DTO
        ) -> [CategoryV10DTO] {
            // 61-02: реализация
            return categories
        }
    }
    ```
  </action>
  <verify>
    <automated>test -d ios/BudgetPlanner/Features/PlanEditor</automated>
    <automated>test -f ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift</automated>
    <automated>test -f ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift</automated>
    <automated>grep -c "enum PlanEditorRoute" ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "case row(categoryId:" ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "enum PlanEditorData" ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "computeSurplus\|sortCategoriesForDisplay\|factCentsByCategory\|computeRolloverAggregates\|applyOptimisticUpdate" ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift</automated>
    <!-- Expected: ≥5 -->
    <automated>grep -c "import SwiftUI" ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift</automated>
    <!-- Expected: 0 (pure compute — no SwiftUI) -->
  </verify>
  <acceptance_criteria>
    - PlanEditorRoute enum с Hashable conformance + case row(categoryId: Int).
    - PlanEditorData enum scaffold с 5 helper signatures (return stub values).
    - No SwiftUI imports в PlanEditorData.swift.
  </acceptance_criteria>
  <done>
    All grep gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Scaffold PlanEditorView + PlanEditorViewModel</name>
  <files>ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift, ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountsView.swift (line 24-90 — body scaffold pattern)
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (line 23-80 — @Observable VM surface pattern)
    - ios/BudgetPlanner/FeaturesV10/Plan/PlanViewModel.swift (V10 pattern — load fields)
  </read_first>
  <action>
    **File 3: `PlanEditorView.swift`** — Scaffold (body заполняется в 61-02):
    ```swift
    import SwiftUI

    /// Phase 61: PlanEditorView — master list editor месячного плана.
    ///
    /// Master в master-detail flow (CONTEXT D-2). NavigationStack принадлежит
    /// родителю ManagementView — здесь только `.navigationTitle("План месяца")`
    /// + `.navigationDestination(for: PlanEditorRoute.self)` (61-02 wiring).
    ///
    /// Scaffold (61-01): пустой body. Реализация body — в 61-02 Task 2 (Hero +
    /// Aggregates + Expense Section + Income Section + tap-to-detail).
    struct PlanEditorView: View {
        @State private var viewModel = PlanEditorViewModel()

        var body: some View {
            // 61-02: реализация (List + switch на status + Hero + Aggregates + Categories)
            Text("PlanEditorView — body в 61-02")
                .navigationTitle("План месяца")
                .task { await viewModel.load() }
        }
    }
    ```

    **File 4: `PlanEditorViewModel.swift`** — Scaffold с surface:
    ```swift
    import Foundation
    import Observation

    /// Phase 61: PlanEditorViewModel — master view ViewModel.
    ///
    /// `load()`:
    ///   - parallel fetch CategoriesV10API.list() + MeV10API.fetchMeV10() (incomeCents);
    ///   - PeriodsAPI.current() с graceful 404 → period = nil, actuals = [];
    ///   - if period есть — ActualV10API.list(periodId: per.id) для fact compute.
    ///
    /// Pattern parallel AccountsViewModel + PlanViewModel:
    ///   - @Observable + private(set) properties + status enum
    ///   - inFlight guard для re-entrancy (T-61-02)
    ///   - Europe/Moscow Calendar для day-based context (если потребуется)
    ///   - filtered Russian copy на failure (T-61-03)
    ///
    /// Scaffold (61-01): load() — empty. Реализация в 61-02 Task 1.
    @MainActor
    @Observable
    final class PlanEditorViewModel {
        enum Status: Equatable {
            case idle
            case loading
            case ready
            case error(String)
        }

        private(set) var status: Status = .idle
        private(set) var categories: [CategoryV10DTO] = []
        private(set) var actuals: [ActualV10DTO] = []
        private(set) var period: PeriodDTO?
        private(set) var incomeCents: Int = 0

        @ObservationIgnored
        private var inFlight: Bool = false

        @ObservationIgnored
        var calendar: Calendar = PlanEditorViewModel.defaultCalendar()

        private static func defaultCalendar() -> Calendar {
            var c = Calendar(identifier: .gregorian)
            c.timeZone = TimeZone(identifier: "Europe/Moscow") ?? .current
            return c
        }

        init() {}

        // MARK: - Load

        /// 61-02: реализация parallel fetch + period-404 grace + filtered copy.
        func load() async {
            // 61-02: заполняем
        }

        // MARK: - Optimistic update (called from PlanRowEditorViewModel.onSaved)

        /// 61-02: реализация — replace category в self.categories по id.
        func applyOptimisticUpdate(_ updated: CategoryV10DTO) {
            // 61-02: заполняем
        }

        // MARK: - DEBUG test backdoor

        #if DEBUG
        func _setStateForTesting(
            categories: [CategoryV10DTO] = [],
            actuals: [ActualV10DTO] = [],
            period: PeriodDTO? = nil,
            incomeCents: Int = 0
        ) {
            self.categories = categories
            self.actuals = actuals
            self.period = period
            self.incomeCents = incomeCents
        }
        #endif
    }
    ```
  </action>
  <verify>
    <automated>test -f ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift</automated>
    <automated>test -f ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <automated>grep -c "struct PlanEditorView" ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "PlanEditorViewModel()" ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "navigationTitle(\"План месяца\")" ios/BudgetPlanner/Features/PlanEditor/PlanEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "final class PlanEditorViewModel" ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "@Observable" ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "func load() async" ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "func applyOptimisticUpdate" ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "_setStateForTesting" ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c 'TimeZone(identifier: "Europe/Moscow")' ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
  </verify>
  <acceptance_criteria>
    - PlanEditorView scaffold с body containing navigationTitle + .task hook.
    - PlanEditorViewModel scaffold с Status enum, 4 private(set) properties, inFlight, calendar, init, empty load(), empty applyOptimisticUpdate, DEBUG backdoor.
  </acceptance_criteria>
  <done>
    All grep gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Scaffold PlanRowEditorView + PlanRowEditorViewModel</name>
  <files>ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift, ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift (init pattern с accountId + State VM)
    - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (Status enum + init с id + load surface)
  </read_first>
  <action>
    **File 5: `PlanRowEditorView.swift`** — Scaffold:
    ```swift
    import SwiftUI

    /// Phase 61: PlanRowEditorView — detail editor для одной категории.
    ///
    /// Detail в master-detail flow (CONTEXT D-3). Form с Stepper + TextField
    /// (.decimalPad) + Picker (rollover) + Toggle (paused).
    ///
    /// `onSaved` closure инжектируется родителем PlanEditorView и вызывается
    /// PlanRowEditorViewModel после successful CategoriesV10API.update для
    /// optimistic-refresh master list без full reload.
    ///
    /// Scaffold (61-01): пустой Form body. Реализация — в 61-03 Task 2.
    struct PlanRowEditorView: View {
        let categoryId: Int
        let onSaved: (CategoryV10DTO) -> Void

        @State private var viewModel: PlanRowEditorViewModel

        init(categoryId: Int, onSaved: @escaping (CategoryV10DTO) -> Void) {
            self.categoryId = categoryId
            self.onSaved = onSaved
            self._viewModel = State(
                wrappedValue: PlanRowEditorViewModel(categoryId: categoryId)
            )
        }

        var body: some View {
            // 61-03: Form реализация (Stepper + TextField + Picker + Toggle + banner)
            Text("PlanRowEditorView — body в 61-03")
                .navigationTitle("Категория")
                .navigationBarTitleDisplayMode(.inline)
                .onAppear {
                    viewModel.onSaved = onSaved
                }
                .task { await viewModel.load() }
        }
    }
    ```

    **File 6: `PlanRowEditorViewModel.swift`** — Scaffold с onSaved contract:
    ```swift
    import Foundation
    import Observation

    /// Phase 61: PlanRowEditorViewModel — detail editor ViewModel.
    ///
    /// `load()`:
    ///   - CategoriesV10API.list() → find by categoryId (cross-tenant guard
    ///     T-61-03: missing id → status = .error("Категория не найдена")).
    ///   - Seed local editing state (planCents / rollover / paused) из загруженной DTO.
    ///
    /// `save()`:
    ///   - CategoriesV10API.update(id:, payload: CategoryV10UpdateRequest(...))
    ///     immediate per-row save (CONTEXT D-4 — NO PlanMonthAPI batch).
    ///   - On success: self.onSaved?(updated) → parent VM optimistic refresh.
    ///   - On failure: saveError = filtered Russian copy (T-61-03).
    ///
    /// `onSaved` closure инжектируется родительским View — это **interface
    /// contract зафиксирован в 61-01** для parallel 61-02 / 61-03 work.
    ///
    /// Threat-model:
    ///   - T-61-01 (Tampering planCents): UI gate planCents ≥ 0; backend
    ///     CategoryV10UpdateRequest Pydantic validation; server overflow guard.
    ///   - T-61-02 (Concurrency multiple saves): `inFlight` guard.
    ///   - T-61-03 (Info disclosure): filtered Russian copy banner; NO
    ///     `error.localizedDescription` в UI.
    ///
    /// Scaffold (61-01): load()/save() empty. Реализация в 61-03 Task 1.
    @MainActor
    @Observable
    final class PlanRowEditorViewModel {
        enum Status: Equatable {
            case idle
            case loading
            case ready
            case error(String)
        }

        let categoryId: Int

        private(set) var status: Status = .idle
        private(set) var category: CategoryV10DTO?

        /// Local editing state — seeded из category на load; mutated UI bindings.
        var planCents: Int = 0
        var rollover: CategoryRollover = .misc
        var paused: Bool = false

        /// True пока CategoriesV10API.update в flight (disables CTA — T-61-02).
        private(set) var submitting: Bool = false

        /// Inline banner copy на save failure (T-61-03 filtered).
        var saveError: String? = nil

        /// Closure, инжектируемая родительским View. PlanRowEditorViewModel.save()
        /// вызывает её после successful PATCH /categories/:id для optimistic-refresh
        /// master list (61-02 wiring).
        var onSaved: ((CategoryV10DTO) -> Void)?

        @ObservationIgnored
        private var inFlight: Bool = false

        init(categoryId: Int) {
            self.categoryId = categoryId
        }

        // MARK: - Load

        /// 61-03: реализация — fetch CategoriesV10API.list() + find + seed
        /// editing state.
        func load() async {
            // 61-03: заполняем
        }

        // MARK: - Save

        /// 61-03: реализация — PATCH /categories/{id} → on success вызывает
        /// onSaved closure + dismiss caller (через @Environment dismiss);
        /// on failure — set saveError filtered Russian copy.
        func save() async -> Bool {
            // 61-03: заполняем
            return false
        }

        // MARK: - Dirty check

        /// True когда планируемое сохранение изменит хотя бы одно поле
        /// (planCents / rollover / paused). Disabled CTA когда !isDirty (D-3).
        /// 61-03: реализация.
        var isDirty: Bool {
            // 61-03: заполняем
            return false
        }

        // MARK: - DEBUG test backdoor

        #if DEBUG
        func _setStateForTesting(
            category: CategoryV10DTO? = nil,
            planCents: Int = 0,
            rollover: CategoryRollover = .misc,
            paused: Bool = false
        ) {
            self.category = category
            self.planCents = planCents
            self.rollover = rollover
            self.paused = paused
        }
        #endif
    }
    ```
  </action>
  <verify>
    <automated>test -f ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <automated>test -f ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <automated>grep -c "struct PlanRowEditorView" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "onSaved: @escaping (CategoryV10DTO) -> Void" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "final class PlanRowEditorViewModel" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "var onSaved: ((CategoryV10DTO) -> Void)?" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "func save() async -> Bool" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "func load() async" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "var isDirty: Bool" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "_setStateForTesting" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "T-61-01\|T-61-02\|T-61-03" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: ≥3 -->
  </verify>
  <acceptance_criteria>
    - PlanRowEditorView scaffold с let categoryId + let onSaved closure + init + State VM + body containing onAppear hook to wire onSaved into VM.
    - PlanRowEditorViewModel scaffold с Status, properties для editing state, onSaved closure (var, not let), inFlight guard, empty load()/save()/isDirty, DEBUG backdoor.
    - Threat IDs referenced in docstring (T-61-01/02/03).
  </acceptance_criteria>
  <done>
    All grep gates pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Build verification + coexistence grep gate</name>
  <files></files>
  <read_first>
    - (none — verification only)
  </read_first>
  <action>
    Run xcodegen (capture new files в project) + xcodebuild для smoke build clean. Затем verify coexistence — FeaturesV10/Plan/* и smoke-critical файлы UNTOUCHED.

    1. `cd ios && xcodegen generate` — добавить новые файлы в .xcodeproj.
    2. `cd ios && make build` — должен SUCCEEDED.
    3. Verify coexistence (negative grep gates).
  </action>
  <verify>
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && echo OK</automated>
    <!-- Expected: OK -->
    <automated>cd ios && make build 2>&1 | tail -10 | grep -cE "BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
    <automated>git diff HEAD --name-only -- ios/BudgetPlanner/FeaturesV10/Plan/PlanView.swift ios/BudgetPlanner/FeaturesV10/Plan/PlanViewModel.swift ios/BudgetPlanner/FeaturesV10/Plan/PlanData.swift ios/BudgetPlanner/Features/Onboarding/ ios/BudgetPlanner/Features/Management/TemplateView.swift ios/BudgetPlanner/Features/Accounts/AccountsView.swift ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift 2>&1 | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 (coexistence — none of V10 Plan, Onboarding, TemplateView, Accounts touched в этой фазе) -->
    <automated>ls ios/BudgetPlanner/Features/PlanEditor/ 2>&1 | wc -l | tr -d ' '</automated>
    <!-- Expected: 6 (PlanEditorRoute, PlanEditorData, PlanEditorView, PlanEditorViewModel, PlanRowEditorView, PlanRowEditorViewModel) -->
  </verify>
  <acceptance_criteria>
    - xcodegen generate OK.
    - make build SUCCEEDED.
    - Coexistence verified: V10 Plan / Onboarding / Template / Accounts untouched.
    - Features/PlanEditor/ содержит 6 файлов.
  </acceptance_criteria>
  <done>
    All grep gates pass; build clean; coexistence verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app ↔ ManagementView shared NavigationStack | PlanEditorRoute enum избегает collision с AccountsView's `Int.self` binding (DOS gate avoidance — два конкурирующих `for: Int.self` в shared stack приводят к ambiguous push). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | PlanRowEditorViewModel surface (planCents bounds) | mitigate (in 61-03 — здесь only contract) | Scaffold устанавливает `var planCents: Int = 0`; 61-03 реализация ограничит UI Stepper 0...10_000_000 + TextField .decimalPad parse через MoneyParser (returns nil на invalid input). Backend Pydantic CategoryV10UpdateRequest валидирует planCents ≥ 0. |
| T-61-02 | DoS / Concurrency | PlanRowEditorViewModel.save() | mitigate (in 61-03 — здесь only contract) | Scaffold устанавливает `private var inFlight: Bool = false` + `private(set) var submitting: Bool`. 61-03 реализует guard. |
| T-61-03 | Information Disclosure | load() / save() catch blocks | mitigate (in 61-03 — здесь only contract) | Scaffold устанавливает `var saveError: String?` и documentation для catch блоков с filtered Russian copy. 61-03 verify `grep -c "error.localizedDescription" → 0`. |
</threat_model>

<verification>
- `grep -c "case planEditor" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 2.
- `grep -c "PlanEditorView()" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 1.
- `test -d ios/BudgetPlanner/Features/PlanEditor` passes.
- `ls ios/BudgetPlanner/Features/PlanEditor/` lists 6 files.
- `grep -c "enum PlanEditorRoute" ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift` returns 1.
- `grep -c "case row(categoryId:" ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift` returns 1.
- `grep -c "enum PlanEditorData" ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift` returns 1.
- `grep -c "var onSaved: ((CategoryV10DTO) -> Void)?" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift` returns 1.
- `cd ios && make build` returns SUCCEEDED.
- `git diff HEAD --name-only -- ios/BudgetPlanner/FeaturesV10/Plan/*` returns no output (coexistence).
</verification>

<success_criteria>
1. ManagementItem.planEditor row виден в Management list (icon slider.horizontal.3, label «План месяца»), inserted перед «Подписки».
2. Tap row → push PlanEditorView (scaffold body «PlanEditorView — body в 61-02» — placeholder).
3. Features/PlanEditor/ создан, содержит 6 файлов: PlanEditorRoute, PlanEditorData, PlanEditorView, PlanEditorViewModel, PlanRowEditorView, PlanRowEditorViewModel.
4. PlanEditorRoute enum с case row(categoryId: Int) + Hashable conformance.
5. PlanEditorData enum surface с 5 helper signatures (signatures only, bodies в 61-02).
6. PlanEditorViewModel scaffold с status, 4 private(set) properties, inFlight, Europe/Moscow calendar, empty load(), empty applyOptimisticUpdate, DEBUG backdoor.
7. PlanRowEditorViewModel scaffold с Status, editing state, onSaved closure (var), inFlight, empty load()/save()/isDirty, DEBUG backdoor.
8. PlanRowEditorView scaffold с init(categoryId:, onSaved:) + body containing .onAppear { viewModel.onSaved = onSaved }.
9. Build clean (cd ios && make build).
10. Coexistence: FeaturesV10/Plan/* + Onboarding + TemplateView + Accounts/* — untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/61-plan-editor-v06/61-01-SUMMARY.md` summarising:
- ManagementItem.planEditor wiring (position перед .subscriptions; D-1)
- Файлы созданы в Features/PlanEditor/ + их purpose
- PlanEditorRoute disambiguation rationale (avoid Int.self collision с AccountsView в shared stack)
- PlanRowEditorViewModel.onSaved contract зафиксирован для 61-02 / 61-03 parallel work
- Build clean status
- Coexistence guards verified
</output>
