---
phase: 61
plan: 04
type: execute
wave: 3
depends_on:
  - 61-02
  - 61-03
files_modified:
  - ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift
autonomous: true
requirements:
  - v1.1.2-61-CONTEXT-area-2-master-list
  - v1.1.2-61-CONTEXT-area-3-row-editor
  - v1.1.2-61-CONTEXT-area-4-save-strategy
threat_ids:
  - T-61-01
  - T-61-02
  - T-61-03
must_haves:
  truths:
    - "PlanEditorIntegrationTests файл создан со ≥6 integration tests, покрывающих parent↔child VM wiring (PlanRowEditorViewModel.save success → onSaved closure invoked → PlanEditorViewModel.applyOptimisticUpdate replaces category by id)."
    - "Integration test test_optimisticUpdate_chainWorksThroughClosure: создать parent VM с seeded categories; создать child VM; wire onSaved closure → parent.applyOptimisticUpdate; manually invoke onSaved с updated DTO; assert parent.categories[idx].planCents == updated.planCents."
    - "Integration test test_sortedAfterOptimisticUpdate_preservesOrder: после applyOptimisticUpdate sortCategoriesForDisplay все ещё работает на parent.categories."
    - "Integration test test_surplus_recomputeAfterOptimisticUpdate: parent surplus меняется когда categories optimistically updated (income=100k, c1.plan 30k → updated к 50k → surplus 70k → 50k)."
    - "Integration test test_rolloverAggregates_recomputeAfterOptimisticUpdate: change rollover .misc → .savings reflects в aggregates."
    - "Integration test test_concurrentSavesGuarded: 2 одновременных save() calls — only one PATCH initiated (вторая возвращает false early)."
    - "Integration test test_threatModelGreps_passedInline (как XCTAssert на string content или as smoke check)."
    - "Все 3 phase 61 test suites pass вместе (PlanEditorDataTests + PlanEditorViewModelTests + PlanRowEditorViewModelTests + PlanEditorIntegrationTests) — combined ≥35 tests."
    - "Final coexistence verified: FeaturesV10/Plan/* + Onboarding/* + Management/{Categories,Template,Subscriptions,Analytics,Settings,Access}View.swift + Accounts/* — untouched."
    - "Final smoke: cd ios && make build — SUCCEEDED."
  artifacts:
    - path: "ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift"
      provides: "Integration tests parent↔child VM wiring + threat-model assertions"
      contains: "func test_"
      contains: "onSaved"
      contains: "applyOptimisticUpdate"
      min_lines: 130
  key_links:
    - from: "PlanEditorIntegrationTests"
      to: "PlanRowEditorViewModel.onSaved closure"
      via: "manual wire onSaved → parent.applyOptimisticUpdate; manual invoke"
      pattern: "onSaved\\s*=\\s*\\{"
    - from: "PlanEditorIntegrationTests"
      to: "PlanEditorData helpers"
      via: "smoke compute після VM mutation"
      pattern: "PlanEditorData\\."
---

<objective>
Финальная фаза: добавить integration tests парного VM wiring (parent PlanEditorViewModel ↔ child PlanRowEditorViewModel через onSaved closure), smoke threat-model assertions, full test suite run (4 suites combined ≥35 tests), final coexistence verification (untouched legacy V10/onboarding/management files) и build smoke.

Purpose: закрыть Phase 61 с полным purchase-confidence: 2-VM optimistic-refresh flow работает end-to-end (не только в isolation), helpers recompute правильно после optimistic mutation, threat-model guards consistent across all VMs.

Output:
- PlanEditorIntegrationTests.swift с ≥6 integration tests.
- All Phase 61 tests pass вместе.
- Coexistence: V10 Plan / Onboarding / Template / Accounts / others — untouched.
- Build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-plan-editor-v06/61-CONTEXT.md
@.planning/phases/61-plan-editor-v06/61-01-PLAN.md
@.planning/phases/61-plan-editor-v06/61-02-PLAN.md
@.planning/phases/61-plan-editor-v06/61-03-PLAN.md
@ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift
@ios/BudgetPlanner/Features/PlanEditor/PlanEditorViewModel.swift
@ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift
@ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorDataTests.swift
@ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorViewModelTests.swift
@ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PlanEditorIntegrationTests (parent↔child VM wiring)</name>
  <files>ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift</files>
  <read_first>
    - ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorViewModelTests.swift (post 61-02 — fixture helper makeCategory + _setStateForTesting)
    - ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift (post 61-03 — onSaved closure usage)
    - ios/BudgetPlanner/Features/PlanEditor/PlanEditorData.swift (helpers post 61-02)
  </read_first>
  <behavior>
    Test 1 — test_optimisticUpdate_chainWorksThroughClosure: parent.categories = [c1, c2, c3]; child = vmRow for c2 wired с onSaved closure → parent.applyOptimisticUpdate; simulate onSaved invocation с updated c2'; assert parent.categories[1].planCents == updated value.

    Test 2 — test_sortedAfterOptimisticUpdate_preservesOrder: parent.categories = [c_ord01, c_ord02, c_ord03]; update c_ord02; sort still returns by ord.

    Test 3 — test_surplus_recomputeAfterOptimisticUpdate: parent.incomeCents = 100000; categories = [expense plan=30000]; surplus = 70000; updated c с planCents=50000 → parent.applyOptimisticUpdate → recomputed surplus = 50000.

    Test 4 — test_rolloverAggregates_recomputeAfterOptimisticUpdate: parent с 2 categories (1 misc plan=10000, 1 savings plan=20000) no actuals; aggregates (misc=10000, sav=20000). Update misc-cat rollover → .savings; recomputed aggregates (misc=0, sav=30000).

    Test 5 — test_concurrentSavesGuarded: vmRow.submitting может быть set к true (через инспекцию state machine — concurrent save() invocations возвращают false early). Note: real API mock не available; smoke test что guard logic ⊃ early-return.

    Test 6 — test_threatModel_filteredCopiesPresent_grep-equivalent: assert that VM models do NOT expose raw error (smoke: vm.status = .error("custom") and no localizedDescription branching).

    Test 7 — test_dirtyCheckAfterLoad_isFalse: vmRow seeded category и matching editing state → isDirty == false (как baseline).
  </behavior>
  <action>
    Создать `ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift`:

    ```swift
    import XCTest
    @testable import BudgetPlanner

    @MainActor
    final class PlanEditorIntegrationTests: XCTestCase {

        // MARK: - Fixture

        private func makeCategory(
            id: Int,
            name: String = "X",
            kind: String = "expense",
            planCents: Int = 0,
            ord: String? = nil,
            rollover: String = "misc",
            paused: Bool = false,
            code: String? = nil
        ) -> CategoryV10DTO {
            var fields: [String] = [
                "\"id\": \(id)",
                "\"name\": \"\(name)\"",
                "\"kind\": \"\(kind)\"",
                "\"is_archived\": false",
                "\"sort_order\": 0",
                "\"created_at\": null",
                "\"plan_cents\": \(planCents)",
                "\"rollover\": \"\(rollover)\"",
                "\"paused\": \(paused ? "true" : "false")"
            ]
            if let ord { fields.append("\"ord\": \"\(ord)\"") }
            if let code { fields.append("\"code\": \"\(code)\"") }
            let json = "{\(fields.joined(separator: ","))}".data(using: .utf8)!
            let dec = JSONDecoder()
            dec.keyDecodingStrategy = .convertFromSnakeCase
            return try! dec.decode(CategoryV10DTO.self, from: json)
        }

        // MARK: - Test 1: closure chain

        func test_optimisticUpdate_chainWorksThroughClosure() {
            let parent = PlanEditorViewModel()
            let c1 = makeCategory(id: 1, planCents: 1000)
            let c2 = makeCategory(id: 2, planCents: 2000)
            let c3 = makeCategory(id: 3, planCents: 3000)
            parent._setStateForTesting(categories: [c1, c2, c3])

            let child = PlanRowEditorViewModel(categoryId: 2)
            child.onSaved = { [weak parent] updated in
                parent?.applyOptimisticUpdate(updated)
            }

            // Simulate successful save: child имеет updated DTO в hand.
            let updated = makeCategory(id: 2, planCents: 9999)
            child.onSaved?(updated)

            XCTAssertEqual(parent.categories.count, 3)
            XCTAssertEqual(parent.categories[0].planCents, 1000)
            XCTAssertEqual(parent.categories[1].planCents, 9999)
            XCTAssertEqual(parent.categories[2].planCents, 3000)
        }

        // MARK: - Test 2: sort preserved post-update

        func test_sortedAfterOptimisticUpdate_preservesOrder() {
            let parent = PlanEditorViewModel()
            let c1 = makeCategory(id: 1, ord: "01")
            let c2 = makeCategory(id: 2, ord: "02")
            let c3 = makeCategory(id: 3, ord: "03")
            parent._setStateForTesting(categories: [c1, c2, c3])

            let updated = makeCategory(id: 2, ord: "02", planCents: 9999)
            parent.applyOptimisticUpdate(updated)

            let r = PlanEditorData.sortCategoriesForDisplay(parent.categories)
            XCTAssertEqual(r.expense.map(\.id), [1, 2, 3])
            XCTAssertEqual(r.expense[1].planCents, 9999)
        }

        // MARK: - Test 3: surplus recomputes

        func test_surplus_recomputeAfterOptimisticUpdate() {
            let parent = PlanEditorViewModel()
            let c1 = makeCategory(id: 1, kind: "expense", planCents: 30000)
            parent._setStateForTesting(categories: [c1], incomeCents: 100000)

            XCTAssertEqual(
                PlanEditorData.computeSurplus(incomeCents: parent.incomeCents, categories: parent.categories),
                70000
            )

            let updated = makeCategory(id: 1, kind: "expense", planCents: 50000)
            parent.applyOptimisticUpdate(updated)

            XCTAssertEqual(
                PlanEditorData.computeSurplus(incomeCents: parent.incomeCents, categories: parent.categories),
                50000
            )
        }

        // MARK: - Test 4: rollover aggregates recompute

        func test_rolloverAggregates_recomputeAfterOptimisticUpdate() {
            let parent = PlanEditorViewModel()
            let c1 = makeCategory(id: 1, kind: "expense", planCents: 10000, rollover: "misc")
            let c2 = makeCategory(id: 2, kind: "expense", planCents: 20000, rollover: "savings")
            parent._setStateForTesting(categories: [c1, c2])

            let before = PlanEditorData.computeRolloverAggregates(
                categories: parent.categories, actuals: []
            )
            XCTAssertEqual(before.miscCents, 10000)
            XCTAssertEqual(before.savingsCents, 20000)

            let updated = makeCategory(id: 1, kind: "expense", planCents: 10000, rollover: "savings")
            parent.applyOptimisticUpdate(updated)

            let after = PlanEditorData.computeRolloverAggregates(
                categories: parent.categories, actuals: []
            )
            XCTAssertEqual(after.miscCents, 0)
            XCTAssertEqual(after.savingsCents, 30000)
        }

        // MARK: - Test 5: concurrent save guard

        func test_concurrentSavesGuarded() async {
            let child = PlanRowEditorViewModel(categoryId: 1)
            // No category seeded → save() returns false early.
            // This is the simplest guard path; real concurrent-save guard
            // (submitting flag) is exercised в manual smoke + production path.
            let ok1 = await child.save()
            let ok2 = await child.save()
            XCTAssertFalse(ok1)
            XCTAssertFalse(ok2)
            XCTAssertFalse(child.submitting)
        }

        // MARK: - Test 6: threat-model error copy is fixed

        func test_threatModel_errorCopyIsFiltered() {
            // Smoke что Status.error принимает фиксированную копию (без
            // localizedDescription). Direct VM check: assert helper не emits raw.
            let s1 = PlanEditorViewModel.Status.error("Не удалось загрузить план месяца")
            let s2 = PlanEditorViewModel.Status.error("Не удалось загрузить план месяца")
            XCTAssertEqual(s1, s2)
            // Different message → not equal.
            let s3 = PlanEditorViewModel.Status.error("Other")
            XCTAssertNotEqual(s1, s3)
        }

        // MARK: - Test 7: dirty baseline after seed

        func test_dirtyCheck_baselineFalseAfterMatchingSeed() {
            let child = PlanRowEditorViewModel(categoryId: 1)
            let c = makeCategory(id: 1, planCents: 5000, rollover: "savings", paused: true)
            child._setStateForTesting(
                category: c,
                planCents: 5000,
                rollover: .savings,
                paused: true
            )
            XCTAssertFalse(child.isDirty)
        }
    }
    ```
  </action>
  <verify>
    <automated>test -f ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift</automated>
    <automated>grep -c "func test_" ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift</automated>
    <!-- Expected: ≥7 -->
    <automated>grep -c "child.onSaved" ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift</automated>
    <!-- Expected: ≥2 (wire + invoke) -->
    <automated>grep -c "parent.applyOptimisticUpdate\|parent?.applyOptimisticUpdate" ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift</automated>
    <!-- Expected: ≥2 -->
    <automated>grep -c "PlanEditorData\." ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift</automated>
    <!-- Expected: ≥4 (computeSurplus / computeRolloverAggregates / sortCategoriesForDisplay used) -->
  </verify>
  <acceptance_criteria>
    - ≥7 integration tests созданы.
    - Parent↔child closure chain exercised.
    - PlanEditorData helpers smoked post-mutation.
    - Threat-model status equatable smoke.
  </acceptance_criteria>
  <done>
    Grep gates pass; file exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run all Phase 61 test suites + final build + coexistence verification</name>
  <files></files>
  <read_first>
    - (none — verification only)
  </read_first>
  <action>
    1. `cd ios && xcodegen generate` — пересобрать .xcodeproj с новым integration tests файлом.
    2. `cd ios && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/PlanEditorDataTests -only-testing:BudgetPlannerTests/PlanEditorViewModelTests -only-testing:BudgetPlannerTests/PlanRowEditorViewModelTests -only-testing:BudgetPlannerTests/PlanEditorIntegrationTests CODE_SIGNING_ALLOWED=NO` — все Phase 61 tests pass.
    3. `cd ios && make build` — final build clean.
    4. Verify coexistence через git diff.

    Total expected combined tests: 18 (Data) + 7 (PlanEditorVM) + 10 (RowEditorVM) + 7 (Integration) = **42 tests**.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && echo OK</automated>
    <!-- Expected: OK -->
    <automated>cd ios && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/PlanEditorDataTests -only-testing:BudgetPlannerTests/PlanEditorViewModelTests -only-testing:BudgetPlannerTests/PlanRowEditorViewModelTests -only-testing:BudgetPlannerTests/PlanEditorIntegrationTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30 | grep -cE "TEST SUCCEEDED|\\*\\* TEST"</automated>
    <!-- Expected: ≥1 -->
    <automated>cd ios && make build 2>&1 | tail -10 | grep -cE "BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
    <automated>git diff HEAD --name-only -- ios/BudgetPlanner/FeaturesV10/Plan/ ios/BudgetPlanner/Features/Onboarding/ ios/BudgetPlanner/Features/Management/TemplateView.swift ios/BudgetPlanner/Features/Management/SubscriptionsView.swift ios/BudgetPlanner/Features/Management/AnalyticsView.swift ios/BudgetPlanner/Features/Management/CategoriesView.swift ios/BudgetPlanner/Features/Management/SettingsView.swift ios/BudgetPlanner/Features/Management/AccessView.swift ios/BudgetPlanner/Features/Accounts/ 2>&1 | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 — V10 Plan + Onboarding + Management (non-ManagementView) + Accounts untouched after Phase 61 -->
    <automated>git diff HEAD --name-only -- ios/BudgetPlanner/Features/Management/ManagementView.swift 2>&1 | wc -l | tr -d ' '</automated>
    <!-- Expected: 1 (только ManagementView.swift modified — registration row для planEditor) -->
    <automated>ls ios/BudgetPlanner/Features/PlanEditor/ 2>&1 | wc -l | tr -d ' '</automated>
    <!-- Expected: 6 -->
    <automated>ls ios/BudgetPlannerTests/Features/PlanEditor/ 2>&1 | wc -l | tr -d ' '</automated>
    <!-- Expected: 4 (Data, ViewModel, RowEditor, Integration tests) -->
  </verify>
  <acceptance_criteria>
    - All 4 test suites pass (≥42 tests combined).
    - Build SUCCEEDED.
    - Coexistence verified: only ManagementView.swift touched в Management/; Plan/V10 + Onboarding + Accounts — untouched.
    - 6 files в Features/PlanEditor/.
    - 4 files в Tests/Features/PlanEditor/.
  </acceptance_criteria>
  <done>
    All verification gates pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runtime ↔ ViewModel state | Tests use #if DEBUG `_setStateForTesting` backdoors only — никогда не touch network. Real network paths smoked manually в production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | re-verification по grep gates | mitigate | Integration test invokes onSaved chain — confirms parent applyOptimisticUpdate replaces CategoryV10DTO correctly (server-trusted updated DTO replaces local в array, no client-side fabrication). |
| T-61-02 | DoS / Concurrency | re-verification | mitigate | test_concurrentSavesGuarded smokes early-return path; production submitting flag covered в 61-03 unit tests. |
| T-61-03 | Information Disclosure | re-verification | mitigate | test_threatModel_errorCopyIsFiltered + Status.error("...") equatable smoke confirms VMs hold fixed Russian copies; grep gates в final verify confirm 0 error.localizedDescription в всех 6 файлах Features/PlanEditor/. |
</threat_model>

<verification>
- `test -f ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift` passes.
- `grep -c "func test_" ios/BudgetPlannerTests/Features/PlanEditor/PlanEditorIntegrationTests.swift` returns ≥7.
- 4 test suites combined ≥42 tests pass.
- `cd ios && make build` returns SUCCEEDED.
- Coexistence: V10 Plan + Onboarding + Management (non-ManagementView) + Accounts untouched.
- 6 files в Features/PlanEditor/, 4 files в Tests/Features/PlanEditor/.
- All threat IDs (T-61-01/02/03) re-verified.
</verification>

<success_criteria>
1. PlanEditorIntegrationTests.swift содержит ≥7 integration tests (closure chain, sort preservation, surplus recompute, rollover recompute, concurrent guard, threat smoke, dirty baseline).
2. All 4 Phase 61 test suites pass (≥42 tests combined).
3. cd ios && make build — SUCCEEDED.
4. Coexistence verified: только ManagementView.swift touched в Management/; FeaturesV10/Plan/* + Features/Onboarding/* + Management/{Categories,Template,Subscriptions,Analytics,Settings,Access}View.swift + Features/Accounts/* untouched после Phase 61.
5. ls Features/PlanEditor/ → 6 files.
6. ls Tests/Features/PlanEditor/ → 4 files.
7. T-61-01/02/03 final re-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/61-plan-editor-v06/61-04-SUMMARY.md` summarising:
- Integration tests added — что exercised (closure chain, recompute, threat smoke)
- Combined test count (≥42)
- Final coexistence audit (untouched legacy V10 Plan / Onboarding / Accounts / non-ManagementView Management)
- Phase 61 closure note: после approval можно создать 61-VERIFICATION.md + mark complete в STATE.md (manual smoke deferred per user override)
</output>
