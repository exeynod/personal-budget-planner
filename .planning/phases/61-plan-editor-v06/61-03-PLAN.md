---
phase: 61
plan: 03
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift
  - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift
  - ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift
autonomous: true
requirements:
  - v1.1.2-61-CONTEXT-area-3-row-editor
  - v1.1.2-61-CONTEXT-area-4-save-strategy
threat_ids:
  - T-61-01
  - T-61-02
  - T-61-03
must_haves:
  truths:
    - "PlanRowEditorViewModel.load() через CategoriesV10API.list() + find(by id) (нет GET /categories/{id} в API surface)."
    - "PlanRowEditorViewModel.load() cross-tenant guard: missing id → status = .error(\"Категория не найдена\") (T-61-03 — single message без leak)."
    - "PlanRowEditorViewModel.load() seed editing state (planCents, rollover, paused) из найденной CategoryV10DTO."
    - "PlanRowEditorViewModel.save() через CategoriesV10API.update(id:, payload: CategoryV10UpdateRequest(planCents:, rollover:, paused:)) — immediate per-row save (CONTEXT D-4)."
    - "PlanRowEditorViewModel.save() on success: self.category = updated DTO; onSaved?(updated); returns true; saveError = nil."
    - "PlanRowEditorViewModel.save() on failure: saveError = \"Не удалось сохранить категорию\" (T-61-03 filtered); returns false; raw error через print() only."
    - "PlanRowEditorViewModel.save() inFlight guard через submitting flag (T-61-02 — no concurrent PATCHes); сразу set к true, defer reset."
    - "PlanRowEditorViewModel.isDirty true если планируемое сохранение поменяет хотя бы один field (planCents != category.planCents OR rollover != category.rollover OR paused != category.paused)."
    - "PlanRowEditorView Form содержит 3 секции: «Лимит» (Stepper +/- 500₽ step + TextField .decimalPad через MoneyParser); «Перенос остатка» (Picker .misc / .savings — segmented style); «Статус» (Toggle paused)."
    - "PlanRowEditorView Stepper UI gate planCents 0...10_000_000 (T-61-01 — UI bounds)."
    - "PlanRowEditorView toolbar Save button в .confirmationAction — disabled когда !isDirty || submitting."
    - "PlanRowEditorView показывает inline banner с saveError если non-nil (filtered copy, NO error.localizedDescription leak — T-61-03)."
    - "PlanRowEditorView Cancel confirmation: system back button presents .confirmationDialog «Отменить изменения?» когда isDirty (через ConfirmationDialog или alert)."
    - "PlanRowEditorView .task hook вызывает viewModel.load() и (через .onAppear) wire onSaved closure."
    - "PlanRowEditorViewModelTests ≥10 tests pass."
    - "Build clean; T-61-03 verified."
  artifacts:
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift"
      provides: "filled load() + save() + isDirty с full implementation"
      contains: "CategoriesV10API.list"
      contains: "CategoriesV10API.update"
      contains: "Категория не найдена"
      contains: "Не удалось сохранить категорию"
      contains: "CategoryV10UpdateRequest"
      contains_not: "error.localizedDescription"
      min_lines: 130
    - path: "ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift"
      provides: "filled Form body (Stepper + TextField + Picker + Toggle + banner + cancel-confirmation + Save toolbar)"
      contains: "Stepper"
      contains: "TextField"
      contains: "Picker"
      contains: "Toggle"
      contains: ".decimalPad"
      contains: ".confirmationAction"
      contains: "MoneyParser"
      min_lines: 130
    - path: "ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift"
      provides: "Unit tests для load + save + isDirty + state machine"
      contains: "func test_"
      min_lines: 130
  key_links:
    - from: "PlanRowEditorViewModel.save()"
      to: "CategoriesV10API.update(id:, payload:)"
      via: "PATCH /categories/{id} per-row immediate save"
      pattern: "CategoriesV10API\\.update\\(id:"
    - from: "PlanRowEditorViewModel.save() success path"
      to: "self.onSaved?(updated)"
      via: "callback to parent VM applyOptimisticUpdate"
      pattern: "onSaved\\?\\(updated\\)|onSaved\\?\\(.+\\)"
    - from: "PlanRowEditorView Stepper"
      to: "PlanRowEditorViewModel.planCents"
      via: "Binding<Int> rubles ↔ cents conversion"
      pattern: "viewModel\\.planCents"
    - from: "PlanRowEditorView TextField"
      to: "MoneyParser.parseToCents"
      via: ".decimalPad input parse"
      pattern: "MoneyParser\\.parseToCents"
---

<objective>
Заполнить PlanRowEditorViewModel.load() (CategoriesV10API.list + find by id + cross-tenant guard + seed editing state) и save() (CategoriesV10API.update per-row immediate save + onSaved callback + filtered banner copy + inFlight guard) и isDirty (3-field comparison). Полностью переписать PlanRowEditorView body (Form с 3 секциями: Лимит/Stepper+TextField, Перенос/Picker, Статус/Toggle; toolbar Save в .confirmationAction; banner на failure; cancel-confirmation). Добавить unit tests.

Purpose: tap по row в PlanEditorView → push в детальный editor — функциональная save flow: Stepper изменяет план, Save в toolbar (disabled пока !isDirty), на success parent master refreshes без full reload (через onSaved closure инжектированную в 61-02).

Output:
- PlanRowEditorViewModel.load() + save() + isDirty реализованы.
- PlanRowEditorView body заполнен (Form Stepper+TextField+Picker+Toggle + banner + cancel-confirmation).
- T-61-01 (planCents bounds 0...10_000_000), T-61-02 (inFlight guard), T-61-03 (filtered copy) mitigated.
- ≥10 unit tests pass.
- Build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/61-plan-editor-v06/61-CONTEXT.md
@.planning/phases/61-plan-editor-v06/61-01-PLAN.md
@.planning/phases/60-accounts-v06/60-04-SUMMARY.md
@ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift
@ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
@ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep3PlanView.swift
@ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift
@ios/BudgetPlanner/Networking/Endpoints/CategoriesV10API.swift
@ios/BudgetPlanner/Domain/MoneyFormatter.swift
@ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift

<interfaces>
<!-- Production contracts. -->

CategoriesV10API (existing):
```swift
@MainActor
enum CategoriesV10API {
    static func list(includeArchived: Bool = false) async throws -> [CategoryV10DTO]
    static func update(id: Int, payload: CategoryV10UpdateRequest) async throws -> CategoryV10DTO
}
```

CategoryV10UpdateRequest (existing):
```swift
struct CategoryV10UpdateRequest: Encodable {
    init(name: String? = nil, sortOrder: Int? = nil, isArchived: Bool? = nil,
         planCents: Int? = nil, rollover: CategoryRollover? = nil,
         paused: Bool? = nil, parentId: Int? = nil)
}
```

MoneyParser (existing):
```swift
enum MoneyParser {
    /// "100" → 10000; "1 500,50" → 150050; "abc" → nil
    static func parseToCents(_ raw: String) -> Int?
}
```

MoneyFormatter (existing):
```swift
enum MoneyFormatter {
    static func format(cents: Int) -> String         // "1 500,50"
    static func formatWithSymbol(cents: Int) -> String  // "1 500,50 ₽"
}
```

PlanRowEditorViewModel surface (from 61-01 scaffold, FILL load/save/isDirty):
```swift
@MainActor @Observable
final class PlanRowEditorViewModel {
    enum Status: Equatable { case idle, loading, ready, error(String) }
    let categoryId: Int
    private(set) var status: Status = .idle
    private(set) var category: CategoryV10DTO?
    var planCents: Int = 0
    var rollover: CategoryRollover = .misc
    var paused: Bool = false
    private(set) var submitting: Bool = false
    var saveError: String? = nil
    var onSaved: ((CategoryV10DTO) -> Void)?
    init(categoryId: Int)
    func load() async  // FILL
    func save() async -> Bool  // FILL
    var isDirty: Bool  // FILL (computed)
    #if DEBUG func _setStateForTesting(...) #endif  // existing scaffold
}
```

Onboarding Step 3 Stepper pattern (Phase 57 reference):
```swift
let rubles = Binding<Int>(
    get: { (flow.categoryPlans[cat.code] ?? 0) / 100 },
    set: { newRub in flow.setPlan(code: cat.code, cents: max(0, newRub) * 100) }
)
Stepper(value: rubles, in: 0...1_000_000, step: 500) {
    LabeledContent(cat.name) {
        Text(MoneyFormatter.formatWithSymbol(cents: (flow.categoryPlans[cat.code] ?? 0)))
            .monospacedDigit()
    }
}
```

Cancel-confirmation pattern (использовать .alert или .confirmationDialog с .interactiveDismissDisabled при isDirty):
```swift
.interactiveDismissDisabled(viewModel.isDirty && !viewModel.submitting)
.alert("Отменить изменения?", isPresented: $showCancelAlert) {
    Button("Отменить", role: .destructive) { dismiss() }
    Button("Продолжить редактирование", role: .cancel) {}
}
```
NOTE: Так как мы НЕ в sheet а в push (NavigationLink), `dismiss` Environment работает; cancel-confirmation детектится через onChange / explicit back-toolbar OR через .interactiveDismissDisabled effective только для sheets. **Pragmatic approach (минимум скоупа):** добавить Cancel toolbar item с .alert; default system back button — без confirmation (как в Form-стандарте iOS). Это документировано в 61-CONTEXT как Claude's Discretion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PlanRowEditorViewModel (load + save + isDirty)</name>
  <files>ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift (61-01 scaffold)
    - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (load pattern с cross-tenant guard для find-by-id из list)
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (createAccount pattern — submitting flag + filtered Russian copy on failure)
    - ios/BudgetPlanner/Networking/Endpoints/CategoriesV10API.swift
    - ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift (CategoryV10UpdateRequest)
  </read_first>
  <behavior>
    Test 1 — test_initialState_idleZero: status == .idle; category == nil; planCents == 0; rollover == .misc; paused == false; submitting == false; saveError == nil; isDirty == false.
    Test 2 — test_load_seedsEditingState (через _setStateForTesting — load() requires real API, skipped в unit tests; integration smoke в final phase): _setStateForTesting(category: c1, planCents: 50000, rollover: .savings, paused: true) → editing state matches.
    Test 3 — test_isDirty_falseWhenAllMatch: category planCents=10000 rollover=.misc paused=false; editing state matches → isDirty == false.
    Test 4 — test_isDirty_trueWhenPlanCentsChanged: category.planCents=10000; vm.planCents=15000 → isDirty == true.
    Test 5 — test_isDirty_trueWhenRolloverChanged: category.rollover=.misc; vm.rollover=.savings → isDirty == true.
    Test 6 — test_isDirty_trueWhenPausedChanged: category.paused=false; vm.paused=true → isDirty == true.
    Test 7 — test_isDirty_falseWhenCategoryNil: category=nil → isDirty == false (no anchor для diff).
    Test 8 — test_onSavedCallback_canBeWired: vm.onSaved = { _ in capture }; verify nil-default + manual set.
    Test 9 — test_status_equatable_distinctErrors: Status.error("a") != Status.error("b"); Status.idle == Status.idle.
    Test 10 — test_save_returnsBoolEarlyWhenCategoryNil: vm.save() w/o seeded category → return false (no api call). Note: real save path runs API → covered manual smoke; здесь только smoke что guard работает.
    Test 11 — test_setStateForTesting_doesNotFlipStatus: state populated но status остаётся .idle.
  </behavior>
  <action>
    Replace stub bodies в `PlanRowEditorViewModel.swift`:

    ```swift
    @MainActor
    @Observable
    final class PlanRowEditorViewModel {
        enum Status: Equatable {
            case idle, loading, ready, error(String)
        }

        let categoryId: Int

        private(set) var status: Status = .idle
        private(set) var category: CategoryV10DTO?

        var planCents: Int = 0
        var rollover: CategoryRollover = .misc
        var paused: Bool = false

        private(set) var submitting: Bool = false
        var saveError: String? = nil

        var onSaved: ((CategoryV10DTO) -> Void)?

        @ObservationIgnored private var inFlight: Bool = false

        init(categoryId: Int) {
            self.categoryId = categoryId
        }

        // MARK: - Load

        /// Fetch categories list + find by id. Seeds editing state (planCents,
        /// rollover, paused) из найденной DTO. Cross-tenant / missing id —
        /// single error message (T-61-03 — no existence leak).
        func load() async {
            if inFlight { return }
            inFlight = true
            defer { inFlight = false }

            status = .loading

            do {
                let cats = try await CategoriesV10API.list()
                guard let c = cats.first(where: { $0.id == categoryId }) else {
                    status = .error("Категория не найдена")
                    return
                }
                self.category = c
                self.planCents = c.planCents
                self.rollover = c.rollover
                self.paused = c.paused
                status = .ready
            } catch {
                print("[PlanRowEditorViewModel] load failed: \(error)")
                status = .error("Не удалось загрузить категорию")
            }
        }

        // MARK: - Save

        /// PATCH /categories/{id} — per-row immediate save (CONTEXT D-4).
        /// On success: self.category = updated; onSaved?(updated); saveError = nil.
        /// On failure: saveError = filtered Russian copy; returns false.
        /// inFlight guard через submitting flag (T-61-02).
        func save() async -> Bool {
            guard category != nil else { return false }
            if submitting { return false }
            submitting = true
            defer { submitting = false }

            saveError = nil

            // T-61-01: UI gate планируется в View (Stepper 0...10_000_000);
            // на ViewModel уровне — defensive clamp до 0.
            let safePlanCents = Swift.max(0, planCents)

            let payload = CategoryV10UpdateRequest(
                planCents: safePlanCents,
                rollover: rollover,
                paused: paused
            )

            do {
                let updated = try await CategoriesV10API.update(
                    id: categoryId,
                    payload: payload
                )
                self.category = updated
                self.planCents = updated.planCents
                self.rollover = updated.rollover
                self.paused = updated.paused
                onSaved?(updated)
                return true
            } catch {
                print("[PlanRowEditorViewModel] save failed: \(error)")
                // T-61-03: filtered Russian copy, raw error → print only.
                saveError = "Не удалось сохранить категорию"
                return false
            }
        }

        // MARK: - Dirty check

        /// True когда any editing field отличается от загруженной DTO.
        /// Returns false если category == nil (no anchor для diff).
        var isDirty: Bool {
            guard let c = category else { return false }
            return planCents != c.planCents
                || rollover != c.rollover
                || paused != c.paused
        }

        // MARK: - DEBUG test backdoor

        #if DEBUG
        func _setStateForTesting(
            category: CategoryV10DTO? = nil,
            planCents: Int = 0,
            rollover: CategoryRollover = .misc,
            paused: Bool = false
        ) {
            self.category = category
            self.planCents = planCents
            self.rollover = rollover
            self.paused = paused
        }
        #endif
    }
    ```

    Создать `ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift`:

    ```swift
    import XCTest
    @testable import BudgetPlanner

    @MainActor
    final class PlanRowEditorViewModelTests: XCTestCase {

        // Fixture
        private func makeCategory(
            id: Int,
            planCents: Int = 0,
            rollover: String = "misc",
            paused: Bool = false
        ) -> CategoryV10DTO {
            let json = """
            {"id":\(id),"name":"X","kind":"expense","is_archived":false,
            "sort_order":0,"created_at":null,"plan_cents":\(planCents),
            "rollover":"\(rollover)","paused":\(paused ? "true" : "false")}
            """.data(using: .utf8)!
            let dec = JSONDecoder()
            dec.keyDecodingStrategy = .convertFromSnakeCase
            return try! dec.decode(CategoryV10DTO.self, from: json)
        }

        func test_initialState_idleZero() {
            let vm = PlanRowEditorViewModel(categoryId: 42)
            XCTAssertEqual(vm.status, .idle)
            XCTAssertNil(vm.category)
            XCTAssertEqual(vm.planCents, 0)
            XCTAssertEqual(vm.rollover, .misc)
            XCTAssertFalse(vm.paused)
            XCTAssertFalse(vm.submitting)
            XCTAssertNil(vm.saveError)
            XCTAssertFalse(vm.isDirty)
            XCTAssertEqual(vm.categoryId, 42)
        }

        func test_setStateForTesting_seedsEditingState() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            let c = makeCategory(id: 1, planCents: 10000, rollover: "savings", paused: true)
            vm._setStateForTesting(category: c, planCents: 50000, rollover: .savings, paused: true)
            XCTAssertEqual(vm.planCents, 50000)
            XCTAssertEqual(vm.rollover, .savings)
            XCTAssertTrue(vm.paused)
        }

        func test_isDirty_falseWhenAllMatch() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            let c = makeCategory(id: 1, planCents: 10000, rollover: "misc", paused: false)
            vm._setStateForTesting(category: c, planCents: 10000, rollover: .misc, paused: false)
            XCTAssertFalse(vm.isDirty)
        }

        func test_isDirty_trueWhenPlanCentsChanged() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            let c = makeCategory(id: 1, planCents: 10000)
            vm._setStateForTesting(category: c, planCents: 15000)
            XCTAssertTrue(vm.isDirty)
        }

        func test_isDirty_trueWhenRolloverChanged() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            let c = makeCategory(id: 1, rollover: "misc")
            vm._setStateForTesting(category: c, rollover: .savings)
            XCTAssertTrue(vm.isDirty)
        }

        func test_isDirty_trueWhenPausedChanged() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            let c = makeCategory(id: 1, paused: false)
            vm._setStateForTesting(category: c, paused: true)
            XCTAssertTrue(vm.isDirty)
        }

        func test_isDirty_falseWhenCategoryNil() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            vm.planCents = 50000
            vm.rollover = .savings
            vm.paused = true
            XCTAssertFalse(vm.isDirty)
        }

        func test_onSaved_canBeWiredAndDefaultsNil() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            XCTAssertNil(vm.onSaved)
            var captured: CategoryV10DTO?
            vm.onSaved = { c in captured = c }
            let c = makeCategory(id: 1)
            vm.onSaved?(c)
            XCTAssertEqual(captured?.id, 1)
        }

        func test_status_equatable() {
            XCTAssertEqual(PlanRowEditorViewModel.Status.idle, .idle)
            XCTAssertNotEqual(
                PlanRowEditorViewModel.Status.error("a"),
                PlanRowEditorViewModel.Status.error("b")
            )
        }

        func test_save_earlyReturnFalseWhenCategoryNil() async {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            let ok = await vm.save()
            XCTAssertFalse(ok)
            // No state mutation expected.
            XCTAssertNil(vm.saveError)
            XCTAssertFalse(vm.submitting)
        }

        func test_setStateForTesting_doesNotFlipStatus() {
            let vm = PlanRowEditorViewModel(categoryId: 1)
            vm._setStateForTesting(category: makeCategory(id: 1, planCents: 1000))
            XCTAssertEqual(vm.status, .idle)
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "CategoriesV10API.list" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "CategoriesV10API.update" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "Категория не найдена" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "Не удалось сохранить категорию" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "error.localizedDescription" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 0 (T-61-03) -->
    <automated>grep -c "CategoryV10UpdateRequest" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "onSaved?(updated)" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "submitting" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift</automated>
    <!-- Expected: ≥3 (declaration + guard + defer) -->
    <automated>test -f ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift</automated>
    <automated>grep -c "func test_" ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift</automated>
    <!-- Expected: ≥10 -->
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/PlanRowEditorViewModelTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -25 | grep -cE "Test Suite 'PlanRowEditorViewModelTests' passed|TEST SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - load() через CategoriesV10API.list + find-by-id + cross-tenant guard.
    - save() через CategoriesV10API.update; on success → onSaved?(updated).
    - isDirty 3-field comparison; false когда category=nil.
    - T-61-02: submitting flag guard.
    - T-61-03: filtered Russian copy on both load/save failures; no localizedDescription.
    - ≥10 tests pass.
  </acceptance_criteria>
  <done>
    All grep gates pass; PlanRowEditorViewModelTests SUCCEEDED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement PlanRowEditorView body (Form Stepper+TextField+Picker+Toggle + Save toolbar + banner + cancel-confirmation)</name>
  <files>ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift (61-01 scaffold)
    - ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift (post Task 1 — full surface)
    - ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep3PlanView.swift (Stepper rubles binding pattern)
    - ios/BudgetPlanner/Domain/MoneyFormatter.swift (MoneyParser.parseToCents)
    - ios/BudgetPlanner/Features/Accounts/AccountsView.swift (banner Section pattern для filtered Russian copy)
  </read_first>
  <action>
    Replace body of `PlanRowEditorView.swift`:

    ```swift
    import SwiftUI

    /// Phase 61: PlanRowEditorView — detail editor для одной категории.
    ///
    /// 61-03 реализация:
    ///   Form с 3 секциями:
    ///     • «Лимит» — Stepper (+/- 500₽ step) + TextField (.decimalPad через
    ///       MoneyParser). Real-time sync rubles ↔ cents.
    ///     • «Перенос остатка» — Picker (segmented) с 2 опциями: «В прочее»
    ///       (.misc) / «В накопления» (.savings).
    ///     • «Статус» — Toggle paused.
    ///   Toolbar Save в .confirmationAction — disabled пока !isDirty || submitting.
    ///   Inline banner Section с saveError filtered Russian copy на failure.
    ///   Cancel: Cancel toolbar item → .alert «Отменить изменения?» когда isDirty.
    ///
    /// `onSaved` closure инжектируется родителем PlanEditorView (61-02) и
    /// вызывается PlanRowEditorViewModel.save() после successful PATCH.
    ///
    /// Threat-model:
    ///   - T-61-01: Stepper UI bounds 0...10_000_000.
    ///   - T-61-02: Save disabled когда submitting (inFlight guard).
    ///   - T-61-03: banner copy фиксированный, NO error.localizedDescription.
    struct PlanRowEditorView: View {
        let categoryId: Int
        let onSaved: (CategoryV10DTO) -> Void

        @State private var viewModel: PlanRowEditorViewModel
        @State private var rublesText: String = ""
        @State private var showCancelAlert: Bool = false
        @Environment(\.dismiss) private var dismiss

        init(categoryId: Int, onSaved: @escaping (CategoryV10DTO) -> Void) {
            self.categoryId = categoryId
            self.onSaved = onSaved
            self._viewModel = State(
                wrappedValue: PlanRowEditorViewModel(categoryId: categoryId)
            )
        }

        var body: some View {
            Form {
                switch viewModel.status {
                case .idle, .loading:
                    Section { ProgressView().frame(maxWidth: .infinity) }
                case .error(let msg):
                    Section {
                        Label(msg, systemImage: "exclamationmark.triangle")
                            .foregroundStyle(.red)
                    }
                case .ready:
                    if let cat = viewModel.category {
                        if let err = viewModel.saveError {
                            saveErrorBanner(err)
                        }
                        limitSection(cat)
                        rolloverSection
                        statusSection
                    } else {
                        Section {
                            Label("Категория не найдена", systemImage: "exclamationmark.triangle")
                                .foregroundStyle(.red)
                        }
                    }
                }
            }
            .navigationTitle(viewModel.category?.name ?? "Категория")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar { toolbarContent }
            .alert("Отменить изменения?", isPresented: $showCancelAlert) {
                Button("Отменить", role: .destructive) { dismiss() }
                Button("Продолжить", role: .cancel) {}
            } message: {
                Text("Несохранённые изменения будут потеряны.")
            }
            .onAppear {
                viewModel.onSaved = onSaved
            }
            .task { await viewModel.load() }
            .onChange(of: viewModel.planCents) { _, newCents in
                // Keep TextField rubles in sync with Stepper changes.
                let rub = newCents / 100
                let formatted = MoneyFormatter.format(cents: newCents)
                if rublesText != "\(rub)" && rublesText != formatted {
                    rublesText = "\(rub)"
                }
            }
            .onChange(of: viewModel.category?.planCents) { _, _ in
                // Seed rublesText once category loaded.
                rublesText = "\(viewModel.planCents / 100)"
            }
        }

        // MARK: - Toolbar

        @ToolbarContentBuilder
        private var toolbarContent: some ToolbarContent {
            ToolbarItem(placement: .cancellationAction) {
                Button("Отмена") {
                    if viewModel.isDirty {
                        showCancelAlert = true
                    } else {
                        dismiss()
                    }
                }
            }
            ToolbarItem(placement: .confirmationAction) {
                Button {
                    Task {
                        let ok = await viewModel.save()
                        if ok { dismiss() }
                    }
                } label: {
                    if viewModel.submitting {
                        ProgressView().controlSize(.small)
                    } else {
                        Text("Сохранить")
                    }
                }
                .disabled(!viewModel.isDirty || viewModel.submitting)
            }
        }

        // MARK: - Save error banner (T-61-03)

        private func saveErrorBanner(_ msg: String) -> some View {
            Section {
                HStack(spacing: 12) {
                    Image(systemName: "exclamationmark.triangle.fill")
                        .foregroundStyle(.red)
                    Text(msg)
                        .font(.callout)
                        .foregroundStyle(.primary)
                    Spacer(minLength: 8)
                    Button {
                        viewModel.saveError = nil
                    } label: {
                        Image(systemName: "xmark.circle.fill")
                            .foregroundStyle(.secondary)
                    }
                    .buttonStyle(.borderless)
                    .accessibilityLabel("Скрыть ошибку")
                }
            }
        }

        // MARK: - Limit section

        private func limitSection(_ cat: CategoryV10DTO) -> some View {
            Section {
                // Stepper bound to rubles (Int).
                let rubBinding = Binding<Int>(
                    get: { Swift.max(0, viewModel.planCents / 100) },
                    set: { newRub in
                        // T-61-01: UI bound clamp.
                        viewModel.planCents = Swift.max(0, Swift.min(10_000_000, newRub * 100))
                    }
                )
                Stepper(value: rubBinding, in: 0...100_000, step: 500) {
                    LabeledContent("Лимит") {
                        Text(MoneyFormatter.formatWithSymbol(cents: viewModel.planCents))
                            .monospacedDigit()
                    }
                }
                // Precise input via .decimalPad → MoneyParser.
                HStack {
                    TextField("Точная сумма (₽)", text: $rublesText)
                        .keyboardType(.decimalPad)
                        .onChange(of: rublesText) { _, newValue in
                            if let cents = MoneyParser.parseToCents(newValue) {
                                viewModel.planCents = Swift.max(0, Swift.min(10_000_000, cents))
                            }
                        }
                    Text("₽").foregroundStyle(.secondary)
                }
            } header: {
                Text("Лимит")
            } footer: {
                if let cat = viewModel.category {
                    Text("Текущий сохранённый: \(MoneyFormatter.formatWithSymbol(cents: cat.planCents))")
                }
            }
        }

        // MARK: - Rollover section

        private var rolloverSection: some View {
            Section {
                Picker("Куда переносить", selection: $viewModel.rollover) {
                    Text("В прочее").tag(CategoryRollover.misc)
                    Text("В накопления").tag(CategoryRollover.savings)
                }
                .pickerStyle(.segmented)
            } header: {
                Text("Перенос остатка")
            } footer: {
                Text("Остаток (план − факт) переходит в выбранный буфер при закрытии периода.")
            }
        }

        // MARK: - Status section

        private var statusSection: some View {
            Section {
                Toggle("Приостановлено", isOn: $viewModel.paused)
            } footer: {
                Text("Приостановленные категории не учитываются в распределении бюджета.")
            }
        }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "Stepper(value:" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "TextField(\"Точная сумма" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "\.decimalPad" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "Picker(\"Куда переносить\"" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "Toggle(\"Приостановлено\"" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "MoneyParser.parseToCents" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c ".confirmationAction" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c ".cancellationAction" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "viewModel.isDirty" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: ≥2 (disabled + cancel guard) -->
    <automated>grep -c "viewModel.submitting" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: ≥2 -->
    <automated>grep -c "viewModel.saveError" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: ≥2 (banner + dismiss) -->
    <automated>grep -c "Отменить изменения?" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "viewModel.onSaved = onSaved" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "error.localizedDescription" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift</automated>
    <!-- Expected: 0 (T-61-03 — banner shows VM.saveError filtered copy) -->
    <automated>cd ios && make build 2>&1 | tail -10 | grep -cE "BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - Form с 3 секциями: Лимит (Stepper + TextField .decimalPad через MoneyParser), Перенос (Picker segmented), Статус (Toggle).
    - Stepper UI gate 0...10_000_000 cents (T-61-01); TextField parse clamps в same bounds.
    - Toolbar: Cancel + Сохранить (.confirmationAction, disabled !isDirty || submitting).
    - Banner Section на saveError с dismiss button.
    - .alert «Отменить изменения?» при isDirty + cancel tap.
    - .onAppear wire viewModel.onSaved = onSaved (closure injection).
    - Build clean.
  </acceptance_criteria>
  <done>
    All grep gates pass; make build SUCCEEDED.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app ↔ /api/v1/categories | UI Stepper + TextField clamp 0...10_000_000 cents; backend Pydantic CategoryUpdate валидирует planCents ≥ 0. |
| iOS app ↔ PlanRowEditorViewModel.save() concurrent calls | submitting flag + guard. |
| iOS app ↔ raw network errors | All catch blocks → filtered Russian copy; raw error → print() console. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Tampering | planCents wire input | mitigate | UI guards: Stepper `in: 0...100_000` rubles step 500 (caps planCents 0...10_000_000 cents); TextField parses via MoneyParser, then clamps `Swift.max(0, Swift.min(10_000_000, cents))` before binding. ViewModel save() applies defensive `Swift.max(0, planCents)` clamp before PATCH. Backend Pydantic CategoryV10UpdateRequest validation gates plan_cents ≥ 0 + overflow check. Verified: `grep -c "Swift.max(0, Swift.min(10_000_000"` ≥1 в View; `grep -c "Swift.max(0, planCents)"` ≥1 в VM. |
| T-61-02 | DoS / Concurrency | PlanRowEditorViewModel.save() concurrent invocations | mitigate | `submitting: Bool` flag set к true до await, defer reset. Early-return false если уже submitting. Save toolbar button has `.disabled(submitting)` UI guard. Verified: `grep -c "submitting"` ≥3 в VM. |
| T-61-03 | Information Disclosure | load() + save() catch blocks + banner | mitigate | Filtered Russian copy: «Категория не найдена» (cross-tenant guard), «Не удалось загрузить категорию» (load failure), «Не удалось сохранить категорию» (save failure). Inline banner отображает `viewModel.saveError` — фиксированную копию (not error.localizedDescription). Raw error → `print()` only. Verified: `grep -c "error.localizedDescription"` returns 0 в обоих файлах. |
</threat_model>

<verification>
- `grep -c "CategoriesV10API.update" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift` returns 1.
- `grep -c "Категория не найдена\|Не удалось" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift` returns ≥3 (3 filtered copies).
- `grep -c "error.localizedDescription" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorViewModel.swift` returns 0.
- `grep -c "error.localizedDescription" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift` returns 0.
- `grep -c "func test_" ios/BudgetPlannerTests/Features/PlanEditor/PlanRowEditorViewModelTests.swift` returns ≥10.
- `grep -c "Stepper\|TextField\|Picker\|Toggle" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift` returns ≥4.
- `grep -c "MoneyParser.parseToCents" ios/BudgetPlanner/Features/PlanEditor/PlanRowEditorView.swift` returns 1.
- `cd ios && make build` returns SUCCEEDED.
- `xcodebuild test -only-testing:BudgetPlannerTests/PlanRowEditorViewModelTests` ≥10 pass.
</verification>

<success_criteria>
1. PlanRowEditorViewModel.load() через CategoriesV10API.list + find by id, seeds editing state.
2. Cross-tenant guard: missing id → status = .error("Категория не найдена") (T-61-03).
3. PlanRowEditorViewModel.save() через CategoriesV10API.update(id:, payload: CategoryV10UpdateRequest(planCents:, rollover:, paused:)) — per-row immediate.
4. Save on success: self.category = updated; onSaved?(updated); returns true.
5. Save on failure: saveError = "Не удалось сохранить категорию" (T-61-03 filtered); returns false; raw error → print().
6. T-61-02 mitigated: submitting flag + early-return guard.
7. isDirty 3-field comparison; false когда category=nil.
8. PlanRowEditorView Form с 3 секциями: Лимит (Stepper + TextField .decimalPad через MoneyParser), Перенос (Picker), Статус (Toggle).
9. Stepper bounds rubles 0...100_000 (planCents 0...10_000_000) — T-61-01.
10. Toolbar Save в .confirmationAction — disabled when !isDirty || submitting.
11. Cancel toolbar → .alert «Отменить изменения?» когда isDirty.
12. Inline banner на saveError с dismiss button.
13. .onAppear wire viewModel.onSaved = onSaved.
14. ≥10 PlanRowEditorViewModelTests pass.
15. Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/61-plan-editor-v06/61-03-SUMMARY.md` summarising:
- load() control flow (list + find + seed)
- save() control flow (PATCH + onSaved callback chain → parent VM optimistic refresh)
- Threat mitigation (T-61-01 UI clamp + VM defensive max(0); T-61-02 submitting; T-61-03 filtered copy)
- PlanRowEditorView Form composition (3 sections + 2 toolbar items + banner + alert)
- Rubles ↔ cents binding pattern (Stepper rubles + TextField clamp через MoneyParser)
- Tests coverage matrix
- Build status
</output>
