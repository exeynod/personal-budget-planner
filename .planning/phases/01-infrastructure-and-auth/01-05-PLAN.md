---
phase: 01-infrastructure-and-auth
plan: 05
type: execute
wave: 3
depends_on:
  - "01-04"
files_modified:
  - app/api/router.py
  - main_api.py
  - main_bot.py
  - main_worker.py
  - entrypoint.sh
autonomous: true
requirements:
  - INF-01
  - INF-04
  - INF-05
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "GET /healthz → 200 {status: ok}"
    - "GET /api/v1/me без заголовка → 403"
    - "GET /api/v1/me с валидным initData OWNER → 200"
    - "GET /api/v1/internal/health с неверным X-Internal-Token → 403"
    - "GET /api/v1/internal/health с верным X-Internal-Token → 200"
    - "main_bot.py: bot polling + /healthz на порту 8001"
    - "main_worker.py: APScheduler AsyncIOScheduler старт + heartbeat в app_health"
    - "entrypoint.sh: alembic upgrade head затем uvicorn"
  artifacts:
    - path: "main_api.py"
      provides: "FastAPI app с lifespan, роутерами /api/v1/, /healthz"
      exports: ["app"]
    - path: "app/api/router.py"
      provides: "APIRouter с /me и /internal/health эндпоинтами"
    - path: "main_bot.py"
      provides: "aiogram polling + aiohttp /healthz на :8001"
    - path: "main_worker.py"
      provides: "APScheduler AsyncIOScheduler + heartbeat job"
    - path: "entrypoint.sh"
      provides: "alembic upgrade head + uvicorn старт"
  key_links:
    - from: "main_api.py"
      to: "app/api/router.py"
      via: "app.include_router(api_router, prefix='/api/v1')"
      pattern: "include_router.*api_router"
    - from: "app/api/router.py"
      to: "app/api/dependencies.py"
      via: "Depends(get_current_user), Depends(verify_internal_token)"
      pattern: "Depends(get_current_user)"
    - from: "main_worker.py"
      to: "app/db/session.py"
      via: "async DB session для записи heartbeat в app_health"
      pattern: "AsyncSessionLocal"
---

<objective>
Точки входа и API роутеры: FastAPI app с lifespan, /healthz, /api/v1/me, /api/v1/internal/health; bot с polling + healthz на 8001; worker с APScheduler + heartbeat; entrypoint.sh для api.

Purpose: Собрать все компоненты в работающие сервисы (INF-01, INF-05) с auth layer (AUTH-01, AUTH-02) и internal endpoint (INF-04).
Output: main_api.py, app/api/router.py, main_bot.py, main_worker.py, entrypoint.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-infrastructure-and-auth/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-auth/01-RESEARCH.md
@docs/HLD.md
@app/api/dependencies.py
@app/core/settings.py
@app/db/session.py

<interfaces>
<!-- Из app/api/dependencies.py (Plan 04) -->
```python
async def get_current_user(x_telegram_init_data: str | None = Header(...)) -> dict
async def verify_internal_token(x_internal_token: str | None = Header(...)) -> None
async def get_db() -> AsyncGenerator[AsyncSession, None]
```

<!-- Из app/core/settings.py (Plan 02) -->
```python
settings.DATABASE_URL: str
settings.BOT_TOKEN: str
settings.LOG_LEVEL: str
settings.LOG_FORMAT: str
settings.APP_TZ: str  # "Europe/Moscow"
```

<!-- Из RESEARCH.md Pattern 6: bot healthz на aiohttp -->
```python
# main_bot.py паттерн
from aiohttp import web
from aiogram import Bot, Dispatcher
site = web.TCPSite(runner, "0.0.0.0", 8001)
await dp.start_polling(bot)  # blocks
```

<!-- Из RESEARCH.md Pattern 7: APScheduler 3.x -->
```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler
scheduler = AsyncIOScheduler(timezone=pytz.timezone("Europe/Moscow"))
scheduler.start()
```

<!-- Из RESEARCH.md Pattern 9: entrypoint.sh -->
```bash
#!/bin/sh
set -e
uv run alembic upgrade head
exec uv run uvicorn main_api:app --host 0.0.0.0 --port 8000
```

<!-- app_user upsert при первом запросе (D-11) -->
При GET /api/v1/me с валидным initData: upsert app_user по tg_user_id
(INSERT ... ON CONFLICT DO UPDATE) чтобы создать запись при первом запросе.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать FastAPI app (main_api.py) и api router</name>
  <read_first>
    - app/api/dependencies.py (сигнатуры get_current_user, verify_internal_token, get_db)
    - app/db/models.py (AppUser модель для upsert в /me)
    - app/core/settings.py (settings.DEV_MODE)
    - .planning/phases/01-infrastructure-and-auth/01-CONTEXT.md (D-11 app_user upsert, D-12 healthz)
    - .planning/phases/01-infrastructure-and-auth/01-RESEARCH.md (State of the Art — lifespan вместо @app.on_event)
    - docs/HLD.md §4.1 (GET /me response schema)
  </read_first>
  <files>app/api/router.py, main_api.py</files>
  <action>
Создать `app/api/router.py`:
```python
"""
API routers for TG Budget Planner.
Phase 1: /me (auth test), /internal/health (internal token test)
"""
from typing import Annotated

from fastapi import APIRouter, Depends
from pydantic import BaseModel
from sqlalchemy import select
from sqlalchemy.dialects.postgresql import insert
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db, verify_internal_token
from app.db.models import AppUser


# ---- Public router (requires initData auth) ----
public_router = APIRouter()


class MeResponse(BaseModel):
    tg_user_id: int
    tg_chat_id: int | None
    cycle_start_day: int
    onboarded_at: str | None
    chat_id_known: bool


@public_router.get("/me", response_model=MeResponse)
async def get_me(
    current_user: Annotated[dict, Depends(get_current_user)],
    db: Annotated[AsyncSession, Depends(get_db)],
) -> MeResponse:
    """
    Return current user info. Creates app_user record on first request (D-11: upsert).
    """
    tg_user_id = current_user["id"]

    # D-11: upsert app_user on first valid request (no migration seed)
    stmt = (
        insert(AppUser)
        .values(tg_user_id=tg_user_id)
        .on_conflict_do_nothing(index_elements=["tg_user_id"])
    )
    await db.execute(stmt)

    result = await db.execute(
        select(AppUser).where(AppUser.tg_user_id == tg_user_id)
    )
    user = result.scalar_one()

    return MeResponse(
        tg_user_id=user.tg_user_id,
        tg_chat_id=user.tg_chat_id,
        cycle_start_day=user.cycle_start_day,
        onboarded_at=user.onboarded_at.isoformat() if user.onboarded_at else None,
        chat_id_known=user.tg_chat_id is not None,
    )


# ---- Internal router (requires X-Internal-Token) ----
internal_router = APIRouter(
    prefix="/internal",
    dependencies=[Depends(verify_internal_token)],
)


@internal_router.get("/health")
async def internal_health() -> dict:
    """Health check for internal service-to-service communication (bot↔api)."""
    return {"status": "ok", "service": "api-internal"}
```

Создать `main_api.py`:
```python
"""
FastAPI entry point for the api container.
Entrypoint: entrypoint.sh → alembic upgrade head → uvicorn main_api:app
"""
from contextlib import asynccontextmanager

import structlog
from fastapi import FastAPI

from app.api.router import internal_router, public_router
from app.core.logging import configure_logging
from app.core.settings import settings
from app.db.session import async_engine

logger = structlog.get_logger(__name__)


@asynccontextmanager
async def lifespan(app: FastAPI):
    """FastAPI lifespan: setup → yield → teardown."""
    configure_logging(settings.LOG_LEVEL, settings.LOG_FORMAT)
    logger.info("api.startup", dev_mode=settings.DEV_MODE, domain=settings.PUBLIC_DOMAIN)
    yield
    await async_engine.dispose()
    logger.info("api.shutdown")


app = FastAPI(
    title="TG Budget Planner API",
    version="1.0.0",
    lifespan=lifespan,
    # Disable docs in production to reduce attack surface
    docs_url="/api/docs" if settings.DEV_MODE else None,
    redoc_url=None,
)


@app.get("/healthz", tags=["health"])
async def healthz() -> dict:
    """Health check endpoint for Docker and Caddy (INF-05)."""
    return {"status": "ok"}


# Mount routers
app.include_router(public_router, prefix="/api/v1", tags=["public"])
app.include_router(internal_router, prefix="/api/v1", tags=["internal"])
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "
import ast, os
for f in ['app/api/router.py', 'main_api.py']:
    assert os.path.exists(f), f'Missing: {f}'
    ast.parse(open(f).read())
# Verify key patterns
router = open('app/api/router.py').read()
main = open('main_api.py').read()
assert 'get_current_user' in router, 'get_current_user missing in router'
assert 'verify_internal_token' in router, 'verify_internal_token missing in router'
assert 'lifespan' in main, 'lifespan missing in main_api.py'
assert '/healthz' in main, '/healthz endpoint missing'
assert 'include_router' in main, 'include_router missing'
print('main_api.py and router.py OK')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - main_api.py существует: `test -f main_api.py`
    - main_api.py содержит lifespan context manager: `grep -c "asynccontextmanager" main_api.py` == 1
    - main_api.py содержит /healthz endpoint: `grep -c '"/healthz"' main_api.py` == 1
    - main_api.py включает оба роутера через include_router: `grep -c "include_router" main_api.py` == 2
    - app/api/router.py содержит get_current_user dependency: `grep -c "get_current_user" app/api/router.py` >= 2
    - app/api/router.py содержит verify_internal_token dependency: `grep -c "verify_internal_token" app/api/router.py` >= 1
    - router.py содержит GET /me: `grep -c '"/me"' app/api/router.py` == 1
    - router.py содержит GET /internal/health: `grep -c '"/health"' app/api/router.py` == 1
    - router.py содержит app_user upsert: `grep -c "on_conflict_do_nothing" app/api/router.py` == 1
    - main_api.py НЕ содержит @app.on_event: `grep -c "on_event" main_api.py` == 0 (используем lifespan per State of the Art)
    - После загрузки модуля (syntax only) ошибок нет
  </acceptance_criteria>
  <done>FastAPI app с lifespan, /healthz, /api/v1/me (с upsert app_user), /api/v1/internal/health (с verify_internal_token)</done>
</task>

<task type="auto">
  <name>Task 2: Создать main_bot.py, main_worker.py, entrypoint.sh</name>
  <read_first>
    - .planning/phases/01-infrastructure-and-auth/01-RESEARCH.md (Pattern 6 aiogram polling+healthz, Pattern 7 APScheduler, Pattern 9 entrypoint.sh)
    - .planning/phases/01-infrastructure-and-auth/01-CONTEXT.md (D-04 long-poll, D-12 worker heartbeat в app_health, D-13 structlog)
    - docs/HLD.md §5 (bot команды — /start stub), §6 (scheduler jobs — Phase 1 пустые)
  </read_first>
  <files>main_bot.py, main_worker.py, entrypoint.sh</files>
  <action>
Создать `main_bot.py`:
```python
"""
aiogram bot entry point for the bot container.
Phase 1: /start stub + /healthz on port 8001 (INF-05).
Real command handlers (add/income/balance/today/app) will be added in Phase 4.
"""
import asyncio

import structlog
from aiogram import Bot, Dispatcher, Router
from aiogram.client.default import DefaultBotProperties
from aiogram.enums import ParseMode
from aiogram.filters import CommandStart
from aiogram.types import Message
from aiohttp import web

from app.core.logging import configure_logging
from app.core.settings import settings

configure_logging(settings.LOG_LEVEL, settings.LOG_FORMAT)
logger = structlog.get_logger(__name__)

router = Router()


@router.message(CommandStart())
async def cmd_start(message: Message) -> None:
    """
    /start: record chat_id via internal API (Phase 1 stub).
    Full implementation in Phase 2 (ONB-03): call /api/v1/internal/bot/chat-bound.
    """
    if message.from_user and message.from_user.id == settings.OWNER_TG_ID:
        await message.answer("Бот запущен. Привязка push-уведомлений будет в Phase 2.")
    else:
        await message.answer("Бот приватный.")


async def health_handler(request: web.Request) -> web.Response:
    """Health check endpoint for bot container (INF-05, port 8001)."""
    return web.Response(
        text='{"status":"ok","service":"bot"}',
        content_type="application/json",
    )


async def main() -> None:
    bot = Bot(
        token=settings.BOT_TOKEN,
        default=DefaultBotProperties(parse_mode=ParseMode.HTML),
    )
    dp = Dispatcher()
    dp.include_router(router)

    # Start aiohttp healthz server on port 8001 (D-12, INF-05)
    app = web.Application()
    app.router.add_get("/healthz", health_handler)
    runner = web.AppRunner(app)
    await runner.setup()
    site = web.TCPSite(runner, "0.0.0.0", 8001)
    await site.start()
    logger.info("bot.healthz.started", port=8001)

    # Start long-polling (D-04: polling mode, not webhook)
    logger.info("bot.polling.started")
    try:
        await dp.start_polling(bot)
    finally:
        await runner.cleanup()
        await bot.session.close()


if __name__ == "__main__":
    asyncio.run(main())
```

Создать `main_worker.py`:
```python
"""
APScheduler worker entry point for the worker container.
Phase 1: scheduler initialised with heartbeat job only.
Real cron jobs (notify_subscriptions, charge_subscriptions, close_period) added in Phase 5/6.
"""
import asyncio
from datetime import datetime, timezone

import pytz
import structlog
from apscheduler.schedulers.asyncio import AsyncIOScheduler

from app.core.logging import configure_logging
from app.core.settings import settings
from app.db.session import AsyncSessionLocal
from app.db.models import AppHealth

configure_logging(settings.LOG_LEVEL, settings.LOG_FORMAT)
logger = structlog.get_logger(__name__)

MOSCOW_TZ = pytz.timezone(settings.APP_TZ)


async def heartbeat_job() -> None:
    """
    Write worker heartbeat to app_health table (D-12).
    Runs every 5 minutes. Allows external monitoring of worker liveness.
    """
    async with AsyncSessionLocal() as session:
        try:
            from sqlalchemy import select
            result = await session.execute(
                select(AppHealth).where(AppHealth.service == "worker")
            )
            record = result.scalar_one_or_none()
            now = datetime.now(timezone.utc)
            if record:
                record.last_heartbeat_at = now
            else:
                session.add(AppHealth(service="worker", last_heartbeat_at=now))
            await session.commit()
            logger.info("worker.heartbeat.written")
        except Exception:
            await session.rollback()
            logger.exception("worker.heartbeat.failed")


async def main() -> None:
    scheduler = AsyncIOScheduler(timezone=MOSCOW_TZ)

    # Phase 1: heartbeat every 5 minutes
    scheduler.add_job(
        heartbeat_job,
        "interval",
        minutes=5,
        id="heartbeat",
        replace_existing=True,
    )

    # Placeholder jobs (will be replaced in Phase 5/6):
    # scheduler.add_job(notify_subscriptions, "cron", hour=9, minute=0, id="notify_subscriptions")
    # scheduler.add_job(charge_subscriptions, "cron", hour=0, minute=5, id="charge_subscriptions")
    # scheduler.add_job(close_period, "cron", hour=0, minute=1, id="close_period")

    scheduler.start()
    logger.info("worker.scheduler.started")

    # Keep running
    try:
        while True:
            await asyncio.sleep(60)
    except (KeyboardInterrupt, SystemExit):
        scheduler.shutdown()
        logger.info("worker.scheduler.stopped")


if __name__ == "__main__":
    asyncio.run(main())
```

Создать `entrypoint.sh` в корне проекта (per D-09):
```bash
#!/bin/sh
# api container entrypoint: run migrations then start uvicorn
# Source: D-09 from CONTEXT.md
set -e

echo "Running Alembic migrations..."
uv run alembic upgrade head
echo "Migrations complete."

exec uv run uvicorn main_api:app --host 0.0.0.0 --port 8000
```

Сделать entrypoint.sh исполняемым: файл должен иметь chmod +x (docker-compose передаст это внутри контейнера через COPY и RUN chmod).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "
import ast, os
for f in ['main_bot.py', 'main_worker.py']:
    assert os.path.exists(f), f'Missing: {f}'
    ast.parse(open(f).read())
assert os.path.exists('entrypoint.sh'), 'entrypoint.sh missing'
# Check key patterns
bot = open('main_bot.py').read()
worker = open('main_worker.py').read()
entry = open('entrypoint.sh').read()
assert '8001' in bot, 'healthz port 8001 missing in main_bot.py'
assert 'start_polling' in bot, 'start_polling missing in main_bot.py'
assert 'AsyncIOScheduler' in worker, 'AsyncIOScheduler missing in main_worker.py'
assert 'heartbeat_job' in worker, 'heartbeat_job missing in main_worker.py'
assert 'alembic upgrade head' in entry, 'alembic upgrade head missing in entrypoint.sh'
assert 'uvicorn main_api:app' in entry, 'uvicorn cmd missing in entrypoint.sh'
print('main_bot.py, main_worker.py, entrypoint.sh OK')
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - main_bot.py существует: `test -f main_bot.py`
    - main_bot.py содержит порт 8001 для healthz: `grep -c "8001" main_bot.py` >= 1
    - main_bot.py содержит start_polling: `grep -c "start_polling" main_bot.py` == 1
    - main_bot.py НЕ содержит webhook: `grep -c "webhook" main_bot.py` == 0 (D-04: long-poll mode)
    - main_worker.py существует: `test -f main_worker.py`
    - main_worker.py использует AsyncIOScheduler: `grep -c "AsyncIOScheduler" main_worker.py` == 1
    - main_worker.py содержит heartbeat_job: `grep -c "def heartbeat_job" main_worker.py` == 1
    - main_worker.py использует MOSCOW_TZ: `grep -c "MOSCOW_TZ" main_worker.py` >= 2
    - heartbeat записывает в app_health: `grep -c "AppHealth" main_worker.py` >= 2
    - entrypoint.sh существует: `test -f entrypoint.sh`
    - entrypoint.sh содержит "alembic upgrade head": `grep -c "alembic upgrade head" entrypoint.sh` == 1
    - entrypoint.sh содержит "uvicorn main_api:app": `grep -c "uvicorn main_api:app" entrypoint.sh` == 1
    - entrypoint.sh содержит "set -e": `grep -c "set -e" entrypoint.sh` == 1
  </acceptance_criteria>
  <done>main_bot.py (polling D-04 + healthz :8001), main_worker.py (APScheduler + heartbeat → app_health D-12), entrypoint.sh (alembic + uvicorn D-09)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram → /api/v1/me | initData в X-Telegram-Init-Data; проверяется через get_current_user |
| bot → /api/v1/internal/* | X-Internal-Token; внутренний docker network |
| caddy → /healthz | публичный endpoint, не требует auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-replay | Elevation | main_api.py → get_current_user | mitigate | validate_init_data проверяет auth_date ≤ 24h (реализовано в Plan 04) |
| T-spoof | Spoofing | main_api.py → /api/v1/me | mitigate | get_current_user требует валидный HMAC и OWNER_TG_ID whitelist (реализовано в Plan 04) |
| T-internal | Elevation | app/api/router.py /internal/health | mitigate | Защищён Depends(verify_internal_token); Caddy блокирует /api/v1/internal/* через respond 403 до передачи в api (обеспечивается в Plan 06 Caddyfile) |
| T-devmode | Spoofing | main_api.py lifespan | mitigate | docs_url=None когда DEV_MODE=False — не экспонирует API documentation в prod |
| T-bot-polling | Info Disclosure | main_bot.py | accept | Bot polling mode (D-04) не требует публичного endpoint; healthz на :8001 не проксируется через Caddy |
</threat_model>

<verification>
Plan 05 завершён когда:
1. Все 5 файлов синтаксически корректны
2. `grep -c "/healthz" main_api.py` == 1
3. `grep -c "verify_internal_token" app/api/router.py` >= 1
4. `grep -c "start_polling" main_bot.py` == 1
5. `grep -c "AsyncIOScheduler" main_worker.py` == 1
6. Unit тесты (не требуют DB): `uv run pytest tests/test_auth.py -x -q`
   — проверяет AUTH-01 unit (validate_init_data) и AUTH-02 whitelist через dependency_overrides stub
7. Integration тесты (требуют TEST_DATABASE_URL): `uv run pytest tests/test_health.py tests/test_internal_auth.py -x -q`
   — тесты содержат `pytest.skip` если TEST_DATABASE_URL не задан; запускать только при наличии тестовой БД
   — Изоляция: conftest.py подменяет get_db через dependency_overrides, тесты test_health и test_internal_auth
     не обращаются к БД напрямую и должны проходить без PostgreSQL
</verification>

<success_criteria>
- main_api.py: FastAPI с lifespan, /healthz → 200, /api/v1/me с auth, /api/v1/internal/health с internal token
- app/api/router.py: public_router + internal_router с правильными dependencies
- main_bot.py: aiogram polling (D-04) + aiohttp healthz на :8001 (D-12)
- main_worker.py: APScheduler 3.x AsyncIOScheduler + heartbeat → app_health каждые 5 минут
- entrypoint.sh: set -e, alembic upgrade head, exec uvicorn (D-09)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure-and-auth/01-05-SUMMARY.md`
</output>
