---
phase: 01-infrastructure-and-auth
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_auth.py
  - tests/test_health.py
  - tests/test_internal_auth.py
  - tests/test_migrations.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - INF-04
  - INF-05
  - INF-02

must_haves:
  truths:
    - "Тестовый пакет tests/ существует и pytest распознаёт его"
    - "test_auth.py содержит failing-тесты для AUTH-01 и AUTH-02 (RED)"
    - "test_health.py содержит failing-тест /healthz (RED)"
    - "test_internal_auth.py содержит failing-тест /internal/* (RED)"
    - "test_migrations.py содержит failing-тест наличия таблиц (RED)"
    - "conftest.py определяет async_client fixture"
  artifacts:
    - path: "tests/conftest.py"
      provides: "async_client fixture, test settings override"
    - path: "tests/test_auth.py"
      provides: "AUTH-01 и AUTH-02 unit-тесты"
    - path: "tests/test_health.py"
      provides: "INF-05 integration-тест"
    - path: "tests/test_internal_auth.py"
      provides: "INF-04 integration-тест"
    - path: "tests/test_migrations.py"
      provides: "INF-02 integration-тест"
  key_links:
    - from: "tests/conftest.py"
      to: "app.main_api"
      via: "httpx.AsyncClient + ASGITransport"
      pattern: "ASGITransport"
---

<objective>
Wave 0: создать все тестовые файлы-заглушки (RED состояние). Тесты написаны против ещё не существующего кода — они намеренно упадут. Это гарантирует, что последующие планы создают код, а не просто обходят тесты.

Purpose: Nyquist compliance — каждый требования имеет автоматизированный тест до имплементации.
Output: tests/__init__.py, conftest.py, test_auth.py, test_health.py, test_internal_auth.py, test_migrations.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-and-auth/01-VALIDATION.md
@.planning/phases/01-infrastructure-and-auth/01-RESEARCH.md

<interfaces>
<!-- Тесты пишутся против контрактов, которые будут созданы в следующих планах -->

Ожидаемые импорты (после имплементации):
```python
# app.core.auth (Plan 04)
from app.core.auth import validate_init_data

# app.main_api (Plan 05)
from app.main_api import app  # FastAPI instance

# app.core.settings (Plan 02)
from app.core.settings import settings
```

AUTH-01 контракт:
- validate_init_data(init_data_raw: str, bot_token: str) -> dict
- Raises ValueError("Missing hash") если нет hash в params
- Raises ValueError("Invalid hash") если HMAC не совпадает
- Raises ValueError("auth_date expired") если auth_date > 24h назад
- При успехе возвращает dict с полем "id" (tg_user_id)

AUTH-02 контракт:
- GET /api/v1/me с заголовком X-Telegram-Init-Data: <valid data for non-owner> → 403
- GET /api/v1/me с заголовком X-Telegram-Init-Data: <valid data for owner> → 200
- GET /api/v1/me без заголовка → 403

INF-04 контракт:
- GET /api/v1/internal/health без X-Internal-Token → 403
- GET /api/v1/internal/health с правильным X-Internal-Token → 200

INF-05 контракт:
- GET /healthz → 200 {"status": "ok"}

INF-02 контракт (через psycopg2 или asyncpg):
- После alembic upgrade head в БД должны существовать таблицы:
  app_user, category, budget_period, plan_template_item,
  planned_transaction, actual_transaction, subscription, app_health
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать pytest infrastructure и conftest.py</name>
  <read_first>
    - .planning/phases/01-infrastructure-and-auth/01-VALIDATION.md (Wave 0 Requirements)
    - .planning/phases/01-infrastructure-and-auth/01-RESEARCH.md (Pattern 3, Pattern 4, Test Framework)
  </read_first>
  <files>tests/__init__.py, tests/conftest.py</files>
  <behavior>
    - conftest.py определяет `async_client` fixture через httpx.AsyncClient + ASGITransport
    - conftest.py определяет `override_settings` fixture с DEV_MODE=False, BOT_TOKEN="test_token", OWNER_TG_ID=123456789, INTERNAL_TOKEN="test_internal_token"
    - conftest.py определяет вспомогательную функцию `make_init_data(tg_user_id: int, bot_token: str, age_seconds: int = 0) -> str` для генерации валидных initData строк в тестах
  </behavior>
  <action>
Создать `tests/__init__.py` (пустой файл).

Создать `tests/conftest.py`:
```python
import hashlib
import hmac
import json
import time
from urllib.parse import urlencode
import pytest
import pytest_asyncio
from httpx import AsyncClient, ASGITransport


def make_init_data(tg_user_id: int, bot_token: str, age_seconds: int = 0) -> str:
    """Generate valid Telegram initData for testing."""
    auth_date = int(time.time()) - age_seconds
    user_json = json.dumps({"id": tg_user_id, "first_name": "Test"})
    params = {
        "auth_date": str(auth_date),
        "user": user_json,
    }
    data_check_string = "\n".join(f"{k}={v}" for k, v in sorted(params.items()))
    secret_key = hmac.new(b"WebAppData", bot_token.encode(), hashlib.sha256).digest()
    calc_hash = hmac.new(secret_key, data_check_string.encode(), hashlib.sha256).hexdigest()
    params["hash"] = calc_hash
    return urlencode(params)


@pytest.fixture
def bot_token() -> str:
    return "1234567890:test_bot_token_for_testing_only"


@pytest.fixture
def owner_tg_id() -> int:
    return 123456789


@pytest.fixture
def internal_token() -> str:
    return "test_internal_secret_token"


@pytest_asyncio.fixture
async def async_client(bot_token, owner_tg_id, internal_token):
    """HTTP client for FastAPI app with test settings injected via env."""
    import os
    os.environ["BOT_TOKEN"] = bot_token
    os.environ["OWNER_TG_ID"] = str(owner_tg_id)
    os.environ["INTERNAL_TOKEN"] = internal_token
    os.environ["DEV_MODE"] = "false"
    os.environ["DATABASE_URL"] = "postgresql+asyncpg://budget:budget@localhost:5432/budget_test"
    os.environ["DATABASE_URL_SYNC"] = "postgresql://budget:budget@localhost:5432/budget_test"
    os.environ["PUBLIC_DOMAIN"] = "localhost"

    from app.main_api import app
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as client:
        yield client
```

Примечание: импорт `from app.main_api import app` будет падать до создания Plan 05 — это ожидаемое поведение Wave 0 (RED).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('tests/conftest.py').read()); print('conftest.py syntax OK')" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `tests/__init__.py` существует: `test -f tests/__init__.py`
    - `tests/conftest.py` существует: `test -f tests/conftest.py`
    - В conftest.py есть функция make_init_data: `grep -c "def make_init_data" tests/conftest.py` == 1
    - В conftest.py есть async_client fixture: `grep -c "async_client" tests/conftest.py` >= 2
    - В conftest.py используется ASGITransport: `grep -c "ASGITransport" tests/conftest.py` == 1
    - Синтаксис Python корректен: `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` завершается без ошибок
  </acceptance_criteria>
  <done>tests/__init__.py и tests/conftest.py созданы, синтаксис корректен, make_init_data и async_client fixture определены</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать test_auth.py, test_health.py, test_internal_auth.py, test_migrations.py (RED)</name>
  <read_first>
    - tests/conftest.py (только что созданный — fixture names и make_init_data сигнатура)
    - .planning/phases/01-infrastructure-and-auth/01-VALIDATION.md (Per-Task Verification Map)
    - .planning/phases/01-infrastructure-and-auth/01-RESEARCH.md (Security Domain, Pattern 3, Pattern 4, Pattern 5)
    - docs/HLD.md §2 (список 6 таблиц + app_health)
  </read_first>
  <files>tests/test_auth.py, tests/test_health.py, tests/test_internal_auth.py, tests/test_migrations.py</files>
  <behavior>
    - test_auth.py::test_validate_init_data_valid: validate_init_data с корректным HMAC → возвращает dict с id
    - test_auth.py::test_validate_init_data_invalid_hash: validate_init_data с неверным hash → ValueError("Invalid hash")
    - test_auth.py::test_validate_init_data_missing_hash: validate_init_data без hash → ValueError("Missing hash")
    - test_auth.py::test_validate_init_data_expired: validate_init_data с auth_date 25h назад → ValueError("auth_date expired")
    - test_auth.py::test_owner_whitelist_valid: GET /api/v1/me с initData для OWNER_TG_ID → 200
    - test_auth.py::test_owner_whitelist_foreign: GET /api/v1/me с initData для tg_user_id=999 (не owner) → 403
    - test_auth.py::test_no_init_data: GET /api/v1/me без заголовка → 403
    - test_health.py::test_api_healthz: GET /healthz → 200, body contains {"status": "ok"}
    - test_internal_auth.py::test_internal_without_token: GET /api/v1/internal/health без X-Internal-Token → 403
    - test_internal_auth.py::test_internal_with_wrong_token: GET /api/v1/internal/health с неверным токеном → 403
    - test_internal_auth.py::test_internal_with_valid_token: GET /api/v1/internal/health с верным X-Internal-Token → 200
    - test_migrations.py::test_all_tables_exist: после alembic upgrade head все 8 таблиц (6 доменных + app_health + alembic_version) существуют в БД
  </behavior>
  <action>
Создать `tests/test_auth.py`:
```python
import hashlib
import hmac
import json
import time
from urllib.parse import urlencode
import pytest

# Unit tests — не требуют запущенного приложения

def test_validate_init_data_valid(bot_token, owner_tg_id):
    from app.core.auth import validate_init_data
    from tests.conftest import make_init_data
    init_data = make_init_data(tg_user_id=owner_tg_id, bot_token=bot_token)
    result = validate_init_data(init_data, bot_token)
    assert result["id"] == owner_tg_id

def test_validate_init_data_invalid_hash(bot_token):
    from app.core.auth import validate_init_data
    init_data = "auth_date=1000000000&user=%7B%22id%22%3A1%7D&hash=deadbeef"
    with pytest.raises(ValueError, match="Invalid hash"):
        validate_init_data(init_data, bot_token)

def test_validate_init_data_missing_hash(bot_token):
    from app.core.auth import validate_init_data
    init_data = "auth_date=1000000000&user=%7B%22id%22%3A1%7D"
    with pytest.raises(ValueError, match="Missing hash"):
        validate_init_data(init_data, bot_token)

def test_validate_init_data_expired(bot_token, owner_tg_id):
    from app.core.auth import validate_init_data
    from tests.conftest import make_init_data
    # 25 hours ago = 90000 seconds
    init_data = make_init_data(tg_user_id=owner_tg_id, bot_token=bot_token, age_seconds=90000)
    with pytest.raises(ValueError, match="auth_date expired"):
        validate_init_data(init_data, bot_token)

@pytest.mark.asyncio
async def test_owner_whitelist_valid(async_client, bot_token, owner_tg_id):
    from tests.conftest import make_init_data
    init_data = make_init_data(tg_user_id=owner_tg_id, bot_token=bot_token)
    response = await async_client.get(
        "/api/v1/me",
        headers={"X-Telegram-Init-Data": init_data}
    )
    assert response.status_code == 200

@pytest.mark.asyncio
async def test_owner_whitelist_foreign(async_client, bot_token):
    from tests.conftest import make_init_data
    init_data = make_init_data(tg_user_id=999999, bot_token=bot_token)
    response = await async_client.get(
        "/api/v1/me",
        headers={"X-Telegram-Init-Data": init_data}
    )
    assert response.status_code == 403

@pytest.mark.asyncio
async def test_no_init_data(async_client):
    response = await async_client.get("/api/v1/me")
    assert response.status_code == 403
```

Создать `tests/test_health.py`:
```python
import pytest

@pytest.mark.asyncio
async def test_api_healthz(async_client):
    response = await async_client.get("/healthz")
    assert response.status_code == 200
    data = response.json()
    assert data.get("status") == "ok"
```

Создать `tests/test_internal_auth.py`:
```python
import pytest

@pytest.mark.asyncio
async def test_internal_without_token(async_client):
    response = await async_client.get("/api/v1/internal/health")
    assert response.status_code == 403

@pytest.mark.asyncio
async def test_internal_with_wrong_token(async_client):
    response = await async_client.get(
        "/api/v1/internal/health",
        headers={"X-Internal-Token": "wrong_token"}
    )
    assert response.status_code == 403

@pytest.mark.asyncio
async def test_internal_with_valid_token(async_client, internal_token):
    response = await async_client.get(
        "/api/v1/internal/health",
        headers={"X-Internal-Token": internal_token}
    )
    assert response.status_code == 200
```

Создать `tests/test_migrations.py`:
```python
"""
Integration test: проверяет что после alembic upgrade head все таблицы существуют.
Требует запущенного PostgreSQL и переменной DATABASE_URL.
Запускается отдельно: uv run pytest tests/test_migrations.py -x
"""
import pytest
import os

EXPECTED_TABLES = {
    "app_user",
    "category",
    "budget_period",
    "plan_template_item",
    "planned_transaction",
    "actual_transaction",
    "subscription",
    "app_health",
}

@pytest.mark.asyncio
async def test_all_tables_exist():
    """После alembic upgrade head все 8 таблиц должны существовать."""
    from sqlalchemy.ext.asyncio import create_async_engine
    from sqlalchemy import text

    db_url = os.environ.get("DATABASE_URL", "")
    if not db_url:
        pytest.skip("DATABASE_URL not set — skipping migration test")

    engine = create_async_engine(db_url, echo=False)
    async with engine.connect() as conn:
        result = await conn.execute(text(
            "SELECT tablename FROM pg_tables WHERE schemaname = 'public'"
        ))
        existing = {row[0] for row in result}

    await engine.dispose()

    missing = EXPECTED_TABLES - existing
    assert not missing, f"Missing tables after migration: {missing}"
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && for f in tests/test_auth.py tests/test_health.py tests/test_internal_auth.py tests/test_migrations.py; do python -c "import ast; ast.parse(open('$f').read()); print('$f syntax OK')"; done 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_auth.py существует с 7 тест-функциями: `grep -c "^def test_\|^async def test_" tests/test_auth.py` == 7
    - tests/test_health.py существует: `grep -c "def test_api_healthz" tests/test_health.py` == 1
    - tests/test_internal_auth.py существует с 3 тест-функциями: `grep -c "^async def test_" tests/test_internal_auth.py` == 3
    - tests/test_migrations.py существует, содержит EXPECTED_TABLES с 8 именами: `grep -c "\"app_user\"" tests/test_migrations.py` == 1 и `grep -c "\"app_health\"" tests/test_migrations.py` == 1
    - Все 4 файла имеют корректный синтаксис Python (проверить ast.parse)
    - test_auth.py использует compare_digest через make_init_data (а не raw ==): `grep -c "make_init_data" tests/test_auth.py` >= 3
    - test_auth.py проверяет AUTH-02 whitelist для non-owner id=999999: `grep -c "999999" tests/test_auth.py` == 1
  </acceptance_criteria>
  <done>4 тестовых файла созданы в RED состоянии (падают из-за отсутствующих модулей app.*), синтаксис корректен, покрыты AUTH-01, AUTH-02, INF-04, INF-05, INF-02</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app | Тесты инжектируют ENV-переменные напрямую; в prod ENV поступает из .env файла |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-wave0-01 | Spoofing | tests/conftest.py make_init_data | accept | Функция генерирует валидный HMAC — это намеренно, для тестирования happy-path. Реальный bot_token в тестах не используется, только фиктивный. |
| T-wave0-02 | Info Disclosure | tests/ ENV injection | accept | Тестовые ENV-переменные содержат фиктивные значения (не реальные токены). Реальный .env не читается в тестах. |
</threat_model>

<verification>
Wave 0 считается завершённым когда:
1. `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` — OK
2. Все 4 test-файла существуют и имеют корректный синтаксис
3. Тесты *падают* с ImportError или ModuleNotFoundError (ожидаемое RED состояние до Plan 02-05)
</verification>

<success_criteria>
- tests/__init__.py, tests/conftest.py, tests/test_auth.py, tests/test_health.py, tests/test_internal_auth.py, tests/test_migrations.py созданы
- Все файлы синтаксически корректны
- conftest.py содержит make_init_data с корректной HMAC-SHA256 логикой
- Тесты покрывают все 7 requirements IDs (через behavioral expectations)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-infrastructure-and-auth/01-01-SUMMARY.md`
</output>
