---
phase: 06-subscriptions-worker-jobs
plan: 05
type: execute
wave: 3
depends_on: [06-03]
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/subscriptions.ts
  - frontend/src/hooks/useSubscriptions.ts
  - frontend/src/components/SubscriptionEditor.tsx
  - frontend/src/components/SubscriptionEditor.module.css
autonomous: true
requirements: [SUB-01, SUB-04, SUB-05]
must_haves:
  truths:
    - "TS-типы SubscriptionRead/Create/Update/SubCycle/ChargeNowResponse экспортируются"
    - "API-функции listSubscriptions/createSubscription/updateSubscription/deleteSubscription/chargeNow"
    - "Хук useSubscriptions возвращает {subscriptions, loading, error, refetch, mutate}"
    - "SubscriptionEditor работает в create + edit mode (по образцу ActualEditor)"
    - "SubscriptionEditor рендерит все 7 полей: name, amount, cycle, next_charge_date, category, notify_days_before, is_active (edit only)"
  artifacts:
    - path: "frontend/src/api/types.ts"
      provides: "TS-типы подписок (extended)"
      contains: "SubscriptionRead"
    - path: "frontend/src/api/subscriptions.ts"
      provides: "5 API-функций"
      exports: ["listSubscriptions", "createSubscription", "updateSubscription", "deleteSubscription", "chargeNow"]
    - path: "frontend/src/hooks/useSubscriptions.ts"
      provides: "useSubscriptions hook"
      exports: ["useSubscriptions"]
    - path: "frontend/src/components/SubscriptionEditor.tsx"
      provides: "Bottom-sheet форма create/edit с 7 полями"
      exports: ["SubscriptionEditor"]
  key_links:
    - from: "frontend/src/api/subscriptions.ts"
      to: "apiFetch helper"
      via: "import { apiFetch } from './client'"
      pattern: "apiFetch.*subscriptions"
    - from: "frontend/src/components/SubscriptionEditor.tsx"
      to: "useCategories hook"
      via: "select категории из useCategories"
      pattern: "useCategories"
---

<objective>
Wave 3 (параллельно с 06-04): frontend data-layer — типы, API-клиенты, хук и форма-компонент.
Per D-82 (частично — Editor) / D-83 / D-85 / D-86. Без screens (это в 06-06).

Purpose: подготовить переиспользуемые building blocks для SubscriptionsScreen в 06-06.

Output: typed API + form-компонент готовы; ничего не отрендерено в App.tsx (это в 06-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-subscriptions-worker-jobs/06-CONTEXT.md
@.planning/phases/06-subscriptions-worker-jobs/06-UI-SPEC.md
@frontend/src/api/types.ts
@frontend/src/api/client.ts
@frontend/src/api/planned.ts
@frontend/src/api/actual.ts
@frontend/src/hooks/useActualTransactions.ts
@frontend/src/hooks/useCategories.ts
@frontend/src/components/ActualEditor.tsx
@frontend/src/components/BottomSheet.tsx

<interfaces>
# Existing API client pattern (frontend/src/api/planned.ts):
import { apiFetch } from './client';

export async function listPlanned(periodId: number): Promise<PlannedRead[]> {
  return apiFetch(`/planned?period_id=${periodId}`);
}

# Existing hook pattern (frontend/src/hooks/useActualTransactions.ts):
export function useActualTransactions(periodId) {
  const [items, setItems] = useState([]);
  const [loading, setLoading] = useState(false);
  const refetch = useCallback(async () => { ... });
  const mutate = useCallback(async (op) => { await op(); await refetch(); });
  return { items, loading, error, refetch, mutate };
}

# ActualEditor pattern (frontend/src/components/ActualEditor.tsx):
interface Props {
  mode: 'create' | 'edit';
  initial?: ActualRead;
  onClose: () => void;
  onSubmit: (payload) => Promise<void>;
  onDelete?: () => Promise<void>;
}
// BottomSheet wrapper, form fields, submit button, delete button (edit mode)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TS-типы + API-функции (D-85, D-86)</name>
  <files>frontend/src/api/types.ts, frontend/src/api/subscriptions.ts</files>
  <behavior>
    - SubCycle = 'monthly' | 'yearly'
    - SubscriptionRead с category nested
    - 5 API functions с правильными signatures
  </behavior>
  <action>
    1. В `frontend/src/api/types.ts` добавить (рядом с другими типами):
       ```typescript
       export type SubCycle = 'monthly' | 'yearly';

       export interface SubscriptionRead {
         id: number;
         name: string;
         amount_cents: number;
         cycle: SubCycle;
         next_charge_date: string;  // ISO date
         category_id: number;
         notify_days_before: number;
         is_active: boolean;
         category: CategoryRead;
       }

       export interface SubscriptionCreatePayload {
         name: string;
         amount_cents: number;
         cycle: SubCycle;
         next_charge_date: string;
         category_id: number;
         notify_days_before?: number;
         is_active?: boolean;
       }

       export interface SubscriptionUpdatePayload {
         name?: string;
         amount_cents?: number;
         cycle?: SubCycle;
         next_charge_date?: string;
         category_id?: number;
         notify_days_before?: number;
         is_active?: boolean;
       }

       export interface ChargeNowResponse {
         planned_id: number;
         next_charge_date: string;
       }
       ```

    2. Создать `frontend/src/api/subscriptions.ts` per D-85:
       ```typescript
       import { apiFetch } from './client';
       import type { SubscriptionRead, SubscriptionCreatePayload, SubscriptionUpdatePayload, ChargeNowResponse } from './types';

       export async function listSubscriptions(): Promise<SubscriptionRead[]> {
         return apiFetch('/subscriptions');
       }
       export async function createSubscription(payload: SubscriptionCreatePayload): Promise<SubscriptionRead> {
         return apiFetch('/subscriptions', { method: 'POST', body: JSON.stringify(payload) });
       }
       export async function updateSubscription(id: number, payload: SubscriptionUpdatePayload): Promise<SubscriptionRead> {
         return apiFetch(`/subscriptions/${id}`, { method: 'PATCH', body: JSON.stringify(payload) });
       }
       export async function deleteSubscription(id: number): Promise<void> {
         await apiFetch(`/subscriptions/${id}`, { method: 'DELETE' });
       }
       export async function chargeNow(id: number): Promise<ChargeNowResponse> {
         return apiFetch(`/subscriptions/${id}/charge-now`, { method: 'POST' });
       }
       ```

    3. Также расширить SettingsRead/SettingsUpdate в types.ts добавлением `notify_days_before` per D-77 на frontend стороне.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript компилируется без ошибок. Импорты из subscriptions.ts работают.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useSubscriptions хук (D-85)</name>
  <files>frontend/src/hooks/useSubscriptions.ts</files>
  <behavior>
    - Загружает list при mount
    - refetch перезапрашивает
    - mutate(fn) выполняет операцию + refetch
    - error / loading state
  </behavior>
  <action>
    Создать `frontend/src/hooks/useSubscriptions.ts` копируя pattern из `useActualTransactions.ts`:
    ```typescript
    import { useCallback, useEffect, useState } from 'react';
    import { listSubscriptions } from '../api/subscriptions';
    import type { SubscriptionRead } from '../api/types';

    export function useSubscriptions() {
      const [subscriptions, setSubscriptions] = useState<SubscriptionRead[]>([]);
      const [loading, setLoading] = useState(false);
      const [error, setError] = useState<string | null>(null);

      const refetch = useCallback(async () => {
        setLoading(true); setError(null);
        try {
          setSubscriptions(await listSubscriptions());
        } catch (e: any) {
          setError(e?.message ?? 'load failed');
        } finally {
          setLoading(false);
        }
      }, []);

      const mutate = useCallback(async <T,>(op: () => Promise<T>): Promise<T> => {
        const r = await op();
        await refetch();
        return r;
      }, [refetch]);

      useEffect(() => { void refetch(); }, [refetch]);

      return { subscriptions, loading, error, refetch, mutate };
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>
    Hook компилируется, имеет правильные return types.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SubscriptionEditor.tsx + CSS (D-83) — все 7 полей</name>
  <files>frontend/src/components/SubscriptionEditor.tsx, frontend/src/components/SubscriptionEditor.module.css</files>
  <behavior>
    - BottomSheet wrapper
    - 7 полей рендерятся (по образцу ActualEditor):
      1. **Name input** — `<input type="text">` controlled, placeholder "Название", min/max length 1..255
      2. **Amount input** — `<input type="text" inputMode="decimal">` контролируется, форматирует ввод (запятая/точка), при submit конвертируется в копейки через `Math.round(parseFloat(...)*100)`
      3. **Cycle segmented control** — 2 кнопки `[Месяц | Год]` styled как ActualEditor segmented (active кнопка с accent-фоном, click меняет state)
      4. **next_charge_date date picker** — `<input type="date">` controlled, default = today (create) или initial.next_charge_date (edit), ISO формат `YYYY-MM-DD`
      5. **Category select** — `<select>` или custom dropdown с options из `useCategories({ includeArchived: false })`, отрендеренные по `name`, ключ `id`
      6. **notify_days_before number input** — `<input type="number" min="0" max="30">`, label «Уведомить за N дней до списания», default = `defaultNotifyDays` prop
      7. **is_active toggle** — `<input type="checkbox">` или toggle-switch (только edit mode; в create mode is_active всегда true)
    - Submit-кнопка disabled при `busy || !name.trim() || !categoryId || !amountRub`
    - Delete-кнопка только в edit mode, с `confirm('Удалить подписку?')` и onDelete handler
    - Error banner при `err !== null`
  </behavior>
  <action>
    Скопировать структуру `frontend/src/components/ActualEditor.tsx` и адаптировать под подписки.
    Полная реализация (все 7 полей рендерятся явно — не комментарием!):

    ```typescript
    import { useState } from 'react';
    import { BottomSheet } from './BottomSheet';
    import { useCategories } from '../hooks/useCategories';
    import type { SubscriptionRead, SubscriptionCreatePayload, SubscriptionUpdatePayload, SubCycle } from '../api/types';
    import styles from './SubscriptionEditor.module.css';

    interface Props {
      mode: 'create' | 'edit';
      initial?: SubscriptionRead;
      defaultNotifyDays: number;  // из useSettings
      onClose: () => void;
      onSubmit: (payload: SubscriptionCreatePayload | SubscriptionUpdatePayload) => Promise<void>;
      onDelete?: () => Promise<void>;
    }

    export function SubscriptionEditor({ mode, initial, defaultNotifyDays, onClose, onSubmit, onDelete }: Props) {
      const { categories } = useCategories({ includeArchived: false });
      const [name, setName] = useState(initial?.name ?? '');
      const [amountRub, setAmountRub] = useState(initial ? (initial.amount_cents / 100).toString() : '');
      const [cycle, setCycle] = useState<SubCycle>(initial?.cycle ?? 'monthly');
      const [chargeDate, setChargeDate] = useState(initial?.next_charge_date ?? new Date().toISOString().slice(0, 10));
      const [categoryId, setCategoryId] = useState<number | null>(initial?.category_id ?? null);
      const [notifyDays, setNotifyDays] = useState(initial?.notify_days_before ?? defaultNotifyDays);
      const [isActive, setIsActive] = useState(initial?.is_active ?? true);
      const [busy, setBusy] = useState(false);
      const [err, setErr] = useState<string | null>(null);

      const handleSubmit = async () => {
        if (!name.trim() || !categoryId || !amountRub) { setErr('заполните обязательные поля'); return; }
        setBusy(true); setErr(null);
        try {
          const cents = Math.round(parseFloat(amountRub.replace(',', '.')) * 100);
          const payload: SubscriptionCreatePayload = {
            name: name.trim(), amount_cents: cents, cycle,
            next_charge_date: chargeDate, category_id: categoryId,
            notify_days_before: notifyDays, is_active: isActive,
          };
          await onSubmit(payload);
          onClose();
        } catch (e: any) {
          setErr(e?.message ?? 'ошибка');
        } finally { setBusy(false); }
      };

      const handleDelete = async () => {
        if (!confirm('Удалить подписку?')) return;
        setBusy(true);
        try { await onDelete!(); onClose(); }
        finally { setBusy(false); }
      };

      return (
        <BottomSheet open onClose={onClose} title={mode === 'create' ? 'Новая подписка' : 'Подписка'}>
          <div className={styles.form}>
            {err && <div className={styles.error}>{err}</div>}

            {/* 1. Name */}
            <label className={styles.field}>
              <span className={styles.label}>Название</span>
              <input
                type="text"
                value={name}
                onChange={(e) => setName(e.target.value)}
                placeholder="Например, Netflix"
                maxLength={255}
                className={styles.input}
              />
            </label>

            {/* 2. Amount (rub → cents on submit) */}
            <label className={styles.field}>
              <span className={styles.label}>Сумма, ₽</span>
              <input
                type="text"
                inputMode="decimal"
                value={amountRub}
                onChange={(e) => setAmountRub(e.target.value)}
                placeholder="0,00"
                className={styles.input}
              />
            </label>

            {/* 3. Cycle segmented control (monthly | yearly) */}
            <div className={styles.field}>
              <span className={styles.label}>Цикл</span>
              <div className={styles.segmented}>
                <button
                  type="button"
                  className={cycle === 'monthly' ? styles.segActive : styles.seg}
                  onClick={() => setCycle('monthly')}
                >Месяц</button>
                <button
                  type="button"
                  className={cycle === 'yearly' ? styles.segActive : styles.seg}
                  onClick={() => setCycle('yearly')}
                >Год</button>
              </div>
            </div>

            {/* 4. next_charge_date date picker */}
            <label className={styles.field}>
              <span className={styles.label}>Следующее списание</span>
              <input
                type="date"
                value={chargeDate}
                onChange={(e) => setChargeDate(e.target.value)}
                className={styles.input}
              />
            </label>

            {/* 5. Category select */}
            <label className={styles.field}>
              <span className={styles.label}>Категория</span>
              <select
                value={categoryId ?? ''}
                onChange={(e) => setCategoryId(e.target.value ? Number(e.target.value) : null)}
                className={styles.input}
              >
                <option value="">— выбрать —</option>
                {categories.map((c) => (
                  <option key={c.id} value={c.id}>{c.name}</option>
                ))}
              </select>
            </label>

            {/* 6. notify_days_before number input */}
            <label className={styles.field}>
              <span className={styles.label}>Уведомить за N дней</span>
              <input
                type="number"
                min={0}
                max={30}
                value={notifyDays}
                onChange={(e) => setNotifyDays(Math.max(0, Math.min(30, Number(e.target.value) || 0)))}
                className={styles.input}
              />
            </label>

            {/* 7. is_active toggle (edit mode only — create всегда true) */}
            {mode === 'edit' && (
              <label className={`${styles.field} ${styles.toggleRow}`}>
                <span className={styles.label}>Активна</span>
                <input
                  type="checkbox"
                  checked={isActive}
                  onChange={(e) => setIsActive(e.target.checked)}
                  className={styles.toggle}
                />
              </label>
            )}

            {/* Action buttons */}
            <div className={styles.actions}>
              <button
                type="button"
                onClick={handleSubmit}
                disabled={busy || !name.trim() || !categoryId || !amountRub}
                className={styles.primary}
              >
                {mode === 'create' ? 'Создать' : 'Сохранить'}
              </button>
              {mode === 'edit' && onDelete && (
                <button
                  type="button"
                  onClick={handleDelete}
                  disabled={busy}
                  className={styles.danger}
                >Удалить</button>
              )}
            </div>
          </div>
        </BottomSheet>
      );
    }
    ```

    CSS-модуль `SubscriptionEditor.module.css` — переиспользовать токены из `ActualEditor.module.css`
    (form layout, segmented buttons с `var(--c-accent)` для active, toggle-switch стили,
    `.primary`/`.danger` кнопки). Скопировать селекторы `.form`, `.field`, `.label`, `.input`,
    `.segmented`, `.seg`, `.segActive`, `.actions`, `.primary`, `.danger`, `.error`, `.toggleRow`,
    `.toggle` — структура идентична ActualEditor с минимальными корректировками под 7 полей.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -20 && grep -c -E 'type="text"|type="date"|type="number"|type="checkbox"|<select' frontend/src/components/SubscriptionEditor.tsx</automated>
  </verify>
  <done>
    Component компилируется, build успешен. Импорт компонента доступен для 06-06.
    grep подтверждает наличие всех типов inputs: text (name + amount), date (chargeDate),
    number (notifyDays), checkbox (isActive), select (category) — итого ≥5 матчей разных типов.
    Cycle рендерится как 2 button-элемента (segmented control). Все 7 полей видимы в JSX.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → API | Все запросы через apiFetch, который добавляет initData header |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | amount input | mitigate | parseFloat + Math.round; backend Pydantic gt=0 — двойная защита |
| T-06-11 | Information Disclosure | API error messages | accept | single-tenant, error содержит только domain message |
</threat_model>

<verification>
- `npx tsc --noEmit` без ошибок
- `npm run build` без ошибок
- Импорты SubscriptionEditor и useSubscriptions доступны
- Все 7 полей формы рендерятся (name/amount/cycle/date/category/notify/is_active)
</verification>

<success_criteria>
Frontend foundation готов. 06-06 может рендерить SubscriptionsScreen используя только готовые API/hooks/components.
</success_criteria>

<output>
Создать `.planning/phases/06-subscriptions-worker-jobs/06-05-SUMMARY.md`:
- Описание API surface (5 функций + хук)
- Описание SubscriptionEditor props/behaviour + список 7 рендерящихся полей
- Заметки для 06-06: какие компоненты переиспользовать
</output>
