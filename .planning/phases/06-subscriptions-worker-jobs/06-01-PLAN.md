---
phase: 06-subscriptions-worker-jobs
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_subscriptions.py
autonomous: true
requirements: [SUB-01, SUB-04, SET-02]
must_haves:
  truths:
    - "RED-фаза: тесты CRUD подписок написаны и падают (модули/роуты ещё не существуют)"
    - "RED-фаза: тест charge-now 409 на повторный вызов написан и падает"
    - "RED-фаза: тесты Settings.notify_days_before (GET + PATCH) написаны и падают"
  artifacts:
    - path: "tests/test_subscriptions.py"
      provides: "Pytest-набор RED-тестов для SUB-01/SUB-04/SET-02"
      contains: "_require_db"
  key_links:
    - from: "tests/test_subscriptions.py"
      to: "app/api/routes/subscriptions.py (будущий)"
      via: "httpx AsyncClient → /api/v1/subscriptions"
      pattern: "client\\.(get|post|patch|delete).*subscriptions"
---

<objective>
Wave 0 RED gate per D-87. Создать pytest-файл `tests/test_subscriptions.py`,
который покрывает все основные сценарии CRUD подписок, charge-now идемпотентность
и Settings notify_days_before. Тесты ОБЯЗАНЫ падать (модули и роуты ещё не существуют) —
это валидно для RED-фазы. После реализации в плане 06-02/06-03 они пойдут зелёными.

Purpose: TDD-контракт для backend-волны. Executor 06-02/06-03 строит код против этих тестов,
гарантируя что всё behaviorial-требование зафиксировано до имплементации.

Output: один файл с 12+ тест-функциями в self-skip pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-subscriptions-worker-jobs/06-CONTEXT.md
@tests/test_planned.py
@tests/test_actual.py
@tests/test_settings.py
@app/db/models.py
@app/api/schemas/categories.py

<interfaces>
# Существующие тестовые конвенции (из tests/test_*.py phase 2-5):

```python
import pytest
from httpx import AsyncClient

pytestmark = pytest.mark.asyncio

async def _require_db():
    """Self-skip if DB unavailable (CI без Postgres)."""
    try:
        from app.db.session import AsyncSessionLocal
        async with AsyncSessionLocal() as s:
            await s.execute(text("SELECT 1"))
    except Exception:
        pytest.skip("Postgres unavailable")
```

# AppUser.tg_chat_id и Subscription модель (из app/db/models.py):
class Subscription(Base):
    id: Mapped[int]
    name: Mapped[str]
    amount_cents: Mapped[int]
    cycle: Mapped[SubCycle]  # 'monthly' | 'yearly'
    next_charge_date: Mapped[date]
    category_id: Mapped[int]
    notify_days_before: Mapped[int]
    is_active: Mapped[bool]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Написать RED-тесты CRUD подписок + charge-now (SUB-01, SUB-04)</name>
  <files>tests/test_subscriptions.py</files>
  <behavior>
    - test_create_subscription: POST /subscriptions с {name, amount_cents, cycle, next_charge_date, category_id} → 200 + id, default notify_days_before берётся из user setting
    - test_list_subscriptions: GET /subscriptions → отсортированный по next_charge_date ASC
    - test_update_subscription: PATCH /subscriptions/{id} с partial → 200, поля обновлены
    - test_delete_subscription: DELETE /subscriptions/{id} → 204, последующий GET 404
    - test_create_archived_category_400: создание подписки с архивной категорией → 400
    - test_charge_now_creates_planned: POST /subscriptions/{id}/charge-now → создаёт PlannedTransaction(source=subscription_auto), сдвигает next_charge_date (+1 mo для monthly)
    - test_charge_now_yearly_advance: cycle=yearly → +1 year
    - test_charge_now_409_on_duplicate: повторный вызов в тот же день (после восстановления next_charge_date в фикстуре) → 409
    - test_subscriptions_auth_403: запрос без X-Telegram-Init-Data → 403
  </behavior>
  <action>
    Создать `tests/test_subscriptions.py` per D-87. Использовать httpx AsyncClient через ASGI transport (тот же паттерн что tests/test_planned.py, tests/test_actual.py). Все тесты обёрнуты в `await _require_db()` для self-skip. Фикстуры:
    - `_test_init_data()` — re-use existing helper из tests/conftest.py или скопировать паттерн (HMAC-подписанный mock initData)
    - Создание тестовой category (kind=expense, не archived) для category_id
    - Cleanup: после каждого теста truncate subscription, planned_transaction (через session.execute)

    Импорты: `from datetime import date, timedelta`. Используем `date.today() + timedelta(days=10)` для next_charge_date чтобы не пересекаться с реальной датой системы.

    Per D-87: тесты ОБЯЗАНЫ падать сейчас (роутер не существует) — это валидно для Wave 0.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && pytest tests/test_subscriptions.py -x --tb=short 2>&1 | head -60</automated>
  </verify>
  <done>
    Файл создан. Запуск pytest показывает 9+ тестов, все падают/skipped (404 на роуты или ImportError на сервисы — это OK для RED). Если DB недоступна — все skipped с reason "Postgres unavailable".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Расширить test_settings.py + добавить notify_days_before тесты (SET-02)</name>
  <files>tests/test_subscriptions.py</files>
  <behavior>
    - test_get_settings_includes_notify_days_before: GET /settings → response содержит `notify_days_before: int` (default 2)
    - test_patch_settings_notify_days_before: PATCH /settings {notify_days_before: 5} → 200, GET снова → 5
    - test_patch_settings_notify_validation: PATCH с notify_days_before=-1 → 422; с 100 → 422 (range 0..30)
    - test_patch_settings_partial: PATCH только cycle_start_day → не затирает notify_days_before
  </behavior>
  <action>
    В тот же `tests/test_subscriptions.py` (либо отдельный класс TestSettingsExtension) добавить 4 теста для D-77. Можно вынести в отдельную секцию `# --- Settings extension tests ---`. Использовать тот же `_require_db()` self-skip и existing helper для initData.

    Per D-87 — тесты падают сейчас (поле notify_days_before отсутствует в схеме, в response модели нет). Это RED-фаза.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && pytest tests/test_subscriptions.py -k settings -x --tb=short 2>&1 | head -40</automated>
  </verify>
  <done>
    4 теста про Settings присутствуют в файле, падают (KeyError 'notify_days_before' / 422 на валидную PATCH / Pydantic validation отсутствует). RED-фаза валидна.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_subscriptions.py --collect-only` показывает 12+ тестов
- Все тесты падают (RED), это ожидаемо до реализации в волне 1+
- При наличии DB ни один тест не зелёный (если зелёный — что-то реализовано преждевременно)
- При отсутствии DB — все skipped
</verification>

<success_criteria>
RED gate активирован: 12+ тестов написаны, все падают/skipped, executor 06-02/06-03 имеет точный контракт для имплементации.
</success_criteria>

<output>
После завершения создать `.planning/phases/06-subscriptions-worker-jobs/06-01-SUMMARY.md` с:
- Список созданных тестов с кратким описанием каждого
- Подтверждение RED-статуса (вывод pytest)
- Заметки для следующих executor'ов: какие endpoint paths/contracts фиксированы тестами
</output>
