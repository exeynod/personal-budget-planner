---
phase: 06-subscriptions-worker-jobs
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - app/db/models.py
  - alembic/versions/0002_add_notify_days_before.py
  - app/api/schemas/subscriptions.py
  - app/api/schemas/settings.py
  - app/services/subscriptions.py
  - app/services/settings.py
autonomous: true
requirements: [SUB-01, SUB-04, SUB-05, SET-02]
must_haves:
  truths:
    - "AppUser.notify_days_before колонка существует в БД (default=2, NOT NULL)"
    - "uq_planned_sub_charge_date constraint существует в live БД на planned_transaction (SUB-05 enforcement)"
    - "Pydantic-схемы SubscriptionCreate/Update/Read валидируют поля и диапазоны"
    - "SettingsRead/Update содержат notify_days_before (Optional на update)"
    - "service-функции subscriptions: list/create/update/delete/charge_subscription существуют"
    - "_advance_charge_date корректно сдвигает дату для monthly/yearly"
    - "AlreadyChargedError raised при duplicate (subscription_id, original_charge_date)"
  artifacts:
    - path: "app/db/models.py"
      provides: "AppUser.notify_days_before (Mapped[int] default=2)"
      contains: "notify_days_before"
    - path: "alembic/versions/0002_add_notify_days_before.py"
      provides: "Миграция ALTER TABLE app_user ADD COLUMN notify_days_before INTEGER NOT NULL DEFAULT 2"
      contains: "notify_days_before"
    - path: "app/api/schemas/subscriptions.py"
      provides: "Pydantic models SubscriptionCreate, SubscriptionUpdate, SubscriptionRead, ChargeNowResponse"
      exports: ["SubscriptionCreate", "SubscriptionUpdate", "SubscriptionRead", "ChargeNowResponse"]
    - path: "app/api/schemas/settings.py"
      provides: "SettingsRead.notify_days_before, SettingsUpdate.notify_days_before"
      contains: "notify_days_before"
    - path: "app/services/subscriptions.py"
      provides: "list/create/update/delete/charge_subscription + _advance_charge_date + AlreadyChargedError"
      exports: ["list_subscriptions", "create_subscription", "update_subscription", "delete_subscription", "charge_subscription", "AlreadyChargedError"]
    - path: "app/services/settings.py"
      provides: "get_notify_days_before, update_notify_days_before"
      exports: ["get_notify_days_before", "update_notify_days_before"]
  key_links:
    - from: "app/services/subscriptions.py"
      to: "app/db/models.py:Subscription / PlannedTransaction"
      via: "SQLAlchemy session.execute / session.add"
      pattern: "INSERT.*PlannedTransaction|select\\(Subscription\\)"
    - from: "app/services/subscriptions.py:charge_subscription"
      to: "app/services/actual.py:_resolve_period_for_date"
      via: "import + call для определения period_id"
      pattern: "_resolve_period_for_date"
    - from: "alembic upgrade head"
      to: "uq_planned_sub_charge_date constraint в planned_transaction"
      via: "live DB check via information_schema"
      pattern: "uq_planned_sub_charge_date"
---

<objective>
Wave 1: создать БД-колонку, Pydantic-схемы и сервисный слой для подписок и notify_days_before
без HTTP-роутов (роуты в 06-03). Per D-72/D-73/D-74/D-75/D-76.

Purpose: чистая отделимая foundation-волна. После 06-02 unit-тесты сервисов могут запускаться, но
HTTP-тесты из 06-01 ещё не зелёные (нет роутера) — это ожидаемо.

Output: миграция применяется, схемы валидируются, сервисы вызываются изолированно.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-subscriptions-worker-jobs/06-CONTEXT.md
@app/db/models.py
@app/api/schemas/settings.py
@app/api/schemas/categories.py
@app/services/settings.py
@app/services/actual.py
@app/services/planned.py
@alembic/versions/

<interfaces>
# Существующая Subscription модель (app/db/models.py:139-156):
class Subscription(Base):
    __tablename__ = "subscription"
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(255))
    amount_cents: Mapped[int] = mapped_column(BigInteger)
    cycle: Mapped[SubCycle] = mapped_column(Enum(SubCycle, name="sub_cycle"))
    next_charge_date: Mapped[date] = mapped_column(Date)
    category_id: Mapped[int] = mapped_column(ForeignKey("category.id"))
    notify_days_before: Mapped[int] = mapped_column(Integer, default=2)
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)

# PlannedTransaction unique constraint (уже в схеме):
UniqueConstraint("subscription_id", "original_charge_date", name="uq_planned_sub_charge_date")

# Существующий _resolve_period_for_date (app/services/actual.py):
async def _resolve_period_for_date(db, tx_date: date, *, cycle_start_day: int) -> int:
    """Returns period_id, creates period if needed."""

# Existing settings service pattern:
async def get_cycle_start_day(db, tg_user_id: int) -> int:
    user = await db.scalar(select(AppUser).where(AppUser.tg_user_id == tg_user_id))
    return user.cycle_start_day if user else 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppUser.notify_days_before + Alembic миграция + SUB-05 constraint check (D-75)</name>
  <files>app/db/models.py, alembic/versions/0002_add_notify_days_before.py</files>
  <behavior>
    - AppUser в models.py получает `notify_days_before: Mapped[int] = mapped_column(Integer, default=2, nullable=False)`
    - Миграция 0002 применяется: `alembic upgrade head` без ошибок
    - Колонка существует в БД с дефолтом 2
    - После `alembic upgrade head` constraint `uq_planned_sub_charge_date` существует на planned_transaction (SUB-05 — enforcement идемпотентности charge job)
  </behavior>
  <action>
    1. Открыть `app/db/models.py`, найти класс `AppUser`. После существующего `cycle_start_day` поля добавить:
       ```python
       notify_days_before: Mapped[int] = mapped_column(Integer, default=2, nullable=False, server_default="2")
       ```
       `server_default="2"` важен — без него существующие строки получат NULL при ALTER.

    2. Создать `alembic/versions/0002_add_notify_days_before.py` (manual revision per D-75) с прямым ALTER:
       ```python
       """add notify_days_before to app_user"""
       revision = "0002_add_notify_days_before"
       down_revision = "0001_..."  # узнать из alembic/versions/

       def upgrade():
           op.add_column("app_user", sa.Column("notify_days_before", sa.Integer(), nullable=False, server_default="2"))

       def downgrade():
           op.drop_column("app_user", "notify_days_before")
       ```
       Узнать `down_revision` через `ls alembic/versions/`.

    3. Запустить `alembic upgrade head` локально (если БД доступна) — иначе только синтаксис.

    4. **SUB-05 constraint verification (live DB check)**: После применения миграции выполнить SQL и
       убедиться что `uq_planned_sub_charge_date` существует на таблице `planned_transaction`. Этот
       constraint декларируется в models.py (Phase 4) и должен быть материализован в live БД для
       того, чтобы charge_subscriptions_job (06-04) мог полагаться на IntegrityError → AlreadyChargedError
       (идемпотентность SUB-04/SUB-05). Команда:
       ```bash
       psql "$DATABASE_URL" -tAc "SELECT constraint_name FROM information_schema.table_constraints WHERE table_name='planned_transaction' AND constraint_name='uq_planned_sub_charge_date'"
       ```
       Должна вернуть строку `uq_planned_sub_charge_date`. Если пусто — constraint отсутствует
       в live БД (значит prior migration не применена) → создать `alembic/versions/0003_ensure_uq_planned_sub_charge_date.py`
       с `op.create_unique_constraint("uq_planned_sub_charge_date", "planned_transaction", ["subscription_id", "original_charge_date"])`
       и повторно `alembic upgrade head`. Если БД недоступна — отметить в SUMMARY как deferred check
       и зафиксировать на этапе 06-07 (Wave 5).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.db.models import AppUser; assert hasattr(AppUser, 'notify_days_before')" && ls alembic/versions/0002_add_notify_days_before.py && (psql "${DATABASE_URL:-postgresql://localhost/tg_budget}" -tAc "SELECT constraint_name FROM information_schema.table_constraints WHERE table_name='planned_transaction' AND constraint_name='uq_planned_sub_charge_date'" 2>/dev/null | grep -q uq_planned_sub_charge_date && echo "CONSTRAINT_OK" || echo "CONSTRAINT_MISSING_OR_NO_DB")</automated>
  </verify>
  <done>
    Колонка в модели + миграция файл существует. Если DB доступна — `alembic upgrade head` без ошибок
    И SQL `SELECT constraint_name FROM information_schema.table_constraints WHERE table_name='planned_transaction'
    AND constraint_name='uq_planned_sub_charge_date'` возвращает строку (verify output: `CONSTRAINT_OK`).
    Если БД недоступна — `CONSTRAINT_MISSING_OR_NO_DB`, и в SUMMARY явно отмечено что check deferred к 06-07.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pydantic-схемы subscriptions + settings extension (D-72, D-77)</name>
  <files>app/api/schemas/subscriptions.py, app/api/schemas/settings.py</files>
  <behavior>
    - SubscriptionCreate валидирует amount_cents > 0, notify_days_before in [0, 30]
    - SubscriptionUpdate все поля Optional
    - SubscriptionRead содержит nested CategoryRead
    - ChargeNowResponse: planned_id + next_charge_date
    - SettingsRead.notify_days_before: int
    - SettingsUpdate.notify_days_before: Optional[int] с Field(ge=0, le=30)
  </behavior>
  <action>
    1. Создать `app/api/schemas/subscriptions.py` per D-72:
       ```python
       from datetime import date
       from typing import Optional
       from pydantic import BaseModel, ConfigDict, Field
       from app.db.models import SubCycle
       from app.api.schemas.categories import CategoryRead

       class SubscriptionCreate(BaseModel):
           name: str = Field(..., min_length=1, max_length=255)
           amount_cents: int = Field(..., gt=0)
           cycle: SubCycle
           next_charge_date: date
           category_id: int
           notify_days_before: Optional[int] = Field(None, ge=0, le=30)
           is_active: bool = True

       class SubscriptionUpdate(BaseModel):
           name: Optional[str] = Field(None, min_length=1, max_length=255)
           amount_cents: Optional[int] = Field(None, gt=0)
           cycle: Optional[SubCycle] = None
           next_charge_date: Optional[date] = None
           category_id: Optional[int] = None
           notify_days_before: Optional[int] = Field(None, ge=0, le=30)
           is_active: Optional[bool] = None

       class SubscriptionRead(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: int
           name: str
           amount_cents: int
           cycle: SubCycle
           next_charge_date: date
           category_id: int
           notify_days_before: int
           is_active: bool
           category: CategoryRead

       class ChargeNowResponse(BaseModel):
           planned_id: int
           next_charge_date: date
       ```

    2. Открыть `app/api/schemas/settings.py`, добавить `notify_days_before: int` в `SettingsRead` и `notify_days_before: Optional[int] = Field(None, ge=0, le=30)` в `SettingsUpdate` per D-77.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.schemas.subscriptions import SubscriptionCreate, SubscriptionUpdate, SubscriptionRead, ChargeNowResponse; from app.api.schemas.settings import SettingsRead, SettingsUpdate; s = SettingsRead(cycle_start_day=1, notify_days_before=2, is_bot_bound=False); print('OK', s)"</automated>
  </verify>
  <done>
    Импорты проходят. SubscriptionCreate(amount_cents=0) → ValidationError. SettingsRead instantiable с обоими полями.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Сервисный слой — subscriptions.py + settings extension (D-73, D-74, D-76)</name>
  <files>app/services/subscriptions.py, app/services/settings.py</files>
  <behavior>
    - list_subscriptions(db) → list[Subscription] с joined category, sort by next_charge_date ASC
    - create_subscription валидирует категорию (не archived), defaults notify_days_before из user.notify_days_before
    - update_subscription partial-update
    - delete_subscription hard delete
    - charge_subscription возвращает (PlannedTransaction, new_next_date), raises AlreadyChargedError на IntegrityError
    - _advance_charge_date: monthly +relativedelta(months=1), yearly +relativedelta(years=1)
    - settings: get_notify_days_before/update_notify_days_before
  </behavior>
  <action>
    1. Создать `app/services/subscriptions.py` per D-73:
       ```python
       from datetime import date
       from dateutil.relativedelta import relativedelta
       from sqlalchemy import select, delete
       from sqlalchemy.exc import IntegrityError
       from sqlalchemy.orm import selectinload
       from app.db.models import Subscription, SubCycle, Category, PlannedTransaction, AppUser, PlanSource

       class AlreadyChargedError(Exception):
           def __init__(self, sub_id: int, charge_date: date):
               self.sub_id = sub_id; self.charge_date = charge_date
               super().__init__(f"Subscription {sub_id} already charged on {charge_date}")

       class CategoryNotFoundOrArchived(Exception): ...

       def _advance_charge_date(sub: Subscription) -> date:
           if sub.cycle == SubCycle.monthly:
               return sub.next_charge_date + relativedelta(months=1)
           return sub.next_charge_date + relativedelta(years=1)

       async def list_subscriptions(db) -> list[Subscription]:
           result = await db.execute(
               select(Subscription).options(selectinload(Subscription.category))
               .order_by(Subscription.next_charge_date.asc())
           )
           return list(result.scalars().all())

       async def create_subscription(db, *, tg_user_id: int, name, amount_cents, cycle, next_charge_date, category_id, notify_days_before=None, is_active=True) -> Subscription:
           cat = await db.scalar(select(Category).where(Category.id == category_id))
           if cat is None or cat.is_archived:
               raise CategoryNotFoundOrArchived()
           if notify_days_before is None:
               user = await db.scalar(select(AppUser).where(AppUser.tg_user_id == tg_user_id))
               notify_days_before = user.notify_days_before if user else 2
           sub = Subscription(name=name, amount_cents=amount_cents, cycle=cycle,
                              next_charge_date=next_charge_date, category_id=category_id,
                              notify_days_before=notify_days_before, is_active=is_active)
           db.add(sub); await db.flush()
           await db.refresh(sub, ["category"])
           return sub

       async def update_subscription(db, sub_id: int, patch: dict) -> Subscription:
           sub = await db.scalar(select(Subscription).where(Subscription.id == sub_id).options(selectinload(Subscription.category)))
           if sub is None: raise LookupError(f"subscription {sub_id} not found")
           for k, v in patch.items():
               if v is not None:
                   setattr(sub, k, v)
           await db.flush()
           return sub

       async def delete_subscription(db, sub_id: int) -> None:
           await db.execute(delete(Subscription).where(Subscription.id == sub_id))

       async def charge_subscription(db, sub_id: int, *, cycle_start_day: int):
           # Lazy import to avoid cyclic
           from app.services.actual import _resolve_period_for_date
           sub = await db.scalar(select(Subscription).where(Subscription.id == sub_id).options(selectinload(Subscription.category)))
           if sub is None: raise LookupError(f"subscription {sub_id}")
           original_date = sub.next_charge_date
           period_id = await _resolve_period_for_date(db, original_date, cycle_start_day=cycle_start_day)
           planned = PlannedTransaction(
               period_id=period_id, category_id=sub.category_id,
               kind=sub.category.kind, amount_cents=sub.amount_cents,
               source=PlanSource.subscription_auto, subscription_id=sub.id,
               original_charge_date=original_date,
           )
           db.add(planned)
           try:
               await db.flush()
           except IntegrityError:
               await db.rollback()
               raise AlreadyChargedError(sub_id, original_date)
           sub.next_charge_date = _advance_charge_date(sub)
           await db.flush()
           return planned, sub.next_charge_date
       ```

    2. Расширить `app/services/settings.py` per D-76: добавить `get_notify_days_before` и `update_notify_days_before` в стиле существующих `get_cycle_start_day` / `update_cycle_start_day`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.services.subscriptions import _advance_charge_date, AlreadyChargedError, list_subscriptions, create_subscription, charge_subscription; from app.services.settings import get_notify_days_before, update_notify_days_before; print('OK')"</automated>
  </verify>
  <done>
    Все импорты успешны. _advance_charge_date вычисляет +1 month / +1 year корректно. AlreadyChargedError инстанцируется.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Service → DB | Untrusted user input (subscription create/update) валидируется Pydantic + service слоем |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | SubscriptionCreate amount_cents | mitigate | Pydantic Field(gt=0) — отрицательные/нулевые суммы 422 |
| T-06-02 | Information Disclosure | category_id | mitigate | service проверяет category_id принадлежит user (single-tenant) и не archived → 400 |
| T-06-03 | DoS | notify_days_before | mitigate | Field(ge=0, le=30) ограничивает диапазон |
</threat_model>

<verification>
- `python -c` импорт всех новых символов проходит
- `alembic upgrade head` без ошибок (если DB доступна)
- SQL `SELECT constraint_name ... WHERE constraint_name='uq_planned_sub_charge_date'` возвращает строку (SUB-05)
- pytest tests/test_subscriptions.py всё ещё RED (роуты не подключены — это OK для волны 1)
</verification>

<success_criteria>
БД-колонка добавлена, миграция готова, схемы валидируются, сервисы импортируются, constraint
`uq_planned_sub_charge_date` подтверждён в live БД (или deferred к 06-07 при отсутствии DB).
Wave 2 (роуты) может строиться поверх.
</success_criteria>

<output>
Создать `.planning/phases/06-subscriptions-worker-jobs/06-02-SUMMARY.md` с:
- Список созданных модулей и их exports
- Подтверждение `alembic upgrade head` (или причина пропуска)
- Результат SUB-05 constraint check (CONSTRAINT_OK / CONSTRAINT_MISSING_OR_NO_DB) — если deferred, явно зафиксировать что 06-07 должен повторить проверку
- Заметки для 06-03: signature всех service-функций
</output>
