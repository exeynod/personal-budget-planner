---
phase: 12-role-based-auth-refactor
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/api/router.py
  - frontend/src/api/types.ts
autonomous: true
requirements: [ROLE-05]
tags: [api, me-endpoint, frontend-types, role, fastapi-pydantic]
must_haves:
  truths:
    - "GET /api/v1/me возвращает JSON body с полями: tg_user_id, tg_chat_id, role, cycle_start_day, onboarded_at, chat_id_known"
    - "Pydantic MeResponse model (defined inline в app/api/router.py) расширен полем `role: Literal['owner', 'member', 'revoked']` (или эквивалент через UserRole enum)"
    - "/me endpoint больше НЕ читает current_user['id'] (dict access) — теперь использует AppUser ORM из refactored Plan 12-02 get_current_user; читает .tg_user_id, .role, .cycle_start_day, .tg_chat_id, .onboarded_at напрямую"
    - "/me upsert path сохраняется ТОЛЬКО в DEV_MODE (тогда get_current_user уже upsert'ит); в production AppUser должен существовать заранее (Phase 14 onboarding pre-create) — поэтому /me handler без upsert"
    - "Frontend MeResponse interface в frontend/src/api/types.ts добавляет `role: 'owner' | 'member' | 'revoked'` (string union); тип используется в frontend/src/hooks/useUser.ts (без изменений в hook coding)"
    - "test_me_includes_role_for_owner + test_me_includes_role_for_member из Plan 12-01 → GREEN"
    - "Existing GET /me semantics (response shape minus role) сохранены — backward compatible для frontend, который ещё не читает role (Phase 13 будет читать)"
  artifacts:
    - path: app/api/router.py
      provides: "MeResponse pydantic model расширен role; /me endpoint refactor через AppUser ORM"
      contains: "role"
      contains_2: "MeResponse"
    - path: frontend/src/api/types.ts
      provides: "MeResponse interface с role field"
      contains: "role"
  key_links:
    - from: app/api/router.py::get_me
      to: app/api/dependencies.py::get_current_user
      via: "Depends(get_current_user) теперь возвращает AppUser ORM (Plan 12-02); /me reads attributes напрямую"
      pattern: "current_user.role"
    - from: app/api/router.py::MeResponse
      to: app/db/models.py::UserRole
      via: "role field type — Literal или string union отражающий UserRole enum values"
      pattern: 'role:\\s*Literal'
    - from: frontend/src/api/types.ts::MeResponse
      to: app/api/router.py::MeResponse
      via: "shape mirror; role field added"
      pattern: "role: 'owner' \\| 'member' \\| 'revoked'"
---

<objective>
Расширить GET /api/v1/me endpoint для возврата `role` field; обновить frontend MeResponse type.

Конкретно:
1. **`app/api/router.py`**:
   - `MeResponse` pydantic model — добавить `role: Literal['owner', 'member', 'revoked']` (или приоритетно — string использующий UserRole.value)
   - `get_me` handler refactor:
     * Удалить старый upsert pattern (`pg_insert(...).on_conflict_do_nothing`) — теперь get_current_user (Plan 12-02) делает это в DEV_MODE; в production AppUser обязан существовать (либо через dev_seed, либо через Phase 14 onboarding)
     * Читать AppUser ORM из `Depends(get_current_user)` напрямую (после Plan 12-02 это AppUser instance)
     * Возвращать MeResponse с `role=user.role.value`
2. **`frontend/src/api/types.ts`**:
   - В `MeResponse` interface добавить `role: 'owner' | 'member' | 'revoked'`

После этого Plan'а: 2 RED tests из Plan 12-01 (test_me_returns_role.py) → GREEN.

Output: 2 файла обновлены; 2 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-role-based-auth-refactor/12-CONTEXT.md
@CLAUDE.md
@app/api/router.py
@app/db/models.py
@frontend/src/api/types.ts
@frontend/src/hooks/useUser.ts
@frontend/src/screens/OnboardingScreen.tsx
@tests/test_me_returns_role.py

<interfaces>
CURRENT (Phase 11) app/api/router.py:78-105:
```python
class MeResponse(BaseModel):
    tg_user_id: int
    tg_chat_id: int | None
    cycle_start_day: int
    onboarded_at: str | None
    chat_id_known: bool

@public_router.get("/me", response_model=MeResponse)
async def get_me(
    current_user: Annotated[dict, Depends(get_current_user)],  # OLD: dict
    db: Annotated[AsyncSession, Depends(get_db)],
) -> MeResponse:
    tg_user_id = current_user["id"]
    # D-11: upsert app_user on first request
    stmt = (
        insert(AppUser)
        .values(tg_user_id=tg_user_id)
        .on_conflict_do_nothing(index_elements=["tg_user_id"])
    )
    await db.execute(stmt)
    result = await db.execute(select(AppUser).where(AppUser.tg_user_id == tg_user_id))
    user = result.scalar_one()
    return MeResponse(
        tg_user_id=user.tg_user_id, tg_chat_id=user.tg_chat_id,
        cycle_start_day=user.cycle_start_day,
        onboarded_at=user.onboarded_at.isoformat() if user.onboarded_at else None,
        chat_id_known=user.tg_chat_id is not None,
    )
```

TARGET (Phase 12) app/api/router.py:
```python
from typing import Literal
from app.db.models import AppUser, UserRole

class MeResponse(BaseModel):
    tg_user_id: int
    tg_chat_id: int | None
    cycle_start_day: int
    onboarded_at: str | None
    chat_id_known: bool
    role: Literal["owner", "member", "revoked"]  # ROLE-05

@public_router.get("/me", response_model=MeResponse)
async def get_me(
    current_user: Annotated[AppUser, Depends(get_current_user)],
) -> MeResponse:
    """Return current user info; AppUser already resolved by get_current_user."""
    return MeResponse(
        tg_user_id=current_user.tg_user_id,
        tg_chat_id=current_user.tg_chat_id,
        cycle_start_day=current_user.cycle_start_day,
        onboarded_at=current_user.onboarded_at.isoformat() if current_user.onboarded_at else None,
        chat_id_known=current_user.tg_chat_id is not None,
        role=current_user.role.value,  # UserRole.owner.value == "owner"
    )
```

CURRENT frontend/src/api/types.ts:1-8:
```typescript
export interface MeResponse {
  tg_user_id: number;
  tg_chat_id: number | null;
  cycle_start_day: number;
  onboarded_at: string | null;
  chat_id_known: boolean;
}
```

TARGET frontend:
```typescript
export type UserRole = 'owner' | 'member' | 'revoked';

export interface MeResponse {
  tg_user_id: number;
  tg_chat_id: number | null;
  cycle_start_day: number;
  onboarded_at: string | null;
  chat_id_known: boolean;
  role: UserRole;  // ROLE-05
}
```

From app/db/models.py:
```python
class UserRole(str, enum.Enum):
    owner = "owner"; member = "member"; revoked = "revoked"
```

UserRole inherits from str → `UserRole.owner.value == "owner"`. Pydantic Literal accepts string values verbatim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor app/api/router.py — MeResponse + role; /me handler через AppUser ORM</name>
  <files>app/api/router.py</files>
  <read_first>
    - app/api/router.py (lines 1-160 entire file)
    - app/db/models.py (AppUser fields + UserRole enum)
    - tests/test_me_returns_role.py (что должно pass)
    - app/api/dependencies.py (после Plan 12-02 — get_current_user → AppUser ORM)
  </read_first>
  <behavior>
    - test_me_includes_role_for_owner: GET /me с initData(OWNER) + AppUser(tg_user_id=OWNER, role=owner) seed → 200 + body содержит "role": "owner"
    - test_me_includes_role_for_member: GET /me с member юзером → "role": "member"

    Existing /me semantics:
    - Все остальные fields (tg_user_id, tg_chat_id, cycle_start_day, onboarded_at, chat_id_known) сохраняются.
    - Status code 200 maintained.
  </behavior>
  <action>
    В `app/api/router.py`:

    1. **Импорты** (top of file): добавить `from typing import Literal` (если ещё нет); сохранить existing `from sqlalchemy.dialects.postgresql import insert` (он используется ниже только если оставляем; в новом /me не нужен — удалить если не используется в других местах файла).

       Verify: `grep -n 'from sqlalchemy.dialects.postgresql import insert' app/api/router.py` → одно совпадение. Если используется только в get_me — удалить импорт. Иначе оставить.

    2. **MeResponse** Pydantic model — добавить поле:
       ```python
       class MeResponse(BaseModel):
           tg_user_id: int
           tg_chat_id: int | None
           cycle_start_day: int
           onboarded_at: str | None
           chat_id_known: bool
           role: Literal["owner", "member", "revoked"]  # Phase 12 ROLE-05
       ```

    3. **get_me handler** полная переработка:
       ```python
       from app.db.models import AppUser  # already imported at top — verify

       @public_router.get("/me", response_model=MeResponse)
       async def get_me(
           current_user: Annotated[AppUser, Depends(get_current_user)],
       ) -> MeResponse:
           """Return current user info (Phase 12 ROLE-05).

           AppUser is resolved by get_current_user (Plan 12-02 refactor).
           - DEV_MODE: get_current_user upserts OWNER row first → here we just read.
           - Production: get_current_user requires existing AppUser → 403 если нет.
             Phase 14 onboarding pre-creates rows for invited members.

           No upsert in this handler. Existing /me-as-bootstrap semantics moved
           into get_current_user (DEV_MODE) и Phase 14 onboarding (production).
           """
           return MeResponse(
               tg_user_id=current_user.tg_user_id,
               tg_chat_id=current_user.tg_chat_id,
               cycle_start_day=current_user.cycle_start_day,
               onboarded_at=current_user.onboarded_at.isoformat()
                   if current_user.onboarded_at else None,
               chat_id_known=current_user.tg_chat_id is not None,
               role=current_user.role.value,  # UserRole.<x>.value == "owner"|"member"|"revoked"
           )
       ```

    4. Удалить ставшие unused imports если применимо:
       - `from sqlalchemy import select` → если используется в других handlers, оставить; verify grep.
       - `from sqlalchemy.dialects.postgresql import insert` → удалить если не используется.
       - `from sqlalchemy.ext.asyncio import AsyncSession` → используется в `get_db` chains в этом файле, оставить.

       Use grep после edit:
       ```
       grep -n 'select\|insert\|AsyncSession' app/api/router.py
       ```
       Только удалять если 0 matches вне импортов.

    5. Обновить module docstring (top of file) — обновить ссылку на /me handler:
       ```
       - ``/me`` (public_router) — returns current user info incl. role; protected by
         ``get_current_user`` (Telegram initData + role-based whitelist, Phase 12).
       ```
       Заменить упоминание "OWNER_TG_ID whitelist" на "role-based whitelist".
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.api.router import MeResponse; m = MeResponse(tg_user_id=1, tg_chat_id=None, cycle_start_day=5, onboarded_at=None, chat_id_known=False, role='owner'); print(m.model_dump())"</automated>
  </verify>
  <acceptance_criteria>
    - Команда выше выводит dict содержащий `'role': 'owner'` без validation errors
    - `grep -c 'role: Literal\[' app/api/router.py` ≥ 1
    - `grep -c 'current_user\["id"\]' app/api/router.py` → 0 (старый dict access removed)
    - `grep -c 'current_user.role.value\|current_user\.tg_user_id' app/api/router.py` ≥ 1
  </acceptance_criteria>
  <done>
    /me endpoint возвращает role; MeResponse pydantic-валидирует.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update frontend/src/api/types.ts — MeResponse.role + UserRole type</name>
  <files>frontend/src/api/types.ts</files>
  <read_first>
    - frontend/src/api/types.ts (lines 1-10 текущий MeResponse)
    - frontend/src/hooks/useUser.ts (consumer — verify нет breaking change)
    - frontend/src/screens/OnboardingScreen.tsx (другой consumer)
  </read_first>
  <behavior>
    Frontend компилируется без ошибок; existing consumers (useUser, OnboardingScreen) продолжают работать (они не читают role yet — Phase 13 будет).

    TypeScript принимает добавление поля без regression в callers.
  </behavior>
  <action>
    В `frontend/src/api/types.ts` (top of file):

    1. **Добавить тип** `UserRole` СРАЗУ ПЕРЕД `MeResponse`:
       ```typescript
       /** Mirrors Python `UserRole` enum (app/db/models.py). Phase 12 ROLE-05. */
       export type UserRole = 'owner' | 'member' | 'revoked';
       ```

    2. **Расширить `MeResponse`** добавлением поля `role`:
       ```typescript
       /** Mirrors `MeResponse` from app/api/router.py. */
       export interface MeResponse {
         tg_user_id: number;
         tg_chat_id: number | null;
         cycle_start_day: number;
         onboarded_at: string | null; // ISO datetime
         chat_id_known: boolean;
         /** Phase 12 ROLE-05: backend-driven role for admin-tab visibility (Phase 13). */
         role: UserRole;
       }
       ```

    3. Никаких других изменений в файле. Existing types (CategoryRead, BudgetPeriod, etc.) intact.

    Verify TS compilation:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner/frontend && \
      npx tsc --noEmit 2>&1 | tail -20
    ```
    Ожидание: 0 errors. Если existing consumer (e.g. test files mocking MeResponse) ругается на missing role — найти и обновить mocks.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit: 0 errors (или только про unrelated existing pre-Phase-12 issues)
    - `grep -c 'role: UserRole' frontend/src/api/types.ts` → 1
    - `grep -c "export type UserRole" frontend/src/api/types.ts` → 1
    - `grep -c "'owner' | 'member' | 'revoked'" frontend/src/api/types.ts` → 1
  </acceptance_criteria>
  <done>
    Frontend types отражают backend MeResponse shape; TS компилируется.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run pytest для test_me_returns_role.py — 2 tests GREEN</name>
  <files></files>
  <read_first>
    - tests/test_me_returns_role.py
    - tests/test_role_based_auth.py (no regressions allowed)
  </read_first>
  <behavior>
    После Plan 12-02 (auth refactor) + Tasks 1-2 здесь, /me endpoint возвращает role; tests pass.
  </behavior>
  <action>
    Запустить:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      uv run pytest \
        tests/test_me_returns_role.py \
        tests/test_role_based_auth.py \
        tests/test_require_owner.py \
        tests/test_auth.py \
        -v --tb=short 2>&1 | tail -30
    ```

    Ожидание:
    - test_me_returns_role.py: 2 passed
    - test_role_based_auth.py: 6 passed (no regression)
    - test_require_owner.py: 3 passed (no regression)
    - test_auth.py: 5 passed / 2 skipped

    Если test_owner_whitelist_valid (test_auth.py) падает с 403 потому что AppUser не существует — это потому что старый /me upsert ушёл. → Решение: test_auth.py на этапе fixture seed'ит AppUser перед /me. Verify; если нет — обновить fixture в этом плане (минимальное изменение в `db_client` fixture внутри tests/test_auth.py чтобы seed'ил AppUser(tg_user_id=owner_tg_id, role=UserRole.owner) после TRUNCATE).

    → Этот edge case добавляет файл `tests/test_auth.py` к possibly-touched. Но Plan 12-06 (fixture sweep) — общая работа над fixtures. Пока: если test_auth регрессирует, минимально fix в этом плане (1-line seed in db_client fixture); это в scope ROLE-05.

    → Проверить: tests/test_auth.py:db_client fixture — после TRUNCATE надо вставить AppUser owner row, чтобы /me prod path работал. Это 1-line fix (insert в той же fixture engine.begin блоке).

    Если потребуется — обновить tests/test_auth.py (добавить файл к files_modified; будет noted в SUMMARY).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_me_returns_role.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test_me_returns_role.py: 2 passed
    - Никаких regressions в test_role_based_auth + test_require_owner + test_auth (combined: 14 passed / 2 skipped)
  </acceptance_criteria>
  <done>
    /me endpoint работает с role field; tests GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram client → API /me | Reads AppUser data + role |
| API /me → Frontend | Frontend will use `role` for conditional admin UI rendering (Phase 13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | I (Info Disclosure) | /me leaks role to client | accept | Role is required UI signal (Phase 13 admin tab visibility); client does NOT trust role for security — server enforces via require_owner |
| T-12-03-02 | T (Tampering) | Client modifies role in localStorage / state and assumes admin access | mitigate | Frontend role used ONLY for visibility/UX; server-side require_owner is source of truth — admin endpoints reject members regardless of client-claimed role (Phase 13 ADM-* endpoints will use require_owner) |
| T-12-03-03 | S (Spoofing) | Old client (frontend-cached SPA) lacks role field after deploy | accept | TS compile error if old code ships; runtime: role is added field — old code ignores; default behaviour (admin tab hidden) is safe |
| T-12-03-04 | E (Elevation) | /me returns role for revoked user (theoretical) | mitigate | get_current_user (Plan 12-02) raises 403 for revoked BEFORE handler runs — /me never sees revoked users |
</threat_model>

<verification>
- `grep -c 'role: Literal\[' app/api/router.py` ≥ 1
- `grep -c 'role: UserRole' frontend/src/api/types.ts` → 1
- pytest tests/test_me_returns_role.py: 2 passed
- Combined Plan 12-01 tests: 11 passed / total assertions intact (test_role_based_auth: 6, test_require_owner: 3, test_me_returns_role: 2)
- `grep -c 'current_user\["id"\]' app/api/router.py` → 0
- frontend tsc --noEmit: 0 errors related к MeResponse changes
</verification>

<success_criteria>
- ROLE-05 satisfied: GET /api/v1/me возвращает role; frontend MeResponse interface содержит role
- 2 RED tests из Plan 12-01 (test_me_returns_role) → GREEN
- Никаких regressions в Plan 12-02 tests
- Frontend types корректны (TS compile clean)
</success_criteria>

<output>
After completion, create `.planning/phases/12-role-based-auth-refactor/12-03-SUMMARY.md`
</output>
