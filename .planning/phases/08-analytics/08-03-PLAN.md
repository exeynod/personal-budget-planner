---
phase: 08-analytics
plan: 03
type: execute
wave: 2
depends_on:
  - 08-02
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/analytics.ts
  - frontend/src/hooks/useAnalytics.ts
autonomous: true
requirements:
  - ANL-01
  - ANL-02

must_haves:
  truths:
    - "TypeScript типы для всех 4 analytics endpoints добавлены в types.ts"
    - "api/analytics.ts экспортирует 4 fetch-функции через apiFetch"
    - "useAnalytics(range) хук загружает все 4 endpoints параллельно с cancelled-флагом"
    - "При смене range хук перезапускает fetch (range в useEffect dependencies)"
    - "TypeScript компилируется без ошибок"
  artifacts:
    - path: "frontend/src/api/types.ts"
      provides: "Analytics TypeScript типы (TrendPoint, TrendResponse, OverspendItem, TopOverspendResponse, TopCategoryItem, TopCategoriesResponse, ForecastResponse)"
      contains: "interface TrendPoint"
    - path: "frontend/src/api/analytics.ts"
      provides: "4 fetch функции + AnalyticsRange type"
      exports: ["getAnalyticsTrend", "getTopOverspend", "getTopCategories", "getForecast", "AnalyticsRange"]
    - path: "frontend/src/hooks/useAnalytics.ts"
      provides: "useAnalytics(range) хук"
      contains: "export function useAnalytics"
  key_links:
    - from: "frontend/src/hooks/useAnalytics.ts"
      to: "frontend/src/api/analytics.ts"
      via: "getAnalyticsTrend / getTopOverspend / getTopCategories / getForecast"
      pattern: "getAnalyticsTrend"
    - from: "frontend/src/api/analytics.ts"
      to: "frontend/src/api/client.ts"
      via: "apiFetch<T>"
      pattern: "apiFetch"
---

<objective>
Wave 2: Создать frontend data layer для analytics — TypeScript типы, API клиент, хук.

Purpose: Подготовить типизированный слой данных для AnalyticsScreen (Plan 08-04). Хук useAnalytics управляет загрузкой всех 4 endpoints, обработкой ошибок и отменой запросов при смене range.

Output: frontend/src/api/types.ts (расширен), frontend/src/api/analytics.ts (новый), frontend/src/hooks/useAnalytics.ts (новый).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-analytics/08-CONTEXT.md
@.planning/phases/08-analytics/08-PATTERNS.md

<interfaces>
<!-- apiFetch из frontend/src/api/client.ts -->
```typescript
// apiFetch<T>(path: string, init?: RequestInit): Promise<T>
// path — относительный, без /api/v1 (добавляется внутри)
// Автоматически инжектирует X-Telegram-Init-Data
// Бросает ApiError при non-2xx ответе
```

<!-- Паттерн api/periods.ts (аналог для analytics.ts) -->
```typescript
import { apiFetch } from './client';
import type { SomeResponse } from './types';

export async function getSomething(): Promise<SomeResponse> {
  return apiFetch<SomeResponse>('/some-endpoint');
}
```

<!-- Паттерн usePeriods.ts — cancelled flag -->
```typescript
useEffect(() => {
  let cancelled = false;
  setLoading(true);
  fetchSomething()
    .then((data) => { if (!cancelled) setState(data); })
    .catch((e: unknown) => { if (!cancelled) setError(e instanceof Error ? e.message : String(e)); })
    .finally(() => { if (!cancelled) setLoading(false); });
  return () => { cancelled = true; };
}, [dependency]);
```

<!-- Существующий конец frontend/src/api/types.ts — вставить секцию Phase 8 в конец файла -->
<!-- Phase 6 секция заканчивается на SubscriptionRead или аналоге -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить analytics типы в types.ts и создать api/analytics.ts</name>
  <read_first>
    - frontend/src/api/types.ts (полностью — найти конец файла для добавления секции Phase 8)
    - frontend/src/api/periods.ts (паттерн: import apiFetch, export async function)
    - frontend/src/api/client.ts (сигнатура apiFetch — убедиться что path начинается без /api/v1)
    - .planning/phases/08-analytics/08-PATTERNS.md (раздел "frontend/src/api/types.ts" и "frontend/src/api/analytics.ts")
  </read_first>
  <files>
    frontend/src/api/types.ts
    frontend/src/api/analytics.ts
  </files>
  <action>
    **frontend/src/api/types.ts** — добавить в конец файла (после последней Phase секции):
    ```typescript
    // ---------- Phase 8: Analytics ----------

    export interface TrendPoint {
      period_label: string;      // e.g. "Янв", "Фев"
      expense_cents: number;
      income_cents: number;
    }

    export interface TrendResponse {
      points: TrendPoint[];
    }

    export interface OverspendItem {
      category_id: number;
      name: string;
      planned_cents: number;
      actual_cents: number;
      overspend_pct: number;     // actual/plan*100, float
    }

    export interface TopOverspendResponse {
      items: OverspendItem[];
    }

    export interface TopCategoryItem {
      category_id: number;
      name: string;
      actual_cents: number;
      planned_cents: number;
    }

    export interface TopCategoriesResponse {
      items: TopCategoryItem[];
    }

    export interface ForecastResponse {
      insufficient_data: boolean;
      current_balance_cents: number;
      projected_end_balance_cents: number | null;
      will_burn_cents: number | null;
      period_end: string | null;   // ISO date
    }
    ```

    **frontend/src/api/analytics.ts** — создать с нуля:
    ```typescript
    import { apiFetch } from './client';
    import type {
      TrendResponse,
      TopOverspendResponse,
      TopCategoriesResponse,
      ForecastResponse,
    } from './types';

    export type AnalyticsRange = '1M' | '3M' | '6M' | '12M';

    export async function getAnalyticsTrend(range: AnalyticsRange): Promise<TrendResponse> {
      return apiFetch<TrendResponse>(`/analytics/trend?range=${range}`);
    }

    export async function getTopOverspend(range: AnalyticsRange): Promise<TopOverspendResponse> {
      return apiFetch<TopOverspendResponse>(`/analytics/top-overspend?range=${range}`);
    }

    export async function getTopCategories(range: AnalyticsRange): Promise<TopCategoriesResponse> {
      return apiFetch<TopCategoriesResponse>(`/analytics/top-categories?range=${range}`);
    }

    export async function getForecast(): Promise<ForecastResponse> {
      return apiFetch<ForecastResponse>('/analytics/forecast');
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface TrendPoint" frontend/src/api/types.ts` возвращает 1
    - `grep -c "interface ForecastResponse" frontend/src/api/types.ts` возвращает 1
    - `grep -c "projected_end_balance_cents: number | null" frontend/src/api/types.ts` возвращает 1
    - `grep -c "export type AnalyticsRange" frontend/src/api/analytics.ts` возвращает 1
    - `grep -c "export async function getAnalyticsTrend" frontend/src/api/analytics.ts` возвращает 1
    - `grep -c "export async function getForecast" frontend/src/api/analytics.ts` возвращает 1
    - `grep -c "apiFetch" frontend/src/api/analytics.ts` возвращает >= 4
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <done>types.ts расширен секцией Phase 8 с 7 интерфейсами; analytics.ts создан с 4 fetch-функциями; TypeScript компилируется без ошибок.</done>
</task>

<task type="auto">
  <name>Task 2: Создать frontend/src/hooks/useAnalytics.ts</name>
  <read_first>
    - frontend/src/hooks/usePeriods.ts (полностью — cancelled flag pattern, useCallback, useEffect structure)
    - frontend/src/api/analytics.ts (только что созданный — экспорты для импорта в хук)
    - frontend/src/api/types.ts (TrendResponse, TopOverspendResponse, TopCategoriesResponse, ForecastResponse)
    - .planning/phases/08-analytics/08-PATTERNS.md (раздел "frontend/src/hooks/useAnalytics.ts")
  </read_first>
  <files>
    frontend/src/hooks/useAnalytics.ts
  </files>
  <action>
    Создать `frontend/src/hooks/useAnalytics.ts` с нуля:

    ```typescript
    import { useCallback, useEffect, useState } from 'react';
    import {
      getAnalyticsTrend,
      getTopOverspend,
      getTopCategories,
      getForecast,
    } from '../api/analytics';
    import type { AnalyticsRange } from '../api/analytics';
    import type {
      TrendResponse,
      TopOverspendResponse,
      TopCategoriesResponse,
      ForecastResponse,
    } from '../api/types';

    export interface UseAnalyticsResult {
      trend: TrendResponse | null;
      topOverspend: TopOverspendResponse | null;
      topCategories: TopCategoriesResponse | null;
      forecast: ForecastResponse | null;
      loading: boolean;
      error: string | null;
      refetch: () => Promise<void>;
    }

    export function useAnalytics(range: AnalyticsRange): UseAnalyticsResult {
      const [trend, setTrend] = useState<TrendResponse | null>(null);
      const [topOverspend, setTopOverspend] = useState<TopOverspendResponse | null>(null);
      const [topCategories, setTopCategories] = useState<TopCategoriesResponse | null>(null);
      const [forecast, setForecast] = useState<ForecastResponse | null>(null);
      const [loading, setLoading] = useState(true);
      const [error, setError] = useState<string | null>(null);

      const refetch = useCallback(async () => {
        setLoading(true);
        setError(null);
        try {
          const [t, o, c, f] = await Promise.all([
            getAnalyticsTrend(range),
            getTopOverspend(range),
            getTopCategories(range),
            getForecast(),
          ]);
          setTrend(t);
          setTopOverspend(o);
          setTopCategories(c);
          setForecast(f);
        } catch (e) {
          setError(e instanceof Error ? e.message : String(e));
        } finally {
          setLoading(false);
        }
      }, [range]);

      useEffect(() => {
        let cancelled = false;
        setLoading(true);
        setError(null);
        Promise.all([
          getAnalyticsTrend(range),
          getTopOverspend(range),
          getTopCategories(range),
          getForecast(),
        ])
          .then(([t, o, c, f]) => {
            if (!cancelled) {
              setTrend(t);
              setTopOverspend(o);
              setTopCategories(c);
              setForecast(f);
            }
          })
          .catch((e: unknown) => {
            if (!cancelled) setError(e instanceof Error ? e.message : String(e));
          })
          .finally(() => {
            if (!cancelled) setLoading(false);
          });
        return () => {
          cancelled = true;
        };
      }, [range]);

      return { trend, topOverspend, topCategories, forecast, loading, error, refetch };
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useAnalytics" frontend/src/hooks/useAnalytics.ts` возвращает 1
    - `grep -c "cancelled = true" frontend/src/hooks/useAnalytics.ts` возвращает 1
    - `grep -c "\[range\]" frontend/src/hooks/useAnalytics.ts` возвращает >= 2 (в useEffect и useCallback deps)
    - `grep -c "Promise.all" frontend/src/hooks/useAnalytics.ts` возвращает >= 2
    - `grep -c "getForecast" frontend/src/hooks/useAnalytics.ts` возвращает >= 2
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <done>useAnalytics.ts создан; range в deps useEffect и useCallback; cancelled flag предотвращает stale writes; TypeScript компилируется без ошибок.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → /api/v1/analytics/* | apiFetch инжектирует X-Telegram-Init-Data; бэкенд валидирует HMAC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Information Disclosure | useAnalytics error state | accept | error — строка для UI, не stack trace; ApiError из client.ts не раскрывает серверные детали |
| T-08-08 | Tampering | range query param (frontend) | accept | AnalyticsRange type = Literal — TypeScript не даст передать невалидное значение; backend дополнительно валидирует Literal в FastAPI |
</threat_model>

<verification>
- `grep -c "interface TrendPoint" frontend/src/api/types.ts` >= 1
- `grep -c "export async function getAnalyticsTrend" frontend/src/api/analytics.ts` == 1
- `grep -c "export function useAnalytics" frontend/src/hooks/useAnalytics.ts` == 1
- `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` == 0
</verification>

<success_criteria>
- 7 analytics TypeScript интерфейсов в types.ts
- 4 fetch-функции в api/analytics.ts через apiFetch
- useAnalytics(range) хук с cancelled flag и range в deps
- TypeScript компилируется без ошибок
- ANL-01 и ANL-02 покрыты
</success_criteria>

<output>
После завершения создать `.planning/phases/08-analytics/08-03-SUMMARY.md`
</output>
