---
phase: 22-backend-schema-logic-foundation
plan: 04
type: execute
wave: 1
depends_on: [22.03]
files_modified:
  - alembic/versions/0015_v10_rls_finalize.py
autonomous: true
requirements: [BE-16]
tags: [alembic, rls, composite-fk, finalize]

must_haves:
  truths:
    - "RLS enabled with policy tenant_isolation_goal on goal table"
    - "RLS enabled with policy tenant_isolation_savings_config on savings_config table"
    - "Composite UNIQUE on actual_transaction(id, user_id) created so parent_txn_id can target it"
    - "Composite FK actual_transaction.(parent_txn_id, user_id) → (id, user_id) created (replaces simple FK from 0014)"
    - "All BE-16 cross-tenant защита in place"
  artifacts:
    - path: "alembic/versions/0015_v10_rls_finalize.py"
      provides: "RLS on goal/savings_config + composite FK on actual_transaction.parent_txn_id"
      contains: "tenant_isolation_goal"
  key_links:
    - from: "actual_transaction.parent_txn_id"
      to: "actual_transaction.(id, user_id)"
      via: "composite FK ON DELETE CASCADE"
      pattern: "fk_actual_parent_txn_composite.*REFERENCES actual_transaction.*id, user_id"
---

<objective>
Create alembic migration `0015_v10_rls_finalize.py` — Phase 22 schema closer.

1. Enable RLS + create `tenant_isolation_goal` policy on `goal`.
2. Enable RLS + create `tenant_isolation_savings_config` policy on `savings_config`.
3. Replace simple FK `fk_actual_parent_txn` (from migration 0014) with composite FK `(parent_txn_id, user_id) → (id, user_id)` for cross-tenant защита (BE-16).
4. Add composite UNIQUE constraint `ux_actual_id_user` on `actual_transaction(id, user_id)` as the FK target.

Purpose: BE-16 acceptance — new tables RLS-protected; parent_txn_id linkage cannot cross tenant boundaries even with compromised app layer.
Output: One alembic file with idempotent DROP POLICY IF EXISTS in downgrade.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-backend-schema-logic-foundation/22-CONTEXT.md
@CLAUDE.md
@app/db/models.py
@alembic/versions/0006_multitenancy.py
@alembic/versions/0014_v10_actual_goal_savings.py
@alembic/versions/0012_v10_user_account.py

<interfaces>
RLS template (from 0006_multitenancy.py — must use same `coalesce(NULLIF(…), '')::bigint, -1` defense pattern):

```python
op.execute("ALTER TABLE goal ENABLE ROW LEVEL SECURITY")
op.execute("ALTER TABLE goal FORCE ROW LEVEL SECURITY")
op.execute(
    "CREATE POLICY tenant_isolation_goal ON goal "
    "USING (user_id = coalesce(NULLIF(current_setting('app.current_user_id', true), '')::bigint, -1)) "
    "WITH CHECK (user_id = coalesce(NULLIF(current_setting('app.current_user_id', true), '')::bigint, -1))"
)
```

Composite FK pattern (replaces simple FK from 0014):

```python
op.drop_constraint("fk_actual_parent_txn", "actual_transaction", type_="foreignkey")
op.create_unique_constraint("ux_actual_id_user", "actual_transaction", ["id", "user_id"])
op.execute(
    "ALTER TABLE actual_transaction "
    "ADD CONSTRAINT fk_actual_parent_txn_composite "
    "FOREIGN KEY (parent_txn_id, user_id) REFERENCES actual_transaction (id, user_id) "
    "ON DELETE CASCADE"
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create migration 0015 — RLS on goal/savings_config + composite FK on parent_txn_id</name>
  <files>alembic/versions/0015_v10_rls_finalize.py</files>
  <read_first>
    - alembic/versions/0014_v10_actual_goal_savings.py (down_revision; verify FK name `fk_actual_parent_txn`)
    - alembic/versions/0006_multitenancy.py (RLS pattern with coalesce/NULLIF)
    - alembic/versions/0013_v10_category_ext.py (composite FK pattern for category.parent_id)
    - .planning/phases/22-backend-schema-logic-foundation/22-CONTEXT.md §Area 4 (RLS naming `tenant_isolation_<table>` per D-08; composite FK D-09)
  </read_first>
  <action>
Create file `alembic/versions/0015_v10_rls_finalize.py`.

Header docstring opens with `"""v1.0 RLS on goal/savings_config + composite FK on actual_transaction.parent_txn_id (Phase 22 BE-16)`. Include in docstring: revision metadata, what each step does (RLS on goal/savings_config + drop simple parent FK + add composite uniqueness + add composite parent FK), and the naming convention adopted (`tenant_isolation_<table>` per D-08, distinct from legacy `<table>_user_isolation`).

Revision metadata (exact):
- `revision = "0015_v10_rls_finalize"`
- `down_revision = "0014_v10_actual_goal_savings"`
- `branch_labels = None`
- `depends_on = None`

Upgrade body in this exact order:

1. RLS on `goal`: ENABLE + FORCE ROW LEVEL SECURITY, then CREATE POLICY `tenant_isolation_goal` using the verbatim `coalesce(NULLIF(current_setting('app.current_user_id', true), '')::bigint, -1)` pattern from 0006_multitenancy.py.

2. RLS on `savings_config`: ENABLE + FORCE ROW LEVEL SECURITY, then CREATE POLICY `tenant_isolation_savings_config` using identical pattern.

3. `op.drop_constraint("fk_actual_parent_txn", "actual_transaction", type_="foreignkey")` — drops the simple FK created in 0014.

4. `op.create_unique_constraint("ux_actual_id_user", "actual_transaction", ["id", "user_id"])` — composite unique target.

5. Raw DDL via `op.execute(...)` to ADD CONSTRAINT `fk_actual_parent_txn_composite` FOREIGN KEY (parent_txn_id, user_id) REFERENCES actual_transaction (id, user_id) ON DELETE CASCADE — alembic helpers do not support composite FK to non-PK target, so use raw SQL.

Downgrade body (symmetric, reverse order):

1. `op.execute("ALTER TABLE actual_transaction DROP CONSTRAINT IF EXISTS fk_actual_parent_txn_composite")`
2. `op.drop_constraint("ux_actual_id_user", "actual_transaction", type_="unique")`
3. Re-create simple FK `fk_actual_parent_txn` on (parent_txn_id) with ondelete=CASCADE.
4. DROP POLICY IF EXISTS `tenant_isolation_savings_config` ON `savings_config`; NO FORCE ROW LEVEL SECURITY; DISABLE ROW LEVEL SECURITY.
5. Same for `goal`: DROP POLICY IF EXISTS `tenant_isolation_goal`, NO FORCE, DISABLE.

Imports:
```python
from __future__ import annotations
from alembic import op
import sqlalchemy as sa  # noqa: F401
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner &amp;&amp; python -c "import ast; ast.parse(open('alembic/versions/0015_v10_rls_finalize.py').read())" &amp;&amp; grep -q 'tenant_isolation_goal' alembic/versions/0015_v10_rls_finalize.py &amp;&amp; grep -q 'tenant_isolation_savings_config' alembic/versions/0015_v10_rls_finalize.py &amp;&amp; grep -q 'fk_actual_parent_txn_composite' alembic/versions/0015_v10_rls_finalize.py &amp;&amp; grep -q 'ux_actual_id_user' alembic/versions/0015_v10_rls_finalize.py</automated>
  </verify>
  <acceptance_criteria>
    - File exists, parses
    - `grep -c 'down_revision = "0014_v10_actual_goal_savings"' alembic/versions/0015_v10_rls_finalize.py` returns 1
    - `grep -c 'tenant_isolation_goal' alembic/versions/0015_v10_rls_finalize.py` returns at least 2 (CREATE + DROP)
    - `grep -c 'tenant_isolation_savings_config' alembic/versions/0015_v10_rls_finalize.py` returns at least 2
    - `grep -c 'fk_actual_parent_txn_composite' alembic/versions/0015_v10_rls_finalize.py` returns at least 2
    - `grep -c 'ux_actual_id_user' alembic/versions/0015_v10_rls_finalize.py` returns at least 2
    - `grep -c "ENABLE ROW LEVEL SECURITY" alembic/versions/0015_v10_rls_finalize.py` returns 2 (goal + savings_config)
    - `grep -c "FORCE ROW LEVEL SECURITY" alembic/versions/0015_v10_rls_finalize.py` returns 2
    - `grep -c "coalesce.NULLIF.current_setting" alembic/versions/0015_v10_rls_finalize.py` returns at least 2
  </acceptance_criteria>
  <done>
    Migration 0015 file written with RLS policies on goal and savings_config, composite UNIQUE on actual_transaction(id, user_id), composite FK on parent_txn_id replacing simple FK from 0014, and symmetric downgrade.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db→app via RLS on goal | RLS policy enforces user_id == GUC; without GUC, query returns 0 rows |
| db→app via RLS on savings_config | Same enforcement |
| Cross-tenant parent_txn_id | Composite FK rejects child txn linked to parent owned by different user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04-01 | Information Disclosure | goal cross-tenant read | mitigate | RLS policy `tenant_isolation_goal` enforced via FORCE ROW LEVEL SECURITY |
| T-22-04-02 | Information Disclosure | savings_config cross-tenant read | mitigate | RLS policy `tenant_isolation_savings_config` enforced via FORCE ROW LEVEL SECURITY |
| T-22-04-03 | Tampering | Cross-tenant parent_txn_id linkage | mitigate | Composite FK `(parent_txn_id, user_id) → (id, user_id)` ensures DB rejects cross-tenant linkage even if app layer is compromised (BE-16) |
| T-22-04-04 | Spoofing | RLS bypass via SUPERUSER | accept | budget_app role is NOSUPERUSER NOBYPASSRLS (per 0007); migrations run as `budget` (admin) |
| T-22-04-05 | Denial of Service | Composite FK invalidates existing data | mitigate | Migration 0014 just added parent_txn_id with NULL default — no rows reference yet, FK creation cannot fail on existing data |

ASVS L1 mapping: V1.1.1 (auth context propagation), V8.2.2 (data validation at boundary).
</threat_model>

<verification>
- alembic syntax check
- forward+back: alembic upgrade head from 0014 then downgrade -1 (verified e2e in 22.19)
- RLS smoke: SET LOCAL app.current_user_id = '999'; SELECT * FROM goal returns 0 rows for user 999 when no row exists for that user (verified in 22.16)
- composite FK smoke: INSERT actual_transaction with parent_txn_id from different user fails with FK violation (verified in 22.16)
</verification>

<success_criteria>
- All grep gates pass
- File parses
- BE-16 cross-tenant защита in place
</success_criteria>

<output>
After completion, create `.planning/phases/22-backend-schema-logic-foundation/22-04-SUMMARY.md`
</output>
