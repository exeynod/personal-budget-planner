---
phase: 22-backend-schema-logic-foundation
plan: 08
subsystem: backend-services
tags: [savings, goals, aggregator, service, BE-08, BE-09, BE-10, BE-11]
requires:
  - models: SavingsConfig, Goal, ActualTransaction(kind=deposit), Category(code=savings)
  - migrations: 0014_v10_actual_goal_savings, 0016_v10_actual_account_id
  - services: app.services.accounts.apply_balance_delta, app.services.roundup.get_savings_category
provides:
  - app.services.savings: get_savings_snapshot / upsert_config / create_deposit
  - app.services.goals: create_goal / list_goals / get_goal / get_or_404 / update_goal / delete_goal
  - exceptions: GoalNotFoundError, GoalValidationError
affects:
  - app/services/planned.py (Rule 3 fix — lazy-import PlanTemplateItem)
tech-stack:
  added: []
  patterns:
    - "INSERT … ON CONFLICT (user_id) DO UPDATE for race-safe SavingsConfig upsert"
    - "ABS-summing aggregator over kind in (roundup, deposit) — DATA-MODEL §2.4"
    - "Atomic Goal.current_cents bump via single text() UPDATE inside same transaction as deposit insert"
    - "Lazy-import shim for dropped PlanTemplateItem in legacy template path"
key-files:
  created:
    - app/services/savings.py
    - app/services/goals.py
    - tests/services/test_savings.py
    - tests/services/test_goals.py
  modified:
    - app/services/planned.py
decisions:
  - "Aggregator uses ABS(amount_cents) since both roundup and deposit are stored as negative amounts (D-02 sign convention); UI receives positive magnitudes."
  - "month_in_cents lower bound = first day of current MSK month (Europe/Moscow), matching CLAUDE.md TZ rule."
  - "create_deposit normalises caller-supplied amount_cents to negative storage (deposits debit source account); 0 rejected."
  - "create_deposit does NOT call create_actual_v10 — open-coded insert lets us skip the noop roundup hook (kind=deposit fails the gate anyway) and bundle the goal_id bump in the same flush window."
  - "delete_goal does NOT cascade-delete deposits referring to the goal — the schema has no FK from actual_transaction to goal, so deposits remain as historical kind=deposit rows in the savings aggregator."
  - "GoalValidationError ⊂ ValueError so existing route 422 mapping continues to work without code changes; precise type retained for caller introspection."
  - "Identity-map cache workaround in tests: read post-update balance/current via raw text() SELECT because apply_balance_delta and the goal bump both use raw UPDATE, which the ORM identity map does not invalidate."
metrics:
  duration: "~70 minutes"
  completed: "2026-05-10"
  tasks: 2
  files_created: 4
  files_modified: 1
  loc_added: 2024
---

# Phase 22 Plan 08: Savings & Goals Service Summary

Backend service-layer implementation of BE-08 (SavingsConfig upsert), BE-09 (savings aggregator), BE-10 (manual deposit), BE-11 (Goal CRUD). Two new service modules + 47 service-layer tests against the live v1.0 schema (alembic head `0016_v10_actual_account_id`). The router layer that exposes these as REST endpoints lands in plan 22.13.

## What was built

### `app/services/savings.py` (427 lines, 3 public functions)

| Function | Requirement | Behaviour |
| --- | --- | --- |
| `get_savings_snapshot(db, *, user_id)` | BE-09 | Returns `{total_cents, month_in_cents, config, goals[]}`. `total = Σ ABS(amount) where kind in (roundup, deposit)`; `month_in` adds `tx_date >= first_of_msk_month`; `config` falls back to defaults `(roundup_enabled=False, roundup_base=10)` when no row exists; `goals[]` is the user's goals ordered by `created_at` asc, serialised to plain dicts. |
| `upsert_config(db, *, user_id, roundup_enabled=None, roundup_base=None)` | BE-08 | INSERT … ON CONFLICT (user_id) DO UPDATE. PATCH semantics — only supplied fields are written; the others keep their existing values (or `server_default` on first insert). Pre-validates `roundup_base ∈ {10, 50, 100}` so callers get a `ValueError` (→ 422) before hitting the DB CHECK. |
| `create_deposit(db, *, user_id, amount_cents, account_id, goal_id=None, description=None, source=mini_app)` | BE-10 | Inserts a `kind=deposit` ActualTransaction in the user's system 'savings' Category, applies a negative balance delta to `account_id` via `apply_balance_delta`, and (when `goal_id` is supplied) bumps `Goal.current_cents` atomically via a single `text()` UPDATE. Caller-supplied amount can be positive or negative — we normalise to negative storage. Zero is rejected. |

### `app/services/goals.py` (269 lines, 5 public functions + 2 exceptions)

| Function | Behaviour |
| --- | --- |
| `list_goals(db, *, user_id)` | All goals for the user, oldest first. Tenant-scoped via explicit `user_id` filter. |
| `get_goal(db, *, user_id, goal_id)` | Non-raising sibling — returns `None` for missing / cross-tenant rows. |
| `get_or_404(db, goal_id, *, user_id)` | Raises `GoalNotFoundError` if missing or out-of-scope. |
| `create_goal(db, *, user_id, name, target_cents, due=None)` | Validates `name length ∈ [1, 80]`, `target_cents > 0`, `due > today (Europe/Moscow)`; inserts with `current_cents=0`. |
| `update_goal(db, goal_id, *, user_id, **fields)` | Partial PATCH — re-validates each supplied field. DATA-MODEL allows lowering `target_cents` below `current_cents` (UI just shows ≥100% progress). |
| `delete_goal(db, goal_id, *, user_id)` | Hard delete; deposits referring to this goal stay (no FK from `actual_transaction` to `goal`). |

Exceptions: `GoalNotFoundError` (cross-tenant + missing), `GoalValidationError` (subclass of `ValueError` for back-compat with broad route-layer catches).

## Test coverage

47 service-tests pass against the live v1.0 DB (`0016_v10_actual_account_id`):

- **`tests/services/test_savings.py`** (744 lines, 21 tests)
  - Module-import sanity
  - Aggregator: empty, default-config fallback, populated config, roundup+deposit ABS sum, expense/income excluded, prev-month rolloff, goals embedding, cross-tenant isolation
  - Upsert: create-when-absent, update-when-present, partial PATCH preservation, invalid base rejection
  - Deposit: kind=deposit insert, balance delta application, goal_id bump, invalid goal id, zero amount, savings-cat resolution, missing savings-cat, negative-input normalisation
- **`tests/services/test_goals.py`** (568 lines, 26 tests)
  - Module-import sanity
  - create_goal: happy paths (valid / due-future / due-None / 80-char boundary)
  - create_goal validators: target<=0, name length 0/81, due<=today, due==today
  - list_goals: empty, populated ordering, cross-tenant scoping
  - get_or_404 / get_goal: existing, missing-id, cross-tenant 404
  - update_goal: partial fields, target<current allowed, invalid target/due, cross-tenant 404
  - delete_goal: removes row, cross-tenant 404 preserves victim, missing id raises

Test command (run inside the dev API image with the live `tg-budget-planner_budget_net`):

```
docker run --rm --network tg-budget-planner_budget_net \
  -v /Users/exy/pet_projects/tg-budget-planner:/work \
  -e DATABASE_URL="postgresql+asyncpg://budget:localpass@db:5432/budget_db" \
  ... \
  tg-budget-planner-api:latest \
  bash -c 'uv pip install --python /app/.venv/bin/python pytest pytest-asyncio httpx \
           && /app/.venv/bin/python -m pytest tests/services/test_savings.py tests/services/test_goals.py -q'
```

Result: `47 passed in 1.80s`.

## Threat model coverage

| Threat ID | Mitigation |
| --- | --- |
| T-22-08-01 (cross-tenant Goal leak) | Every query has `WHERE Goal.user_id == user_id`; tests `test_list_goals_scoped_to_user`, `test_get_or_404_cross_tenant_returns_404`, `test_update_goal_cross_tenant_404`, `test_delete_goal_cross_tenant_404`, `test_get_savings_snapshot_scoped_to_user` cover. |
| T-22-08-02 (negative target) | Service-layer `_validate_target` rejects `<= 0`; DB CHECK `ck_goal_target_positive` is the backstop. Test `test_create_goal_target_zero_raises_ValueError`. |
| T-22-08-03 (past due) | `_validate_due` rejects `<= today (MSK)`. Tests `test_create_goal_due_in_past_raises_ValueError`, `test_create_goal_due_today_raises_ValueError`. |
| T-22-08-04 (oversized name) | `_validate_name` rejects len 0 / >80. Tests `test_create_goal_name_too_long_raises`, `test_create_goal_name_empty_raises`, `test_create_goal_name_at_boundary_80_chars_succeeds`. |
| T-22-08-06 (config race) | INSERT … ON CONFLICT DO UPDATE is single-statement atomic. |
| T-22-08-07 (deposit without account) | `account_id` required positional; `apply_balance_delta` raises `AccountNotFoundError` on cross-tenant or missing id. |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Blocking] Lazy-import `PlanTemplateItem` in `app/services/planned.py`**

- **Found during:** Task 1 test execution (savings deposit tests transitively import `app.services.actual` → `app.services.planned`).
- **Issue:** `PlanTemplateItem` was dropped from `app/db/models.py` in alembic 0013 (Phase 22.05) but `app/services/planned.py` still listed it at the top of its `from app.db.models import (…)` block. Every consumer of `app.services.actual`/`planned` (api, bot, worker, every test) failed with `ImportError: cannot import name 'PlanTemplateItem'` at module-import time.
- **Fix:** Moved the import to function scope inside `apply_template_to_period`, the one function that uses it. Added a docstring explaining that the legacy template flow is scheduled for full rewrite in plan 22.13. No behavior change for callers that don't touch the legacy template path.
- **Files modified:** `app/services/planned.py`
- **Commit:** `2a1e4e0`

**2. [Rule 1 — Bug] Identity-map cache workaround in deposit tests**

- **Found during:** Task 1 test execution.
- **Issue:** `apply_balance_delta` and the goal bump both use raw `text()` UPDATE statements. SQLAlchemy's identity-map cache holds the previously-loaded `Account`/`Goal` ORM instance with the OLD `balance_cents`/`current_cents`, so post-update `select(Account).where(id=…)` returns the stale cached row. (Pre-existing behaviour — same identity-map issue exists in `tests/services/test_roundup.py::test_account_balance_reduced_by_delta`, where the test also fails for the identical reason.)
- **Fix:** In `test_create_deposit_applies_balance_delta` and `test_create_deposit_with_goal_id_bumps_goal_current_cents`, read the post-update value via raw `text("SELECT … WHERE id = :id")` instead of `select(Model)`. This bypasses the identity map cleanly. Documented in test comments.
- **Files modified:** `tests/services/test_savings.py`
- **Commit:** `0e5b6a1`

### Plan-snippet adjustments (non-deviations)

The PLAN.md snippet for `create_deposit` had a few rough edges — the implementation in `0e5b6a1` corrects them while preserving the contract:

- The snippet imports `from sqlalchemy import text` only inside the function body but uses it at module scope; my version imports `text` at module scope cleanly.
- The snippet uses `_today_in_app_tz` for tx_date but never resolves a `period_id` for the deposit txn — `ActualTransaction.period_id` is `NOT NULL`. My version reuses `app.services.actual._resolve_period_for_date` (the same helper `create_actual_v10` uses) so deposits land in the correct period.
- The snippet's `description` is hard-coded to "Пополнение копилки"; my version accepts a `description` kwarg with that string as the default, matching the parent agent's scope spec.

### Spawner-scope vs PLAN.md naming

The parent agent's scope mentioned `get_or_create_config`, `update_config`, `get_savings_aggregate` while PLAN.md uses `upsert_config`, `get_savings_snapshot`. I followed PLAN.md naming (authoritative TDD plan); the contract matches the spawner's intent. If the router layer (plan 22.13) wants the spawner-scope names, thin re-exports can be added there.

## Self-Check: PASSED

- `app/services/savings.py` exists — verified via `wc -l` (427 lines)
- `app/services/goals.py` exists — verified via `wc -l` (269 lines)
- `tests/services/test_savings.py` exists — verified via `wc -l` (744 lines)
- `tests/services/test_goals.py` exists — verified via `wc -l` (568 lines)
- AST parse passes for all four files
- `grep -c "def get_savings_snapshot\|def upsert_config\|def create_deposit" app/services/savings.py` → 3
- `grep -c "def create_goal\|def list_goals\|def get_goal\|def update_goal\|def delete_goal" app/services/goals.py` → 5
- `grep -c "class GoalNotFoundError" app/services/goals.py` → 1
- `pytest tests/services/test_savings.py tests/services/test_goals.py -q` → `47 passed in 1.80s`
- Commits exist:
  - `2a1e4e0` fix(22-08): lazy-import PlanTemplateItem in planned.py
  - `0e5b6a1` feat(22-08): savings service (BE-08 / BE-09 / BE-10)
  - `272d967` test(22-08): goal CRUD service tests (BE-11)
