---
phase: 70-convergence-abstractions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  # --- New type + DTO retypes (the source of the change) ---
  - ios/BudgetPlanner/Networking/BusinessDate.swift
  - ios/BudgetPlanner/Networking/DTO/GoalDTO.swift
  - ios/BudgetPlanner/Networking/DTO/SubscriptionV10DTO.swift
  - ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift
  - ios/BudgetPlanner/Networking/DTO/CommonDTO.swift
  - ios/BudgetPlanner/Networking/DTO/ManagementDTO.swift
  - ios/BudgetPlanner/Networking/APIClient.swift
  - ios/BudgetPlannerTests/Networking/BusinessDateTests.swift
  - ios/project.yml
  # --- Consumer fallout: retyping the 6 wire-DATE fields to BusinessDate
  #     ripples to ~126 references across 27 files in BOTH shells. The
  #     executor MUST audit every one and fix mixed Date/BusinessDate
  #     expressions (see Task 2 audit list + two confirmed hotspots).
  #     This is the full enumerated consumer set, not an estimate. ---
  # v06 shell (Features/* + Domain/*):
  - ios/BudgetPlanner/Domain/LocalNotifications.swift
  - ios/BudgetPlanner/Domain/Period.swift
  - ios/BudgetPlanner/Features/AI/AIChatView.swift
  - ios/BudgetPlanner/Features/AI/AIProposalSheet.swift
  - ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift
  - ios/BudgetPlanner/Features/Management/AnalyticsView.swift
  - ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift
  - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift
  - ios/BudgetPlanner/Features/Management/SubscriptionsViewData.swift
  - ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep4GoalsView.swift
  - ios/BudgetPlanner/Features/Savings/GoalDetailView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsViewData.swift
  - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift
  - ios/BudgetPlanner/Features/Transactions/TransactionsView.swift   # HOTSPOT (a): createdAt ?? txDate @ :158
  # V10 shell (FeaturesV10/*):
  - ios/BudgetPlanner/FeaturesV10/Accounts/AccountDetailV10View.swift
  - ios/BudgetPlanner/FeaturesV10/Accounts/AccountDetailV10ViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsData.swift
  - ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Analytics/AnalyticsData.swift        # HOTSPOT (b): txDate >= periodStart / grouping / Calendar @ :96-121
  - ios/BudgetPlanner/FeaturesV10/Analytics/AnalyticsV10ViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/CategoryDetail/CategoryDetailView.swift
  - ios/BudgetPlanner/FeaturesV10/Onboarding/Step04GoalView.swift
  - ios/BudgetPlanner/FeaturesV10/Savings/SavingsV10View.swift
  - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsData.swift
  - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift
  - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10View.swift
autonomous: true
requirements: [R7]
must_haves:
  truths:
    - "A distinct BusinessDate type represents wire DATE (yyyy-MM-dd) business-date fields, separate from audit Date timestamps"
    - "BusinessDate carries MSK (Europe/Moscow) midnight semantics as a property of the type — encode and decode are MSK-pinned in one place"
    - "The shared APIClient JSONDecoder no longer special-cases yyyy-MM-dd by string-format heuristic; bare-date fields decode through BusinessDate, audit timestamps decode as ISO-8601 Date"
    - "Every mixed Date/BusinessDate expression across BOTH shells (~126 refs / 27 files) is audited and fixed; the two confirmed hotspots (TransactionsView createdAt ?? txDate; AnalyticsData txDate>=periodStart + grouping + Calendar) compile AND stay behavior-correct"
    - "Both shells build and the full iOS suite (609 baseline) plus the existing date-decode regression tests stay green"
  artifacts:
    - path: "ios/BudgetPlanner/Networking/BusinessDate.swift"
      provides: "BusinessDate value type with MSK-pinned Codable + Date/Comparable bridging"
      min_lines: 40
      contains: "struct BusinessDate"
    - path: "ios/BudgetPlannerTests/Networking/BusinessDateTests.swift"
      provides: "Round-trip + MSK-midnight + comparison tests for BusinessDate"
      contains: "Europe/Moscow"
  key_links:
    - from: "ios/BudgetPlanner/Networking/DTO/GoalDTO.swift"
      to: "ios/BudgetPlanner/Networking/BusinessDate.swift"
      via: "due / business-date fields typed as BusinessDate"
      pattern: "BusinessDate"
    - from: "ios/BudgetPlanner/Networking/APIClient.swift"
      to: "ios/BudgetPlanner/Networking/BusinessDate.swift"
      via: "decoder date strategy no longer branches on yyyy-MM-dd format"
      pattern: "dateDecodingStrategy"
---

<objective>
Workstream E / E2 / R7 — introduce a distinct `BusinessDate` type on the iOS client for wire `DATE` (yyyy-MM-dd) business-date fields, separating them from audit-time `Date` timestamps. MSK (Europe/Moscow) midnight semantics become a property of the type instead of a format-detection heuristic in the shared `APIClient` decoder. Removes the MSK-decode band-aid (WR-05) while preserving the exact wire behavior the date-decode regression tests lock.

Purpose: Root-fix the "is this string a business date or a timestamp?" guessing in the decoder; make MSK semantics explicit and type-checked.
Output: `BusinessDate.swift`, business-date DTO fields retyped, decoder de-heuristified, ALL consumer fallout fixed across both shells, regression-locked tests.

BLAST RADIUS NOTE: retyping the 6 wire-DATE fields ripples to ~126 references across 27 consumer files in BOTH shells (v06 Features/Domain + V10 FeaturesV10). `files_modified` enumerates the full set. This is intentionally a large but cohesive single plan: the unit of work is "BusinessDate type + every site that touches a business date." If the blast radius feels too large mid-execution, the executor MAY keep it cohesive (do NOT split mid-flight) but MUST gate on BOTH shells building AND the full 609 suite green — the existing Task 3 gate already catches any breakage. Do not partial-land.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-convergence-abstractions/70-CONTEXT.md
@ios/BudgetPlanner/Networking/APIClient.swift
@ios/BudgetPlannerTests/Networking/APIClientDateDecodeTests.swift

<interfaces>
<!-- Field classification — extracted from DTO inventory. Executor: use directly. -->

BUSINESS-DATE fields (wire yyyy-MM-dd, MSK semantics) -> retype to BusinessDate / BusinessDate?:
  - GoalDTO.due (Date? -> BusinessDate?)            — GoalDTO.swift
  - SubscriptionV10DTO.nextChargeDate (Date -> BusinessDate) — SubscriptionV10DTO.swift (custom init(from:) — update the decode line ~58)
  - SubscriptionV10UpdateRequest.nextChargeDate (Date? -> BusinessDate?) — SubscriptionV10DTO.swift encode side (~79,103)
  - ActualV10DTO/TransactionDTO txDate (Date -> BusinessDate) — TransactionDTO.swift (~21, ~62)
  - PlannedDTO.plannedDate (Date? -> BusinessDate?)  — TransactionDTO.swift (~86)
  - BudgetPeriod periodStart / periodEnd (Date -> BusinessDate) — CommonDTO.swift (~41,42,62,63) AND ManagementDTO.swift (~82,83)
  - ManagementDTO base SubscriptionDTO.nextChargeDate at ~13 (Date -> BusinessDate). The String-typed nextChargeDate at ~24/~33 are SEPARATE legacy request/draft DTOs — LEAVE those String fields untouched.

AUDIT-TIME fields (wire ISO-8601 timestamp) -> STAY Date (do NOT touch):
  createdAt, closedAt, onboardedAt, generatedAt, lastSeenAt.

GoalCreateRequest.due (GoalDTO.swift ~50-74) already hand-rolls a yyyy-MM-dd MSK encode. Once `due` is BusinessDate, REMOVE that band-aid encode block and encode the BusinessDate directly (its own encode emits MSK yyyy-MM-dd).

Decoder heuristic to REMOVE (APIClient.swift ~29-63): the `.custom` closure's `yyyy-MM-dd -> MSK` branch. KEEP the 3 timestamp branches the regression tests `test_timestamp_withZ/withFractionalSeconds/withoutZone` pin (ISO8601 with fractional seconds, ISO8601 plain, no-zone DateFormatter fallback). After E2 the shared decoder only handles audit-time `Date`; `BusinessDate` self-decodes from its own singleValueContainer string and is unaffected by `decoder.dateDecodingStrategy`.

CONSUMER FALLOUT — the 27 files in `files_modified` read these fields today as `Date` and will NOT compile after the retype until each access is reconciled. Three reconciliation moves:
  1. SwiftUI DatePicker binding / Calendar component / DateFormatter string-build -> read the `.date` bridge (e.g. `goal.due?.date`, `sub.nextChargeDate.date`).
  2. Comparison / sort (`<`, `>=`, `==`) between two BusinessDates -> compare BusinessDate directly (Comparable/Equatable) — no `.date` needed.
  3. Mixed expression where one side is an audit `Date?` and the other is a now-`BusinessDate` -> bridge the BusinessDate side with `.date` so both sides are `Date`. (This is hotspot (a).)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BusinessDate type with MSK-pinned Codable</name>
  <files>ios/BudgetPlanner/Networking/BusinessDate.swift, ios/BudgetPlannerTests/Networking/BusinessDateTests.swift, ios/project.yml</files>
  <behavior>
    - Decoding "2027-01-01" yields a BusinessDate whose Date bridge is 2027-01-01 00:00 Europe/Moscow (== 2026-12-31 21:00 UTC) — matches the WR-05 contract APIClientDateDecodeTests pins.
    - Encoding that BusinessDate emits "2027-01-01" (MSK-formatted, no time) — symmetric with the old GoalCreateRequest hand-roll.
    - BusinessDate is Comparable so `nextChargeDate < nextChargeDate` sorts keep compiling.
    - BusinessDate is Equatable + Hashable (so it can be a Dictionary(grouping:by:) key — see hotspot (b)).
    - A `var date: Date` bridge exists for SwiftUI DatePicker / Calendar reads; `init(_ date: Date)` normalizes a picked Date to MSK midnight for encode.
  </behavior>
  <action>
Create `ios/BudgetPlanner/Networking/BusinessDate.swift`: `struct BusinessDate: Codable, Equatable, Hashable, Comparable`. Store the canonical instant as a `Date` at MSK midnight (document the representation). `init(from:)` decodes a single `String` "yyyy-MM-dd" via a DateFormatter pinned to Europe/Moscow + en_US_POSIX (reuse the exact config from the current APIClient yyyy-MM-dd branch and GoalCreateRequest encode). `encode(to:)` writes the MSK-formatted "yyyy-MM-dd" string. Add `var date: Date` and `init(_ date: Date)` (MSK-midnight normalize). Comparable + Hashable on the underlying MSK-midnight instant (so two BusinessDates representing the same calendar day in MSK hash equal — critical for hotspot (b) grouping).
Check how XcodeGen groups sources in `ios/project.yml` (likely a directory glob over BudgetPlanner/ and BudgetPlannerTests/). Only edit project.yml if the glob does NOT auto-include the two new files; otherwise leave it. Run `cd ios && xcodegen generate`.
Write `BusinessDateTests.swift` covering the 5 behaviors. RED first (type absent), then GREEN. swift-format the new files only.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/BusinessDateTests 2>&1 | grep -E "Test Suite .*(passed|failed)|error:" | head -5</automated>
  </verify>
  <done>BusinessDate exists; BusinessDateTests all pass; type compiles standalone; Hashable groups same-MSK-day instances equal.</done>
</task>

<task type="auto">
  <name>Task 2: Retype business-date DTO fields + de-heuristify decoder + audit ALL consumer fallout in both shells</name>
  <files>ios/BudgetPlanner/Networking/DTO/GoalDTO.swift, ios/BudgetPlanner/Networking/DTO/SubscriptionV10DTO.swift, ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift, ios/BudgetPlanner/Networking/DTO/CommonDTO.swift, ios/BudgetPlanner/Networking/DTO/ManagementDTO.swift, ios/BudgetPlanner/Networking/APIClient.swift, plus the 27 consumer files enumerated in files_modified (both shells)</files>
  <action>
STEP 1 — Retype + de-heuristify (the source change):
Retype the BUSINESS-DATE fields from the interfaces block to `BusinessDate`/`BusinessDate?` (leave the String-typed legacy nextChargeDate fields and all AUDIT-TIME fields as-is). In SubscriptionV10DTO custom `init(from:)`, change the nextChargeDate decode to `try c.decode(BusinessDate.self, ...)`. In GoalDTO remove the hand-rolled MSK encode block in GoalCreateRequest and encode the BusinessDate `due` directly. In APIClient.swift remove the `yyyy-MM-dd -> MSK` branch from the `.custom` dateDecodingStrategy closure, KEEPING the 3 timestamp branches the regression tests lock; the strategy now only serves audit-time Date.

STEP 2 — Audit EVERY mixed Date/BusinessDate expression across BOTH shells (NOT just compile-fix; verify behavior):
Run `grep -rn "\.due\b\|nextChargeDate\|txDate\|plannedDate\|periodStart\|periodEnd" BudgetPlanner/Features BudgetPlanner/FeaturesV10 BudgetPlanner/Domain BudgetPlanner/App` (≈126 refs / 27 files — they are all in files_modified). For each site apply the correct reconciliation move from the interfaces CONSUMER FALLOUT note: `.date` bridge for DatePicker/Calendar/formatting, direct BusinessDate compare for sorts/filters, `.date` bridge on the BusinessDate side of any mixed audit-Date expression. Do NOT blanket-append `.date` — a comparison between two BusinessDates must stay a BusinessDate comparison (semantically clearer + avoids re-introducing a Date round-trip).

Two CONFIRMED hotspots that MUST be handled explicitly (do not let them silently slip through a mechanical fix):
  (a) `Features/Transactions/TransactionsView.swift:158-159` — `lhs.createdAt ?? lhs.txDate` (and the rhs twin). `createdAt` is an audit `Date?`; `txDate` is now `BusinessDate`. The `??` won't type-check until the BusinessDate side bridges: `lhs.createdAt ?? lhs.txDate.date`. This sort key (`createdAt ?? txDate` DESC, mirrors V10) must keep producing the same ordering — bridging txDate to its MSK-midnight Date preserves it.
  (b) `FeaturesV10/Analytics/AnalyticsData.swift:96-121` — three distinct BusinessDate-semantics shifts in one function:
       - `$0.txDate >= periodStart && $0.txDate <= periodEnd` (line ~96): periodStart/periodEnd here are the LOCAL `Date` params of the bucketing fn (param at ~92), while `txDate` is now BusinessDate. Reconcile so the comparison is well-typed AND the inclusive range still selects the same transactions (bridge txDate via `.date`, OR — cleaner — confirm whether the surrounding period bounds should themselves be BusinessDate; pick whichever keeps the day-inclusive boundary correct and document the choice).
       - `Dictionary(grouping: filtered, by: { $0.txDate })` (line ~98): the grouping KEY moves from `Date` to `BusinessDate`. This relies on BusinessDate Hashable equating same-MSK-day instances (Task 1). Verify buckets are still one-per-calendar-day in MSK and do NOT fragment (e.g. if any txDate carried a non-midnight Date before, grouping changes — confirm it doesn't).
       - `cal.component(.day, from: t.txDate)` (line ~121): Calendar reads a `Date`, so bridge `t.txDate.date`. Ensure `cal` is (or the bridge yields) MSK so the day-of-month is the MSK day, not UTC.
     Hotspot (b) must be COVERED behaviorally, not merely compiled: rely on the full-suite AnalyticsData/Analytics tests in Task 3; if no test exercises the grouping-by-day path, add a focused assertion that a known set of txDates buckets into the expected MSK day-buckets.

swift-format only touched files. `cd ios && xcodegen generate` (no new files, but harmless).
  </action>
  <verify>
    <automated>cd ios && grep -v '^[[:space:]]*//' BudgetPlanner/Networking/DTO/GoalDTO.swift | grep -q "BusinessDate" && grep -v '^[[:space:]]*//' BudgetPlanner/Networking/DTO/SubscriptionV10DTO.swift | grep -q "BusinessDate" && ! grep -v '^[[:space:]]*//' BudgetPlanner/Networking/APIClient.swift | grep -q 'yyyy-MM-dd' && grep -q "txDate.date\|txDate\.date" BudgetPlanner/Features/Transactions/TransactionsView.swift && echo "retyped-deheuristified-hotspotA-bridged"</automated>
  </verify>
  <done>Business-date fields typed BusinessDate; APIClient decoder no longer contains the yyyy-MM-dd branch; ALL 27 consumer files reconciled; hotspot (a) bridges createdAt ?? txDate.date keeping sort order; hotspot (b) range-filter + grouping-by-MSK-day + Calendar-day all type-check AND stay behavior-correct.</done>
</task>

<task type="auto">
  <name>Task 3: Both shells build + full suite + date-decode regression gate</name>
  <files>ios/BudgetPlannerTests/Networking/APIClientDateDecodeTests.swift</files>
  <action>
The existing `APIClientDateDecodeTests` decode `GoalDTO` with a `due` field; after retyping `due` to BusinessDate, update ONLY the assertion plumbing (read `goal.due?.date` instead of `goal.due` as Date) — the MSK-midnight expectation (2027-01-01 MSK == 2026-12-31 21:00 UTC) MUST remain unchanged and still pass, proving BusinessDate preserves the exact WR-05 contract. The `test_timestamp_*` cases assert on `createdAt` (audit Date, unchanged) and must pass untouched. If Task 2 added a focused AnalyticsData grouping-by-MSK-day assertion, ensure it runs here. Build BOTH shells and run the full suite as the zero-regression gate — this gate is what guarantees the 27-file blast radius landed cleanly; do not consider the plan done until it is green.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -3</automated>
  </verify>
  <done>BUILD SUCCEEDED; full `xcodebuild test ... iPhone 17 Pro` suite 609 passing 0 failing (incl. APIClientDateDecodeTests MSK-midnight + BusinessDateTests + Analytics grouping coverage); both shells render dates identically (no UTC drift).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> iOS client | Wire DATE strings cross here; misclassifying a business date as a timestamp shifts fire-dates a day (T-67-07-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-02-01 | Tampering | BusinessDate encode/decode timezone | mitigate | MSK pinned in one type; APIClientDateDecodeTests MSK-midnight assertion regression-locks the exact instant; BusinessDateTests round-trip |
| T-70-02-02 | Information Disclosure | none new — pure client type change | accept | No wire/format change; same yyyy-MM-dd strings on the wire |
| T-70-02-03 | Tampering | Analytics grouping-by-day fragments or shifts under BusinessDate Hashable/Calendar semantics (hotspot b) | mitigate | BusinessDate Hashable keyed on MSK-midnight; AnalyticsData grouping covered behaviorally by full-suite (+ focused assertion if uncovered), not just compiled |
</threat_model>

<verification>
- BusinessDateTests + APIClientDateDecodeTests green (MSK-midnight contract preserved).
- APIClient decoder no longer branches on yyyy-MM-dd (grep gate, comment-filtered).
- Both confirmed hotspots fixed AND behavior-correct (sort order preserved; MSK-day grouping intact).
- Both shells build; full suite 609 green; dates render identically in MainShell + V10MainShell.
</verification>

<success_criteria>
- BusinessDate type introduced; all wire DATE fields retyped; audit timestamps unchanged.
- MSK semantics live in the type; decoder de-heuristified.
- All 27 consumer files reconciled; hotspots (a) and (b) handled explicitly and behavior-correct.
- Zero behavioral regression; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/70-convergence-abstractions/70-02-SUMMARY.md`
</output>
</content>
</invoke>
