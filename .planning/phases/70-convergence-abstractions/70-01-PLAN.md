---
phase: 70-convergence-abstractions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # AuthAPI.swift = where the actual deprecation edits land: the legacy
  # `enum MeAPI` and `enum CategoriesAPI` to deprecate live HERE (AuthAPI.swift
  # lines ~16 and ~23), NOT in MeAPI.swift.
  - ios/BudgetPlanner/Networking/Endpoints/AuthAPI.swift
  - ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift
  - ios/BudgetPlanner/Networking/Endpoints/ManagementAPI.swift
  # MeAPI.swift = READ-ONLY reference. It houses the CANONICAL `MeV10API`
  # (read-only `MeV10APIClient` / `LiveMeV10API`); the executor reads it only
  # to name MeV10API correctly in the MeAPI deprecation message. No edits expected here.
  - ios/BudgetPlanner/Networking/Endpoints/MeAPI.swift
  - .planning/LEGACY-V10-DEBT-REGISTRY.md
autonomous: true
requirements: [R3]
must_haves:
  truths:
    - "Every legacy endpoint enum still exercised by call-sites carries @available(*, deprecated, message:) pointing at the canonical V10 enum"
    - "Provably-equivalent legacy call-sites (same route, interchangeable DTO) are migrated to the canonical V10 enum"
    - "Non-equivalent legacy↔V10 pairs (different DTO shape, V10 missing create/delete) are left in place and ticketed in the debt registry"
    - "Both shells (MainShell v06 + V10MainShell) build with zero new warnings-as-errors and the full iOS suite (609 baseline) stays green"
  artifacts:
    - path: ".planning/LEGACY-V10-DEBT-REGISTRY.md"
      provides: "Per-route convergence audit: legacy enum, V10 enum, canonical pick, equivalence verdict, migration disposition, follow-up ticket"
      min_lines: 40
    - path: "ios/BudgetPlanner/Networking/Endpoints/AuthAPI.swift"
      provides: "@available(deprecated) on MeAPI + CategoriesAPI (non-equivalent legacy reads) — both enums live in this file"
      contains: "@available(*, deprecated"
  key_links:
    - from: "ios/BudgetPlanner/Networking/Endpoints/*API.swift"
      to: ".planning/LEGACY-V10-DEBT-REGISTRY.md"
      via: "deprecation message references the canonical enum + registry ticket id"
      pattern: "@available\\(\\*, deprecated"
---

<objective>
Workstream C / R3 — converge the iOS legacy enum-API layer onto the V10 canonical surface WITHOUT touching shells. For every duplicated route, pick the canonical enum (V10 = superset), mark the legacy enum `@available(*, deprecated, message:)`, migrate only the call-sites that are PROVABLY equivalent (same route + interchangeable DTO), and record every non-equivalent pair in a debt registry instead of force-migrating.

Purpose: Stop the dozens of "legacy↔V10" comments from rotting into silent drift; give a single canonical API per route while keeping both shells alive.
Output: Deprecated legacy enums, a few equivalent call-site migrations, and `.planning/LEGACY-V10-DEBT-REGISTRY.md` enumerating the rest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-convergence-abstractions/70-CONTEXT.md

<interfaces>
<!-- Convergence audit — canonical decisions (extracted from inventory). Executor: use directly, do not re-derive. -->

FILE MAP (where each enum lives — read carefully, the legacy/canonical split spans two files):
  - AuthAPI.swift  : `enum AuthAPI` (~4), legacy `enum MeAPI` (~16), legacy `enum CategoriesAPI` (~23)  ← deprecation edits land HERE
  - MeAPI.swift    : canonical `MeV10API` + `MeV10APIClient` protocol + `LiveMeV10API` (read-only reference; the deprecation MESSAGE for MeAPI names MeV10API from here) ← NO edits

Legacy↔V10 enum pairs and their equivalence verdict:

| Route | Legacy enum (file) | V10 enum (file) | Canonical | Equivalent? | Disposition |
|-------|--------------------|-----------------|-----------|-------------|-------------|
| GET /me | MeAPI.current() → UserDTO (AuthAPI.swift ~16; 1 call-site: Auth/AuthStore.swift:70) | MeV10API.shared.fetchMeV10() → MeV10Response (MeAPI.swift) | MeV10 (superset) | NO — different DTO (UserDTO vs MeV10Response); AuthStore depends on UserDTO shape | Deprecate MeAPI (in AuthAPI.swift); LEAVE call-site; ticket DEBT-70-ME |
| GET /categories | CategoriesAPI.list() → [CategoryDTO] (AuthAPI.swift ~23; 5 call-sites in v06 Features/*) | CategoriesV10API.list() → [CategoryV10DTO] (CategoriesV10API.swift) | V10 (4-valued superset) | NO — CategoryDTO (2-valued) vs CategoryV10DTO differ; v06 screens decode 2-valued | Deprecate CategoriesAPI (in AuthAPI.swift); LEAVE call-sites; ticket DEBT-70-CAT |
| POST/PATCH/DELETE /actual | ActualAPI (TransactionsAPI.swift; create/update used in v06, delete shared by BOTH shells) | ActualV10API (list/create only) | mixed — ActualV10API lacks update+delete | PARTIAL — ActualAPI.delete is the shared canonical delete already used by V10 VMs | Deprecate ActualAPI.create/update only (NOT .delete — it is the canonical shared delete); keep .delete un-deprecated; ticket DEBT-70-ACT for V10 create/update parity |
| GET/POST/PATCH /subscriptions | SubscriptionsAPI (ManagementAPI.swift; only create+update still called, in v06 SubscriptionsView) | SubscriptionsV10API (no create) | mixed — V10 lacks create | NO — V10API has no create; v06 editor needs legacy create then V10 patch | Deprecate SubscriptionsAPI; LEAVE create/update call-sites; ticket DEBT-70-SUB for V10 create parity |
| categories write | CategoriesWriteAPI (TransactionsAPI.swift; create/update/delete in Features/Management/CategoriesView.swift) | CategoriesV10API.update (no create/delete) | mixed | NO — V10 lacks create/delete | Deprecate nothing yet OR deprecate with message naming the gap; ticket DEBT-70-CATW |

Migration that IS provably equivalent (do migrate):
- ActualAPI.delete → it is already the canonical shared delete; no migration needed, just DO NOT deprecate it. Add a doc-comment marking it canonical-shared.
- No other call-site is provably equivalent given DTO divergence. Everything else is ticketed.

Deprecation message format (use exactly this shape):
  @available(*, deprecated, message: "Legacy v0.x — canonical is <V10Enum>. Non-equivalent (<reason>); tracked DEBT-70-XX. See .planning/LEGACY-V10-DEBT-REGISTRY.md")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the legacy↔V10 debt registry</name>
  <files>.planning/LEGACY-V10-DEBT-REGISTRY.md</files>
  <action>
Create `.planning/LEGACY-V10-DEBT-REGISTRY.md`. For EACH row in the audit table above, write a ticket section: DEBT-70-ME, DEBT-70-CAT, DEBT-70-ACT, DEBT-70-SUB, DEBT-70-CATW. Each ticket records: legacy enum + file, V10 enum + file, canonical pick, the concrete equivalence-blocker (e.g. "CategoryDTO is 2-valued CategoryKind; CategoryV10DTO is 4-valued ActualKindV10 — v06 Home/Template/Categories/AI screens decode the 2-valued shape and changing them is behavioral"), current call-sites (use the grep results: CategoriesAPI has 5, SubscriptionsAPI.create/update 2, MeAPI 1 in AuthStore), and the follow-up action (what would make migration safe — typically "after D shared-domain extraction or write-DTO codegen tail"). Add a top section "Convergence audit summary" with the full table. Also sweep the dozens of inline "legacy↔V10" comments: list the files carrying them (App/AppRouter.swift, Networking/DTO/*, Endpoints/*, Features/*, FeaturesV10/*) under a "Comment debt — sources" heading so the registry is the single index. Do NOT delete the inline comments in this plan.
  </action>
  <verify>
    <automated>test -f .planning/LEGACY-V10-DEBT-REGISTRY.md && grep -q "DEBT-70-ME" .planning/LEGACY-V10-DEBT-REGISTRY.md && grep -q "DEBT-70-CATW" .planning/LEGACY-V10-DEBT-REGISTRY.md && grep -c "DEBT-70-" .planning/LEGACY-V10-DEBT-REGISTRY.md</automated>
  </verify>
  <done>Registry exists with 5 DEBT-70-* tickets, each naming legacy enum / V10 canonical / blocker / call-sites / follow-up; audit summary table present.</done>
</task>

<task type="auto">
  <name>Task 2: Deprecate legacy enums + mark canonical-shared routes</name>
  <files>ios/BudgetPlanner/Networking/Endpoints/AuthAPI.swift, ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift, ios/BudgetPlanner/Networking/Endpoints/ManagementAPI.swift</files>
  <action>
Add `@available(*, deprecated, message: ...)` to the legacy enums per the audit table, using the exact message format from the interfaces block (name the canonical V10 enum + the DEBT-70-XX ticket):
  - AuthAPI.swift: `enum MeAPI` (line ~16) → deprecated, canonical MeV10API, DEBT-70-ME. `enum CategoriesAPI` (line ~23) → deprecated, canonical CategoriesV10API, DEBT-70-CAT. (BOTH legacy enums live in AuthAPI.swift — this is the only file edited for the /me + /categories deprecations. MeAPI.swift is read-only: open it ONLY to confirm the canonical name `MeV10API` for the message; do not edit it.)
  - ManagementAPI.swift: `enum SubscriptionsAPI` (line ~4) → deprecated, canonical SubscriptionsV10API, DEBT-70-SUB.
  - TransactionsAPI.swift: `enum ActualAPI` (line ~4) → deprecate the ENUM but you MUST keep `ActualAPI.delete` callable without a deprecation warning at its many call-sites (it is the canonical shared delete used by BOTH shells). Approach: do NOT mark the whole `ActualAPI` enum deprecated. Instead mark ONLY `create` and `update` static methods with `@available(*, deprecated, ...)`, add a `/// Canonical shared delete — used by v06 + V10 VMs (DEBT-70-ACT).` doc-comment on `delete`, and leave `delete` un-attributed. `enum CategoriesWriteAPI` (line ~103) → deprecated, message naming the V10 create/delete gap, DEBT-70-CATW.
NOTE: `MeV10API` lives in Endpoints/MeAPI.swift — verify the canonical name you reference compiles. Do NOT migrate any call-site in this task (all are non-equivalent per audit); deprecation warnings at existing call-sites are EXPECTED and acceptable (they are warnings, not errors — the build must not treat them as errors). Run `cd ios && xcodegen generate` is NOT needed (no new files), but swift-format the 3 touched files only.
  </action>
  <verify>
    <automated>cd ios && grep -c "@available(\*, deprecated" BudgetPlanner/Networking/Endpoints/AuthAPI.swift BudgetPlanner/Networking/Endpoints/ManagementAPI.swift BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift | grep -v ':0'</automated>
  </verify>
  <done>MeAPI, CategoriesAPI (both in AuthAPI.swift), SubscriptionsAPI, CategoriesWriteAPI enums + ActualAPI.create/update marked deprecated with canonical-naming messages; ActualAPI.delete left un-deprecated with canonical-shared doc-comment; MeAPI.swift untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build both shells + full iOS suite — zero-regression gate</name>
  <files>ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift</files>
  <action>
Verify the deprecations do not break compilation and produce NO warnings-as-errors. The project must NOT have `-warnings-as-errors` such that deprecation warnings fail the build — confirm by building. If `xcodebuild` reports the build FAILED due to deprecation warnings being treated as errors, fix by suppressing at the legacy call-sites is OUT of scope; instead the deprecation must be downgradeable — but per repo config deprecation warnings are warnings only, so a clean build is expected. swift-format the touched files. This task is the zero-regression gate, not new code; the file edit is a no-op confirmation touch only if needed.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate >/dev/null 2>&1; xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | grep -E "BUILD SUCCEEDED|error:" | head -5</automated>
  </verify>
  <done>`xcodebuild build` → BUILD SUCCEEDED (deprecation warnings present but NOT errors). Then full suite green: `xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | xcbeautify` shows 609 tests passing, 0 failing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS client → API | All endpoint enums cross this; deprecation is compile-time only, no wire change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01-01 | Tampering | Accidental call-site behavior change during migration | mitigate | Only migrate provably-equivalent routes (none here — all ticketed); zero call-site edits in this plan keeps wire behavior identical |
| T-70-01-02 | Repudiation | Silent drift if legacy enum un-deprecated | mitigate | Every legacy enum carries deprecation message + registry ticket; CI grep gate in Task 2 verify |
</threat_model>

<verification>
- `xcodebuild build` BUILD SUCCEEDED for the single BudgetPlanner target (hosts both shells); deprecation warnings present, not errors.
- Full iOS suite: 609 passing, 0 failing (no behavioral change — zero call-site migrations).
- Registry has 5 tickets; grep gate confirms deprecation attributes present.
</verification>

<success_criteria>
- Legacy enums (MeAPI, CategoriesAPI, SubscriptionsAPI, CategoriesWriteAPI, ActualAPI.create/update) deprecated with canonical-naming messages.
- ActualAPI.delete preserved as canonical-shared (no deprecation, both shells keep calling it).
- `.planning/LEGACY-V10-DEBT-REGISTRY.md` enumerates every non-equivalent pair + comment-debt index.
- Both shells build; full suite green; zero behavioral regression.
</success_criteria>

<output>
After completion, create `.planning/phases/70-convergence-abstractions/70-01-SUMMARY.md`
</output>
</content>
</invoke>
