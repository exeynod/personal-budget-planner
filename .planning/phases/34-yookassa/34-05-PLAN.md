---
phase: 34-yookassa
plan: 05
type: execute
wave: 3
depends_on: [34-02, 34-03]
requirements:
  - REQ-34-04
  - REQ-34-06
commit: 62c7a29
---

<objective>
HTTP-фасад: `POST /api/v1/billing/create-payment`, `GET /api/v1/me/subscription`, `POST /api/v1/me/subscription/cancel`. Это то, что вызывает frontend / iOS.
</objective>

## Task

1. `app/api/routes/billing.py`: `POST /billing/create-payment` — auth required, dispatches на `YookassaClient.create_payment` + persists `Payment` row (status=pending).
2. `app/api/routes/me.py` extend: `GET /me/subscription` (current SubscriptionBilling или null) + `POST /me/subscription/cancel` (status=canceled, period_end остаётся текущим).
3. `tests/test_billing.py` — 3 tests: create-payment happy + get-subscription для Pro user + cancel idempotency.

## Acceptance

- `pytest tests/test_billing.py` → 3 passed.

## Commit

`62c7a29` — feat(34-05): billing + subscription endpoints (REQ-34-04, REQ-34-06)
