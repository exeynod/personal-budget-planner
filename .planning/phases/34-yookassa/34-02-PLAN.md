---
phase: 34-yookassa
plan: 02
type: execute
wave: 2
depends_on: [34-01]
requirements:
  - REQ-34-01
commit: b701b47
---

<objective>
SQLAlchemy ORM models (`Payment`, `SubscriptionBilling`, status enums) + Pydantic schemas (`PaymentCreateRequest`, `PaymentCreateResponse`, `SubscriptionRead`) — типобезопасный слой над миграцией 0021.
</objective>

## Task

1. Добавить в `app/db/models.py`: классы `Payment`, `SubscriptionBilling`, PgEnum'ы `PaymentStatus`, `BillingTier`, `BillingStatus`.
2. Создать `app/api/schemas/billing.py` с Pydantic v2 schemas: `PaymentCreateRequest`, `PaymentCreateResponse`, `SubscriptionRead`.
3. `python -c "from app.db.models import Payment, SubscriptionBilling"` smoke.

## Acceptance

- ORM-классы importable.
- Pydantic schemas валидируют sample payloads.

## Commit

`b701b47` — feat(34-02): SQLAlchemy models + Pydantic schemas for billing (REQ-34-01)
