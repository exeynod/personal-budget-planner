---
phase: 34-yookassa
plan: 03
type: execute
wave: 2
depends_on: [34-02]
requirements:
  - REQ-34-02
commit: f6fa963
---

<objective>
Async YookassaClient wrapper (`app/services/yookassa_client.py`) — единственный HTTP-клиент к ЮKassa API. Обернутый mock-transport tests без сетевых вызовов.
</objective>

## Task

1. `app/services/yookassa_client.py`: `class YookassaClient` с `async create_payment(amount_cents, description, return_url) -> dict`.
2. Реальный HTTP через `httpx.AsyncClient`; auth — basic auth (shop_id:secret_key); base URL `https://api.yookassa.ru/v3/payments`.
3. `tests/test_yookassa_client.py` — 3 tests с `respx`/`httpx.MockTransport`:
   - happy path (200 + confirmation_url).
   - 4xx error → raises `YookassaAPIError`.
   - timeout → raises `YookassaTimeoutError`.

## Acceptance

- `pytest tests/test_yookassa_client.py` → 3 passed.

## Commit

`f6fa963` — feat(34-03): YookassaClient async wrapper + mock-transport tests (REQ-34-02)
