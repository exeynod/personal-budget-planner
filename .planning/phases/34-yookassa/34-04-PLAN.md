---
phase: 34-yookassa
plan: 04
type: execute
wave: 3
depends_on: [34-02, 34-03]
requirements:
  - REQ-34-03
  - REQ-34-05
commit: 312acb1
---

<objective>
Webhook handler `POST /webhooks/yookassa` + state machine для перевода `payment.status` (pending → succeeded/canceled) + side-effect: создание/продление `subscription_billing` записи (tier=pro, 30 days).
</objective>

## Task

1. `app/api/routes/webhooks.py`: `POST /webhooks/yookassa` (public, no auth).
2. Idempotency: lookup по `payment.yookassa_payment_id`; повторный webhook с тем же ID — no-op (return 200).
3. State machine: `pending → succeeded` triggers `_grant_pro(user, days=30)`; `pending → canceled` — без side-effects.
4. `tests/test_webhook_yookassa.py` — 3 tests: succeeded → активирует Pro; duplicate webhook → idempotent; canceled → no Pro.

## Acceptance

- `pytest tests/test_webhook_yookassa.py` → 3 passed.
- Webhook возвращает 200 даже на duplicate.

## Commit

`312acb1` — feat(34-04): YooKassa webhook + idempotent state machine (REQ-34-03, REQ-34-05)
