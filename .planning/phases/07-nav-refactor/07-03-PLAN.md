---
phase: 07-nav-refactor
plan: 03
type: execute
wave: 1
depends_on:
  - "07-02"
files_modified:
  - frontend/src/screens/HistoryView.tsx
  - frontend/src/screens/HistoryView.module.css
  - frontend/src/screens/PlannedView.tsx
  - frontend/src/screens/PlannedView.module.css
  - frontend/src/screens/TransactionsScreen.tsx
  - frontend/src/screens/TransactionsScreen.module.css
autonomous: true
requirements:
  - TXN-01
  - TXN-02
  - TXN-03
  - TXN-04
  - TXN-05

must_haves:
  truths:
    - "TransactionsScreen рендерит SubTabBar с под-табами История/План"
    - "HistoryView группирует факт-транзакции по дням с day-total (сумма расходов)"
    - "PlannedView группирует строки по категориям с source-badge (template/manual/subscription)"
    - "Фильтр-чипы Все/Расходы/Доходы + категории видны над списком в горизонтальном скролле"
    - "FAB context-aware: История → открывает ActualEditor, План → открывает PlanItemEditor"
    - "Старые ActualScreen.tsx и PlannedScreen.tsx остаются (удалятся в Plan 05 с App.tsx)"
  artifacts:
    - path: "frontend/src/screens/TransactionsScreen.tsx"
      provides: "Оболочка с SubTabBar и context-aware FAB"
      exports: ["TransactionsScreen"]
    - path: "frontend/src/screens/HistoryView.tsx"
      provides: "Список факт-транзакций по дням с filter chips"
      exports: ["HistoryView"]
    - path: "frontend/src/screens/PlannedView.tsx"
      provides: "Список плановых строк с source-badge и filter chips"
      exports: ["PlannedView"]
  key_links:
    - from: "TransactionsScreen.tsx"
      to: "SubTabBar.tsx"
      via: "import SubTabBar, SubTabId"
      pattern: "SubTabBar"
    - from: "TransactionsScreen.tsx"
      to: "HistoryView.tsx + PlannedView.tsx"
      via: "conditional render by activeSubTab"
      pattern: "activeSubTab === 'history'"
    - from: "HistoryView.tsx"
      to: "ActualEditor / BottomSheet"
      via: "import без изменений"
      pattern: "ActualEditor"
---

<objective>
Wave 1b: Создать TransactionsScreen (оболочка с SubTabBar), HistoryView (рефакторинг ActualScreen), PlannedView (рефакторинг PlannedScreen).

Purpose: Объединяет «История» и «План» под единым табом «Транзакции» с под-табами, filter chips и context-aware FAB.

Output: 6 новых файлов (.tsx + .module.css). Старые ActualScreen/PlannedScreen не удаляются на этом этапе — удалятся в Plan 05 при переключении App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-nav-refactor/07-CONTEXT.md
@.planning/phases/07-nav-refactor/07-02-SUMMARY.md

<interfaces>
<!-- SubTabBar из Plan 02 -->
```typescript
// frontend/src/components/SubTabBar.tsx
export type SubTabId = 'history' | 'plan';
export interface SubTabItem<T extends string> { id: T; label: string; }
export interface SubTabBarProps<T extends string> {
  active: T;
  onChange: (tab: T) => void;
  tabs: SubTabItem<T>[];
}
export function SubTabBar<T extends string>(props: SubTabBarProps<T>): JSX.Element;
```

<!-- Из ActualScreen.tsx — логика для HistoryView -->
```typescript
// groupByDate(rows: ActualRead[]) — переносим без изменений
// formatAmount(cents, kind) — переносим без изменений
// Props нового HistoryView:
export interface HistoryViewProps {
  categoryFilter?: number | null;
  onClearFilter?: () => void;
  inTransactions?: boolean; // если true — убираем header (SubTabBar уже есть выше)
}
```

<!-- Из PlannedScreen.tsx — логика для PlannedView -->
```typescript
// Всю бизнес-логику переносим
// Props нового PlannedView:
export interface PlannedViewProps {
  inTransactions?: boolean; // убираем ScreenHeader если true
  onNavigateToTemplate?: () => void;
}
```

<!-- FilterChip контракт (inline в TransactionsScreen или отдельный) -->
// Фильтр-чипы: Все / Расходы / Доходы + категории как горизонтальный scroll
// activeKindFilter: 'all' | 'expense' | 'income'
// activeCategoryFilter: number | null

<!-- Fab из Plan 02 -->
```typescript
// frontend/src/components/Fab.tsx
export interface FabProps { onClick: () => void; ariaLabel: string; }
export function Fab({ onClick, ariaLabel }: FabProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать HistoryView (рефакторинг ActualScreen)</name>
  <read_first>
    - frontend/src/screens/ActualScreen.tsx (полностью — копируем groupByDate, formatAmount, всю логику)
    - frontend/src/screens/ActualScreen.module.css (стили для копирования)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (HistoryView: убрать full-screen container, inTransactions prop; day-header total = сумма расходов за день)
  </read_first>
  <files>
    frontend/src/screens/HistoryView.tsx
    frontend/src/screens/HistoryView.module.css
  </files>
  <behavior>
    - Принимает `inTransactions?: boolean` — если true, НЕ рендерит header (его заменяет SubTabBar сверху)
    - Принимает `categoryFilter?: number | null` и `onClearFilter?: () => void` (cross-tab navigation из HomeScreen)
    - `activeKindFilter?: 'all' | 'expense' | 'income'` — принимается снаружи из TransactionsScreen
    - Day-header: label + total за день (сумма всех amount_cents расходов дня)
    - Сохранить swipe-to-delete (нет в ActualScreen — но FAB и BottomSheet остаются)
    - groupByDate() и formatAmount() — скопировать без изменений
    - FAB НЕ рендерится внутри HistoryView (управляется TransactionsScreen)
    - BottomSheet с ActualEditor — рендерится внутри HistoryView
  </behavior>
  <action>
    Создать frontend/src/screens/HistoryView.tsx на основе ActualScreen.tsx.

    Ключевые изменения от ActualScreen:
    1. Имя компонента: `ActualScreen` → `HistoryView`
    2. Props: добавить `inTransactions?: boolean` и `activeKindFilter?: 'all' | 'expense' | 'income'`
    3. CSS класс root: убрать минимальную высоту если `inTransactions` (контейнер TransactionsScreen уже задаёт размер)
    4. Header: `if (!inTransactions)` рендерить `<header>` блок (с onBack и titleBlock)
    5. Filter badge (categoryFilter) — оставить как есть
    6. Day-header total: добавить в `groupByDate` возвращаемый объект `totalCents: number` = сумма `amount_cents` строк где `kind === 'expense'`
    7. В JSX day-header: `<div className={styles.dateLabel}>{g.label}</div>` → добавить `{g.totalCents > 0 && <span className={styles.dateTotal}>−{(g.totalCents / 100).toLocaleString('ru-RU')} ₽</span>}`
    8. Фильтрация по kindFilter: если `activeKindFilter !== 'all'` (и !== undefined) — фильтровать `filteredRows` по `row.kind === activeKindFilter`
    9. FAB убрать полностью — FAB будет в TransactionsScreen
    10. BottomSheet + ActualEditor + handleSave/handleDelete — сохранить без изменений

    Создать HistoryView.module.css: скопировать ActualScreen.module.css + добавить:
    ```css
    .dateGroup {
      /* если уже есть в ActualScreen.module.css — скопировать */
    }
    .dateLabel {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .dateTotal {
      font-size: 12px;
      color: var(--color-text-muted);
      font-variant-numeric: tabular-nums;
    }
    ```

    Экспортировать тип `HistoryViewHandle` для возможности управления sheet из TransactionsScreen (необязательно для Phase 7, но предусмотреть через ref если нужно):
    ```typescript
    export interface HistoryViewHandle {
      openCreateSheet: () => void;
    }
    ```
    Использовать `forwardRef` + `useImperativeHandle` чтобы TransactionsScreen мог программно открыть sheet через FAB.
    ```typescript
    export const HistoryView = forwardRef<HistoryViewHandle, HistoryViewProps>(
      function HistoryView({ inTransactions, categoryFilter, onClearFilter, activeKindFilter }, ref) {
        // ...
        useImperativeHandle(ref, () => ({
          openCreateSheet: () => setSheet({ open: true, mode: 'create' }),
        }));
        // ...
      }
    );
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "export.*HistoryView" frontend/src/screens/HistoryView.tsx` возвращает >= 1
    - `grep -c "inTransactions" frontend/src/screens/HistoryView.tsx` возвращает >= 2
    - `grep -c "totalCents" frontend/src/screens/HistoryView.tsx` возвращает >= 2
    - `grep -c "activeKindFilter" frontend/src/screens/HistoryView.tsx` возвращает >= 2
    - `grep -c "forwardRef\|useImperativeHandle" frontend/src/screens/HistoryView.tsx` возвращает >= 2
    - `grep -c "dateTotal" frontend/src/screens/HistoryView.module.css` возвращает 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -i "HistoryView" | grep -c "error"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>HistoryView.tsx создан с inTransactions prop, day-total в header, activeKindFilter фильтрацией, forwardRef для FAB управления. TypeScript компилируется чисто.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать PlannedView + TransactionsScreen с filter chips и context-aware FAB</name>
  <read_first>
    - frontend/src/screens/PlannedScreen.tsx (полностью — бизнес-логика для PlannedView)
    - frontend/src/screens/PlannedScreen.module.css (стили)
    - frontend/src/screens/HistoryView.tsx (только что созданный — паттерн для PlannedView)
    - .planning/phases/07-nav-refactor/07-CONTEXT.md (TransactionsScreen: filter chips horizontal scroll, context-aware FAB)
  </read_first>
  <files>
    frontend/src/screens/PlannedView.tsx
    frontend/src/screens/PlannedView.module.css
    frontend/src/screens/TransactionsScreen.tsx
    frontend/src/screens/TransactionsScreen.module.css
  </files>
  <behavior>
    PlannedView:
    - Рефакторинг PlannedScreen: убрать ScreenHeader если `inTransactions` prop true
    - Принять `inTransactions?: boolean` prop
    - FAB убрать (управляется TransactionsScreen)
    - PlannedViewHandle с openCreateSheet() через forwardRef + useImperativeHandle
    - Source badge: 'template' → 'Шаблон', 'manual' → 'Вручную', 'subscription_auto' → '🔁 Подписка'
    - Сохранить applyTemplate, snapshotFromPeriod, все мутации
    - Вынести кнопки «Применить шаблон» и «↻ В шаблон» ВНУТРЬ PlannedView

    TransactionsScreen:
    - Содержит SubTabBar с табами История/План
    - Содержит FilterChips (горизонтальный скролл): Все/Расходы/Доходы + категории
    - Filter chips влияют на activeKindFilter передаваемый в HistoryView
    - Filter chips в под-табе «План» фильтруют по kind (expense/income)
    - Context-aware FAB: если activeSubTab === 'history' → historyRef.current?.openCreateSheet()
    - Context-aware FAB: если activeSubTab === 'plan' → plannedRef.current?.openCreateSheet()
    - Принимает `categoryFilter?: number | null` и `onClearFilter?: () => void` (проброс из App.tsx)
  </behavior>
  <action>
    СНАЧАЛА создать `frontend/src/screens/PlannedView.tsx`:
    - Скопировать PlannedScreen.tsx
    - Имя: `PlannedScreen` → `PlannedView`
    - Props: добавить `inTransactions?: boolean`, убрать обязательный `onBack` (сделать optional)
    - FAB: удалить (нет в PlannedView — управляется через ref)
    - forwardRef: аналогично HistoryView — PlannedViewHandle { openCreateSheet: () => void }
    - Если `!inTransactions` — рендерить ScreenHeader, иначе — нет
    - CSS: скопировать PlannedScreen.module.css как PlannedView.module.css

    ЗАТЕМ создать `frontend/src/screens/TransactionsScreen.tsx`:

    ```tsx
    import { useRef, useState } from 'react';
    import { SubTabBar } from '../components/SubTabBar';
    import { Fab } from '../components/Fab';
    import { useCategories } from '../hooks/useCategories';
    import { HistoryView, type HistoryViewHandle } from './HistoryView';
    import { PlannedView, type PlannedViewHandle } from './PlannedView';
    import styles from './TransactionsScreen.module.css';

    type SubTab = 'history' | 'plan';
    type KindFilter = 'all' | 'expense' | 'income';

    const SUB_TABS = [
      { id: 'history' as SubTab, label: 'История' },
      { id: 'plan' as SubTab, label: 'План' },
    ];

    export interface TransactionsScreenProps {
      categoryFilter?: number | null;
      onClearFilter?: () => void;
    }

    export function TransactionsScreen({ categoryFilter, onClearFilter }: TransactionsScreenProps) {
      const [activeSubTab, setActiveSubTab] = useState<SubTab>('history');
      const [kindFilter, setKindFilter] = useState<KindFilter>('all');
      const { categories } = useCategories(false);
      const historyRef = useRef<HistoryViewHandle>(null);
      const plannedRef = useRef<PlannedViewHandle>(null);

      const handleFab = () => {
        if (activeSubTab === 'history') {
          historyRef.current?.openCreateSheet();
        } else {
          plannedRef.current?.openCreateSheet();
        }
      };

      const handleSubTabChange = (tab: SubTab) => {
        setActiveSubTab(tab);
        setKindFilter('all');
      };

      return (
        <div className={styles.root}>
          <SubTabBar active={activeSubTab} onChange={handleSubTabChange} tabs={SUB_TABS} />

          {/* Filter chips */}
          <div className={styles.chips}>
            {(['all', 'expense', 'income'] as KindFilter[]).map((f) => (
              <button
                key={f}
                type="button"
                className={[styles.chip, kindFilter === f ? styles.chipActive : ''].filter(Boolean).join(' ')}
                onClick={() => setKindFilter(f)}
              >
                {f === 'all' ? 'Все' : f === 'expense' ? 'Расходы' : 'Доходы'}
              </button>
            ))}
            {categories.map((cat) => (
              <button
                key={cat.id}
                type="button"
                className={styles.chip}
                onClick={() => {/* категориальная фильтрация — Phase 7 discretion */}}
              >
                {cat.name}
              </button>
            ))}
          </div>

          {/* Screens */}
          {activeSubTab === 'history' && (
            <HistoryView
              ref={historyRef}
              inTransactions
              categoryFilter={categoryFilter}
              onClearFilter={onClearFilter}
              activeKindFilter={kindFilter}
            />
          )}
          {activeSubTab === 'plan' && (
            <PlannedView
              ref={plannedRef}
              inTransactions
            />
          )}

          <Fab
            onClick={handleFab}
            ariaLabel={activeSubTab === 'history' ? 'Добавить транзакцию' : 'Добавить строку плана'}
          />
        </div>
      );
    }
    ```

    Создать `TransactionsScreen.module.css`:
    ```css
    .root {
      display: flex;
      flex-direction: column;
      height: 100%;
      overflow: hidden;
    }

    .chips {
      display: flex;
      gap: 6px;
      padding: 8px 12px;
      overflow-x: auto;
      scrollbar-width: none;
      flex-shrink: 0;
      border-bottom: 1px solid var(--color-border);
    }

    .chips::-webkit-scrollbar { display: none; }

    .chip {
      flex-shrink: 0;
      padding: 4px 12px;
      border-radius: 20px;
      border: 1px solid var(--color-border);
      background: var(--color-bg-elevated);
      color: var(--color-text-muted);
      font-size: 13px;
      font-family: inherit;
      cursor: pointer;
      white-space: nowrap;
      transition: background 100ms, color 100ms;
      -webkit-tap-highlight-color: transparent;
    }

    .chipActive {
      background: var(--color-primary);
      color: #fff;
      border-color: var(--color-primary);
    }
    ```
  </action>
  <acceptance_criteria>
    - `grep -c "export.*PlannedView" frontend/src/screens/PlannedView.tsx` возвращает >= 1
    - `grep -c "inTransactions" frontend/src/screens/PlannedView.tsx` возвращает >= 2
    - `grep -c "forwardRef\|useImperativeHandle" frontend/src/screens/PlannedView.tsx` возвращает >= 2
    - `grep -c "export.*TransactionsScreen" frontend/src/screens/TransactionsScreen.tsx` возвращает 1
    - `grep -c "SubTabBar" frontend/src/screens/TransactionsScreen.tsx` возвращает >= 2
    - `grep -c "kindFilter\|KindFilter" frontend/src/screens/TransactionsScreen.tsx` возвращает >= 3
    - `grep -c "chipActive" frontend/src/screens/TransactionsScreen.module.css` возвращает 1
    - `grep -c "overflow-x: auto" frontend/src/screens/TransactionsScreen.module.css` возвращает 1
    - `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` возвращает 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"</automated>
  </verify>
  <done>PlannedView.tsx, TransactionsScreen.tsx с SubTabBar + filter chips + context-aware FAB созданы. TypeScript компилируется чисто.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TransactionsScreen → API | Читает данные через хуки; API защищён HMAC на уровне FastAPI |
| FilterChip → URL params | Нет — state локальный в TransactionsScreen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | HistoryView mutations | accept | Мутации идут через API с HMAC-валидацией |
| T-07-05 | Information Disclosure | categoryFilter prop | accept | categoryId — не чувствительные данные |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — 0 ошибок
- HistoryView.tsx: inTransactions prop, totalCents в day-header, forwardRef
- PlannedView.tsx: inTransactions prop, source badges, forwardRef
- TransactionsScreen.tsx: SubTabBar, filter chips, context-aware FAB
- Старые ActualScreen.tsx и PlannedScreen.tsx НЕ удалены (будут удалены в Plan 05)
</verification>

<success_criteria>
- 6 файлов созданы (HistoryView, PlannedView, TransactionsScreen + их .module.css)
- TypeScript компилируется чисто
- TransactionsScreen содержит SubTabBar с 2 под-табами
- FAB меняет ariaLabel в зависимости от активного под-таба
</success_criteria>

<output>
После завершения создать `.planning/phases/07-nav-refactor/07-03-SUMMARY.md`
</output>
