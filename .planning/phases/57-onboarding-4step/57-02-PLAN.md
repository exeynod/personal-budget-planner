---
phase: 57-onboarding-4step
plan: 02
type: execute
wave: 2
depends_on:
  - "57-01"
files_modified:
  - ios/BudgetPlanner/App/AppRouter.swift
  - ios/BudgetPlanner.xcodeproj/project.pbxproj
autonomous: false

requirements: []

must_haves:
  truths:
    - "v06 users (themeRaw == \"v06\") with `isOnboarded == false` see the new NativeOnboardingWizardView, not the legacy OnboardingView."
    - "Non-v06 users with `isOnboarded == false` continue to see the existing onboarding path (legacy OnboardingView — V10 onboarding routing is V10MainShell's concern, untouched here)."
    - "v06 authenticated users (isOnboarded == true) continue to see MainShell — Phase 56 wiring is preserved."
    - "xcodeproj was regenerated via xcodegen so the 5 new files (1 wizard + 4 steps) are part of the BudgetPlanner target sources."
    - "iOS build for iPhone 17 Pro simulator succeeds with 0 errors and 0 NEW warnings vs the pre-Phase-57 baseline."
    - "Legacy `Features/Onboarding/OnboardingView.swift` remains in the source tree (deferred removal per CONTEXT)."
  artifacts:
    - path: "ios/BudgetPlanner/App/AppRouter.swift"
      provides: "Conditional onboarding mount: v06 → NativeOnboardingWizardView, else legacy OnboardingView"
      contains: "isLegacyV06Shell"
    - path: "ios/BudgetPlanner.xcodeproj/project.pbxproj"
      provides: "Regenerated xcodeproj including the 5 new files"
      contains: "NativeOnboardingWizardView"
  key_links:
    - from: "AppRouter (.onboardingRequired branch)"
      to: "NativeOnboardingWizardView"
      via: "if isLegacyV06Shell { NativeOnboardingWizardView(initialUser: user) } else { OnboardingView(initialUser: user) }"
      pattern: "NativeOnboardingWizardView\\(initialUser:"
    - from: "Project file"
      to: "Features/Onboarding/Native* swift files"
      via: "xcodegen generate"
      pattern: "NativeOnboardingWizardView"
---

<objective>
Wire the new native v06 onboarding wizard (delivered in Plan 57-01) into `AppRouter` so it mounts only for the v06 shell, regenerate the Xcode project so the new files are part of the target sources, build the app, and run a manual simulator smoke test.

Purpose: Complete Phase 57 — make the new wizard reachable on the running device and observable end-to-end (steps → submit → AppRouter switches to MainShell). Keep the legacy `OnboardingView` in tree (per CONTEXT deferred-removal decision).

Output:
- Updated `ios/BudgetPlanner/App/AppRouter.swift` with conditional onboarding routing.
- Regenerated `ios/BudgetPlanner.xcodeproj/project.pbxproj`.
- Successful simulator build (0 errors, 0 new warnings).
- Manual smoke evidence (XcodeBuildMCP screenshots captured during the human-verify checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-onboarding-4step/57-CONTEXT.md
@.planning/phases/57-onboarding-4step/57-01-PLAN.md
@CLAUDE.md

@ios/BudgetPlanner/App/AppRouter.swift
@ios/project.yml
@ios/Makefile

# New files from Plan 57-01 (must exist before this plan runs)
@ios/BudgetPlanner/Features/Onboarding/NativeOnboardingWizardView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Conditional onboarding mount in AppRouter + xcodegen regenerate</name>

  <read_first>
    - ios/BudgetPlanner/App/AppRouter.swift (current routing — Phase 56 added `isLegacyV06Shell`)
    - ios/BudgetPlanner/Features/Onboarding/NativeOnboardingWizardView.swift (created in 57-01)
    - ios/project.yml (xcodegen project file — confirms `BudgetPlanner` source path picks up new files automatically)
    - ios/Makefile (build / generate / run shortcuts)
  </read_first>

  <files>
    ios/BudgetPlanner/App/AppRouter.swift,
    ios/BudgetPlanner.xcodeproj/project.pbxproj
  </files>

  <action>
**Step A — Patch `AppRouter.swift`:**

Replace the current `.onboardingRequired` branch:

```swift
case .onboardingRequired(let user):
    OnboardingView(initialUser: user)
```

with the conditional native-wizard routing:

```swift
case .onboardingRequired(let user):
    if isLegacyV06Shell {
        // Phase 57 (v06 Native Rebuild): native 4-step wizard for the
        // v06 shell. Legacy OnboardingView (`Features/Onboarding/OnboardingView.swift`)
        // remains in tree but is no longer reachable from this router.
        NativeOnboardingWizardView(initialUser: user)
    } else {
        // V10 path: existing onboarding mount (legacy view here while
        // V10MainShell owns its own routing). Untouched in Phase 57.
        OnboardingView(initialUser: user)
    }
```

Do NOT change anything outside the `.onboardingRequired` branch. The Phase 56 `isLegacyV06Shell` computed property and the `.authenticated` branch with `MainShell` vs `V10MainShell` stay verbatim.

**Step B — Regenerate the xcodeproj:**

Run `xcodegen generate` from `/Users/exy/pet_projects/tg-budget-planner/ios` (equivalent to `make -C ios generate`). xcodegen scans `BudgetPlanner/` recursively per `project.yml`, so the 5 new files in `Features/Onboarding/` are automatically picked up — no manual `sources:` edit needed.

```bash
cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodegen generate
```

Confirm by greping the regenerated project file for the new symbol:

```bash
grep -c "NativeOnboardingWizardView.swift" ios/BudgetPlanner.xcodeproj/project.pbxproj
```

Expect `>= 1`.
  </action>

  <verify>
    <automated>grep -q "isLegacyV06Shell" ios/BudgetPlanner/App/AppRouter.swift && grep -q "NativeOnboardingWizardView(initialUser: user)" ios/BudgetPlanner/App/AppRouter.swift && grep -q "OnboardingView(initialUser: user)" ios/BudgetPlanner/App/AppRouter.swift && [ "$(grep -c 'NativeOnboardingWizardView.swift' ios/BudgetPlanner.xcodeproj/project.pbxproj)" -ge 1 ]</automated>
  </verify>

  <acceptance_criteria>
    - `ios/BudgetPlanner/App/AppRouter.swift` contains both `NativeOnboardingWizardView(initialUser: user)` AND `OnboardingView(initialUser: user)` strings (conditional branches both present).
    - The `.authenticated` branch still gates `MainShell` vs `V10MainShell` via `isLegacyV06Shell` — diff vs HEAD shows no change there.
    - `ios/BudgetPlanner.xcodeproj/project.pbxproj` contains `NativeOnboardingWizardView.swift` at least once.
    - All 4 step view filenames also appear in `project.pbxproj` (xcodegen recursive scan).
  </acceptance_criteria>

  <done>
    AppRouter routes v06 unboarded users to the new wizard, V10 users unchanged. xcodeproj knows about all 5 new files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build for iPhone 17 Pro simulator + log warnings baseline</name>

  <read_first>
    - ios/Makefile (build target uses `xcbeautify`; XcodeBuildMCP also available per memory `xcodebuildmcp-setup.md`)
    - ios/project.yml (deployment target iOS 26.0, scheme `BudgetPlanner`)
  </read_first>

  <files>
    (no source files modified — this task only invokes the build to surface errors)
  </files>

  <action>
Build the app for the iPhone 17 Pro simulator using XcodeBuildMCP (preferred — session defaults per memory) or `make -C ios build` as fallback. Capture the build log; expectation: 0 errors, 0 new warnings vs the pre-Phase-57 baseline.

Preferred path — XcodeBuildMCP:
1. Call `session_show_defaults` first (mandatory per XcodeBuildMCP instructions before the first build in a session).
2. Call `build_run_sim` with empty/default args (session defaults should resolve project + scheme + simulator).
3. If session defaults are not configured (project / scheme / simulator missing) — set them via `session_set_defaults` using `project: ios/BudgetPlanner.xcodeproj`, `scheme: BudgetPlanner`, `simulator: iPhone 17 Pro`.

Fallback path (if XcodeBuildMCP unavailable):
```bash
cd /Users/exy/pet_projects/tg-budget-planner && make -C ios build 2>&1 | tee /tmp/phase-57-build.log
```

Then count warnings:
```bash
grep -cE "warning:" /tmp/phase-57-build.log
```

Note any warning lines from files inside `BudgetPlanner/Features/Onboarding/Native*.swift` (must be zero — new code), and any warnings introduced from the `AppRouter.swift` edit (also zero — single-line conditional add).

If the build FAILS:
- Read the error.
- If the error references one of the V10 symbols listed in 57-01 `<interfaces>` (e.g. `OnboardingFlow`, `OnboardingV10API`, `DefaultCategories`) — the symbol is present, double-check the import (single Swift module — no `import` needed).
- If the error is `cannot find type 'APIError.unprocessable'` — open `ios/BudgetPlanner/Networking/APIClient.swift`, inspect the actual `enum APIError` cases, replace `.unprocessable` in `NativeOnboardingWizardView.submit()` with the correct case (e.g. `.serverError(422, _)` or similar).
- Re-run.

Do NOT silence warnings by suppressing them — fix the root cause or document the warning as pre-existing in the SUMMARY.
  </action>

  <verify>
    <automated>(if command -v xcodebuild >/dev/null 2>&1; then set -o pipefail; xcodebuild -project ios/BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug CODE_SIGNING_ALLOWED=NO build 2>&1 | tee /tmp/phase-57-build.log | tail -50; grep -q "BUILD SUCCEEDED" /tmp/phase-57-build.log || { echo "BUILD FAILED"; exit 1; }; NEW_WARN=$(grep -E "warning:" /tmp/phase-57-build.log | grep -E "BudgetPlanner/(Features/Onboarding/Native|App/AppRouter)" | wc -l | tr -d ' '); test "$NEW_WARN" = "0" || { echo "NEW WARNINGS IN PHASE-57 FILES: $NEW_WARN"; exit 1; }; else echo "xcodebuild not available — rely on XcodeBuildMCP session result"; fi)</automated>
  </verify>

  <acceptance_criteria>
    - Build target `BudgetPlanner` succeeds for `iPhone 17 Pro` simulator (`BUILD SUCCEEDED` in log).
    - Zero warnings from files matching the regex `BudgetPlanner/(Features/Onboarding/Native|App/AppRouter)`.
    - If pre-existing warnings appear elsewhere in the codebase, they are documented in the SUMMARY (not introduced by this phase).
  </acceptance_criteria>

  <done>
    `BUILD SUCCEEDED` recorded in `/tmp/phase-57-build.log`. App `.app` bundle exists in DerivedData and can be installed on the booted simulator for the next task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual simulator smoke — happy path + persistence</name>

  <what-built>
    Built the BudgetPlanner app for iPhone 17 Pro simulator. New v06 native onboarding wizard mounts when the app theme is `v06` AND the current user is unonboarded. Wizard reuses `OnboardingFlow` data model with UserDefaults draft persistence (key `onboarding.v10.draft`). Final step submits via `OnboardingV10API.postOnboardingComplete` → `authStore.refreshUser` → AppRouter switches to `MainShell`.
  </what-built>

  <how-to-verify>
**Pre-step — get into v06 unonboarded state:**

1. Boot iPhone 17 Pro simulator + install the freshly-built app:
   ```bash
   make -C ios run
   ```
   (Alternatively: XcodeBuildMCP `boot_sim` + `install_app_sim` + `launch_app_sim`.)
2. Force the v06 theme:
   ```bash
   xcrun simctl spawn booted defaults write com.exeynod.BudgetPlanner ui.theme v06
   ```
3. Force the onboarding state on next launch:
   ```bash
   xcrun simctl spawn booted defaults write com.exeynod.BudgetPlanner DEV_FORCE_ONBOARDING 1
   ```
4. Re-launch the app:
   ```bash
   xcrun simctl launch booted com.exeynod.BudgetPlanner
   ```

**Smoke verification — happy path:**

A. **Step 1 (Income):** App opens to "Шаг 1 из 4 · Доход". Type `120000` → "Дальше" becomes enabled → tap.

B. **Step 2 (Accounts):** Title "Шаг 2 из 4 · Счета". Tap a preset (e.g. "Т-Банк"). At least one account row appears with `· основной` chip. "Дальше" enabled → tap.

C. **Step 3 (Plan):** Title "Шаг 3 из 4 · План". 8 default categories listed (ПРОДУКТЫ / КАФЕ / ДОМ / ТРАНСПОРТ / РАЗВЛЕЧ. / ПОДАРКИ / ЗДОРОВЬЕ / ПОДПИСКИ) with pre-filled Stepper values (auto-allocation from `DefaultCategories.defaultPlan`). Increment one, then "Дальше".

D. **Step 4 (Goals):** Title "Шаг 4 из 4 · Цель". Skip goal (leave toggle off). Optionally toggle roundup OFF. Tap "Готово" → button label becomes "Сохранение…", then within 1-2s the app transitions to `MainShell` (the v06 home view).

**Smoke verification — persistence across app kill:**

E. Re-trigger onboarding:
   ```bash
   xcrun simctl spawn booted defaults write com.exeynod.BudgetPlanner DEV_FORCE_ONBOARDING 1
   xcrun simctl launch booted com.exeynod.BudgetPlanner
   ```
F. Step 1: enter `90000` → Дальше.
G. Step 2: add a preset → Дальше.
H. **Kill the app** via simulator multitasking (Cmd+Shift+H twice → swipe up). Re-launch.
I. Confirm the wizard restores to Step 2 (or later) with the previously entered data visible — `OnboardingFlow` reloads from `UserDefaults["onboarding.v10.draft"]`.

**Capture screenshots:**

XcodeBuildMCP UI-automation:
- `screenshot_sim` after each of A/B/C/D and after the persistence restore at step I.
- Save under `.planning/phases/57-onboarding-4step/screenshots/` (5 PNGs minimum).

**Failure modes to look out for (REPORT, don't auto-fix):**
- V10 chrome leaks into a step view (PosterCard / Mass typography visible).
- Step 1 "Дальше" never enables.
- Step 4 submit hangs > 5s or shows an `error.localizedDescription` string literally (per T-57-02 it must be fixed Russian copy).
- After successful submit, AppRouter does not switch to MainShell (likely AuthStore.refreshUser didn't update state).
  </how-to-verify>

  <resume-signal>Type "approved" once all 9 verification points (A-I) pass, or describe the failing step.</resume-signal>
</task>

</tasks>

<verification>
- AppRouter shows two branches in the `.onboardingRequired` case (native vs legacy).
- xcodeproj contains `NativeOnboardingWizardView.swift` reference.
- Build log shows `BUILD SUCCEEDED` with 0 new warnings.
- Manual smoke (9 steps A-I) passes per the checkpoint.
- Screenshots captured under `.planning/phases/57-onboarding-4step/screenshots/`.
- Legacy `Features/Onboarding/OnboardingView.swift` is still on disk (deferred removal).
</verification>

<success_criteria>
- v06 unonboarded users land on `NativeOnboardingWizardView`; non-v06 users remain on the legacy `OnboardingView`.
- iOS build for iPhone 17 Pro simulator: 0 errors, 0 new warnings in phase-57 files.
- Manual happy-path smoke (Step 1 → Step 4 → submit → MainShell) succeeds.
- Persistence smoke (mid-wizard kill → relaunch → state restored) succeeds.
- Legacy onboarding code retained in tree per CONTEXT deferred-removal decision.
</success_criteria>

<output>
After completion, create `.planning/phases/57-onboarding-4step/57-02-SUMMARY.md` with:
- diff summary of `AppRouter.swift` (which lines changed).
- xcodegen result (was the proj regenerated cleanly, which files added).
- Build result (succeed / fail, warning count).
- Smoke result (list of verified items A-I + any issues found).
- Screenshot list with relative paths.
- Open follow-ups (e.g. "legacy OnboardingView removal still deferred — track in closure phase").
</output>
