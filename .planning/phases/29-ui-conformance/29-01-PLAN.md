---
phase: 29-ui-conformance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/fixtures/onboarded-user.ts
  - frontend/tests/e2e/v10-pixel-snapshots.spec.ts
  - frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/
autonomous: true
requirements:
  - UICONF-01
must_haves:
  truths:
    - "Запуск `npx playwright test v10-pixel-snapshots.spec.ts --update-snapshots` создаёт 8 baseline PNG (home, transactions, add-sheet, category-detail, plan-month, subscriptions, savings, ai-initial) в `tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/`."
    - "Повторный запуск без `--update-snapshots` зелёный (snapshots детерминированы)."
    - "Тест использует переиспользуемую fixture `onboarded-user.ts` вместо inline mock-router."
    - "Каждый из 8 экранов рендерит ожидаемое hero-состояние (Home → «Дневной темп», Transactions → «Реестр.», AddSheet → «NEW ENTRY», и т.д.) до screenshot-capture."
  artifacts:
    - path: "frontend/tests/e2e/fixtures/onboarded-user.ts"
      provides: "Reusable Playwright fixture: устанавливает localStorage `ui.theme=v10`, мокает /api/v1/me, /accounts, /categories, /periods/current, /periods/{id}/actual, catch-all /api/v1/** → []."
      exports: ["installOnboardedFixture", "ME_ONBOARDED_V10", "ACCOUNTS_V10", "CATEGORIES_V10", "PERIOD_CURRENT_V10"]
    - path: "frontend/tests/e2e/v10-pixel-snapshots.spec.ts"
      provides: "Refactored spec, использующий fixture из `onboarded-user.ts`; goto-helpers остаются in-file."
      contains: "installOnboardedFixture"
    - path: "frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/"
      provides: "8 PNG baseline (home/transactions/add-sheet/category-detail/plan-month/subscriptions/savings/ai-initial), platform-suffixed `-darwin`."
      contains: "pixel-home"
  key_links:
    - from: "v10-pixel-snapshots.spec.ts"
      to: "fixtures/onboarded-user.ts"
      via: "import { installOnboardedFixture } from './fixtures/onboarded-user'"
      pattern: "import.*installOnboardedFixture.*from.*fixtures/onboarded-user"
---

<objective>
Создать минимальную reusable Playwright-фикстуру для onboarded user и
сгенерировать pixel-baseline PNG для всех 8 V10 экранов. Это разблокирует
Plan 29-02 (side-by-side analysis vs prototype) и Phase 31 REG-01
(полная regression-фикстура).

Purpose: До v1.0 baseline-snapshots существовали только в виде `.gitkeep`
(см. DIVERGENCES.md §W-04). Без них side-by-side audit не имеет на чём
строить дифф. Этот plan закрывает W-04 как pre-condition для UICONF-02.

Output: 8 baseline PNG + переиспользуемая фикстура для последующих spec-файлов.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-ui-conformance/29-CONTEXT.md
@.planning/v1.0-handoff/DIVERGENCES.md
@frontend/tests/e2e/v10-pixel-snapshots.spec.ts
@frontend/tests/e2e/fixtures/onboarding-mocks.ts

<interfaces>
<!-- Existing fixture pattern to follow (onboarding-mocks.ts) -->
From frontend/tests/e2e/fixtures/onboarding-mocks.ts:
- `export const ME_NOT_ONBOARDED`, `export const ME_ONBOARDED` (MeV10Response-shaped objects)
- `export const STORAGE_KEY = 'tg-budget-planner.ui-theme'` (likely; verify on read)
- `export async function mockMe(page: Page, me: typeof ME_ONBOARDED)`
- `export async function mockOnboardingComplete200(page: Page, response: ...)`

Reuse pattern: route `**/api/v1/me`, `**/api/v1/accounts`, `**/api/v1/categories**`,
`**/api/v1/periods/current`, `**/api/v1/periods/5/actual**`, catch-all `**/api/v1/**`.

Mock shapes уже определены в текущем v10-pixel-snapshots.spec.ts inline
(ME_ONBOARDED / ACCOUNTS / CATEGORIES / PERIOD_CURRENT) — извлеки их as-is в fixture file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract onboarded fixture module</name>
  <files>frontend/tests/e2e/fixtures/onboarded-user.ts</files>
  <read_first>
    - frontend/tests/e2e/v10-pixel-snapshots.spec.ts (lines 30-140 — извлечь mocks)
    - frontend/tests/e2e/fixtures/onboarding-mocks.ts (pattern reference)
  </read_first>
  <action>
    Create `frontend/tests/e2e/fixtures/onboarded-user.ts` экспортирующий:

    1. **Mock data constants** (move as-is from v10-pixel-snapshots.spec.ts lines 34-97):
       - `export const ME_ONBOARDED_V10` (MeV10Response shape, onboarded_at set, income_cents 150_000_00, cycle_start_day 1)
       - `export const ACCOUNTS_V10` (one Т-Банк card, balance 50_000_00)
       - `export const CATEGORIES_V10` (Кафе id=7 + savings id=99)
       - `export const PERIOD_CURRENT_V10` (id=5, May 2026 active)

    2. **`installOnboardedFixture(page: Page, opts?: { extraRoutes?: ... })`** function:
       - Calls `page.addInitScript()` to set `localStorage.setItem('ui.theme', 'v10')` BEFORE app boot.
       - Registers `page.route()` handlers identical to `installMocks()` in current spec (lines 99-124):
         - `**/api/v1/me` → ME_ONBOARDED_V10
         - `**/api/v1/accounts` → ACCOUNTS_V10
         - `**/api/v1/categories**` → CATEGORIES_V10
         - `**/api/v1/periods/current` → PERIOD_CURRENT_V10
         - `**/api/v1/periods/5/actual**` → []
         - Catch-all `**/api/v1/**` GET → '[]'; non-GET → `route.continue()`
       - Apply `opts.extraRoutes` AFTER catch-all так, чтобы они переопределяли (last-registered wins per Playwright route precedence — verify with `route.fallback()` semantics).

    3. **`freezeMotion(page: Page)`** helper (move as-is from current spec lines 127-139):
       - addStyleTag injecting `animation-duration: 0s` + `transition-duration: 0s` для `*, *::before, *::after`.
       - `await page.waitForTimeout(150)` для settle.

    JSDoc на каждом export указывая что Phase 29-01 это создал и почему (см. W-04). Используй те же типы что `onboarding-mocks.ts` (no `any`, MeV10-shaped objects). Per UICONF-01.
  </action>
  <verify>
    <automated>cd frontend && node -e "const m = require('./tests/e2e/fixtures/onboarded-user.ts'); console.log(Object.keys(m))" 2>&1 | grep -E "ME_ONBOARDED_V10|installOnboardedFixture" || npx tsc --noEmit tests/e2e/fixtures/onboarded-user.ts 2>&1 | grep -c "error" | grep -q "^0$"</automated>
  </verify>
  <done>
    File exists; exports `installOnboardedFixture`, `ME_ONBOARDED_V10`,
    `ACCOUNTS_V10`, `CATEGORIES_V10`, `PERIOD_CURRENT_V10`, `freezeMotion`.
    `npx tsc --noEmit` clean для этого файла.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor v10-pixel-snapshots.spec.ts to use fixture + generate baselines</name>
  <files>
    frontend/tests/e2e/v10-pixel-snapshots.spec.ts,
    frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/
  </files>
  <read_first>
    - frontend/tests/e2e/v10-pixel-snapshots.spec.ts (current full file)
  </read_first>
  <behavior>
    - Spec импортирует `installOnboardedFixture` и `freezeMotion` из `./fixtures/onboarded-user`.
    - Inline mocks (lines 34-97 `ME_ONBOARDED`/`ACCOUNTS`/`CATEGORIES`/`PERIOD_CURRENT`) удалены — теперь приходят из fixture.
    - `test.beforeEach` вызывает `installOnboardedFixture(page)` единственным вызовом.
    - Все 8 setup-функций (gotoHome, gotoTransactions, gotoAddSheet, gotoCategoryDetail, gotoPlanMonth, gotoSubscriptions, gotoSavings, gotoAi) остаются in-file и продолжают работать.
    - После запуска `--update-snapshots` 8 PNG появляются в `tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/` (filenames: `pixel-home-1-chromium-mobile-darwin.png` и т.п.).
    - Повторный запуск без `--update-snapshots` зелёный.
  </behavior>
  <action>
    1. **Refactor spec:**
       - Удалить inline mock constants (ME_ONBOARDED/ACCOUNTS/CATEGORIES/PERIOD_CURRENT).
       - Удалить inline `installMocks()` функцию.
       - Импортировать `import { installOnboardedFixture, freezeMotion } from './fixtures/onboarded-user';`.
       - Удалить inline `freezeMotion()` функцию (или ре-экспортировать из fixture).
       - В `test.beforeEach` заменить `await installMocks(page)` + `addInitScript` блок на `await installOnboardedFixture(page);` (это покрывает и localStorage init, и routes).
       - Goto-helpers (gotoHome, gotoTransactions, ...) — leave as-is.
       - Список SCREENS — leave as-is (8 элементов).

    2. **Generate baselines:**
       - Убедись что `docker compose -f docker-compose.dev.yml up -d` поднят (api/bot/worker/db/caddy запущены) и Vite dev server на http://localhost:5173 (пользователь подтвердил в planning_context что dev stack up).
       - Из `frontend/`: `npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --update-snapshots --project=chromium-mobile`.
       - Если playwright.config указывает другой project name, run `npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --list` чтобы найти exact project name, потом обнови команду.
       - Подтверди что в `tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/` появилось 8 PNG.

    3. **Re-run без `--update-snapshots`** чтобы проверить детерминизм:
       - `npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile`.
       - Все 8 tests pass green. Если какой-то flake — расширить freezeMotion (например, ещё `waitForTimeout(200)`) или добавить `page.evaluate()` для freezing BigFig count-up state explicitly.

    4. **Git-track:** `git add frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/` — но НЕ коммить здесь, это сделает execute-plan. Per UICONF-01 + closes DIVERGENCES.md §W-04.
  </action>
  <verify>
    <automated>cd frontend && ls tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/ | grep -E "\.png$" | wc -l | grep -q "^8$" && npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile --reporter=line 2>&1 | grep -qE "8 passed|all .* tests passed"</automated>
  </verify>
  <done>
    8 PNG baseline в `__screenshots__/v10-pixel-snapshots.spec.ts/`, spec
    зелёный на повторном запуске без `--update-snapshots`. Inline mock
    constants вырезаны из spec, fixture импортирована.
  </done>
</task>

</tasks>

<verification>
1. `ls frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/*.png | wc -l` → 8.
2. `cd frontend && npx playwright test tests/e2e/v10-pixel-snapshots.spec.ts --project=chromium-mobile` → all 8 tests pass.
3. `grep -c "installOnboardedFixture" frontend/tests/e2e/v10-pixel-snapshots.spec.ts` → ≥1.
4. `grep -v '^//' frontend/tests/e2e/v10-pixel-snapshots.spec.ts | grep -c "const ME_ONBOARDED = {"` → 0 (inline mock cleared).
</verification>

<success_criteria>
- 8 PNG созданы и Playwright re-run зелёный.
- Spec использует `installOnboardedFixture` из shared fixture.
- DIVERGENCES.md §W-04 «baseline PNGs deferred» можно закрыть (но фактическое обновление DIVERGENCES.md — в plan 29-05).
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-conformance/29-01-SUMMARY.md` documenting:
- Final list of 8 PNG filenames (с byte-size) — чтобы plan 29-02 знал где их читать.
- Fixture API (exported names + signatures) — для Phase 31 REG-01 переиспользования.
- Any flake fixes (если потребовалось extend freezeMotion).
</output>
