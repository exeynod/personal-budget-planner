---
phase: 29-ui-conformance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-ui-conformance/UI-REVIEW.md
  - .planning/phases/29-ui-conformance/ios-screenshots/
autonomous: false
requirements:
  - UICONF-03
must_haves:
  truths:
    - "8 PNG screenshots для всех V10 экранов сгенерированы через XcodeBuildMCP на iPhone 17 Pro Simulator (booted, BudgetPlanner.app installed)."
    - "UI-REVIEW.md содержит секцию `## iOS` (append, не overwrite web-секции из plan 29-02) с per-screen разделами для всех 8 экранов."
    - "Каждое iOS finding ссылается на конкретный Swift-файл `ios/BudgetPlanner/FeaturesV10/...` ИЛИ token в `PosterTokens.swift` ИЛИ animation в `PosterAnimations.swift`."
    - "Известные DIVERGENCES.md записи (I-01..I-05, X-01..X-02) НЕ переоткрываются."
    - "Один human-checkpoint: оператор подтверждает, что navigation routes между экранами успешно проведены (Mgmt → Подписки, Home → category, etc.)."
  artifacts:
    - path: ".planning/phases/29-ui-conformance/ios-screenshots/"
      provides: "Каталог с 8 PNG (home.png, transactions.png, add-sheet.png, category-detail.png, plan-month.png, subscriptions.png, savings.png, ai-initial.png) plus opt. accounts.png + analytics.png если приложены к v1.0-MILESTONE-AUDIT iOS checklist."
      contains: "home.png"
    - path: ".planning/phases/29-ui-conformance/UI-REVIEW.md"
      provides: "Append `## iOS` секция к существующему файлу (web-section уже там из plan 29-02)."
      contains: "## iOS"
  key_links:
    - from: "UI-REVIEW.md ## iOS section"
      to: ".planning/v1.0-handoff/handoff/DESIGN-SYSTEM.md"
      via: "Each finding cites DESIGN-SYSTEM section (e.g., §2.4, §6.3)"
      pattern: "DESIGN-SYSTEM"
    - from: "UI-REVIEW.md ## iOS section"
      to: ".planning/v1.0-handoff/handoff/SCREENS.md"
      via: "Per-screen spec reference"
      pattern: "SCREENS"
---

<objective>
Сгенерировать iOS screenshots для всех 8 V10 экранов через XcodeBuildMCP
и сделать per-screen audit против `DESIGN-SYSTEM.md` + `SCREENS.md`. Вписать
results в существующий `UI-REVIEW.md` как `## iOS` секцию (append).

Purpose: iOS не имеет prototype/index.html (web-only reference). Spec —
text-based в `DESIGN-SYSTEM.md` (палитра, типографика, components) и
`SCREENS.md` (per-screen layouts). Screenshots are the only визуальный
input для нашей классификации BLOCKER/WARNING/INFO.

Output: 8 iOS PNG + UI-REVIEW.md `## iOS` section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-ui-conformance/29-CONTEXT.md
@.planning/v1.0-handoff/DIVERGENCES.md
@.planning/v1.0-handoff/handoff/DESIGN-SYSTEM.md
@.planning/v1.0-handoff/handoff/SCREENS.md
@ios/BudgetPlanner/PosterTokens.swift

<!-- Project skill in scope -->
@$HOME/.claude/skills/xcodebuildmcp/SKILL.md

<!-- Per-screen iOS source paths (read on demand): -->
<!-- ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10View.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetView.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/CategoryDetail/CategoryDetailV10View.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/Plan/PlanMonthV10View.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsV10View.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/Savings/SavingsV10View.swift -->
<!-- ios/BudgetPlanner/FeaturesV10/Ai/AiV10View.swift -->

<!-- DIVERGENCES.md iOS rows to exclude: -->
<!-- I-01 (DM Serif Cyrillic fallback), I-02 (custom PosterNavStack), I-03 (easing curve approx), -->
<!-- I-04 (safe-area padding), I-05 (bare .animation() callsites — flagged Phase 28-02 audit) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture 8 iOS screenshots via XcodeBuildMCP</name>
  <files>.planning/phases/29-ui-conformance/ios-screenshots/</files>
  <read_first>
    - $HOME/.claude/skills/xcodebuildmcp/SKILL.md (полный — обязательно follow Step 1: session_show_defaults BEFORE first action)
    - .planning/v1.0-handoff/DIVERGENCES.md (iOS Manual Screenshot Checklist таблица в нижней части — список navigation routes для каждого экрана)
    - ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift (для понимания state setup нужен ли)
  </read_first>
  <acceptance_criteria>
    - Каталог `.planning/phases/29-ui-conformance/ios-screenshots/` содержит 8 PNG: `home.png`, `transactions.png`, `add-sheet.png`, `category-detail.png`, `plan-month.png`, `subscriptions.png`, `savings.png`, `ai-initial.png`.
    - Каждый PNG > 50 KB (sanity-check что не пустой / не error-screen).
    - Каталог добавлен в `.gitignore` ИЛИ committed (default — committed, чтобы plan 29-05 re-snapshot мог делать diff). Если binary блокирован git LFS — поговорить с пользователем; default = git add.
  </acceptance_criteria>
  <action>
    **Setup (per xcodebuildmcp SKILL.md Step 1):**
    1. Call `session_show_defaults` MCP tool. Verify project = `ios/BudgetPlanner.xcodeproj`, scheme = `BudgetPlanner`, simulator = iPhone 17 Pro (per planning_context: «iPhone 17 Pro Simulator уже booted, BudgetPlanner.app installed»).
    2. If defaults missing — `discover_projs` → set defaults → confirm.
    3. Skip build/install (planning_context подтверждает, что BudgetPlanner.app уже installed). Если `simctl_get_app_container` показывает что не installed, build via `simctl_build_run_app` сначала.

    **Pre-step: ensure onboarded state.**
    - Login через autologin (UserDefaults — см. memory/project-ios-app.md).
    - Если приложение в not-onboarded state, оператор может вручную провести onboarding ИЛИ использовать `/api/v1/internal/onboarding/complete` endpoint вручную через `curl` (внутренний docker network — см. .env `INTERNAL_TOKEN=test_internal_secret_token`).

    **Capture sequence (per screen):**

    Для каждого из 8 экранов:
    1. **Navigate** через xcodebuildmcp UI automation (`ui_tap` / `ui_swipe`):
       - **home**: launch app → autoland on Home tab.
       - **transactions**: tap «ВСЕ ОПЕРАЦИИ →» row на Home.
       - **add-sheet**: tap FAB (центральный +) на Home.
       - **category-detail**: tap первая категория («Кафе») на Home.
       - **plan-month**: tap «PLAN мая» badge на Home.
       - **subscriptions**: switch to УПР. tab → tap «04 ПОДПИСКИ» row.
       - **savings**: switch to КОПИЛКА tab.
       - **ai-initial**: switch to AI tab.
    2. **Wait for animation settle** (≥500ms — posterSlideInFwd 420ms + buffer).
    3. **`ui_screenshot`** → save bytes to `.planning/phases/29-ui-conformance/ios-screenshots/{screen-name}.png`.
    4. **Navigate back to Home** before next iteration (если на детальном экране — pop). Чтобы избежать nav state collision.

    **Per UICONF-03.** Если какой-то screen не достижим (e.g., AI tab unimplemented) — записать как BLOCKER в task 3 (`Screen unreachable from Mgmt → ...`); НЕ создавать пустой PNG.

    **Verification screenshots are non-zero:** `ls -la ios-screenshots/*.png | awk '{print $5, $NF}' | grep -v "^[0-4]\? "` (filter > 5KB).
  </action>
  <verify>
    <automated>test -d .planning/phases/29-ui-conformance/ios-screenshots && ls .planning/phases/29-ui-conformance/ios-screenshots/*.png | wc -l | grep -q "^8$" && ! find .planning/phases/29-ui-conformance/ios-screenshots/ -name "*.png" -size -50k | grep -q "."</automated>
  </verify>
  <done>
    8 iOS PNG в `.planning/phases/29-ui-conformance/ios-screenshots/`, каждый > 50 KB.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Confirm iOS screenshots show correct screens</name>
  <files>.planning/phases/29-ui-conformance/ios-screenshots/</files>
  <what-built>8 iOS PNG в `.planning/phases/29-ui-conformance/ios-screenshots/`</what-built>
  <how-to-verify>
    Open в Preview.app:

    ```bash
    open .planning/phases/29-ui-conformance/ios-screenshots/*.png
    ```

    Проверь что каждый файл показывает ожидаемый экран:

    | File | Expected hero text / element |
    |------|------------------------------|
    | home.png | coral bg, eyebrow `VOL.NN`, italic «Дневной темп —» |
    | transactions.png | cobalt bg, «Реестр.» italic, day groups |
    | add-sheet.png | black bg, BigFig 86px yellow, 3×4 keypad |
    | category-detail.png | cobalt OR red bg per isOver, Mass UPPER name |
    | plan-month.png | cobalt bg, «План мая.» italic, sliders |
    | subscriptions.png | coral bg, «Подписки.» italic |
    | savings.png | black bg, «Копилка.» italic, jaune plate |
    | ai-initial.png | DM Serif italic 36px observation, ASSISTANT/ONLINE eyebrow |

    Если какой-то PNG показывает Home вместо expected (navigation не сработала) — заметь в response чтобы task 3 это flagged как BLOCKER setup-issue.
  </how-to-verify>
  <action>
    Pause execution. Wait for human operator response. No autonomous action — task 3 reads operator's reply to determine setup-issue findings.
  </action>
  <verify>
    <automated>echo "checkpoint task — verified by human operator response"</automated>
  </verify>
  <done>
    Operator typed "approved" OR provided list of file → actual-screen mismatches (для passthrough к task 3).
  </done>
  <resume-signal>
    Type "approved" если все 8 PNG корректные. Перечисли file → actual-screen для тех что не совпали (formatting: `subscriptions.png: stuck on Home` etc.).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Append `## iOS` section to UI-REVIEW.md</name>
  <files>.planning/phases/29-ui-conformance/UI-REVIEW.md</files>
  <read_first>
    - .planning/phases/29-ui-conformance/UI-REVIEW.md (existing file from plan 29-02 — append, не overwrite!)
    - .planning/v1.0-handoff/handoff/DESIGN-SYSTEM.md (sections 1-7 для token/component spec)
    - .planning/v1.0-handoff/handoff/SCREENS.md (per-screen layouts: §01 Home, §02 Транзакции, §03 AI, §04 Категория, §07 PLAN мая, §09 Подписки, §10 Add Sheet, §11 Копилка)
    - ios/BudgetPlanner/PosterTokens.swift (current implemented token values)
    - .planning/v1.0-handoff/DIVERGENCES.md (iOS section + Manual Screenshot Checklist)
    - 8 PNG в `.planning/phases/29-ui-conformance/ios-screenshots/` (visual reference — Read tool рендерит)
    - Per-screen iOS sources (через grep first):
      - ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift
      - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10View.swift
      - ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetView.swift
      - ios/BudgetPlanner/FeaturesV10/CategoryDetail/CategoryDetailV10View.swift
      - ios/BudgetPlanner/FeaturesV10/Plan/PlanMonthV10View.swift
      - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsV10View.swift
      - ios/BudgetPlanner/FeaturesV10/Savings/SavingsV10View.swift
      - ios/BudgetPlanner/FeaturesV10/Ai/AiV10View.swift
  </read_first>
  <acceptance_criteria>
    - Файл `.planning/phases/29-ui-conformance/UI-REVIEW.md` теперь содержит `## iOS` секцию ниже `## Web` (web-section не тронут).
    - `## iOS` имеет 8 screen-подразделов `### iOS-1. Home` … `### iOS-8. AI initial-state` (8 штук).
    - Каждый подраздел: Status (PASS/BLOCKER/WARNING/INFO), Findings (список или «No deviations»), reference DESIGN-SYSTEM.md / SCREENS.md / PosterTokens.swift / Swift file:line.
    - Подсекция `## Excluded — known iOS DIVERGENCES.md`: I-01, I-02, I-03, I-04, I-05.
    - Total Summary table в конце документа обновлён, включая web + iOS counts (Web vs iOS отдельные строки).
    - Если checkpoint task 2 surfaced setup issue (e.g., subscriptions stuck on Home) — соответствующий screen-раздел имеет BLOCKER finding `Screenshot capture failed — navigation route broken (см. checkpoint task 2 response)`.
  </acceptance_criteria>
  <action>
    **Method (per-screen iOS, repeat 8 times — analogous to plan 29-02):**

    1. **Read iOS screenshot** (`ios-screenshots/{screen-name}.png` через Read tool — visual rendering).
    2. **Read iOS source** (Swift file для этого экрана).
    3. **Cross-reference DESIGN-SYSTEM.md spec:**
       - `§1 Палитра` — coral/cobalt/red/black/jaune RGB values vs PosterTokens.swift `.posterCoral` etc.
       - `§2 Типографика` — DM Serif/PT Serif/Manrope/JetBrains Mono usage; sizes (Mass, BigFig, eyebrow).
       - `§3 Spacing` — verify padding/spacing constants vs PosterTokens.swift `.spacing*`.
       - `§6 Компоненты` — Eyebrow / Mass / BigFig / Plate / CTA / Chips / Slider / Tab / FAB / Toast usage match.
       - `§7 Анимации` — already covered in I-03/I-05; не re-flag.
    4. **Cross-reference SCREENS.md** для per-screen layout (e.g., §01 Home expects: eyebrow VOL.NN + italic «Дневной темп —» + BigFig + day-pace bar + category list).
    5. **Severity classification** (same rubric as web from CONTEXT.md):
       - BLOCKER: wrong color (≥3 hex digit diff in PosterTokens), missing element from SCREENS.md spec, completely wrong layout.
       - WARNING: spacing 1-3pt drift, opacity 5-15%, font-weight mismatch.
       - INFO: micro-polish, icon alignment.
    6. **Dedupe** против I-01..I-05 + X-01..X-02 (iOS-related).

    **Append structure (do NOT overwrite web section):**

    ```markdown
    ## iOS

    Per-screen deviation report against `DESIGN-SYSTEM.md` + `SCREENS.md`.
    Screenshots via XcodeBuildMCP on iPhone 17 Pro Simulator.

    ### Excluded — known iOS DIVERGENCES.md

    - I-01 (DM Serif Cyrillic → PT Serif fallback) — accepted ADR-001
    - I-02 (PosterNavStack vs UIKit) — accepted ADR-002
    - I-03 (easing curve approximation) — accepted
    - I-04 (safe-area padding) — accepted (Apple HIG)
    - I-05 (bare `.animation()` callsites: PosterStyle.swift:44, KeypadView.swift:72) — already deferred to Phase 30 DEBT-06

    ### iOS-1. Home

    **Status:** [PASS | BLOCKER | WARNING | INFO]

    **Findings:**
    - [<severity>] <description>
      - File: `ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift:<line>` OR `PosterTokens.swift:<line>`
      - Expected (DESIGN-SYSTEM §<n>): <spec value>
      - Actual: <implemented value>
    - ...

    ### iOS-2. Transactions
    ...
    (continue for all 8 screens)
    ```

    **Update Summary table** в конце файла (extend the one из plan 29-02 web):

    ```markdown
    ## Summary

    | Platform | Severity | Count | Screens affected |
    |----------|----------|-------|------------------|
    | Web      | BLOCKER  | N     | <list>           |
    | Web      | WARNING  | N     | <list>           |
    | Web      | INFO     | N     | <list>           |
    | Web      | PASS     | N     | <list>           |
    | iOS      | BLOCKER  | N     | <list>           |
    | iOS      | WARNING  | N     | <list>           |
    | iOS      | INFO     | N     | <list>           |
    | iOS      | PASS     | N     | <list>           |

    **Total BLOCKER:** N (Web=N, iOS=N)
    **Next:** Plan 29-04 spawns one fix-plan per BLOCKER-flagged screen.
    ```

    Per UICONF-03.
  </action>
  <verify>
    <automated>grep -c "^## iOS$" .planning/phases/29-ui-conformance/UI-REVIEW.md | grep -q "^1$" && grep -c "^### iOS-[0-9]\." .planning/phases/29-ui-conformance/UI-REVIEW.md | grep -q "^8$" && grep -q "Excluded — known iOS DIVERGENCES" .planning/phases/29-ui-conformance/UI-REVIEW.md && grep -qE "I-0[1-5]" .planning/phases/29-ui-conformance/UI-REVIEW.md && grep -q "## Summary" .planning/phases/29-ui-conformance/UI-REVIEW.md</automated>
  </verify>
  <done>
    UI-REVIEW.md содержит `## iOS` секцию с 8 screen-подразделами и
    обновлённым Summary table (web + iOS counts).
  </done>
</task>

</tasks>

<verification>
1. `ls .planning/phases/29-ui-conformance/ios-screenshots/*.png | wc -l` = 8.
2. `grep -c "^### iOS-[0-9]\." .planning/phases/29-ui-conformance/UI-REVIEW.md` = 8.
3. `grep -c "^### [0-9]\." .planning/phases/29-ui-conformance/UI-REVIEW.md` ≥ 8 (web subsections all preserved).
4. `grep -E "I-0[1-5]" UI-REVIEW.md | wc -l` ≥ 5.
5. Manual: оператор подтвердил task 2 checkpoint.
</verification>

<success_criteria>
- 8 iOS PNG в `ios-screenshots/`, доступны для plan 29-05 re-snapshot diff.
- UI-REVIEW.md имеет `## Web` (из 29-02) + `## iOS` (из 29-03) с unified Summary table.
- BLOCKER list для plan 29-04 готов: web BLOCKERS из 29-02 SUMMARY + iOS BLOCKERS из этого 29-03 SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-conformance/29-03-SUMMARY.md` documenting:
- iOS BLOCKER counts per screen.
- Список iOS PNG paths (для plan 29-05 re-snapshot reference).
- Setup issues (если task 2 checkpoint surfaced unreachable screens).
- Combined web+iOS BLOCKER list для plan 29-04 sub-plan creation.
</output>
