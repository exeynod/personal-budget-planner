---
phase: 29-ui-conformance
plan: 02
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - .planning/phases/29-ui-conformance/UI-REVIEW.md
autonomous: true
requirements:
  - UICONF-02
must_haves:
  truths:
    - "UI-REVIEW.md содержит секцию `## Web` с per-screen разделами для всех 8 V10 экранов."
    - "Каждый screen-раздел перечисляет конкретные deviations (или явное «PASS — no deviations») со severity BLOCKER / WARNING / INFO."
    - "Каждая deviation ссылается на конкретный файл `frontend/src/screensV10/...` ИЛИ token `tokens.css` ИЛИ animation в `posterAnimations.ts` — никаких abstract findings."
    - "Известные DIVERGENCES.md записи (W-01..W-05, X-01..X-02) НЕ переоткрываются как новые findings — auditor явно проверяет dedupe."
  artifacts:
    - path: ".planning/phases/29-ui-conformance/UI-REVIEW.md"
      provides: "Audit report с web-секцией; iOS-секция будет добавлена plan 29-03 (append, не overwrite)."
      min_lines: 120
      contains: "## Web"
  key_links:
    - from: "UI-REVIEW.md"
      to: ".planning/v1.0-handoff/handoff/prototype/index.html"
      via: "Each finding cites prototype line/section"
      pattern: "prototype/index.html"
    - from: "UI-REVIEW.md"
      to: ".planning/v1.0-handoff/DIVERGENCES.md"
      via: "Dedupe section explicitly listing W-01..W-05/X-01..X-02 as «excluded as known»"
      pattern: "W-0[1-5]|X-0[12]"
---

<objective>
Side-by-side dimensional comparison каждого из 8 web V10 экранов
(`frontend/src/screensV10/...`) против `prototype/index.html`. Произвести
UI-REVIEW.md с findings classified BLOCKER / WARNING / INFO, исключая
известные DIVERGENCES.md записи. Это input для plan 29-04 (BLOCKER fixes).

Purpose: Без формального deviation report у нас нет согласованного списка
что чинить. Plan 29-04 будет запускать sub-fix-plans только для тех
экранов где есть BLOCKER finding.

Output: `.planning/phases/29-ui-conformance/UI-REVIEW.md` (web section).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-ui-conformance/29-CONTEXT.md
@.planning/v1.0-handoff/DIVERGENCES.md
@.planning/v1.0-handoff/handoff/prototype/index.html
@.planning/v1.0-handoff/handoff/DESIGN-SYSTEM.md
@frontend/src/styles/tokens.css

<!-- Per-screen source paths (read on demand): -->
<!-- frontend/src/screensV10/Home/HomeMount.tsx + .module.css -->
<!-- frontend/src/screensV10/Transactions/TransactionsV10.tsx + .module.css -->
<!-- frontend/src/screensV10/AddSheet/AddSheet.tsx + .module.css -->
<!-- frontend/src/screensV10/CategoryDetail/... -->
<!-- frontend/src/screensV10/Plan/... -->
<!-- frontend/src/screensV10/Subscriptions/... -->
<!-- frontend/src/screensV10/Savings/... -->
<!-- frontend/src/screensV10/Ai/... -->

<!-- Baseline screenshots from 29-01: -->
<!-- frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/*.png -->
<!-- (8 PNG: home, transactions, add-sheet, category-detail, plan-month, subscriptions, savings, ai-initial) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-screen side-by-side audit + write UI-REVIEW.md (web section)</name>
  <files>.planning/phases/29-ui-conformance/UI-REVIEW.md</files>
  <read_first>
    - .planning/v1.0-handoff/handoff/prototype/index.html (full file — это reference render всех 8 экранов в одном HTML; найди соответствующие секции через id/class/data-screen или порядок в DOM)
    - .planning/v1.0-handoff/handoff/DESIGN-SYSTEM.md (sections 1-7 для token reference: палитра, типографика, spacing, радиусы, анимации)
    - .planning/v1.0-handoff/DIVERGENCES.md (full file — список known deviations W-01..W-05, I-01..I-05, X-01..X-02)
    - frontend/src/styles/tokens.css (current implemented token values для проверки соответствия prototype)
    - .planning/phases/29-ui-conformance/29-01-SUMMARY.md (если plan 29-01 готов — ссылка на baseline PNG paths)
    - Все 8 baseline PNG: `frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/*.png` (visual reference)
    - Per-screen sources (read только нужные секции через grep first):
      - frontend/src/screensV10/Home/HomeMount.tsx + HomeMount.module.css
      - frontend/src/screensV10/Transactions/TransactionsV10.tsx + .module.css
      - frontend/src/screensV10/AddSheet/AddSheet.tsx + .module.css
      - frontend/src/screensV10/CategoryDetail/{main view}.tsx + .module.css
      - frontend/src/screensV10/Plan/{main view}.tsx + .module.css
      - frontend/src/screensV10/Subscriptions/{main view}.tsx + .module.css
      - frontend/src/screensV10/Savings/{main view}.tsx + .module.css
      - frontend/src/screensV10/Ai/{main view}.tsx + .module.css
  </read_first>
  <acceptance_criteria>
    - Файл `.planning/phases/29-ui-conformance/UI-REVIEW.md` существует (≥120 строк).
    - YAML frontmatter содержит: `phase: 29`, `audit_target: web`, `audited_at: <ISO date>`, `prototype_ref:`, `screens_total: 8`.
    - Содержит секцию `## Web` (главная) с подсекциями `### 1. Home` … `### 8. AI initial-state` (8 штук).
    - Каждый screen-раздел содержит:
      - **Status** (одна из: PASS / BLOCKER / WARNING / INFO).
      - **Findings** (список или явное «No deviations»). Для каждой finding: `[BLOCKER|WARNING|INFO]` префикс, what + where (file:line или token name) + expected (prototype reference) + actual (implemented).
    - Секция `## Excluded — known DIVERGENCES.md` явно перечисляет W-01, W-02, W-03, W-04, W-05, X-01, X-02 (web-related) с пометкой «not re-flagged».
    - Секция `## Summary` с counts: BLOCKER count, WARNING count, INFO count, PASS count screens. Total findings ≥ 0.
    - `grep -c BLOCKER UI-REVIEW.md` returns N where N matches the value заявленный в Summary.
  </acceptance_criteria>
  <action>
    **Method (per-screen, repeat 8 times):**

    1. **Open prototype reference.** Find соответствующую секцию в `prototype/index.html`. Если секции imeet id-attributes (#home, #transactions etc.) — используй их; иначе ищи по hero-text («Дневной темп», «Реестр.», «NEW ENTRY», «Подписки.», «Копилка.», и т.д.).
    2. **Open implemented screenshot.** Используй Read tool на PNG из `frontend/tests/e2e/__screenshots__/v10-pixel-snapshots.spec.ts/{screen-name}.png`. Read tool рендерит изображение визуально.
    3. **Open implemented source.** Read TSX + CSS module для этого экрана (через Grep сначала чтобы найти главный файл). Note token usage (`var(--poster-coral)`, `var(--poster-ink)`, etc.) — крест-проверь с `tokens.css` value.
    4. **Dimensional checks (apply rubric из CONTEXT.md):**
       - **Color:** prototype hex vs implemented `--poster-*` token vs CSS module override. Diff ≥ 3 hex digits → BLOCKER.
       - **Layout:** order of elements (eyebrow → hero → bigfig → CTA), missing element → BLOCKER. Spacing 1-3px off → WARNING; ≥4px → BLOCKER.
       - **Typography:** font-family (DM Serif vs PT Serif для cyrillic — already W-01, exclude!), weight (500 vs 600 → WARNING), size (px diff ≥ 4px → BLOCKER).
       - **Element existence:** missing component (BLOCKER), wrong icon (WARNING если subjective, BLOCKER если semantically misleading).
       - **Animation:** static screenshot не покажет — для motion deviations only flag если можно увидеть в final state (например, отсутствует hover-state CSS class).
    5. **Dedupe против DIVERGENCES.md.** Для каждой prospective finding проверь не описана ли она в W-01..W-05 / X-01..X-02. Если да — НЕ записывай в findings (запиши в `## Excluded` блок).
    6. **Write screen subsection** в формате (см. acceptance_criteria).

    **Special considerations:**
    - **PlanMonth и Subscriptions** (per DIVERGENCES.md §W-05): selectors permissive, screenshot мог захватить Home если CTA не нашлась. Если screenshot для plan-month / subscriptions выглядит как Home — flag это в finding `[BLOCKER] Screenshot setup did not navigate to PlanMonth — needs test-id (см. §W-05)` — это валидный BLOCKER потому что нельзя проводить аудит без правильного screenshot.
    - **AddSheet:** Проверь black bg + BigFig 86px yellow + 3×4 keypad presence (per CONTEXT.md screen list).
    - **Color hex precision:** `tokens.css` имеет `--poster-coral`, `--poster-cobalt`, etc. Извлекай RGB значения для prototype через grep на `prototype/index.html` для inline style/class definitions, либо ищи design-system token mapping в `DESIGN-SYSTEM.md §1`.

    **File structure:**

    ```markdown
    ---
    phase: 29
    audit_target: web
    audited_at: <ISO timestamp>
    prototype_ref: .planning/v1.0-handoff/handoff/prototype/index.html
    design_system_ref: .planning/v1.0-handoff/handoff/DESIGN-SYSTEM.md
    screens_total: 8
    ---

    # Phase 29 — UI Conformance Audit (Web)

    Per-screen deviation report against `prototype/index.html`.
    Severity rubric defined in 29-CONTEXT.md §decisions.

    ## Excluded — known DIVERGENCES.md (not re-flagged)

    - W-01 (DM Serif Cyrillic fallback) — accepted ADR-001
    - W-02 (snapshot tolerance 2%) — accepted
    - W-03 (animation duration zeroed) — accepted
    - W-04 (baseline PNGs deferred) — closed by plan 29-01
    - W-05 (selectors permissive) — see screen-level findings
    - X-01 (tab swap differs web vs iOS) — accepted
    - X-02 (toast lifetime symmetric) — PASS

    ## Web

    ### 1. Home

    **Status:** [PASS | BLOCKER | WARNING | INFO]

    **Findings:**
    - [BLOCKER] coral background hex `#FF6B5B` (prototype) vs `--poster-coral: #...` (tokens.css L<n>) — diff <X> digits.
      - File: `frontend/src/styles/tokens.css:<line>`
      - Expected: `#FF6B5B`
      - Actual: `<implemented hex>`
    - [WARNING] eyebrow letter-spacing `0.16em` (prototype) vs `0.14em` (HomeMount.module.css L<n>).
    - ...

    ### 2. Transactions
    ...

    ### 3. AddSheet
    ...

    (continue for all 8 screens)

    ## Summary

    | Severity | Count | Screens affected |
    |----------|-------|------------------|
    | BLOCKER  | N     | <list>           |
    | WARNING  | N     | <list>           |
    | INFO     | N     | <list>           |
    | PASS     | N     | <list>           |

    **Next:** Plan 29-04 will spawn one fix-plan per BLOCKER-flagged screen.
    ```

    Per UICONF-02. Используй conservative scoring: если сомневаешься между BLOCKER и WARNING → выбирай WARNING (BLOCKER значит обязательно фиксить inline в Phase 29).
  </action>
  <verify>
    <automated>test -f .planning/phases/29-ui-conformance/UI-REVIEW.md && wc -l .planning/phases/29-ui-conformance/UI-REVIEW.md | awk '{ exit ($1 < 120) }' && grep -c "^### " .planning/phases/29-ui-conformance/UI-REVIEW.md | grep -qE "^([89]|1[0-9])$" && grep -q "## Excluded" .planning/phases/29-ui-conformance/UI-REVIEW.md && grep -q "## Summary" .planning/phases/29-ui-conformance/UI-REVIEW.md</automated>
  </verify>
  <done>
    UI-REVIEW.md существует, содержит web-секцию с 8 screen-подразделами,
    Excluded-блок с W-01..W-05/X-01..X-02, Summary с counts. Каждая
    BLOCKER/WARNING finding ссылается на конкретный file:line или token.
  </done>
</task>

</tasks>

<verification>
1. `wc -l .planning/phases/29-ui-conformance/UI-REVIEW.md` ≥ 120.
2. `grep -c "^### [0-9]\." .planning/phases/29-ui-conformance/UI-REVIEW.md` = 8.
3. `grep -E "^- (\[BLOCKER\]|\[WARNING\]|\[INFO\])" UI-REVIEW.md | wc -l` ≥ 0 (file may have zero findings if PASS).
4. `grep -E "W-0[1-5]|X-0[12]" UI-REVIEW.md` присутствует в Excluded section.
5. Manual sanity: каждая BLOCKER finding имеет `File: frontend/src/...` reference.
</verification>

<success_criteria>
- UI-REVIEW.md (web) готов как input для plan 29-04 (BLOCKER fix wave).
- BLOCKER count явно записан в Summary table (включая «0 BLOCKER» если найдено только WARNING/INFO).
</success_criteria>

<output>
After completion, create `.planning/phases/29-ui-conformance/29-02-SUMMARY.md` documenting:
- Final BLOCKER/WARNING/INFO counts.
- List of screens с BLOCKER findings (это feeds plan 29-04 sub-plan creation).
- List of screens с PASS status (no work для них в plan 29-04).
- Любые edge-cases (например W-05 setup-issue) которые plan 29-04 должен решать как pre-condition.
</output>
