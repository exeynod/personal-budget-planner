---
phase: 02-domain-foundation-and-onboarding
plan: 06
type: execute
wave: 4
depends_on: [04]
files_modified:
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/App.module.css
  - frontend/src/styles/tokens.css
  - frontend/src/api/client.ts
  - frontend/src/api/types.ts
  - frontend/src/hooks/useUser.ts
  - frontend/src/components/SectionCard.tsx
  - frontend/src/components/SectionCard.module.css
  - frontend/src/components/Stepper.tsx
  - frontend/src/components/Stepper.module.css
  - frontend/src/components/MainButton.tsx
  - frontend/src/screens/OnboardingScreen.tsx
  - frontend/src/screens/OnboardingScreen.module.css
  - frontend/src/screens/HomeScreen.tsx
  - frontend/src/screens/HomeScreen.module.css
autonomous: false  # contains checkpoint:human-verify
requirements:
  - ONB-01
  - ONB-02

must_haves:
  truths:
    - "Mini App открывается → App.tsx делает GET /me и роутит на onboarding если onboarded_at is null"
    - "OnboardingScreen — single-page scrollable с 4 нумерованными секциями (sketch 006-B)"
    - "Section 1 (bot bind): кнопка openTelegramLink → /start onboard; через polling /me меняет состояние на ✓ Привязано"
    - "Section 2 (balance): input в рублях, парсится в копейки на отправке"
    - "Section 3 (cycle day): stepper 1..28, default 5"
    - "Section 4 (seed cats): checkbox, default true"
    - "Telegram MainButton (или fallback button) активна только когда форма валидна; click → POST /onboarding/complete → home"
    - "После завершения онбординга — home placeholder с кнопками 'Категории' / 'Настройки' (placeholders в Plan 02-07)"
  artifacts:
    - path: "frontend/src/styles/tokens.css"
      provides: "Скопирован/импортирован дизайн-токен набор из sketches/themes/default.css"
    - path: "frontend/src/api/client.ts"
      provides: "apiFetch wrapper с X-Telegram-Init-Data header"
      exports: ["apiFetch"]
    - path: "frontend/src/api/types.ts"
      provides: "TypeScript типы для всех API responses (MeResponse, CategoryRead, PeriodRead, etc.)"
    - path: "frontend/src/screens/OnboardingScreen.tsx"
      provides: "Single-page scrollable onboarding (sketch 006-B)"
    - path: "frontend/src/screens/HomeScreen.tsx"
      provides: "Placeholder home с навигацией (Категории, Настройки)"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "@telegram-apps/sdk-react retrieveLaunchParams"
      via: "import"
      pattern: "retrieveLaunchParams\\|tgWebAppData\\|initData"
    - from: "frontend/src/screens/OnboardingScreen.tsx"
      to: "POST /api/v1/onboarding/complete"
      via: "apiFetch"
      pattern: "/onboarding/complete"
    - from: "frontend/src/components/MainButton.tsx"
      to: "@telegram-apps/sdk-react mainButton"
      via: "SDK signal/hook"
      pattern: "mainButton"
---

<objective>
Wave 4 (frontend, parallel with bot Plan 02-05): построить полноценный SPA скаффолд + onboarding-экран. Заменить Phase 1 placeholder в App.tsx на router-state machine, создать API client с initData header, реализовать OnboardingScreen по паттерну sketch 006-B + базовые компоненты (SectionCard, Stepper, MainButton fallback).

Что НЕ входит в этот план: CategoriesScreen и SettingsScreen — Plan 02-07 (но HomeScreen с навигационными кнопками сюда входит, чтобы можно было визуально проверить переход после onboarding).

Покрывает: ONB-01 (scrollable single-page), ONB-02 (openTelegramLink button) — frontend часть.

Output: 16 frontend-файлов; checkpoint:human-verify в конце для UI walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md
@.planning/phases/02-domain-foundation-and-onboarding/02-UI-SPEC.md
@.planning/sketches/006-onboarding/index.html
@.planning/sketches/themes/default.css
@frontend/package.json
@frontend/src/App.tsx
@frontend/src/main.tsx

<interfaces>
<!-- Установленные пакеты (frontend/package.json) -->
react@18.3.1
react-dom@18.3.1
@telegram-apps/sdk-react@3.3.9
typescript@^5.6.2
vite@8.0.10

<!-- API контракты (Plan 02-04) -->

GET  /api/v1/me                          → MeResponse
GET  /api/v1/categories?include_archived → CategoryRead[]
POST /api/v1/categories                  → CategoryRead
PATCH /api/v1/categories/{id}            → CategoryRead
DELETE /api/v1/categories/{id}           → CategoryRead
GET  /api/v1/periods/current             → PeriodRead | 404
POST /api/v1/onboarding/complete         → OnboardingCompleteResponse | 409 | 422
GET  /api/v1/settings                    → SettingsRead
PATCH /api/v1/settings                   → SettingsRead

Response schemas:
- MeResponse: { tg_user_id: number; tg_chat_id: number | null; cycle_start_day: number; onboarded_at: string | null; chat_id_known: boolean }
- CategoryRead: { id: number; name: string; kind: 'expense'|'income'; is_archived: boolean; sort_order: number; created_at: string }
- PeriodRead: { id: number; period_start: string; period_end: string; starting_balance_cents: number; ending_balance_cents: number | null; status: 'active'|'closed'; closed_at: string | null }
- OnboardingCompleteResponse: { period_id: number; seeded_categories: number; onboarded_at: string }
- SettingsRead: { cycle_start_day: number }

<!-- @telegram-apps/sdk-react 3.3.9 API (наиболее вероятные имена; проверяется первым в Task 1) -->

import { init, retrieveLaunchParams } from '@telegram-apps/sdk-react'
const launchParams = retrieveLaunchParams()
// launchParams.tgWebAppData — raw initData query string

import { mainButton, openTelegramLink } from '@telegram-apps/sdk-react'
mainButton.setParams({ text: 'Готово', isVisible: true, isEnabled: true })
mainButton.onClick(() => {...})
mainButton.isMounted()  // защита от вызова до init

openTelegramLink('https://t.me/<bot>?start=onboard')

// Если эти прямые импорты не работают, в качестве fallback использовать window.Telegram.WebApp:
window.Telegram?.WebApp?.initDataRaw
window.Telegram?.WebApp?.MainButton.setParams({...})
window.Telegram?.WebApp?.openTelegramLink(url)
</interfaces>

<sketch_reference>
Sketch 006-B (выбранный winner) показывает:
- Header «Добро пожаловать»
- Hero icon + заголовок «Несколько шагов» + subtitle «Заполните по порядку — займёт минуту»
- 4 нумерованных секции в виде карточек:
  - .vb-section: padding 16px 18px, background var(--color-surface), border-radius var(--radius-md)
  - .head: flex с numbered circle (24px round) + h4 + опциональным check-mark
  - num: фон primary blue (или success green если done)
- MainButton «Готово» внизу
- Стиль из sketches/themes/default.css (banking-premium dark)
</sketch_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Установить дизайн-токены, создать API client + types + useUser hook</name>
  <read_first>
    - .planning/sketches/themes/default.css (только первые 100 строк — design tokens)
    - .planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md (Pattern 5: API client; Pitfall 5: SDK init order)
    - .planning/phases/02-domain-foundation-and-onboarding/02-UI-SPEC.md (Design Tokens table)
    - frontend/package.json (sdk-react version)
  </read_first>
  <files>frontend/src/styles/tokens.css, frontend/src/api/client.ts, frontend/src/api/types.ts, frontend/src/hooks/useUser.ts</files>
  <behavior>
    - tokens.css: скопировать содержимое .planning/sketches/themes/default.css полностью; импортируется в main.tsx
    - api/types.ts: TypeScript интерфейсы для всех API responses
    - api/client.ts: apiFetch<T>(path, init) — добавляет X-Telegram-Init-Data header (через retrieveLaunchParams или window.Telegram.WebApp); throws Error с message на non-2xx; парсит JSON
    - hooks/useUser.ts: hook с MeResponse + refetch(); делает GET /me, кэширует через useState
    - Если import { retrieveLaunchParams } from '@telegram-apps/sdk-react' падает на TypeScript-уровне — fallback на (window as any).Telegram?.WebApp?.initData (raw)
  </behavior>
  <action>
**1. Создать `frontend/src/styles/tokens.css`:**

Скопировать СОДЕРЖИМОЕ файла `.planning/sketches/themes/default.css` целиком (~250 строк включая light theme override). Сохранить путь: `frontend/src/styles/tokens.css`. Не добавлять/менять переменные — design system уже спроектирована, переиспользуем.

В command-form (для исполнителя):
```bash
cp /Users/exy/pet_projects/tg-budget-planner/.planning/sketches/themes/default.css \
   /Users/exy/pet_projects/tg-budget-planner/frontend/src/styles/tokens.css
```

**2. Создать `frontend/src/api/types.ts`:**

```typescript
/** Mirrors `MeResponse` from app/api/router.py. */
export interface MeResponse {
  tg_user_id: number;
  tg_chat_id: number | null;
  cycle_start_day: number;
  onboarded_at: string | null;  // ISO datetime
  chat_id_known: boolean;
}

export type CategoryKind = 'expense' | 'income';

/** Mirrors `CategoryRead` from app/api/schemas/categories.py. */
export interface CategoryRead {
  id: number;
  name: string;
  kind: CategoryKind;
  is_archived: boolean;
  sort_order: number;
  created_at: string;
}

export interface CategoryCreatePayload {
  name: string;
  kind: CategoryKind;
  sort_order?: number;
}

export interface CategoryUpdatePayload {
  name?: string;
  sort_order?: number;
  is_archived?: boolean;
}

export type PeriodStatus = 'active' | 'closed';

/** Mirrors `PeriodRead` from app/api/schemas/periods.py. */
export interface PeriodRead {
  id: number;
  period_start: string;  // ISO date
  period_end: string;
  starting_balance_cents: number;
  ending_balance_cents: number | null;
  status: PeriodStatus;
  closed_at: string | null;
}

/** POST /onboarding/complete request. */
export interface OnboardingCompleteRequest {
  starting_balance_cents: number;
  cycle_start_day: number;
  seed_default_categories: boolean;
}

/** POST /onboarding/complete response. */
export interface OnboardingCompleteResponse {
  period_id: number;
  seeded_categories: number;
  onboarded_at: string;
}

export interface SettingsRead {
  cycle_start_day: number;
}

export interface SettingsUpdatePayload {
  cycle_start_day: number;
}
```

**3. Создать `frontend/src/api/client.ts`:**

```typescript
const API_BASE = '/api/v1';

declare global {
  interface Window {
    Telegram?: {
      WebApp?: {
        initData?: string;
        initDataUnsafe?: unknown;
        MainButton?: any;
        openTelegramLink?: (url: string) => void;
        ready?: () => void;
      };
    };
  }
}

/**
 * Read raw initData from Telegram WebApp environment.
 *
 * Tries `@telegram-apps/sdk-react` retrieveLaunchParams first; falls back to
 * `window.Telegram.WebApp.initData` if SDK API surface differs in 3.3.9.
 * In dev (browser, no Telegram), returns null — backend with DEV_MODE=true
 * will inject mock owner per Phase 1 D-05.
 */
export function getInitDataRaw(): string | null {
  // Strategy 1: SDK retrieveLaunchParams (preferred)
  try {
    // Lazy import via dynamic require for tolerance to API differences
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const sdk = require('@telegram-apps/sdk-react') as Record<string, unknown>;
    if (typeof sdk.retrieveLaunchParams === 'function') {
      const params = (sdk.retrieveLaunchParams as () => Record<string, unknown>)();
      const candidate = (params.tgWebAppData ?? params.initDataRaw ?? params.initData) as
        | string
        | undefined;
      if (candidate) return candidate;
    }
  } catch {
    // SDK not available or signature mismatch; fall through to window probe.
  }

  // Strategy 2: window.Telegram.WebApp.initData (raw query string)
  const wa = typeof window !== 'undefined' ? window.Telegram?.WebApp : undefined;
  if (wa?.initData) return wa.initData;

  return null;
}

export class ApiError extends Error {
  status: number;
  body: string;
  constructor(message: string, status: number, body: string) {
    super(message);
    this.status = status;
    this.body = body;
  }
}

export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> {
  const headers = new Headers(init?.headers);
  headers.set('Content-Type', 'application/json');

  const initDataRaw = getInitDataRaw();
  if (initDataRaw) {
    headers.set('X-Telegram-Init-Data', initDataRaw);
  } else if (import.meta.env.DEV) {
    // Backend with DEV_MODE=true ignores header content (Phase 1 D-05).
    headers.set('X-Telegram-Init-Data', 'dev-mode-stub');
  }

  const response = await fetch(API_BASE + path, { ...init, headers });
  const text = await response.text();
  if (!response.ok) {
    throw new ApiError(`API ${path} → ${response.status}`, response.status, text);
  }
  return text ? (JSON.parse(text) as T) : (undefined as T);
}

/** Small Telegram bridge — kept here to centralise SDK version tolerance. */
export function openTelegramLink(url: string): void {
  try {
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const sdk = require('@telegram-apps/sdk-react') as Record<string, unknown>;
    if (typeof sdk.openTelegramLink === 'function') {
      (sdk.openTelegramLink as (u: string) => void)(url);
      return;
    }
  } catch {
    /* fall through */
  }
  const wa = typeof window !== 'undefined' ? window.Telegram?.WebApp : undefined;
  if (wa?.openTelegramLink) {
    wa.openTelegramLink(url);
    return;
  }
  // Final fallback (browser dev): open in new tab
  window.open(url, '_blank');
}
```

**4. Создать `frontend/src/hooks/useUser.ts`:**

```typescript
import { useCallback, useEffect, useState } from 'react';
import { apiFetch } from '../api/client';
import type { MeResponse } from '../api/types';

export interface UseUserResult {
  user: MeResponse | null;
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

export function useUser(): UseUserResult {
  const [user, setUser] = useState<MeResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refetch = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await apiFetch<MeResponse>('/me');
      setUser(data);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    void refetch();
  }, [refetch]);

  return { user, loading, error, refetch };
}
```

**Validation:**

```bash
cd /Users/exy/pet_projects/tg-budget-planner/frontend
# TypeScript compile check
npx tsc --noEmit
```

Должно завершиться без ошибок (или с warnings, но не errors).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && test -f src/styles/tokens.css && test -f src/api/client.ts && test -f src/api/types.ts && test -f src/hooks/useUser.ts && npx tsc --noEmit 2>&1 | (grep -E "error TS" || echo "tsc: no errors")</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/styles/tokens.css существует и содержит --color-bg переменную: `grep -c "color-bg" frontend/src/styles/tokens.css` >= 1
    - frontend/src/api/client.ts экспортирует apiFetch + getInitDataRaw + openTelegramLink + ApiError: `grep -c "^export" frontend/src/api/client.ts` >= 4
    - client.ts добавляет X-Telegram-Init-Data: `grep -c "X-Telegram-Init-Data" frontend/src/api/client.ts` >= 1
    - api/types.ts экспортирует MeResponse + CategoryRead + PeriodRead + OnboardingCompleteRequest: `grep -c "^export interface\\|^export type" frontend/src/api/types.ts` >= 7
    - hooks/useUser.ts экспортирует useUser: `grep -c "export function useUser\|export const useUser" frontend/src/hooks/useUser.ts` >= 1
    - **TypeScript компилируется без ошибок:** `cd frontend && npx tsc --noEmit` — нет TS errors (warnings допустимы)
  </acceptance_criteria>
  <done>4 файла основания frontend созданы; tsc проходит; API client готов к использованию остальными компонентами</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать общие компоненты (SectionCard, Stepper, MainButton fallback) + screens (Onboarding, Home) + обновить main.tsx и App.tsx</name>
  <read_first>
    - .planning/phases/02-domain-foundation-and-onboarding/02-UI-SPEC.md (Onboarding layout, Home placeholder)
    - .planning/sketches/006-onboarding/index.html (точный layout vb-section, stepper, sections numbering)
    - frontend/src/api/client.ts (apiFetch, openTelegramLink — только что созданные)
    - frontend/src/api/types.ts (MeResponse, OnboardingCompleteRequest)
  </read_first>
  <files>frontend/src/components/SectionCard.tsx, frontend/src/components/SectionCard.module.css, frontend/src/components/Stepper.tsx, frontend/src/components/Stepper.module.css, frontend/src/components/MainButton.tsx, frontend/src/screens/OnboardingScreen.tsx, frontend/src/screens/OnboardingScreen.module.css, frontend/src/screens/HomeScreen.tsx, frontend/src/screens/HomeScreen.module.css, frontend/src/App.tsx, frontend/src/App.module.css, frontend/src/main.tsx</files>
  <behavior>
    - SectionCard: пронумерованная карточка с состоянием (active | done | locked); рендерит { number, title, children, done?: bool }
    - Stepper: компонент `[ − ] N [ + ]` с пропсами { value, min, max, onChange, wrap?: bool }
    - MainButton: либо рендерит Telegram MainButton (через SDK signal/hook), либо fallback `<button>` внизу — определяется по window.Telegram наличию; пропсы { text, enabled, onClick }
    - OnboardingScreen: использует useUser() для chat_id_known; полная форма с 4 секциями; на submit POST /onboarding/complete; на success — onComplete callback (App.tsx переключает на 'home')
    - HomeScreen: простой плейсхолдер с двумя кнопками-навигаторами «Категории» и «Настройки» + текстом «Дашборд будет в Phase 5»
    - App.tsx: useState<'onboarding'|'home'|'categories'|'settings'>; useUser() для определения isOnboarded; роутит через switch; «categories» / «settings» рендерят placeholder text «Будет в Plan 02-07» (полная реализация в следующем плане)
    - main.tsx: импортирует tokens.css; вызывает init() из @telegram-apps/sdk-react если доступно (try/catch)
  </behavior>
  <action>
**1. Создать `frontend/src/components/SectionCard.tsx`:**

```typescript
import type { ReactNode } from 'react';
import styles from './SectionCard.module.css';

export interface SectionCardProps {
  number: number | string;
  title: string;
  done?: boolean;
  locked?: boolean;
  children?: ReactNode;
}

export function SectionCard({ number, title, done, locked, children }: SectionCardProps) {
  const cls = [styles.section, locked ? styles.locked : '', done ? styles.done : '']
    .filter(Boolean)
    .join(' ');
  return (
    <div className={cls}>
      <div className={styles.head}>
        <span className={`${styles.num} ${done ? styles.numDone : ''}`}>
          {done ? '✓' : number}
        </span>
        <h4 className={styles.title}>{title}</h4>
        {done && <span className={styles.checkMark}>✓</span>}
      </div>
      {children && <div className={styles.body}>{children}</div>}
    </div>
  );
}
```

**2. Создать `frontend/src/components/SectionCard.module.css`:**

```css
.section {
  margin: 0 0 16px;
  padding: 16px 18px;
  background: var(--color-surface);
  border-radius: var(--radius-md);
  transition: opacity 0.3s;
}
.section.locked {
  opacity: 0.4;
  pointer-events: none;
}
.section.done {
  background: var(--color-surface);
}
.head {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 12px;
}
.num {
  width: 24px;
  height: 24px;
  border-radius: 999px;
  background: var(--color-primary);
  color: #fff;
  display: grid;
  place-items: center;
  font-size: 12px;
  font-weight: 700;
  flex-shrink: 0;
}
.numDone {
  background: var(--color-success);
}
.locked .num {
  background: var(--color-text-dim);
}
.title {
  margin: 0;
  font-size: 15px;
  font-weight: 600;
  flex: 1;
  color: var(--color-text);
}
.checkMark {
  color: var(--color-success);
  font-size: 16px;
}
.body {
  /* allows children to manage their own margin */
}
```

**3. Создать `frontend/src/components/Stepper.tsx`:**

```typescript
import styles from './Stepper.module.css';

export interface StepperProps {
  value: number;
  min: number;
  max: number;
  onChange: (next: number) => void;
  wrap?: boolean;
}

export function Stepper({ value, min, max, onChange, wrap }: StepperProps) {
  const dec = () => {
    const next = value - 1;
    if (next < min) onChange(wrap ? max : min);
    else onChange(next);
  };
  const inc = () => {
    const next = value + 1;
    if (next > max) onChange(wrap ? min : max);
    else onChange(next);
  };
  return (
    <div className={styles.stepper}>
      <button type="button" onClick={dec} aria-label="Уменьшить">−</button>
      <span className={styles.val}>{value}</span>
      <button type="button" onClick={inc} aria-label="Увеличить">+</button>
    </div>
  );
}
```

**4. Создать `frontend/src/components/Stepper.module.css`:**

```css
.stepper {
  display: flex;
  align-items: center;
  gap: 14px;
}
.stepper button {
  width: 32px;
  height: 32px;
  border-radius: 999px;
  background: var(--color-bg-elevated);
  border: 0;
  color: var(--color-text);
  font-size: 18px;
  cursor: pointer;
  font-family: inherit;
  transition: background 0.15s;
}
.stepper button:hover {
  background: var(--color-primary-soft);
  color: var(--color-primary);
}
.val {
  flex: 1;
  text-align: center;
  font-size: 22px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  color: var(--color-text);
  min-width: 48px;
}
```

**5. Создать `frontend/src/components/MainButton.tsx`:**

```typescript
import { useEffect } from 'react';

export interface MainButtonProps {
  text: string;
  enabled: boolean;
  onClick: () => void;
}

/**
 * Telegram MainButton wrapper with browser fallback.
 *
 * In Telegram: uses window.Telegram.WebApp.MainButton API directly (most
 * portable across SDK versions). In browser dev: renders an inline button
 * fixed to the bottom of the viewport.
 */
export function MainButton({ text, enabled, onClick }: MainButtonProps) {
  const wa = typeof window !== 'undefined' ? window.Telegram?.WebApp : undefined;
  const tgMainButton = (wa as any)?.MainButton;

  useEffect(() => {
    if (!tgMainButton) return;
    tgMainButton.setText(text);
    tgMainButton.show();
    if (enabled) tgMainButton.enable();
    else tgMainButton.disable();
    tgMainButton.onClick(onClick);
    return () => {
      tgMainButton.offClick(onClick);
      tgMainButton.hide();
    };
  }, [tgMainButton, text, enabled, onClick]);

  if (tgMainButton) {
    // SDK renders the button — nothing to render in DOM.
    return null;
  }

  // Browser fallback
  return (
    <button
      type="button"
      onClick={enabled ? onClick : undefined}
      disabled={!enabled}
      style={{
        position: 'fixed',
        left: 16,
        right: 16,
        bottom: 28,
        height: 'var(--main-button-height, 54px)',
        background: 'var(--color-primary)',
        color: '#fff',
        border: 0,
        borderRadius: 'var(--radius-md, 14px)',
        fontSize: 16,
        fontWeight: 600,
        cursor: enabled ? 'pointer' : 'not-allowed',
        opacity: enabled ? 1 : 0.5,
        boxShadow: '0 6px 18px rgba(78,164,255,0.35)',
      }}
    >
      {text}
    </button>
  );
}
```

**6. Создать `frontend/src/screens/OnboardingScreen.tsx`:**

```typescript
import { useCallback, useEffect, useState } from 'react';
import { apiFetch, openTelegramLink, ApiError } from '../api/client';
import type { OnboardingCompleteRequest, MeResponse } from '../api/types';
import { SectionCard } from '../components/SectionCard';
import { Stepper } from '../components/Stepper';
import { MainButton } from '../components/MainButton';
import styles from './OnboardingScreen.module.css';

const BOT_USERNAME = 'tg_budget_planner_bot';  // matches settings.BOT_USERNAME default

function parseRubles(input: string): number | null {
  const cleaned = input.replace(/\s/g, '').replace(',', '.');
  if (cleaned === '' || cleaned === '-') return null;
  const value = Number.parseFloat(cleaned);
  if (Number.isNaN(value)) return null;
  return Math.round(value * 100);
}

export interface OnboardingScreenProps {
  user: MeResponse;
  onRefreshUser: () => Promise<void>;
  onComplete: () => void;
}

export function OnboardingScreen({ user, onRefreshUser, onComplete }: OnboardingScreenProps) {
  const [balanceStr, setBalanceStr] = useState<string>('');
  const [cycleDay, setCycleDay] = useState<number>(user.cycle_start_day || 5);
  const [seedCats, setSeedCats] = useState<boolean>(true);
  const [submitting, setSubmitting] = useState<boolean>(false);
  const [error, setError] = useState<string | null>(null);

  const balanceCents = parseRubles(balanceStr);
  const isValid =
    user.chat_id_known &&
    balanceCents !== null &&
    cycleDay >= 1 &&
    cycleDay <= 28 &&
    !submitting;

  // Polling: when chat is not bound yet, refresh /me every 2s (max 30s) so the
  // UI flips to "✓ Привязано" without manual reload after the user taps /start.
  useEffect(() => {
    if (user.chat_id_known) return;
    let cancelled = false;
    let attempts = 0;
    const tick = async () => {
      if (cancelled || attempts >= 15) return;
      attempts += 1;
      await onRefreshUser();
    };
    const id = window.setInterval(tick, 2000);
    return () => {
      cancelled = true;
      window.clearInterval(id);
    };
  }, [user.chat_id_known, onRefreshUser]);

  const handleOpenBot = useCallback(() => {
    openTelegramLink(`https://t.me/${BOT_USERNAME}?start=onboard`);
  }, []);

  const handleSubmit = useCallback(async () => {
    if (!isValid || balanceCents === null) return;
    setSubmitting(true);
    setError(null);
    try {
      const body: OnboardingCompleteRequest = {
        starting_balance_cents: balanceCents,
        cycle_start_day: cycleDay,
        seed_default_categories: seedCats,
      };
      await apiFetch('/onboarding/complete', {
        method: 'POST',
        body: JSON.stringify(body),
      });
      onComplete();
    } catch (e) {
      // 409 means already onboarded — proceed to home (server is source of truth).
      if (e instanceof ApiError && e.status === 409) {
        onComplete();
        return;
      }
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setSubmitting(false);
    }
  }, [isValid, balanceCents, cycleDay, seedCats, onComplete]);

  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <div className={styles.title}>Добро пожаловать</div>
      </header>

      <div className={styles.intro}>
        <div className={styles.heroIcon}>💸</div>
        <div className={styles.heroTitle}>Несколько шагов</div>
        <div className={styles.heroHint}>Заполните по порядку — займёт минуту</div>
      </div>

      {/* Section 1: bot bind */}
      <SectionCard number={1} title={user.chat_id_known ? 'Бот подключён' : 'Подключите бота'} done={user.chat_id_known}>
        {user.chat_id_known ? (
          <div className={styles.sectionMuted}>@{BOT_USERNAME} · готов отправлять уведомления</div>
        ) : (
          <button type="button" className={styles.openBotBtn} onClick={handleOpenBot}>
            Открыть @{BOT_USERNAME} в Telegram
          </button>
        )}
      </SectionCard>

      {/* Section 2: starting balance */}
      <SectionCard number={2} title="Стартовый баланс">
        <div className={styles.field}>
          <input
            type="text"
            inputMode="decimal"
            placeholder="0"
            value={balanceStr}
            onChange={(e) => setBalanceStr(e.target.value)}
            className={styles.balanceInput}
          />
          <div className={styles.fieldHint}>
            Будет начальной точкой для текущего периода. Можно ввести 0 или отрицательное (долг).
          </div>
        </div>
      </SectionCard>

      {/* Section 3: cycle day */}
      <SectionCard number={3} title="День начала периода">
        <Stepper value={cycleDay} min={1} max={28} onChange={setCycleDay} wrap />
        <div className={styles.fieldHint}>
          Например, 5 = период с 5-го одного месяца по 4-е следующего. Можно поменять в Настройках.
        </div>
      </SectionCard>

      {/* Section 4: seed categories */}
      <SectionCard number={4} title="Стартовые категории">
        <label className={styles.checkboxRow}>
          <input
            type="checkbox"
            checked={seedCats}
            onChange={(e) => setSeedCats(e.target.checked)}
          />
          <span>Засеять 14 стартовых категорий (Продукты, Дом, Машина и т.д.)</span>
        </label>
        <div className={styles.fieldHint}>Можно отредактировать или добавить свои в разделе «Категории».</div>
      </SectionCard>

      {error && <div className={styles.errorBanner}>Ошибка: {error}</div>}

      <MainButton text={submitting ? 'Сохранение…' : 'Готово'} enabled={isValid} onClick={handleSubmit} />
    </div>
  );
}
```

**7. Создать `frontend/src/screens/OnboardingScreen.module.css`:**

```css
.root {
  max-width: 375px;
  margin: 0 auto;
  padding: 12px 16px 120px;  /* bottom padding для MainButton */
  min-height: 100vh;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
}
.header {
  text-align: center;
  padding: 8px 0 12px;
}
.title {
  font-weight: 600;
  font-size: 16px;
  color: var(--color-text);
}
.intro {
  text-align: center;
  padding: 12px 24px 16px;
}
.heroIcon {
  width: 56px;
  height: 56px;
  margin: 0 auto 12px;
  border-radius: 24px;
  background: var(--color-primary-soft);
  color: var(--color-primary);
  display: grid;
  place-items: center;
  font-size: 28px;
}
.heroTitle {
  font-size: 18px;
  font-weight: 700;
  letter-spacing: -0.02em;
  margin-bottom: 4px;
  color: var(--color-text);
}
.heroHint {
  font-size: 13px;
  color: var(--color-text-muted);
  line-height: 1.5;
}
.sectionMuted {
  font-size: 13px;
  color: var(--color-text-muted);
}
.openBotBtn {
  width: 100%;
  padding: 12px 16px;
  background: var(--color-primary);
  color: #fff;
  border: 0;
  border-radius: var(--radius-md);
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  font-family: inherit;
}
.openBotBtn:hover { background: var(--color-primary-hover); }
.field {
  padding: 14px 16px;
  background: var(--color-bg-elevated);
  border-radius: var(--radius-md);
}
.balanceInput {
  width: 100%;
  border: 0;
  background: transparent;
  color: var(--color-text);
  font-size: 22px;
  font-weight: 700;
  font-family: inherit;
  outline: none;
  font-variant-numeric: tabular-nums;
  margin-bottom: 6px;
}
.fieldHint {
  font-size: 11px;
  color: var(--color-text-dim);
  margin-top: 6px;
}
.checkboxRow {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 14px;
  color: var(--color-text);
  cursor: pointer;
}
.errorBanner {
  margin: 12px 0;
  padding: 12px 16px;
  background: var(--color-danger-soft);
  color: var(--color-danger);
  border-radius: var(--radius-md);
  font-size: 13px;
}
```

**8. Создать `frontend/src/screens/HomeScreen.tsx`:**

```typescript
import styles from './HomeScreen.module.css';

export interface HomeScreenProps {
  onNavigate: (screen: 'categories' | 'settings') => void;
}

export function HomeScreen({ onNavigate }: HomeScreenProps) {
  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <div className={styles.title}>TG Budget</div>
      </header>
      <div className={styles.placeholder}>
        Дашборд будет в Phase 5.
        <br />Сейчас доступны только настройки и категории.
      </div>
      <div className={styles.nav}>
        <button type="button" className={styles.navBtn} onClick={() => onNavigate('categories')}>
          Категории
        </button>
        <button type="button" className={styles.navBtn} onClick={() => onNavigate('settings')}>
          Настройки
        </button>
      </div>
    </div>
  );
}
```

**9. Создать `frontend/src/screens/HomeScreen.module.css`:**

```css
.root {
  max-width: 375px;
  margin: 0 auto;
  padding: 12px 16px;
  min-height: 100vh;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
  display: flex;
  flex-direction: column;
}
.header {
  text-align: center;
  padding: 12px 0 24px;
}
.title {
  font-weight: 700;
  font-size: 22px;
  letter-spacing: -0.02em;
}
.placeholder {
  flex: 1;
  display: grid;
  place-items: center;
  text-align: center;
  font-size: 14px;
  color: var(--color-text-muted);
  line-height: 1.6;
  padding: 24px;
}
.nav {
  display: flex;
  gap: 12px;
  padding: 16px 0 24px;
}
.navBtn {
  flex: 1;
  padding: 14px 16px;
  background: var(--color-surface);
  color: var(--color-text);
  border: 0;
  border-radius: var(--radius-md);
  font-size: 15px;
  font-weight: 600;
  cursor: pointer;
  font-family: inherit;
}
.navBtn:hover { background: var(--color-surface-2); }
```

**10. Перезаписать `frontend/src/App.tsx`:**

```typescript
import { useState } from 'react';
import { useUser } from './hooks/useUser';
import { OnboardingScreen } from './screens/OnboardingScreen';
import { HomeScreen } from './screens/HomeScreen';
import styles from './App.module.css';

type Screen = 'onboarding' | 'home' | 'categories' | 'settings';

export default function App() {
  const { user, loading, error, refetch } = useUser();
  const [overrideScreen, setOverrideScreen] = useState<Screen | null>(null);

  if (loading && !user) {
    return <div className={styles.loadingRoot}>Загрузка…</div>;
  }
  if (error || !user) {
    return (
      <div className={styles.errorRoot}>
        Не удалось загрузить пользователя.<br />
        {error && <code>{error}</code>}
      </div>
    );
  }

  const isOnboarded = user.onboarded_at !== null;
  const screen: Screen = overrideScreen ?? (isOnboarded ? 'home' : 'onboarding');

  if (screen === 'onboarding') {
    return (
      <OnboardingScreen
        user={user}
        onRefreshUser={refetch}
        onComplete={() => {
          setOverrideScreen('home');
          void refetch();  // sync onboarded_at
        }}
      />
    );
  }
  if (screen === 'home') {
    return <HomeScreen onNavigate={(s) => setOverrideScreen(s)} />;
  }
  // categories | settings — placeholders until Plan 02-07
  return (
    <div className={styles.placeholderScreen}>
      <button type="button" className={styles.backBtn} onClick={() => setOverrideScreen('home')}>
        ← Назад
      </button>
      <h2>{screen === 'categories' ? 'Категории' : 'Настройки'}</h2>
      <p>Этот экран реализован в Plan 02-07.</p>
    </div>
  );
}
```

**11. Создать `frontend/src/App.module.css`:**

```css
.loadingRoot, .errorRoot, .placeholderScreen {
  max-width: 375px;
  margin: 0 auto;
  padding: 32px 16px;
  min-height: 100vh;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
  text-align: center;
}
.errorRoot code {
  display: block;
  margin-top: 12px;
  font-family: var(--font-mono);
  font-size: 12px;
  color: var(--color-danger);
}
.backBtn {
  display: inline-block;
  margin-bottom: 16px;
  padding: 8px 12px;
  background: transparent;
  border: 1px solid var(--color-border);
  color: var(--color-primary);
  border-radius: var(--radius-md);
  cursor: pointer;
  font-family: inherit;
}
```

**12. Перезаписать `frontend/src/main.tsx`:**

```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App.tsx';
import './styles/tokens.css';

// Initialise Telegram SDK (best-effort — tolerate missing/altered API surface).
try {
  // eslint-disable-next-line @typescript-eslint/no-require-imports
  const sdk = require('@telegram-apps/sdk-react') as Record<string, unknown>;
  if (typeof sdk.init === 'function') {
    (sdk.init as () => void)();
  }
} catch {
  // SDK not available or init shape differs in this version — frontend
  // gracefully falls back to window.Telegram.WebApp via api/client.ts.
}

// Tell Telegram WebApp we're ready (if running inside Telegram)
if (typeof window !== 'undefined' && window.Telegram?.WebApp?.ready) {
  window.Telegram.WebApp.ready();
}

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

**Validation:**

```bash
cd /Users/exy/pet_projects/tg-budget-planner/frontend

# TypeScript compile
npx tsc --noEmit

# Vite build (should produce dist/)
npm run build
```

Build must succeed (allows warnings, but no errors).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && for f in src/components/SectionCard.tsx src/components/Stepper.tsx src/components/MainButton.tsx src/screens/OnboardingScreen.tsx src/screens/HomeScreen.tsx src/App.tsx src/main.tsx; do test -f "$f" && echo "$f exists" || echo "MISSING $f"; done && npx tsc --noEmit 2>&1 | (grep -E "error TS" || echo "tsc: no errors") && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 12+ файлов созданы: 3 components (.tsx + .module.css), 2 screens (.tsx + .module.css), App.tsx + App.module.css, main.tsx
    - SectionCard принимает number/title/done props: `grep -c "number\|title\|done" frontend/src/components/SectionCard.tsx` >= 3
    - Stepper принимает min/max/wrap: `grep -c "min\|max\|wrap" frontend/src/components/Stepper.tsx` >= 3
    - MainButton использует window.Telegram.WebApp.MainButton: `grep -c "MainButton" frontend/src/components/MainButton.tsx` >= 2
    - OnboardingScreen имеет 4 SectionCard: `grep -c "SectionCard" frontend/src/screens/OnboardingScreen.tsx` >= 4
    - OnboardingScreen парсит рубли в копейки: `grep -c "parseRubles\|\\* 100" frontend/src/screens/OnboardingScreen.tsx` >= 1
    - OnboardingScreen вызывает /onboarding/complete: `grep -c "/onboarding/complete" frontend/src/screens/OnboardingScreen.tsx` == 1
    - OnboardingScreen вызывает openTelegramLink: `grep -c "openTelegramLink" frontend/src/screens/OnboardingScreen.tsx` >= 1
    - OnboardingScreen имеет polling /me каждые 2s: `grep -c "setInterval\|2000" frontend/src/screens/OnboardingScreen.tsx` >= 1
    - App.tsx использует useUser hook: `grep -c "useUser" frontend/src/App.tsx` >= 1
    - App.tsx роутит на onboarding если onboarded_at is null: `grep -c "onboarded_at" frontend/src/App.tsx` >= 1
    - main.tsx импортирует tokens.css: `grep -c "tokens.css" frontend/src/main.tsx` == 1
    - **TypeScript компилируется:** `cd frontend && npx tsc --noEmit` — нет TS errors
    - **Vite build проходит:** `cd frontend && npm run build` — генерирует dist/ без ошибок
  </acceptance_criteria>
  <done>Frontend SPA scaffold готов: dependency tokens, API client, useUser hook, 3 переиспользуемых компонента, OnboardingScreen полная, HomeScreen placeholder; tsc + vite build проходят</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: ручная проверка onboarding flow в Telegram</name>
  <what-built>
    - Полный SPA scaffold + OnboardingScreen + HomeScreen placeholder
    - main_bot.py обновлён (Plan 02-05): /start handler делает chat-bind + WebApp button
    - Backend (Plan 02-04): все endpoints для /me, /onboarding/complete, /periods/current работают
  </what-built>
  <how-to-verify>
**Pre-requisites:**
1. `.env` заполнен реальными значениями: `BOT_TOKEN`, `BOT_USERNAME`, `OWNER_TG_ID`, `INTERNAL_TOKEN`, `MINI_APP_URL` (для local dev — например `https://your-ngrok.ngrok.io` или dev-домен через Caddy override)
2. `docker compose up -d --build api bot db worker` — все 4 сервиса в `healthy`
3. `docker compose logs api bot` — нет ошибок при старте
4. Frontend собран и доступен через Caddy: `curl -sf http://localhost/healthz` → `{"status":"ok"}` (проверка что reverse proxy жив)

**Walkthrough (см. 02-UI-SPEC.md §Acceptance.1):**

1. Открыть Telegram, найти своего бота, отправить `/start` (без payload).
   - **Expected:** Bot отвечает «Бот запущен и готов к работе. Откройте Mini App, чтобы управлять бюджетом.» + InlineKeyboardButton «Открыть бюджет»
   - **Verify:** `docker compose exec db psql -U budget -d budget_db -c "SELECT tg_user_id, tg_chat_id, onboarded_at FROM app_user"` — строка существует, `tg_chat_id` заполнен, `onboarded_at` IS NULL

2. Нажать «Открыть бюджет» → открывается Mini App.
   - **Expected:** Виден экран «Добро пожаловать» с 4 секциями (Section 1 уже в done-состоянии «✓ Бот подключён», Sections 2-4 active)

3. Заполнить:
   - Section 2: ввести `12450` (или с пробелами/запятыми: `12 450,50`)
   - Section 3: оставить 5 (или поменять, например на 10)
   - Section 4: оставить чекбокс включённым
   - **Expected:** MainButton «Готово» становится активной (синяя)

4. Нажать MainButton.
   - **Expected:** Mini App переключается на HomeScreen «Дашборд будет в Phase 5» с двумя кнопками
   - **Verify:** `docker compose exec db psql -U budget -d budget_db -c "SELECT COUNT(*) FROM category"` → 14
   - `docker compose exec db psql -U budget -d budget_db -c "SELECT COUNT(*), MAX(starting_balance_cents) FROM budget_period WHERE status='active'"` → 1 строка с `1245000` (или 1245050 если ввели десятичные)
   - `docker compose exec db psql -U budget -d budget_db -c "SELECT onboarded_at FROM app_user"` → не NULL

5. Закрыть Mini App, открыть заново.
   - **Expected:** Открывается сразу HomeScreen, OnboardingScreen НЕ показывается

6. Edge case: если `chat_id_known=false` (например, очистить tg_chat_id в БД через `UPDATE app_user SET tg_chat_id=NULL`), открыть Mini App:
   - **Expected:** Section 1 показывает «Подключите бота» с кнопкой; MainButton DISABLED; click на кнопку → открывается чат с ботом

7. **Optional**: invalid balance (буквы) — поле осталось без значения → MainButton остаётся DISABLED.

**If anything fails:**
- 4xx ответ от backend → проверить `docker compose logs api`
- Бот не отвечает на /start → `docker compose logs bot` (проверить INTERNAL_TOKEN+API_BASE_URL)
- Mini App пустая страница → `docker compose logs caddy` + open browser DevTools console
- TypeScript / runtime error в Mini App → раскрыть Telegram WebApp DevTools (на iOS — нужен Safari Web Inspector, на Android — Chrome remote debugging)
  </how-to-verify>
  <resume-signal>Type "approved" если flow прошёл; иначе опиши, что не работает (с логами docker compose logs если применимо)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API | Все запросы идут через Caddy (TLS) → /api/v1/* → api:8000; X-Telegram-Init-Data добавляется фронтом |
| Telegram → Mini App | initData приходит из Telegram-cloud, валидируется на бэке (Phase 1 verified) |
| User → Mini App | React по умолчанию escape'ит strings; нет dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fe-xss | XSS | OnboardingScreen, HomeScreen | mitigate | Все динамические значения проходят через JSX (auto-escape); пользовательский ввод (balanceStr, имена будущих категорий) тоже идёт через `{}` |
| T-fe-balance-overflow | Tampering | parseRubles | mitigate | parseRubles → Math.round(... * 100); JS Number.MAX_SAFE_INTEGER (2^53) >> разумного баланса даже в копейках; для очень больших значений (триллионы) backend BigInt всё равно принимает |
| T-fe-leaked-token | Info Disclosure | api/client.ts | mitigate | INITDATA пишется только в header исходящего запроса; не логируется (нет console.log с initData); не сохраняется в localStorage |
| T-fe-csrf | CSRF | API endpoints | accept | Backend требует валидный X-Telegram-Init-Data (HMAC); классический CSRF (cookie auth) не применим — auth stateless через initData |
| T-fe-clickjack | Clickjacking | Mini App | accept | Telegram WebApp работает только внутри Telegram-клиента; iframe-embedding не возможен извне |
| T-onboarding-double-click | Tampering | OnboardingScreen.handleSubmit | mitigate | submitting flag блокирует повторный submit; backend 409 на повтор → onComplete (idempotent UX) |
| T-polling-resource-leak | DoS | OnboardingScreen polling | mitigate | Polling cleared в useEffect cleanup; max 15 attempts (30s) hard cap |
</threat_model>

<verification>
План завершён когда:
1. Все 16 файлов frontend созданы и tsc проходит без ошибок
2. `npm run build` в frontend/ успешен (генерирует dist/)
3. Checkpoint:human-verify пройден (см. above)
4. Никакого изменения в backend (Plan 02-04) или bot (Plan 02-05)
5. Phase 1 backend тесты не сломаны (косвенно: backend не трогался)
</verification>

<success_criteria>
- React SPA загружается (Caddy раздаёт frontend/dist/)
- App.tsx роутит между screens на основе onboarded_at
- OnboardingScreen полностью функционален: 4 секции, валидация, submit
- Bot bind polling работает (chat_id_known обновляется без manual reload)
- MainButton (Telegram или fallback) реагирует на валидность формы
- После onboarding пользователь видит HomeScreen с кнопками-навигаторами
- Categories/Settings — placeholder text «Plan 02-07»
- Manual UI walkthrough пройден без ошибок
</success_criteria>

<output>
После завершения создать `.planning/phases/02-domain-foundation-and-onboarding/02-06-SUMMARY.md`
</output>
