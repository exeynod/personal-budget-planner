---
phase: 02-domain-foundation-and-onboarding
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_period_engine.py
  - tests/test_categories.py
  - tests/test_periods.py
  - tests/test_onboarding.py
  - tests/test_settings.py
  - tests/test_telegram_chat_bind.py
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - PER-01
  - PER-02
  - PER-03
  - PER-05
  - ONB-01
  - ONB-03
  - SET-01

must_haves:
  truths:
    - "Тесты Phase 2 существуют в RED-состоянии (упадут с ImportError на app.services/app.core.period)"
    - "test_period_engine.py покрывает 9+ кейсов (включая edge: cycle_start_day=31 в феврале, year-rollover)"
    - "test_categories.py покрывает CRUD + soft-archive + seed-idempotency"
    - "test_onboarding.py проверяет 409 на повторный complete и atomic rollback"
    - "test_settings.py валидирует 1..28 диапазон и не-перепресчёт прошлых периодов"
    - "test_telegram_chat_bind.py проверяет upsert и 403 без internal-токена"
  artifacts:
    - path: "tests/test_period_engine.py"
      provides: "Параметризованные unit-тесты period_for"
    - path: "tests/test_categories.py"
      provides: "Integration-тесты CRUD категорий + seed"
    - path: "tests/test_periods.py"
      provides: "GET /periods/current после onboarding"
    - path: "tests/test_onboarding.py"
      provides: "POST /onboarding/complete + idempotency"
    - path: "tests/test_settings.py"
      provides: "GET/PATCH /settings + валидация"
    - path: "tests/test_telegram_chat_bind.py"
      provides: "POST /internal/telegram/chat-bind upsert + auth"
  key_links:
    - from: "tests/*.py"
      to: "app.services, app.core.period, app.api.routes"
      via: "import (will fail until later plans)"
      pattern: "from app\\."
---

<objective>
Wave 0: создать все тестовые файлы Phase 2 в RED-состоянии. Тесты пишутся против контрактов из 02-CONTEXT.md и 02-RESEARCH.md (модули app.services.*, app.core.period, app.api.routes.* ещё не существуют — импорт падает с ModuleNotFoundError, это ожидаемое RED). Гарантирует, что последующие планы создают код, удовлетворяющий контрактам, а не просто обходят тесты.

Purpose: Nyquist compliance — каждое требование Phase 2 имеет автоматизированный тест ДО имплементации.
Output: 6 тестовых файлов в `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md
@.planning/phases/02-domain-foundation-and-onboarding/02-VALIDATION.md
@tests/conftest.py

<interfaces>
<!-- Контракты, против которых пишутся тесты. Будут созданы в Plan 02-02..02-05. -->

period_for (Plan 02-02):
```python
# app/core/period.py
def period_for(d: date, cycle_start_day: int) -> tuple[date, date]: ...
```

Categories service (Plan 02-03):
```python
# app/services/categories.py
SEED_CATEGORIES: list[tuple[str, str, int]]  # 14 элементов

async def list_categories(db, *, include_archived: bool = False) -> list[Category]: ...
async def create_category(db, *, name: str, kind: CategoryKind, sort_order: int = 0) -> Category: ...
async def update_category(db, category_id: int, **fields) -> Category: ...
async def archive_category(db, category_id: int) -> Category: ...
async def seed_default_categories(db) -> list[Category]: ...  # idempotent
```

API routes (Plan 02-04):
- GET /api/v1/categories?include_archived=<bool>
- POST /api/v1/categories  body: {name, kind, sort_order?}
- PATCH /api/v1/categories/{id}  body: {name?, sort_order?, is_archived?}
- DELETE /api/v1/categories/{id}  → 200 + {archived: true}
- GET /api/v1/periods/current  → активный период (если нет — 404 в Phase 2)
- POST /api/v1/onboarding/complete  body: {starting_balance_cents, cycle_start_day, seed_default_categories}
  → 200 (первый раз) / 409 (повтор)
- GET /api/v1/settings  → {cycle_start_day}
- PATCH /api/v1/settings  body: {cycle_start_day: int 1..28}  → 200 / 422 (out of range)
- POST /api/v1/internal/telegram/chat-bind  body: {tg_user_id, tg_chat_id} + X-Internal-Token

Conftest fixtures (already exist from Phase 1):
- async_client: httpx.AsyncClient через ASGITransport
- bot_token, owner_tg_id, internal_token (string fixtures)
- make_init_data(tg_user_id, bot_token, age_seconds=0): str — генерирует валидный initData

DB-backed test pattern (как в test_migrations.py):
- Self-skip если DATABASE_URL не указывает на тестовый Postgres
- Использовать `os.environ.get("DATABASE_URL")` и `pytest.skip(...)` если не задан
- Иначе override get_db чтобы возвращать рабочую сессию из тестовой БД
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать test_period_engine.py + test_categories.py (unit + integration RED)</name>
  <read_first>
    - .planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md (Pattern 4: period_for + 9 test cases)
    - .planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md (D-16: SEED_CATEGORIES)
    - tests/conftest.py (async_client fixture контракт)
  </read_first>
  <files>tests/test_period_engine.py, tests/test_categories.py</files>
  <behavior>
    - test_period_engine.py: 9+ параметризованных кейсов period_for (HLD §3 примеры + edge: cycle=31 в feb, year-rollover, day==cycle_start, day==cycle_start-1, leap-year)
    - test_categories.py покрывает:
      * test_list_empty: GET /categories → []
      * test_create_category: POST → 200 + body содержит id
      * test_seed_creates_14: POST /onboarding/seed (или внутри onboarding) создаёт ровно 14 категорий
      * test_seed_idempotent: повторный вызов seed возвращает 0 новых, всего по-прежнему 14
      * test_update_renames: PATCH name → 200 + GET показывает новое имя
      * test_archive_hides_from_default_list: DELETE /categories/{id} → GET без include_archived НЕ возвращает её
      * test_include_archived_returns_archived: GET с include_archived=true возвращает архивные
      * test_archived_can_be_unarchived: PATCH is_archived=false → возвращается в активные
  </behavior>
  <action>
Создать `tests/test_period_engine.py`:

```python
"""Unit tests for period_for() — pure date arithmetic (PER-01).

These tests must run without DB access. period_for is a pure function
imported from app.core.period (will be created in Plan 02-02).
"""
from datetime import date

import pytest


# (date, cycle_start_day, expected_period_start, expected_period_end, rationale)
CASES = [
    (date(2026, 2, 15), 5, date(2026, 2, 5), date(2026, 3, 4), "HLD §3 example 1"),
    (date(2026, 2, 3), 5, date(2026, 1, 5), date(2026, 2, 4), "HLD §3 example 2"),
    (date(2026, 2, 5), 5, date(2026, 2, 5), date(2026, 3, 4), "day == cycle_start"),
    (date(2026, 2, 4), 5, date(2026, 1, 5), date(2026, 2, 4), "day == cycle_start - 1"),
    (date(2026, 1, 15), 31, date(2026, 1, 31), date(2026, 2, 27),
     "Jan has 31, Feb 2026 (non-leap) has 28 — clamp"),
    (date(2024, 2, 29), 31, date(2024, 2, 29), date(2024, 3, 30),
     "leap year: Feb 2024 has 29 — clamp"),
    (date(2026, 12, 15), 5, date(2026, 12, 5), date(2027, 1, 4), "year rollover"),
    (date(2026, 1, 3), 5, date(2025, 12, 5), date(2026, 1, 4), "year rollunder"),
    (date(2026, 3, 1), 28, date(2026, 2, 28), date(2026, 3, 27),
     "Feb 2026 doesn't have day 28+ — clamp to 28"),
]


@pytest.mark.parametrize("d,csd,exp_start,exp_end,reason", CASES)
def test_period_for(d, csd, exp_start, exp_end, reason):
    from app.core.period import period_for
    start, end = period_for(d, csd)
    assert start == exp_start, f"{reason}: start mismatch (got {start})"
    assert end == exp_end, f"{reason}: end mismatch (got {end})"


def test_period_for_returns_tuple_of_dates():
    """Sanity: возвращаемые значения — date (не datetime)."""
    from app.core.period import period_for
    start, end = period_for(date(2026, 2, 15), 5)
    assert type(start) is date
    assert type(end) is date


def test_period_for_invariant_end_minus_start_plus_1_equals_month_length():
    """Invariant: длина периода ≈ 28-31 день."""
    from app.core.period import period_for
    start, end = period_for(date(2026, 2, 15), 5)
    delta_days = (end - start).days + 1
    assert 28 <= delta_days <= 31, f"period length {delta_days} out of range"
```

Создать `tests/test_categories.py`:

```python
"""Integration tests for Categories CRUD + seed (CAT-01, CAT-02, CAT-03).

DB-backed: requires DATABASE_URL pointing to a test Postgres database
with `alembic upgrade head` applied. Skipped via `pytest.skip` otherwise
(self-skip pattern from test_migrations.py).
"""
import os

import pytest


def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")


@pytest.fixture
async def auth_headers(bot_token, owner_tg_id):
    """Helper: returns headers dict with X-Telegram-Init-Data for owner."""
    from tests.conftest import make_init_data
    return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}


@pytest.fixture
async def db_client(async_client, bot_token, owner_tg_id):
    """async_client + a real DB session injected via dependency_overrides.

    Truncates relevant tables before yielding to ensure clean state.
    """
    _require_db()
    from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
    from sqlalchemy import text
    from app.api.dependencies import get_db
    from app.main_api import app

    db_url = os.environ["DATABASE_URL"]
    engine = create_async_engine(db_url, echo=False)
    SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

    # Clean state
    async with engine.begin() as conn:
        await conn.execute(text("TRUNCATE TABLE category, planned_transaction, "
                               "actual_transaction, plan_template_item, subscription, "
                               "budget_period, app_user RESTART IDENTITY CASCADE"))

    async def real_get_db():
        async with SessionLocal() as session:
            try:
                yield session
                await session.commit()
            except Exception:
                await session.rollback()
                raise

    app.dependency_overrides[get_db] = real_get_db
    yield async_client
    await engine.dispose()


@pytest.mark.asyncio
async def test_list_empty(db_client, auth_headers):
    response = await db_client.get("/api/v1/categories", headers=auth_headers)
    assert response.status_code == 200
    assert response.json() == []


@pytest.mark.asyncio
async def test_create_category(db_client, auth_headers):
    response = await db_client.post(
        "/api/v1/categories",
        json={"name": "Спорт", "kind": "expense", "sort_order": 50},
        headers=auth_headers,
    )
    assert response.status_code in (200, 201)
    data = response.json()
    assert data["name"] == "Спорт"
    assert data["kind"] == "expense"
    assert data["is_archived"] is False
    assert "id" in data


@pytest.mark.asyncio
async def test_create_then_list(db_client, auth_headers):
    await db_client.post(
        "/api/v1/categories",
        json={"name": "Хобби", "kind": "expense"},
        headers=auth_headers,
    )
    response = await db_client.get("/api/v1/categories", headers=auth_headers)
    assert response.status_code == 200
    items = response.json()
    assert len(items) == 1
    assert items[0]["name"] == "Хобби"


@pytest.mark.asyncio
async def test_update_renames(db_client, auth_headers):
    create = await db_client.post(
        "/api/v1/categories",
        json={"name": "Старое", "kind": "expense"},
        headers=auth_headers,
    )
    cat_id = create.json()["id"]
    update = await db_client.patch(
        f"/api/v1/categories/{cat_id}",
        json={"name": "Новое"},
        headers=auth_headers,
    )
    assert update.status_code == 200
    assert update.json()["name"] == "Новое"


@pytest.mark.asyncio
async def test_archive_hides_from_default_list(db_client, auth_headers):
    create = await db_client.post(
        "/api/v1/categories",
        json={"name": "Удалить", "kind": "expense"},
        headers=auth_headers,
    )
    cat_id = create.json()["id"]
    delete = await db_client.delete(f"/api/v1/categories/{cat_id}", headers=auth_headers)
    assert delete.status_code == 200

    listing = await db_client.get("/api/v1/categories", headers=auth_headers)
    assert listing.status_code == 200
    assert len(listing.json()) == 0


@pytest.mark.asyncio
async def test_include_archived_returns_archived(db_client, auth_headers):
    create = await db_client.post(
        "/api/v1/categories",
        json={"name": "Архивная", "kind": "expense"},
        headers=auth_headers,
    )
    cat_id = create.json()["id"]
    await db_client.delete(f"/api/v1/categories/{cat_id}", headers=auth_headers)

    listing = await db_client.get(
        "/api/v1/categories?include_archived=true",
        headers=auth_headers,
    )
    assert listing.status_code == 200
    items = listing.json()
    assert len(items) == 1
    assert items[0]["is_archived"] is True


@pytest.mark.asyncio
async def test_archived_can_be_unarchived(db_client, auth_headers):
    create = await db_client.post(
        "/api/v1/categories",
        json={"name": "Восстановимая", "kind": "expense"},
        headers=auth_headers,
    )
    cat_id = create.json()["id"]
    await db_client.delete(f"/api/v1/categories/{cat_id}", headers=auth_headers)

    restore = await db_client.patch(
        f"/api/v1/categories/{cat_id}",
        json={"is_archived": False},
        headers=auth_headers,
    )
    assert restore.status_code == 200
    assert restore.json()["is_archived"] is False


@pytest.mark.asyncio
async def test_seed_creates_14_categories(db_client, auth_headers):
    """CAT-03: seed via /onboarding/complete (with seed_default_categories=true)."""
    response = await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 0,
            "cycle_start_day": 5,
            "seed_default_categories": True,
        },
        headers=auth_headers,
    )
    assert response.status_code == 200
    listing = await db_client.get("/api/v1/categories", headers=auth_headers)
    assert len(listing.json()) == 14


@pytest.mark.asyncio
async def test_seed_idempotent_skips_when_categories_exist(db_client, auth_headers):
    """Если уже есть хоть одна категория, seed не добавляет новых."""
    # Manually create one category first
    await db_client.post(
        "/api/v1/categories",
        json={"name": "Existing", "kind": "expense"},
        headers=auth_headers,
    )
    # Now run onboarding with seed=true
    await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 0,
            "cycle_start_day": 5,
            "seed_default_categories": True,
        },
        headers=auth_headers,
    )
    listing = await db_client.get("/api/v1/categories", headers=auth_headers)
    # Should remain at 1 (seed skipped because >= 1 existed)
    assert len(listing.json()) == 1
```

Примечание: `db_client` fixture использует TRUNCATE — это требует postgres user с TRUNCATE правами (стандартно есть). Если тесты запускаются параллельно — нужны схемы или disable parallel run; для Phase 2 достаточно последовательного запуска.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && for f in tests/test_period_engine.py tests/test_categories.py; do python3 -c "import ast; ast.parse(open('$f').read()); print('$f syntax OK')"; done 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_period_engine.py существует с >= 9 параметризованными кейсами: `grep -c "date(" tests/test_period_engine.py` >= 27 (3 даты на кейс × 9 кейсов)
    - tests/test_categories.py содержит 9+ функций test_*: `grep -c "^async def test_\|^def test_" tests/test_categories.py` >= 9
    - test_categories.py использует self-skip pattern: `grep -c "DATABASE_URL" tests/test_categories.py` >= 1
    - test_categories.py покрывает archive/unarchive: `grep -c "is_archived" tests/test_categories.py` >= 3
    - test_categories.py покрывает seed: `grep -c "seed_default_categories" tests/test_categories.py` >= 2
    - Оба файла синтаксически корректны (ast.parse)
  </acceptance_criteria>
  <done>tests/test_period_engine.py и tests/test_categories.py созданы, RED-состояние (импорты падают на app.core.period / app.api.routes), синтаксис OK, покрывают PER-01, CAT-01, CAT-02, CAT-03</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать test_periods.py, test_onboarding.py, test_settings.py, test_telegram_chat_bind.py (RED)</name>
  <read_first>
    - tests/test_categories.py (db_client fixture pattern — переиспользуем стиль)
    - .planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md (Pattern 7: idempotency, Pitfall 4: atomicity)
    - .planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md (D-09, D-10, D-11)
    - docs/HLD.md §4.1, §4.4, §4.9, §4.10 (API контракты)
  </read_first>
  <files>tests/test_periods.py, tests/test_onboarding.py, tests/test_settings.py, tests/test_telegram_chat_bind.py</files>
  <behavior>
    - test_periods.py: GET /periods/current до onboarding → 404; после onboarding → 200 + период
    - test_onboarding.py:
      * test_complete_creates_period_and_seeds: POST /onboarding/complete → 200, GET /me показывает onboarded_at != null, GET /periods/current возвращает период с правильным starting_balance, GET /categories возвращает 14
      * test_repeat_complete_returns_409: повторный POST → 409
      * test_no_seed_when_flag_false: seed_default_categories=false → 0 категорий, но period создан
      * test_invalid_cycle_start_day_422: cycle_start_day=0 или 29 → 422
    - test_settings.py:
      * test_get_settings_returns_cycle_day: GET /settings → {cycle_start_day: 5}
      * test_patch_updates_cycle_day: PATCH cycle_start_day=10 → 200, GET показывает 10
      * test_invalid_cycle_day_422: PATCH с 0/29/-1 → 422
      * test_patch_does_not_recompute_existing_period: создать период (cycle=5), потом PATCH на 10, проверить что period_start/period_end не изменились
    - test_telegram_chat_bind.py:
      * test_bind_creates_user_with_chat_id: POST с правильным X-Internal-Token → 200, и в БД появляется AppUser с tg_chat_id
      * test_bind_updates_existing_chat_id: повторный POST с другим chat_id → обновляет
      * test_bind_without_token_403: POST без X-Internal-Token → 403
      * test_bind_with_wrong_token_403: → 403
  </behavior>
  <action>
Создать `tests/test_periods.py`:

```python
"""Integration tests for Periods endpoints (PER-01, PER-02)."""
import os

import pytest


def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")


@pytest.fixture
async def auth_headers(bot_token, owner_tg_id):
    from tests.conftest import make_init_data
    return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}


@pytest.fixture
async def db_client(async_client):
    _require_db()
    from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
    from sqlalchemy import text
    from app.api.dependencies import get_db
    from app.main_api import app

    engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
    SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

    async with engine.begin() as conn:
        await conn.execute(text(
            "TRUNCATE TABLE category, planned_transaction, actual_transaction, "
            "plan_template_item, subscription, budget_period, app_user "
            "RESTART IDENTITY CASCADE"
        ))

    async def real_get_db():
        async with SessionLocal() as session:
            try:
                yield session
                await session.commit()
            except Exception:
                await session.rollback()
                raise

    app.dependency_overrides[get_db] = real_get_db
    yield async_client
    await engine.dispose()


@pytest.mark.asyncio
async def test_periods_current_before_onboarding_is_404(db_client, auth_headers):
    """До onboarding активного периода нет."""
    # First trigger /me to create app_user row (Phase 1 D-11 upsert pattern)
    await db_client.get("/api/v1/me", headers=auth_headers)
    response = await db_client.get("/api/v1/periods/current", headers=auth_headers)
    assert response.status_code == 404


@pytest.mark.asyncio
async def test_periods_current_after_onboarding_returns_period(db_client, auth_headers):
    """После /onboarding/complete есть активный период."""
    onboard = await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 1500000,  # 15 000 ₽
            "cycle_start_day": 5,
            "seed_default_categories": False,
        },
        headers=auth_headers,
    )
    assert onboard.status_code == 200

    response = await db_client.get("/api/v1/periods/current", headers=auth_headers)
    assert response.status_code == 200
    period = response.json()
    assert period["starting_balance_cents"] == 1500000
    assert period["status"] == "active"
    assert "period_start" in period
    assert "period_end" in period
```

Создать `tests/test_onboarding.py`:

```python
"""Integration tests for Onboarding (ONB-01, PER-02, PER-03, CAT-03, atomicity)."""
import os

import pytest


def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")


@pytest.fixture
async def auth_headers(bot_token, owner_tg_id):
    from tests.conftest import make_init_data
    return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}


@pytest.fixture
async def db_client(async_client):
    _require_db()
    from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
    from sqlalchemy import text
    from app.api.dependencies import get_db
    from app.main_api import app

    engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
    SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

    async with engine.begin() as conn:
        await conn.execute(text(
            "TRUNCATE TABLE category, planned_transaction, actual_transaction, "
            "plan_template_item, subscription, budget_period, app_user "
            "RESTART IDENTITY CASCADE"
        ))

    async def real_get_db():
        async with SessionLocal() as session:
            try:
                yield session
                await session.commit()
            except Exception:
                await session.rollback()
                raise

    app.dependency_overrides[get_db] = real_get_db
    yield async_client
    await engine.dispose()


@pytest.mark.asyncio
async def test_complete_creates_period_and_seeds_categories(db_client, auth_headers):
    """ONB-01 / PER-02 / CAT-03: complete creates period + 14 categories + sets onboarded_at."""
    response = await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 1000000,
            "cycle_start_day": 5,
            "seed_default_categories": True,
        },
        headers=auth_headers,
    )
    assert response.status_code == 200

    me = await db_client.get("/api/v1/me", headers=auth_headers)
    assert me.json()["onboarded_at"] is not None

    period = await db_client.get("/api/v1/periods/current", headers=auth_headers)
    assert period.status_code == 200
    assert period.json()["starting_balance_cents"] == 1000000

    cats = await db_client.get("/api/v1/categories", headers=auth_headers)
    assert len(cats.json()) == 14


@pytest.mark.asyncio
async def test_repeat_complete_returns_409(db_client, auth_headers):
    """D-10: повторный POST → 409 Conflict."""
    body = {
        "starting_balance_cents": 0,
        "cycle_start_day": 5,
        "seed_default_categories": False,
    }
    first = await db_client.post(
        "/api/v1/onboarding/complete", json=body, headers=auth_headers
    )
    assert first.status_code == 200
    second = await db_client.post(
        "/api/v1/onboarding/complete", json=body, headers=auth_headers
    )
    assert second.status_code == 409


@pytest.mark.asyncio
async def test_no_seed_when_flag_false(db_client, auth_headers):
    """seed_default_categories=false → period создан, категории НЕ создаются."""
    response = await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 0,
            "cycle_start_day": 5,
            "seed_default_categories": False,
        },
        headers=auth_headers,
    )
    assert response.status_code == 200
    cats = await db_client.get("/api/v1/categories", headers=auth_headers)
    assert len(cats.json()) == 0
    period = await db_client.get("/api/v1/periods/current", headers=auth_headers)
    assert period.status_code == 200


@pytest.mark.asyncio
@pytest.mark.parametrize("invalid_day", [0, 29, 30, 31, -1])
async def test_invalid_cycle_start_day_422(db_client, auth_headers, invalid_day):
    """Pydantic Field(ge=1, le=28) → 422 на out-of-range."""
    response = await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 0,
            "cycle_start_day": invalid_day,
            "seed_default_categories": False,
        },
        headers=auth_headers,
    )
    assert response.status_code == 422


@pytest.mark.asyncio
async def test_negative_starting_balance_allowed(db_client, auth_headers):
    """Отрицательный balance = долг, разрешено (D-09)."""
    response = await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": -50000,
            "cycle_start_day": 5,
            "seed_default_categories": False,
        },
        headers=auth_headers,
    )
    assert response.status_code == 200
```

Создать `tests/test_settings.py`:

```python
"""Integration tests for Settings (SET-01, PER-01)."""
import os

import pytest


def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")


@pytest.fixture
async def auth_headers(bot_token, owner_tg_id):
    from tests.conftest import make_init_data
    return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}


@pytest.fixture
async def db_client(async_client):
    _require_db()
    from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
    from sqlalchemy import text
    from app.api.dependencies import get_db
    from app.main_api import app

    engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
    SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

    async with engine.begin() as conn:
        await conn.execute(text(
            "TRUNCATE TABLE category, planned_transaction, actual_transaction, "
            "plan_template_item, subscription, budget_period, app_user "
            "RESTART IDENTITY CASCADE"
        ))

    async def real_get_db():
        async with SessionLocal() as session:
            try:
                yield session
                await session.commit()
            except Exception:
                await session.rollback()
                raise

    app.dependency_overrides[get_db] = real_get_db
    yield async_client
    await engine.dispose()


@pytest.mark.asyncio
async def test_get_settings_default(db_client, auth_headers):
    """До изменений: cycle_start_day=5 (default из app_user)."""
    # Trigger /me to create app_user
    await db_client.get("/api/v1/me", headers=auth_headers)
    response = await db_client.get("/api/v1/settings", headers=auth_headers)
    assert response.status_code == 200
    assert response.json()["cycle_start_day"] == 5


@pytest.mark.asyncio
async def test_patch_updates_cycle_day(db_client, auth_headers):
    """PATCH /settings → читается обратно через GET."""
    await db_client.get("/api/v1/me", headers=auth_headers)
    patch = await db_client.patch(
        "/api/v1/settings",
        json={"cycle_start_day": 10},
        headers=auth_headers,
    )
    assert patch.status_code == 200
    get = await db_client.get("/api/v1/settings", headers=auth_headers)
    assert get.json()["cycle_start_day"] == 10


@pytest.mark.asyncio
@pytest.mark.parametrize("invalid_day", [0, 29, -1, 100])
async def test_invalid_cycle_day_422(db_client, auth_headers, invalid_day):
    """Field(ge=1, le=28) → 422 для out-of-range."""
    await db_client.get("/api/v1/me", headers=auth_headers)
    response = await db_client.patch(
        "/api/v1/settings",
        json={"cycle_start_day": invalid_day},
        headers=auth_headers,
    )
    assert response.status_code == 422


@pytest.mark.asyncio
async def test_patch_does_not_recompute_existing_period(db_client, auth_headers):
    """SET-01: изменение cycle_start_day не пересчитывает прошлые/текущие периоды."""
    # 1. Onboarding с cycle_start_day=5 — создаёт период
    await db_client.post(
        "/api/v1/onboarding/complete",
        json={
            "starting_balance_cents": 0,
            "cycle_start_day": 5,
            "seed_default_categories": False,
        },
        headers=auth_headers,
    )
    period_before = (await db_client.get(
        "/api/v1/periods/current", headers=auth_headers
    )).json()

    # 2. Сменить cycle_start_day на 10
    await db_client.patch(
        "/api/v1/settings",
        json={"cycle_start_day": 10},
        headers=auth_headers,
    )

    # 3. Текущий период остался с теми же датами
    period_after = (await db_client.get(
        "/api/v1/periods/current", headers=auth_headers
    )).json()
    assert period_before["period_start"] == period_after["period_start"]
    assert period_before["period_end"] == period_after["period_end"]
```

Создать `tests/test_telegram_chat_bind.py`:

```python
"""Integration tests for Internal /telegram/chat-bind endpoint (ONB-03)."""
import os

import pytest


def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")


@pytest.fixture
async def db_client(async_client):
    _require_db()
    from sqlalchemy.ext.asyncio import create_async_engine, async_sessionmaker
    from sqlalchemy import text
    from app.api.dependencies import get_db
    from app.main_api import app

    engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
    SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

    async with engine.begin() as conn:
        await conn.execute(text(
            "TRUNCATE TABLE category, planned_transaction, actual_transaction, "
            "plan_template_item, subscription, budget_period, app_user "
            "RESTART IDENTITY CASCADE"
        ))

    async def real_get_db():
        async with SessionLocal() as session:
            try:
                yield session
                await session.commit()
            except Exception:
                await session.rollback()
                raise

    app.dependency_overrides[get_db] = real_get_db
    yield async_client
    await engine.dispose()


@pytest.mark.asyncio
async def test_chat_bind_without_internal_token_403(db_client, owner_tg_id):
    """INF-04: без X-Internal-Token → 403."""
    response = await db_client.post(
        "/api/v1/internal/telegram/chat-bind",
        json={"tg_user_id": owner_tg_id, "tg_chat_id": owner_tg_id},
    )
    assert response.status_code == 403


@pytest.mark.asyncio
async def test_chat_bind_with_wrong_token_403(db_client, owner_tg_id):
    """С неправильным X-Internal-Token → 403."""
    response = await db_client.post(
        "/api/v1/internal/telegram/chat-bind",
        json={"tg_user_id": owner_tg_id, "tg_chat_id": owner_tg_id},
        headers={"X-Internal-Token": "wrong_token"},
    )
    assert response.status_code == 403


@pytest.mark.asyncio
async def test_chat_bind_creates_user_with_chat_id(
    db_client, owner_tg_id, internal_token
):
    """Если AppUser ещё не существует — upsert создаёт строку с tg_chat_id."""
    response = await db_client.post(
        "/api/v1/internal/telegram/chat-bind",
        json={"tg_user_id": owner_tg_id, "tg_chat_id": 555},
        headers={"X-Internal-Token": internal_token},
    )
    assert response.status_code == 200

    # Verify via /me (the OWNER initData path will see the populated tg_chat_id)
    from tests.conftest import make_init_data
    init_data = make_init_data(owner_tg_id, "1234567890:test_bot_token_for_testing_only")
    me = await db_client.get(
        "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
    )
    assert me.json()["chat_id_known"] is True
    assert me.json()["tg_chat_id"] == 555


@pytest.mark.asyncio
async def test_chat_bind_updates_existing_chat_id(
    db_client, owner_tg_id, internal_token
):
    """Повторный bind с другим chat_id обновляет."""
    await db_client.post(
        "/api/v1/internal/telegram/chat-bind",
        json={"tg_user_id": owner_tg_id, "tg_chat_id": 100},
        headers={"X-Internal-Token": internal_token},
    )
    response = await db_client.post(
        "/api/v1/internal/telegram/chat-bind",
        json={"tg_user_id": owner_tg_id, "tg_chat_id": 200},
        headers={"X-Internal-Token": internal_token},
    )
    assert response.status_code == 200

    from tests.conftest import make_init_data
    init_data = make_init_data(owner_tg_id, "1234567890:test_bot_token_for_testing_only")
    me = await db_client.get(
        "/api/v1/me", headers={"X-Telegram-Init-Data": init_data}
    )
    assert me.json()["tg_chat_id"] == 200
```

Все 4 файла используют общий паттерн self-skip + db_client fixture для изоляции. **Замечание:** db_client fixture определён в каждом тестовом файле локально (нет shared в conftest), чтобы минимизировать изменения conftest и избежать риска регрессии Phase 1 тестов. Если потом захочется DRY — можно вынести в conftest позже.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && for f in tests/test_periods.py tests/test_onboarding.py tests/test_settings.py tests/test_telegram_chat_bind.py; do python3 -c "import ast; ast.parse(open('$f').read()); print('$f syntax OK')"; done 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_periods.py содержит 2+ функций test_*: `grep -c "^async def test_" tests/test_periods.py` >= 2
    - tests/test_onboarding.py содержит 5+ функций test_*: `grep -c "^async def test_" tests/test_onboarding.py` >= 5
    - test_onboarding.py покрывает 409: `grep -c "409" tests/test_onboarding.py` >= 1
    - test_onboarding.py использует parametrize для invalid days: `grep -c "parametrize" tests/test_onboarding.py` >= 1
    - tests/test_settings.py содержит 4+ функций: `grep -c "^async def test_" tests/test_settings.py` >= 4
    - test_settings.py покрывает PATCH-без-перепресчёта: `grep -c "test_patch_does_not_recompute" tests/test_settings.py` == 1
    - tests/test_telegram_chat_bind.py содержит 4 функций: `grep -c "^async def test_" tests/test_telegram_chat_bind.py` == 4
    - test_telegram_chat_bind.py покрывает 403 без токена: `grep -c "without_internal_token_403\|wrong_token_403" tests/test_telegram_chat_bind.py` >= 2
    - Все 4 файла синтаксически корректны (ast.parse не выбрасывает ошибок)
  </acceptance_criteria>
  <done>4 тестовых файла созданы в RED-состоянии (импорты падают), синтаксис корректен, покрывают PER-01, PER-02, PER-05, ONB-01, ONB-03, SET-01 + threat checks (T-double-onboard, T-cycle-validation, T-internal-token, T-chatbind-spoof)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app | Тесты инжектируют ENV и dependency_overrides; в prod ENV приходит из .env |
| test → DB | DB-backed тесты делают TRUNCATE — изолированная тестовая БД обязательна |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-w0-01 | Tampering | tests/ | accept | Тесты в RED-состоянии не выполняют опасных мутаций (TRUNCATE — на тестовой БД, ENV inject — фиктивные значения). Реальная prod БД защищена тем, что DATABASE_URL указывает на test (другая БД), и self-skip срабатывает если переменная не задана |
| T-w0-02 | Info Disclosure | tests/conftest.py инжекшн ENV | accept | bot_token/owner_tg_id/internal_token — фиктивные значения для тестов (см. tests/conftest.py — Phase 1 уже верифицировано) |
</threat_model>

<verification>
Wave 0 завершён когда:
1. `python3 -c "import ast; ast.parse(open('tests/test_period_engine.py').read())"` — OK
2. Все 6 тестовых файлов существуют и синтаксически корректны (см. acceptance_criteria каждой задачи)
3. `uv run pytest tests/ --collect-only` падает на ImportError для новых файлов (ожидаемое RED)
4. Существующие Phase 1 тесты НЕ затронуты (`uv run pytest tests/test_auth.py tests/test_health.py tests/test_internal_auth.py -x` всё ещё green в подходящем окружении)
</verification>

<success_criteria>
- 6 файлов созданы: test_period_engine.py, test_categories.py, test_periods.py, test_onboarding.py, test_settings.py, test_telegram_chat_bind.py
- Все файлы синтаксически корректны
- Покрыты все 11 requirements Phase 2 (через behavioral expectations)
- Тесты используют self-skip для DB-backed integration tests
- Phase 1 тесты не затронуты
</success_criteria>

<output>
После завершения создать `.planning/phases/02-domain-foundation-and-onboarding/02-01-SUMMARY.md`
</output>
