---
phase: 02-domain-foundation-and-onboarding
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - pyproject.toml
  - app/core/period.py
  - app/core/settings.py
  - app/api/schemas/__init__.py
  - app/api/schemas/categories.py
  - app/api/schemas/periods.py
  - app/api/schemas/onboarding.py
  - app/api/schemas/settings.py
  - app/api/schemas/telegram.py
autonomous: true
requirements:
  - PER-01
  - PER-02
  - CAT-01
  - SET-01
  - ONB-01
  - ONB-03

must_haves:
  truths:
    - "period_for(date, cycle_start_day) — чистая функция, корректная на 9 параметризованных кейсах"
    - "python-dateutil добавлен в dependencies; uv lock обновлён"
    - "MINI_APP_URL добавлен в Settings c default 'https://localhost'"
    - "Pydantic v2 схемы определены для категорий, периодов, onboarding, settings, telegram chat-bind"
    - "test_period_engine.py становится GREEN после применения этого плана"
  artifacts:
    - path: "app/core/period.py"
      provides: "period_for() — pure function"
      exports: ["period_for"]
    - path: "app/api/schemas/categories.py"
      provides: "CategoryCreate, CategoryUpdate, CategoryRead"
    - path: "app/api/schemas/onboarding.py"
      provides: "OnboardingCompleteRequest, OnboardingCompleteResponse"
    - path: "app/api/schemas/periods.py"
      provides: "PeriodRead"
    - path: "app/api/schemas/settings.py"
      provides: "SettingsRead, SettingsUpdate (с Field(ge=1, le=28))"
    - path: "app/api/schemas/telegram.py"
      provides: "ChatBindRequest"
  key_links:
    - from: "app/core/period.py"
      to: "dateutil.relativedelta"
      via: "import"
      pattern: "from dateutil"
    - from: "app/api/schemas/onboarding.py"
      to: "pydantic.Field(ge=1, le=28)"
      via: "validator"
      pattern: "Field\\(ge=1.*le=28"
---

<objective>
Wave 1 (foundation, no DB layer touched): добавить зависимость `python-dateutil` и `httpx` (prod), реализовать чистую функцию `period_for`, и определить все Pydantic v2 request/response схемы для Phase 2 endpoint-ов. Этот плана НЕ трогает БД и НЕ создаёт routes — только pure logic + контракты данных.

Purpose: Изолированный, легко тестируемый фундамент для последующих сервисов и роутов. После этого плана `test_period_engine.py` становится GREEN.

Output: 1 utility-модуль (period.py), 5 schema-файлов, обновлённые pyproject.toml + settings.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md
@docs/HLD.md
@app/db/models.py
@app/core/settings.py
@pyproject.toml

<interfaces>
<!-- Существующие модули (Phase 1) — используем без изменений -->

ORM модели (app/db/models.py):
```python
class CategoryKind(str, enum.Enum):
    expense = "expense"
    income = "income"

class PeriodStatus(str, enum.Enum):
    active = "active"
    closed = "closed"

class Category(Base):
    id: int; name: str; kind: CategoryKind; is_archived: bool; sort_order: int; created_at: datetime

class BudgetPeriod(Base):
    id: int; period_start: date; period_end: date
    starting_balance_cents: int; ending_balance_cents: Optional[int]
    status: PeriodStatus; closed_at: Optional[datetime]

class AppUser(Base):
    id: int; tg_user_id: int; tg_chat_id: Optional[int]
    cycle_start_day: int = 5; onboarded_at: Optional[datetime]; created_at: datetime
```

Existing settings (app/core/settings.py):
```python
class Settings(BaseSettings):
    DATABASE_URL: str = ...; DATABASE_URL_SYNC: str = ...
    BOT_TOKEN: str; OWNER_TG_ID: int; INTERNAL_TOKEN: str
    API_BASE_URL: str = "http://api:8000"
    PUBLIC_DOMAIN: str = "localhost"
    DEV_MODE: bool = False; LOG_LEVEL: str = "INFO"; LOG_FORMAT: str = "json"
    APP_TZ: str = "Europe/Moscow"
```

<!-- Новые контракты, создаваемые этим планом -->

period_for (Pattern 4 из 02-RESEARCH.md):
```python
def period_for(d: date, cycle_start_day: int) -> tuple[date, date]
```

Schemas:
- CategoryCreate { name: str (1..200), kind: 'expense'|'income', sort_order: int = 0 }
- CategoryUpdate { name?: str, sort_order?: int, is_archived?: bool } — все опциональны
- CategoryRead { id, name, kind, is_archived, sort_order, created_at: datetime }
- PeriodRead { id, period_start: date, period_end: date, starting_balance_cents: int, ending_balance_cents: int|null, status: str, closed_at: datetime|null }
- OnboardingCompleteRequest { starting_balance_cents: int, cycle_start_day: int (1..28), seed_default_categories: bool }
- OnboardingCompleteResponse { period_id: int, seeded_categories: int, onboarded_at: datetime }
- SettingsRead { cycle_start_day: int }
- SettingsUpdate { cycle_start_day: int (1..28) }
- ChatBindRequest { tg_user_id: int, tg_chat_id: int }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Добавить python-dateutil + httpx (prod), MINI_APP_URL в Settings, реализовать app/core/period.py</name>
  <read_first>
    - .planning/phases/02-domain-foundation-and-onboarding/02-RESEARCH.md (Pattern 4: period_for reference impl + 9 test cases)
    - tests/test_period_engine.py (целевые кейсы)
    - app/core/settings.py (текущая структура)
    - pyproject.toml (текущие deps)
  </read_first>
  <files>pyproject.toml, app/core/settings.py, app/core/period.py</files>
  <behavior>
    - pyproject.toml: добавлен `python-dateutil==2.9.0.post0` (или ближайшая стабильная) в `[project] dependencies`; `httpx==0.28.1` промотирован из dev в prod (нужен в bot/api_client.py из Plan 02-05)
    - app/core/settings.py: добавлено поле `MINI_APP_URL: str = "https://localhost"` (default; в prod из .env)
    - app/core/period.py: реализует `period_for(d: date, cycle_start_day: int) -> tuple[date, date]` per Pattern 4 из 02-RESEARCH.md
    - period.py содержит docstring с примерами + ссылками на HLD §3
    - period.py использует `dateutil.relativedelta` и `calendar.monthrange`
    - test_period_engine.py становится GREEN: `uv run pytest tests/test_period_engine.py -x -q` проходит все 9 кейсов
  </behavior>
  <action>
**1. Обновить pyproject.toml:**

Добавить в `[project] dependencies` две новые строки (сохранив сортировку):
- `"python-dateutil==2.9.0.post0",` (после `"pydantic-settings==..."`)
- `"httpx==0.28.1",` (промоция из dev — оставить также в `[project.optional-dependencies] dev` и `[tool.uv] dev-dependencies` для тестового раздела)

Запустить `uv lock` если доступен, иначе зафиксировать вручную; в Docker все равно будет `uv sync --locked`.

**2. Обновить app/core/settings.py:**

Добавить новое поле `MINI_APP_URL: str = "https://localhost"` после `PUBLIC_DOMAIN`. Добавить также `BOT_USERNAME: str = "tg_budget_planner_bot"` (используется бот-handler для отображения имени, опционально). Финальная структура:

```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    DATABASE_URL: str = "postgresql+asyncpg://budget:budget@db:5432/budget_db"
    DATABASE_URL_SYNC: str = "postgresql://budget:budget@db:5432/budget_db"

    # Telegram
    BOT_TOKEN: str = "changeme"
    BOT_USERNAME: str = "tg_budget_planner_bot"
    OWNER_TG_ID: int = 0

    # Internal API protection
    INTERNAL_TOKEN: str = "changeme"
    API_BASE_URL: str = "http://api:8000"

    # Public hosting / Caddy
    PUBLIC_DOMAIN: str = "localhost"
    MINI_APP_URL: str = "https://localhost"

    # Dev / observability
    DEV_MODE: bool = False
    LOG_LEVEL: str = "INFO"
    LOG_FORMAT: str = "json"
    APP_TZ: str = "Europe/Moscow"


settings = Settings()
```

Также обновить `.env.example` — добавить:
```
# Mini App public URL (used by bot to render WebApp button on /start)
MINI_APP_URL=https://your-domain.example.com

# Bot username (без @) — для отображения в UI и deep-links
BOT_USERNAME=tg_budget_planner_bot
```
(вставить рядом с PUBLIC_DOMAIN; не помещать boilerplate комментарий в .env.example, если он уже структурирован).

**3. Создать app/core/period.py:**

```python
"""Period engine: pure function `period_for(date, cycle_start_day) -> (start, end)`.

Reference: docs/HLD.md §3.

Examples (cycle_start_day=5):
  date=2026-02-15 → (2026-02-05, 2026-03-04)
  date=2026-02-03 → (2026-01-05, 2026-02-04)
  date=2026-02-05 → (2026-02-05, 2026-03-04)  # day == cycle_start_day = start

Edge: if cycle_start_day > last_day_of(month) (e.g. 31 in February),
clamp to last day of month per HLD §3 contract.
"""
from calendar import monthrange
from datetime import date, timedelta

from dateutil.relativedelta import relativedelta


def _clamp_day_to_month(year: int, month: int, day: int) -> int:
    """Return min(day, last_day_of_month).

    Allows callers to pass cycle_start_day=31 and have it auto-fit Feb (28/29).
    """
    last = monthrange(year, month)[1]
    return min(day, last)


def period_for(d: date, cycle_start_day: int) -> tuple[date, date]:
    """Compute (period_start, period_end) for the period containing `d`.

    Args:
        d: Any date — function returns the period that includes this date.
        cycle_start_day: 1..28 (validated upstream by Pydantic). Values 29..31
            are tolerated via month-day clamping but not officially supported.

    Returns:
        (period_start, period_end) — both inclusive, both `date` objects.

    Raises:
        ValueError: if cycle_start_day < 1 (defensive; upstream validation
            should reject this earlier).
    """
    if cycle_start_day < 1:
        raise ValueError(f"cycle_start_day must be >= 1, got {cycle_start_day}")

    # Step 1: figure out which month's "cycle_start_day" this date belongs to.
    # The current month's start-day, possibly clamped if cycle > 28.
    cur_clamped = _clamp_day_to_month(d.year, d.month, cycle_start_day)

    if d.day >= cur_clamped:
        # `d` is on/after this month's cycle start — it's within the
        # period that started this month.
        ps_year, ps_month = d.year, d.month
    else:
        # `d` is before this month's cycle start — it belongs to the
        # period that started the previous month.
        prev = d - relativedelta(months=1)
        ps_year, ps_month = prev.year, prev.month

    period_start_day = _clamp_day_to_month(ps_year, ps_month, cycle_start_day)
    period_start = date(ps_year, ps_month, period_start_day)

    # period_end = (period_start + 1 month, clamped) - 1 day
    next_anchor = period_start + relativedelta(months=1)
    next_day = _clamp_day_to_month(next_anchor.year, next_anchor.month, cycle_start_day)
    period_end = date(next_anchor.year, next_anchor.month, next_day) - timedelta(days=1)

    return period_start, period_end
```

**Validation steps after writing:**

```bash
# Установить deps и запустить unit-тесты period_engine
uv sync
uv run pytest tests/test_period_engine.py -v
```

Все 9 параметризованных кейсов + 2 sanity-теста должны быть GREEN.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python3 -c "import ast; ast.parse(open('app/core/period.py').read()); print('period.py syntax OK')" && grep -c "python-dateutil" pyproject.toml && grep -c "MINI_APP_URL" app/core/settings.py 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - app/core/period.py существует и синтаксически корректен (ast.parse)
    - period.py определяет функцию period_for: `grep -c "^def period_for" app/core/period.py` == 1
    - period.py использует dateutil: `grep -c "dateutil" app/core/period.py` >= 1
    - pyproject.toml содержит python-dateutil: `grep -c "python-dateutil" pyproject.toml` >= 1
    - pyproject.toml содержит httpx в dependencies (не только dev): первая встреча `httpx` в файле — в `[project] dependencies`. Можно проверить: `awk '/\[project\.optional-dependencies\]/{exit} /httpx/{print NR": "$0}' pyproject.toml` — должна быть >=1 строка
    - app/core/settings.py содержит MINI_APP_URL: `grep -c "MINI_APP_URL" app/core/settings.py` == 1
    - .env.example содержит MINI_APP_URL placeholder: `grep -c "MINI_APP_URL" .env.example` == 1
    - **GREEN тест:** `uv run pytest tests/test_period_engine.py -x -q` проходит без ошибок (все 9 параметризованных кейсов + 2 sanity-теста)
  </acceptance_criteria>
  <done>python-dateutil установлен; period_for реализован per Pattern 4; test_period_engine.py GREEN; MINI_APP_URL в settings; httpx в prod-deps</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать app/api/schemas/ — Pydantic v2 схемы для всех новых endpoint-ов</name>
  <read_first>
    - app/db/models.py (CategoryKind, PeriodStatus enum-имена)
    - .planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md (D-05, D-09, D-17)
    - docs/HLD.md §4.1, §4.2, §4.4, §4.9, §4.10 (поля API)
    - tests/test_categories.py (json bodies, ожидаемые поля response)
    - tests/test_onboarding.py (ожидаемая 422 на cycle_start_day=29)
  </read_first>
  <files>app/api/schemas/__init__.py, app/api/schemas/categories.py, app/api/schemas/periods.py, app/api/schemas/onboarding.py, app/api/schemas/settings.py, app/api/schemas/telegram.py</files>
  <behavior>
    - app/api/schemas/__init__.py: пустой, помечает пакет
    - categories.py: CategoryCreate (POST body), CategoryUpdate (PATCH body, все поля Optional), CategoryRead (response)
    - periods.py: PeriodRead (response для GET /periods/current)
    - onboarding.py: OnboardingCompleteRequest (body) с Field(ge=1, le=28) для cycle_start_day; OnboardingCompleteResponse (response)
    - settings.py: SettingsRead (response), SettingsUpdate (body) с Field(ge=1, le=28)
    - telegram.py: ChatBindRequest (body для internal endpoint)
    - Все схемы — Pydantic v2; используют ConfigDict(from_attributes=True) для ORM-совместимости в Read-моделях
    - kind поле использует строковый Literal['expense', 'income'] (не enum-import) для упрощения сериализации; внутри сервиса конвертируется в CategoryKind
  </behavior>
  <action>
**1. Создать `app/api/schemas/__init__.py`** (пустой):

```python
"""Pydantic v2 schemas for API request/response models."""
```

**2. Создать `app/api/schemas/categories.py`:**

```python
"""Pydantic schemas for /api/v1/categories endpoints (CAT-01, CAT-02)."""
from datetime import datetime
from typing import Literal, Optional

from pydantic import BaseModel, ConfigDict, Field


CategoryKindStr = Literal["expense", "income"]


class CategoryCreate(BaseModel):
    """POST /categories request body."""
    name: str = Field(min_length=1, max_length=200)
    kind: CategoryKindStr
    sort_order: int = Field(default=0, ge=0)


class CategoryUpdate(BaseModel):
    """PATCH /categories/{id} request body — all fields optional."""
    name: Optional[str] = Field(default=None, min_length=1, max_length=200)
    sort_order: Optional[int] = Field(default=None, ge=0)
    is_archived: Optional[bool] = None


class CategoryRead(BaseModel):
    """GET /categories response item."""
    model_config = ConfigDict(from_attributes=True)

    id: int
    name: str
    kind: CategoryKindStr
    is_archived: bool
    sort_order: int
    created_at: datetime
```

**3. Создать `app/api/schemas/periods.py`:**

```python
"""Pydantic schemas for /api/v1/periods endpoints (PER-01, PER-02)."""
from datetime import date, datetime
from typing import Literal, Optional

from pydantic import BaseModel, ConfigDict


PeriodStatusStr = Literal["active", "closed"]


class PeriodRead(BaseModel):
    """GET /periods/current response — also used in lists."""
    model_config = ConfigDict(from_attributes=True)

    id: int
    period_start: date
    period_end: date
    starting_balance_cents: int
    ending_balance_cents: Optional[int]
    status: PeriodStatusStr
    closed_at: Optional[datetime]
```

**4. Создать `app/api/schemas/onboarding.py`:**

```python
"""Pydantic schemas for /api/v1/onboarding endpoints (ONB-01, PER-02, CAT-03)."""
from datetime import datetime

from pydantic import BaseModel, Field


class OnboardingCompleteRequest(BaseModel):
    """POST /onboarding/complete request body.

    Fields:
        starting_balance_cents: signed integer in kopecks (negative = debt allowed).
        cycle_start_day: 1..28 (constrained by Pydantic Field per HLD §3 + D-09).
        seed_default_categories: if True, create the 14 default categories
            from D-16 (skipped if any category already exists — idempotent).
    """
    starting_balance_cents: int
    cycle_start_day: int = Field(ge=1, le=28)
    seed_default_categories: bool


class OnboardingCompleteResponse(BaseModel):
    """POST /onboarding/complete response."""
    period_id: int
    seeded_categories: int
    onboarded_at: datetime
```

**5. Создать `app/api/schemas/settings.py`:**

```python
"""Pydantic schemas for /api/v1/settings endpoints (SET-01)."""
from pydantic import BaseModel, Field


class SettingsRead(BaseModel):
    """GET /settings response."""
    cycle_start_day: int


class SettingsUpdate(BaseModel):
    """PATCH /settings request body.

    cycle_start_day must be in [1, 28] per HLD §3 + PER-01 contract.
    Pydantic returns 422 on out-of-range, satisfying tests/test_settings.py.
    """
    cycle_start_day: int = Field(ge=1, le=28)
```

**6. Создать `app/api/schemas/telegram.py`:**

```python
"""Pydantic schemas for internal telegram chat-bind endpoint (ONB-03)."""
from pydantic import BaseModel


class ChatBindRequest(BaseModel):
    """POST /api/v1/internal/telegram/chat-bind request body.

    Called by bot service after `/start` is received from OWNER.
    Endpoint is protected by X-Internal-Token (verify_internal_token dep).
    """
    tg_user_id: int
    tg_chat_id: int
```

**Validation steps after writing:**

```bash
uv run python -c "from app.api.schemas.categories import CategoryCreate, CategoryRead; from app.api.schemas.onboarding import OnboardingCompleteRequest; from app.api.schemas.settings import SettingsUpdate; print('schemas import OK')"

# Тест Field validation
uv run python -c "
from app.api.schemas.settings import SettingsUpdate
try: SettingsUpdate(cycle_start_day=29)
except Exception as e: print('Validation works:', e)
"
```

  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && for f in app/api/schemas/__init__.py app/api/schemas/categories.py app/api/schemas/periods.py app/api/schemas/onboarding.py app/api/schemas/settings.py app/api/schemas/telegram.py; do python3 -c "import ast; ast.parse(open('$f').read()); print('$f OK')"; done 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 6 schema-файлов созданы и синтаксически корректны (ast.parse)
    - app/api/schemas/categories.py определяет 3 класса: `grep -c "^class " app/api/schemas/categories.py` == 3
    - schemas/onboarding.py использует Field(ge=1, le=28): `grep -c "ge=1.*le=28\|ge=1, le=28" app/api/schemas/onboarding.py` >= 1
    - schemas/settings.py использует Field(ge=1, le=28): `grep -c "ge=1.*le=28\|ge=1, le=28" app/api/schemas/settings.py` >= 1
    - CategoryRead использует from_attributes для ORM compat: `grep -c "from_attributes" app/api/schemas/categories.py` >= 1
    - PeriodRead использует from_attributes: `grep -c "from_attributes" app/api/schemas/periods.py` >= 1
    - **Schemas импортируются без ошибок:** `cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.api.schemas.categories import CategoryCreate, CategoryUpdate, CategoryRead; from app.api.schemas.periods import PeriodRead; from app.api.schemas.onboarding import OnboardingCompleteRequest, OnboardingCompleteResponse; from app.api.schemas.settings import SettingsRead, SettingsUpdate; from app.api.schemas.telegram import ChatBindRequest; print('OK')"`
    - **Validation работает:** `uv run python -c "from app.api.schemas.settings import SettingsUpdate; SettingsUpdate(cycle_start_day=29)"` завершается с ValidationError
  </acceptance_criteria>
  <done>6 schema-файлов созданы; все импорты проходят; Pydantic Field validation для cycle_start_day работает (422 на out-of-range); ConfigDict(from_attributes=True) на Read-моделях</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API request body | Pydantic v2 валидация Field(ge=1, le=28) на cycle_start_day |
| client → API request body | Pydantic v2 валидация min/max length на name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-cycle-validation | Tampering | OnboardingCompleteRequest, SettingsUpdate | mitigate | `Field(ge=1, le=28)` на cycle_start_day гарантирует, что недопустимые значения возвращают 422 до достижения сервиса/БД |
| T-name-injection | Tampering | CategoryCreate.name | mitigate | `Field(min_length=1, max_length=200)` ограничивает длину; XSS защищается React при рендеринге; SQL-injection защищается SQLAlchemy parameterized queries |
| T-period-clamping | Tampering | period_for | accept | Функция clamp'ит cycle_start_day > 28 к последнему дню месяца — это документированное поведение per HLD §3, не уязвимость |
</threat_model>

<verification>
План завершён когда:
1. `uv run pytest tests/test_period_engine.py -x -v` — GREEN (все 11 тестов проходят: 9 parametrized + 2 sanity)
2. Все 6 schema-файлов импортируются без ошибок
3. SettingsUpdate(cycle_start_day=29) → ValidationError
4. CategoryCreate(name="", kind="expense") → ValidationError (min_length=1)
5. Phase 1 тесты не сломаны: `uv run pytest tests/test_auth.py tests/test_health.py tests/test_internal_auth.py -x` — GREEN
</verification>

<success_criteria>
- python-dateutil + httpx (prod) установлены через `uv sync`
- app/core/period.py с period_for() покрывает HLD §3 + edge cases
- 6 файлов в app/api/schemas/ создают полный набор request/response контрактов
- Pydantic Field validation для cycle_start_day, name, sort_order работает
- test_period_engine.py — GREEN после применения плана
- Никаких изменений в БД-слое (SQLAlchemy сессии, ORM модели не трогаются)
</success_criteria>

<output>
После завершения создать `.planning/phases/02-domain-foundation-and-onboarding/02-02-SUMMARY.md`
</output>
