---
phase: 02-domain-foundation-and-onboarding
plan: 07
type: execute
wave: 5
depends_on: [06]
files_modified:
  - frontend/src/api/categories.ts
  - frontend/src/api/settings.ts
  - frontend/src/hooks/useCategories.ts
  - frontend/src/components/CategoryRow.tsx
  - frontend/src/components/CategoryRow.module.css
  - frontend/src/components/NewCategoryForm.tsx
  - frontend/src/components/NewCategoryForm.module.css
  - frontend/src/screens/CategoriesScreen.tsx
  - frontend/src/screens/CategoriesScreen.module.css
  - frontend/src/screens/SettingsScreen.tsx
  - frontend/src/screens/SettingsScreen.module.css
  - frontend/src/App.tsx
autonomous: false  # contains final checkpoint:human-verify
requirements:
  - CAT-01
  - CAT-02
  - SET-01

must_haves:
  truths:
    - "CategoriesScreen показывает список с группировкой по kind (Расходы/Доходы), упорядоченных по sort_order"
    - "Inline-редактирование name через [✎]; сохранение PATCH"
    - "Архивирование через [⊟] с confirm; PATCH is_archived=true"
    - "Toggle 'Показать архивные' пере-fetch'ит с include_archived=true; архивные с opacity 0.5 + кнопка 'Восстановить'"
    - "'+ Новая' открывает inline-form; на submit POST + re-fetch"
    - "SettingsScreen показывает текущий cycle_start_day; stepper меняет; MainButton 'Сохранить' активна только при изменении"
    - "Дисклеймер 'Изменение применится со следующего периода' видим на SettingsScreen"
    - "App.tsx роутит на полнофункциональные categories/settings экраны (placeholders Plan 02-06 заменены)"
  artifacts:
    - path: "frontend/src/screens/CategoriesScreen.tsx"
      provides: "Полный CRUD UI для категорий"
    - path: "frontend/src/screens/SettingsScreen.tsx"
      provides: "cycle_start_day editor с дисклеймером"
    - path: "frontend/src/hooks/useCategories.ts"
      provides: "useCategories(includeArchived) hook"
      exports: ["useCategories"]
  key_links:
    - from: "frontend/src/screens/CategoriesScreen.tsx"
      to: "POST/PATCH/DELETE /api/v1/categories"
      via: "apiFetch"
      pattern: "/categories"
    - from: "frontend/src/screens/SettingsScreen.tsx"
      to: "PATCH /api/v1/settings"
      via: "apiFetch"
      pattern: "/settings"
---

<objective>
Wave 5 (final frontend wave): реализация CategoriesScreen и SettingsScreen полностью; обновление App.tsx для маршрутизации на эти экраны (вместо placeholder text из Plan 02-06). Заключительный checkpoint:human-verify покрывает acceptance из 02-UI-SPEC.md sections 2 и 3.

Output: 11 новых файлов + обновлённый App.tsx; финальный UI walkthrough.
Покрывает: CAT-01, CAT-02 (UI), SET-01 (UI). ONB-01..03 уже готовы из Plan 02-04..02-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-CONTEXT.md
@.planning/phases/02-domain-foundation-and-onboarding/02-UI-SPEC.md
@frontend/src/api/client.ts
@frontend/src/api/types.ts
@frontend/src/components/Stepper.tsx
@frontend/src/components/MainButton.tsx
@frontend/src/App.tsx

<interfaces>
<!-- Уже созданное в Plan 02-06 -->

apiFetch<T>(path, init): Promise<T>
ApiError class
Stepper: { value, min, max, onChange, wrap }
MainButton: { text, enabled, onClick }

CategoryRead, CategoryCreatePayload, CategoryUpdatePayload, CategoryKind
SettingsRead, SettingsUpdatePayload

<!-- API endpoints (Plan 02-04) -->

GET /api/v1/categories?include_archived=<bool>  → CategoryRead[]
POST /api/v1/categories  body: CategoryCreatePayload  → CategoryRead
PATCH /api/v1/categories/{id}  body: CategoryUpdatePayload  → CategoryRead
DELETE /api/v1/categories/{id}  → CategoryRead (with is_archived=true)
GET /api/v1/settings  → SettingsRead
PATCH /api/v1/settings  body: SettingsUpdatePayload  → SettingsRead

<!-- Что заменяется в App.tsx -->

Сейчас в App.tsx (после Plan 02-06):
```
if (screen === 'categories' || screen === 'settings') {
  return <PlaceholderScreen ... />
}
```

После этого плана:
```
if (screen === 'categories') return <CategoriesScreen onBack={...} />
if (screen === 'settings') return <SettingsScreen onBack={...} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API клиенты + useCategories hook + CategoryRow + NewCategoryForm + CategoriesScreen</name>
  <read_first>
    - .planning/phases/02-domain-foundation-and-onboarding/02-UI-SPEC.md (CategoriesScreen layout, behavior)
    - frontend/src/api/client.ts (apiFetch usage)
    - frontend/src/api/types.ts (CategoryRead, CategoryCreatePayload)
    - .planning/sketches/005-plan-and-categories/index.html (если интересен grouped+inline pattern; не обязателен)
  </read_first>
  <files>frontend/src/api/categories.ts, frontend/src/hooks/useCategories.ts, frontend/src/components/CategoryRow.tsx, frontend/src/components/CategoryRow.module.css, frontend/src/components/NewCategoryForm.tsx, frontend/src/components/NewCategoryForm.module.css, frontend/src/screens/CategoriesScreen.tsx, frontend/src/screens/CategoriesScreen.module.css</files>
  <behavior>
    - api/categories.ts: тонкие функции listCategories/createCategory/updateCategory/archiveCategory вокруг apiFetch
    - hooks/useCategories: useCategories(includeArchived) → { categories, loading, error, refetch }
    - CategoryRow: { category, onEdit, onArchive, onUnarchive }; render name + edit + archive icons; поддержка inline-edit mode (state в самом компоненте)
    - NewCategoryForm: { onCreate, onCancel } — inline form с input + radio group expense/income + кнопкой
    - CategoriesScreen: загружает /categories; группирует по kind; рендерит CategoryRow/NewCategoryForm; toggle "Показать архивные"
  </behavior>
  <action>
**1. Создать `frontend/src/api/categories.ts`:**

```typescript
import { apiFetch } from './client';
import type { CategoryCreatePayload, CategoryRead, CategoryUpdatePayload } from './types';

export async function listCategories(includeArchived = false): Promise<CategoryRead[]> {
  const qs = includeArchived ? '?include_archived=true' : '';
  return apiFetch<CategoryRead[]>(`/categories${qs}`);
}

export async function createCategory(payload: CategoryCreatePayload): Promise<CategoryRead> {
  return apiFetch<CategoryRead>('/categories', {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export async function updateCategory(
  id: number,
  patch: CategoryUpdatePayload,
): Promise<CategoryRead> {
  return apiFetch<CategoryRead>(`/categories/${id}`, {
    method: 'PATCH',
    body: JSON.stringify(patch),
  });
}

export async function archiveCategory(id: number): Promise<CategoryRead> {
  return apiFetch<CategoryRead>(`/categories/${id}`, { method: 'DELETE' });
}
```

**2. Создать `frontend/src/hooks/useCategories.ts`:**

```typescript
import { useCallback, useEffect, useState } from 'react';
import { listCategories } from '../api/categories';
import type { CategoryRead } from '../api/types';

export interface UseCategoriesResult {
  categories: CategoryRead[];
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

export function useCategories(includeArchived: boolean): UseCategoriesResult {
  const [categories, setCategories] = useState<CategoryRead[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refetch = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await listCategories(includeArchived);
      setCategories(data);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setLoading(false);
    }
  }, [includeArchived]);

  useEffect(() => {
    void refetch();
  }, [refetch]);

  return { categories, loading, error, refetch };
}
```

**3. Создать `frontend/src/components/CategoryRow.tsx`:**

```typescript
import { useState } from 'react';
import type { CategoryRead } from '../api/types';
import styles from './CategoryRow.module.css';

export interface CategoryRowProps {
  category: CategoryRead;
  onRename: (id: number, newName: string) => Promise<void>;
  onArchive: (id: number) => Promise<void>;
  onUnarchive: (id: number) => Promise<void>;
}

export function CategoryRow({ category, onRename, onArchive, onUnarchive }: CategoryRowProps) {
  const [editing, setEditing] = useState(false);
  const [draft, setDraft] = useState(category.name);
  const [saving, setSaving] = useState(false);

  const handleSave = async () => {
    if (draft.trim() === '' || draft === category.name) {
      setEditing(false);
      setDraft(category.name);
      return;
    }
    setSaving(true);
    try {
      await onRename(category.id, draft.trim());
      setEditing(false);
    } finally {
      setSaving(false);
    }
  };

  const handleArchive = async () => {
    if (!window.confirm(`Архивировать категорию «${category.name}»?`)) return;
    await onArchive(category.id);
  };

  const cls = [styles.row, category.is_archived ? styles.archived : ''].filter(Boolean).join(' ');

  return (
    <div className={cls}>
      {editing ? (
        <>
          <input
            type="text"
            value={draft}
            onChange={(e) => setDraft(e.target.value)}
            onKeyDown={(e) => {
              if (e.key === 'Enter') void handleSave();
              if (e.key === 'Escape') {
                setEditing(false);
                setDraft(category.name);
              }
            }}
            autoFocus
            disabled={saving}
            className={styles.editInput}
          />
          <button type="button" onClick={() => void handleSave()} disabled={saving} className={styles.iconBtn}>
            {saving ? '…' : '✓'}
          </button>
        </>
      ) : (
        <>
          <span className={styles.name}>{category.name}</span>
          {category.is_archived ? (
            <button type="button" onClick={() => void onUnarchive(category.id)} className={styles.restoreBtn}>
              Восстановить
            </button>
          ) : (
            <>
              <button type="button" onClick={() => setEditing(true)} className={styles.iconBtn} aria-label="Переименовать">
                ✎
              </button>
              <button type="button" onClick={() => void handleArchive()} className={styles.iconBtn} aria-label="Архивировать">
                ⊟
              </button>
            </>
          )}
        </>
      )}
    </div>
  );
}
```

**4. Создать `frontend/src/components/CategoryRow.module.css`:**

```css
.row {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 14px;
  background: var(--color-surface);
  border-radius: var(--radius-sm);
  margin-bottom: 6px;
}
.row.archived {
  opacity: 0.5;
}
.name {
  flex: 1;
  font-size: 15px;
  color: var(--color-text);
}
.iconBtn {
  width: 32px;
  height: 32px;
  border-radius: var(--radius-sm);
  background: transparent;
  border: 0;
  color: var(--color-text-muted);
  font-size: 14px;
  cursor: pointer;
  font-family: inherit;
}
.iconBtn:hover {
  background: var(--color-bg-elevated);
  color: var(--color-primary);
}
.restoreBtn {
  padding: 6px 12px;
  background: transparent;
  border: 1px solid var(--color-primary);
  color: var(--color-primary);
  border-radius: var(--radius-sm);
  font-size: 12px;
  cursor: pointer;
  font-family: inherit;
}
.editInput {
  flex: 1;
  border: 0;
  background: var(--color-bg-elevated);
  color: var(--color-text);
  padding: 6px 10px;
  border-radius: var(--radius-sm);
  font-size: 15px;
  font-family: inherit;
  outline: none;
}
```

**5. Создать `frontend/src/components/NewCategoryForm.tsx`:**

```typescript
import { useState } from 'react';
import type { CategoryKind } from '../api/types';
import styles from './NewCategoryForm.module.css';

export interface NewCategoryFormProps {
  onCreate: (name: string, kind: CategoryKind) => Promise<void>;
  onCancel: () => void;
}

export function NewCategoryForm({ onCreate, onCancel }: NewCategoryFormProps) {
  const [name, setName] = useState('');
  const [kind, setKind] = useState<CategoryKind>('expense');
  const [submitting, setSubmitting] = useState(false);

  const handleSubmit = async () => {
    const trimmed = name.trim();
    if (trimmed === '') return;
    setSubmitting(true);
    try {
      await onCreate(trimmed, kind);
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className={styles.form}>
      <input
        type="text"
        placeholder="Название категории"
        value={name}
        onChange={(e) => setName(e.target.value)}
        onKeyDown={(e) => {
          if (e.key === 'Enter') void handleSubmit();
          if (e.key === 'Escape') onCancel();
        }}
        autoFocus
        disabled={submitting}
        className={styles.nameInput}
      />
      <div className={styles.kindRow}>
        <label>
          <input
            type="radio"
            name="kind"
            value="expense"
            checked={kind === 'expense'}
            onChange={() => setKind('expense')}
          />
          <span>Расход</span>
        </label>
        <label>
          <input
            type="radio"
            name="kind"
            value="income"
            checked={kind === 'income'}
            onChange={() => setKind('income')}
          />
          <span>Доход</span>
        </label>
      </div>
      <div className={styles.actions}>
        <button type="button" onClick={() => void handleSubmit()} disabled={submitting || name.trim() === ''} className={styles.submitBtn}>
          {submitting ? 'Создание…' : 'Создать'}
        </button>
        <button type="button" onClick={onCancel} disabled={submitting} className={styles.cancelBtn}>
          ×
        </button>
      </div>
    </div>
  );
}
```

**6. Создать `frontend/src/components/NewCategoryForm.module.css`:**

```css
.form {
  margin-bottom: 12px;
  padding: 14px;
  background: var(--color-surface);
  border-radius: var(--radius-md);
  border: 1px dashed var(--color-primary);
}
.nameInput {
  width: 100%;
  padding: 10px 12px;
  background: var(--color-bg-elevated);
  border: 0;
  color: var(--color-text);
  border-radius: var(--radius-sm);
  font-size: 15px;
  font-family: inherit;
  outline: none;
  margin-bottom: 10px;
}
.kindRow {
  display: flex;
  gap: 12px;
  margin-bottom: 12px;
}
.kindRow label {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  color: var(--color-text);
  cursor: pointer;
}
.actions {
  display: flex;
  gap: 8px;
  align-items: center;
}
.submitBtn {
  flex: 1;
  padding: 10px 16px;
  background: var(--color-primary);
  color: #fff;
  border: 0;
  border-radius: var(--radius-sm);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  font-family: inherit;
}
.submitBtn:disabled { opacity: 0.5; cursor: not-allowed; }
.cancelBtn {
  width: 36px;
  height: 36px;
  border-radius: 999px;
  background: transparent;
  border: 1px solid var(--color-border);
  color: var(--color-text-muted);
  font-size: 16px;
  cursor: pointer;
  font-family: inherit;
}
```

**7. Создать `frontend/src/screens/CategoriesScreen.tsx`:**

```typescript
import { useMemo, useState } from 'react';
import { archiveCategory, createCategory, updateCategory } from '../api/categories';
import { useCategories } from '../hooks/useCategories';
import type { CategoryKind, CategoryRead } from '../api/types';
import { CategoryRow } from '../components/CategoryRow';
import { NewCategoryForm } from '../components/NewCategoryForm';
import styles from './CategoriesScreen.module.css';

export interface CategoriesScreenProps {
  onBack: () => void;
}

interface Group {
  title: string;
  kind: CategoryKind;
  rows: CategoryRead[];
}

export function CategoriesScreen({ onBack }: CategoriesScreenProps) {
  const [includeArchived, setIncludeArchived] = useState(false);
  const [showNewForm, setShowNewForm] = useState(false);
  const { categories, loading, error, refetch } = useCategories(includeArchived);

  const groups = useMemo<Group[]>(() => {
    const expense = categories
      .filter((c) => c.kind === 'expense')
      .sort((a, b) => a.sort_order - b.sort_order || a.name.localeCompare(b.name));
    const income = categories
      .filter((c) => c.kind === 'income')
      .sort((a, b) => a.sort_order - b.sort_order || a.name.localeCompare(b.name));
    const result: Group[] = [];
    if (expense.length > 0) result.push({ title: 'Расходы', kind: 'expense', rows: expense });
    if (income.length > 0) result.push({ title: 'Доходы', kind: 'income', rows: income });
    return result;
  }, [categories]);

  const handleCreate = async (name: string, kind: CategoryKind) => {
    await createCategory({ name, kind });
    setShowNewForm(false);
    await refetch();
  };

  const handleRename = async (id: number, newName: string) => {
    await updateCategory(id, { name: newName });
    await refetch();
  };

  const handleArchive = async (id: number) => {
    await archiveCategory(id);
    await refetch();
  };

  const handleUnarchive = async (id: number) => {
    await updateCategory(id, { is_archived: false });
    await refetch();
  };

  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <button type="button" onClick={onBack} className={styles.backBtn} aria-label="Назад">←</button>
        <div className={styles.title}>Категории</div>
        <button type="button" onClick={() => setShowNewForm((s) => !s)} className={styles.addBtn}>
          + Новая
        </button>
      </header>

      {showNewForm && (
        <NewCategoryForm onCreate={handleCreate} onCancel={() => setShowNewForm(false)} />
      )}

      {loading && <div className={styles.muted}>Загрузка…</div>}
      {error && <div className={styles.error}>Ошибка: {error}</div>}
      {!loading && !error && groups.length === 0 && (
        <div className={styles.muted}>Нет категорий. Нажмите «+ Новая», чтобы создать первую.</div>
      )}

      {groups.map((g) => (
        <section key={g.kind} className={styles.group}>
          <h3 className={styles.groupTitle}>{g.title}</h3>
          {g.rows.map((cat) => (
            <CategoryRow
              key={cat.id}
              category={cat}
              onRename={handleRename}
              onArchive={handleArchive}
              onUnarchive={handleUnarchive}
            />
          ))}
        </section>
      ))}

      <label className={styles.toggleArchived}>
        <input
          type="checkbox"
          checked={includeArchived}
          onChange={(e) => setIncludeArchived(e.target.checked)}
        />
        <span>Показать архивные</span>
      </label>
    </div>
  );
}
```

**8. Создать `frontend/src/screens/CategoriesScreen.module.css`:**

```css
.root {
  max-width: 375px;
  margin: 0 auto;
  padding: 12px 16px 80px;
  min-height: 100vh;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
}
.header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 0 16px;
}
.backBtn {
  width: 36px;
  height: 36px;
  border-radius: var(--radius-sm);
  background: transparent;
  border: 0;
  color: var(--color-primary);
  font-size: 18px;
  cursor: pointer;
  font-family: inherit;
}
.title {
  flex: 1;
  text-align: center;
  font-size: 16px;
  font-weight: 600;
  color: var(--color-text);
}
.addBtn {
  padding: 8px 12px;
  background: var(--color-primary);
  color: #fff;
  border: 0;
  border-radius: var(--radius-sm);
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  font-family: inherit;
}
.group {
  margin-bottom: 16px;
}
.groupTitle {
  margin: 12px 0 8px;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  font-weight: 600;
  color: var(--color-text-muted);
}
.muted {
  color: var(--color-text-muted);
  font-size: 13px;
  text-align: center;
  padding: 24px;
}
.error {
  color: var(--color-danger);
  font-size: 13px;
  padding: 12px;
  background: var(--color-danger-soft);
  border-radius: var(--radius-md);
  margin: 8px 0;
}
.toggleArchived {
  display: flex;
  align-items: center;
  gap: 8px;
  margin: 16px 0;
  font-size: 13px;
  color: var(--color-text-muted);
  cursor: pointer;
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && for f in src/api/categories.ts src/hooks/useCategories.ts src/components/CategoryRow.tsx src/components/NewCategoryForm.tsx src/screens/CategoriesScreen.tsx; do test -f "$f" || echo "MISSING $f"; done && npx tsc --noEmit 2>&1 | (grep -E "error TS" || echo "tsc: no errors")</automated>
  </verify>
  <acceptance_criteria>
    - 8 файлов созданы: api/categories.ts, hooks/useCategories.ts, 2 components с CSS, 1 screen с CSS
    - api/categories.ts экспортирует 4 функции: `grep -c "^export async function" frontend/src/api/categories.ts` == 4
    - useCategories принимает includeArchived: `grep -c "includeArchived" frontend/src/hooks/useCategories.ts` >= 2
    - CategoryRow поддерживает inline edit: `grep -c "editing\|setEditing" frontend/src/components/CategoryRow.tsx` >= 2
    - CategoryRow вызывает onArchive с confirm: `grep -c "window.confirm\|Архивировать" frontend/src/components/CategoryRow.tsx` >= 1
    - NewCategoryForm имеет radio для kind: `grep -c "type=\"radio\"" frontend/src/components/NewCategoryForm.tsx` == 2
    - CategoriesScreen группирует по kind: `grep -c "expense\\|income" frontend/src/screens/CategoriesScreen.tsx` >= 2
    - **TypeScript компилируется:** `cd frontend && npx tsc --noEmit` — без TS errors
  </acceptance_criteria>
  <done>Categories CRUD UI готов: list/create/rename/archive/unarchive + group by kind + toggle archived; tsc проходит</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API клиент settings + SettingsScreen + обновить App.tsx</name>
  <read_first>
    - .planning/phases/02-domain-foundation-and-onboarding/02-UI-SPEC.md (SettingsScreen layout)
    - frontend/src/components/Stepper.tsx (переиспользуем)
    - frontend/src/components/MainButton.tsx (для «Сохранить»)
    - frontend/src/App.tsx (нужно изменить — заменить placeholder)
  </read_first>
  <files>frontend/src/api/settings.ts, frontend/src/screens/SettingsScreen.tsx, frontend/src/screens/SettingsScreen.module.css, frontend/src/App.tsx</files>
  <behavior>
    - api/settings.ts: getSettings + updateSettings функции
    - SettingsScreen: useState для current/draft cycle_start_day; useEffect для GET /settings; Stepper для редактирования; MainButton «Сохранить» enabled только если draft != current; PATCH на click; toast/alert «Сохранено»; back-callback
    - SettingsScreen рендерит дисклеймер «Изменение применится со следующего периода. Текущий период продолжается с тем же днём начала.»
    - App.tsx: добавить импорты CategoriesScreen и SettingsScreen; заменить placeholder JSX на эти компоненты
  </behavior>
  <action>
**1. Создать `frontend/src/api/settings.ts`:**

```typescript
import { apiFetch } from './client';
import type { SettingsRead, SettingsUpdatePayload } from './types';

export async function getSettings(): Promise<SettingsRead> {
  return apiFetch<SettingsRead>('/settings');
}

export async function updateSettings(payload: SettingsUpdatePayload): Promise<SettingsRead> {
  return apiFetch<SettingsRead>('/settings', {
    method: 'PATCH',
    body: JSON.stringify(payload),
  });
}
```

**2. Создать `frontend/src/screens/SettingsScreen.tsx`:**

```typescript
import { useCallback, useEffect, useState } from 'react';
import { getSettings, updateSettings } from '../api/settings';
import { Stepper } from '../components/Stepper';
import { MainButton } from '../components/MainButton';
import styles from './SettingsScreen.module.css';

export interface SettingsScreenProps {
  onBack: () => void;
}

export function SettingsScreen({ onBack }: SettingsScreenProps) {
  const [current, setCurrent] = useState<number | null>(null);
  const [draft, setDraft] = useState<number>(5);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [savedFlash, setSavedFlash] = useState(false);
  const [saving, setSaving] = useState(false);

  useEffect(() => {
    let active = true;
    setLoading(true);
    getSettings()
      .then((s) => {
        if (!active) return;
        setCurrent(s.cycle_start_day);
        setDraft(s.cycle_start_day);
        setError(null);
      })
      .catch((e: unknown) => {
        if (!active) return;
        setError(e instanceof Error ? e.message : String(e));
      })
      .finally(() => {
        if (active) setLoading(false);
      });
    return () => {
      active = false;
    };
  }, []);

  const dirty = current !== null && draft !== current;

  const handleSave = useCallback(async () => {
    if (!dirty || saving) return;
    setSaving(true);
    setError(null);
    try {
      const updated = await updateSettings({ cycle_start_day: draft });
      setCurrent(updated.cycle_start_day);
      setSavedFlash(true);
      window.setTimeout(() => setSavedFlash(false), 1500);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setSaving(false);
    }
  }, [dirty, draft, saving]);

  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <button type="button" onClick={onBack} className={styles.backBtn} aria-label="Назад">←</button>
        <div className={styles.title}>Настройки</div>
      </header>

      {loading && <div className={styles.muted}>Загрузка…</div>}
      {error && <div className={styles.error}>Ошибка: {error}</div>}

      {!loading && current !== null && (
        <section className={styles.card}>
          <div className={styles.cardTitle}>День начала периода</div>
          <Stepper value={draft} min={1} max={28} onChange={setDraft} wrap />
          <div className={styles.disclaimer}>
            ⓘ Изменение применится со следующего периода. Текущий период продолжается с тем же
            днём начала.
          </div>
        </section>
      )}

      {savedFlash && <div className={styles.savedToast}>✓ Сохранено</div>}

      <MainButton
        text={saving ? 'Сохранение…' : 'Сохранить'}
        enabled={dirty && !saving}
        onClick={handleSave}
      />
    </div>
  );
}
```

**3. Создать `frontend/src/screens/SettingsScreen.module.css`:**

```css
.root {
  max-width: 375px;
  margin: 0 auto;
  padding: 12px 16px 120px;
  min-height: 100vh;
  background: var(--color-bg);
  color: var(--color-text);
  font-family: var(--font-sans);
}
.header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 0 16px;
}
.backBtn {
  width: 36px;
  height: 36px;
  border-radius: var(--radius-sm);
  background: transparent;
  border: 0;
  color: var(--color-primary);
  font-size: 18px;
  cursor: pointer;
  font-family: inherit;
}
.title {
  flex: 1;
  text-align: center;
  font-size: 16px;
  font-weight: 600;
}
.card {
  padding: 16px 18px;
  background: var(--color-surface);
  border-radius: var(--radius-md);
  margin-bottom: 16px;
}
.cardTitle {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text);
  margin-bottom: 12px;
}
.disclaimer {
  margin-top: 12px;
  font-size: 12px;
  color: var(--color-text-muted);
  line-height: 1.5;
}
.muted {
  color: var(--color-text-muted);
  font-size: 13px;
  text-align: center;
  padding: 24px;
}
.error {
  color: var(--color-danger);
  font-size: 13px;
  padding: 12px;
  background: var(--color-danger-soft);
  border-radius: var(--radius-md);
  margin: 8px 0;
}
.savedToast {
  position: fixed;
  left: 50%;
  bottom: 100px;
  transform: translateX(-50%);
  padding: 10px 18px;
  background: var(--color-success);
  color: #fff;
  border-radius: 999px;
  font-size: 13px;
  font-weight: 600;
  z-index: 100;
}
```

**4. Обновить `frontend/src/App.tsx` — заменить placeholder JSX:**

В существующем App.tsx (Plan 02-06) заменить блок:

```typescript
// categories | settings — placeholders until Plan 02-07
return (
  <div className={styles.placeholderScreen}>
    <button type="button" className={styles.backBtn} onClick={() => setOverrideScreen('home')}>
      ← Назад
    </button>
    <h2>{screen === 'categories' ? 'Категории' : 'Настройки'}</h2>
    <p>Этот экран реализован в Plan 02-07.</p>
  </div>
);
```

на полнофункциональные экраны:

```typescript
if (screen === 'categories') {
  return <CategoriesScreen onBack={() => setOverrideScreen('home')} />;
}
return <SettingsScreen onBack={() => setOverrideScreen('home')} />;
```

И добавить импорты в начало файла:

```typescript
import { CategoriesScreen } from './screens/CategoriesScreen';
import { SettingsScreen } from './screens/SettingsScreen';
```

Финальный App.tsx должен выглядеть так:

```typescript
import { useState } from 'react';
import { useUser } from './hooks/useUser';
import { OnboardingScreen } from './screens/OnboardingScreen';
import { HomeScreen } from './screens/HomeScreen';
import { CategoriesScreen } from './screens/CategoriesScreen';
import { SettingsScreen } from './screens/SettingsScreen';
import styles from './App.module.css';

type Screen = 'onboarding' | 'home' | 'categories' | 'settings';

export default function App() {
  const { user, loading, error, refetch } = useUser();
  const [overrideScreen, setOverrideScreen] = useState<Screen | null>(null);

  if (loading && !user) {
    return <div className={styles.loadingRoot}>Загрузка…</div>;
  }
  if (error || !user) {
    return (
      <div className={styles.errorRoot}>
        Не удалось загрузить пользователя.<br />
        {error && <code>{error}</code>}
      </div>
    );
  }

  const isOnboarded = user.onboarded_at !== null;
  const screen: Screen = overrideScreen ?? (isOnboarded ? 'home' : 'onboarding');

  if (screen === 'onboarding') {
    return (
      <OnboardingScreen
        user={user}
        onRefreshUser={refetch}
        onComplete={() => {
          setOverrideScreen('home');
          void refetch();
        }}
      />
    );
  }
  if (screen === 'home') {
    return <HomeScreen onNavigate={(s) => setOverrideScreen(s)} />;
  }
  if (screen === 'categories') {
    return <CategoriesScreen onBack={() => setOverrideScreen('home')} />;
  }
  return <SettingsScreen onBack={() => setOverrideScreen('home')} />;
}
```

**Validation:**

```bash
cd /Users/exy/pet_projects/tg-budget-planner/frontend
npx tsc --noEmit
npm run build
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && test -f src/api/settings.ts && test -f src/screens/SettingsScreen.tsx && grep -c "CategoriesScreen\|SettingsScreen" src/App.tsx && npx tsc --noEmit 2>&1 | (grep -E "error TS" || echo "tsc: no errors") && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - api/settings.ts экспортирует 2 функции: `grep -c "^export async function" frontend/src/api/settings.ts` == 2
    - SettingsScreen использует Stepper: `grep -c "Stepper" frontend/src/screens/SettingsScreen.tsx` >= 1
    - SettingsScreen использует MainButton: `grep -c "MainButton" frontend/src/screens/SettingsScreen.tsx` >= 1
    - SettingsScreen содержит дисклеймер "следующего периода": `grep -c "следующего периода" frontend/src/screens/SettingsScreen.tsx` == 1
    - App.tsx импортирует оба новых screen: `grep -c "from './screens/CategoriesScreen'\|from './screens/SettingsScreen'" frontend/src/App.tsx` == 2
    - App.tsx больше НЕ содержит placeholder text "Plan 02-07": `grep -c "Plan 02-07\|Этот экран реализован" frontend/src/App.tsx` == 0
    - **TypeScript компилируется:** `cd frontend && npx tsc --noEmit` — без TS errors
    - **Vite build проходит:** `cd frontend && npm run build` — успешен
  </acceptance_criteria>
  <done>SettingsScreen полностью реализован с дисклеймером; App.tsx роутит на новые экраны; tsc + vite build зелёные</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Final Phase 2 Checkpoint: end-to-end UI walkthrough</name>
  <what-built>
    - Полная Phase 2: backend (services + routes) + bot /start handler + frontend (3 экрана)
    - Покрыто: ONB-01..03, CAT-01..03, PER-01..02, PER-05 (структурно), SET-01
  </what-built>
  <how-to-verify>
**Pre-requisites (если ещё не сделано в Plan 02-06 checkpoint):**
- `.env` заполнен реальными значениями (BOT_TOKEN, OWNER_TG_ID, INTERNAL_TOKEN, MINI_APP_URL, BOT_USERNAME)
- `docker compose up -d --build` — все сервисы healthy
- Mini App доступен через Caddy (`https://<MINI_APP_URL>` или dev override)

**Reset state for clean walkthrough:**
```bash
docker compose exec db psql -U budget -d budget_db -c \
  "TRUNCATE TABLE category, planned_transaction, actual_transaction, plan_template_item, subscription, budget_period, app_user RESTART IDENTITY CASCADE"
```

**Walkthrough — Onboarding (повтор из Plan 02-06 checkpoint, чтобы убедиться что не сломали):**

1. Telegram → бот → `/start` → нажать «Открыть бюджет»
2. Заполнить onboarding (balance=12000, cycle=5, seed=on) → click «Готово»
3. **Expected:** HomeScreen, в БД 14 категорий + 1 период + onboarded_at != null

**Walkthrough — Categories (новое в Plan 02-07):**

4. На HomeScreen → click «Категории»
   - **Expected:** CategoriesScreen с группами «Расходы» (12 строк) и «Доходы» (2 строки), отсортированы по sort_order

5. Click «+ Новая» → открывается inline form
6. Ввести «Спорт», kind=expense, click «Создать»
   - **Expected:** «Спорт» появляется в группе «Расходы»; в БД новая строка
   - **Verify:** `docker compose exec db psql -U budget -d budget_db -c "SELECT id, name, kind FROM category WHERE name='Спорт'"`

7. Click [✎] на «Спорт» → inline edit input → ввести «Фитнес» → Enter
   - **Expected:** имя обновлено в UI; PATCH послан
   - **Verify:** `... -c "SELECT name FROM category WHERE id=15"` (или нужный id)

8. Click [⊟] на «Фитнес» → confirm → подтвердить
   - **Expected:** «Фитнес» исчезает из списка
   - **Verify:** `... -c "SELECT name, is_archived FROM category WHERE name='Фитнес'"` → is_archived=true

9. Toggle «Показать архивные» (внизу экрана)
   - **Expected:** «Фитнес» возвращается в список с opacity 0.5 + кнопкой «Восстановить»

10. Click «Восстановить» на «Фитнес»
    - **Expected:** возвращается в активные (без opacity)

11. Click «← Назад» → возврат на HomeScreen

**Walkthrough — Settings (новое в Plan 02-07):**

12. HomeScreen → click «Настройки»
    - **Expected:** SettingsScreen с stepper, текущее значение `5`
    - **Expected:** Дисклеймер «Изменение применится со следующего периода…» виден

13. Click `+` на stepper несколько раз → значение меняется на 10
    - **Expected:** MainButton «Сохранить» становится активной

14. Click MainButton → toast «✓ Сохранено» (через ~1.5s исчезает)
    - **Expected:** MainButton становится disabled (т.к. draft теперь == current)
    - **Verify:** `... -c "SELECT cycle_start_day FROM app_user"` → 10
    - **Verify (SET-01 critical):** период не пересчитан — `... -c "SELECT period_start, period_end FROM budget_period"` показывает прежние даты (полученные из cycle_start_day=5)

15. Click «← Назад» → HomeScreen

**Walkthrough — Persistence:**

16. Закрыть и заново открыть Mini App
    - **Expected:** сразу попадает в HomeScreen (не на onboarding) — onboarded_at != null
    - Открыть Категории → видны те же категории + «Фитнес»

**Walkthrough — Edge cases:**

17. SettingsScreen → попытаться через DevTools отправить PATCH с cycle_start_day=29 (вне UI):
    ```bash
    # из браузерного консоли или curl
    curl -X PATCH https://<host>/api/v1/settings \
      -H 'X-Telegram-Init-Data: <valid initData>' \
      -H 'Content-Type: application/json' \
      -d '{"cycle_start_day": 29}'
    ```
    - **Expected:** 422 Unprocessable Entity (Pydantic Field validation)

18. CategoriesScreen → быстро 5 раз нажать «Создать» с одним именем
    - **Expected:** все запросы выполняются (нет дедупликации в Phase 2 — это ОК), создаётся 5 строк с одинаковым именем; в production пользователь увидит дубли и почистит сам (out of scope MVP)

**If anything fails:**
- UI ошибка → DevTools Console + Network tab
- 4xx/5xx от backend → `docker compose logs api`
- Категория не появляется после create → проверить response JSON; возможно, refetch не сработал
- SettingsScreen рендерит «Загрузка…» бесконечно → проверить /me + /settings 200 OK

**Final sanity:**

```bash
# Backend smoke (если есть тестовая БД настроена)
DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test \
  uv run pytest tests/ -v

# Все 6 новых тестов + Phase 1 — все GREEN
```
  </how-to-verify>
  <resume-signal>Type "approved" если все 18 шагов прошли успешно; иначе опиши, какой шаг не работает (со скриншотами/логами)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API (CRUD категорий) | initData header валидируется backend; user-input (name) проходит Pydantic validation min_length=1 |
| Mini App → API (settings) | cycle_start_day validated client-side (Stepper enforces 1..28) и server-side (Pydantic Field) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-fe-cat-xss | XSS | CategoryRow.name display | mitigate | React auto-escapes string content; `{category.name}` безопасно |
| T-fe-bypass-validation | Tampering | SettingsScreen client-side validation | mitigate | Stepper UI ограничивает 1..28; даже если bypass через DevTools — backend Pydantic Field(ge=1, le=28) → 422 |
| T-fe-confirm-bypass | Tampering | CategoryRow archive (window.confirm) | accept | Если пользователь bypass'ит confirm и archive ошибочно — categories мягко архивируются; восстанавливаются через unarchive (CAT-02 design) |
| T-fe-stale-state | Tampering | useCategories кэш | mitigate | Каждый mutation вызывает refetch(); на гонках — последний refetch выигрывает (acceptable для single-user) |
| T-fe-empty-name | Tampering | NewCategoryForm submit | mitigate | Client-side: trim().length === 0 → submitBtn disabled. Backend: Pydantic min_length=1 → 422 backup |
| T-fe-toast-leak | Info Disclosure | error display | mitigate | Error messages показывают HTTP status + path (не stack trace, не БД-content) |
</threat_model>

<verification>
План завершён когда:
1. Все 11 новых файлов созданы; `npx tsc --noEmit` без TS errors; `npm run build` успешен
2. App.tsx роутит на CategoriesScreen и SettingsScreen (placeholder JSX удалён)
3. Final checkpoint:human-verify пройден (все 18 шагов)
4. Backend integration тесты GREEN при наличии тестовой БД
5. Phase 1 тесты не сломаны
6. Документ 02-VALIDATION.md обновлён по результатам walkthrough (фронтенд checkpoint statuses → ✅)
</verification>

<success_criteria>
- CategoriesScreen: list/create/rename/archive/unarchive/show-archived — все работает
- SettingsScreen: read/edit/save cycle_start_day; дисклеймер виден; SET-01 contract соблюдён (period не пересчитывается)
- Persistence через перезагрузку — onboarded_at, cycle_start_day, категории сохраняются
- Все 11 requirements Phase 2 покрыты code-level + verified manually
- Mini App, бот и backend работают согласованно (e2e flow)
</success_criteria>

<output>
После завершения создать `.planning/phases/02-domain-foundation-and-onboarding/02-07-SUMMARY.md`
</output>
