---
phase: 11-multi-tenancy-db-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_multitenancy_isolation.py
  - tests/test_rls_policy.py
  - tests/test_migration_backfill.py
autonomous: true
requirements: [MUL-03, MUL-05]
tags: [tests, multitenancy, rls, fixtures, red-phase]
must_haves:
  truths:
    - "RED test files существуют и в текущем состоянии падают (raise NotImplementedError или fail с ясным сообщением)"
    - "Pytest fixture two_tenants создаёт user_a + user_b с изолированными ID и возвращает их id-ы"
    - "Future executor имеет конкретные тест-сигнатуры для заполнения в Plan 11-07"
    - "Запуск pytest показывает 3 fail/error в новых файлах (не collection error — реальные fail)"
  artifacts:
    - path: tests/test_multitenancy_isolation.py
      provides: "Skeleton тестов изоляции данных между двумя tenant"
      contains: "two_tenants"
    - path: tests/test_rls_policy.py
      provides: "Skeleton теста RLS — без SET app.current_user_id query возвращает 0 rows"
      contains: "current_setting"
    - path: tests/test_migration_backfill.py
      provides: "Skeleton теста backfill — после миграции все user_id равны OWNER_TG_ID-юзеру"
      contains: "backfill"
    - path: tests/conftest.py
      provides: "two_tenants fixture (расширение существующего conftest)"
      contains: "two_tenants"
  key_links:
    - from: tests/test_multitenancy_isolation.py
      to: tests/conftest.py
      via: "two_tenants fixture import"
      pattern: "two_tenants"
    - from: tests/test_rls_policy.py
      to: app/db/session.py
      via: "asyncpg raw connection для verify RLS без app.current_user_id setting"
      pattern: "AsyncSessionLocal|create_async_engine"
---

<objective>
Wave 0 / RED phase для Phase 11: создать failing test skeleton'ы, которые executor более поздних планов будет заполнять реальными assertions. Это даёт future-self чёткие test-cases как acceptance criteria для всей миграции.

Цель — не запустить тесты (они должны fail!), а зафиксировать в коде:
1. Сигнатуру fixture `two_tenants` (два изолированных юзера, у каждого свои категории/транзакции)
2. Имена и сигнатуры теста для проверки изоляции (`test_user_a_does_not_see_user_b_categories`)
3. Имя и сигнатуру теста для проверки RLS (`test_rls_blocks_query_without_setting`)
4. Имя теста для проверки backfill (`test_migration_backfilled_user_id`)

Output: 3 новых tests/*.py файла + расширенный conftest.py; pytest -k "multitenancy or rls or migration_backfill" показывает 3+ failures (NotImplementedError или explicit assert False).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md
@CLAUDE.md
@tests/conftest.py

<interfaces>
<!-- Текущий conftest.py имеет fixtures: bot_token, owner_tg_id, internal_token, db_session, async_client. -->
<!-- Здесь добавляем НОВЫЙ fixture two_tenants (только TENANT-A id и TENANT-B id; настоящие данные seed'нем в 11-07 verify) -->

# Существующий db_session fixture (tests/conftest.py:142):
@pytest_asyncio.fixture
async def db_session():
    """Real async DB session for integration tests that need real Postgres."""
    db_url = os.environ.get("DATABASE_URL", "")
    if not db_url:
        pytest.skip("DATABASE_URL not set — integration test requires DB")
    # ... creates async engine, yields session
    # На момент 11-01 — db_session НЕ используется новыми тестами для seed'а
    # (модели ещё без user_id). Используется только для probe-skip.

# Сигнатура fixture two_tenants (планируется к расширению в 11-07):
@pytest_asyncio.fixture
async def two_tenants(db_session) -> tuple[int, int]:
    """Создаёт двух юзеров (user_a_id, user_b_id) с разными tg_user_id.
    На момент 11-01: возвращает hardcoded (1, 2) с pytest.skip если db_session недоступен.
    На момент 11-07: реально создаёт AppUser строки + seed категории/транзакции для каждого.
    """
    ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить tests/conftest.py — добавить two_tenants fixture skeleton</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (целиком — нужно понимать существующие fixtures и не сломать их)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (раздел "specifics" про 2-tenant fixture pattern)
  </read_first>
  <action>
    В конец tests/conftest.py (после существующего async_client fixture, около строки 236) ДОБАВИТЬ новый fixture:

    ```python
    @pytest_asyncio.fixture
    async def two_tenants(db_session) -> tuple[int, int]:
        """Two-tenant fixture — возвращает (user_a_id, user_b_id).

        На момент Phase 11-01 (RED): возвращает фиктивные id-ы (1, 2) и НЕ создаёт
        реальные AppUser строки — миграция ещё не применена и user_id колонок
        в доменных таблицах нет, seed бессмысленен.

        В Plan 11-07 (verification) этот fixture будет расширен: реально INSERT
        двух AppUser строк + seed по 2-3 категории и транзакции для каждого
        (категории с одинаковыми именами в обоих tenant — для проверки unique
        scoped по user_id), потом возвращает их PK id-ы. Тесты multitenancy
        опираются на это.

        В 11-01 — placeholder: pytest.skip с reason="multitenancy fixture not
        yet implemented — filled in Plan 11-07" чтобы все тесты, использующие
        этот fixture, явно skip'ались а не passing-by-accident.
        """
        import pytest as _pytest
        _pytest.skip(
            "two_tenants fixture is a Plan 11-01 RED placeholder — "
            "real implementation lands in Plan 11-07"
        )
        # Unreachable, но Python требует return-path для type hint:
        return (1, 2)
    ```

    НЕ удалять и НЕ переименовывать существующие fixtures (bot_token, owner_tg_id, internal_token, db_session, async_client). Импорт `pytest_asyncio` уже есть на строке 10.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner &amp;&amp; grep -c 'def two_tenants' tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'async def two_tenants' tests/conftest.py` returns `1`
    - `grep -c 'pytest.skip' tests/conftest.py` returns at least `1` (новый skip + возможно старые)
    - Существующие fixture-сигнатуры не изменены: `grep -E "def (bot_token|owner_tg_id|internal_token|db_session|async_client)" tests/conftest.py` returns 5+ matches
    - Файл синтаксически корректен: `python -c "import ast; ast.parse(open('tests/conftest.py').read())"` exits 0
  </acceptance_criteria>
  <done>two_tenants fixture добавлен с pytest.skip placeholder; существующие fixtures не сломаны; файл парсится Python AST</done>
</task>

<task type="auto">
  <name>Task 2: Создать tests/test_multitenancy_isolation.py — RED skeleton</name>
  <files>tests/test_multitenancy_isolation.py</files>
  <read_first>
    - tests/conftest.py (только-что обновлённый — нужен two_tenants fixture)
    - tests/test_categories.py (как пример формата integration теста — заголовки docstring, structure)
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (раздел "Testing Strategy")
  </read_first>
  <action>
    Создать НОВЫЙ файл tests/test_multitenancy_isolation.py со следующим содержимым:

    ```python
    """Multi-tenancy isolation integration tests — Phase 11 (MUL-03).

    RED phase (Plan 11-01): эти тесты намеренно падают с NotImplementedError;
    они заполняются реальной логикой в Plan 11-07 после того как:
      - 11-02 применил миграцию с user_id + RLS
      - 11-03 обновил ORM модели
      - 11-04..06 добавил user_id фильтрацию во всех queries

    Каждый тест проверяет одно из:
      1. test_user_a_does_not_see_user_b_categories — list_categories(user_a)
         НЕ возвращает категории user_b
      2. test_user_a_cannot_get_user_b_category_by_id — direct GET category/{B_id}
         от user_a возвращает 404, не 200 (или 403 — выбрать)
      3. test_user_a_cannot_get_user_b_subscription_by_id — то же для subscription
      4. test_user_a_cannot_see_user_b_planned_transactions — list query изолирован
      5. test_user_a_cannot_see_user_b_actual_transactions — list query изолирован
      6. test_unique_category_name_scoped_per_user — оба tenant могут иметь
         категорию "Продукты" одновременно (MUL-04)
    """
    from __future__ import annotations

    import pytest


    pytestmark = pytest.mark.asyncio


    async def test_user_a_does_not_see_user_b_categories(two_tenants, db_session):
        """MUL-03: list_categories для user_a не должен возвращать категории user_b."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: implement after services accept user_id parameter "
            "(11-05/11-06). Call list_categories(db_session, user_id=user_a_id) "
            "and assert no row has user_id == user_b_id."
        )


    async def test_user_a_cannot_get_user_b_category_by_id(two_tenants, db_session):
        """MUL-03: прямой GET category by id user_b от имени user_a → 404."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: после refactor get_or_404(db, category_id, user_id) "
            "вызвать с user_id=user_a и category_id принадлежащим user_b — "
            "ожидать CategoryNotFoundError."
        )


    async def test_user_a_cannot_get_user_b_subscription_by_id(two_tenants, db_session):
        """MUL-03: subscription lookup by id у чужого юзера → not found."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: вызвать get_subscription(db, sub_id, user_id) и "
            "ожидать SubscriptionNotFoundError или None."
        )


    async def test_user_a_cannot_see_user_b_planned_transactions(two_tenants, db_session):
        """MUL-03: list_planned для user_a не возвращает строки user_b."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: list_planned_for_period(db, period_id, user_id=user_a) — "
            "assert все returned rows .user_id == user_a_id."
        )


    async def test_user_a_cannot_see_user_b_actual_transactions(two_tenants, db_session):
        """MUL-03: list_actuals для user_a не возвращает строки user_b."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: list_actuals_for_period(db, period_id, user_id=user_a) — "
            "assert все returned rows .user_id == user_a_id."
        )


    async def test_unique_category_name_scoped_per_user(two_tenants, db_session):
        """MUL-04: оба tenant могут иметь категорию 'Продукты' (unique scoped per user_id)."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: создать Category(name='Продукты', user_id=user_a_id) и "
            "Category(name='Продукты', user_id=user_b_id) — обе должны успешно "
            "INSERT'нуться без IntegrityError."
        )
    ```

    Файл — pure RED placeholder; все тесты raise NotImplementedError. Они НЕ должны проходить — pytest должен показать 6 FAIL.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner &amp;&amp; python -c "import ast; ast.parse(open('tests/test_multitenancy_isolation.py').read())" &amp;&amp; grep -c 'NotImplementedError' tests/test_multitenancy_isolation.py</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_multitenancy_isolation.py` exists
    - `grep -c "^async def test_" tests/test_multitenancy_isolation.py` returns `6`
    - `grep -c "NotImplementedError" tests/test_multitenancy_isolation.py` returns at least `6`
    - File is valid Python: `python -c "import ast; ast.parse(open('tests/test_multitenancy_isolation.py').read())"` exits 0
    - Imports `two_tenants` fixture (используется в каждом тесте через параметр)
  </acceptance_criteria>
  <done>RED skeleton tests/test_multitenancy_isolation.py создан с 6 namedtest-функциями; каждая raise NotImplementedError с pointer на Plan 11-07</done>
</task>

<task type="auto">
  <name>Task 3: Создать tests/test_rls_policy.py + tests/test_migration_backfill.py — RED skeletons</name>
  <files>tests/test_rls_policy.py, tests/test_migration_backfill.py</files>
  <read_first>
    - .planning/phases/11-multi-tenancy-db-migration/11-CONTEXT.md (разделы RLS Strategy + specifics — RLS verification snippet)
    - alembic/versions/0004_pgvector_embeddings.py (как пример структуры raw SQL операций)
  </read_first>
  <action>
    Создать НОВЫЙ файл tests/test_rls_policy.py со следующим содержимым:

    ```python
    """RLS policy integration tests — Phase 11 (MUL-02).

    RED phase (Plan 11-01): тесты raise NotImplementedError. Заполнение в Plan 11-07.

    Идея тестов:
      1. Без SET LOCAL app.current_user_id — query на любую доменную таблицу
         возвращает 0 rows (RLS coalesce(setting, -1) даёт filter user_id = -1
         который не матчит ни одну строку).
      2. С SET LOCAL app.current_user_id = user_a_id — query возвращает только
         строки user_a (никаких user_b).
      3. SET LOCAL — transaction-scoped: после COMMIT/ROLLBACK setting сбрасывается.
    """
    from __future__ import annotations

    import pytest
    from sqlalchemy import text


    pytestmark = pytest.mark.asyncio


    async def test_rls_blocks_query_without_setting(two_tenants, db_session):
        """MUL-02: SELECT без app.current_user_id должен вернуть 0 rows."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: открыть transaction без SET LOCAL app.current_user_id, "
            "выполнить SELECT count(*) FROM category — ожидать 0 (RLS coalesce "
            "к -1 не матчит). Verify сразу что SET LOCAL = user_a_id даёт "
            "только категории user_a."
        )


    async def test_rls_filters_by_app_current_user_id(two_tenants, db_session):
        """MUL-02: SELECT с SET LOCAL app.current_user_id = user_a_id → видим только user_a данные."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: SET LOCAL app.current_user_id = :user_a_id, потом "
            "SELECT user_id FROM category — все возвращённые user_id == user_a_id. "
            "Аналогично для actual_transaction, planned_transaction, subscription."
        )


    async def test_rls_setting_resets_after_commit(two_tenants, db_session):
        """MUL-02: после COMMIT app.current_user_id сбрасывается (SET LOCAL = transaction scope)."""
        user_a_id, user_b_id = two_tenants
        raise NotImplementedError(
            "Plan 11-07: в одной transaction SET LOCAL = user_a_id, COMMIT, "
            "затем в новой transaction (без SET LOCAL) SELECT — должен снова "
            "вернуть 0 rows (setting сброшен)."
        )


    async def test_rls_enabled_on_all_nine_tables(db_session):
        """MUL-02: pg_class.relrowsecurity = true на всех 9 доменных таблицах."""
        raise NotImplementedError(
            "Plan 11-07: SELECT relname, relrowsecurity, relforcerowsecurity "
            "FROM pg_class WHERE relname IN ('category', 'budget_period', "
            "'plan_template_item', 'planned_transaction', 'actual_transaction', "
            "'subscription', 'category_embedding', 'ai_conversation', 'ai_message') — "
            "все 9 строк должны иметь relrowsecurity = True."
        )
    ```

    И второй файл tests/test_migration_backfill.py:

    ```python
    """Migration backfill integration test — Phase 11 (MUL-05).

    RED phase (Plan 11-01): тест raise NotImplementedError. Заполнение в Plan 11-07.

    Что проверяет:
      1. После alembic upgrade head все user_id колонки доменных таблиц
         НЕ NULL (миграция backfill'нула).
      2. Все user_id равны id у app_user строки с tg_user_id == OWNER_TG_ID
         (backfill через subquery работает).
      3. app_user.role существует и равен 'owner' для OWNER_TG_ID-юзера.
      4. Unique constraint category(user_id, name) существует, старый
         глобальный unique (если был) убран.
    """
    from __future__ import annotations

    import pytest
    from sqlalchemy import text


    pytestmark = pytest.mark.asyncio


    async def test_user_id_backfilled_to_owner(db_session):
        """MUL-05: после миграции все user_id == app_user.id WHERE tg_user_id = OWNER_TG_ID."""
        raise NotImplementedError(
            "Plan 11-07: для каждой из 9 таблиц SELECT count(*) WHERE user_id != "
            "(SELECT id FROM app_user WHERE tg_user_id = OWNER_TG_ID) — должно "
            "быть 0. Также SELECT count(*) WHERE user_id IS NULL — 0. "
            "Запускается на seed-БД где dev_seed создал OWNER + образцы данных."
        )


    async def test_role_owner_assigned_to_owner_tg_id(db_session):
        """ROLE-01: app_user.role = 'owner' для существующего OWNER_TG_ID юзера."""
        raise NotImplementedError(
            "Plan 11-07: SELECT role FROM app_user WHERE tg_user_id = OWNER_TG_ID — "
            "ожидать строковое значение 'owner'."
        )


    async def test_user_role_enum_type_exists(db_session):
        """ROLE-01: postgres enum user_role существует с тремя значениями."""
        raise NotImplementedError(
            "Plan 11-07: SELECT enumlabel FROM pg_enum e JOIN pg_type t ON "
            "e.enumtypid = t.oid WHERE t.typname = 'user_role' ORDER BY enumsortorder — "
            "ожидать ['owner', 'member', 'revoked']."
        )


    async def test_category_unique_scoped_per_user(db_session):
        """MUL-04: unique constraint category(user_id, name) — old global unique (если был) удалён."""
        raise NotImplementedError(
            "Plan 11-07: SELECT con.conname FROM pg_constraint con "
            "JOIN pg_class cl ON con.conrelid = cl.oid WHERE cl.relname = 'category' "
            "AND con.contype = 'u' — ожидать имя содержащее 'user_id' (например "
            "'uq_category_user_id_name'). Старый uq_category_name (глобальный) "
            "не должен существовать (его в 0001 не было — но проверяем что "
            "новый scoped есть)."
        )
    ```

    Оба файла — pure RED placeholders.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner &amp;&amp; python -c "import ast; ast.parse(open('tests/test_rls_policy.py').read()); ast.parse(open('tests/test_migration_backfill.py').read())" &amp;&amp; grep -c 'NotImplementedError' tests/test_rls_policy.py tests/test_migration_backfill.py</automated>
  </verify>
  <acceptance_criteria>
    - Files `tests/test_rls_policy.py` and `tests/test_migration_backfill.py` exist
    - `grep -c "^async def test_" tests/test_rls_policy.py` returns `4`
    - `grep -c "^async def test_" tests/test_migration_backfill.py` returns `4`
    - Both files raise NotImplementedError (`grep -c "NotImplementedError" tests/test_rls_policy.py` >= 4, same for backfill)
    - Both files parse as valid Python
  </acceptance_criteria>
  <done>RED skeletons для RLS policy testing и backfill verification созданы; все 8 тестов raise NotImplementedError с pointer на Plan 11-07</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test author → CI | RED тесты должны fail в CI/local pytest run чтобы не дать ложного зелёного — иначе Plan 11-07 пропустит реальную проверку |
| pytest fixture → DB | two_tenants на момент 11-01 — pytest.skip placeholder; не трогает БД (миграция ещё не применена) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | Future executor «накатывает» pass-by-default тесты | mitigate | Все тесты raise NotImplementedError EXPLICITLY (не `pass`/`assert True`); pytest показывает FAIL не SKIP — sigh-checking невозможно пропустить |
| T-11-01-02 | Repudiation | Скиппинг без следа (untracked silent skip) | mitigate | two_tenants fixture использует pytest.skip с reason указывающим на Plan 11-07 — skip-line печатается в pytest output |
| T-11-01-03 | Information Disclosure | RED тесты содержат hardcoded sensitive id-ы / tokens | accept | Используются только placeholder `(1, 2)` — никаких prod-id-ов / токенов; OWNER_TG_ID берётся из существующего conftest.py fixture |
| T-11-01-04 | DoS | RED тесты тормозят CI | accept | NotImplementedError raise — мгновенный exit; pytest.skip — мгновенный skip; нагрузки нет |
</threat_model>

<verification>
1. `cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast; ast.parse(open('tests/conftest.py').read()); ast.parse(open('tests/test_multitenancy_isolation.py').read()); ast.parse(open('tests/test_rls_policy.py').read()); ast.parse(open('tests/test_migration_backfill.py').read())"` — все 4 файла парсятся.
2. `grep -c "async def two_tenants" tests/conftest.py` returns 1.
3. `grep -E "^async def test_" tests/test_multitenancy_isolation.py tests/test_rls_policy.py tests/test_migration_backfill.py | wc -l` returns >= 14 (6+4+4).
4. (Optional manual): `pytest tests/test_multitenancy_isolation.py tests/test_rls_policy.py tests/test_migration_backfill.py -x --tb=no -q 2>&1 | grep -E "FAILED|SKIPPED" | wc -l` — все тесты или fail или skip (НЕ pass).
</verification>

<success_criteria>
- 3 новых tests/*.py файла существуют (multitenancy, rls, migration_backfill)
- tests/conftest.py содержит two_tenants fixture (skip placeholder)
- 14 RED тестов в сумме (6 + 4 + 4); каждый raise NotImplementedError с pointer на Plan 11-07
- Никаких изменений в существующем коде (app/* остаётся прежним)
- Файлы парсятся как валидный Python
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-tenancy-db-migration/11-01-SUMMARY.md` documenting:
- Список созданных файлов (4 файла: conftest расширен, 3 новых test файла)
- Сигнатуры всех 14 RED тестов (имя + одна строка о цели)
- Pointer для Plan 11-07: «эти тесты заполняются после применения миграции и refactor сервисного слоя»
</output>
