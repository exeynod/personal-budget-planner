---
phase: 67-remediation-cleanup
plan: 09
type: execute
wave: 4
depends_on: [67-02, 67-06]
files_modified:
  - frontend/src/hooks/useAiCategorize.ts
  - frontend/src/screensV10/Subscriptions/computeSubscriptions.ts
  - frontend/src/utils/parseMoney.ts
  - frontend/src/utils/parseWireDate.ts
  - frontend/src/screensV10/Savings/SavingsMount.tsx
  - frontend/src/screensV10/Plan/PlanMount.tsx
  - frontend/src/screensV10/Management/SettingsMount.tsx
  - frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx
  - frontend/src/screensV10/Subscriptions/SubscriptionsMount.tsx
  - frontend/src/screensV10/Transactions/TransactionsMount.tsx
  - frontend/src/screensV10/Accounts/AccountsListMount.tsx
autonomous: true
requirements: [P2-8, P2-9, P2-10, P2-11, R5]
must_haves:
  truths:
    - "useAiCategorize ignores stale in-flight results (cancelled-flag guard)"
    - "Wire DATE strings parse in local time (no UTC off-by-one)"
    - "A single parseRublesToKopecks is the only money parser; no kopeck loss"
    - "v10 mounts use the Toast component instead of window.alert"
  artifacts:
    - path: "frontend/src/utils/parseMoney.ts"
      provides: "centralized parseRublesToKopecks"
      contains: "parseRublesToKopecks"
    - path: "frontend/src/utils/parseWireDate.ts"
      provides: "local-time wire DATE parser"
      contains: "parseWireDate"
  key_links:
    - from: "v10 sheets"
      to: "parseRublesToKopecks"
      via: "single import"
      pattern: "parseRublesToKopecks"
    - from: "v10 mounts"
      to: "Toast"
      via: "replace window.alert"
      pattern: "Toast"
---

<objective>
Web P2 minors + R5 (alert→Toast + parser centralization):
- **P2-8 (FE-F5):** `useAiCategorize` has no stale-guard on in-flight requests →
  add a `let cancelled = false` effect-cleanup guard.
- **P2-9 (FE-F6):** `new Date("YYYY-MM-DD")` parses as UTC → off-by-one in local
  time; use a local parser `new Date(y, m-1, d)` (centralized as `parseWireDate`).
- **P2-10 (FE-F7):** three different money parsers in sheets drop kopecks →
  centralize one `parseRublesToKopecks`.
- **P2-11 (FE-F8):** `window.alert` used in multiple v10 mounts → unify on `Toast`.
- **R5:** the alert→Toast + money/date parser centralization items (dead-shell
  deletion stays deferred — see 67-06 inventory).

Purpose: correctness (no kopeck/day loss, no stale AI hint) + consistent UX.
Output: stale-guard, local date parse, single money parser, Toast everywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P2-8..11 (92-95), R5 (121-123)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- window.alert mounts: SavingsMount, PlanMount, SettingsMount, CategoryDetailMount,
     SubscriptionsMount, TransactionsMount, AccountsListMount -->
<!-- money parsers scattered: Home/Savings/CategoryDetail/Analytics compute*.ts + sheets -->
<!-- date parse: screensV10/Subscriptions/computeSubscriptions.ts (new Date("YYYY-MM-DD")) -->
<!-- existing Toast component: grep 'Toast' under screensV10/common or components for the import path -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useAiCategorize stale-guard + parseWireDate local parser (P2-8, P2-9)</name>
  <files>frontend/src/hooks/useAiCategorize.ts, frontend/src/utils/parseWireDate.ts, frontend/src/screensV10/Subscriptions/computeSubscriptions.ts</files>
  <read_first>
    - frontend/src/hooks/useAiCategorize.ts (the in-flight effect)
    - frontend/src/screensV10/Subscriptions/computeSubscriptions.ts (new Date site)
    - existing date helpers under frontend/src/utils or api (avoid duplicating)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-8 (92), P2-9 (93)
  </read_first>
  <action>
    1. useAiCategorize: add `let cancelled = false;` at the top of the effect; in the
       async resolution bail (no setState) if `cancelled`; return
       `() => { cancelled = true; }` cleanup so a superseded query never overwrites a
       newer result.
    2. Create `frontend/src/utils/parseWireDate.ts` exporting
       `parseWireDate(s: string): Date` that parses `YYYY-MM-DD` as LOCAL
       (`new Date(y, m-1, d)`) and leaves full ISO timestamps to `new Date(s)`.
       Replace the `new Date("YYYY-MM-DD")` usage in computeSubscriptions.ts with it.
    No new deps.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && grep -q "cancelled" src/hooks/useAiCategorize.ts && grep -q "parseWireDate" src/screensV10/Subscriptions/computeSubscriptions.ts && npm run build</automated>
  </verify>
  <done>Stale-guard in useAiCategorize; parseWireDate created + used; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize parseRublesToKopecks (P2-10)</name>
  <files>frontend/src/utils/parseMoney.ts, frontend/src/screensV10/Savings/computeSavings.ts, frontend/src/screensV10/CategoryDetail/computeCategoryDetail.ts</files>
  <read_first>
    - grep the 3 divergent money parsers (sheets / compute*.ts) — note each formula
    - existing MoneyFormatter / money utils to colocate
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-10 (94)
  </read_first>
  <action>
    Create `frontend/src/utils/parseMoney.ts` exporting `parseRublesToKopecks(s):
    number` — a single rubles→kopecks parser that does NOT lose kopecks (handle
    comma/dot decimal, spaces, up to 2 fractional digits; round to integer
    kopecks). Replace the 3 ad-hoc parsers in the sheets / compute files with
    imports of this function. Add a vitest unit test if a vitest setup exists
    ("1 234,56" → 123456, "10" → 1000, "0,1" → 10). BIGINT-cents discipline.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && grep -q "parseRublesToKopecks" src/utils/parseMoney.ts && npm run build</automated>
  </verify>
  <done>Single parseRublesToKopecks; 3 ad-hoc parsers replaced; no kopeck loss; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Replace window.alert with Toast across v10 mounts (P2-11, R5)</name>
  <files>frontend/src/screensV10/Savings/SavingsMount.tsx, frontend/src/screensV10/Plan/PlanMount.tsx, frontend/src/screensV10/Management/SettingsMount.tsx, frontend/src/screensV10/CategoryDetail/CategoryDetailMount.tsx, frontend/src/screensV10/Subscriptions/SubscriptionsMount.tsx, frontend/src/screensV10/Transactions/TransactionsMount.tsx, frontend/src/screensV10/Accounts/AccountsListMount.tsx</files>
  <read_first>
    - each *Mount.tsx window.alert call site
    - the existing Toast component (grep 'Toast' under screensV10 / components) for its API
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-11 (95), R5 (123)
  </read_first>
  <action>
    Replace each `window.alert(...)` in the listed v10 mounts with the existing
    `Toast` component / hook (match how other v10 screens already surface Toasts).
    Preserve the message text. If a mount has no Toast host, add the minimal host
    consistent with sibling mounts. No new deps.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && test "$(grep -rl 'window.alert' src/screensV10/ 2>/dev/null | wc -l | tr -d ' ')" = "0" && npm run build</automated>
  </verify>
  <done>No window.alert in screensV10; mounts use Toast; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user input → money parse | rubles string must convert to exact kopecks |
| wire DATE → local Date | UTC parse must not shift business dates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-09-01 | Tampering | money parser | mitigate | Single parseRublesToKopecks rounds to integer kopecks — no silent loss across screens. |
| T-67-09-02 | Information Disclosure | useAiCategorize stale result | accept | Stale-guard prevents wrong category hint render; auxiliary path, no data write. |
| T-67-09-03 | Repudiation | wire DATE off-by-one | mitigate | parseWireDate local-parse keeps business dates on the correct day. |
</threat_model>

<verification>
- grep cancelled / parseWireDate / parseRublesToKopecks present; window.alert count 0 in screensV10.
- `npm run build` green after each task.
</verification>

<success_criteria>
Stale-guard added; local wire-date parse; single money parser; Toast replaces window.alert; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-09-SUMMARY.md`
</output>
