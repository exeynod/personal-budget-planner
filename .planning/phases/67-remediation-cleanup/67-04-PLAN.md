---
phase: 67-remediation-cleanup
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - app/api/routes/categories.py
  - app/services/subscriptions.py
  - alembic/versions/0025_subscription_posted_txn_unique.py
  - tests/test_categories.py
  - tests/test_subscriptions.py
autonomous: true
requirements: [P1-1, P1-2, P2-13]
must_haves:
  truths:
    - "Category embedding is created after onboarding/create without TypeError"
    - "_refresh_embedding background session sets tenant scope before upsert"
    - "Two concurrent POST /subscriptions/{id}/post produce exactly one ActualTransaction"
    - "A partial-failure inside post_subscription rolls back via savepoint, no orphan txn"
  artifacts:
    - path: "app/api/routes/categories.py"
      provides: "_refresh_embedding with user_id + set_tenant_scope"
      contains: "set_tenant_scope"
    - path: "app/services/subscriptions.py"
      provides: "post_subscription with FOR UPDATE row lock / unique guard"
      contains: "with_for_update"
    - path: "alembic/versions/0025_subscription_posted_txn_unique.py"
      provides: "partial unique index on posted_txn_id WHERE NOT NULL"
      contains: "posted_txn_id"
  key_links:
    - from: "_refresh_embedding"
      to: "upsert_category_embedding"
      via: "kw-only user_id passed through"
      pattern: "upsert_category_embedding\\([^)]*user_id"
    - from: "post_subscription"
      to: "Subscription row"
      via: "SELECT ... FOR UPDATE before idempotency check"
      pattern: "with_for_update"
---

<objective>
Close two backend P1s + the QA savepoint test:
- **P1-1 (BE-F2):** `_refresh_embedding` calls `upsert_category_embedding` without
  the kw-only `user_id` (→ TypeError, swallowed) and its background session never
  calls `set_tenant_scope` → user-category suggest silently degrades to
  substring-fallback only. Pass `user_id` through and set tenant scope.
- **P1-2 (BE-F4):** `/subscriptions/{id}/post` idempotency is in-memory only
  (`posted_txn_id is None`) with no row lock / unique constraint. Two parallel
  posts → two ActualTransactions + double balance delta + orphan txn. Add
  `SELECT ... FOR UPDATE` on the subscription at the top of `post_subscription`
  AND a partial unique index `posted_txn_id WHERE NOT NULL` (migration) →
  IntegrityError → 409.
- **P2-13 (QA-F10):** add a test for partial-failure inside post (SAVEPOINT
  rollback — no orphan).

Purpose: correctness + concurrency safety on a money path; working AI suggest.
Output: fixed embedding refresh, race-safe post, migration, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P1-1 (lines 46-49), P1-2 (lines 51-54), P2-13 (line 97)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- app/api/routes/categories.py:91-118 -->
async def _refresh_embedding(category_id: int, name: str) -> None:
    ... AsyncSessionLocal() as session:
        await embedding_svc.upsert_category_embedding(session, category_id, vector)  # MISSING user_id
        await session.commit()
# create_category :87 / update_category :163 call: background_tasks.add_task(_refresh_embedding, cat.id, cat.name)
# set_tenant_scope: see app/db/session.py / app/api/dependencies.py (get_db_with_tenant_scope sets SET LOCAL app.current_user_id)

<!-- app/services/subscriptions.py:390-431 post_subscription -->
sub = await db.scalar(select(Subscription).where(id==sub_id, user_id==user_id))  # add .with_for_update()
if sub.posted_txn_id is not None: raise SubscriptionAlreadyPostedError(...)
... create_actual_v10(...); sub.posted_txn_id = parent.id; await db.flush()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix _refresh_embedding (P1-1) — user_id + tenant scope + test</name>
  <files>app/api/routes/categories.py, tests/test_categories.py</files>
  <read_first>
    - app/api/routes/categories.py (_refresh_embedding :91-118, create :72-88, update :146-163)
    - app/ai/embedding_service.py (upsert_category_embedding signature — confirm kw-only user_id)
    - app/db/session.py + app/api/dependencies.py (set_tenant_scope helper / SET LOCAL pattern)
    - tests/test_categories.py + tests/test_embedding_backfill.py (fixture style)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-1 (lines 46-49)
  </read_first>
  <behavior>
    - Post-onboarding (or POST /categories) for a user category → embedding row is
      created for that category (no TypeError swallowed).
    - The background session has tenant scope set before the upsert (RLS lets the
      insert see/write the row).
  </behavior>
  <action>
    Thread `user_id` into `_refresh_embedding`:
      - change signature to `_refresh_embedding(category_id, name, user_id)`;
      - both `background_tasks.add_task(_refresh_embedding, cat.id, cat.name)` call
        sites (create_category :87, update_category :163) pass `user_id`;
      - inside the `async with AsyncSessionLocal() as session:` block, call
        `await set_tenant_scope(session, user_id)` (use the existing helper / the
        same `SET LOCAL app.current_user_id` mechanism used by
        get_db_with_tenant_scope) BEFORE upsert;
      - pass `user_id=user_id` (kw-only) to `upsert_category_embedding(...)`.
    Respect RLS/tenant scope (CLAUDE.md). No float, no new libs.
    Add a test in tests/test_categories.py asserting an embedding is persisted for
    a freshly created user category (gate on ENABLE_AI_CATEGORIZATION / mock the
    embedding service's embed_text to return a fixed vector; assert upsert ran with
    user_id, e.g. via the category embedding row existing).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_categories.py -x -q -k "embed or refresh"</automated>
  </verify>
  <done>_refresh_embedding passes user_id + sets tenant scope; no TypeError; test proves embedding created for a user category.</done>
</task>

<task type="auto">
  <name>Task 2: Double-post race guard (P1-2) — FOR UPDATE + partial unique index migration</name>
  <files>app/services/subscriptions.py, alembic/versions/0025_subscription_posted_txn_unique.py</files>
  <read_first>
    - app/services/subscriptions.py (post_subscription :360-431)
    - app/api/routes/subscriptions.py (post_subscription handler :220-272 — IntegrityError→409 mapping)
    - alembic/versions/ (most recent: 0024_analytics_event.py — copy revision/down_revision header style)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-2 (lines 51-54)
  </read_first>
  <action>
    Two-pronged defence:
    1. Row lock: in `post_subscription`, add `.with_for_update()` to the
       `select(Subscription).where(id==sub_id, user_id==user_id)` so concurrent
       posts serialise on the row; the `posted_txn_id is not None` check then sees
       the committed prior post → SubscriptionAlreadyPostedError → 409.
    2. Belt-and-braces unique constraint: new alembic migration
       `0025_subscription_posted_txn_unique.py` (revision after 0024) creating a
       PARTIAL unique index:
         CREATE UNIQUE INDEX uq_subscription_posted_txn_id
           ON subscription (posted_txn_id) WHERE posted_txn_id IS NOT NULL;
       (op.create_index with postgresql_where=sa.text("posted_txn_id IS NOT NULL"),
       unique=True). Provide a downgrade dropping the index.
    3. Ensure the route maps a DB IntegrityError on this index to HTTP 409
       (reuse/extend the SubscriptionAlreadyPostedError path; if the unique
       violation surfaces as IntegrityError, catch it in the service and raise
       SubscriptionAlreadyPostedError, OR map IntegrityError→409 in the handler).
    Money stays BIGINT cents. Tenant scope preserved.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import ast,glob;f=[p for p in glob.glob('alembic/versions/0025_*') ][0];src=open(f).read();assert 'posted_txn_id' in src and 'unique' in src.lower()" && grep -n "with_for_update" app/services/subscriptions.py</automated>
  </verify>
  <done>post_subscription locks the sub row with FOR UPDATE; migration 0025 adds partial unique index; IntegrityError maps to 409.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests — double-post idempotency + partial-failure savepoint (P1-2, P2-13)</name>
  <files>tests/test_subscriptions.py</files>
  <read_first>
    - tests/test_subscriptions.py (existing post/unpost tests + savepoint helpers)
    - app/services/subscriptions.py (post_subscription final shape after Task 2)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-2 (line 53), P2-13 (line 97)
  </read_first>
  <behavior>
    - Two posts of the same subscription → exactly one ActualTransaction exists,
      second call yields 409 (AlreadyPosted), posted_txn_id set once.
    - Partial failure mid-post (e.g. forced error after create_actual_v10 but before
      flush, via monkeypatch) → SAVEPOINT/transaction rollback leaves no orphan
      ActualTransaction and posted_txn_id stays NULL.
  </behavior>
  <action>
    Add two tests mirroring existing test_subscriptions.py style: (a) sequential
    double-post asserting single txn + 409; (b) partial-failure rollback via
    monkeypatch that raises after the actual is created — assert
    ActualTransaction count unchanged and sub.posted_txn_id is None.
    (Sequential simulation of the race is acceptable; the FOR UPDATE + unique index
    guarantee the property under true concurrency.)
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_subscriptions.py -x -q -k "double or idempot or savepoint or rollback or orphan"</automated>
  </verify>
  <done>Both tests pass: single-txn idempotency + savepoint rollback no-orphan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API (money) | POST /subscriptions/{id}/post mutates balance — must be idempotent |
| background task→DB | _refresh_embedding runs outside request; needs explicit tenant scope |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04-01 | Tampering | post_subscription | mitigate | SELECT ... FOR UPDATE + partial unique index → at most one ActualTransaction per posted subscription; second post → 409. |
| T-67-04-02 | Repudiation | money double-charge | mitigate | Savepoint rollback test proves no orphan txn on partial failure. |
| T-67-04-03 | Information Disclosure | _refresh_embedding bg session | mitigate | set_tenant_scope on background session keeps embeddings RLS-scoped to the owning user. |
</threat_model>

<verification>
- pytest test_categories (embed) + test_subscriptions (double/savepoint) green.
- grep with_for_update in post_subscription; migration 0025 has partial unique index.
- Full backend suite for touched modules: `pytest tests/test_subscriptions.py tests/test_categories.py -q`.
</verification>

<success_criteria>
Embedding refresh works post-onboarding (user_id + tenant scope); double-post yields single txn + 409; savepoint rollback leaves no orphan; migration 0025 present.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-04-SUMMARY.md`
</output>
