---
phase: 67-remediation-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/v10/analytics.ts
  - frontend/src/screensV10/Ai/AiView.tsx
autonomous: true
requirements: [P0-2]
must_haves:
  truths:
    - "npm run build (tsc -b && vite build) exits 0"
    - "AnalyticsRange resolves to its real declaration, not a missing types export"
    - "bottomRef type matches the <li> element it is attached to"
  artifacts:
    - path: "frontend/src/api/v10/analytics.ts"
      provides: "correct AnalyticsRange import from '../analytics'"
      contains: "from '../analytics'"
    - path: "frontend/src/screensV10/Ai/AiView.tsx"
      provides: "bottomRef typed to its host element"
      contains: "bottomRef"
  key_links:
    - from: "frontend/src/api/v10/analytics.ts"
      to: "frontend/src/api/analytics.ts"
      via: "type import of AnalyticsRange"
      pattern: "import.*AnalyticsRange.*from '\\.\\./analytics'"
---

<objective>
Close **P0-2** (review-doc FE-F1/F2/F3, BLOCKER): `npm run build` runs
`tsc -b && vite build` and fails (exit 2). `vite build` alone is green, masking
two type errors:
1. `frontend/src/api/v10/analytics.ts:19` imports `AnalyticsRange` from `'../types'`,
   but it is declared/exported in `'../analytics'` (confirmed: analytics.ts:9
   `export type AnalyticsRange = '1M' | '3M' | '6M' | '12M'`).
2. `frontend/src/screensV10/Ai/AiView.tsx:182` attaches `bottomRef` (typed
   `HTMLDivElement`) to a `<li>` element — element/ref type mismatch.

Purpose: restore a green tsc-gated production build.
Output: `npm run build` exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P0-2 (lines 31-36)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- frontend/src/api/analytics.ts:9 -->
export type AnalyticsRange = '1M' | '3M' | '6M' | '12M';
<!-- frontend/src/api/v10/analytics.ts:19 (BROKEN) -->
import type { TopCategoriesResponse, AnalyticsRange } from '../types';
<!-- frontend/src/screensV10/Ai/AiView.tsx -->
const bottomRef = useRef<HTMLDivElement | null>(null);   // :81
<li ref={bottomRef} className={styles.bottomAnchor} aria-hidden />  // :182
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix AnalyticsRange import + bottomRef element type</name>
  <read_first>
    - frontend/src/api/v10/analytics.ts (import line :19)
    - frontend/src/api/analytics.ts (:9 export of AnalyticsRange)
    - frontend/src/api/types.ts (confirm TopCategoriesResponse lives here; AnalyticsRange does NOT)
    - frontend/src/screensV10/Ai/AiView.tsx (:81 ref decl, :182 host element)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P0-2 (lines 31-36)
  </read_first>
  <action>
    1. In `frontend/src/api/v10/analytics.ts:19`: split the import so `TopCategoriesResponse`
       stays from `'../types'` and `AnalyticsRange` comes from `'../analytics'`:
         import type { TopCategoriesResponse } from '../types';
         import type { AnalyticsRange } from '../analytics';
       (Verify TopCategoriesResponse is actually in '../types'; if it too lives in
       '../analytics', import both from there.)
    2. In `frontend/src/screensV10/Ai/AiView.tsx`: align the ref with its host element.
       Preferred (matches review "anchor <div>"): change the host to a `<div>` anchor
       (`<div ref={bottomRef} className={styles.bottomAnchor} aria-hidden />`). If the
       `<li>` must stay (semantic list), instead retype the ref:
       `useRef<HTMLLIElement | null>(null)`. Pick the lower-churn option for the
       surrounding JSX; do not change scroll behaviour.
    Do NOT introduce third-party libs (memory: no AI-pattern deps).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm run build</automated>
  </verify>
  <done>npm run build exits 0; no other tsc errors surface (if tests have tsc errors, note them — the build script gates src, fix only what the build touches).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build pipeline | tsc gate must run; type-only fixes, no runtime trust change |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02-01 | Tampering | build integrity | mitigate | Restoring tsc gate (npm run build) prevents shipping type-unsound bundles; CI should invoke `npm run build`, not bare `vite build`. |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0.
- grep `from '../analytics'` present for AnalyticsRange in v10/analytics.ts.
- AiView ref + host element types match (no TS2322).
</verification>

<success_criteria>
`npm run build` green (tsc -b passes); AnalyticsRange and bottomRef type errors gone; no new deps.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-02-SUMMARY.md`
</output>
