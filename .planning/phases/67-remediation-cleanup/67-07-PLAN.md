---
phase: 67-remediation-cleanup
plan: 07
type: execute
wave: 3
depends_on: [67-03, 67-05]
files_modified:
  - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift
  - ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift
  - ios/BudgetPlannerTests/Features/Savings/SavingsViewModelTests.swift
  - ios/BudgetPlannerTests/Features/Savings/GoalDetailViewModelTests.swift
  - ios/BudgetPlannerTests/Networking/APIClientForbiddenTests.swift
  - ios/BudgetPlannerTests/Networking/APIClientDateDecodeTests.swift
autonomous: true
requirements: [P1-4, P1-7, R2]
must_haves:
  truths:
    - "SavingsViewModel and GoalDetailViewModel take an injectable API struct seam (default .live)"
    - "deposit/createGoal/deleteGoal/optimistic-revert are behaviorally tested via stubbed seam"
    - "reloadPending coalescing exists on SavingsViewModel (mirrors SubscriptionsViewModel)"
    - "APIClient 403→logout (strict, post 67-03) and 401→always-logout have direct URLProtocol tests"
    - "MSK yyyy-MM-dd decode and timestamp formats are pinned by a decode test"
  artifacts:
    - path: "ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift"
      provides: "injectable API seam + reloadPending"
      contains: "struct API"
    - path: "ios/BudgetPlannerTests/Networking/APIClientForbiddenTests.swift"
      provides: "403/401 logout regression tests"
      contains: "onUnauthenticated"
    - path: "ios/BudgetPlannerTests/Networking/APIClientDateDecodeTests.swift"
      provides: "MSK date + timestamp decode pin"
      contains: "Europe/Moscow"
  key_links:
    - from: "SavingsViewModel"
      to: "injected API.live"
      via: "init(api:) defaulting to live"
      pattern: "init\\(api:"
    - from: "APIClientForbiddenTests"
      to: "onUnauthenticated"
      via: "URLProtocol stub asserts logout fires on 403/401"
      pattern: "onUnauthenticated"
---

<objective>
Close iOS seam + regression-test debt:
- **P1-4 / R2 (iOS-F3, QA-F1, QA-F2):** `SavingsViewModel`/`GoalDetailViewModel`
  call static APIs directly → money mutations (deposit/createGoal/deleteGoal/
  optimistic-revert) are untestable. Introduce an injectable `API` struct seam
  (exactly like `SubscriptionsViewModel.API`, defaulting to `.live`) and port the
  `reloadPending` coalescing pattern (SavingsViewModel lacks it → stale snapshot
  after deposit during pull-to-refresh). Add behavioural tests.
- **P1-7 (QA-F3, QA-F4):** `APIClient` 403-strict-logout (post 67-03) +
  401-always-logout and the MSK `yyyy-MM-dd` decode pin have ZERO direct tests.
  Add URLProtocol-stub tests: 403→logout, 401→logout; decode `"2027-01-01"` via
  the production decoder == MSK-midnight AND timestamp formats (createdAt) parse.

NOTE on ownership: this is the ONLY plan that modifies SavingsViewModel.swift /
GoalDetailViewModel.swift. It depends on 67-03 (APIClient final → tests assert
the right behaviour) and 67-05 (the `lastCreatedGoalId` dead-code removal deferred
from 67-05 completes here, on this owned file).

Purpose: make money mutations behaviorally testable; lock app-wide auth/date.
Output: seam + coalesce + behavioural tests + APIClient regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P1-4 (61-64), P1-7 (76-79), R2 (112-114)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- SEAM ETALON: ios/.../Features/Management/SubscriptionsView.swift (SubscriptionsViewModel) -->
struct API {
    var listSubs / listCategories / listAccounts / reschedule / post / unpost / delete / patch
    static let live = API( /* proxies static endpoints */ )
}
init(api: API = .live) { self.api = api }
// reloadPending pattern (SubscriptionsViewModel.load):
//   if inFlight { reloadPending = true; return }
//   defer { inFlight=false; if reloadPending { reloadPending=false; Task { await load() } } }

<!-- SavingsViewModel (no seam): SavingsAPI.summary/patchConfig/postDeposit,
     AccountsAPI.list, GoalsAPI.create/delete. load() has inFlight but NO reloadPending.
     dead: lastCreatedGoalId (:57) + clearLastCreatedGoalId() (:200). -->
<!-- GoalDetailViewModel (no seam): GoalsAPI.list/delete, AccountsAPI.list, SavingsAPI.postDeposit. -->
<!-- _setStateForTesting backdoor already exists on both. -->

<!-- APIClient (FINAL post-67-03): 401 → onUnauthenticated + .unauthorized;
     403 → if !skipAuth { onUnauthenticated() } + .forbidden; no suppress flag.
     date decoder: yyyy-MM-dd pinned to Europe/Moscow; ISO8601 + fractional + timestamp.
     init(baseURL:session:) + var onUnauthenticated are injectable. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject API seam + reloadPending into Savings/GoalDetail VMs (P1-4, R2)</name>
  <files>ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift, ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift (SubscriptionsViewModel.API, init, load+reloadPending)
    - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift (full)
    - ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift (full)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-4 (61-64), R2 (112-114)
  </read_first>
  <behavior>
    - With a stubbed API: deposit success → postDeposit called once, reload runs,
      sheet=.none, returns true. deposit failure → mutationError set, returns false.
    - submitting guard: second concurrent mutation while submitting → returns false,
      no second API call.
    - optimistic toggleRoundup/selectBase failure → reload() invoked.
    - reloadPending: load() requested while one is in flight re-runs after completion.
  </behavior>
  <action>
    1. SavingsViewModel: add `struct API` seam with closures for every endpoint used
       (summary, patchConfig roundupEnabled / roundupBase, postDeposit, accountsList,
       goalsCreate, goalsDelete) + `static let live`; add `init(api: API = .live)`;
       replace direct static calls with `api.*`.
    2. Add the `reloadPending` coalescing pattern to `load()` (copy the
       SubscriptionsViewModel shape).
    3. GoalDetailViewModel: same seam (goalsList, accountsList, postDeposit,
       goalsDelete) + `init(goalId:api:)` defaulting to `.live`.
    4. Dead-code (deferred from 67-05): remove `lastCreatedGoalId` +
       `clearLastCreatedGoalId()` if still unread by any view (grep callers); if a
       consumer exists, wire scroll-to-new; else delete.
    Preserve filtered-RU-copy + money-cents behaviour. No new libs.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && grep -q "struct API" ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift && grep -q "reloadPending" ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift && grep -q "struct API" ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift</automated>
  </verify>
  <done>Both VMs take injectable API (default .live); SavingsViewModel has reloadPending; dead lastCreatedGoalId resolved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Behavioural tests for Savings money mutations (P1-4, QA-F1/F2)</name>
  <files>ios/BudgetPlannerTests/Features/Savings/SavingsViewModelTests.swift, ios/BudgetPlannerTests/Features/Savings/GoalDetailViewModelTests.swift</files>
  <read_first>
    - the seam from Task 1
    - ios/BudgetPlannerTests/Features/Subscriptions/ (existing seam-test template)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-4 (line 63)
  </read_first>
  <behavior>
    - SavingsViewModel: deposit success/failure; createGoal success/failure +
      validation guard; deleteGoal success/failure; submitting-guard blocks reentry;
      toggleRoundup/selectBase optimistic-revert calls reload on failure;
      reloadPending replays a coalesced load.
    - GoalDetailViewModel: deposit success/failure + submitting guard; deleteGoal
      success/failure; load goal-not-found → .error("Цель не найдена").
  </behavior>
  <action>
    Add XCTest files instantiating the VMs with a stubbed `API` (closures recording
    calls / throwing on demand). Assert state transitions, call counts,
    mutationError copy, sheet state, reload-on-failure. Use `_setStateForTesting`
    to seed snapshot/accounts. Mirror the Subscriptions seam-test style.
    Run xcodegen (new files) before test.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodegen generate >/dev/null && make test 2>&1 | tail -8</automated>
  </verify>
  <done>New Savings/GoalDetail behavioural tests pass; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: APIClient 403/401 logout + MSK date decode regression tests (P1-7)</name>
  <files>ios/BudgetPlannerTests/Networking/APIClientForbiddenTests.swift, ios/BudgetPlannerTests/Networking/APIClientDateDecodeTests.swift</files>
  <read_first>
    - ios/BudgetPlanner/Networking/APIClient.swift (FINAL post-67-03: 401 :158-165, 403 :166-174, date decoder :29-63)
    - existing URLProtocol-stub test patterns under ios/BudgetPlannerTests/Networking/ (if any) or MoneyTests/PeriodTests for XCTest style
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-7 (76-79)
  </read_first>
  <behavior>
    - 403 response (with !skipAuth) → onUnauthenticated fires once AND .forbidden thrown.
    - 401 response → onUnauthenticated fires once AND .unauthorized thrown.
    - skipAuth=true 403 → onUnauthenticated does NOT fire.
    - decode "2027-01-01" via the production decoder → date equals 2027-01-01 00:00 in Europe/Moscow.
    - decode an ISO timestamp ("...T...Z" with/without fractional) for createdAt still parses (no throw).
  </behavior>
  <action>
    Add two XCTest files using a `URLProtocol` stub injected via
    `URLSession(configuration:)` → `APIClient(baseURL:session:)`; set a recording
    `onUnauthenticated` closure (flag/counter). 
    - APIClientForbiddenTests: stub 403 then 401 responses; assert logout fires for
      each; assert skipAuth path suppresses logout where applicable; mirror the
      FINAL 67-03 semantics exactly.
    - APIClientDateDecodeTests: decode a model carrying a yyyy-MM-dd field and a
      timestamp field; assert MSK-midnight for the date and successful parse for the
      timestamp (use a small Decodable test struct + the same custom decoder; if the
      decoder is private, exercise it through a real request decode via the
      URLProtocol stub returning JSON).
    Run xcodegen (new files) before test.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodegen generate >/dev/null && make test 2>&1 | tail -8</automated>
  </verify>
  <done>403→logout, 401→logout, skipAuth-suppress, MSK date pin, timestamp parse all asserted; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| iOS client→API (money) | deposit/createGoal are money mutations — must be guarded + tested |
| API auth response → client | 401/403 must invalidate token (regression-locked) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-07-01 | Tampering | money mutation reentry | mitigate | submitting-guard tested via seam; double-submit blocked. |
| T-67-07-02 | Elevation of Privilege | APIClient 401/403 | mitigate | URLProtocol regression tests pin onUnauthenticated firing — silent auth regression in other screens caught by CI. |
| T-67-07-03 | Repudiation | MSK date drift | mitigate | Decode test pins yyyy-MM-dd to Europe/Moscow midnight, preventing off-by-one fire-date drift. |
</threat_model>

<verification>
- grep `struct API` + `reloadPending` in SavingsViewModel; `struct API` in GoalDetailViewModel.
- New test files present; `make test` green (full suite, was 568 baseline).
- APIClient.swift NOT modified (owned by 67-03 — this plan adds tests only).
</verification>

<success_criteria>
Savings/GoalDetail money mutations behaviorally tested via injectable seam; reloadPending coalescing added; APIClient 403/401 + MSK decode regression-locked; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-07-SUMMARY.md`
</output>
