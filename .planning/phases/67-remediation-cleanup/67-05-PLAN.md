---
phase: 67-remediation-cleanup
plan: 05
type: execute
wave: 2
depends_on: [67-03]
files_modified:
  - ios/BudgetPlanner/Networking/APIError.swift
  - ios/BudgetPlanner/Networking/SSEClient.swift
  - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift
  - ios/BudgetPlanner/Features/Transactions/AccountPickerLogic.swift
  - ios/BudgetPlanner/Features/Management/SettingsView.swift
  - ios/BudgetPlanner/Features/Home/HomeView.swift
  - ios/BudgetPlanner/Features/Management/TemplateView.swift
  - ios/BudgetPlanner/Features/Management/CategoriesView.swift
  - ios/BudgetPlanner/Features/Management/AnalyticsView.swift
  - ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift
  - ios/BudgetPlanner/Features/AI/AIChatView.swift
  - ios/BudgetPlanner/Domain/LocalNotifications.swift
  - ios/BudgetPlanner/Features/Savings/SavingsView.swift
  - ios/BudgetPlanner/Features/Savings/GoalDetailView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift
  - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift
  - ios/BudgetPlanner/Features/Common/MutationErrorBanner.swift
  - ios/BudgetPlannerTests/Networking/APIErrorMapperTests.swift
autonomous: true
requirements: [P1-3, P1-5, R1]
must_haves:
  truths:
    - "No in-scope view sets a banner/error label to error.localizedDescription"
    - "APIError exposes a userFacingRu mapping to fixed Russian copy"
    - "SSEClient routes 401→onUnauthenticated+.unauthorized, 403→.forbidden mirroring REST"
    - "Account-label formatting has a single source (AccountPickerLogic.label)"
    - "mutationErrorBanner is a single shared ViewModifier"
    - "LocalNotifications has one reschedule helper; dead legacy [SubscriptionDTO] overload removed"
  artifacts:
    - path: "ios/BudgetPlanner/Networking/APIError.swift"
      provides: "userFacingRu computed property / mapper"
      contains: "userFacingRu"
    - path: "ios/BudgetPlanner/Features/Common/MutationErrorBanner.swift"
      provides: "shared mutationErrorBanner ViewModifier"
      contains: "ViewModifier"
    - path: "ios/BudgetPlanner/Networking/SSEClient.swift"
      provides: "split 401/403 handling"
      contains: "onUnauthenticated"
  key_links:
    - from: "error-leak cluster views"
      to: "APIError.userFacingRu"
      via: "replace error.localizedDescription"
      pattern: "userFacingRu"
    - from: "SSEClient 401 branch"
      to: "onUnauthenticated"
      via: "401-only logout"
      pattern: "onUnauthenticated"
---

<objective>
Close iOS auth/leak + dedup cluster:
- **P1-3 (iOS-F1/F2):** `TransactionEditor.save()` (:361), `SettingsView` (:34,:58)
  and a wider cluster write raw `error.localizedDescription` into banners — info
  leak + violates the v06 fixed-RU-copy convention.
- **R1 (APIError→RU mapper):** add `APIError.userFacingRu` and replace the
  in-scope `error.localizedDescription` cluster — closes P1-3 systemically.
- **R1 (dedup):** single account-label source (`AccountPickerLogic.label`),
  shared `mutationErrorBanner` ViewModifier (3 views), one `LocalNotifications`
  reschedule helper (drop dead legacy `[SubscriptionDTO]` overload).
- **P1-5 (iOS-F7):** `SSEClient` collapses 401 and 403 into `.unauthorized` and
  never calls `onUnauthenticated` → expired token in chat doesn't log out. Split
  to mirror the final REST APIClient semantics (from 67-03).

NOTE: this plan does NOT touch APIClient.swift (owned by 67-03, Wave 1) or the
Savings/GoalDetail ViewModels (owned by 67-07, Wave 3). It `depends_on: [67-03]`
so APIError 403 semantics are final before SSE mirrors them. SavingsView /
GoalDetailView (the Views) are touched here for banner adoption — distinct from
the ViewModels in 67-07.

Purpose: stop UI error leaks, unify auth on SSE, remove iOS duplication.
Output: RU error mapper, split SSE auth, shared banner, single label/reschedule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P1-3 (lines 56-59), P1-5 (lines 66-69), R1 (lines 105-110)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- ios/.../Networking/APIError.swift -->
enum APIError: LocalizedError {
    case invalidURL / invalidResponse / network(Error) / unauthorized
    case forbidden(String) / notFound / conflict(String) / unprocessable(String)
    case rateLimited(retryAfter: Int?) / serverError(Int, String) / decoding(Error)
}
<!-- error.localizedDescription IN-SCOPE views (replace with userFacingRu / fixed copy): -->
//  TransactionEditor.swift(:361), SettingsView.swift(:34,:58), HomeView.swift,
//  TemplateView.swift, CategoriesView.swift, AnalyticsView.swift,
//  CategoryDetailScreen.swift, AIChatView.swift
//  (AuthStore + Onboarding views + FeaturesV10 VMs: convert only if mechanical;
//   else note as follow-up — do not expand risk.)
<!-- AccountPickerLogic.label(:28): a.bank + (a.mask.map { " ·\($0)" } ?? "") — canonical -->
//  inline dups: SavingsDepositSheet:65, TransactionEditor:228, SubscriptionEditor:585 (" · " vs " ·" drift)
<!-- mutationErrorBanner dup: SavingsView.swift, GoalDetailView.swift, SubscriptionsView.swift -->
<!-- LocalNotifications: reschedule(subscriptionsV10:) :20 KEEP; reschedule(subscriptions:[SubscriptionDTO]) :73 DEAD per STATE 63-01 -->
<!-- SSEClient.swift:120-129 — 401||403 → .unauthorized (collapse to split) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: APIError.userFacingRu mapper + replace localizedDescription cluster (P1-3, R1)</name>
  <files>ios/BudgetPlanner/Networking/APIError.swift, ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift, ios/BudgetPlanner/Features/Management/SettingsView.swift, ios/BudgetPlanner/Features/Home/HomeView.swift, ios/BudgetPlanner/Features/Management/TemplateView.swift, ios/BudgetPlanner/Features/Management/CategoriesView.swift, ios/BudgetPlanner/Features/Management/AnalyticsView.swift, ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift, ios/BudgetPlanner/Features/AI/AIChatView.swift, ios/BudgetPlannerTests/Networking/APIErrorMapperTests.swift</files>
  <read_first>
    - ios/BudgetPlanner/Networking/APIError.swift (full enum)
    - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift (:361)
    - ios/BudgetPlanner/Features/Management/SettingsView.swift (:34,:58)
    - filtered-RU-copy precedent (SavingsViewModel :79; SubscriptionsViewModel :109)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-3 (56-59) + R1 mapper bullet (109)
  </read_first>
  <behavior>
    - APIError.userFacingRu returns fixed Russian copy per case (no raw/server text):
      .unauthorized → «Сессия истекла, войдите снова»; .network → «Нет связи с
      сервером»; .notFound → «Не найдено»; .rateLimited → «Слишком часто,
      попробуйте позже»; serverError/decoding/invalid* → «Что-то пошло не так»;
      .forbidden/.conflict/.unprocessable → generic RU copy, NOT the embedded
      server detail string.
    - A non-APIError Error → generic «Что-то пошло не так».
  </behavior>
  <action>
    1. In APIError.swift add `var userFacingRu: String` (cases above) plus a helper
       for arbitrary errors — `func userFacingRu(_ error: Error) -> String`
       returning `(error as? APIError)?.userFacingRu ?? "Что-то пошло не так"`
       (or an `Error` extension — whichever callers use cleanly).
    2. In each in-scope view (TransactionEditor:361, SettingsView:34/58, HomeView,
       TemplateView, CategoriesView, AnalyticsView, CategoryDetailScreen,
       AIChatView): replace `error.localizedDescription` UI assignments with
       `userFacingRu(error)`. Keep raw error in `#if DEBUG print()` only — never
       interpolate raw error into UI (IN-01 PII rule).
    3. Add APIErrorMapperTests.swift asserting representative cases map to fixed RU
       strings and that .forbidden("internal detail") does NOT surface the detail.
    Run `cd ios && xcodegen generate` before build (new test file added).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && test "$(grep -rl 'error.localizedDescription' ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift ios/BudgetPlanner/Features/Management/SettingsView.swift ios/BudgetPlanner/Features/Home/HomeView.swift ios/BudgetPlanner/Features/Management/TemplateView.swift ios/BudgetPlanner/Features/Management/CategoriesView.swift ios/BudgetPlanner/Features/Management/AnalyticsView.swift ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift ios/BudgetPlanner/Features/AI/AIChatView.swift 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>userFacingRu exists; 8 in-scope views no longer assign error.localizedDescription to UI; mapper test green.</done>
</task>

<task type="auto">
  <name>Task 2: Split SSE 401/403 (P1-5)</name>
  <files>ios/BudgetPlanner/Networking/SSEClient.swift</files>
  <read_first>
    - ios/BudgetPlanner/Networking/SSEClient.swift (:120-129)
    - ios/BudgetPlanner/Networking/APIClient.swift (FINAL 401/403 branches from 67-03 — mirror exactly)
    - ios/BudgetPlanner/Networking/APIError.swift (.unauthorized / .forbidden)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-5 (66-69)
  </read_first>
  <action>
    Replace `if statusCode == 401 || 403 { throw .unauthorized }` with:
      - 401: `await APIClient.shared.onUnauthenticated?()` then
        `throw APIError.unauthorized` (genuine 401 always logs out, like REST).
      - 403: mirror the FINAL post-67-03 APIClient 403 branch — if REST calls
        onUnauthenticated on 403 (strict), do the same here; otherwise
        `throw APIError.forbidden("")` without logout. Read APIClient first and
        match it so SSE and REST agree.
      - 429 branch unchanged.
    AIChatAPI is @MainActor, so onUnauthenticated is callable. No new libs.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && grep -n "401" ios/BudgetPlanner/Networking/SSEClient.swift && grep -n "onUnauthenticated\|forbidden" ios/BudgetPlanner/Networking/SSEClient.swift</automated>
  </verify>
  <done>SSEClient distinguishes 401 (logout) from 403, mirroring the final APIClient semantics.</done>
</task>

<task type="auto">
  <name>Task 3: R1 dedup — account-label, shared banner, LocalNotifications; build+test</name>
  <files>ios/BudgetPlanner/Features/Transactions/AccountPickerLogic.swift, ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift, ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift, ios/BudgetPlanner/Features/Management/SubscriptionsView.swift, ios/BudgetPlanner/Features/Common/MutationErrorBanner.swift, ios/BudgetPlanner/Features/Savings/SavingsView.swift, ios/BudgetPlanner/Features/Savings/GoalDetailView.swift, ios/BudgetPlanner/Domain/LocalNotifications.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Transactions/AccountPickerLogic.swift (label :28 canonical)
    - inline label dups: SavingsDepositSheet:65, TransactionEditor:228, SubscriptionEditor:585
    - mutationErrorBanner in SavingsView.swift / GoalDetailView.swift / SubscriptionsView.swift
    - ios/BudgetPlanner/Domain/LocalNotifications.swift (V10 :20 keep; legacy :73 dead)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md R1 (105-110); STATE.md 63-01/63-02
  </read_first>
  <action>
    1. Account-label: route SavingsDepositSheet:65, TransactionEditor:228,
       SubscriptionEditor:585 through `AccountPickerLogic.label(_:)`; fix separator
       drift to canonical `" ·<mask>"`.
    2. Shared banner: create `Features/Common/MutationErrorBanner.swift` with a
       `ViewModifier` + `View.mutationErrorBanner(_:)` extension; adopt in
       SavingsView, GoalDetailView, SubscriptionsView; delete the local copies.
       (Touches the Views only — NOT the ViewModels owned by 67-07.)
    3. LocalNotifications: keep `reschedule(subscriptionsV10:)`, DELETE the dead
       legacy `reschedule(subscriptions: [SubscriptionDTO])` (:73). Preserve MSK /
       09:00 fire logic exactly.
    4. Dead code (`lastCreatedGoalId`/`clearLastCreatedGoalId`): these live on
       SavingsViewModel (owned by 67-07). DEFER their removal to 67-07 to avoid
       same-file ownership conflict; note in SUMMARY. Do NOT edit SavingsViewModel.swift here.
    Run `cd ios && xcodegen generate` then build + test.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodegen generate >/dev/null && make build 2>&1 | tail -5; cd /Users/exy/pet_projects/tg-budget-planner && test "$(grep -c 'func reschedule' ios/BudgetPlanner/Domain/LocalNotifications.swift)" = "1"</automated>
  </verify>
  <done>Single account-label source; shared MutationErrorBanner in 3 views; one reschedule helper (dead legacy removed); iOS build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API error → UI | Server/raw error text must not reach the user banner |
| iOS client→SSE | ai/chat stream Bearer; 401 must invalidate token |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-05-01 | Information Disclosure | error banners | mitigate | userFacingRu returns fixed RU copy; raw error only in #if DEBUG print (IN-01); .forbidden detail never surfaced. |
| T-67-05-02 | Elevation of Privilege | SSEClient 401 | mitigate | 401 on chat stream now calls onUnauthenticated → expired token logs out, matching REST. |
| T-67-05-03 | Tampering | label/banner/reschedule dedup | accept | Pure refactor; behaviour preserved; covered by existing view tests + build. |
</threat_model>

<verification>
- grep: 8 in-scope views → 0 `error.localizedDescription`.
- APIErrorMapperTests green; SSEClient 401/403 split present.
- LocalNotifications has exactly one reschedule func; iOS build + test green.
- APIClient.swift NOT modified (owned by 67-03).
</verification>

<success_criteria>
RU error mapper replaces the leak cluster; SSE auth split mirrors REST; account-label/banner/reschedule deduped; build green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-05-SUMMARY.md`
</output>
