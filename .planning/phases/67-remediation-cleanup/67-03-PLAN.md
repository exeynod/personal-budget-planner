---
phase: 67-remediation-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Networking/APIClient.swift
  - ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift
autonomous: true
requirements: [P0-3]
must_haves:
  truths:
    - "A genuine 403 always triggers onUnauthenticated (strict auth restored)"
    - "suppressForbiddenHandler no longer exists on APIClient.request/requestVoid/rawRequest"
    - "AISuggestCategoryAPI.suggest still returns nil on 402/403/404/network without logout"
    - "A 402 (PRO_TIER_REQUIRED) from /ai/suggest-category is swallowed to nil, not a logout"
  artifacts:
    - path: "ios/BudgetPlanner/Networking/APIClient.swift"
      provides: "strict 403→onUnauthenticated, no suppress flag"
      contains: "case 403"
    - path: "ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift"
      provides: "suggest relies on nil-on-error (no suppress flag)"
      contains: "suggest(q:"
  key_links:
    - from: "APIClient.rawRequest 403 branch"
      to: "onUnauthenticated"
      via: "unconditional call (no suppress gate)"
      pattern: "case 403"
---

<objective>
Close **P0-3** (review-doc iOS phase 64 / ARCH-A4, BLOCKER-class): the
`suppressForbiddenHandler` flag protects a non-existent case and weakens auth.

Verified: `require_pro` returns **HTTP 402** PRO_TIER_REQUIRED
(`app/api/dependencies.py:430`), NOT 403. A non-pro caller already gets 402 →
APIClient `serverError` → `AISuggestCategoryAPI`'s catch → nil. The 403-suppress
flag therefore does nothing for require_pro, but silently swallows a *real* 403
(auth failure) on the AI path → an expired/invalid token there never logs out.

Fix: delete `suppressForbiddenHandler` everywhere; restore strict
`403 → onUnauthenticated`. `AISuggestCategoryAPI.suggest` keeps its existing
nil-on-error contract (402 → serverError → catch → nil). Do NOT touch the 401
branch (already always-logout). Optional: map 402 to a typed `.proRequired` case
WITHOUT logout — only if `APIError` already has room; otherwise leave 402 in the
default serverError branch (still caught → nil, behaviour identical).

NOTE for sequencing: this plan owns APIClient.swift in Wave 1. SSE auth (P1-5,
plan 67-05) touches SSEClient.swift (different file). Regression tests (P1-7,
plan 67-07) are Wave 3 and only ADD test files — they read this final APIClient.

Purpose: restore strict app-wide auth on the AI path; remove dead flag.
Output: APIClient with no suppress flag, AISuggest still silent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P0-3 (lines 38-42) + Verified facts (line 14)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- ios/.../APIClient.swift — current signatures carrying the flag to delete -->
func request<T: Decodable>(_ method:_ path:, query:, body:, skipAuth: Bool = false,
                           suppressForbiddenHandler: Bool = false) async throws -> T
func requestVoid(... suppressForbiddenHandler: Bool = false) async throws
private func rawRequest(... suppressForbiddenHandler: Bool = false) async throws -> Data
// 401 branch (KEEP): onUnauthenticated?(); throw .unauthorized
// 403 branch (line 166-174): if !skipAuth, !suppressForbiddenHandler { onUnauthenticated?() }
//   → becomes: if !skipAuth { onUnauthenticated?() }
// AISuggestCategoryAPI.suggest passes suppressForbiddenHandler: true → remove that arg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove suppressForbiddenHandler from APIClient; restore strict 403</name>
  <read_first>
    - ios/BudgetPlanner/Networking/APIClient.swift (request :76-95, requestVoid :97-108, rawRequest :110-187, 403 branch :166-174)
    - app/api/dependencies.py (require_pro :412-432 — 402 confirmation)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P0-3 (lines 38-42)
  </read_first>
  <action>
    In `ios/BudgetPlanner/Networking/APIClient.swift`:
      - Delete the `suppressForbiddenHandler: Bool = false` parameter from
        `request<T>`, `requestVoid`, and `rawRequest` (and remove it from the
        internal `rawRequest(...)` call sites inside request/requestVoid).
      - 403 branch: replace
          `if !skipAuth, !suppressForbiddenHandler { onUnauthenticated?() }`
        with
          `if !skipAuth { onUnauthenticated?() }`
        Keep `throw APIError.forbidden(detail)`.
      - DO NOT touch the 401 branch (already unconditional logout — WR-02).
      - Update the WR-02 comments in the 401/403 branches to reflect that 403 is
        now strict (no per-call suppression) and that require_pro is 402.
    Optional (only if APIError already defines a non-logout case): add a `case 402`
    branch mapping to a `.proRequired`/typed error. If not trivially available,
    leave 402 in the `default → serverError` branch (caught → nil downstream).
    No third-party libs.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && ! grep -rn "suppressForbiddenHandler" ios/BudgetPlanner/</automated>
  </verify>
  <done>suppressForbiddenHandler removed from all signatures and call sites; 403 unconditionally calls onUnauthenticated when !skipAuth; 401 unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: AISuggestCategoryAPI.suggest relies on nil-on-error; build</name>
  <read_first>
    - ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift (suggest :36-56, header comment :1-15)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P0-3 (line 40)
  </read_first>
  <action>
    In `AISuggestCategoryAPI.suggest`:
      - Remove the `suppressForbiddenHandler: true` argument from the
        `APIClient.shared.request(...)` call. The remaining call relies on the
        existing do/catch → nil contract: a non-pro 402 falls to serverError →
        catch → nil; 404/network/decoding → catch → nil.
      - Update the header doc comment + the in-catch comment: the silent contract
        is now "any thrown error → nil"; require_pro is 402 (not 403); the 403
        suppression no longer exists; a genuine 403/401 auth failure now logs out
        globally (correct — owner token is broken). Keep the IN-01 PII rule
        (never interpolate raw error / URL).
    Then regenerate the Xcode project (no new files added, but run for safety) and
    build + test on the simulator.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodegen generate >/dev/null && make build 2>&1 | tail -5</automated>
  </verify>
  <done>suggest() compiles without the flag; iOS build succeeds (BudgetPlanner scheme, iPhone 17 Pro); suggest still returns nil on error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| iOS client→API | Bearer token; 401/403 must invalidate token via onUnauthenticated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03-01 | Elevation of Privilege | APIClient 403 branch | mitigate | Restoring unconditional 403→onUnauthenticated closes the path where a broken token on the AI route stayed authenticated. |
| T-67-03-02 | Information Disclosure | AISuggest error log | mitigate | Keep IN-01: never interpolate raw error/URL (q= holds PII); DEBUG logs only the error type. |
| T-67-03-03 | Spoofing | require_pro 402 handling | accept | 402 is a benign tier signal; swallowed to nil (hint hidden), no auth state change — matches verified backend behaviour. |
</threat_model>

<verification>
- `grep -rn suppressForbiddenHandler ios/BudgetPlanner/` → zero matches.
- iOS build green (make build).
- 403 branch calls onUnauthenticated when !skipAuth; 401 branch unchanged.
</verification>

<success_criteria>
suppressForbiddenHandler fully removed; strict 403→logout restored; AISuggest silent contract intact; iOS build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-03-SUMMARY.md`
</output>
