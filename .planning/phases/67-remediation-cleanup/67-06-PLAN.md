---
phase: 67-remediation-cleanup
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/src/main.tsx
  - frontend/src/screensV10/common/useTheme.ts
  - frontend/src/vite-env.d.ts
  - .planning/phases/67-remediation-cleanup/DEAD-SHELL-INVENTORY.md
autonomous: true
requirements: [P1-6, R5]
must_haves:
  truths:
    - "Shell dispatch and theme selection use distinct localStorage keys (no collision)"
    - "Selecting a v10 theme no longer makes the v06 web shell unreachable via key clash"
    - "npm run build stays green after the key split"
    - "A dead-shell reachability inventory exists documenting which files load only under the v06 shell"
  artifacts:
    - path: "frontend/src/main.tsx"
      provides: "shell dispatch on ui.shell (not ui.theme)"
      contains: "ui.shell"
    - path: "frontend/src/screensV10/common/useTheme.ts"
      provides: "theme selection on ui.theme only"
      contains: "ui.theme"
    - path: ".planning/phases/67-remediation-cleanup/DEAD-SHELL-INVENTORY.md"
      provides: "reachability decision for v06 web shell (~50 files)"
      contains: "v06"
  key_links:
    - from: "main.tsx readTheme/shell dispatch"
      to: "localStorage 'ui.shell'"
      via: "distinct key"
      pattern: "ui\\.shell"
---

<objective>
Close **P1-6 (FE-F4)** + scope the R5 dead-shell question:
- `main.tsx` shell-dispatcher writes/reads `localStorage['ui.theme']` with the
  vocabulary `v06`/`v10`, while `useTheme` writes the SAME key with
  `maximal_poster`/`liquid_glass`/`ios_default`. The vocabularies are incompatible
  — choosing a v10 theme stores a value the shell-dispatcher doesn't recognise, so
  the v06 web shell becomes unreachable.
- **Fix:** split keys — shell dispatch → `ui.shell` (`v06`/`v10`); theme selection
  stays on `ui.theme` (`maximal_poster`/...). Migrate legacy stored values where
  cheap.
- **R5 (inventory only):** produce a DEAD-SHELL-INVENTORY.md documenting which
  files load only under the (now reachability-fixed) v06 shell. The ~50-file
  deletion is OUT of scope for this plan — decision recorded for follow-up
  (per CONTEXT R5 default: split key now, inventory now, delete later only if
  confirmed unreachable + low-risk).

Purpose: stop the key collision so theme choice and shell choice are orthogonal.
Output: split keys + green build + dead-shell inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P1-6 (lines 71-74) + R5 (lines 121-123)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- main.tsx: readTheme() reads localStorage 'ui.theme' for 'v06'|'v10' (shell). -->
<!--           plus an IIFE :56-69 sets <html data-theme> from 'ui.theme' (maximal_poster|...). -->
<!-- useTheme.ts: STORAGE_KEY = 'ui.theme', values maximal_poster|liquid_glass|ios_default. -->
<!-- VITE_UI_THEME env still wins for shell dispatch (vite-env.d.ts). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split shell key (ui.shell) from theme key (ui.theme); migrate; build</name>
  <read_first>
    - frontend/src/main.tsx (readTheme :30-46, data-theme IIFE :56-69, dispatch :74-122)
    - frontend/src/screensV10/common/useTheme.ts (STORAGE_KEY :30, isTheme :34-38)
    - frontend/src/vite-env.d.ts (VITE_UI_THEME typing)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P1-6 (71-74)
  </read_first>
  <action>
    1. In `main.tsx`, change the SHELL dispatcher to read `localStorage['ui.shell']`
       (values `v06`/`v10`); keep `VITE_UI_THEME` env precedence (or rename to
       VITE_UI_SHELL — pick one and update vite-env.d.ts comment; minimise churn,
       env override may stay VITE_UI_THEME if documented). Default new installs to
       `v10`.
    2. Keep the `<html data-theme>` bootstrap IIFE reading `ui.theme`
       (maximal_poster/liquid_glass/ios_default) — that is the THEME, untouched.
    3. `useTheme.ts` already uses `ui.theme` for themes — leave its key as is. Now
       the two systems no longer share a vocabulary on one key.
    4. One-time migration shim (optional, cheap): on shell read, if `ui.shell` is
       absent but a legacy `ui.theme` holds `v06`/`v10`, adopt it as the shell value
       and write `ui.shell` (so existing installs keep their shell). Do not clobber
       theme values.
    5. Update the now-stale comments in main.tsx (:48-55) and useTheme.ts (:13-17)
       describing the old shared-key coexistence.
    No new deps (memory: no AI-pattern libs).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && grep -q "ui.shell" src/main.tsx && npm run build</automated>
  </verify>
  <done>Shell dispatch reads ui.shell; theme stays on ui.theme; legacy migration shim present; npm run build green.</done>
</task>

<task type="auto">
  <name>Task 2: Dead-shell reachability inventory (R5)</name>
  <files>.planning/phases/67-remediation-cleanup/DEAD-SHELL-INVENTORY.md</files>
  <read_first>
    - frontend/src/main.tsx (the v06 branch :104-122 — what it imports: ./App, legacy styles)
    - frontend/src/App.tsx + frontend/src/screens/* + frontend/src/components/* + frontend/src/api/subscriptions.ts + frontend/src/hooks/useSubscriptions.ts
    - .planning/v1.1.2-MULTILEAD-REVIEW.md R5 (121-123)
  </read_first>
  <action>
    Produce `.planning/phases/67-remediation-cleanup/DEAD-SHELL-INVENTORY.md`:
      - List the files reachable ONLY through the v06 web shell (App.tsx,
        screens/*, legacy components/*, api/subscriptions.ts, hooks/useSubscriptions.ts,
        plus any transitive-only imports). Use grep/import-graph evidence.
      - State whether, after the key split, the v06 shell is reachable
        (env VITE_UI_THEME=v06 or localStorage ui.shell=v06) — i.e. is it dead UI or
        a maintained alternative?
      - Record the decision: KEEP (reachable, maintained) vs DELETE-LATER
        (confirmed unreachable + low-risk → propose a bounded follow-up task) — do
        NOT delete the ~50 files in this plan.
    This is documentation only; no source deletion.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && test -f .planning/phases/67-remediation-cleanup/DEAD-SHELL-INVENTORY.md && grep -qi "v06" .planning/phases/67-remediation-cleanup/DEAD-SHELL-INVENTORY.md</automated>
  </verify>
  <done>Inventory lists v06-only files + reachability + KEEP/DELETE-LATER decision; no source files deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| localStorage → shell/theme dispatch | Untrusted client storage; values must be whitelist-validated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06-01 | Tampering | localStorage shell/theme keys | mitigate | Both readers keep strict whitelist validation (v06/v10 for shell; maximal_poster/... for theme); unknown → default. |
| T-67-06-02 | DoS (self) | key collision | mitigate | Splitting ui.shell vs ui.theme removes the state that made a valid shell unreachable. |
</threat_model>

<verification>
- grep `ui.shell` in main.tsx; `npm run build` green.
- DEAD-SHELL-INVENTORY.md exists with reachability + decision.
- useTheme.ts still owns ui.theme for themes.
</verification>

<success_criteria>
Shell and theme keys are orthogonal; v06 shell reachable independent of theme choice; build green; inventory documented.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-06-SUMMARY.md`
</output>
