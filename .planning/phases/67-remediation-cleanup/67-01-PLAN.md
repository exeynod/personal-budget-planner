---
phase: 67-remediation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/routes/subscriptions.py
  - app/api/schemas/subscriptions.py
  - tests/test_subscriptions.py
autonomous: true
requirements: [P0-1]
must_haves:
  truths:
    - "GET /api/v1/subscriptions returns day_of_month, account_id, posted_txn_id for each item"
    - "POST /api/v1/subscriptions returns the same V10 fields in the created row"
    - "PATCH /api/v1/subscriptions/{id} returns the V10 fields after update"
    - "iOS phase 63 round-trips day_of_month/account_id without nil-loss"
  artifacts:
    - path: "app/api/schemas/subscriptions.py"
      provides: "SubscriptionReadV10 class inheriting SubscriptionRead + SubscriptionV10Extension"
      contains: "class SubscriptionReadV10"
    - path: "app/api/routes/subscriptions.py"
      provides: "list/post/patch wired to SubscriptionReadV10 response_model"
      contains: "SubscriptionReadV10"
    - path: "tests/test_subscriptions.py"
      provides: "integration test asserting V10 fields round-trip"
      contains: "day_of_month"
  key_links:
    - from: "app/api/routes/subscriptions.py"
      to: "SubscriptionReadV10"
      via: "response_model on list/post/patch"
      pattern: "response_model=.*SubscriptionReadV10"
    - from: "ORM Subscription"
      to: "SubscriptionReadV10"
      via: "from_attributes model_validate"
      pattern: "SubscriptionReadV10.model_validate"
---

<objective>
Close **P0-1** (review-doc BE-F1, BLOCKER): the public `/subscriptions` GET/POST/PATCH
routes return `SubscriptionRead`, which lacks `day_of_month` / `account_id` /
`posted_txn_id`. iOS phase 63 writes those fields but reads back `nil`, and the
posted-badge never shows — phase 63 is non-functional end-to-end.

Define `SubscriptionReadV10` (inherits the existing `SubscriptionRead` +
`SubscriptionV10Extension` mixin already in the schema file) and switch the three
routes to it. Add an integration test proving the three fields round-trip.

Purpose: unblock iOS phase 63 — the cheapest, highest-impact P0.
Output: V10-shaped subscription responses + green integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE SPEC — read P0-1 (lines 25-29) + "Verified facts" (lines 12-15)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- From app/api/schemas/subscriptions.py — already present, just not wired -->
class SubscriptionRead(BaseModel):  # from_attributes=True
    id, name, amount_cents, cycle, next_charge_date, category_id,
    notify_days_before, is_active, category: CategoryRead

class SubscriptionV10Extension(BaseModel):  # from_attributes=True
    day_of_month: Optional[int] = Field(default=None, ge=1, le=28)
    account_id: Optional[int] = None
    posted_txn_id: Optional[int] = None

# Routes today (app/api/routes/subscriptions.py):
#   GET   "" line 46  response_model=list[SubscriptionRead]   returns [SubscriptionRead.model_validate(s) ...]
#   POST  "" line 56  response_model=SubscriptionRead         returns SubscriptionRead.model_validate(sub)
#   PATCH "/{sub_id}" line 104 response_model=SubscriptionRead returns SubscriptionRead.model_validate(sub)
# DELETE / charge-now / post / unpost stay UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define SubscriptionReadV10 and wire list/post/patch response_model</name>
  <read_first>
    - app/api/schemas/subscriptions.py (SubscriptionRead :53-66, SubscriptionV10Extension :109-122)
    - app/api/routes/subscriptions.py (list :46-53, post :56-101, patch :104-128)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P0-1 (lines 25-29)
  </read_first>
  <action>
    In `app/api/schemas/subscriptions.py`, add (below the existing V10 schemas):

      class SubscriptionReadV10(SubscriptionRead, SubscriptionV10Extension):
          """v1.0 read shape: legacy SubscriptionRead + day_of_month/account_id/posted_txn_id."""
          model_config = ConfigDict(from_attributes=True)

    The ORM `Subscription` model already carries `day_of_month`, `account_id`,
    `posted_txn_id` columns (migration 0014) so `from_attributes` proxies them.

    In `app/api/routes/subscriptions.py`:
      - import `SubscriptionReadV10` into the existing schema import block.
      - GET list (line 46): `response_model=list[SubscriptionReadV10]`; change the
        return comprehension to `SubscriptionReadV10.model_validate(s)`.
      - POST create (line 56): `response_model=SubscriptionReadV10`; return
        `SubscriptionReadV10.model_validate(sub)`.
      - PATCH (line 104): `response_model=SubscriptionReadV10`; return
        `SubscriptionReadV10.model_validate(sub)`.
      - Do NOT touch DELETE / charge-now / post / unpost.

    Per CLAUDE.md: money stays BIGINT cents (no float); no new fields beyond the three.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.schemas.subscriptions import SubscriptionReadV10; assert {'day_of_month','account_id','posted_txn_id'} <= set(SubscriptionReadV10.model_fields)"</automated>
  </verify>
  <done>SubscriptionReadV10 exists with the 3 V10 fields; list/post/patch use it as response_model; other routes unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test — V10 fields round-trip on GET/POST</name>
  <files>tests/test_subscriptions.py</files>
  <read_first>
    - tests/test_subscriptions.py (existing client/auth fixtures + sub-create helpers)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P0-1 "Проверка" (line 28)
  </read_first>
  <behavior>
    - Create a subscription with day_of_month + account_id (via the v1.0 PATCH path
      or direct service if create has no v10 body) → GET /subscriptions → response
      item includes day_of_month, account_id (echoed back, not nil).
    - After POST /{id}/post sets posted_txn_id → GET (or POST response) exposes
      posted_txn_id as a non-null int.
    - A plain create with no day/account → those fields are present and null
      (not missing keys).
  </behavior>
  <action>
    Add an async test mirroring existing test_subscriptions.py style (same client +
    onboarded-user fixtures, same tenant-scope setup). Assert the three V10 keys
    appear in the JSON body. If create() has no v10 fields, set day_of_month/account_id
    via PATCH first, then GET. Use an existing account fixture for account_id.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_subscriptions.py -x -q -k "v10 or day_of_month or posted"</automated>
  </verify>
  <done>New test passes; asserts day_of_month/account_id/posted_txn_id present in GET and POST responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API | Telegram client reads subscription rows; tenant scope via SET LOCAL app.current_user_id |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-01 | Information Disclosure | SubscriptionReadV10 response | accept | New fields (day/account/posted_txn) are the caller's own tenant rows; RLS + user_id filter in list_subscriptions already scope them. No cross-tenant leak. |
| T-67-01-02 | Tampering | response_model widening | mitigate | Read-only widening; request bodies (SubscriptionCreate/Update) keep extra="forbid" — no new writable surface. |
</threat_model>

<verification>
- `python -c` import check confirms SubscriptionReadV10 fields.
- pytest test_subscriptions.py round-trip test green.
- grep `response_model=.*SubscriptionReadV10` matches 3 routes (list/post/patch).
</verification>

<success_criteria>
GET/POST/PATCH /subscriptions return day_of_month, account_id, posted_txn_id; integration test green; no change to DELETE/charge-now/post/unpost.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-01-SUMMARY.md`
</output>
