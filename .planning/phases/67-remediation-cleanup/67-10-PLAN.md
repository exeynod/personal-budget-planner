---
phase: 67-remediation-cleanup
plan: 10
type: execute
wave: 5
depends_on: [67-05, 67-07]
files_modified:
  - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift
  - ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift
  - ios/BudgetPlanner/Features/Transactions/TransactionsViewModel.swift
  - CLAUDE.md
  - docs/HLD.md
autonomous: true
requirements: [P2-1, P2-2, P2-3, P2-12, R9]
must_haves:
  truths:
    - "Subscription create-success triggers a single reload (no onPatchV10→load + onSaved→load double)"
    - "day_of_month and nextChargeDate consistency is enforced or documented as source-of-truth"
    - "toggleRoundup/selectBase are serialized via a config-inFlight guard"
    - "test_notificationTxnCreated_triggersLoad uses an injected load-seam, not a 300ms sleep"
    - "CLAUDE.md/HLD reflect the de-facto multi-tenant reality (RLS active, owner/member roles)"
  artifacts:
    - path: "ios/BudgetPlanner/Features/Management/SubscriptionsView.swift"
      provides: "single-reload create path"
      contains: "onSaved"
    - path: "ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift"
      provides: "config-inFlight guard on toggleRoundup/selectBase"
      contains: "configInFlight"
    - path: "CLAUDE.md"
      provides: "multi-tenant reality documented"
      contains: "multi-tenant"
  key_links:
    - from: "SubscriptionEditor create success"
      to: "viewModel.load"
      via: "no-op onSaved when patch already reloaded"
      pattern: "onSaved"
---

<objective>
iOS P2 minors + flaky-test fix + R9 docs. This plan re-touches two files owned by
earlier plans (SubscriptionsView by 67-05, SavingsViewModel by 67-07) so it runs
AFTER them (Wave 5, depends_on: [67-05, 67-07]) — never in parallel:
- **P2-1 (iOS-F9):** create-success runs 2-3 reloads (onPatchV10→load then
  onSaved→load). Make `onSaved` a no-op when the patch already triggered a reload.
- **P2-2 (iOS-F8):** `day_of_month` and `nextChargeDate` are not reconciled
  (inconsistent pair sent to backend). Synchronize the day, OR document the
  source-of-truth (whichever the editor's existing logic supports cleanly).
- **P2-3 (iOS-F10):** `toggleRoundup`/`selectBase` lack a submitting-guard →
  serialize via a `configInFlight` flag.
- **P2-12 (QA-F6):** flaky `test_notificationTxnCreated_triggersLoad` uses a 300ms
  sleep → inject a load-seam so the test observes the load deterministically.
- **R9 (ARCH-A7):** CLAUDE.md / HLD say "single-tenant, RLS not introduced", but
  the system is de-facto multi-tenant (RLS active, owner/member roles work) —
  update the docs to reflect reality (an asset, not carrying cost).

Purpose: remove redundant reloads, flaky sleeps, stale docs; guard config writes.
Output: single-reload create, config guard, deterministic test, accurate docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P2-1/2/3 (85-87), P2-12 (96), R9 (139-140)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- SubscriptionsView.swift: SubscriptionEditor onSaved (:309/:320/:555) + onPatchV10 (:558/:796-804).
     create flow: create → if needsFollowUpPatch onPatchV10(created.id,...) → onSaved(). -->
<!-- SavingsViewModel.swift (post-67-07 seam): toggleRoundup :86 / selectBase :109 — no submitting/inFlight guard around config PATCH. -->
<!-- TransactionsViewModelTests.swift: test_notificationTxnCreated_triggersLoad uses Task.sleep ~300ms. -->
<!-- TransactionsViewModel.swift: the notification→load path (add an injectable load-seam / hook). -->
<!-- CLAUDE.md:6,34 + docs/HLD.md:117 — "Single-tenant", "FK на app_user НЕ вводим", "RLS не вводим". -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-reload create (P2-1) + day_of_month/nextChargeDate (P2-2) in SubscriptionEditor</name>
  <files>ios/BudgetPlanner/Features/Management/SubscriptionsView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift (create flow :790-810, onSaved/onPatchV10 wiring :309-322, editor decls :555-558)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-1 (85), P2-2 (86)
  </read_first>
  <action>
    1. P2-1: when the create path calls `onPatchV10(created.id, ...)` which already
       performs a `viewModel.load()`, make the subsequent `onSaved()` a no-op (e.g.
       only call onSaved when no follow-up patch ran, or have onPatchV10's success
       skip the extra onSaved). Net: exactly one reload after create-success.
    2. P2-2: reconcile day_of_month with nextChargeDate before sending — set
       day_of_month from the chosen nextChargeDate's day (clamped 1..28 per the
       backend CHECK), OR if nextChargeDate is the source of truth, drop the
       independent day field and document it in a code comment. Pick the option that
       fits the editor's existing date handling (STATE 63-02: edit-path date stays
       on legacy yyyy-MM-dd; day via follow-up V10 PATCH).
    This plan re-touches SubscriptionsView.swift AFTER 67-05 (banner adoption) — preserve those edits.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodegen generate >/dev/null && make build 2>&1 | tail -5</automated>
  </verify>
  <done>Create-success reloads once; day_of_month/nextChargeDate reconciled or documented; build green; 67-05 banner edits intact.</done>
</task>

<task type="auto">
  <name>Task 2: config-inFlight guard on toggleRoundup/selectBase (P2-3)</name>
  <files>ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift (post-67-07: seam + toggleRoundup :86 / selectBase :109)
    - submitting-guard precedent in the same file (createGoal/deposit)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-3 (87)
  </read_first>
  <action>
    Add a `@ObservationIgnored private var configInFlight: Bool = false` and guard
    both `toggleRoundup` and `selectBase`: if `configInFlight` return early; else set
    true with `defer { configInFlight = false }` around the optimistic-update +
    PATCH. Serializes concurrent config writes (rapid toggles) without blocking
    other mutations. Preserve the 67-07 seam (call via `api.patchConfig...`) and the
    filtered-RU-copy. Re-touches SavingsViewModel.swift AFTER 67-07 — keep its seam/reloadPending.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && grep -q "configInFlight" ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift</automated>
  </verify>
  <done>toggleRoundup/selectBase serialized via configInFlight; 67-07 seam preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: De-flake notification test via load-seam (P2-12) + R9 docs</name>
  <files>ios/BudgetPlanner/Features/Transactions/TransactionsViewModel.swift, ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift, CLAUDE.md, docs/HLD.md</files>
  <read_first>
    - ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift (test_notificationTxnCreated_triggersLoad + the 300ms sleep)
    - ios/BudgetPlanner/Features/Transactions/TransactionsViewModel.swift (the notification→load path)
    - CLAUDE.md (:6 single-tenant, :34 FK/RLS), docs/HLD.md (:117)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-12 (96), R9 (139-140)
  </read_first>
  <behavior>
    - The notification-triggered load is observable deterministically (a hook/seam
      the test awaits or a recorded flag), with NO Task.sleep timing dependency.
  </behavior>
  <action>
    1. P2-12: introduce an injectable load-seam (or an awaitable "did-load" hook /
       continuation) on TransactionsViewModel so the test can deterministically
       observe that the notification handler triggered a load; rewrite
       test_notificationTxnCreated_triggersLoad to await that seam instead of
       `Task.sleep(300ms)`. Keep production behaviour identical.
    2. R9: update CLAUDE.md (:6, :34) and docs/HLD.md (:117): the system is de-facto
       multi-tenant — RLS is active and owner/member roles work; reframe the
       "single-tenant / RLS не вводим / FK на app_user НЕ вводим" wording as
       "multi-tenant via RLS (owner + member roles); per-row user_id + SET LOCAL
       app.current_user_id". State it is an asset, not carrying cost. Keep the doc
       conventions otherwise intact (money cents, MSK dates, internal-token).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && ! grep -n "sleep" ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift; grep -qi "multi-tenant" CLAUDE.md && grep -qi "multi-tenant\|RLS" docs/HLD.md && cd ios && xcodegen generate >/dev/null && make test 2>&1 | tail -8</automated>
  </verify>
  <done>Notification test is deterministic (no sleep); CLAUDE.md/HLD reflect multi-tenant reality; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| iOS config write | rapid toggle taps must not race the config PATCH |
| docs vs reality | inaccurate single-tenant docs mislead future RLS/security work |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-10-01 | Tampering | toggleRoundup/selectBase | mitigate | configInFlight serializes config PATCH — last-write race avoided. |
| T-67-10-02 | Repudiation | flaky load test | mitigate | Deterministic load-seam removes timing flake → CI reliably catches notification-load regressions. |
| T-67-10-03 | Information Disclosure | tenancy docs | accept | Doc accuracy (multi-tenant via RLS) prevents mistaken "no isolation needed" assumptions in future work; no runtime change here. |
</threat_model>

<verification>
- iOS build + test green; no `sleep` in the notification test.
- grep configInFlight in SavingsViewModel; single reload on create.
- CLAUDE.md + HLD mention multi-tenant/RLS reality.
- 67-05 / 67-07 edits to the shared files preserved.
</verification>

<success_criteria>
Single-reload create; reconciled day/nextChargeDate; config guard; deterministic notification test; accurate tenancy docs; suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-10-SUMMARY.md`
</output>
