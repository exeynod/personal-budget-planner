---
phase: 67-remediation-cleanup
plan: 08
type: execute
wave: 4
depends_on: [67-04]
files_modified:
  - app/api/schemas/ai.py
  - app/api/routes/ai_suggest.py
  - app/api/routes/me.py
  - app/api/dependencies.py
  - app/db/session.py
  - app/services/spend_cap.py
  - app/db/models.py
  - alembic/versions/0026_ai_usage_cost_cents.py
  - tests/test_ai_suggest.py
  - tests/test_me.py
autonomous: true
requirements: [P2-4, P2-5, P2-6, P2-7, R8]
must_haves:
  truths:
    - "ChatRequest.message has min/max length bounds (422 on empty/oversize)"
    - "suggest-category docstring matches the actual 0.35 threshold and returns the real confidence on miss"
    - "GET /me and PATCH /me are symmetric on income_cents via a shared builder"
    - "ai_usage_log stores cost as BIGINT cost_cents (no Float), and embedding spend is logged"
  artifacts:
    - path: "app/api/schemas/ai.py"
      provides: "ChatRequest.message bounded"
      contains: "max_length"
    - path: "app/db/models.py"
      provides: "AiUsageLog.cost_cents BIGINT (Float removed)"
      contains: "cost_cents"
    - path: "alembic/versions/0026_ai_usage_cost_cents.py"
      provides: "est_cost_usd Float → cost_cents BIGINT migration"
      contains: "cost_cents"
  key_links:
    - from: "GET /me + PATCH /me"
      to: "shared MeResponse builder"
      via: "single build_me_response helper"
      pattern: "income_cents"
    - from: "spend_cap"
      to: "AiUsageLog.cost_cents"
      via: "SUM in cents"
      pattern: "cost_cents"
---

<objective>
Backend P2 minors + R8 hygiene (no float per CLAUDE.md):
- **P2-4 (BE-F5):** `ChatRequest.message` has no bounds → add
  `Field(min_length=1, max_length=4000)`.
- **P2-5 (BE-F6):** suggest-category docstring says threshold 0.5 but the code
  uses 0.35; on a miss it returns `confidence=0.0` instead of the actual value.
  Fix the docstring to 0.35 and return the real confidence.
- **P2-6 (BE-F7):** `GET /me` omits `income_cents` while `PATCH /me` includes it
  (asymmetry). Make symmetric via a shared MeResponse builder (also satisfies the
  R8 builder-dedup item).
- **P2-7 (BE-F3):** suggest-category does not log the embedding cost to
  `ai_usage_log` → the cap can't see embedding spend. Log est cost (in cents per
  R8) or a discrete entry.
- **R8:** `est_cost_usd: Float` → BIGINT `cost_cents` (model + migration +
  spend_cap aggregation); dedupe `get_db` (dependencies.py vs session.py → one
  source); MeResponse builder dedup (with P2-6); finish `KindStr` rename / remove
  legacy alias symbol if present.

Purpose: enforce no-float money rule, fix cap accounting, symmetry, dedup.
Output: bounded ChatRequest, correct confidence, symmetric /me, cents-based cost,
single get_db, migration 0026.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# THE SPEC — read P2-4..7 (88-91), R8 (132-137)
@.planning/v1.1.2-MULTILEAD-REVIEW.md

<interfaces>
<!-- app/api/schemas/ai.py:10-15 ChatRequest.message: str (unbounded) -->
<!-- app/api/routes/ai_suggest.py:58-89 docstring "0.5"; miss returns confidence=0.0 -->
<!-- app/api/routes/me.py: patch_me builds MeResponse(income_cents=...); get_me omits it -->
<!-- app/api/dependencies.py:35 get_db()  AND  app/db/session.py:20 get_db()  → duplicate -->
<!-- app/db/models.py:867 est_cost_usd: Mapped[float] = mapped_column(Float(asdecimal=False)) -->
<!-- app/services/spend_cap.py:120 SUM(AiUsageLog.est_cost_usd) → must become SUM(cost_cents) -->
<!-- app/api/schemas/planned.py:9 KindStr = Literal["expense","income"] (clean) — check for any ActualKindStr alias to retire -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatRequest bounds + suggest confidence/docstring (P2-4, P2-5)</name>
  <files>app/api/schemas/ai.py, app/api/routes/ai_suggest.py, tests/test_ai_suggest.py</files>
  <read_first>
    - app/api/schemas/ai.py (:10-15 ChatRequest)
    - app/api/routes/ai_suggest.py (:55-89 threshold + miss path)
    - tests/test_ai_suggest.py
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-4 (88), P2-5 (89)
  </read_first>
  <behavior>
    - POST /ai/chat with empty message → 422; >4000 chars → 422.
    - suggest-category miss → response.confidence == the actual computed value
      (not hardcoded 0.0); category_id/name null.
    - docstring states the 0.35 threshold.
  </behavior>
  <action>
    1. ai.py: `message: str = Field(min_length=1, max_length=4000)`.
    2. ai_suggest.py: change the miss-return to carry the real `confidence` (the
       computed similarity), keeping category_id/name None; update the docstring
       0.5 → 0.35 (match the actual code threshold).
    3. Add/extend tests: empty + oversize chat message → 422; suggest miss returns
       a non-zero real confidence with null category.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_ai_suggest.py -x -q</automated>
  </verify>
  <done>message bounded; miss returns real confidence; docstring says 0.35; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Symmetric /me via shared builder + dedupe get_db (P2-6, R8)</name>
  <files>app/api/routes/me.py, app/api/dependencies.py, app/db/session.py, tests/test_me.py</files>
  <read_first>
    - app/api/routes/me.py (get_me + patch_me, MeResponse build :87)
    - app/api/dependencies.py (:35 get_db) + app/db/session.py (:20 get_db)
    - tests/test_me.py
    - .planning/v1.1.2-MULTILEAD-REVIEW.md P2-6 (90), R8 (133-136)
  </read_first>
  <behavior>
    - GET /me returns income_cents (same shape as PATCH /me response).
    - PATCH /me unchanged. Both built by one helper.
    - get_db imported from a single module everywhere.
  </behavior>
  <action>
    1. Extract a `build_me_response(user) -> MeResponse` helper used by both get_me
       and patch_me; add income_cents to GET /me.
    2. Dedupe get_db: keep one canonical definition (prefer app/db/session.py as the
       source) and re-export / import it in dependencies.py (or vice-versa — pick
       one, update all import sites). Do not change behaviour (tenant-scope variant
       get_db_with_tenant_scope stays).
    3. Update tests: GET /me asserts income_cents present and equals PATCH echo.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -m pytest tests/test_me.py -x -q && python -c "import app.api.dependencies, app.db.session"</automated>
  </verify>
  <done>GET/PATCH /me symmetric via shared builder; single get_db source; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: est_cost_usd Float → cost_cents BIGINT + embedding spend logging (R8, P2-7)</name>
  <files>app/db/models.py, app/services/spend_cap.py, app/ai/embedding_service.py, alembic/versions/0026_ai_usage_cost_cents.py, app/api/routes/ai_suggest.py</files>
  <read_first>
    - app/db/models.py (AiUsageLog :832-880, est_cost_usd :867)
    - app/services/spend_cap.py (SUM aggregation :99-132)
    - app/ai/embedding_service.py (where suggest embeds — to log cost)
    - alembic/versions/0025_* (from 67-04; chain 0026 after it)
    - .planning/v1.1.2-MULTILEAD-REVIEW.md R8 (132-133), P2-7 (91)
  </read_first>
  <behavior>
    - AiUsageLog stores cost_cents: BIGINT (no Float column).
    - spend_cap sums cost_cents directly (cents in, cents out — no float math).
    - A suggest-category embedding call records its est cost (in cents) to
      ai_usage_log so the cap sees it.
  </behavior>
  <action>
    1. models.py: replace `est_cost_usd: Mapped[float] (Float)` with
       `cost_cents: Mapped[int] = mapped_column(BigInteger, ...)`. Remove the Float
       import if now unused. Per CLAUDE.md: BIGINT cents, no float.
    2. Migration `0026_ai_usage_cost_cents.py` (revision after 0025): add cost_cents
       BIGINT, backfill `cost_cents = ceil(est_cost_usd * 100)` for existing rows,
       drop est_cost_usd. Provide downgrade (re-add Float, est_cost_usd = cost_cents/100.0).
    3. spend_cap.py: SUM(AiUsageLog.cost_cents) directly; drop the float→cents
       conversion + the "Float legacy" comments.
    4. Embedding logging: at the suggest-category embedding call site, write an
       ai_usage_log row with cost_cents for the embedding (P2-7) so cap accounting
       includes it. Reuse the existing usage-log write path; compute est cost in cents.
    5. Update any other reader/writer of est_cost_usd to cost_cents.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && test "$(grep -rc 'est_cost_usd' app/db/models.py app/services/spend_cap.py | awk -F: '{s+=$2} END{print s}')" = "0" && grep -q "cost_cents" alembic/versions/0026_ai_usage_cost_cents.py && python -m pytest tests/ -q -k "spend_cap or cap or usage" 2>&1 | tail -3</automated>
  </verify>
  <done>cost_cents BIGINT replaces est_cost_usd; migration 0026 backfills + drops Float; spend_cap sums cents; embedding spend logged; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→API | ChatRequest.message untrusted free text — bound length |
| AI spend accounting | cost must be tracked accurately to enforce the cap |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-08-01 | DoS | ChatRequest.message | mitigate | min_length=1/max_length=4000 caps oversized prompts (token-cost amplification). |
| T-67-08-02 | Repudiation | AI cost accounting | mitigate | cost_cents BIGINT + embedding-spend logging make cap math exact (no float drift), embedding spend visible. |
| T-67-08-03 | Information Disclosure | /me symmetry | accept | income_cents is the owner's own field; GET exposing it matches PATCH; single-tenant. |
</threat_model>

<verification>
- pytest test_ai_suggest, test_me, spend_cap tests green.
- grep est_cost_usd → 0 in models.py + spend_cap.py; migration 0026 present.
- single get_db source (imports resolve).
</verification>

<success_criteria>
ChatRequest bounded; real confidence on miss + 0.35 docstring; symmetric /me builder; cost_cents BIGINT + migration + embedding logging; single get_db; KindStr alias retired if present.
</success_criteria>

<output>
After completion, create `.planning/phases/67-remediation-cleanup/67-08-SUMMARY.md`
</output>
