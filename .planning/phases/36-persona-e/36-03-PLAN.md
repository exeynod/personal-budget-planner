---
phase: 36-persona-e
plan: 03
type: execute
wave: 3
depends_on: [36-01]
requirements:
  - REQ-36-03
commit: 8e3d32b
---

<objective>
CSV export endpoint `GET /api/v1/tax/export.csv` (Pro-gated) — выгрузка
факт-транзакций пользователя за период в Excel-совместимый формат
(UTF-8 с BOM, RFC 4180). Self-contained: денорм category.code/name/tag.
</objective>

## Task

1. Service `app/services/csv_export.py`:
   - `CSV_HEADERS` константа (10 столбцов).
   - `export_user_transactions_csv(db, user_id, period_start, period_end)`
     → `str` (CSV с UTF-8 BOM в первом символе). Join с `category` для
     денорм code/name/tag. Sort `tx_date DESC`.
2. Endpoint `GET /api/v1/tax/export.csv` (append to `app/api/routes/tax.py`)
   — Pro-gated; query params `period_start`, `period_end`. Response
   `text/csv; charset=utf-8` + `Content-Disposition: attachment`.
3. Tests `tests/test_csv_export.py` — 2 scenarios:
   - Period с одной transaction → header row + 1 data row, все поля
     корректны (date, code, name, cat-tag, amount, kind, txn-tag, note, source).
   - Empty period → только header row (Excel-friendly empty export).

## Acceptance

- `pytest tests/test_csv_export.py -v` → 2 passed.
- BOM присутствует first char (`csv_str.startswith("﻿")`).
- Manual smoke `curl /api/v1/tax/export.csv?...` возвращает корректный
  CSV (откроется в Excel без warnings о encoding).

## Commit

`8e3d32b` — feat(36-03): CSV export endpoint /tax/export.csv (REQ-36-03)
