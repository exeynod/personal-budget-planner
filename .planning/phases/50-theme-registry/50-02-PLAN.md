---
phase: 50-theme-registry
plan: 02
type: execute
wave: 2
depends_on: [50-01]
files_modified:
  - frontend/src/stylesV10/themes/registry.ts
  - frontend/src/hooks/useTheme.ts
  - frontend/src/hooks/useTheme.test.ts
  - frontend/src/main.tsx (bootstrap hydration)
  - ios/BudgetPlanner/App/BudgetPlannerApp.swift (@AppStorage binding)
autonomous: true
requirements: [THEME-01, THEME-02, THEME-04]
status: complete
commit: 972046a
---

# Plan 50-02 — useTheme hook + iOS @AppStorage env binding

Web: `themes/registry.ts` (`Theme` type + helpers) + `useTheme()` hook (mirror `useHomeColor`):
localStorage[`ui.theme`] read/write, whitelist validation, default = `maximal_poster`, setter
dispatches `theme-changed` CustomEvent + cross-tab storage listener. Bootstrap hydration
в `main.tsx` (apply `data-theme` attr до React mount — anti-flash).

iOS: `@AppStorage("ui.theme")` binding в `BudgetPlannerApp.swift`, environment injection через
`PosterTokens.currentTheme` static accessor (mirror v0.6 wise-tide pattern).

**Commit:** 972046a — `useTheme.test.ts` 6/6 green, tsc clean, iOS `make build` succeeded.
