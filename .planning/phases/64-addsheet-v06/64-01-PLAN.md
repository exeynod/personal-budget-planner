---
phase: 64-addsheet-v06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift
  - ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift
  - ios/BudgetPlannerTests/Features/Transactions/TransactionEditorAccountTests.swift
autonomous: true
requirements: [ADD-V10-04]
must_haves:
  truths:
    - "В режимах createActual/editActual в editor появляется секция «Счёт списания» с Picker'ом"
    - "Picker по умолчанию выбирает primary-счёт (или первый, если primary нет)"
    - "Опция «Не указан» (nil) доступна и сохраняет accountId=nil — текущее поведение «без счёта» работает"
    - "В planned-режимах секция счёта отсутствует"
    - "Если accounts не загрузились или список пуст — секция счёта скрыта, сохранение работает с accountId=nil"
    - "Выбранный accountId передаётся в ActualCreateRequest.accountId и ActualUpdateRequest.accountId"
    - "3 call-site (HomeView/TransactionsView/TemplateView) продолжают работать без изменений сигнатуры TransactionEditor(mode:categories:onSaved:onDelete?)"
  artifacts:
    - path: "ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift"
      provides: "ActualUpdateRequest расширен полем accountId (encodeIfPresent)"
      contains: "accountId"
    - path: "ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift"
      provides: "Account Picker секция + load accounts в .task + default primary?? first"
      contains: "AccountsAPI.list"
    - path: "ios/BudgetPlannerTests/Features/Transactions/TransactionEditorAccountTests.swift"
      provides: "Unit-тесты default-account логики + label форматирования"
  key_links:
    - from: "TransactionEditor.swift .task"
      to: "AccountsAPI.list()"
      via: "async load внутри editor"
      pattern: "AccountsAPI\\.list"
    - from: "TransactionEditor.save()"
      to: "ActualCreateRequest.accountId / ActualUpdateRequest.accountId"
      via: "selectedAccountId передаётся в request"
      pattern: "accountId:"
---

<objective>
Добавить опциональный account Picker «Счёт списания» в `TransactionEditor` для actual-режимов (createActual / editActual). Editor расширяется in-place; публичный API `TransactionEditor(mode:categories:onSaved:onDelete?)` и 3 call-site не меняются. Счета грузятся внутри editor через `AccountsAPI.list()` в `.task`, default = primary ?? first, опция «Не указан» (nil) сохраняет текущее поведение.

Purpose: ADD-V10-04 — пользователь может указать счёт списания при записи факт-траты; без счёта остаётся валидным.
Output: расширенный `TransactionEditor.swift`, `ActualUpdateRequest` с полем `accountId`, unit-тесты default-account логики.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-addsheet-v06/64-CONTEXT.md

<interfaces>
<!-- Контракты из codebase. Использовать напрямую — без исследования. -->

AccountDTO (Networking/DTO/AccountDTO.swift):
```swift
enum AccountKind: String, Decodable { case card, cash, savings }
struct AccountDTO: Decodable, Identifiable, Equatable {
    let id: Int
    let bank: String
    let mask: String?
    let kind: AccountKind
    let balanceCents: Int
    let primary: Bool        // wire `primary` (alias is_primary), backend sorts primary first
    let createdAt: Date?
}
```

AccountsAPI (Networking/Endpoints/AccountsAPI.swift):
```swift
@MainActor enum AccountsAPI {
    static func list() async throws -> [AccountDTO]  // GET /api/v1/accounts
}
```

ActualCreateRequest (Networking/DTO/TransactionDTO.swift) — УЖЕ имеет accountId:
```swift
struct ActualCreateRequest: Encodable {
    let kind: String
    let amountCents: Int
    let categoryId: Int
    let txDate: String
    let description: String?
    var accountId: Int? = nil   // encodeIfPresent — null не уходит на wire
    // CodingKeys + custom encode(to:) уже реализованы
}
```

ActualUpdateRequest (Networking/DTO/TransactionDTO.swift) — НЕ имеет accountId (добавить в этом плане):
```swift
struct ActualUpdateRequest: Encodable {
    let amountCents: Int?
    let categoryId: Int?
    let txDate: String?
    let description: String?
}
```

ActualDTO (legacy, используется editActual) — ВНИМАНИЕ: НЕ содержит accountId:
```swift
struct ActualDTO: Decodable, Identifiable, Equatable {
    let id, periodId: Int
    let kind: CategoryKind
    let amountCents: Int
    let description: String?
    let categoryId: Int
    let txDate: Date
    let source: ActualSource
    let createdAt: Date?
}
```

Sibling Picker pattern (Features/Savings/SavingsDepositSheet.swift):
```swift
let defaultAccount = accounts.first(where: { $0.primary })?.id ?? accounts.first?.id
private func accountLabel(_ a: AccountDTO) -> String {
    a.bank + (a.mask.map { " ·\($0)" } ?? "")
}
// Picker("Счёт", selection: $selectedAccountId) { ForEach(accounts) { a in Text(accountLabel(a)).tag(Int?.some(a.id)) } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить ActualUpdateRequest полем accountId</name>
  <read_first>
    - ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift (ActualCreateRequest строки ~92-119 как образец encodeIfPresent + CodingKeys; ActualUpdateRequest строки ~121-126)
  </read_first>
  <files>ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift</files>
  <behavior>
    - Test: ActualUpdateRequest с accountId=5 кодируется в JSON содержащий "account_id":5
    - Test: ActualUpdateRequest с accountId=nil НЕ эмитит ключ "account_id" на wire (encodeIfPresent — как ActualCreateRequest)
    - Test: существующие поля (amountCents/categoryId/txDate/description) кодируются как раньше (convertFromSnakeCase → amount_cents и т.д.)
  </behavior>
  <action>
    Добавить `var accountId: Int? = nil` в `ActualUpdateRequest`. Реализовать `CodingKeys` + кастомный `encode(to:)` по образцу `ActualCreateRequest` (строки ~92-119): все nil-optional поля через `encodeIfPresent`, чтобы `null` не уходил на wire (бэкенд использует exclude_unset семантику). НЕ менять сигнатуру вызова в editor пока что — только структуру DTO. Это additive-изменение wire-контракта: существующие вызовы `ActualUpdateRequest(amountCents:categoryId:txDate:description:)` должны компилироваться (accountId имеет default `= nil`).
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug CODE_SIGNING_ALLOWED=NO build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ActualUpdateRequest имеет `var accountId: Int? = nil` + кастомный encode с encodeIfPresent
    - Существующий вызов в TransactionEditor.save() (editActual) компилируется без изменений
    - Build зелёный (iPhone 17 Pro)
  </acceptance_criteria>
  <done>ActualUpdateRequest содержит accountId; encodeIfPresent гарантирует отсутствие "account_id":null; проект собирается</done>
</task>

<task type="auto">
  <name>Task 2: Account Picker секция в TransactionEditor + load в .task + проброс accountId</name>
  <read_first>
    - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift (весь файл — расширяем in-place; populate() строки ~175-195, save() строки ~205-248, body Section'ы)
    - ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift (account Picker + accountLabel + default primary?? first — строки 48-49, 64-66, 92-102)
    - ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift (list())
  </read_first>
  <files>ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift</files>
  <action>
    Расширить `TransactionEditor` (ADD-V10-04), сохранив публичный API и 3 call-site неизменными:

    1. Добавить @State: `private var accounts: [AccountDTO] = []` и `private var selectedAccountId: Int? = nil`.
    2. В body, ТОЛЬКО для `mode.isActual` И когда `!accounts.isEmpty`, добавить новую Section «Счёт списания» с `Picker(selection: $selectedAccountId)`:
       - первая опция `Text("Не указан").tag(Int?.none)`
       - `ForEach(accounts) { a in Text(accountLabel(a)).tag(Int?.some(a.id)) }`
       - приватный helper `accountLabel(_ a: AccountDTO) -> String { a.bank + (a.mask.map { " ·\($0)" } ?? "") }` (копия sibling-паттерна).
       Секция скрыта если `accounts.isEmpty` (graceful — счета не загрузились/пусто).
    3. Добавить `.task { await loadAccounts() }` на Form (рядом с `.onAppear { populate() }`). `loadAccounts()`: только для `mode.isActual`; `guard accounts.isEmpty else { return }`; `do { let list = try await AccountsAPI.list(); accounts = list; if selectedAccountId == nil { selectedAccountId = list.first(where: { $0.primary })?.id ?? list.first?.id } } catch { /* graceful: print() only, секция остаётся скрытой, accountId=nil */ }`. НЕ показывать error banner — счёт опционален.
       ВАЖНО: не перетирать selectedAccountId если populate() уже его выставил (см. п.4).
    4. В `populate()`: для `.editActual` — legacy ActualDTO НЕ содержит accountId (см. interfaces), поэтому преселект из DTO невозможен; оставить selectedAccountId на default из loadAccounts (primary ?? first). Задокументировать это комментарием в коде («legacy ActualDTO has no accountId → default to primary; CONTEXT preselect-from-DTO N/A on legacy surface»). Для createActual — default тоже primary?? first (выставляется в loadAccounts).
    5. В `save()`: передать `accountId: selectedAccountId` в `ActualCreateRequest` (createActual) и в `ActualUpdateRequest` (editActual). Planned-ветки НЕ трогать (счёта нет). Поскольку accountId optional с default — при nil ничего не уходит на wire (поведение «без счёта» сохранено).

    НЕ менять: kind/amount/category/date/description секции, toolbar, detents, canSave, 3 call-site.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug CODE_SIGNING_ALLOWED=NO build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Секция «Счёт списания» рендерится только при mode.isActual && !accounts.isEmpty
    - Опция «Не указан» (nil) присутствует; default = primary ?? first
    - planned-режимы: секции счёта нет
    - save() передаёт selectedAccountId в Create/Update request; при nil "account_id" не уходит на wire
    - 3 call-site (HomeView:72, TemplateView:144, TransactionsView:326/333) компилируются без изменений
    - Build зелёный
  </acceptance_criteria>
  <done>Editor показывает account Picker для actual-режимов, default primary?? first, «Не указан» сохраняет nil, accounts грузятся в .task graceful, call-sites неизменны</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-тесты default-account логики + label форматирования</name>
  <read_first>
    - ios/BudgetPlannerTests/Features/Subscriptions/SubscriptionsViewModelTests.swift (XCTest convention, фикстуры DTO через dict+JSONDecoder для Decodable-only DTO)
    - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift (после Task 2 — нужна тестируемая логика)
  </read_first>
  <files>ios/BudgetPlannerTests/Features/Transactions/TransactionEditorAccountTests.swift</files>
  <action>
    Извлечь чистую логику default-account и label в тестируемую форму. Editor — struct View, поэтому добавить в TransactionEditor.swift (или новый маленький enum-helper `AccountPickerLogic` в том же файле / отдельном файле `Features/Transactions/AccountPickerLogic.swift`) две статические pure-функции:
      - `static func defaultAccountId(_ accounts: [AccountDTO]) -> Int?` → `accounts.first(where: { $0.primary })?.id ?? accounts.first?.id`
      - `static func label(_ a: AccountDTO) -> String` → `a.bank + (a.mask.map { " ·\($0)" } ?? "")`
    Использовать эти функции в editor (Task 2 рефактор: loadAccounts и accountLabel зовут их). Если создаёшь новый файл — он попадёт в build через xcodegen generate.

    Тесты (XCTest, `@testable import BudgetPlanner`), фикстуры AccountDTO через JSONDecoder с convertFromSnakeCase (Decodable-only — паттерн как в Subscriptions tests):
      - defaultAccountId: список с primary → возвращает id primary (не первого по порядку)
      - defaultAccountId: список без primary → возвращает id первого
      - defaultAccountId: пустой список → nil
      - label: bank+mask → "Тинькофф ·1234"
      - label: bank без mask → "Тинькофф"
    Минимум 5 тестов.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild test -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/TransactionEditorAccountTests 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ≥5 тестов проходят
    - defaultAccountId покрывает: primary-present / no-primary / empty
    - label покрывает: with-mask / without-mask
    - Editor использует те же pure-функции (single source of truth)
  </acceptance_criteria>
  <done>TransactionEditorAccountTests зелёные; default-account и label вынесены в pure-функции и переиспользуются editor'ом</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor → AccountsAPI (GET /accounts) | аутентифицированный owner-запрос; ответ — список своих счетов |
| editor → ActualAPI (create/update) | accountId дописывается в существующий wire-контракт (additive) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | Tampering | ActualUpdateRequest.accountId на wire | mitigate | encodeIfPresent — nil не эмитит "account_id":null, не ломает exclude_unset семантику бэкенда (как ActualCreateRequest) |
| T-64-01-02 | Denial-of-Service | AccountsAPI.list() падает / медленный | accept | загрузка в .task graceful: catch → секция скрыта, accountId=nil, сохранение работает; trade-off приемлем (счёт опционален) |
| T-64-01-03 | Info-disclosure | preselect accountId из чужого DTO | accept | single-tenant owner; AccountsAPI scoped по user; legacy ActualDTO вообще не содержит accountId |
</threat_model>

<verification>
- Build зелёный (iPhone 17 Pro) после `cd ios && xcodegen generate`
- `xcodebuild test -only-testing:BudgetPlannerTests/TransactionEditorAccountTests` зелёный
- grep подтверждает `AccountsAPI.list` и `accountId:` в TransactionEditor.swift
- 3 call-site (HomeView/TransactionsView/TemplateView) не изменены (git diff показывает только TransactionEditor.swift + TransactionDTO.swift + новый test/helper)
</verification>

<success_criteria>
- account Picker «Счёт списания» виден в createActual/editActual, скрыт в planned и при пустых accounts
- default = primary ?? first; «Не указан» сохраняет accountId=nil
- accountId уходит в ActualCreateRequest/ActualUpdateRequest; при nil — отсутствует на wire
- публичный API TransactionEditor и 3 call-site неизменны
- ≥5 unit-тестов зелёные
</success_criteria>

<output>
After completion, create `.planning/phases/64-addsheet-v06/64-01-SUMMARY.md`
</output>
