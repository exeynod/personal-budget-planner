---
plan_id: 59-02
phase: 59
title: TransactionsView body rewrite — 3-segment kind picker, subtabs, filter Menu, V10 rows
wave: 2
depends_on:
  - 59-01
files_modified:
  - ios/BudgetPlanner/Features/Transactions/TransactionsView.swift
autonomous: false
requirements:
  - v1.1.2-59-CONTEXT-area-2-categorykind-ux
  - v1.1.2-59-CONTEXT-area-3-subtabs-filter-ui
  - v1.1.2-59-CONTEXT-area-5-coexistence
threat_ids:
  - T-59-03
must_haves:
  truths:
    - "TransactionsView shows large title «Транзакции» with subtabs Picker in toolbar(.principal)."
    - "Kind picker in first List Section header is 3-segment Расходы/Доходы/Сбережения in .history; 2-segment in .plan."
    - "Roundup actuals visible in Расходы segment with arrow.up.forward mini-icon next to amount."
    - "Deposit actuals visible in Сбережения segment."
    - "Category filter Menu in toolbar(.topBarTrailing) — icon line.3.horizontal.decrease.circle (filled when active)."
    - "No persistence across launches: subTab=.history, kind=.expense, categoryFilter=nil, savingsSegmentSelected=false on cold start."
    - "Tap-to-edit on .expense/.income actuals opens TransactionEditor via bridge ActualV10DTO → ActualDTO."
    - "Tap-to-edit on .roundup/.deposit actuals is a no-op (rows are display-only this phase)."
    - "Build succeeds: xcodebuild test target green for TransactionsViewModelTests."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Transactions/TransactionsView.swift"
      provides: "View body rebuilt against V10 ViewModel; rows render ActualV10DTO; tap-to-edit bridges to legacy TransactionEditor"
      contains: "ActualV10DTO"
      contains: "arrow.up.forward"
      contains_not: "categoryFilter: Int?\n\\s*func deleteActual"
  key_links:
    - from: "TransactionsView body"
      to: "TransactionsViewModel.savingsSegmentSelected"
      via: "Picker(selection:)"
      pattern: "savingsSegmentSelected"
    - from: "Section header Picker"
      to: "viewModel.kind"
      via: "binding"
      pattern: "Picker.*selection:.*viewModel\\.kind"
    - from: "Tap on Actual row (expense/income)"
      to: "TransactionEditor sheet"
      via: "ActualV10DTO → ActualDTO bridge function"
      pattern: "func bridgeToLegacyActual\\|legacyEditingActual"
---

<objective>
Переписать тело `TransactionsView` (lines 96–328 of `ios/BudgetPlanner/Features/Transactions/TransactionsView.swift`) под новый ViewModel из 59-01: subtabs в `.toolbar(.principal)`, 3-сегментный kind picker (Расходы/Доходы/Сбережения) в первой Section, фильтр категорий через `Menu` в `.toolbar(.topBarTrailing)`, рендеринг строк из `ActualV10DTO` с mini-icon для roundup, новый `ActualRow`/`PlannedRow` принимают `CategoryV10DTO?`. Реализовать bridge `ActualV10DTO → ActualDTO` для совместимости с `TransactionEditor` (legacy editor untouched per D-05) — tap-to-edit активен только для `.expense`/`.income`; `.roundup`/`.deposit` rows display-only. Empty/loading/error states остаются (но `.error` теперь banner не fullscreen — переменная `deleteError` обрабатывается в 59-03).

Purpose: восстановить compile-готовность файла после ViewModel-миграции (59-01 оставил View body broken intentionally) и закрыть UX-решения D-02 + D-03. Файл становится production-ready, кроме swipe-to-delete с confirmationDialog/banner — это 59-03.

Output:
- `TransactionsView` body полностью переписан.
- `ActualRow` принимает `ActualV10DTO` + `CategoryV10DTO?` + flag для savings-segment context.
- `PlannedRow` принимает `CategoryV10DTO?` (kind enum same; minimal change).
- Bridge function `legacyActualDTO(from: ActualV10DTO, categories: [CategoryV10DTO]) -> ActualDTO?` (returns nil for roundup/deposit).
- `xcodebuild test` зелёный для `TransactionsViewModelTests` из 59-01 Task 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/59-transactions-v06/59-CONTEXT.md
@.planning/phases/59-transactions-v06/59-01-SUMMARY.md
@.planning/phases/65-category-detail/65-01-SUMMARY.md
@ios/BudgetPlanner/Features/Transactions/TransactionsView.swift
@ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift
@ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10View.swift
@ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift
@ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift
@ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift
@ios/BudgetPlanner/Networking/DTO/CommonDTO.swift

<interfaces>
<!-- Key types/contracts for the View rewrite. -->

From 59-01 (post-Task-1) `TransactionsViewModel`:
```swift
@MainActor @Observable
final class TransactionsViewModel {
    enum LoadState: Equatable { case idle, loading, loaded, error(String) }
    private(set) var status: LoadState = .idle  // renamed from `state` to match V10
    var period: PeriodDTO?
    var actuals: [ActualV10DTO] = []
    var planned: [PlannedDTO] = []
    var categories: [CategoryV10DTO] = []
    var subTab: TxSubTab = .history
    var kind: CategoryKind = .expense        // 2-valued, drives expense/income
    var savingsSegmentSelected: Bool = false  // synthetic 3rd segment
    var categoryFilter: Int?
    var deleteError: String? = nil

    func load() async
    func deleteActual(id: Int) async
    func deletePlanned(id: Int) async
    func clearDeleteError()
    func category(_ id: Int) -> CategoryV10DTO?
    var filteredActuals: [ActualV10DTO] { get }
    var filteredPlanned: [PlannedDTO] { get }
    var visibleCategories: [CategoryV10DTO] { get }
    var dayGroups: [TxDayGroup] { get }
}
```

NOTE on naming: 59-01 keeps the field name `status` (was `state` in legacy) — adjust View references accordingly.

From ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift:
```swift
enum TransactionEditorMode: Identifiable {
    case createActual
    case createPlanned(periodId: Int)
    case editActual(ActualDTO)             // <-- LEGACY DTO required
    case editPlanned(PlannedDTO)
}
struct TransactionEditor: View {
    let mode: TransactionEditorMode
    let categories: [CategoryDTO]          // <-- LEGACY categories
    let onSaved: () async -> Void
}
```

CategoryV10DTO → CategoryDTO bridge: trivial, both share 2-valued CategoryKind. Map only the field subset CategoryDTO needs.

From ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift (legacy ActualDTO shape):
```swift
struct ActualDTO {
    let id: Int; let periodId: Int; let kind: CategoryKind  // 2-valued
    let amountCents: Int; let description: String?
    let categoryId: Int; let txDate: Date
    let source: ActualSource; let createdAt: Date?
}
```

ActualV10DTO → ActualDTO bridge: only valid when `v10.kind ∈ {.expense, .income}` (since legacy CategoryKind has no roundup/deposit case). Function returns nil otherwise.

From ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift:
```swift
struct TxDayGroup: Identifiable, Equatable {
    let id: String; let dateLabel: String; let dateKey: String
    let rows: [ActualV10DTO]; let sumCents: Int
}
enum TransactionsData {
    static func groupByDay(_ actuals: [ActualV10DTO], today: Date, calendar: Calendar) -> [TxDayGroup]
    static func tagFor(_ tx: ActualV10DTO) -> TxTag?  // .roundup / .deposit / nil
}
```

`V10Formatters.formatDay(_:today:calendar:)` returns "Сегодня"/"Вчера"/"N мая" (per TransactionsDataTests). Already in module.

From ios/BudgetPlanner/Features/Common/MoneyFormatter or similar — confirm by grep `MoneyFormatter.formatWithSymbol(cents:)` returns ruble-formatted with ₽.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite TransactionsView body — subtabs, kind picker, filter Menu, rows</name>
  <files>ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Transactions/TransactionsView.swift (current full file post-59-01)
    - ios/BudgetPlanner/Features/Transactions/TransactionEditor.swift (lines 1–60 — mode + struct signature)
    - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10View.swift (lines 60–170 — toolbar + List structure for reference)
    - ios/BudgetPlanner/Networking/DTO/CommonDTO.swift (lines 17–30 — CategoryKind + CategoryDTO)
  </read_first>
  <action>
    Replace the entire View body and inner `ActualRow` / `PlannedRow` private structs in `TransactionsView.swift` (everything from line ~96 to end of file).

    Implementation per D-02 + D-03:

    **A. Top-level `body`:**
    ```swift
    var body: some View {
        NavigationStack {
            content
                .navigationTitle("Транзакции")
                .toolbar {
                    ToolbarItem(placement: .principal) {
                        subTabPicker     // segmented История/План
                    }
                    ToolbarItem(placement: .topBarTrailing) {
                        categoryFilterMenu
                    }
                }
        }
        .task { await viewModel.load() }
        .sheet(item: $legacyEditingActual) { legacyActual in
            TransactionEditor(
                mode: .editActual(legacyActual),
                categories: legacyCategories,
                onSaved: { await viewModel.load() }
            )
        }
        .sheet(item: $editingPlanned) { plan in
            TransactionEditor(
                mode: .editPlanned(plan),
                categories: legacyCategories,
                onSaved: { await viewModel.load() }
            )
        }
    }
    ```
    Replace `@State editingActual: ActualDTO?` with `@State legacyEditingActual: ActualDTO?` (same semantics, renamed for clarity). Add `@State editingPlanned: PlannedDTO?` (unchanged). Add computed `private var legacyCategories: [CategoryDTO]` that bridges each `CategoryV10DTO` → `CategoryDTO` (D-05 bridge for editor compat):
    ```swift
    private var legacyCategories: [CategoryDTO] {
        viewModel.categories.map { v in
            CategoryDTO(
                id: v.id, name: v.name, kind: v.kind,
                isArchived: v.isArchived, sortOrder: v.sortOrder,
                createdAt: v.createdAt
            )
        }
    }
    ```

    **B. `subTabPicker`:**
    ```swift
    private var subTabPicker: some View {
        Picker("Подтаб", selection: $viewModel.subTab) {
            ForEach(TxSubTab.allCases) { t in
                Text(t.rawValue).tag(t)
            }
        }
        .pickerStyle(.segmented)
        .frame(maxWidth: 280)
    }
    ```
    When user switches to `.plan` while `savingsSegmentSelected == true`, side-effect: set `savingsSegmentSelected = false` (via `.onChange(of: viewModel.subTab)` modifier attached to the picker). Rationale D-02: Plan subtab is 2-segment only.

    **C. `kindPicker` (in first Section header):**
    Use a Picker bound to a custom Binding<Int> that encodes the 3-segment state when subTab==.history, 2-segment when subTab==.plan:
    ```swift
    private var kindSegmentBinding: Binding<Int> {
        Binding<Int>(
            get: {
                if viewModel.savingsSegmentSelected { return 2 }
                return viewModel.kind == .expense ? 0 : 1
            },
            set: { idx in
                switch idx {
                case 0: viewModel.kind = .expense; viewModel.savingsSegmentSelected = false
                case 1: viewModel.kind = .income;  viewModel.savingsSegmentSelected = false
                case 2: viewModel.savingsSegmentSelected = true; viewModel.kind = .expense  // visual fallback so categoryFilter logic doesn't crash
                default: break
                }
                viewModel.categoryFilter = nil  // reset on segment change (D-03 — sane defaults)
            }
        )
    }

    @ViewBuilder
    private var kindPicker: some View {
        Picker("Тип", selection: kindSegmentBinding) {
            Text("Расходы").tag(0)
            Text("Доходы").tag(1)
            if viewModel.subTab == .history {
                Text("Сбережения").tag(2)
            }
        }
        .pickerStyle(.segmented)
        .listRowBackground(Color.clear)
        .listRowInsets(EdgeInsets(top: 0, leading: 16, bottom: 8, trailing: 16))
        .listRowSeparator(.hidden)
    }
    ```

    **D. `content` switch:**
    ```swift
    @ViewBuilder
    private var content: some View {
        switch viewModel.status {
        case .idle, .loading: loadingView
        case .error(let msg): errorView(msg)
        case .loaded: loadedList
        }
    }

    private var loadingView: some View {
        ProgressView().controlSize(.large)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }

    private func errorView(_ msg: String) -> some View {
        ContentUnavailableView {
            Label("Не удалось загрузить", systemImage: "exclamationmark.triangle")
        } description: {
            Text(msg)
        } actions: {
            Button("Повторить") { Task { await viewModel.load() } }
                .buttonStyle(.borderedProminent)
        }
    }

    private var loadedList: some View {
        List {
            Section {
                kindPicker
            }
            if viewModel.subTab == .history {
                historySections
            } else {
                plannedSections
            }
        }
        .listStyle(.insetGrouped)
        .refreshable { await viewModel.load() }
    }
    ```

    **E. `historySections`:**
    Use `viewModel.dayGroups` (already filteredActuals-aware from 59-01). Group header shows "Сегодня"/"Вчера"/displayDayShort via group.dateLabel; sum from group.sumCents.
    ```swift
    @ViewBuilder
    private var historySections: some View {
        if viewModel.dayGroups.isEmpty {
            emptySection(message: emptyHistoryMessage)
        } else {
            ForEach(viewModel.dayGroups) { group in
                Section {
                    ForEach(group.rows) { actual in
                        ActualRow(
                            actual: actual,
                            category: viewModel.category(actual.categoryId)
                        )
                        .contentShape(Rectangle())
                        .onTapGesture {
                            if let bridged = legacyActualDTO(from: actual) {
                                legacyEditingActual = bridged
                            }
                            // roundup/deposit are display-only (D-02 + scope guard)
                        }
                        // swipeActions added by 59-03
                    }
                } header: {
                    HStack {
                        Text(group.dateLabel)
                        Spacer()
                        Text(MoneyFormatter.formatWithSymbol(cents: group.sumCents))
                            .monospacedDigit()
                    }
                }
            }
        }
    }

    private var emptyHistoryMessage: String {
        if viewModel.savingsSegmentSelected {
            return "Нет операций по копилке. Roundup и пополнения появятся здесь."
        }
        return "Нет операций. Добавьте трату или измените фильтры."
    }
    ```

    Bridge function (file-private, near bottom):
    ```swift
    private func legacyActualDTO(from v: ActualV10DTO) -> ActualDTO? {
        let legacyKind: CategoryKind
        switch v.kind {
        case .expense: legacyKind = .expense
        case .income:  legacyKind = .income
        case .roundup, .deposit: return nil   // editor doesn't support 4-valued kinds
        }
        return ActualDTO(
            id: v.id, periodId: v.periodId, kind: legacyKind,
            amountCents: v.amountCents, description: v.description,
            categoryId: v.categoryId, txDate: v.txDate,
            source: v.source, createdAt: v.createdAt
        )
    }
    ```

    **F. `plannedSections`:**
    Per D-02: when `savingsSegmentSelected == true && subTab == .plan` → ViewModel returns empty filteredPlanned. Show fallback empty state:
    ```swift
    @ViewBuilder
    private var plannedSections: some View {
        let plans = viewModel.filteredPlanned
        if viewModel.savingsSegmentSelected {
            emptySection(message: "Планирование сбережений — в разделе Копилка (Phase 62).")
        } else if plans.isEmpty {
            emptySection(message: "Нет планов. Создайте через + или примените шаблон.")
        } else {
            ForEach(plannedGroups(plans: plans), id: \.id) { group in
                Section {
                    ForEach(group.items) { plan in
                        PlannedRow(plan: plan, category: viewModel.category(plan.categoryId))
                            .contentShape(Rectangle())
                            .onTapGesture { editingPlanned = plan }
                        // swipeActions: planned delete handled by 59-03 (out of scope here)
                    }
                } header: {
                    HStack {
                        Text(group.category?.name ?? "—")
                        Spacer()
                        Text(MoneyFormatter.formatWithSymbol(cents: group.total))
                            .monospacedDigit()
                    }
                }
            }
        }
    }

    private func plannedGroups(plans: [PlannedDTO]) -> [(id: Int, category: CategoryV10DTO?, items: [PlannedDTO], total: Int)] {
        let groups = Dictionary(grouping: plans) { $0.categoryId }
        return groups
            .map { (
                id: $0.key,
                category: viewModel.category($0.key),
                items: $0.value.sorted { $0.id < $1.id },
                total: $0.value.reduce(0) { $0 + $1.amountCents }
            ) }
            .sorted {
                ($0.category?.sortOrder ?? Int.max) < ($1.category?.sortOrder ?? Int.max)
            }
    }
    ```

    **G. `categoryFilterMenu`:** unchanged structurally — iterate `viewModel.visibleCategories` (now `[CategoryV10DTO]`). Icon unchanged.

    **H. `emptySection(message:)`** unchanged.

    **I. `ActualRow` (private struct) rewrite:**
    Accept `ActualV10DTO` + `CategoryV10DTO?`. Render arrow.up.forward mini-icon when `actual.kind == .roundup` (D-02). Color amount text:
    - .expense → .primary
    - .income → .green
    - .roundup → .orange (subtle distinction; aligns with Tokens.Categories visual)
    - .deposit → .blue
    Layout mirrors current (icon + label + meta + amount, padding 2). Use `Tokens.Categories.visual(for: category?.name ?? "")` for the leading icon (unchanged behaviour).

    ```swift
    private struct ActualRow: View {
        let actual: ActualV10DTO
        let category: CategoryV10DTO?

        private var visual: Tokens.Categories.Visual {
            Tokens.Categories.visual(for: category?.name ?? "")
        }

        var body: some View {
            HStack(spacing: 12) {
                Image(systemName: visual.icon)
                    .font(.body)
                    .foregroundStyle(visual.color)
                    .frame(width: 28, height: 28)
                    .background(visual.color.opacity(0.15), in: RoundedRectangle(cornerRadius: 8, style: .continuous))

                VStack(alignment: .leading, spacing: 2) {
                    Text(actual.description?.isEmpty == false ? actual.description! : (category?.name ?? "—"))
                        .font(.body).foregroundStyle(.primary).lineLimit(1)
                    Text(metaLine).font(.caption).foregroundStyle(.secondary).lineLimit(1)
                }

                Spacer(minLength: 8)

                HStack(spacing: 4) {
                    if actual.kind == .roundup {
                        Image(systemName: "arrow.up.forward")
                            .font(.caption2)
                            .foregroundStyle(.secondary)
                            .accessibilityLabel("Округление от родительской траты")
                    }
                    Text(amountText)
                        .font(.body.monospacedDigit().weight(.semibold))
                        .foregroundStyle(amountColor)
                }
            }
            .padding(.vertical, 2)
        }

        private var metaLine: String {
            let cat = category?.name ?? ""
            let source = actual.source == .bot ? "из бота" : nil
            return [cat, source].compactMap { $0 }.filter { !$0.isEmpty }.joined(separator: " · ")
        }

        private var amountText: String {
            let prefix: String
            switch actual.kind {
            case .income: prefix = "+"
            case .expense, .roundup, .deposit: prefix = "−"
            }
            return "\(prefix)\(MoneyFormatter.formatWithSymbol(cents: actual.amountCents))"
        }

        private var amountColor: Color {
            switch actual.kind {
            case .income: return .green
            case .expense: return .primary
            case .roundup: return .orange
            case .deposit: return .blue
            }
        }
    }
    ```

    **J. `PlannedRow`:** accept `CategoryV10DTO?` instead of `CategoryDTO?` (signature change only; body unchanged).

    Per D-03 (Persistence): NO `@AppStorage` in this file. Defaults (subTab=.history, kind=.expense, savingsSegmentSelected=false, categoryFilter=nil) reset on every cold launch via ViewModel default property initialisers (already in 59-01).

    Per T-59-03: error message in errorView is the fixed user-facing copy from ViewModel.status (already filtered through "не удалось загрузить транзакции" in 59-01). No new error-leak risk introduced.
  </action>
  <verify>
    <automated>grep -c "viewModel.status" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: ≥2 (content switch + error case usage). -->
    <automated>grep -c "savingsSegmentSelected" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: ≥3 (binding setter + empty-state branch + plannedSections fallback). -->
    <automated>grep -c "arrow.up.forward" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: ≥1 (roundup mini-icon). -->
    <automated>grep -c "ToolbarItem(placement: .principal)" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: 1. -->
    <automated>grep -c "Сбережения" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: ≥1 (3rd segment label). -->
    <automated>grep -c "legacyActualDTO" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: ≥2 (bridge fn definition + call site). -->
    <automated>grep -c "@AppStorage" ios/BudgetPlanner/Features/Transactions/TransactionsView.swift</automated>
    <!-- Expected: 0 (D-03: no persistence). -->
  </verify>
  <acceptance_criteria>
    - View body fully rewritten; refers to `viewModel.status` (not `viewModel.state`), `viewModel.savingsSegmentSelected`, `viewModel.dayGroups`, `viewModel.categories: [CategoryV10DTO]`.
    - 3-segment Picker (Расходы/Доходы/Сбережения) appears in `.history`; 2-segment in `.plan` (the `.if` clause hides Сбережения tag when not in .history).
    - Tap-to-edit on .expense/.income actuals bridges through `legacyActualDTO(from:)` and opens TransactionEditor with legacy DTO.
    - Tap-to-edit on .roundup/.deposit is a no-op.
    - Roundup rows show `arrow.up.forward` mini-icon.
    - No `@AppStorage` usage.
    - File parses as valid Swift (no syntax errors).
  </acceptance_criteria>
  <done>
    All grep gates pass. Manual sanity: `swiftc -parse ios/BudgetPlanner/Features/Transactions/TransactionsView.swift -sdk $(xcrun --sdk iphoneos --show-sdk-path) -target arm64-apple-ios17.0` returns 0 (or build verifies in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate xcodeproj + build + run TransactionsViewModelTests</name>
  <files>ios/BudgetPlanner.xcodeproj (regenerated artifact, not hand-edited)</files>
  <read_first>
    - ios/project.yml (full — confirm sources path includes BudgetPlannerTests directory recursively)
    - ios/Makefile (confirm `make build` and `make generate` invocations)
  </read_first>
  <action>
    1. Run `cd ios && xcodegen generate` to refresh `BudgetPlanner.xcodeproj` so the new test file `BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift` is picked up. The project.yml uses recursive `path: BudgetPlannerTests` so xcodegen will discover the new subdirectory automatically (no edits to project.yml needed).
    2. Run `make build` from `ios/` (or `xcodebuild -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug CODE_SIGNING_ALLOWED=NO build | xcbeautify`). Expect 0 errors. Warnings allowed only if pre-existing on `master` baseline; NEW warnings introduced by this plan must be addressed.
    3. Run the test target:
       ```
       xcodebuild test \
           -project ios/BudgetPlanner.xcodeproj \
           -scheme BudgetPlanner \
           -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
           -only-testing:BudgetPlannerTests/TransactionsViewModelTests \
           CODE_SIGNING_ALLOWED=NO | xcbeautify
       ```
       Expect all 10+ tests green.
    4. If `xcodegen` is not available (CI / sandbox), fall back to opening `BudgetPlanner.xcodeproj` and using XcodeBuildMCP's `build_run_sim` after manually adding the test file via project.pbxproj — but normally `xcodegen` is the source of truth (see CLAUDE.md / 65-01-SUMMARY.md).
    5. Verify `make format-check` returns 0 (swift-format clean).

    If a test fails because of stale ViewModel API expectations, this indicates 59-01 and 59-02 drifted out of sync — fix in the appropriate plan, do not paper over. Document any test deferred via XCTSkip with a `// TODO(59-XX)` comment + rationale.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate 2>&1 | tail -5</automated>
    <automated>cd ios && xcodebuild -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -configuration Debug CODE_SIGNING_ALLOWED=NO build 2>&1 | tail -20 | grep -E "BUILD SUCCEEDED|BUILD FAILED"</automated>
    <!-- Expected: BUILD SUCCEEDED -->
    <automated>cd ios && xcodebuild test -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/TransactionsViewModelTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30 | grep -E "Test Suite.*passed|Test Suite.*failed"</automated>
    <!-- Expected: "Test Suite 'TransactionsViewModelTests' passed" -->
    <automated>cd ios && make format-check 2>&1 | tail -3</automated>
    <!-- Expected: exit 0 -->
  </verify>
  <acceptance_criteria>
    - xcodegen generate succeeds.
    - `xcodebuild build` returns BUILD SUCCEEDED for the simulator target.
    - `xcodebuild test` for TransactionsViewModelTests reports all tests passed.
    - `make format-check` exits 0.
    - 0 NEW compiler warnings introduced (compare with `master` baseline if necessary by stashing changes and re-running).
  </acceptance_criteria>
  <done>
    All verify gates green. Build artifacts exist under `~/Library/Developer/Xcode/DerivedData/BudgetPlanner-*/Build/Products/Debug-iphonesimulator/BudgetPlanner.app`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual smoke — subtabs / kind picker / filter Menu / row decorations</name>
  <what-built>
    - `ui.theme=v06` MainShell → Транзакции tab now renders rewritten native view.
    - Subtabs (История / План) in toolbar principal.
    - 3-segment kind picker in first Section (Расходы/Доходы/Сбережения when in История; 2-segment in План).
    - Category filter Menu in toolbar trailing — icon `line.3.horizontal.decrease.circle` (filled when active).
    - Roundup actuals visible in Расходы tab with `arrow.up.forward` mini-icon next to amount.
    - Deposit actuals visible in Сбережения segment.
    - Tap on .expense/.income row opens existing TransactionEditor sheet.
    - Tap on .roundup/.deposit row is a no-op (display-only).
    - Switching to План while in Сбережения shows fallback empty state «Планирование сбережений — в разделе Копилка».
  </what-built>
  <how-to-verify>
    Pre-req: backend up with at least one period containing 5+ actuals across kinds .expense / .income / .roundup / .deposit. If no roundup/deposit rows exist in dev DB, seed via SQL (mirror Phase 65 SUMMARY pattern):

    ```sql
    INSERT INTO actual_transaction (period_id, kind, amount_cents, description, category_id, tx_date, source, account_id)
    VALUES
      (<pid>, 'expense', 25000, 'Кафе', <cafe_cat_id>, CURRENT_DATE, 'mini_app', <acc_id>),
      (<pid>, 'income', 100000, 'ЗП', <salary_cat_id>, CURRENT_DATE, 'mini_app', <acc_id>),
      (<pid>, 'roundup', 50, NULL, <cafe_cat_id>, CURRENT_DATE, 'mini_app', <acc_id>),
      (<pid>, 'deposit', 10000, 'В копилку', <savings_cat_id>, CURRENT_DATE, 'mini_app', <acc_id>);
    ```

    Steps:
    1. Set theme: `defaults write com.exeynod.BudgetPlanner ui.theme v06` in simulator OR via in-app Settings (if v06 has the toggle wired).
    2. `make run` from `ios/`. App launches → MainShell → Транзакции tab.
    3. **Subtabs:** verify toolbar shows segmented control «История / План» in the principal slot. Tap «План» → list switches to plans (categories grouped). Tap «История» → back to actuals.
    4. **Kind picker (История):** in first Section header verify 3 segments: Расходы / Доходы / Сбережения. Tap Доходы → only income actuals shown. Tap Сбережения → roundup AND deposit actuals shown together.
    5. **Roundup decoration:** in Расходы segment, verify rows with kind=roundup show small `arrow.up.forward` icon to the left of the amount, amount text colour is orange (subtle).
    6. **Deposit in Сбережения:** tap Сбережения, verify deposit rows render with blue amount colour.
    7. **Kind picker (План):** tap «План» — verify only 2 segments visible (Расходы / Доходы). If previously selected Сбережения in История, the picker should now show Расходы (binding fallback per D-02). The Sсбережения case for План shows empty state «Планирование сбережений — в разделе Копилка» if user manually toggles savingsSegmentSelected (note: in this flow, the segment isn't reachable via UI in .plan, so this is more of a state-safety assertion).
    8. **Category filter:** in toolbar trailing tap the filter icon → Menu opens with «Все категории» + categories that appear in current selection. Pick one → list filters to that category only; icon switches to filled variant. Tap «Все категории» → reset.
    9. **Tap-to-edit (expense/income):** tap a row with kind=expense → TransactionEditor sheet opens with row data prefilled. Save → reload happens (full load() runs).
    10. **Tap-to-no-op (roundup/deposit):** tap a row with kind=roundup → nothing happens (no sheet opens, no error).
    11. **Cold-launch persistence check:** force-kill app, relaunch. Verify defaults: subTab=История, kind=Расходы, filter=nil. (D-03 — no @AppStorage.)
    12. **Empty states:** in dev DB with 0 transactions, verify empty section copy: «Нет операций. Добавьте трату или измените фильтры.» In Сбережения segment with 0 roundup/deposit rows: «Нет операций по копилке. Roundup и пополнения появятся здесь.»
  </how-to-verify>
  <resume-signal>
    Type "approved" to proceed to plan 59-03 (swipe-to-delete + confirmationDialog + banner). If issues, describe them so 59-02 can be revised before unlocking 59-03.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ViewModel ↔ View | Same-process; trusted. Bridge functions sanitise V10 → legacy DTO for editor compat. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | Information Disclosure | errorView copy | mitigate | View reads `viewModel.status.error` which is fixed Russian copy from 59-01; no error.localizedDescription path. |
</threat_model>

<verification>
- `grep -c "viewModel.status" file` ≥ 2.
- `grep -c "savingsSegmentSelected" file` ≥ 3.
- `grep -c "arrow.up.forward" file` ≥ 1.
- `grep -c "ToolbarItem(placement: .principal)" file` = 1.
- `grep -c "@AppStorage" file` = 0.
- `xcodebuild build` returns BUILD SUCCEEDED.
- `xcodebuild test -only-testing:BudgetPlannerTests/TransactionsViewModelTests` passes.
- Manual smoke checkpoint approved by user.
</verification>

<success_criteria>
1. `TransactionsView` compiles and renders without crashes against the 59-01 ViewModel.
2. Subtabs Picker in `.toolbar(.principal)`; segmented kindPicker in first Section; 3 segments in История, 2 in План.
3. Roundup rows show `arrow.up.forward` mini-icon and visible in Расходы segment; deposit rows visible in Сбережения segment.
4. Category filter Menu in `.toolbar(.topBarTrailing)` works (icon toggles to .fill variant when filter active).
5. Tap-to-edit works for .expense/.income; no-op for .roundup/.deposit.
6. No @AppStorage — defaults reset at cold launch.
7. TransactionsViewModelTests target passes (10+ tests green) — this validates 59-01 work end-to-end.
8. Manual smoke checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/59-transactions-v06/59-02-SUMMARY.md`:
- Screenshot from simulator (optional but recommended — store in `.planning/phases/59-transactions-v06/screenshots/`)
- xcodebuild test output snippet
- Decision log: roundup mini-icon final choice (arrow.up.forward vs alternatives), Сбережения empty state copy
- Known gaps:
  - Swipe-to-delete with confirmationDialog → 59-03
  - PlannedAPI swipe-to-delete → 59-03 or deferred
</output>
