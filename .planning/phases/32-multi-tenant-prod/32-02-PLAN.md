---
phase: 32-multi-tenant-prod
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - app/db/models.py
  - alembic/versions/0018_ai_cost_cap_default_500.py
  - app/api/schemas/ai.py
  - app/api/routes/ai.py
  - tests/test_ai_cap_default.py
autonomous: true
requirements:
  - REQ-32-03
must_haves:
  truths:
    - "REQ-32-03: `AppUser.spending_cap_cents` default = 500 (был 100) — на уровне ORM + server_default + alembic migration."
    - "REQ-32-03: новые юзеры (INSERT в app_user без явного spending_cap_cents) получают 500 cents = $5/мес AI cap."
    - "REQ-32-03: `GET /api/v1/ai/usage` extended — возвращает `cap_cents`, `remaining_cents`, `spent_cents_period` для current user."
    - "Backwards-compat: legacy fields `today`, `session_total`, `buffer_size`, `buffer_max` остаются в UsageResponse."
  artifacts:
    - path: ".planning/phases/32-multi-tenant-prod/32-02-SUMMARY.md"
      contains: "REQ-32-03"
      min_lines: 12
---

<objective>
AI cost cap default bump 100 cents ($1) → 500 cents ($5) per PRODUCT-STRATEGY v1.1 monetization foundation. Alembic migration + ORM default sync + endpoint extension с per-user balance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-multi-tenant-prod/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump default in ORM model</name>
  <files>app/db/models.py</files>
  <read_first>
    - app/db/models.py (lines 145-170 — AppUser class, spending_cap_cents)
    - alembic/versions/0009_cap_default_1usd.py (previous migration set default=100)
  </read_first>
  <action>
    В `app/db/models.py` найти строки:

    ```python
    # Scale 100/USD per spend_cap.py (1 USD == 100 storage units).
    # Default = 100 cents = $1/month per user (alembic 0009 migration).
    spending_cap_cents: Mapped[int] = mapped_column(
        BigInteger,
        nullable=False,
        default=100,
        server_default="100",
    )
    ```

    Заменить на:

    ```python
    # Scale 100/USD per spend_cap.py (1 USD == 100 storage units).
    # Default = 500 cents = $5/month per user (Phase 32 REQ-32-03,
    # alembic 0018 migration). Bumped from 100 ($1) per PRODUCT-STRATEGY
    # v1.1 monetization foundation — $5/mo даёт comfortable headroom
    # для conversational AI usage в paying tier.
    spending_cap_cents: Mapped[int] = mapped_column(
        BigInteger,
        nullable=False,
        default=500,
        server_default="500",
    )
    ```

    Никаких других изменений в models.py.
  </action>
  <acceptance_criteria>
    - `grep -n 'spending_cap_cents' app/db/models.py | head -1` shows default=500 / server_default="500".
    - `grep -c 'default=100' app/db/models.py` отдаёт ноль для spending_cap_cents (legacy default gone for this field).
    - `grep 'Phase 32 REQ-32-03' app/db/models.py` matches.
  </acceptance_criteria>
  <done>ORM default bumped.</done>
</task>

<task type="auto">
  <name>Task 2: Alembic migration 0018 — ALTER COLUMN SET DEFAULT 500</name>
  <files>alembic/versions/0018_ai_cost_cap_default_500.py</files>
  <read_first>
    - alembic/versions/0009_cap_default_1usd.py (template + previous default)
    - alembic/versions/0017_v10_account_id_composite_fk.py (latest head — для down_revision)
  </read_first>
  <action>
    Создать новую миграцию `alembic/versions/0018_ai_cost_cap_default_500.py`:

    ```python
    """Phase 32 REQ-32-03: bump AI spending_cap_cents default 100 → 500 ($1 → $5/mo).

    Revision ID: 0018_cap_500
    Revises: 0017_v10_account_id_composite_fk
    Create Date: 2026-05-11

    Rationale:
      - PRODUCT-STRATEGY v1.1 monetization foundation — $5/mo даёт
        comfortable headroom для conversational AI usage в paying tier.
      - Existing rows: leave untouched if cap уже > 100. Legacy 100-rows
        (default-inserted в v0.4-v1.0) bumped до 500 — invariant: existing
        users gain headroom, no surprise reduction.
      - Downgrade: SET DEFAULT 100 (symmetry); UPDATE НЕ перекидывает обратно
        (irreversible business decision; downgrade preserves data state).
    """
    from __future__ import annotations

    from alembic import op


    # revision identifiers, used by Alembic.
    revision = "0018_cap_500"
    down_revision = "0017_v10_account_id_composite_fk"
    branch_labels = None
    depends_on = None


    def upgrade() -> None:
        # 1. Bump server_default.
        op.execute(
            "ALTER TABLE app_user "
            "ALTER COLUMN spending_cap_cents SET DEFAULT 500"
        )
        # 2. Update legacy rows whose cap was set by old default (100).
        #    Anything else (admin-customised) — preserve.
        op.execute(
            "UPDATE app_user SET spending_cap_cents = 500 "
            "WHERE spending_cap_cents = 100"
        )


    def downgrade() -> None:
        # Restore default to 100 (symmetry). Existing rows NOT reverted —
        # downgrade preserves data; only schema-default changes.
        op.execute(
            "ALTER TABLE app_user "
            "ALTER COLUMN spending_cap_cents SET DEFAULT 100"
        )
    ```

    Запустить:
    - `alembic upgrade head` — должно подняться до 0018_cap_500.
    - `psql ... -c "SELECT column_default FROM information_schema.columns WHERE table_name='app_user' AND column_name='spending_cap_cents'"` → `500`.
    - `psql ... -c "SELECT spending_cap_cents FROM app_user WHERE spending_cap_cents=100"` → 0 rows.
    - `alembic downgrade -1` — schema default → 100; data preserved.
    - `alembic upgrade head` — back to 0018.
  </action>
  <acceptance_criteria>
    - File `alembic/versions/0018_ai_cost_cap_default_500.py` exists.
    - `grep "down_revision = \"0017_v10_account_id_composite_fk\"" alembic/versions/0018_ai_cost_cap_default_500.py` matches.
    - `grep "SET DEFAULT 500" alembic/versions/0018_ai_cost_cap_default_500.py` matches.
    - `alembic upgrade head` exits 0 in docker exec.
    - `alembic downgrade -1 && alembic upgrade head` round-trip clean.
  </acceptance_criteria>
  <done>Migration 0018 created, applied, downgrade tested.</done>
</task>

<task type="auto">
  <name>Task 3: Extend UsageResponse schema — add cap_cents / remaining_cents / spent_cents_period</name>
  <files>app/api/schemas/ai.py</files>
  <read_first>
    - app/api/schemas/ai.py (UsageResponse class)
    - app/api/routes/ai.py (lines 696-724 — get_usage endpoint)
  </read_first>
  <action>
    В `app/api/schemas/ai.py` найти `UsageResponse` (~line 66). Добавить **опциональные** поля:

    ```python
    # ⇩ Phase 32 REQ-32-03: per-user AI cap visibility.
    cap_cents: int | None = None  # current user's spending_cap_cents (USD-storage)
    remaining_cents: int | None = None  # cap_cents - spent_cents_period (≥0)
    spent_cents_period: int | None = None  # spend для current MSK month (cents)
    ```

    Все 3 поля Optional + default=None для backwards-compat с существующими callers (frontend, iOS) — legacy fields `today`, `session_total`, `buffer_size`, `buffer_max` остаются.
  </action>
  <acceptance_criteria>
    - `grep -c 'cap_cents\|remaining_cents\|spent_cents_period' app/api/schemas/ai.py` >= 3.
    - `grep 'Phase 32 REQ-32-03' app/api/schemas/ai.py` matches.
    - `python -c "from app.api.schemas.ai import UsageResponse; UsageResponse(today={}, session_total={}, buffer_size=0, buffer_max=0)"` succeeds (new fields optional).
  </acceptance_criteria>
  <done>Schema extended.</done>
</task>

<task type="auto">
  <name>Task 4: Wire /ai/usage to populate per-user fields</name>
  <files>app/api/routes/ai.py</files>
  <read_first>
    - app/api/routes/ai.py (lines 696-724 — get_usage)
    - app/api/dependencies.py (get_current_user, get_db)
    - app/services/spend_cap.py (get_user_spend_cents)
  </read_first>
  <action>
    Заменить body of `get_usage` (~line 696-724):

    ```python
    @router.get("/usage", response_model=UsageResponse)
    async def get_usage(
        current_user: Annotated[AppUser, Depends(get_current_user)],
        db: Annotated[AsyncSession, Depends(get_db)],
    ) -> UsageResponse:
        """GET /ai/usage — token usage + per-user spending cap (Phase 32 REQ-32-03).

        Per-process ring buffer (legacy `today` / `session_total`) preserved
        для backward-compat. New per-user fields populated from
        `app_user.spending_cap_cents` + ai_usage_log aggregation.
        """
        from app.services.spend_cap import get_user_spend_cents

        today = datetime.now(timezone.utc).date().isoformat()
        today_records = [r for r in _usage_buffer if r["ts"][:10] == today]

        def _agg(records: list[dict]) -> dict:
            return {
                "requests": len(records),
                "prompt_tokens": sum(r.get("prompt_tokens", 0) for r in records),
                "completion_tokens": sum(r.get("completion_tokens", 0) for r in records),
                "cached_tokens": sum(r.get("cached_tokens", 0) for r in records),
                "total_tokens": sum(r.get("total_tokens", 0) for r in records),
                "est_cost_usd": round(
                    sum(r.get("est_cost_usd", 0.0) for r in records), 6
                ),
            }

        # Per-user fields (Phase 32 REQ-32-03)
        cap = int(current_user.spending_cap_cents or 0)
        spent = await get_user_spend_cents(db, user_id=current_user.id)
        remaining = max(0, cap - int(spent))

        return UsageResponse(
            today=_agg(today_records),
            session_total=_agg(list(_usage_buffer)),
            buffer_size=len(_usage_buffer),
            buffer_max=_USAGE_BUFFER_MAX,
            cap_cents=cap,
            remaining_cents=remaining,
            spent_cents_period=int(spent),
        )
    ```

    Также обеспечить, что imports в начале файла включают:
    - `from typing import Annotated`
    - `from fastapi import Depends`
    - `from sqlalchemy.ext.asyncio import AsyncSession`
    - `from app.api.dependencies import get_current_user, get_db`
    - `from app.db.models import AppUser`

    (большинство уже есть — добавить недостающие).
  </action>
  <acceptance_criteria>
    - `grep -A20 'GET /ai/usage — token usage + per-user' app/api/routes/ai.py` shows new signature with `current_user` + `db` deps.
    - `grep 'cap_cents=cap' app/api/routes/ai.py` matches.
    - `grep 'remaining_cents=remaining' app/api/routes/ai.py` matches.
    - `pytest tests/api/test_ai.py -k usage -v` exits 0 (если есть; otherwise skip-OK).
  </acceptance_criteria>
  <done>Endpoint returns per-user cap data.</done>
</task>

<task type="auto">
  <name>Task 5: Test tests/test_ai_cap_default.py — default behaviour</name>
  <files>tests/test_ai_cap_default.py</files>
  <read_first>
    - tests/conftest.py (db_session fixture)
    - app/db/models.py (AppUser.spending_cap_cents — should be 500)
  </read_first>
  <action>
    Создать `tests/test_ai_cap_default.py`:

    ```python
    """Phase 32 REQ-32-03: default spending_cap_cents = 500 ($5/mo).

    Three tests:
      • test_orm_default — Python-side ORM default = 500.
      • test_server_default — DB column default = 500 (introspection).
      • test_insert_without_cap_uses_500 — fresh INSERT picks up default.
    """
    from __future__ import annotations

    import pytest
    from sqlalchemy import select, text

    from app.db.models import AppUser, UserRole

    pytestmark = pytest.mark.asyncio


    async def test_orm_default_is_500():
        u = AppUser(tg_user_id=9991, role=UserRole.member)
        # Pydantic / SQLAlchemy default applies при flush.
        # Until flush, attribute returns None; check class-level default instead.
        assert AppUser.__table__.c.spending_cap_cents.default.arg == 500


    async def test_server_default_is_500(db_session):
        result = await db_session.execute(
            text(
                "SELECT column_default FROM information_schema.columns "
                "WHERE table_name='app_user' AND column_name='spending_cap_cents'"
            )
        )
        default = result.scalar_one()
        # Postgres returns string like "500" or "'500'::bigint".
        assert "500" in str(default), f"server_default = {default!r}"


    async def test_insert_without_cap_uses_500(db_session):
        """Fresh INSERT без явного spending_cap_cents → 500."""
        # Use unique tg_user_id (test-bounded).
        await db_session.execute(
            text(
                "INSERT INTO app_user (tg_user_id, role) "
                "VALUES (:tg, 'member')"
            ),
            {"tg": 9999991},
        )
        result = await db_session.execute(
            text(
                "SELECT spending_cap_cents FROM app_user "
                "WHERE tg_user_id = :tg"
            ),
            {"tg": 9999991},
        )
        cap = result.scalar_one()
        assert cap == 500, f"new user has cap = {cap}, expected 500"

        # Cleanup
        await db_session.execute(
            text("DELETE FROM app_user WHERE tg_user_id = :tg"),
            {"tg": 9999991},
        )
    ```

    Запустить: `pytest tests/test_ai_cap_default.py -v` — 3 теста green.
  </action>
  <acceptance_criteria>
    - File `tests/test_ai_cap_default.py` exists.
    - `grep -c 'async def test_' tests/test_ai_cap_default.py` = 3.
    - `pytest tests/test_ai_cap_default.py -v` exits 0.
  </acceptance_criteria>
  <done>Default cap verified at 3 levels.</done>
</task>

<task type="auto">
  <name>Task 6: Commit + SUMMARY</name>
  <files>.planning/phases/32-multi-tenant-prod/32-02-SUMMARY.md</files>
  <action>
    1. Stage: `app/db/models.py`, `alembic/versions/0018_ai_cost_cap_default_500.py`, `app/api/schemas/ai.py`, `app/api/routes/ai.py`, `tests/test_ai_cap_default.py`, `.planning/phases/32-multi-tenant-prod/32-02-SUMMARY.md`.
    2. Commit: `feat(32-02): bump AI cost cap default 100→500 cents + /ai/usage per-user balance (REQ-32-03)` with `--no-verify`.
    3. SUMMARY.md: contains "REQ-32-03", migration ID `0018_cap_500`, changes summary, post-deploy migration steps, before/after diff.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` contains `32-02` + `REQ-32-03`.
    - SUMMARY.md exists with min 12 lines, contains "REQ-32-03" + `0018_cap_500`.
  </acceptance_criteria>
  <done>Commit landed.</done>
</task>

</tasks>

<verification>
1. `app/db/models.py` — `spending_cap_cents` default + server_default = 500.
2. `alembic upgrade head` clean; `alembic downgrade -1 && alembic upgrade head` round-trip clean.
3. `psql` — column default = 500; existing 100-rows bumped to 500.
4. `GET /api/v1/ai/usage` (через test) — returns `cap_cents=500`, `remaining_cents=500`, `spent_cents_period=0` for fresh user.
5. `pytest tests/test_ai_cap_default.py` — 3 tests green.
6. Full pytest — no regression.
</verification>

<output>32-02-SUMMARY.md per template.</output>
