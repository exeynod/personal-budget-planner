---
phase: 32-multi-tenant-prod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_rls_audit.py
  - tests/test_multitenancy_live.py
autonomous: true
requirements:
  - REQ-32-01
must_haves:
  truths:
    - "REQ-32-01: новый `tests/test_rls_audit.py` проверяет ENABLE+FORCE RLS на всех 13 доменных таблицах через `pg_class.relrowsecurity` + `pg_policies`."
    - "REQ-32-01: новый `tests/test_multitenancy_live.py` создаёт 2 users, userA создаёт `actual_transaction` → userB session под `app` (non-superuser) ролью НЕ видит row через прямой SELECT."
    - "Все pytest проходят (`pytest tests/test_rls_audit.py tests/test_multitenancy_live.py -v`) — exit 0."
  artifacts:
    - path: ".planning/phases/32-multi-tenant-prod/32-01-SUMMARY.md"
      contains: "REQ-32-01"
      min_lines: 12
---

<objective>
Backstop pytest для production multi-tenant — ДО любых других изменений зафиксировать, что v0.4+v1.0 RLS работает на всех 13 таблицах + cross-tenant isolation soundnetss на уровне прямого SQL под non-superuser ролью. Audit-only — не меняем existing migration / policy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-multi-tenant-prod/32-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_rls_audit.py — pg_class introspection</name>
  <files>tests/test_rls_audit.py</files>
  <read_first>
    - tests/conftest.py (для async db_session fixture pattern)
    - tests/test_rls_policy.py (existing pattern, если есть; иначе test_multitenancy_isolation.py)
    - alembic/versions/0006_multitenancy.py (для списка 9 v0.4 tables)
    - alembic/versions/0012_v10_user_account.py (account RLS)
    - alembic/versions/0013_v10_category_ext.py (plan_template_item RLS)
    - alembic/versions/0014_v10_actual_goal_savings.py (goal, savings_config RLS)
  </read_first>
  <action>
    Создать `tests/test_rls_audit.py` со следующим содержимым:

    ```python
    """RLS audit — Phase 32 REQ-32-01.

    Проверяет, что все 13 доменных таблиц multi-tenant production-ready:
      • RLS enabled (`pg_class.relrowsecurity = true`)
      • RLS forced for table owner (`pg_class.relforcerowsecurity = true`)
      • Policy с `current_setting('app.current_user_id', true)::bigint` exists
        (по имени фильтрует через `pg_policies`).

    Это audit-test — не вносит изменения, только подтверждает state of v0.4 + v1.0.
    """
    from __future__ import annotations

    import pytest
    from sqlalchemy import text

    pytestmark = pytest.mark.asyncio

    # 9 v0.4 (Phase 11 + Phase 13) + 4 v1.0 (Phase 22 BE-16 + ai_usage_log)
    TENANT_TABLES = [
        # Phase 11
        "category",
        "budget_period",
        "plan_template_item",
        "planned_transaction",
        "actual_transaction",
        "subscription",
        "category_embedding",
        "ai_conversation",
        "ai_message",
        # Phase 13 ai_usage_log
        "ai_usage_log",
        # v1.0 (Phase 22)
        "account",
        "goal",
        "savings_config",
    ]

    @pytest.mark.parametrize("table", TENANT_TABLES)
    async def test_rls_enabled_and_forced(db_session, table):
        result = await db_session.execute(
            text(
                "SELECT relrowsecurity, relforcerowsecurity "
                "FROM pg_class WHERE relname = :t AND relkind = 'r'"
            ),
            {"t": table},
        )
        row = result.first()
        assert row is not None, f"Table {table} not found in pg_class"
        relrowsecurity, relforcerowsecurity = row
        assert relrowsecurity is True, f"RLS not enabled on {table}"
        assert relforcerowsecurity is True, f"RLS not forced on {table}"

    @pytest.mark.parametrize("table", TENANT_TABLES)
    async def test_rls_policy_uses_current_user_id_setting(db_session, table):
        """Each tenant table must have at least one policy that filters by
        `current_setting('app.current_user_id', ...)::bigint`."""
        result = await db_session.execute(
            text(
                "SELECT polname, pg_get_expr(polqual, polrelid) AS qual "
                "FROM pg_policy "
                "WHERE polrelid = (SELECT oid FROM pg_class WHERE relname = :t)"
            ),
            {"t": table},
        )
        rows = result.fetchall()
        assert rows, f"No RLS policies for {table}"
        has_user_id_filter = any(
            "current_setting" in (r[1] or "") and "app.current_user_id" in (r[1] or "")
            for r in rows
        )
        assert has_user_id_filter, (
            f"No policy with current_setting('app.current_user_id') filter on {table}; "
            f"policies found: {[(r[0], r[1]) for r in rows]}"
        )
    ```

    Запустить: `docker compose exec api pytest tests/test_rls_audit.py -v` (или эквивалент через `scripts/run-integration-tests.sh`). Все ассерты должны пройти.
  </action>
  <acceptance_criteria>
    - File `tests/test_rls_audit.py` exists with `TENANT_TABLES` list containing exactly 13 strings.
    - `grep -c '@pytest.mark.parametrize' tests/test_rls_audit.py` = 2 (two parametrized tests).
    - `grep -c 'current_setting' tests/test_rls_audit.py` >= 2.
    - `pytest tests/test_rls_audit.py -v` exits 0.
  </acceptance_criteria>
  <done>RLS audit test passes for all 13 tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_multitenancy_live.py — production isolation scenario</name>
  <files>tests/test_multitenancy_live.py</files>
  <read_first>
    - tests/conftest.py (two_tenants + _rls_test_role fixtures)
    - tests/test_multitenancy_isolation.py (existing 5 tests pattern)
    - app/db/session.py (set_tenant_scope helper)
    - app/db/models.py (ActualTransaction)
  </read_first>
  <action>
    Создать `tests/test_multitenancy_live.py`:

    ```python
    """Live multi-tenant production scenario — Phase 32 REQ-32-01.

    Конкретный production-style сценарий:
      1. Two users created via two_tenants fixture.
      2. userA вставляет actual_transaction под своим RLS-scope.
      3. Switch to non-superuser app role + set userB scope.
      4. Direct SELECT * FROM actual_transaction WHERE id=<userA_tx> → 0 rows.
      5. Direct UPDATE/DELETE row userA от имени userB → 0 affected rows.

    Это complement к Phase 11 test_multitenancy_isolation — там через service
    layer; здесь — raw SQL под non-superuser ролью.
    """
    from __future__ import annotations

    import pytest
    from sqlalchemy import select, text

    from app.db.models import ActualTransaction
    from app.db.session import set_tenant_scope

    pytestmark = pytest.mark.asyncio


    async def test_userB_cannot_see_userA_actual_via_raw_sql(
        two_tenants, db_session, _rls_test_role
    ):
        user_a = two_tenants["user_a"]
        user_b = two_tenants["user_b"]

        # Insert actual_transaction as userA (using superuser; RLS-bypass is OK
        # for setup — sets data in place). Phase 11 confirms that user_id-FK is
        # enforced.
        await set_tenant_scope(db_session, user_a["id"])
        # Need a valid period + category for userA — fixture seeds these.
        cat_a_id = user_a["category_ids"][0]
        period_a_id = user_a["period_ids"][0]
        # Insert via raw SQL to avoid ORM relationship coupling.
        result = await db_session.execute(
            text(
                "INSERT INTO actual_transaction "
                "(user_id, category_id, period_id, kind, amount_cents, txn_date, description) "
                "VALUES (:uid, :cid, :pid, 'expense', 100, CURRENT_DATE, 'phase32-test') "
                "RETURNING id"
            ),
            {"uid": user_a["id"], "cid": cat_a_id, "pid": period_a_id},
        )
        tx_id = result.scalar_one()
        await db_session.commit()

        # Switch to non-superuser app role + set userB scope.
        await db_session.execute(text(f"SET LOCAL ROLE {_rls_test_role}"))
        await set_tenant_scope(db_session, user_b["id"])

        # Direct SELECT для userA's tx_id — должно вернуть 0 rows.
        result = await db_session.execute(
            text("SELECT id FROM actual_transaction WHERE id = :id"),
            {"id": tx_id},
        )
        rows = result.fetchall()
        assert rows == [], (
            f"userB sees userA's transaction id={tx_id}: {rows}"
        )

        # Direct UPDATE attempt — должно вернуть 0 affected rows.
        result = await db_session.execute(
            text("UPDATE actual_transaction SET amount_cents = 999 WHERE id = :id"),
            {"id": tx_id},
        )
        assert result.rowcount == 0, (
            f"userB UPDATE on userA's tx affected {result.rowcount} rows"
        )

        # Direct DELETE attempt — должно вернуть 0 affected rows.
        result = await db_session.execute(
            text("DELETE FROM actual_transaction WHERE id = :id"),
            {"id": tx_id},
        )
        assert result.rowcount == 0, (
            f"userB DELETE on userA's tx affected {result.rowcount} rows"
        )

        # Cleanup: switch back to superuser + delete row.
        await db_session.execute(text("RESET ROLE"))
        await set_tenant_scope(db_session, user_a["id"])
        await db_session.execute(
            text("DELETE FROM actual_transaction WHERE id = :id"),
            {"id": tx_id},
        )


    async def test_userB_cannot_insert_actual_for_userA(
        two_tenants, db_session, _rls_test_role
    ):
        """Cross-tenant write attempt: userB session не может INSERT с user_id=userA."""
        user_a = two_tenants["user_a"]
        user_b = two_tenants["user_b"]

        cat_a_id = user_a["category_ids"][0]
        period_a_id = user_a["period_ids"][0]

        # Switch to non-superuser role + userB scope.
        await db_session.execute(text(f"SET LOCAL ROLE {_rls_test_role}"))
        await set_tenant_scope(db_session, user_b["id"])

        # Attempt INSERT with userA-credentials но user_id=userA — RLS WITH CHECK
        # должно reject (либо как 0 rows либо как RLS-violation).
        from sqlalchemy.exc import DBAPIError, ProgrammingError
        with pytest.raises((DBAPIError, ProgrammingError)):
            await db_session.execute(
                text(
                    "INSERT INTO actual_transaction "
                    "(user_id, category_id, period_id, kind, amount_cents, txn_date) "
                    "VALUES (:uid, :cid, :pid, 'expense', 50, CURRENT_DATE)"
                ),
                {"uid": user_a["id"], "cid": cat_a_id, "pid": period_a_id},
            )
            await db_session.commit()

        await db_session.rollback()
        await db_session.execute(text("RESET ROLE"))
    ```

    Запустить: `pytest tests/test_multitenancy_live.py -v` — оба теста зелёные.
  </action>
  <acceptance_criteria>
    - File `tests/test_multitenancy_live.py` exists.
    - `grep -c 'async def test_' tests/test_multitenancy_live.py` >= 2.
    - `grep 'SET LOCAL ROLE' tests/test_multitenancy_live.py` matches (uses non-superuser role).
    - `pytest tests/test_multitenancy_live.py -v` exits 0.
  </acceptance_criteria>
  <done>Cross-tenant raw-SQL isolation hard-tested.</done>
</task>

<task type="auto">
  <name>Task 3: Run audit suite + verify zero regression</name>
  <files>(no file changes — invocation only)</files>
  <action>
    1. Запустить весь pytest: `pytest tests/ -x -q` (или `scripts/run-integration-tests.sh`).
    2. Confirm exit 0 — никаких regressions.
    3. Specifically rerun: `pytest tests/test_rls_audit.py tests/test_multitenancy_live.py tests/test_multitenancy_isolation.py -v` — все green.
    4. If failure: investigate root cause, fix, document в SUMMARY.
  </action>
  <acceptance_criteria>
    - `pytest tests/test_rls_audit.py tests/test_multitenancy_live.py tests/test_multitenancy_isolation.py -v` exits 0.
    - Full pytest run exits 0 (or only pre-existing skips/xfails).
  </acceptance_criteria>
  <done>Audit suite passes alongside full test suite.</done>
</task>

<task type="auto">
  <name>Task 4: Commit + SUMMARY</name>
  <files>.planning/phases/32-multi-tenant-prod/32-01-SUMMARY.md</files>
  <action>
    1. Stage files: `tests/test_rls_audit.py`, `tests/test_multitenancy_live.py`, `.planning/phases/32-multi-tenant-prod/32-01-SUMMARY.md`.
    2. Commit message: `test(32-01): RLS audit + live multi-tenant isolation hard-test (REQ-32-01)` with `--no-verify`.
    3. SUMMARY.md follows template: contains "REQ-32-01", coverage matrix (13 tables), test count (2 audit + 2 isolation = 4 new tests), known limitations (e.g. что dev DB role is superuser if not switched).
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` contains `32-01` and `REQ-32-01`.
    - SUMMARY.md exists with min 12 lines and contains string "REQ-32-01".
  </acceptance_criteria>
  <done>Commit landed, SUMMARY written.</done>
</task>

</tasks>

<verification>
1. `tests/test_rls_audit.py` существует и pytest проходит для 13 таблиц.
2. `tests/test_multitenancy_live.py` существует и pytest проходит (raw SQL cross-tenant block).
3. SUMMARY.md создан в правильном формате.
4. Commit landed с правильным message + --no-verify.
</verification>

<output>32-01-SUMMARY.md per template.</output>
