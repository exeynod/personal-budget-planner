---
phase: 33-compliance
plan: 03
type: execute
wave: 3
depends_on: ["33-01"]
files_modified:
  - app/api/routes/me.py
  - app/api/routes/onboarding_v10.py
  - app/services/onboarding_v10.py
  - app/bot/handlers.py
  - app/bot/auth.py
  - tests/test_pdn_consent_flow.py
  - tests/test_bot_handlers_consent.py
autonomous: true
requirements:
  - REQ-33-02
must_haves:
  truths:
    - "REQ-33-02: новый `POST /api/v1/me/consent` (idempotent) ставит `pdn_consent_at = now()` + пишет audit-event `granted`."
    - "REQ-33-02: новый `DELETE /api/v1/me/consent` обнуляет `pdn_consent_at` + пишет audit-event `revoked`."
    - "REQ-33-02: `POST /api/v1/onboarding/complete` БЕЗ предварительного `pdn_consent_at IS NOT NULL` → 403 с `{\"error\": \"pdn_consent_required\", \"privacy_url\": \"/legal/privacy\"}`."
    - "REQ-33-02: bot `cmd_start` для user'ов с `pdn_consent_at IS NULL` шлёт prompt c WebApp кнопкой и явным текстом про принятие политики."
    - "`pytest tests/test_pdn_consent_flow.py tests/test_bot_handlers_consent.py -v` exits 0."
  artifacts:
    - path: ".planning/phases/33-compliance/33-03-SUMMARY.md"
      contains: "REQ-33-02"
      min_lines: 12
---

<objective>
Сшить consent flow на server-side: новые `/me/consent` endpoints (grant/revoke), gate на `/onboarding/complete`, bot prompt при отсутствии consent. Это центральный compliance plan — после этого ни один user не может пройти onboarding без явного согласия на обработку ПДн, и согласие audit-журналируется.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-compliance/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend app/api/routes/me.py с consent endpoints</name>
  <files>app/api/routes/me.py</files>
  <read_first>
    - app/api/routes/me.py (current PATCH /me handler)
    - app/api/dependencies.py (get_current_user, get_db)
    - app/services/pdn_audit.py (record_audit helper from 33-01)
    - app/db/models.py (PdnAuditEvent)
  </read_first>
  <action>
    В `app/api/routes/me.py` добавить следующие endpoints (после существующего PATCH /me):

    ```python
    from datetime import datetime, timezone
    from fastapi import Request, status
    from app.db.models import PdnAuditEvent
    from app.services.pdn_audit import record_audit


    @me_router.post(
        "/me/consent",
        status_code=status.HTTP_200_OK,
        responses={
            200: {"description": "Consent granted (idempotent)."},
        },
    )
    async def grant_consent(
        request: Request,
        current_user: Annotated[AppUser, Depends(get_current_user)],
        db: Annotated[AsyncSession, Depends(get_db)],
    ) -> dict:
        """Phase 33 CMP-33-04: idempotent consent grant.

        Sets `app_user.pdn_consent_at = now()` if not already set, writes
        an audit-event `granted`. Idempotent — повторный вызов с уже
        granted consent — no-op (audit event всё равно пишется).
        """
        now = datetime.now(timezone.utc)
        was_null = current_user.pdn_consent_at is None
        if was_null:
            current_user.pdn_consent_at = now
        await record_audit(
            db,
            user_id=current_user.id,
            event=PdnAuditEvent.granted,
            ip=request.client.host if request.client else None,
            metadata={"policy_version": "v0.1", "was_null": was_null},
        )
        await db.commit()
        return {
            "pdn_consent_at": current_user.pdn_consent_at.isoformat(),
            "policy_version": "v0.1",
        }


    @me_router.delete(
        "/me/consent",
        status_code=status.HTTP_200_OK,
    )
    async def revoke_consent(
        request: Request,
        current_user: Annotated[AppUser, Depends(get_current_user)],
        db: Annotated[AsyncSession, Depends(get_db)],
    ) -> dict:
        """Phase 33 CMP-33-04: revoke consent.

        Nulls `app_user.pdn_consent_at`, writes audit event `revoked`.
        После этого user не сможет пройти `/onboarding/complete` без
        нового grant.
        """
        was_set = current_user.pdn_consent_at is not None
        current_user.pdn_consent_at = None
        await record_audit(
            db,
            user_id=current_user.id,
            event=PdnAuditEvent.revoked,
            ip=request.client.host if request.client else None,
            metadata={"was_set": was_set},
        )
        await db.commit()
        return {"pdn_consent_at": None, "revoked": was_set}
    ```

    Если `me_router` пока не имел `Request` import — добавить `from fastapi import Request, status` (уже включён в FastAPI imports).
  </action>
  <acceptance_criteria>
    - `grep -c "@me_router.post(\"/me/consent\"" app/api/routes/me.py` matches.
    - `grep -c "@me_router.delete(\"/me/consent\"" app/api/routes/me.py` matches.
    - `grep -c "record_audit" app/api/routes/me.py` >= 2.
    - `grep -c "PdnAuditEvent.granted\|PdnAuditEvent.revoked" app/api/routes/me.py` >= 2.
    - `python -c "from app.api.routes.me import me_router"` exits 0.
  </acceptance_criteria>
  <done>Consent endpoints added to me_router.</done>
</task>

<task type="auto">
  <name>Task 2: Add consent gate в onboarding_v10 service</name>
  <files>app/services/onboarding_v10.py, app/api/routes/onboarding_v10.py</files>
  <read_first>
    - app/services/onboarding_v10.py (complete_v10 entrypoint)
    - app/api/routes/onboarding_v10.py:54-120 (POST /onboarding/complete handler)
    - app/db/models.py (AppUser.pdn_consent_at)
  </read_first>
  <action>
    1. В `app/services/onboarding_v10.py` найти `complete_v10()` и в начале функции, **перед** existing `OnboardingConflictError` checking, добавить:

    ```python
    # Phase 33 CMP-33-04: consent gate.
    # Load AppUser first и проверить pdn_consent_at IS NOT NULL.
    from app.db.models import AppUser as _AppUser
    user_row = await db.scalar(
        select(_AppUser).where(_AppUser.id == user_id)
    )
    if user_row is None:
        raise OnboardingConflictError(account_count=0)  # safety net
    if user_row.pdn_consent_at is None:
        raise PdnConsentRequiredError()
    ```

    2. В том же файле объявить (вблизи существующих exception classes):

    ```python
    class PdnConsentRequiredError(Exception):
        """Phase 33 CMP-33-04: raised when user attempts onboarding-complete
        without having granted ПДн consent first."""
        pass
    ```

    3. В `app/api/routes/onboarding_v10.py` функция `complete_v10()` — добавить exception handler **перед** `OnboardingConflictError` handler:

    ```python
    except onboarding_v10_svc.PdnConsentRequiredError as exc:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail={
                "error": "pdn_consent_required",
                "privacy_url": "/legal/privacy",
                "consent_endpoint": "/api/v1/me/consent",
                "message": "ПДн consent required before onboarding (152-ФЗ)",
            },
        ) from exc
    ```

    4. В docstring handler-функции отметить новый 403 в `responses=` dict:

    ```python
    responses={
        403: {"description": "PDN consent required (Phase 33 CMP-33-04)."},
        409: {"description": "User already onboarded (T-22-11-01)."},
        422: {"description": "Validation error (T-22-11-02..04)."},
    },
    ```
  </action>
  <acceptance_criteria>
    - `grep "PdnConsentRequiredError" app/services/onboarding_v10.py` matches.
    - `grep "pdn_consent_at is None" app/services/onboarding_v10.py` matches.
    - `grep "pdn_consent_required" app/api/routes/onboarding_v10.py` matches.
    - `grep "PdnConsentRequiredError" app/api/routes/onboarding_v10.py` matches.
    - `grep "HTTP_403_FORBIDDEN" app/api/routes/onboarding_v10.py` matches.
    - `python -c "from app.services.onboarding_v10 import PdnConsentRequiredError; from app.api.routes.onboarding_v10 import onboarding_v10_router"` exits 0.
  </acceptance_criteria>
  <done>Onboarding gate enforced server-side.</done>
</task>

<task type="auto">
  <name>Task 3: Bot /start prompt для user'ов без consent</name>
  <files>app/bot/handlers.py, app/bot/auth.py</files>
  <read_first>
    - app/bot/handlers.py (current cmd_start — full file)
    - app/bot/auth.py (bot_resolve_user_status — full file)
    - app/db/models.py (AppUser.pdn_consent_at)
  </read_first>
  <action>
    1. В `app/bot/auth.py` найти `async def bot_resolve_user_status(...)` и extend tuple-возвращение, добавив `pdn_consent_at`:

    ```python
    # Before:
    # return role, onboarded_at
    # After: extend select() для AppUser чтобы включить pdn_consent_at и
    # return tuple (role, onboarded_at, pdn_consent_at)
    ```

    Конкретно: в SELECT-запросе на AppUser добавить `.pdn_consent_at` и
    return-tuple expand до 3 элементов. Если функция возвращает default
    при отсутствии user — добавить `None` для pdn_consent_at.

    2. В `app/bot/handlers.py` `cmd_start` обновить unpack tuple и
    добавить branch **перед** `if onboarded_at is None:`:

    ```python
    role, onboarded_at, pdn_consent_at = await bot_resolve_user_status(user_id)

    # ... existing role check ...

    # Phase 33 CMP-33-04: consent gate (priority над onboarded_at).
    if pdn_consent_at is None:
        greeting = (
            "Добро пожаловать!\n\n"
            "Прежде чем начать, прочитайте нашу политику обработки "
            "персональных данных и подтвердите согласие в приложении.\n\n"
            "Откройте Mini App — на первом экране будет чекбокс."
        )
        await message.answer(greeting, reply_markup=_open_app_keyboard())
        logger.info(
            "bot.start.consent_required",
            tg_user_id=user_id,
            tg_chat_id=chat_id,
            chat_bound=chat_bound,
            role=role.value,
        )
        return
    ```
  </action>
  <acceptance_criteria>
    - `grep "pdn_consent_at" app/bot/auth.py` matches.
    - `grep "pdn_consent_at" app/bot/handlers.py` matches.
    - `grep "bot.start.consent_required" app/bot/handlers.py` matches.
    - `grep "политику обработки" app/bot/handlers.py` matches.
    - `python -c "from app.bot.handlers import cmd_start; from app.bot.auth import bot_resolve_user_status"` exits 0.
  </acceptance_criteria>
  <done>Bot prompts user без consent c приглашением в Mini App.</done>
</task>

<task type="auto">
  <name>Task 4: tests/test_pdn_consent_flow.py — full server-side flow</name>
  <files>tests/test_pdn_consent_flow.py</files>
  <read_first>
    - tests/conftest.py (auth fixtures, X-Test-User header pattern)
    - tests/test_e2e_multi_user_lifecycle.py (existing full-flow pattern)
    - app/api/routes/onboarding_v10.py (response shape)
    - app/api/routes/me.py (consent endpoints)
  </read_first>
  <action>
    Создать `tests/test_pdn_consent_flow.py`:

    ```python
    """Phase 33 CMP-33-04: end-to-end consent flow tests."""
    from __future__ import annotations

    import pytest
    from httpx import ASGITransport, AsyncClient
    from sqlalchemy import select

    from app.db.models import AppUser, PdnAuditEvent, PdnAuditLog

    pytestmark = pytest.mark.asyncio


    @pytest.fixture
    async def api_client():
        from main_api import app
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as client:
            yield client


    async def test_onboarding_complete_without_consent_returns_403(
        api_client, two_tenants, db_session
    ):
        """User who never called /me/consent cannot complete onboarding."""
        user = two_tenants["user_a"]
        # Ensure pdn_consent_at is NULL (fresh user; fixture may set it).
        await db_session.execute(
            text("UPDATE app_user SET pdn_consent_at = NULL WHERE id = :id"),
            {"id": user["id"]},
        )
        await db_session.commit()

        headers = {"X-Test-User": str(user["tg_user_id"])}
        r = await api_client.post(
            "/api/v1/onboarding/complete",
            headers=headers,
            json={
                "income_cents": 1000000,
                "accounts": [{"name": "Кошелёк", "balance_cents": 0, "is_primary": True}],
                "category_plans": [],
            },
        )
        assert r.status_code == 403
        detail = r.json()["detail"]
        assert detail["error"] == "pdn_consent_required"
        assert detail["privacy_url"] == "/legal/privacy"


    async def test_grant_consent_then_onboard_succeeds(
        api_client, two_tenants, db_session
    ):
        user = two_tenants["user_a"]
        # Reset consent + onboarded_at.
        await db_session.execute(
            text(
                "UPDATE app_user SET pdn_consent_at = NULL, onboarded_at = NULL "
                "WHERE id = :id"
            ),
            {"id": user["id"]},
        )
        await db_session.commit()

        headers = {"X-Test-User": str(user["tg_user_id"])}

        # Grant consent.
        r = await api_client.post("/api/v1/me/consent", headers=headers)
        assert r.status_code == 200
        body = r.json()
        assert body["pdn_consent_at"] is not None
        assert body["policy_version"] == "v0.1"

        # Verify audit event written.
        ev = await db_session.execute(
            select(PdnAuditLog)
            .where(PdnAuditLog.event_type == PdnAuditEvent.granted)
            .order_by(PdnAuditLog.id.desc())
            .limit(1)
        )
        last = ev.scalar_one()
        assert last is not None


    async def test_consent_grant_is_idempotent(api_client, two_tenants, db_session):
        user = two_tenants["user_a"]
        headers = {"X-Test-User": str(user["tg_user_id"])}

        r1 = await api_client.post("/api/v1/me/consent", headers=headers)
        first_ts = r1.json()["pdn_consent_at"]

        r2 = await api_client.post("/api/v1/me/consent", headers=headers)
        assert r2.status_code == 200
        # Second call doesn't overwrite — `was_null=False` in audit metadata.
        # But original timestamp preserved (no change in pdn_consent_at).
        second_ts = r2.json()["pdn_consent_at"]
        assert first_ts == second_ts


    async def test_revoke_consent_clears_field(api_client, two_tenants, db_session):
        user = two_tenants["user_a"]
        headers = {"X-Test-User": str(user["tg_user_id"])}

        await api_client.post("/api/v1/me/consent", headers=headers)
        r = await api_client.delete("/api/v1/me/consent", headers=headers)
        assert r.status_code == 200
        assert r.json()["pdn_consent_at"] is None
        # Verify revoked event.
        ev = await db_session.execute(
            select(PdnAuditLog)
            .where(PdnAuditLog.event_type == PdnAuditEvent.revoked)
            .order_by(PdnAuditLog.id.desc())
            .limit(1)
        )
        assert ev.scalar_one() is not None


    async def test_revoke_then_onboard_fails(api_client, two_tenants, db_session):
        user = two_tenants["user_a"]
        headers = {"X-Test-User": str(user["tg_user_id"])}

        # Reset onboarded_at so onboarding can run.
        await db_session.execute(
            text("UPDATE app_user SET onboarded_at = NULL WHERE id = :id"),
            {"id": user["id"]},
        )
        await db_session.commit()

        await api_client.post("/api/v1/me/consent", headers=headers)
        await api_client.delete("/api/v1/me/consent", headers=headers)

        r = await api_client.post(
            "/api/v1/onboarding/complete",
            headers=headers,
            json={
                "income_cents": 1000000,
                "accounts": [{"name": "К", "balance_cents": 0, "is_primary": True}],
                "category_plans": [],
            },
        )
        assert r.status_code == 403
        assert r.json()["detail"]["error"] == "pdn_consent_required"
    ```

    Import `text` from sqlalchemy at top of file. Запустить: `pytest tests/test_pdn_consent_flow.py -v`.
  </action>
  <acceptance_criteria>
    - File `tests/test_pdn_consent_flow.py` exists.
    - `grep -c "async def test_" tests/test_pdn_consent_flow.py` >= 5.
    - `pytest tests/test_pdn_consent_flow.py -v` exits 0.
  </acceptance_criteria>
  <done>Full server-side flow tested green.</done>
</task>

<task type="auto">
  <name>Task 5: tests/test_bot_handlers_consent.py — bot /start consent prompt</name>
  <files>tests/test_bot_handlers_consent.py</files>
  <read_first>
    - tests/test_bot_handlers.py (existing aiogram mock pattern)
    - app/bot/handlers.py (cmd_start with consent branch)
  </read_first>
  <action>
    Создать `tests/test_bot_handlers_consent.py`:

    ```python
    """Phase 33 CMP-33-04: bot /start consent-prompt tests."""
    from __future__ import annotations

    from unittest.mock import AsyncMock, MagicMock

    import pytest

    from app.bot.handlers import cmd_start
    from app.db.models import UserRole

    pytestmark = pytest.mark.asyncio


    def _mock_message(user_id: int = 12345, chat_id: int = 12345):
        msg = MagicMock()
        msg.from_user = MagicMock(id=user_id)
        msg.chat = MagicMock(id=chat_id)
        msg.answer = AsyncMock()
        return msg


    async def test_cmd_start_without_consent_shows_consent_prompt(monkeypatch):
        """User в whitelist (member), но без pdn_consent_at → consent prompt."""

        async def fake_resolve(uid):
            # role, onboarded_at, pdn_consent_at — все 3 поля.
            return UserRole.member, None, None

        async def fake_bind(**kwargs):
            return None

        monkeypatch.setattr(
            "app.bot.handlers.bot_resolve_user_status", fake_resolve
        )
        monkeypatch.setattr("app.bot.handlers.bind_chat_id", fake_bind)

        msg = _mock_message()
        cmd = MagicMock(args=None)
        await cmd_start(msg, cmd)

        msg.answer.assert_called_once()
        args, kwargs = msg.answer.call_args
        text = args[0]
        assert "политику обработки" in text
        assert "Mini App" in text


    async def test_cmd_start_with_consent_skips_consent_prompt(monkeypatch):
        """User с granted consent НЕ видит consent prompt — invite или onboarded greeting."""
        from datetime import datetime, timezone

        async def fake_resolve(uid):
            # consent granted, but not yet onboarded.
            return UserRole.member, None, datetime.now(timezone.utc)

        async def fake_bind(**kwargs):
            return None

        monkeypatch.setattr(
            "app.bot.handlers.bot_resolve_user_status", fake_resolve
        )
        monkeypatch.setattr("app.bot.handlers.bind_chat_id", fake_bind)

        msg = _mock_message()
        cmd = MagicMock(args=None)
        await cmd_start(msg, cmd)

        msg.answer.assert_called_once()
        args, kwargs = msg.answer.call_args
        text = args[0]
        # Should be the invite-pending copy, NOT consent prompt.
        assert "политику обработки" not in text
        assert "Откройте приложение" in text


    async def test_cmd_start_rejected_for_revoked_role(monkeypatch):
        """Revoked role не доходит до consent check."""

        async def fake_resolve(uid):
            return UserRole.revoked, None, None

        monkeypatch.setattr(
            "app.bot.handlers.bot_resolve_user_status", fake_resolve
        )

        msg = _mock_message()
        cmd = MagicMock(args=None)
        await cmd_start(msg, cmd)

        msg.answer.assert_called_once_with("Бот приватный.")
    ```

    Запустить: `pytest tests/test_bot_handlers_consent.py -v`.
  </action>
  <acceptance_criteria>
    - File `tests/test_bot_handlers_consent.py` exists.
    - `grep -c "async def test_" tests/test_bot_handlers_consent.py` >= 3.
    - `pytest tests/test_bot_handlers_consent.py -v` exits 0.
  </acceptance_criteria>
  <done>Bot consent-prompt branching tested.</done>
</task>

<task type="auto">
  <name>Task 6: Commit + 33-03-SUMMARY.md</name>
  <files>.planning/phases/33-compliance/33-03-SUMMARY.md</files>
  <action>
    1. Stage modified files (`app/api/routes/me.py`, `app/api/routes/onboarding_v10.py`, `app/services/onboarding_v10.py`, `app/bot/handlers.py`, `app/bot/auth.py`) + 2 test файла + SUMMARY.
    2. Commit: `feat(33-03): consent endpoints + onboarding gate + bot prompt (REQ-33-02)` с `--no-verify`.
    3. SUMMARY: REQ-33-02 mentioned; mentions consent endpoints, onboarding gate, bot prompt; counts new tests.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` contains `33-03` and `REQ-33-02`.
    - File `.planning/phases/33-compliance/33-03-SUMMARY.md` exists, contains "REQ-33-02", min 12 lines.
  </acceptance_criteria>
  <done>Commit landed, consent flow live.</done>
</task>

</tasks>

<verification>
1. `POST /api/v1/me/consent` ставит timestamp + пишет audit-event `granted` (idempotent).
2. `DELETE /api/v1/me/consent` обнуляет + audit-event `revoked`.
3. `POST /api/v1/onboarding/complete` без consent → 403 с правильным error-body.
4. Bot `/start` для user без consent → invitation в Mini App с указанием прочитать политику.
5. 5+3 = 8 new tests pass.
6. Commit landed с `--no-verify`.
</verification>

<output>33-03-SUMMARY.md per template.</output>
</content>
</invoke>