---
phase: 33-compliance
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - docs/legal/privacy-policy.ru.md
  - docs/legal/privacy-policy.en.md
  - docs/legal/terms.ru.md
  - docs/legal/terms.en.md
  - app/api/routes/legal.py
  - main_api.py
  - tests/test_legal_endpoints.py
autonomous: true
requirements:
  - REQ-33-03
  - REQ-33-06
must_haves:
  truths:
    - "REQ-33-03: `docs/legal/privacy-policy.ru.md` + `docs/legal/privacy-policy.en.md` exist with full 152-ФЗ-compliant sections."
    - "REQ-33-03: `docs/legal/terms.ru.md` + `docs/legal/terms.en.md` exist (Draft v0.1)."
    - "REQ-33-03: `GET /legal/privacy?lang=ru` returns 200 with `text/markdown; charset=utf-8` and content from `docs/legal/privacy-policy.ru.md`; `?lang=en` returns the EN file."
    - "REQ-33-03: `GET /legal/terms?lang=ru|en` returns 200 with terms markdown."
    - "REQ-33-06: privacy-policy.ru.md перечисляет OpenAI (EU servers), retention 1 год после account-deletion, права субъекта, контакт DPO."
    - "Endpoints mounted on FastAPI app WITHOUT /api/v1 prefix and WITHOUT auth dependency."
    - "`pytest tests/test_legal_endpoints.py -v` exits 0."
  artifacts:
    - path: ".planning/phases/33-compliance/33-02-SUMMARY.md"
      contains: "REQ-33-03"
      min_lines: 12
---

<objective>
Publish Privacy Policy + Terms of Service в RU и EN, expose их через unauthenticated `/legal/privacy` + `/legal/terms` endpoints. Без этого нельзя ссылаться на политику с consent checkbox в onboarding и боте (33-03 depends on 33-02 routes). Документы помечены `Draft v0.1 — pending legal review` и являются legal baseline для launch'а в РФ.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-compliance/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/legal/privacy-policy.ru.md (полный текст RU)</name>
  <files>docs/legal/privacy-policy.ru.md</files>
  <read_first>
    - .planning/phases/33-compliance/33-CONTEXT.md (CMP-33-03 requirements)
    - docs/HLD.md (architecture overview для accurate описания обработки)
  </read_first>
  <action>
    Создать `docs/legal/privacy-policy.ru.md` со следующим содержимым:

    ```markdown
    # Политика обработки персональных данных

    > **Draft v0.1 — pending legal review.** Документ подготовлен до
    > профессиональной юридической экспертизы. Реальный launch требует
    > finalize per `docs/legal/LEGAL-REVIEW-TODO.md`.

    **Версия:** v0.1 (2026-05-11)
    **Применимое право:** Федеральный закон РФ от 27.07.2006 № 152-ФЗ
    «О персональных данных».

    ## 1. Оператор персональных данных

    Оператор — частное физическое лицо, владелец сервиса TG Budget Planner.
    Контакт: **exeynod@gmail.com**.

    ## 2. Цели обработки персональных данных

    Персональные данные (далее — ПДн) обрабатываются исключительно для:
    1. Аутентификации пользователя через Telegram Mini App (Telegram initData).
    2. Хранения бюджетных данных пользователя (категории, транзакции, планы,
       подписки) для отображения в приложении и боте.
    3. Отправки push-уведомлений через Telegram бот (только subscribed user'ам).
    4. Аналитики работы AI-помощника (тратa токенов, ai_usage_log) для
       контроля себестоимости.
    5. Audit-журнала операций над ПДн (consent / экспорт / удаление) для
       compliance с §10.1 152-ФЗ.

    ПДн **не используются** для маркетинга, профилирования третьими
    лицами или передачи рекламодателям.

    ## 3. Виды обрабатываемых ПДн

    | Категория | Источник | Назначение |
    |-----------|----------|------------|
    | Telegram user ID (tg_user_id) | Telegram initData | Идентификация |
    | Telegram chat ID (tg_chat_id) | /start команда | Push-уведомления |
    | Имя/username в Telegram (опционально) | Telegram initData | UI display |
    | Бюджетные данные (категории, транзакции, планы, накопления, цели) | Пользователь | Функционал приложения |
    | Текст сообщений AI-чату | Пользователь | Обработка через OpenAI |
    | IP-адрес (хэш) | HTTP request | Audit log (sha256) |

    ## 4. Правовое основание обработки

    Основанием обработки является **согласие субъекта ПДн** (ст. 6 ч. 1 п. 1
    152-ФЗ), полученное явно через onboarding-чекбокс «Я согласен на
    обработку персональных данных» с предварительным ознакомлением
    с настоящей Политикой. Согласие может быть отозвано в любой момент
    через `DELETE /api/v1/me/consent` или удаление аккаунта.

    ## 5. Сроки хранения ПДн

    - Активный пользователь: ПДн хранятся в течение всего срока использования
      сервиса.
    - После запроса на удаление аккаунта: данные помечаются soft-delete
      (`app_user.deleted_at`) и удаляются окончательно через **30 дней**
      (cooling period для отмены случайного удаления).
    - После окончательного удаления: audit-записи (с захэшированным user_id)
      хранятся ещё **1 год** для compliance с регуляторными запросами,
      затем архивируются.

    ## 6. Способы обработки

    Обработка осуществляется автоматизированным способом с использованием
    средств вычислительной техники. Никакая ручная обработка ПДн оператором
    не предусмотрена.

    ## 7. Хранение

    ПДн хранятся на серверах в РФ (VPS оператора). Резервные копии
    периодически снимаются через `pg_dump` и хранятся в зашифрованном виде.

    ## 8. Передача ПДн третьим лицам (sub-processors)

    | Sub-processor | Цель | Данные | Расположение серверов |
    |---------------|------|--------|------------------------|
    | **OpenAI** | AI-чат / категоризация | Текст сообщений пользователя в AI-чате; названия категорий | EU (api.openai.com EU residency) |
    | **Telegram** | Доставка сообщений / Mini App | tg_user_id, текст бот-сообщений | Зависит от инфраструктуры Telegram |

    Никаким другим третьим лицам ПДн не передаются.

    ## 9. Права субъекта персональных данных

    Согласно ст. 14-15 152-ФЗ, субъект ПДн имеет право на:

    1. **Доступ к ПДн** — через `GET /api/v1/me/export` (JSON-выгрузка всех
       данных пользователя).
    2. **Исправление неточных ПДн** — через UI приложения (любые поля
       редактируются).
    3. **Удаление ПДн** («право на забвение») — через `DELETE /api/v1/me/account`
       (soft-delete + автоматический hard-delete через 30 дней).
    4. **Отзыв согласия** — через `DELETE /api/v1/me/consent` (после отзыва
       приложение блокирует все основные операции до повторного согласия).
    5. **Обжалование действий оператора** — обращение к оператору через
       email **exeynod@gmail.com** или в Роскомнадзор.

    ## 10. Защита ПДн

    1. Все API-запросы проходят через HTTPS (TLS via Let's Encrypt).
    2. PostgreSQL Row-Level Security (RLS) обеспечивает изоляцию данных
       пользователей: каждый запрос выполняется под non-superuser ролью
       с принудительной фильтрацией по `app.current_user_id` GUC.
    3. Telegram initData валидируется HMAC-SHA256 на каждом запросе.
    4. Audit-журнал хранит только захэшированные (sha256) user_id и IP —
       идентификация невозможна после удаления аккаунта.

    ## 11. Cookies

    Приложение использует только **технически обязательные** cookies для
    функционирования Mini App (Telegram session). Аналитические /
    маркетинговые cookies не используются.

    ## 12. Контакты оператора

    **Оператор персональных данных:** частное физическое лицо.
    **Email (DPO contact):** exeynod@gmail.com
    **Канал для запросов от субъектов ПДн:** тот же email или прямое
    сообщение в Telegram бот.

    ## 13. Изменения политики

    Оператор вправе вносить изменения в настоящую Политику. Актуальная
    версия всегда доступна по адресу `/legal/privacy?lang=ru`. Существенные
    изменения сопровождаются повторным запросом согласия в onboarding-flow.

    ---

    *Документ зарегистрирован в Роскомнадзоре: см. `docs/COMPLIANCE.md`
    (reg-номер заполняется после подачи через pd.rkn.gov.ru).*
    ```
  </action>
  <acceptance_criteria>
    - File `docs/legal/privacy-policy.ru.md` exists.
    - `wc -l docs/legal/privacy-policy.ru.md` >= 80.
    - `grep -c "OpenAI" docs/legal/privacy-policy.ru.md` >= 1.
    - `grep -c "152-ФЗ" docs/legal/privacy-policy.ru.md` >= 3.
    - `grep "exeynod@gmail.com" docs/legal/privacy-policy.ru.md` matches.
    - `grep "30 дней" docs/legal/privacy-policy.ru.md` matches.
    - `grep "Draft v0.1" docs/legal/privacy-policy.ru.md` matches.
  </acceptance_criteria>
  <done>RU privacy policy drafted.</done>
</task>

<task type="auto">
  <name>Task 2: docs/legal/privacy-policy.en.md (EN translation)</name>
  <files>docs/legal/privacy-policy.en.md</files>
  <read_first>
    - docs/legal/privacy-policy.ru.md (canonical source)
  </read_first>
  <action>
    Создать `docs/legal/privacy-policy.en.md` — Английский перевод RU-версии, отражающий ту же структуру и факты. Header:

    ```markdown
    # Privacy Policy

    > **Draft v0.1 — pending legal review.** This document was prepared
    > before professional legal review. Real launch requires finalisation
    > per `docs/legal/LEGAL-REVIEW-TODO.md`.

    **Version:** v0.1 (2026-05-11)
    **Applicable law:** Russian Federation Federal Law No. 152-FZ
    "On Personal Data" of 27.07.2006.
    ```

    Все 13 секций из RU-версии (Operator, Purposes, Categories, Legal basis,
    Retention periods, Processing methods, Storage, Sub-processors,
    Subject rights, Protection, Cookies, Contact, Changes) переведены
    структурно одинаково. Email **exeynod@gmail.com** + OpenAI/EU mention +
    30-day cooling **обязаны присутствовать** в EN.
  </action>
  <acceptance_criteria>
    - File `docs/legal/privacy-policy.en.md` exists.
    - `wc -l docs/legal/privacy-policy.en.md` >= 80.
    - `grep -c "OpenAI" docs/legal/privacy-policy.en.md` >= 1.
    - `grep -c "152-FZ\|152-ФЗ" docs/legal/privacy-policy.en.md` >= 2.
    - `grep "exeynod@gmail.com" docs/legal/privacy-policy.en.md` matches.
    - `grep "30 days\|30-day" docs/legal/privacy-policy.en.md` matches.
    - `grep "Draft v0.1" docs/legal/privacy-policy.en.md` matches.
  </acceptance_criteria>
  <done>EN privacy policy drafted.</done>
</task>

<task type="auto">
  <name>Task 3: docs/legal/terms.ru.md + terms.en.md (Terms of Service)</name>
  <files>docs/legal/terms.ru.md, docs/legal/terms.en.md</files>
  <read_first>
    - docs/legal/privacy-policy.ru.md (header style consistency)
  </read_first>
  <action>
    Создать `docs/legal/terms.ru.md`:

    ```markdown
    # Условия использования (Terms of Service)

    > **Draft v0.1 — pending legal review.** Документ подготовлен до
    > профессиональной юридической экспертизы.

    **Версия:** v0.1 (2026-05-11)

    ## 1. Предмет договора

    Настоящие Условия регулируют использование сервиса TG Budget Planner
    («Сервис») — Telegram Mini App для ведения личного бюджета. Регистрируясь
    и используя Сервис, вы соглашаетесь с настоящими Условиями.

    ## 2. Описание сервиса

    Сервис предоставляет:
    - Ведение план/факт бюджета по категориям расходов и доходов.
    - Подписки с автоматическими напоминаниями.
    - AI-помощник (опционально, требует подписки Pro начиная с v1.1).
    - Экспорт данных в JSON / CSV.

    Сервис предоставляется «как есть» (as-is) без каких-либо явных или
    подразумеваемых гарантий.

    ## 3. Учётная запись

    1. Авторизация выполняется через Telegram. Для использования Сервиса
       необходим аккаунт Telegram.
    2. Пользователь несёт ответственность за безопасность своего аккаунта
       Telegram.
    3. Сервис вправе ограничить доступ при нарушении настоящих Условий.

    ## 4. Биллинг (Premium-подписка)

    > Раздел применим после интеграции платежей (Phase 34). До интеграции
    > Сервис предоставляется бесплатно с базовыми лимитами.

    1. **Подписка Pro** — 299 ₽/мес, оплата через ЮKassa (банковская карта)
       или Telegram Stars.
    2. **Срок действия** — 30 дней с момента списания. Auto-renewal (рекуррентные
       платежи) включены по умолчанию для ЮKassa; отключаются в Management → Pro.
    3. **Refund policy** — детали будут уточнены после legal review (см.
       `docs/legal/LEGAL-REVIEW-TODO.md`).
    4. **Налоговый чек** (ЮKassa Self-Employed) — отправляется на email/Telegram
       в течение 24 часов после успешной оплаты.

    ## 5. Ограничение ответственности

    1. Оператор не несёт ответственности за финансовые решения пользователя
       на основе данных Сервиса.
    2. Сервис не является финансовым консультантом, бухгалтером или
       налоговым агентом.
    3. Максимальная ответственность оператора в любом случае ограничена
       суммой подписки за последние 12 месяцев.

    ## 6. Запрещённое использование

    Пользователь обязуется не использовать Сервис для:
    1. Хранения чужих ПДн без согласия их субъектов.
    2. Незаконных финансовых операций или отмывания доходов.
    3. Reverse engineering / автоматизированных запросов вне публичного API.

    ## 7. Применимое право и юрисдикция

    Настоящие Условия регулируются законодательством Российской Федерации.
    Все споры разрешаются в соответствии с гражданско-процессуальным
    законодательством РФ.

    ## 8. Контакты

    **Email:** exeynod@gmail.com
    Также можно обратиться через Telegram бот Сервиса.

    ## 9. Изменения условий

    Оператор вправе обновлять настоящие Условия. Актуальная версия —
    по адресу `/legal/terms?lang=ru`.
    ```

    Создать `docs/legal/terms.en.md` — EN translation тех же 9 секций со
    сохранением header (Draft v0.1 + version + applicable law mention).
  </action>
  <acceptance_criteria>
    - File `docs/legal/terms.ru.md` exists, >= 40 lines.
    - File `docs/legal/terms.en.md` exists, >= 40 lines.
    - `grep "Draft v0.1" docs/legal/terms.ru.md` matches.
    - `grep "Draft v0.1" docs/legal/terms.en.md` matches.
    - `grep "exeynod@gmail.com" docs/legal/terms.ru.md` matches.
    - `grep "exeynod@gmail.com" docs/legal/terms.en.md` matches.
  </acceptance_criteria>
  <done>ToS RU + EN drafted.</done>
</task>

<task type="auto">
  <name>Task 4: Create app/api/routes/legal.py — /legal endpoints</name>
  <files>app/api/routes/legal.py</files>
  <read_first>
    - app/api/routes/onboarding_v10.py (router pattern)
    - main_api.py (mount points)
    - app/api/routes/__init__.py (если есть)
  </read_first>
  <action>
    Создать `app/api/routes/legal.py`:

    ```python
    """Phase 33 CMP-33-03: /legal endpoints — Privacy + ToS public docs.

    Public, unauthenticated endpoints serving markdown documents from
    `docs/legal/`. Mounted on the main FastAPI app WITHOUT the /api/v1
    prefix (privacy policy must be accessible BEFORE Telegram-auth so
    a user can read before granting consent).

    Caching: markdown files are loaded once at first request and cached
    in module-level dict; container restart invalidates cache.
    """
    from __future__ import annotations

    from pathlib import Path
    from typing import Literal

    from fastapi import APIRouter, HTTPException, Query, Response

    legal_router = APIRouter(prefix="/legal", tags=["legal"])

    # Project root — резолвится относительно текущего файла:
    # app/api/routes/legal.py → ../../.. → repo root.
    _REPO_ROOT = Path(__file__).resolve().parents[3]
    _DOCS_DIR = _REPO_ROOT / "docs" / "legal"

    # Filename mapping: (kind, lang) → file basename.
    _FILES: dict[tuple[str, str], str] = {
        ("privacy", "ru"): "privacy-policy.ru.md",
        ("privacy", "en"): "privacy-policy.en.md",
        ("terms", "ru"): "terms.ru.md",
        ("terms", "en"): "terms.en.md",
    }

    # In-memory cache: (kind, lang) → markdown text. Populated lazily.
    _CACHE: dict[tuple[str, str], str] = {}


    def _read_doc(kind: str, lang: str) -> str:
        key = (kind, lang)
        if key in _CACHE:
            return _CACHE[key]
        filename = _FILES.get(key)
        if not filename:
            raise HTTPException(status_code=404, detail=f"No doc for {kind}/{lang}")
        path = _DOCS_DIR / filename
        if not path.exists():
            raise HTTPException(
                status_code=500,
                detail=f"Legal doc missing on disk: {filename}",
            )
        text = path.read_text(encoding="utf-8")
        _CACHE[key] = text
        return text


    @legal_router.get(
        "/privacy",
        responses={200: {"content": {"text/markdown": {}}}},
    )
    async def get_privacy(
        lang: Literal["ru", "en"] = Query("ru"),
    ) -> Response:
        """Public Privacy Policy — Markdown response."""
        body = _read_doc("privacy", lang)
        return Response(content=body, media_type="text/markdown; charset=utf-8")


    @legal_router.get(
        "/terms",
        responses={200: {"content": {"text/markdown": {}}}},
    )
    async def get_terms(
        lang: Literal["ru", "en"] = Query("ru"),
    ) -> Response:
        """Public Terms of Service — Markdown response."""
        body = _read_doc("terms", lang)
        return Response(content=body, media_type="text/markdown; charset=utf-8")
    ```
  </action>
  <acceptance_criteria>
    - File `app/api/routes/legal.py` exists.
    - `grep -c "@legal_router.get" app/api/routes/legal.py` >= 2.
    - `grep "/privacy" app/api/routes/legal.py` matches.
    - `grep "/terms" app/api/routes/legal.py` matches.
    - `grep "text/markdown" app/api/routes/legal.py` matches.
    - `python -c "from app.api.routes.legal import legal_router"` exits 0.
  </acceptance_criteria>
  <done>legal_router module ready to mount.</done>
</task>

<task type="auto">
  <name>Task 5: Mount legal_router в main_api.py</name>
  <files>main_api.py</files>
  <read_first>
    - main_api.py (current mounts at lines 134-152)
    - app/api/routes/legal.py (newly created)
  </read_first>
  <action>
    В `main_api.py` после строки `app.include_router(internal_router, prefix="/api/v1", tags=["internal"])` (вокруг line 152) добавить:

    ```python
    # Phase 33 CMP-33-03 — public legal docs (Privacy + ToS).
    # Mounted WITHOUT /api/v1 prefix so users can read policy BEFORE
    # Telegram auth occurs. No auth dependency.
    from app.api.routes.legal import legal_router
    app.include_router(legal_router, tags=["legal"])
    ```

    Можно также добавить import наверх файла (наряду с `from app.api.router import …`).
  </action>
  <acceptance_criteria>
    - `grep "legal_router" main_api.py` matches.
    - `grep "app.include_router(legal_router" main_api.py` matches.
    - `python -c "import main_api; print('ok')"` exits 0 (no import errors).
  </acceptance_criteria>
  <done>legal endpoints reachable on app level.</done>
</task>

<task type="auto">
  <name>Task 6: tests/test_legal_endpoints.py</name>
  <files>tests/test_legal_endpoints.py</files>
  <read_first>
    - tests/conftest.py (httpx async client fixture or test_client pattern)
    - tests/test_health.py (similar lightweight test pattern)
    - app/api/routes/legal.py
  </read_first>
  <action>
    Создать `tests/test_legal_endpoints.py`:

    ```python
    """Phase 33 CMP-33-03: /legal/privacy + /legal/terms endpoint tests."""
    from __future__ import annotations

    import pytest
    from httpx import ASGITransport, AsyncClient

    pytestmark = pytest.mark.asyncio


    @pytest.fixture
    async def legal_client():
        """ASGI client против main_api.app — no DB, no auth deps."""
        from main_api import app

        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as client:
            yield client


    async def test_privacy_ru_returns_markdown(legal_client):
        r = await legal_client.get("/legal/privacy?lang=ru")
        assert r.status_code == 200
        assert "text/markdown" in r.headers["content-type"]
        body = r.text
        assert "152-ФЗ" in body
        assert "OpenAI" in body
        assert "exeynod@gmail.com" in body
        assert "Draft v0.1" in body


    async def test_privacy_en_returns_markdown(legal_client):
        r = await legal_client.get("/legal/privacy?lang=en")
        assert r.status_code == 200
        assert "text/markdown" in r.headers["content-type"]
        body = r.text
        # EN ToS / Privacy mentions either 152-FZ или 152-ФЗ.
        assert "OpenAI" in body
        assert "Draft v0.1" in body


    async def test_privacy_default_lang_is_ru(legal_client):
        r = await legal_client.get("/legal/privacy")
        assert r.status_code == 200
        assert "152-ФЗ" in r.text


    async def test_terms_ru_and_en(legal_client):
        r_ru = await legal_client.get("/legal/terms?lang=ru")
        assert r_ru.status_code == 200
        assert "Условия использования" in r_ru.text
        r_en = await legal_client.get("/legal/terms?lang=en")
        assert r_en.status_code == 200


    async def test_invalid_lang_rejected(legal_client):
        # FastAPI Query Literal["ru","en"] returns 422 для invalid.
        r = await legal_client.get("/legal/privacy?lang=fr")
        assert r.status_code == 422


    async def test_legal_endpoints_no_auth_required(legal_client):
        """Verify endpoints don't require Telegram initData."""
        # No headers, no token — should still 200.
        r = await legal_client.get("/legal/privacy")
        assert r.status_code == 200
    ```

    Запустить: `pytest tests/test_legal_endpoints.py -v`.
  </action>
  <acceptance_criteria>
    - File `tests/test_legal_endpoints.py` exists.
    - `grep -c "async def test_" tests/test_legal_endpoints.py` >= 5.
    - `pytest tests/test_legal_endpoints.py -v` exits 0.
  </acceptance_criteria>
  <done>Legal endpoints tested green.</done>
</task>

<task type="auto">
  <name>Task 7: Commit + 33-02-SUMMARY.md</name>
  <files>.planning/phases/33-compliance/33-02-SUMMARY.md</files>
  <action>
    1. Stage: 4 legal markdown файла, `app/api/routes/legal.py`, `main_api.py`, `tests/test_legal_endpoints.py`, SUMMARY.
    2. Commit: `feat(33-02): privacy policy + ToS RU/EN + /legal endpoints (REQ-33-03, REQ-33-06)` с `--no-verify`.
    3. SUMMARY mentions REQ-33-03 + REQ-33-06; lists 4 markdown files + endpoint paths + test count.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` contains `33-02` and `REQ-33-03`.
    - File `.planning/phases/33-compliance/33-02-SUMMARY.md` exists, contains "REQ-33-03", min 12 lines.
  </acceptance_criteria>
  <done>Commit landed; SUMMARY written.</done>
</task>

</tasks>

<verification>
1. 4 markdown файла в `docs/legal/` существуют, все помечены Draft v0.1.
2. `GET /legal/privacy?lang=ru|en` + `GET /legal/terms?lang=ru|en` возвращают 200 + text/markdown.
3. Endpoints НЕ требуют auth — публично доступны.
4. Privacy policy перечисляет OpenAI EU, 30-day cooling, DPO email, права субъекта.
5. 6 tests pass.
6. Commit landed с `--no-verify`.
</verification>

<output>33-02-SUMMARY.md per template.</output>
</content>
</invoke>