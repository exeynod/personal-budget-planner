---
phase: 33-compliance
plan: 05
type: execute
wave: 4
depends_on: ["33-02", "33-03"]
files_modified:
  - frontend/src/components/CookieBanner.tsx
  - frontend/src/components/CookieBanner.module.css
  - frontend/src/components/PdnConsentCheckbox.tsx
  - frontend/src/components/PdnConsentCheckbox.module.css
  - frontend/src/api/me.ts
  - frontend/src/App.tsx
autonomous: true
requirements:
  - REQ-33-05
must_haves:
  truths:
    - "REQ-33-05: `<CookieBanner />` компонент существует, минимальный info-only banner с кнопкой «Понятно», state persisted в `localStorage['cookie_consent_v1']`."
    - "REQ-33-05: Banner mount-point — корневой `App.tsx`; рендерится при первом visit, скрывается после dismiss."
    - "REQ-33-05: `<PdnConsentCheckbox />` компонент существует — checkbox + link «Подробнее» (→ `/legal/privacy?lang=ru`) + кнопка/callback для grant; вызывает `POST /api/v1/me/consent` через `frontend/src/api/me.ts`."
    - "REQ-33-05: `frontend/src/api/me.ts` экспортирует функции `grantConsent()`, `revokeConsent()`, `exportData()`, `deleteAccount()`."
    - "Build frontend (`npm run build` или `vite build` от frontend/) exits 0 — TypeScript compiles cleanly."
  artifacts:
    - path: ".planning/phases/33-compliance/33-05-SUMMARY.md"
      contains: "REQ-33-05"
      min_lines: 12
---

<objective>
Frontend integration: minimal cookie banner на корневом уровне Mini App + переиспользуемый PdnConsentCheckbox компонент для onboarding step 1 (а также Settings → Privacy в будущем). Также добавить frontend-helpers для новых ME endpoints (consent, export, account-delete). Без backend tests — это pure-frontend изменения, валидируются через TS build + manual integration (бэкенд проверен в Plan 33-03/33-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-compliance/33-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: frontend/src/api/me.ts — consent / export / delete helpers</name>
  <files>frontend/src/api/me.ts</files>
  <read_first>
    - frontend/src/api/client.ts (если есть — auth/fetch wrapper)
    - frontend/src/api/types.ts (existing typings)
  </read_first>
  <action>
    Создать (или если файл уже частично есть — extend) `frontend/src/api/me.ts`:

    ```typescript
    /** Phase 33 CMP-33-04/06: ME endpoints for consent, export, deletion. */
    import { request } from './client';  // existing fetch wrapper

    export interface ConsentResponse {
      pdn_consent_at: string | null;
      policy_version?: string;
      revoked?: boolean;
    }

    export interface DeleteAccountResponse {
      deleted_at: string;
      purge_after_days: number;
      message: string;
    }

    /** POST /api/v1/me/consent — idempotent grant. */
    export async function grantConsent(): Promise<ConsentResponse> {
      return request<ConsentResponse>('/api/v1/me/consent', { method: 'POST' });
    }

    /** DELETE /api/v1/me/consent — revoke. */
    export async function revokeConsent(): Promise<ConsentResponse> {
      return request<ConsentResponse>('/api/v1/me/consent', { method: 'DELETE' });
    }

    /** GET /api/v1/me/export — full PII dump (JSON). */
    export async function exportData(): Promise<Record<string, unknown>> {
      return request<Record<string, unknown>>('/api/v1/me/export', {
        method: 'GET',
      });
    }

    /** DELETE /api/v1/me/account — soft-delete with 30-day cooling. */
    export async function deleteAccount(): Promise<DeleteAccountResponse> {
      return request<DeleteAccountResponse>('/api/v1/me/account', {
        method: 'DELETE',
      });
    }
    ```

    Если `request` impl signature разная — adapt. Если файл уже существует — добавить новые экспорты без удаления existing.
  </action>
  <acceptance_criteria>
    - File `frontend/src/api/me.ts` exists.
    - `grep "export async function grantConsent" frontend/src/api/me.ts` matches.
    - `grep "export async function revokeConsent" frontend/src/api/me.ts` matches.
    - `grep "export async function exportData" frontend/src/api/me.ts` matches.
    - `grep "export async function deleteAccount" frontend/src/api/me.ts` matches.
  </acceptance_criteria>
  <done>API helpers exported.</done>
</task>

<task type="auto">
  <name>Task 2: CookieBanner component (TSX + CSS module)</name>
  <files>frontend/src/components/CookieBanner.tsx, frontend/src/components/CookieBanner.module.css</files>
  <read_first>
    - frontend/src/components/ (any existing simple banner / toast component for style reference)
    - frontend/src/App.module.css (for design tokens / colors)
  </read_first>
  <action>
    1. Создать `frontend/src/components/CookieBanner.tsx`:

    ```typescript
    /** Phase 33 CMP-33-05: minimal cookie banner (info-only).
     *
     * Renders при первом visit (no `cookie_consent_v1` in localStorage).
     * Dismissed via «Понятно» button → sets localStorage flag.
     * Full opt-in analytics flow deferred to Phase 38.
     */
    import { useEffect, useState } from 'react';
    import styles from './CookieBanner.module.css';

    const STORAGE_KEY = 'cookie_consent_v1';

    export function CookieBanner() {
      const [visible, setVisible] = useState(false);

      useEffect(() => {
        try {
          const acknowledged = window.localStorage.getItem(STORAGE_KEY);
          if (!acknowledged) {
            setVisible(true);
          }
        } catch {
          // localStorage not available — fail gracefully.
        }
      }, []);

      const dismiss = () => {
        try {
          window.localStorage.setItem(STORAGE_KEY, 'acknowledged');
        } catch {
          /* noop */
        }
        setVisible(false);
      };

      if (!visible) {
        return null;
      }

      return (
        <div className={styles.banner} role="region" aria-label="Cookie notice">
          <p className={styles.text}>
            Мы используем только обязательные cookies для работы приложения.
            {' '}
            <a
              href="/legal/privacy?lang=ru"
              target="_blank"
              rel="noopener noreferrer"
              className={styles.link}
            >
              Подробнее
            </a>
          </p>
          <button type="button" onClick={dismiss} className={styles.button}>
            Понятно
          </button>
        </div>
      );
    }
    ```

    2. Создать `frontend/src/components/CookieBanner.module.css`:

    ```css
    .banner {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      padding: 12px 16px;
      background: rgba(20, 20, 20, 0.95);
      color: white;
      display: flex;
      gap: 12px;
      align-items: center;
      justify-content: space-between;
      z-index: 1000;
      font-size: 13px;
      line-height: 1.4;
      box-shadow: 0 -2px 12px rgba(0, 0, 0, 0.3);
    }

    .text {
      margin: 0;
      flex: 1;
    }

    .link {
      color: #ffd166;
      text-decoration: underline;
    }

    .button {
      flex-shrink: 0;
      padding: 8px 16px;
      background: white;
      color: #141414;
      border: none;
      border-radius: 6px;
      font-weight: 600;
      cursor: pointer;
    }

    .button:hover {
      background: #f5f5f5;
    }
    ```
  </action>
  <acceptance_criteria>
    - File `frontend/src/components/CookieBanner.tsx` exists.
    - File `frontend/src/components/CookieBanner.module.css` exists.
    - `grep "cookie_consent_v1" frontend/src/components/CookieBanner.tsx` matches.
    - `grep "/legal/privacy" frontend/src/components/CookieBanner.tsx` matches.
    - `grep "export function CookieBanner" frontend/src/components/CookieBanner.tsx` matches.
  </acceptance_criteria>
  <done>Banner component built.</done>
</task>

<task type="auto">
  <name>Task 3: PdnConsentCheckbox component (TSX + CSS module)</name>
  <files>frontend/src/components/PdnConsentCheckbox.tsx, frontend/src/components/PdnConsentCheckbox.module.css</files>
  <read_first>
    - frontend/src/api/me.ts (newly created — grantConsent)
    - frontend/src/components/CookieBanner.module.css (style reference)
  </read_first>
  <action>
    1. Создать `frontend/src/components/PdnConsentCheckbox.tsx`:

    ```typescript
    /** Phase 33 CMP-33-04: ПДн consent checkbox for onboarding step 1 +
     * Settings → Privacy. Calls grantConsent() on tick. Idempotent on
     * already-granted state.
     */
    import { useState } from 'react';
    import { grantConsent } from '../api/me';
    import styles from './PdnConsentCheckbox.module.css';

    export interface PdnConsentCheckboxProps {
      /** Called после successful grant. Parent should advance UI. */
      onGranted?: (timestamp: string) => void;
      /** Initial state (e.g. user already consented). */
      initialChecked?: boolean;
      /** Disable interaction (читать-только). */
      disabled?: boolean;
    }

    export function PdnConsentCheckbox({
      onGranted,
      initialChecked = false,
      disabled = false,
    }: PdnConsentCheckboxProps) {
      const [checked, setChecked] = useState(initialChecked);
      const [busy, setBusy] = useState(false);
      const [error, setError] = useState<string | null>(null);

      const handleChange = async (e: React.ChangeEvent<HTMLInputElement>) => {
        const next = e.target.checked;
        setChecked(next);
        if (next && !disabled) {
          setBusy(true);
          setError(null);
          try {
            const resp = await grantConsent();
            if (resp.pdn_consent_at && onGranted) {
              onGranted(resp.pdn_consent_at);
            }
          } catch (err) {
            setError('Не удалось сохранить согласие. Попробуйте ещё раз.');
            setChecked(false);
          } finally {
            setBusy(false);
          }
        }
      };

      return (
        <div className={styles.wrapper}>
          <label className={styles.label}>
            <input
              type="checkbox"
              checked={checked}
              onChange={handleChange}
              disabled={busy || disabled}
              className={styles.checkbox}
            />
            <span className={styles.text}>
              Я согласен на обработку персональных данных в соответствии с
              {' '}
              <a
                href="/legal/privacy?lang=ru"
                target="_blank"
                rel="noopener noreferrer"
                className={styles.link}
              >
                Политикой конфиденциальности
              </a>
              {' '}
              и
              {' '}
              <a
                href="/legal/terms?lang=ru"
                target="_blank"
                rel="noopener noreferrer"
                className={styles.link}
              >
                Условиями использования
              </a>
              .
            </span>
          </label>
          {error && <p className={styles.error} role="alert">{error}</p>}
        </div>
      );
    }
    ```

    2. Создать `frontend/src/components/PdnConsentCheckbox.module.css`:

    ```css
    .wrapper {
      padding: 12px;
      background: rgba(255, 255, 255, 0.06);
      border-radius: 8px;
      margin: 16px 0;
    }

    .label {
      display: flex;
      gap: 10px;
      align-items: flex-start;
      cursor: pointer;
      font-size: 14px;
      line-height: 1.5;
    }

    .checkbox {
      margin-top: 2px;
      width: 18px;
      height: 18px;
      flex-shrink: 0;
      cursor: pointer;
    }

    .text {
      flex: 1;
    }

    .link {
      color: var(--accent, #ffd166);
      text-decoration: underline;
    }

    .error {
      margin: 8px 0 0;
      color: #ff6b6b;
      font-size: 13px;
    }
    ```
  </action>
  <acceptance_criteria>
    - File `frontend/src/components/PdnConsentCheckbox.tsx` exists.
    - File `frontend/src/components/PdnConsentCheckbox.module.css` exists.
    - `grep "grantConsent" frontend/src/components/PdnConsentCheckbox.tsx` matches.
    - `grep "/legal/privacy" frontend/src/components/PdnConsentCheckbox.tsx` matches.
    - `grep "/legal/terms" frontend/src/components/PdnConsentCheckbox.tsx` matches.
    - `grep "Я согласен на обработку" frontend/src/components/PdnConsentCheckbox.tsx` matches.
  </acceptance_criteria>
  <done>Checkbox component built.</done>
</task>

<task type="auto">
  <name>Task 4: Mount CookieBanner в App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (current entry component)
    - frontend/src/components/CookieBanner.tsx
  </read_first>
  <action>
    1. В `frontend/src/App.tsx` добавить import (в группу импортов компонентов):

    ```typescript
    import { CookieBanner } from './components/CookieBanner';
    ```

    2. В JSX `export default function App() { … return ( … ) }` — render `<CookieBanner />` как один из children корневого `<>...</>` или `<FabActionContext.Provider>` (на самом верхнем уровне UI tree, чтобы banner всегда был поверх контента).

    Конкретно — найти top-level `return (` и добавить `<CookieBanner />` ПЕРВЫМ child'ом fragment'а или последним перед закрытием. Пример:
    ```tsx
    return (
      <FabActionContext.Provider value={...}>
        <CookieBanner />
        {/* ... existing screen logic ... */}
      </FabActionContext.Provider>
    );
    ```
  </action>
  <acceptance_criteria>
    - `grep "import { CookieBanner }" frontend/src/App.tsx` matches.
    - `grep "<CookieBanner" frontend/src/App.tsx` matches.
  </acceptance_criteria>
  <done>Banner mounted on app root.</done>
</task>

<task type="auto">
  <name>Task 5: Run TypeScript build to verify</name>
  <files>(no file changes)</files>
  <action>
    1. Запустить `cd frontend && npm run build` (или `npx tsc --noEmit && npx vite build`).
    2. Confirm exit 0 — no TS errors.
    3. Если есть error связанный с `request` импортом из `api/client.ts` — поправить путь / signature.
  </action>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0.
  </acceptance_criteria>
  <done>Frontend builds clean.</done>
</task>

<task type="auto">
  <name>Task 6: Commit + 33-05-SUMMARY.md</name>
  <files>.planning/phases/33-compliance/33-05-SUMMARY.md</files>
  <action>
    1. Stage: 4 frontend файла + App.tsx + me.ts + SUMMARY.
    2. Commit: `feat(33-05): cookie banner + pdn consent checkbox + me.ts helpers (REQ-33-05)` с `--no-verify`.
    3. SUMMARY mentions REQ-33-05 + components + build evidence.
  </action>
  <acceptance_criteria>
    - `git log -1 --format=%s` contains `33-05` and `REQ-33-05`.
    - File `.planning/phases/33-compliance/33-05-SUMMARY.md` exists, contains "REQ-33-05", min 12 lines.
  </acceptance_criteria>
  <done>Commit landed.</done>
</task>

</tasks>

<verification>
1. CookieBanner.tsx + module.css existes; renders only при first visit.
2. PdnConsentCheckbox.tsx + module.css exist; вызывает grantConsent при tick.
3. frontend/src/api/me.ts экспортирует 4 helpers.
4. App.tsx mounts CookieBanner.
5. TypeScript build clean (exit 0).
6. Commit landed.
</verification>

<output>33-05-SUMMARY.md per template.</output>
</content>
</invoke>