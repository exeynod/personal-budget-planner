---
phase: 31-regression-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/fixtures/onboarded-user.ts
  - app/api/dependencies.py
  - app/api/routes/internal_onboarding.py
autonomous: true
requirements:
  - REG-01
must_haves:
  truths:
    - "Playwright fixture `installOnboardedFixture` теперь работает в dev-mode: header `X-Test-User: 999000` triggers auth bypass."
    - "Backend `/api/v1/internal/onboarding/seed` endpoint (или эквивалент) создаёт user 999000 + 8 categories + period + 1 account атомарно."
    - "Acceptance + pixel specs реусают тот же fixture."
  artifacts:
    - path: ".planning/phases/31-regression-hardening/31-01-SUMMARY.md"
      contains: "REG-01"
      min_lines: 12
---

<objective>
Production-grade onboarded fixture для Playwright: backend test-mode auth bypass + seed endpoint вместо чистого route mocking. Shared across acceptance + pixel specs.

NB: текущий `onboarded-user.ts` использует чистый mocking (Phase 29-01). Этот plan opt-in upgrade до real-backend mode когда `ENV=dev` + `X-Test-User` header set. Mocking путь сохраняем как default fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-regression-hardening/31-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend dev-mode auth bypass</name>
  <files>app/api/dependencies.py</files>
  <action>
    1. Найти `get_current_user` (or equivalent dependency). Если `ENV=dev` AND `X-Test-User` header set → bypass initData validation, return user matching header value.
    2. Add ENV check to avoid prod leak. Default = strict validation.
    3. Test: `curl -H "X-Test-User: 999000" http://localhost:8000/api/v1/me` returns user.
  </action>
  <done>Dev-mode auth bypass functional.</done>
</task>

<task type="auto">
  <name>Task 2: Internal seed endpoint (если отсутствует)</name>
  <files>app/api/routes/internal_onboarding.py</files>
  <action>
    1. Проверить если `/api/v1/internal/onboarding/seed?tg_user_id=999000` existing. Если нет — добавить idempotent endpoint that ensures user/categories/period/account exist.
    2. Protected by `X-Internal-Token`.
  </action>
  <done>Seed endpoint operational.</done>
</task>

<task type="auto">
  <name>Task 3: Fixture mode toggle + Reuse</name>
  <files>frontend/tests/e2e/fixtures/onboarded-user.ts</files>
  <action>
    1. Add `mode: 'mock' | 'live'` option to `InstallOptions`. Default = 'mock' (existing behaviour).
    2. If `mode === 'live'`: call seed endpoint via fetch (with INTERNAL_TOKEN env), set `X-Test-User: 999000` extraHTTPHeader on context.
    3. Document in JSDoc; existing tests stay on 'mock'.
  </action>
  <done>Fixture supports both modes.</done>
</task>

<task type="auto">
  <name>Task 4: Commit + SUMMARY</name>
  <action>`feat(31-01): live-mode Playwright fixture с dev auth bypass (REG-01)`. SUMMARY.</action>
</task>

</tasks>

<verification>
1. `curl -H "X-Test-User: 999000" -H "X-Env: dev" /api/v1/me` returns 200 + user.
2. Existing pixel specs still green (mode=mock default).
3. SUMMARY exists.
</verification>

<output>31-01-SUMMARY.md per template.</output>
