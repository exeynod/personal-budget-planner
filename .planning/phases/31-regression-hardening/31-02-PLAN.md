---
phase: 31-regression-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/tests/e2e/v10-acceptance-tz14.spec.ts
  - frontend/tests/e2e/v10-pixel-snapshots.spec.ts
autonomous: true
requirements:
  - REG-02
  - REG-03
must_haves:
  truths:
    - "REG-02: `v10-acceptance-tz14.spec.ts` всё проходит на live dev-server."
    - "REG-03: pixel-snapshots regression-test passes (intentional regression triggers diff-fail)."
  artifacts:
    - path: ".planning/phases/31-regression-hardening/31-02-SUMMARY.md"
      contains: "REG-02"
      min_lines: 10
---

<objective>
Two related test-stability fixes: §14 acceptance fix (flexible CTA matching) + pixel sanity-check (sanity regression test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-regression-hardening/31-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: §14 acceptance CTA regex (REG-02)</name>
  <files>frontend/tests/e2e/v10-acceptance-tz14.spec.ts</files>
  <action>
    1. Read existing spec. Find CTA look-up that fails (likely `getByRole('button', { name: /СОХРАНИТЬ/ })`).
    2. Approach A: flexible regex `/СОХРАНИТЬ|ВВЕДИТЕ СУММУ|ВЫБЕРИТЕ КАТЕГОРИЮ/i`.
    3. Approach B (preferred): fill amount + select category first, THEN assert «СОХРАНИТЬ» appears.
    4. Re-run spec until green: `npx playwright test v10-acceptance-tz14 --project=chromium-mobile`.
  </action>
  <done>v10-acceptance-tz14 spec green.</done>
</task>

<task type="auto">
  <name>Task 2: Pixel-snapshots sanity test (REG-03)</name>
  <files>frontend/tests/e2e/v10-pixel-snapshots.spec.ts</files>
  <action>
    1. Existing spec generates 8 baselines (Plan 29-01).
    2. Add a separate sanity-test: temporarily mutate Home `font-weight: 800` via `page.evaluate` → snapshot must fail diff.
    3. Restore after assert; OR isolate as a separate spec file `v10-pixel-snapshots-sanity.spec.ts` that's skipped by default but proves diff-detection works.
    4. Skip approach: `.fixme()` or `test.describe.skip` with comment explaining when to enable.
  </action>
  <done>Sanity test exists + provably fails diff.</done>
</task>

<task type="auto">
  <name>Task 3: Commit + SUMMARY</name>
  <action>`fix(31-02): §14 acceptance CTA regex + pixel sanity test (REG-02+03)`. SUMMARY.</action>
</task>

</tasks>

<verification>
1. `npx playwright test v10-acceptance-tz14` green.
2. `npx playwright test v10-pixel-snapshots` green (8 PNGs match).
3. SUMMARY exists.
</verification>

<output>31-02-SUMMARY.md per template.</output>
