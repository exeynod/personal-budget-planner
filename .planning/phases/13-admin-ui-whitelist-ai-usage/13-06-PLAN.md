---
phase: 13-admin-ui-whitelist-ai-usage
plan: "06"
type: execute
wave: 4
depends_on: ["13-04", "13-05"]
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/admin.ts
  - frontend/src/hooks/useAdminUsers.ts
  - frontend/src/hooks/useAdminAiUsage.ts
autonomous: true
requirements: [ADM-03, ADM-04, ADM-05, ADM-06, AIUSE-01, AIUSE-02, AIUSE-03]
tags: [frontend, api-client, types, hooks]
must_haves:
  truths:
    - "frontend/src/api/types.ts расширен следующими типами: AdminUserResponse, AdminUserCreateRequest, AdminAiUsageRow, AdminAiUsageResponse, AiUsageBucket — mirror backend pydantic schemas exactly"
    - "frontend/src/api/admin.ts экспортирует 4 функции: listAdminUsers(), inviteAdminUser({tg_user_id}), revokeAdminUser(userId), getAdminAiUsage()"
    - "frontend/src/hooks/useAdminUsers.ts: hook возвращает {users, loading, error, refetch, invite, revoke}; invite/revoke с optimistic UI + rollback on error (per CONTEXT decision)"
    - "frontend/src/hooks/useAdminAiUsage.ts: hook возвращает {data, loading, error, refetch}; fetchOnMount via useEffect"
    - "Все типы строго совпадают с backend schemas: spending_cap_cents=number, pct_of_cap=number, role='owner'|'member'|'revoked', last_seen_at='string|null'"
    - "tsc --noEmit passes (no TS errors)"
    - "ApiError 409 в inviteAdminUser обрабатывается: throws с .status=409 → UI показывает inline-error в InviteSheet (Plan 13-07 будет catch'ить)"
  artifacts:
    - path: frontend/src/api/types.ts
      provides: "TS типы для admin endpoints, mirror backend"
      contains: "AdminUserResponse"
      contains_2: "AdminAiUsageRow"
      contains_3: "spending_cap_cents"
    - path: frontend/src/api/admin.ts
      provides: "API client functions: list, invite, revoke, getAdminAiUsage"
      contains: "listAdminUsers"
      contains_2: "inviteAdminUser"
      contains_3: "revokeAdminUser"
      contains_4: "getAdminAiUsage"
    - path: frontend/src/hooks/useAdminUsers.ts
      provides: "Hook с optimistic update + rollback"
      contains: "useAdminUsers"
      contains_2: "optimistic"
    - path: frontend/src/hooks/useAdminAiUsage.ts
      provides: "Hook для AI usage breakdown fetch"
      contains: "useAdminAiUsage"
  key_links:
    - from: frontend/src/api/admin.ts
      to: frontend/src/api/client.ts::apiFetch
      via: "All API functions reuse apiFetch, headers + initData injection inherited"
      pattern: "apiFetch"
    - from: frontend/src/hooks/useAdminUsers.ts
      to: frontend/src/api/admin.ts
      via: "Hook calls listAdminUsers / inviteAdminUser / revokeAdminUser"
      pattern: "from.*api/admin"
    - from: frontend/src/api/types.ts::AdminAiUsageRow
      to: app/api/schemas/admin.py::AdminAiUsageRow (Plan 13-05)
      via: "Field-by-field mirror, names + types match exactly"
      pattern: "AdminAiUsageRow"
---

<objective>
Wave 4 frontend foundation: TS types + API client + 2 hooks. Чисто infra-слой без UI; следующий план (13-07) построит экраны на этих hooks.

Hooks pattern follows existing `useSettings` / `useUser` (см. context):
- mountedRef guard against state updates after unmount
- separate refetch() callback для programmatic refresh
- useEffect для initial load on mount

Optimistic update pattern в `useAdminUsers`:
- `revoke(userId)`: immediately фильтрует userId из state.users; на error rollback из snapshot
- `invite(tg_user_id)`: после server response добавляет new user в state (НЕ optimistic — потому что server присваивает id и created_at; optimism тут не даёт UX wins)

Output: 4 файла. tsc clean. Готовность к Plan 13-07 UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-CONTEXT.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-04-PLAN.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-05-PLAN.md
@CLAUDE.md
@frontend/src/api/types.ts
@frontend/src/api/client.ts
@frontend/src/api/categories.ts
@frontend/src/api/settings.ts
@frontend/src/hooks/useUser.ts
@frontend/src/hooks/useSettings.ts
@frontend/src/hooks/useCategories.ts

<interfaces>
<!-- Backend схемы (mirror exactly) — после Plan 13-04 + 13-05 -->

```python
# app/api/schemas/admin.py
class AdminUserResponse(BaseModel):
    id: int
    tg_user_id: int
    tg_chat_id: Optional[int] = None
    role: Literal["owner", "member", "revoked"]
    last_seen_at: Optional[datetime] = None  # serialized as ISO string
    onboarded_at: Optional[datetime] = None
    created_at: datetime

class AdminUserCreateRequest(BaseModel):
    tg_user_id: int  # ge=10_000

class UsageBucket(BaseModel):  # already in app/api/schemas/ai.py
    requests: int
    prompt_tokens: int
    completion_tokens: int
    cached_tokens: int
    total_tokens: int
    est_cost_usd: float

class AdminAiUsageRow(BaseModel):
    user_id: int
    tg_user_id: int
    name: Optional[str] = None
    role: Literal["owner", "member", "revoked"]
    spending_cap_cents: int
    current_month: UsageBucket
    last_30d: UsageBucket
    est_cost_cents_current_month: int
    pct_of_cap: float

class AdminAiUsageResponse(BaseModel):
    users: list[AdminAiUsageRow]
    generated_at: datetime
```

```typescript
// frontend/src/api/types.ts существующий MeResponse / UserRole — переиспользуем
export type UserRole = 'owner' | 'member' | 'revoked';
```

```typescript
// frontend/src/api/client.ts существующий apiFetch
export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T>
// throws ApiError {status, body, message}
```

Hook pattern (см. useUser):
```typescript
export function useUser(): UseUserResult {
  const [user, setUser] = useState<MeResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const refetch = useCallback(async () => { /* fetch + setState */ }, []);
  useEffect(() => { void refetch(); }, [refetch]);
  return { user, loading, error, refetch };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить frontend/src/api/types.ts — admin types (mirror backend)</name>
  <files>frontend/src/api/types.ts</files>
  <read_first>
    - frontend/src/api/types.ts (текущий state — MeResponse в начале, UserRole в начале; ChatMessageRead уже определён около line 319)
    - app/api/schemas/admin.py (после 13-04 + 13-05 — определения AdminUserResponse, AdminUserCreateRequest, AdminAiUsageRow, AdminAiUsageResponse)
    - app/api/schemas/ai.py (UsageBucket — нужно добавить TS mirror)
  </read_first>
  <action>
    В `frontend/src/api/types.ts` добавить новый блок В САМЫЙ КОНЕЦ файла:

    ```typescript
    // ---------- Phase 13: Admin (Whitelist + AI Usage) ----------

    /**
     * Mirrors `AdminUserResponse` from app/api/schemas/admin.py (Phase 13).
     * Used by GET /api/v1/admin/users.
     */
    export interface AdminUserResponse {
      id: number;
      tg_user_id: number;
      tg_chat_id: number | null;
      role: UserRole;
      last_seen_at: string | null; // ISO datetime UTC
      onboarded_at: string | null;
      created_at: string;
    }

    /**
     * Mirrors `AdminUserCreateRequest` — body для POST /api/v1/admin/users.
     * Min 5 digits enforced backend-side via `ge=10_000` (Pydantic Field).
     * Frontend дополнительно валидирует UI-сторонне (InviteSheet form).
     */
    export interface AdminUserCreateRequest {
      tg_user_id: number;
    }

    /**
     * Mirrors `UsageBucket` from app/api/schemas/ai.py.
     * Reused for current_month / last_30d nested objects in admin AI usage row.
     */
    export interface AiUsageBucket {
      requests: number;
      prompt_tokens: number;
      completion_tokens: number;
      cached_tokens: number;
      total_tokens: number;
      est_cost_usd: number;
    }

    /**
     * Mirrors `AdminAiUsageRow` (AIUSE-01..03) — one user breakdown.
     *
     * UI uses pct_of_cap to render warn-style (≥0.80) / danger-style (≥1.0)
     * in the linear progress bar (mirrors DashboardCategoryRow pattern).
     */
    export interface AdminAiUsageRow {
      user_id: number;
      tg_user_id: number;
      name: string | null;
      role: UserRole;
      spending_cap_cents: number;
      current_month: AiUsageBucket;
      last_30d: AiUsageBucket;
      est_cost_cents_current_month: number;
      pct_of_cap: number;
    }

    /**
     * Mirrors `AdminAiUsageResponse` — wrapper для GET /api/v1/admin/ai-usage.
     */
    export interface AdminAiUsageResponse {
      users: AdminAiUsageRow[];
      generated_at: string; // ISO datetime UTC
    }
    ```

    Self-checks:
    - `grep -c "AdminUserResponse\|AdminUserCreateRequest\|AdminAiUsageRow\|AdminAiUsageResponse\|AiUsageBucket" frontend/src/api/types.ts` → ≥ 5 (один на каждый interface)
    - `grep -c "spending_cap_cents" frontend/src/api/types.ts` → 1
    - `grep -c "pct_of_cap" frontend/src/api/types.ts` → 1
    - tsc check (см. verify): no errors related to types.ts
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "types\.ts|admin\." | head -10 || echo "no admin/types errors"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface AdminUserResponse" frontend/src/api/types.ts` → 1
    - `grep -c "export interface AdminUserCreateRequest" frontend/src/api/types.ts` → 1
    - `grep -c "export interface AdminAiUsageRow" frontend/src/api/types.ts` → 1
    - `grep -c "export interface AdminAiUsageResponse" frontend/src/api/types.ts` → 1
    - `grep -c "export interface AiUsageBucket" frontend/src/api/types.ts` → 1
    - tsc --noEmit: zero errors related to types.ts (existing types intact)
  </acceptance_criteria>
  <done>
    Types added; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать frontend/src/api/admin.ts — 4 API functions</name>
  <files>frontend/src/api/admin.ts</files>
  <read_first>
    - frontend/src/api/categories.ts (pattern: apiFetch wrapper, JSDoc, типизация)
    - frontend/src/api/client.ts (apiFetch signature, ApiError class)
    - frontend/src/api/types.ts (Task 1 — admin types ready)
  </read_first>
  <action>
    Создать `frontend/src/api/admin.ts`:

    ```typescript
    import { apiFetch } from './client';
    import type {
      AdminAiUsageResponse,
      AdminUserCreateRequest,
      AdminUserResponse,
    } from './types';

    /**
     * GET /api/v1/admin/users
     *
     * Returns whitelist sorted owner-first then by last_seen_at desc.
     * 403 if caller is not owner (require_owner backend dep).
     */
    export async function listAdminUsers(): Promise<AdminUserResponse[]> {
      return apiFetch<AdminUserResponse[]>('/admin/users');
    }

    /**
     * POST /api/v1/admin/users
     *
     * Invite a new member by tg_user_id (creates AppUser role=member,
     * onboarded_at=NULL). 409 if tg_user_id already exists; 422 if
     * tg_user_id < 10000 (validation).
     *
     * The invitee can run /start in the bot to begin onboarding (Phase 14).
     */
    export async function inviteAdminUser(
      payload: AdminUserCreateRequest,
    ): Promise<AdminUserResponse> {
      return apiFetch<AdminUserResponse>('/admin/users', {
        method: 'POST',
        body: JSON.stringify(payload),
      });
    }

    /**
     * DELETE /api/v1/admin/users/{user_id}
     *
     * Revoke a member: cascade purges all data (categories, periods,
     * transactions, subscriptions, AI conversation, ai_usage_log) and
     * deletes the AppUser row. The user receives 403 on subsequent requests.
     *
     * Self-revoke (owner deleting own id) returns 403.
     * Unknown user_id returns 404.
     *
     * Returns void (204 No Content from backend).
     */
    export async function revokeAdminUser(userId: number): Promise<void> {
      return apiFetch<void>(`/admin/users/${userId}`, { method: 'DELETE' });
    }

    /**
     * GET /api/v1/admin/ai-usage
     *
     * Returns per-user AI usage breakdown — current_month + last_30d windows
     * с spending_cap_cents и pct_of_cap для UI warn/danger индикатора.
     *
     * Sorted by est_cost_cents_current_month desc. 403 for non-owner.
     */
    export async function getAdminAiUsage(): Promise<AdminAiUsageResponse> {
      return apiFetch<AdminAiUsageResponse>('/admin/ai-usage');
    }
    ```

    Self-checks:
    - `grep -c "listAdminUsers\|inviteAdminUser\|revokeAdminUser\|getAdminAiUsage" frontend/src/api/admin.ts` → 4 exports
    - `grep -c "apiFetch" frontend/src/api/admin.ts` → 4 (один в каждой функции)
    - tsc --noEmit clean for admin.ts
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "admin\.ts" | head -5 || echo "no admin.ts errors"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; имеет 4 exported async functions
    - `grep -c "export async function" frontend/src/api/admin.ts` → 4
    - `grep -c "/admin/users\|/admin/ai-usage" frontend/src/api/admin.ts` → ≥ 4
    - `grep -c "apiFetch" frontend/src/api/admin.ts` → 4
    - Каждая функция возвращает Promise<T> с правильным типом
    - tsc --noEmit: 0 errors specific to admin.ts
  </acceptance_criteria>
  <done>
    API client готов; типизирован; tsc clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать frontend/src/hooks/useAdminUsers.ts + useAdminAiUsage.ts</name>
  <files>frontend/src/hooks/useAdminUsers.ts, frontend/src/hooks/useAdminAiUsage.ts</files>
  <read_first>
    - frontend/src/hooks/useUser.ts (simple fetch-on-mount pattern)
    - frontend/src/hooks/useSettings.ts (mountedRef pattern, refetch callback)
    - frontend/src/hooks/useCategories.ts (если есть optimistic patterns — для refresh после mutate)
    - frontend/src/api/admin.ts (Task 2 готов)
    - frontend/src/api/types.ts (Task 1 готов)
    - frontend/src/api/client.ts (ApiError для 409 detection в invite)
  </read_first>
  <action>
    A) `frontend/src/hooks/useAdminUsers.ts`:

    ```typescript
    import { useCallback, useEffect, useRef, useState } from 'react';
    import {
      inviteAdminUser,
      listAdminUsers,
      revokeAdminUser,
    } from '../api/admin';
    import type { AdminUserResponse } from '../api/types';

    export interface UseAdminUsersResult {
      users: AdminUserResponse[];
      loading: boolean;
      error: string | null;
      refetch: () => Promise<void>;
      /** Invite by tg_user_id; resolves to created user; rejects with ApiError on 409/422 */
      invite: (tg_user_id: number) => Promise<AdminUserResponse>;
      /** Revoke (cascade purge); optimistic UI removes row immediately, rolls back on error */
      revoke: (userId: number) => Promise<void>;
    }

    /**
     * Phase 13 admin users hook (ADM-03..06).
     *
     * Fetch + invite + revoke (optimistic). Owner-only — calling this hook
     * from a member-context will surface a 403 error, but the AccessScreen
     * will not render in that case (ManagementScreen filters the menu item
     * via useUser().role).
     */
    export function useAdminUsers(): UseAdminUsersResult {
      const [users, setUsers] = useState<AdminUserResponse[]>([]);
      const [loading, setLoading] = useState(true);
      const [error, setError] = useState<string | null>(null);
      const mountedRef = useRef(true);

      useEffect(() => {
        mountedRef.current = true;
        return () => {
          mountedRef.current = false;
        };
      }, []);

      const refetch = useCallback(async () => {
        setLoading(true);
        setError(null);
        try {
          const data = await listAdminUsers();
          if (mountedRef.current) setUsers(data);
        } catch (e: unknown) {
          if (mountedRef.current) {
            setError(e instanceof Error ? e.message : 'load failed');
          }
        } finally {
          if (mountedRef.current) setLoading(false);
        }
      }, []);

      useEffect(() => {
        let cancelled = false;
        listAdminUsers()
          .then((data) => {
            if (!cancelled && mountedRef.current) setUsers(data);
          })
          .catch((e: unknown) => {
            if (!cancelled && mountedRef.current) {
              setError(e instanceof Error ? e.message : 'load failed');
            }
          })
          .finally(() => {
            if (!cancelled && mountedRef.current) setLoading(false);
          });
        return () => {
          cancelled = true;
        };
      }, []);

      const invite = useCallback(async (tg_user_id: number) => {
        // Backend assigns id + created_at — wait for server response then append.
        const created = await inviteAdminUser({ tg_user_id });
        if (mountedRef.current) {
          setUsers((prev) => {
            // Insert keeping owner-first sort: append at end (member position).
            return [...prev, created];
          });
        }
        return created;
      }, []);

      const revoke = useCallback(async (userId: number) => {
        // Optimistic: remove immediately; rollback on failure (CONTEXT decision).
        let snapshot: AdminUserResponse[] = [];
        if (mountedRef.current) {
          setUsers((prev) => {
            snapshot = prev;
            return prev.filter((u) => u.id !== userId);
          });
        }
        try {
          await revokeAdminUser(userId);
        } catch (e) {
          // Rollback to snapshot.
          if (mountedRef.current && snapshot.length) {
            setUsers(snapshot);
          }
          throw e;
        }
      }, []);

      return { users, loading, error, refetch, invite, revoke };
    }
    ```

    B) `frontend/src/hooks/useAdminAiUsage.ts`:

    ```typescript
    import { useCallback, useEffect, useRef, useState } from 'react';
    import { getAdminAiUsage } from '../api/admin';
    import type { AdminAiUsageResponse } from '../api/types';

    export interface UseAdminAiUsageResult {
      data: AdminAiUsageResponse | null;
      loading: boolean;
      error: string | null;
      refetch: () => Promise<void>;
    }

    /**
     * Phase 13 admin AI usage hook (AIUSE-01..03).
     *
     * Owner-only fetch of per-user current_month + last_30d AI usage.
     * Refetch is exposed for explicit refresh (e.g. after invite/revoke
     * which may add/remove user rows).
     */
    export function useAdminAiUsage(): UseAdminAiUsageResult {
      const [data, setData] = useState<AdminAiUsageResponse | null>(null);
      const [loading, setLoading] = useState(true);
      const [error, setError] = useState<string | null>(null);
      const mountedRef = useRef(true);

      useEffect(() => {
        mountedRef.current = true;
        return () => {
          mountedRef.current = false;
        };
      }, []);

      const refetch = useCallback(async () => {
        setLoading(true);
        setError(null);
        try {
          const fresh = await getAdminAiUsage();
          if (mountedRef.current) setData(fresh);
        } catch (e: unknown) {
          if (mountedRef.current) {
            setError(e instanceof Error ? e.message : 'load failed');
          }
        } finally {
          if (mountedRef.current) setLoading(false);
        }
      }, []);

      useEffect(() => {
        let cancelled = false;
        getAdminAiUsage()
          .then((fresh) => {
            if (!cancelled && mountedRef.current) setData(fresh);
          })
          .catch((e: unknown) => {
            if (!cancelled && mountedRef.current) {
              setError(e instanceof Error ? e.message : 'load failed');
            }
          })
          .finally(() => {
            if (!cancelled && mountedRef.current) setLoading(false);
          });
        return () => {
          cancelled = true;
        };
      }, []);

      return { data, loading, error, refetch };
    }
    ```

    Self-checks:
    - `grep -c "useAdminUsers\|useAdminAiUsage" frontend/src/hooks/useAdminUsers.ts frontend/src/hooks/useAdminAiUsage.ts` → ≥ 2
    - `grep -c "snapshot" frontend/src/hooks/useAdminUsers.ts` → ≥ 1 (rollback evidence)
    - `grep -c "mountedRef" frontend/src/hooks/useAdminUsers.ts frontend/src/hooks/useAdminAiUsage.ts` → ≥ 4 (mount cleanup pattern)
    - tsc --noEmit clean
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | grep -E "useAdmin" | head -5 || echo "no useAdmin errors"</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; export named hooks `useAdminUsers` / `useAdminAiUsage`
    - `grep -c "export function useAdminUsers" frontend/src/hooks/useAdminUsers.ts` → 1
    - `grep -c "export function useAdminAiUsage" frontend/src/hooks/useAdminAiUsage.ts` → 1
    - useAdminUsers возвращает {users, loading, error, refetch, invite, revoke}
    - useAdminAiUsage возвращает {data, loading, error, refetch}
    - Optimistic + rollback pattern в revoke: `grep -c "snapshot" frontend/src/hooks/useAdminUsers.ts` → ≥ 1
    - tsc --noEmit: 0 errors specific to useAdmin* files
    - `cd frontend && npx tsc --noEmit 2>&1 | tail -5` shows clean OR pre-existing unrelated errors only
  </acceptance_criteria>
  <done>
    Hooks готовы; tsc clean; ready for Plan 13-07 UI consumption.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS types ↔ Backend Pydantic schemas | Manual mirroring; drift detected when tsc fails after backend schema edits |
| Hook → API client → backend | apiFetch attaches initData header; backend require_owner gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | I (Info Disclosure) | Frontend stores response data with cap/cost in plain state | accept | Single-tenant UI on owner device; no broadcast/share; fits MVP threat model |
| T-13-06-02 | T (Tampering) | Optimistic revoke doesn't roll back on partial failure | mitigate | snapshot captured BEFORE setState; restored only on caught error |
| T-13-06-03 | E (Elevation) | Member opens devtools, calls hook directly bypassing UI gate | accept | Backend require_owner returns 403 — hook surfaces error, no escalation |
| T-13-06-04 | T | Server returns 409 invite, hook silently re-throws | mitigate | invite() rethrows ApiError untouched; UI (Plan 13-07) catches and shows inline error |

</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` → 0 errors related to admin files
- All 4 API functions exported, typed, и используют apiFetch
- Hooks следуют существующему mountedRef pattern (no setState after unmount)
- Optimistic revoke с rollback verified in code (snapshot variable)
</verification>

<success_criteria>
- 4 файла модифицированы/созданы:
  - MOD: frontend/src/api/types.ts (5 new interfaces)
  - NEW: frontend/src/api/admin.ts (4 API functions)
  - NEW: frontend/src/hooks/useAdminUsers.ts
  - NEW: frontend/src/hooks/useAdminAiUsage.ts
- tsc --noEmit clean (no admin-specific errors)
- API surface ready for Plan 13-07 UI integration
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ui-whitelist-ai-usage/13-06-SUMMARY.md`
</output>
