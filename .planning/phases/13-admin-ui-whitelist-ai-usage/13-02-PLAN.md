---
phase: 13-admin-ui-whitelist-ai-usage
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - alembic/versions/0008_admin_phase13.py
  - app/db/models.py
autonomous: true
requirements: [AIUSE-01, AIUSE-02, AIUSE-03]
tags: [alembic, schema, ai-usage, spending-cap]
must_haves:
  truths:
    - "Alembic 0008 (`0008_admin_phase13`) ревизия применяется чисто на dev DB после `0007_postgres_role_split`: upgrade ✓ / downgrade -1 ✓ / upgrade head ✓"
    - "Колонка app_user.spending_cap_cents BIGINT NOT NULL DEFAULT 46500 существует после upgrade; SELECT spending_cap_cents FROM app_user WHERE tg_user_id=OWNER_TG_ID → 46500 для existing OWNER row (server_default fills retroactively)"
    - "Таблица ai_usage_log создана: id BIGINT PK, user_id BIGINT NOT NULL FK app_user(id) ON DELETE CASCADE, model TEXT NOT NULL, prompt_tokens/completion_tokens/cached_tokens/total_tokens INTEGER NOT NULL DEFAULT 0, est_cost_usd DOUBLE PRECISION NOT NULL DEFAULT 0.0, created_at TIMESTAMPTZ NOT NULL DEFAULT now()"
    - "Index ix_ai_usage_log_user_id_created_at на (user_id, created_at DESC) для быстрых per-user breakdown запросов"
    - "RLS на ai_usage_log: ENABLE + FORCE ROW LEVEL SECURITY + policy ai_usage_log_user_isolation (`user_id = coalesce(NULLIF(current_setting('app.current_user_id', true), '')::bigint, -1)`) — pattern из alembic 0006"
    - "GRANT SELECT, INSERT, UPDATE, DELETE на ai_usage_log роли budget_app — pattern из 0007 (alembic upgrade под ADMIN_DATABASE_URL устанавливает grants)"
    - "Downgrade -1 удаляет ai_usage_log таблицу (cascade-safe — RLS policy + index уходят вместе) и spending_cap_cents column из app_user"
    - "ORM model AiUsageLog добавлена в app/db/models.py с user_id Mapped[int] FK ON DELETE CASCADE и __table_args__ с Index('ix_ai_usage_log_user_created', 'user_id', 'created_at')"
    - "AppUser.spending_cap_cents Mapped[int] добавлен с default=46500 server_default='46500'"
    - "alembic upgrade head в api контейнере выполняется чисто, нет syntax errors"
  artifacts:
    - path: alembic/versions/0008_admin_phase13.py
      provides: "Migration: spending_cap_cents column + ai_usage_log table + RLS + grants"
      contains: "spending_cap_cents"
      contains_2: "ai_usage_log"
      contains_3: "ON DELETE CASCADE"
      contains_4: "FORCE ROW LEVEL SECURITY"
    - path: app/db/models.py
      provides: "AppUser.spending_cap_cents Mapped[int] + AiUsageLog model"
      contains: "class AiUsageLog(Base)"
      contains_2: "spending_cap_cents"
      contains_3: 'ondelete="CASCADE"'
  key_links:
    - from: alembic/versions/0008_admin_phase13.py
      to: alembic/versions/0007_postgres_role_split.py
      via: "down_revision = '0007_postgres_role_split'"
      pattern: "down_revision.*0007_postgres_role_split"
    - from: app/db/models.py::AiUsageLog
      to: alembic/versions/0008_admin_phase13.py::ai_usage_log table DDL
      via: "ORM mirrors migration; columns + types + index + FK match exactly"
      pattern: "class AiUsageLog"
    - from: alembic/versions/0008_admin_phase13.py::GRANT statements
      to: alembic/versions/0007_postgres_role_split.py::budget_app role
      via: "GRANT SELECT, INSERT ON ai_usage_log TO budget_app — needed for runtime hook (Plan 13-03) to write rows"
      pattern: "GRANT.*budget_app"
---

<objective>
Wave 1 schema foundation. Создаём alembic 0008 (один атомарный revision) который добавляет:

1. `app_user.spending_cap_cents BIGINT NOT NULL DEFAULT 46500` — stub для Phase 15 enforcement; дефолт ≈ $5/мес USD копейки. Existing rows получают значение через server_default.

2. Таблицу `ai_usage_log` — persistent storage для per-user AI usage breakdown (AIUSE-02). Hook в Plan 13-03 будет писать сюда на каждом ai/chat запросе.

3. RLS policy + grants для budget_app — pattern из 0006/0007.

ORM модели в `app/db/models.py` синхронно обновляются: `AppUser.spending_cap_cents` + новый `AiUsageLog` model.

Purpose: Положить фундамент для admin AI usage breakdown (Plans 13-03, 13-05) и для Phase 15 cap enforcement. Один атомарный revision = один rollback (если что-то сломается, downgrade чистый).

Output: 1 новый alembic файл + расширение models.py. После применения миграции:
- `psql \\d app_user` показывает spending_cap_cents column
- `psql \\d ai_usage_log` показывает таблицу с FK + RLS + index
- pytest test_admin_*_api.py уже не падает на ProgrammingError (но ещё RED на 404)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-CONTEXT.md
@.planning/phases/11-multi-tenancy-db-migration/11-VERIFICATION.md
@.planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md
@CLAUDE.md
@alembic/versions/0006_multitenancy.py
@alembic/versions/0007_postgres_role_split.py
@app/db/models.py

<interfaces>
<!-- Существующие контракты из 0006/0007 — копируем pattern. -->

From alembic/versions/0006_multitenancy.py — RLS pattern:
```python
op.execute(f"ALTER TABLE {table} ENABLE ROW LEVEL SECURITY")
op.execute(f"ALTER TABLE {table} FORCE ROW LEVEL SECURITY")
op.execute(
    f"CREATE POLICY {table}_user_isolation ON {table} "
    f"USING (user_id = coalesce(NULLIF(current_setting('app.current_user_id', true), '')::bigint, -1))"
)
```

From alembic/versions/0007_postgres_role_split.py — grants pattern:
```python
op.execute("GRANT SELECT, INSERT, UPDATE, DELETE ON {table} TO budget_app")
op.execute("GRANT USAGE, SELECT ON SEQUENCE {sequence_name} TO budget_app")
```

From app/db/models.py — Mapped column pattern:
```python
class AppUser(Base):
    __tablename__ = "app_user"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    # ... existing columns ...
    enable_ai_categorization: Mapped[bool] = mapped_column(
        Boolean, default=True, nullable=False, server_default="true"
    )
    role: Mapped[UserRole] = mapped_column(
        PgEnum(UserRole, name="user_role", create_type=False),
        nullable=False, server_default="member", default=UserRole.member,
    )
    # add HERE: spending_cap_cents
```

ai_message / ai_conversation pattern (existing models — sibling таблицы):
```python
class AiMessage(Base):
    __tablename__ = "ai_message"
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    conversation_id: Mapped[int] = mapped_column(ForeignKey("ai_conversation.id"), nullable=False)
    user_id: Mapped[int] = mapped_column(
        BigInteger, ForeignKey("app_user.id", ondelete="RESTRICT"), nullable=False,
    )
    __table_args__ = (Index("ix_ai_message_conversation", "conversation_id"),)
```

NB: AI conversation/message используют `ondelete="RESTRICT"` — потому что Phase 13 ADM-05 cascade purge делается service-layer'ом (см. CONTEXT.md). Однако `ai_usage_log` не должна блокировать DELETE app_user — она metadata-only; используем `ondelete="CASCADE"` для упрощения revoke flow.

Проверка после 0007:
- роль budget_app: NOSUPERUSER NOBYPASSRLS LOGIN с грантом SELECT/INSERT/UPDATE/DELETE на существующие таблицы
- Default privileges уже выставлены в 0007 — НО: они срабатывают только для таблиц созданных потом ТЕМ ЖЕ юзером (`budget`). Migration 0008 запустится через ADMIN_DATABASE_URL=`budget`, поэтому default privileges должны автоматически дать гранты budget_app... но Plan 12-05 SUMMARY и 0007 показывают что default privileges выставлены `FOR ROLE budget`. Чтобы быть defensive, ЯВНО грантим в 0008 — это no-op если default уже сработал, не вредит.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать alembic/versions/0008_admin_phase13.py — spending_cap_cents + ai_usage_log + RLS + grants</name>
  <files>alembic/versions/0008_admin_phase13.py</files>
  <read_first>
    - alembic/versions/0006_multitenancy.py (RLS policy pattern, server_default fill, downgrade order)
    - alembic/versions/0007_postgres_role_split.py (grant pattern, file header style, revision id length ≤ 32 chars rule per 11-07 Note 1)
    - app/db/models.py (current AppUser shape — чтобы знать порядок колонок не сломался)
    - alembic/env.py (если есть кастомные конфигурации)
  </read_first>
  <action>
    Создать файл `alembic/versions/0008_admin_phase13.py`:

    ```python
    """admin phase13: spending_cap_cents + ai_usage_log table

    Revision ID: 0008_admin_phase13
    Revises: 0007_postgres_role_split
    Create Date: 2026-05-07

    Phase 13 schema additions:
      1. app_user.spending_cap_cents BIGINT NOT NULL DEFAULT 46500
         (USD копейки, ≈ $5/мес — stub для Phase 15 enforcement).
         Existing rows backfill through server_default at column creation.
      2. ai_usage_log table — persistent record per /ai/chat invocation
         (replaces the in-memory ring buffer for admin breakdown). One row
         per LLM call: tokens + est cost + UTC timestamp.
      3. RLS на ai_usage_log (защита защитой; admin endpoint в 13-05
         использует privileged-query pattern с set_config bypass).
      4. GRANTS на ai_usage_log для budget_app (runtime может INSERT/SELECT).

    ON DELETE CASCADE на user_id FK — отличие от других доменных таблиц
    (которые используют RESTRICT + service-layer purge per Plan 11-02 D-NOTE):
    ai_usage_log — telemetry-метаданные без защищаемой бизнес-семантики;
    каскадное удаление упрощает Phase 13 revoke flow (Plan 13-04 service
    layer не должен явно DELETE из ai_usage_log).

    Operational notes:
      - Migration runs as privileged role (ADMIN_DATABASE_URL → budget).
      - GRANT statements assume budget_app role exists (created in 0007).
      - Idempotency на CREATE TABLE через IF NOT EXISTS — НЕ используем
        чтобы alembic умел rollback'ать чисто; downgrade удаляет таблицу.
    """
    from __future__ import annotations

    from typing import Sequence, Union

    import sqlalchemy as sa
    from alembic import op


    revision: str = "0008_admin_phase13"
    down_revision: Union[str, None] = "0007_postgres_role_split"
    branch_labels: Union[str, Sequence[str], None] = None
    depends_on: Union[str, Sequence[str], None] = None


    # USD копейки (cents-of-USD): 46500 ≈ $5.00 — stub default per CONTEXT.
    DEFAULT_SPENDING_CAP_CENTS = 46500


    def upgrade() -> None:
        # ---- 1. AppUser.spending_cap_cents column ----
        # server_default fills existing rows + future inserts that omit the field.
        op.add_column(
            "app_user",
            sa.Column(
                "spending_cap_cents",
                sa.BigInteger(),
                nullable=False,
                server_default=str(DEFAULT_SPENDING_CAP_CENTS),
            ),
        )

        # ---- 2. ai_usage_log table ----
        op.create_table(
            "ai_usage_log",
            sa.Column("id", sa.BigInteger(), primary_key=True, autoincrement=True),
            sa.Column(
                "user_id",
                sa.BigInteger(),
                sa.ForeignKey("app_user.id", ondelete="CASCADE"),
                nullable=False,
            ),
            sa.Column("model", sa.Text(), nullable=False),
            sa.Column("prompt_tokens", sa.Integer(), nullable=False, server_default="0"),
            sa.Column("completion_tokens", sa.Integer(), nullable=False, server_default="0"),
            sa.Column("cached_tokens", sa.Integer(), nullable=False, server_default="0"),
            sa.Column("total_tokens", sa.Integer(), nullable=False, server_default="0"),
            sa.Column("est_cost_usd", sa.Float(asdecimal=False), nullable=False, server_default="0.0"),
            sa.Column(
                "created_at",
                sa.TIMESTAMP(timezone=True),
                nullable=False,
                server_default=sa.text("now()"),
            ),
        )

        # Composite index on (user_id, created_at) — supports per-user
        # current-month and last-30d aggregations (Plan 13-05).
        op.create_index(
            "ix_ai_usage_log_user_created",
            "ai_usage_log",
            ["user_id", sa.text("created_at DESC")],
            unique=False,
        )

        # ---- 3. RLS on ai_usage_log (defence in depth) ----
        op.execute("ALTER TABLE ai_usage_log ENABLE ROW LEVEL SECURITY")
        op.execute("ALTER TABLE ai_usage_log FORCE ROW LEVEL SECURITY")
        op.execute(
            "CREATE POLICY ai_usage_log_user_isolation ON ai_usage_log "
            "USING (user_id = coalesce(NULLIF(current_setting('app.current_user_id', true), '')::bigint, -1))"
        )

        # ---- 4. GRANTS for budget_app (runtime app role) ----
        # SELECT/INSERT для запись usage events + admin breakdown reads.
        # UPDATE/DELETE — для cascade safety при revoke (Plan 13-04 может
        # делать DELETE FROM ai_usage_log WHERE user_id=... как defensive).
        op.execute(
            "GRANT SELECT, INSERT, UPDATE, DELETE ON ai_usage_log TO budget_app"
        )
        op.execute("GRANT USAGE, SELECT ON SEQUENCE ai_usage_log_id_seq TO budget_app")


    def downgrade() -> None:
        # Reverse order — drop dependent objects first.
        op.execute("REVOKE ALL ON ai_usage_log FROM budget_app")
        # Sequence revoke is safe even if budget_app role is missing.
        op.execute("REVOKE ALL ON SEQUENCE ai_usage_log_id_seq FROM budget_app")

        # Policy / RLS settings drop with the table — but explicit DROP POLICY
        # is harmless and signals intent.
        op.execute("DROP POLICY IF EXISTS ai_usage_log_user_isolation ON ai_usage_log")

        op.drop_index("ix_ai_usage_log_user_created", table_name="ai_usage_log")
        op.drop_table("ai_usage_log")

        op.drop_column("app_user", "spending_cap_cents")
    ```

    Sanity checks при написании:
    - revision id `0008_admin_phase13` = 18 chars ≤ 32 (alembic_version VARCHAR(32) per 11-07 Note 1)
    - down_revision = "0007_postgres_role_split" matches HEAD before this migration
    - server_default используется как строка для INTEGER (`"0"`, `"46500"`); для now() — `sa.text("now()")`
    - Index с DESC: alembic op.create_index принимает sa.text внутри columns list — pattern из 0006 для подобных индексов

    NB: проверьте что в 0006/0007 не использовалось `op.create_index(..., postgresql_using='btree', expressions=...)` варианта. Если возникнут проблемы с DESC, fallback — обычный index без DESC: `["user_id", "created_at"]` (Postgres index сканится в обе стороны эффективно).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import importlib.util, sys; spec = importlib.util.spec_from_file_location('m', 'alembic/versions/0008_admin_phase13.py'); m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); assert m.revision == '0008_admin_phase13'; assert m.down_revision == '0007_postgres_role_split'; assert callable(m.upgrade); assert callable(m.downgrade); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with revision id ≤ 32 chars (`0008_admin_phase13` = 18 chars)
    - `grep -c 'down_revision: Union\[str, None\] = "0007_postgres_role_split"' alembic/versions/0008_admin_phase13.py` → 1
    - `grep -c "ai_usage_log" alembic/versions/0008_admin_phase13.py` → ≥ 8 (CREATE TABLE, ENABLE RLS, FORCE RLS, CREATE POLICY, INDEX, GRANTS, DROP)
    - `grep -c "spending_cap_cents" alembic/versions/0008_admin_phase13.py` → ≥ 3 (add_column + drop_column + DEFAULT_SPENDING_CAP_CENTS docstring)
    - `grep -c "ON DELETE CASCADE\|ondelete=\"CASCADE\"" alembic/versions/0008_admin_phase13.py` → ≥ 1 (FK on ai_usage_log.user_id)
    - `grep -c "GRANT.*budget_app" alembic/versions/0008_admin_phase13.py` → ≥ 2 (table + sequence)
    - `grep -c "FORCE ROW LEVEL SECURITY" alembic/versions/0008_admin_phase13.py` → 1
    - Module loads without SyntaxError; `revision`, `down_revision`, `upgrade`, `downgrade` symbols все present
    - `cd /Users/exy/pet_projects/tg-budget-planner && uv run alembic check 2>&1 | tee /tmp/alembic_check.log` — no "Multiple head revisions" error (можно ignore если check команда unavailable; основная проверка через apply ниже)
  </acceptance_criteria>
  <done>
    Файл создан, ревизия валидна (id ≤ 32 chars), down_revision = 0007_postgres_role_split, upgrade и downgrade функции присутствуют. Apply test делается в Plan 13-08 verification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Расширить app/db/models.py — AppUser.spending_cap_cents + AiUsageLog model</name>
  <files>app/db/models.py</files>
  <read_first>
    - app/db/models.py (полный файл — нужно знать ровно где вставить колонку и model)
    - alembic/versions/0008_admin_phase13.py (созданный в Task 1, для синхронизации column types)
  </read_first>
  <action>
    Изменения в `app/db/models.py`:

    1) В классе `AppUser` ПОСЛЕ строки `enable_ai_categorization: Mapped[bool] = ...` (заканчивается на `server_default="true"\n    )`) и ПЕРЕД `role:` добавить:

    ```python
        spending_cap_cents: Mapped[int] = mapped_column(
            BigInteger,
            nullable=False,
            default=46500,
            server_default="46500",
        )
    ```

    Reasoning по позиции: рядом с другими AI/usage-related колонками (`enable_ai_categorization`); сохраняет семантическую группировку.

    2) В КОНЦЕ файла, после `class CategoryEmbedding(Base):` блока, добавить новый model:

    ```python
    # ---- Phase 13: Admin AI Usage Breakdown ----


    class AiUsageLog(Base):
        """Persistent log одного /ai/chat вызова (Plan 13-03 hooks here from
        app/api/routes/ai.py::_record_usage). Используется admin /ai-usage
        endpoint (Plan 13-05) для per-user breakdown за current month + 30d.

        ON DELETE CASCADE на user_id — telemetry без защищаемой бизнес-
        семантики, упрощает Phase 13 revoke flow (cascade purge без явного
        DELETE из service-слоя).

        RLS policy (alembic 0008): user_id = current_setting(app.current_user_id).
        Admin endpoint обходит RLS через set_config bypass или privileged
        query (Plan 13-05 detail).
        """

        __tablename__ = "ai_usage_log"

        id: Mapped[int] = mapped_column(BigInteger, primary_key=True, autoincrement=True)
        user_id: Mapped[int] = mapped_column(
            BigInteger,
            ForeignKey("app_user.id", ondelete="CASCADE"),
            nullable=False,
        )
        model: Mapped[str] = mapped_column(Text, nullable=False)
        prompt_tokens: Mapped[int] = mapped_column(
            Integer, nullable=False, default=0, server_default="0"
        )
        completion_tokens: Mapped[int] = mapped_column(
            Integer, nullable=False, default=0, server_default="0"
        )
        cached_tokens: Mapped[int] = mapped_column(
            Integer, nullable=False, default=0, server_default="0"
        )
        total_tokens: Mapped[int] = mapped_column(
            Integer, nullable=False, default=0, server_default="0"
        )
        est_cost_usd: Mapped[float] = mapped_column(
            sa_Float(asdecimal=False),
            nullable=False,
            default=0.0,
            server_default="0.0",
        )
        created_at: Mapped[datetime] = mapped_column(
            TIMESTAMP(timezone=True),
            server_default=func.now(),
            nullable=False,
        )

        __table_args__ = (
            Index("ix_ai_usage_log_user_created", "user_id", "created_at"),
        )
    ```

    3) Чтобы `sa_Float` работал, добавить импорт в шапку файла. Проверить — есть ли `Float` в импорте `from sqlalchemy import (...)`. Если нет — ДОБАВИТЬ его в существующий импорт:

    Найти строку `from sqlalchemy import (` — между `BigInteger,` и `ForeignKey,` (или где-то рядом) добавить `Float,`. Использовать алиас НЕ нужно — импортируем напрямую `Float` и в коде пишем `Float(asdecimal=False)`. Корректировка: убрать `sa_Float` алиас и просто использовать `Float`.

    Финальное тело AiUsageLog est_cost_usd column:
    ```python
        est_cost_usd: Mapped[float] = mapped_column(
            Float(asdecimal=False),
            nullable=False,
            default=0.0,
            server_default="0.0",
        )
    ```

    Self-check после редактирования:
    - `grep -c "spending_cap_cents" app/db/models.py` → ≥ 1
    - `grep -c "class AiUsageLog" app/db/models.py` → 1
    - `grep -c '^from sqlalchemy import' app/db/models.py` → 1 строка (один импорт-блок)
    - `grep "Float" app/db/models.py` → должна быть строка с `Float,` в импорт-блоке
    - Python синтаксис валиден: `python -c "import app.db.models; assert hasattr(app.db.models, 'AiUsageLog'); assert hasattr(app.db.models.AppUser, 'spending_cap_cents'); print('OK')"` (требует SET PYTHONPATH=. либо `cd` в репозиторий)
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "import sys; sys.path.insert(0, '.'); from app.db.models import AppUser, AiUsageLog; assert hasattr(AppUser, 'spending_cap_cents'); assert AiUsageLog.__tablename__ == 'ai_usage_log'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spending_cap_cents" app/db/models.py` → ≥ 1
    - `grep -c "class AiUsageLog(Base)" app/db/models.py` → 1
    - `grep -c 'ondelete="CASCADE"' app/db/models.py` → ≥ 1 (новая FK + возможные существующие)
    - `grep "ix_ai_usage_log_user_created" app/db/models.py` → 1 строка
    - Импорт `Float` добавлен в `from sqlalchemy import (...)` блок: `grep -c "    Float," app/db/models.py` → 1 (либо `Float,` где-то в импорт-блоке)
    - Python import works: `python -c "from app.db.models import AppUser, AiUsageLog; print('OK')"` exit 0
    - Структурный self-test: `AppUser.__table__.columns['spending_cap_cents'].nullable == False` и `AiUsageLog.__table__.columns['user_id'].foreign_keys` не пустой set
  </acceptance_criteria>
  <done>
    Models.py содержит AppUser.spending_cap_cents + AiUsageLog; импорты валидны; Python loadable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration runner → DB (admin role) | 0008 runs as `budget` (ADMIN_DATABASE_URL); has CREATE TABLE/POLICY/GRANT privilege |
| Runtime API → ai_usage_log | budget_app INSERTs rows from /ai/chat; SELECT under tenant scope (RLS enforces) |
| Admin endpoint → ai_usage_log | Plan 13-05 will need to bypass RLS to aggregate cross-user — uses set_config bypass or service-layer privileged read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | T (Tampering) | Existing app_user rows lose role due to bad migration order | mitigate | server_default="46500" fills existing rows in single ALTER TABLE step; alembic op.add_column atomic per Postgres DDL semantics |
| T-13-02-02 | I (Info Disclosure) | RLS on ai_usage_log lets cross-user aggregate query leak data | mitigate | Same coalesce-NULLIF pattern as 0006 (`-1` sentinel when GUC unset → 0 rows visible); admin breakdown explicitly bypasses via privileged query (Plan 13-05) |
| T-13-02-03 | E (Elevation) | budget_app gets excess privilege (TRUNCATE, ALTER, OWNER) | mitigate | Migration grants ONLY SELECT/INSERT/UPDATE/DELETE on table + USAGE/SELECT on sequence; no ALTER, no TRUNCATE (matches 0007 pattern) |
| T-13-02-04 | D (Denial of Service) | ai_usage_log unbounded growth fills disk | accept | Phase 15 will add retention job (current scope: shipment of admin breakdown). Volume estimate: 1KB × 100 calls/day × 50 users × 365 = ~1.8GB/year — acceptable for SoC pet hardware until Phase 15 implements TTL |
| T-13-02-05 | R (Repudiation) | spending_cap_cents change not logged | accept | Phase 13 ships stub default; Phase 15 introduces PATCH cap endpoint with audit log |

</threat_model>

<verification>
- alembic/versions/0008_admin_phase13.py loadable as Python module (Task 1 acceptance)
- app/db/models.py importable, AppUser.spending_cap_cents + AiUsageLog defined (Task 2 acceptance)
- alembic upgrade head НЕ запускается в этом плане — это работа Plan 13-08 verification (потому что upgrade требует чистого DB state и зависит от 13-03 наличия hooks; verification фаза проверит full cycle: upgrade → seed → assert → downgrade -1 → upgrade head)
- Никакой production runtime code не модифицирован (только schema descriptors)
</verification>

<success_criteria>
- 1 новый файл: alembic/versions/0008_admin_phase13.py с revision id `0008_admin_phase13`, down_revision `0007_postgres_role_split`, upgrade/downgrade функции
- 1 модифицированный файл: app/db/models.py с AppUser.spending_cap_cents + новым AiUsageLog классом + импортом Float
- Python import: `from app.db.models import AppUser, AiUsageLog` exit 0
- alembic revision file syntactically valid (Python loadable)
- Готовность к Plan 13-03 (hook в ai.py будет писать в AiUsageLog), Plan 13-04 (admin users routes), Plan 13-05 (admin AI usage routes)
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ui-whitelist-ai-usage/13-02-SUMMARY.md`
</output>
