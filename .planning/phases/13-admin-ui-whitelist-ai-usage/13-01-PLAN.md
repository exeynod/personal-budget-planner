---
phase: 13-admin-ui-whitelist-ai-usage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_admin_users_api.py
  - tests/test_admin_ai_usage_api.py
  - tests/helpers/seed.py
autonomous: true
requirements: [ADM-03, ADM-04, ADM-05, ADM-06, AIUSE-01, AIUSE-02, AIUSE-03]
tags: [tdd, red-tests, admin, threat-model-evidence]
must_haves:
  truths:
    - "tests/test_admin_users_api.py существует, импортируется без syntax errors, содержит RED-тесты на GET/POST/DELETE /api/v1/admin/users — все падают на 404 (роут не зарегистрирован) или ImportError"
    - "tests/test_admin_ai_usage_api.py существует, RED — GET /api/v1/admin/ai-usage возвращает 404 пока route не зарегистрирован"
    - "tests/helpers/seed.py получает helper seed_two_role_tenants(SessionLocal, *, owner_tg_id, member_tg_id) → dict с id'ами обеих юзеров (owner+member для admin RBAC тестов)"
    - "Каждый файл декларирует as RED через статус-комментарий + assertions ожидаемо fail на текущем коде (роутов нет, helper-а нет)"
    - "pytest --collect-only собирает все добавленные тесты (≥ 12 items в admin_users + ≥ 5 в admin_ai_usage) без collection errors"
  artifacts:
    - path: tests/test_admin_users_api.py
      provides: "RED tests for ADM-03/04/05/06 — list/invite/revoke endpoints + RBAC + cascade purge + self-revoke 403"
      contains: "/api/v1/admin/users"
      contains_2: "require_owner"
    - path: tests/test_admin_ai_usage_api.py
      provides: "RED tests for AIUSE-01/02/03 — GET /admin/ai-usage per-user breakdown с current_month + last_30d + cap percentage"
      contains: "/api/v1/admin/ai-usage"
      contains_2: "spending_cap_cents"
    - path: tests/helpers/seed.py
      provides: "Расширение: seed_two_role_tenants helper + seed_ai_usage_log helper для admin tests"
      contains: "seed_two_role_tenants"
      contains_2: "seed_ai_usage_log"
  key_links:
    - from: tests/test_admin_users_api.py
      to: app/api/routes/admin.py (does not exist yet — Plan 13-04)
      via: "POST /api/v1/admin/users → 404 пока routes/admin.py не подключён → RED"
      pattern: "POST .*admin/users"
    - from: tests/test_admin_ai_usage_api.py
      to: app/api/routes/admin.py::admin_ai_usage_router (does not exist yet — Plan 13-05)
      via: "GET /api/v1/admin/ai-usage → 404 → RED"
      pattern: "GET .*admin/ai-usage"
    - from: tests/helpers/seed.py::seed_two_role_tenants
      to: app/db/models.py::AppUser, UserRole
      via: "Helper вызывает seed_user(role=UserRole.owner) и seed_user(role=UserRole.member) подряд"
      pattern: "UserRole.owner"
---

<objective>
Wave 0 RED gate для Phase 13. Создаём 2 файла тестов + расширяем `tests/helpers/seed.py`. Тесты покрывают полный admin контракт (whitelist CRUD + RBAC + cascade purge + AI usage per-user). На текущем коде ВСЕ tests RED: роуты `/api/v1/admin/*` ещё не существуют, helper'ы тоже.

Purpose: Test-driven evidence для requirements ADM-03..06 + AIUSE-01..03 + threat-model evidence (member → 403, self-revoke owner → 403, cross-tenant data leak prevention). Plans 13-04, 13-05 будут переводить эти tests в GREEN.

Output: 2 новых файла в `tests/`, расширение `tests/helpers/seed.py`. Все тесты собираются pytest --collect-only, при запуске на текущем коде падают expected RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-admin-ui-whitelist-ai-usage/13-CONTEXT.md
@.planning/phases/12-role-based-auth-refactor/12-VERIFICATION.md
@.planning/phases/12-role-based-auth-refactor/12-01-PLAN.md
@CLAUDE.md
@app/api/dependencies.py
@app/api/router.py
@app/db/models.py
@tests/conftest.py
@tests/helpers/seed.py
@tests/test_role_based_auth.py
@tests/test_require_owner.py

<interfaces>
<!-- Контракты, на которые опираются RED-тесты. После 13-04/13-05 они станут реализованы. -->

Backend admin endpoints (PLANNED in 13-04/13-05 — RED until then):

```python
# app/api/routes/admin.py (будет создан в 13-04)
admin_router = APIRouter(
    prefix="/admin",
    tags=["admin"],
    dependencies=[Depends(require_owner)],  # ROLE-04 enforcement
)

@admin_router.get("/users", response_model=list[AdminUserResponse])
async def list_users(...): ...
# Returns: [{id, tg_user_id, tg_chat_id, role, last_seen_at, onboarded_at, created_at}]
# Sort: owner first, then by last_seen_at desc (NULLs last)

@admin_router.post("/users", response_model=AdminUserResponse, status_code=201)
async def create_user(payload: AdminUserCreateRequest): ...
# Body: {tg_user_id: int}; min 5 digits, no @username
# 409 if tg_user_id already exists; 201 with new AppUser snapshot if created

@admin_router.delete("/users/{user_id}", status_code=204)
async def revoke_user(user_id: int, current_user: AppUser = Depends(require_owner)): ...
# 403 если user_id == current_user.id (self-revoke forbidden)
# 404 если user_id не существует
# Cascade purge всех связанных строк (categories, periods, transactions, subs,
#   templates, embeddings, ai_conversation, ai_message), затем DELETE app_user
# Audit-log: structured log line `audit.user_revoked uid=... by_owner=... purged_rows=...`
```

```python
# app/api/routes/admin.py (extension в 13-05)
@admin_router.get("/ai-usage", response_model=AdminAiUsageResponse)
async def admin_ai_usage(): ...
# Returns: {users: [{user_id, tg_user_id, name, role, current_month: UsageBucket, last_30d: UsageBucket,
#                     spending_cap_cents, est_cost_cents_current_month, pct_of_cap}], ...}
# Sort: by est_cost_cents_current_month DESC; alphabetical fallback when all zero.
```

```python
# app/api/schemas/admin.py (PLANNED in 13-04)
class AdminUserResponse(BaseModel):
    id: int
    tg_user_id: int
    tg_chat_id: int | None
    role: Literal["owner", "member", "revoked"]
    last_seen_at: datetime | None
    onboarded_at: datetime | None
    created_at: datetime

class AdminUserCreateRequest(BaseModel):
    tg_user_id: int = Field(..., ge=10_000)  # min 5 digits

class AdminAiUsageRow(BaseModel):
    user_id: int
    tg_user_id: int
    name: str | None  # tg_chat_id-derived если есть, иначе tg_user_id строкой
    role: Literal["owner", "member", "revoked"]
    spending_cap_cents: int  # USD копейки (Phase 13 stub default 46500)
    current_month: UsageBucket
    last_30d: UsageBucket
    est_cost_cents_current_month: int  # for sort + cap %
    pct_of_cap: float  # current_month_est_cost / spending_cap (0.0-...)

class AdminAiUsageResponse(BaseModel):
    users: list[AdminAiUsageRow]
    generated_at: str  # ISO datetime
```

Existing helpers used (from tests/helpers/seed.py):

```python
async def seed_user(session, *, tg_user_id, role=UserRole.owner, ...) -> AppUser
async def seed_category(session, *, user_id, ...) -> Category
async def seed_budget_period(session, *, user_id, ...) -> BudgetPeriod
async def seed_actual_transaction(session, *, user_id, period_id, ...) -> ActualTransaction
async def truncate_db(*, tables=...) -> None  # uses ADMIN_DATABASE_URL
```

Existing fixture `db_client` pattern (см. tests/test_role_based_auth.py):
- TRUNCATEs domain tables через ADMIN_DATABASE_URL
- Overrides `app.dependency_overrides[get_db]` с real engine
- Returns `(async_client, SessionLocal)` tuple

Existing fixture `bot_token`, `owner_tg_id`, `make_init_data(tg_user_id, bot_token)` from conftest.py.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Расширить tests/helpers/seed.py — seed_two_role_tenants + seed_ai_usage_log helpers</name>
  <files>tests/helpers/seed.py</files>
  <read_first>
    - tests/helpers/seed.py (current state — нужно понять стиль и не сломать существующие helpers)
    - app/db/models.py (AppUser, UserRole, AiConversation, AiMessage)
    - tests/conftest.py (двутенант fixture pattern, two_tenants reference)
  </read_first>
  <action>
    Добавить в существующий tests/helpers/seed.py два новых helper'а ПОСЛЕ функции `seed_actual_transaction` и ДО `_DEFAULT_TRUNCATE_TABLES`:

    1) `seed_two_role_tenants(session, *, owner_tg_user_id, member_tg_user_id)` — создаёт двух юзеров (owner + member) с ролями, возвращает dict `{owner_id, member_id, owner_tg_user_id, member_tg_user_id}`. Использует уже существующий `seed_user(...)`.

    Сигнатура и тело:
    ```python
    async def seed_two_role_tenants(
        session: AsyncSession,
        *,
        owner_tg_user_id: int,
        member_tg_user_id: int,
    ) -> dict[str, int]:
        """Seed an owner + a member in one session, return their PK ids.

        Used by Phase 13 admin RBAC tests where we need:
          - owner row to authenticate as caller of /api/v1/admin/*
          - member row that the caller will list / invite / revoke

        Both users have onboarded_at = NULL (Phase 14 will fill it).
        """
        owner = await seed_user(
            session,
            tg_user_id=owner_tg_user_id,
            role=UserRole.owner,
            cycle_start_day=5,
        )
        member = await seed_user(
            session,
            tg_user_id=member_tg_user_id,
            role=UserRole.member,
            cycle_start_day=5,
        )
        await session.commit()
        return {
            "owner_id": owner.id,
            "member_id": member.id,
            "owner_tg_user_id": owner_tg_user_id,
            "member_tg_user_id": member_tg_user_id,
        }
    ```

    2) `seed_ai_usage_log(session, *, user_id, model, prompt_tokens, completion_tokens, cached_tokens, total_tokens, est_cost_usd, ts=None)` — вставляет одну строку в таблицу `ai_usage_log` (создаётся в Plan 13-02 + 13-03). Helper МОЖЕТ упасть с `ProgrammingError: relation "ai_usage_log" does not exist` пока 13-02 не применён — это ожидаемо для RED phase. Используем raw SQL для не-зависимости от ORM, который тоже создаётся в 13-03.

    Сигнатура и тело:
    ```python
    async def seed_ai_usage_log(
        session: AsyncSession,
        *,
        user_id: int,
        model: str = "gpt-4o-mini",
        prompt_tokens: int = 0,
        completion_tokens: int = 0,
        cached_tokens: int = 0,
        total_tokens: int = 0,
        est_cost_usd: float = 0.0,
        ts: Optional[datetime] = None,
    ) -> None:
        """Insert a single ai_usage_log row via raw SQL (no ORM dependency).

        Phase 13 Plan 13-02 creates the ai_usage_log table; Plan 13-03 wires
        the SQLAlchemy model. This helper uses sqlalchemy.text() to stay
        decoupled from import-time model loading order during RED phase.

        ts: when None, defaults to now(UTC). Caller passes explicit ts to
        produce records inside / outside current month + 30d windows for
        the admin /ai-usage breakdown tests.
        """
        from sqlalchemy import text
        when = ts or datetime.now(timezone.utc)
        await session.execute(
            text(
                "INSERT INTO ai_usage_log "
                "(user_id, model, prompt_tokens, completion_tokens, "
                " cached_tokens, total_tokens, est_cost_usd, created_at) "
                "VALUES (:user_id, :model, :pt, :ct, :ch, :tt, :ec, :ts)"
            ),
            {
                "user_id": user_id, "model": model,
                "pt": prompt_tokens, "ct": completion_tokens,
                "ch": cached_tokens, "tt": total_tokens,
                "ec": est_cost_usd, "ts": when,
            },
        )
        await session.commit()
    ```

    Обновить `_DEFAULT_TRUNCATE_TABLES`: добавить `ai_usage_log` ПЕРЕД `app_user` (cascade-friendly order — child first):
    ```python
    _DEFAULT_TRUNCATE_TABLES = (
        "category, planned_transaction, actual_transaction, plan_template_item, "
        "subscription, budget_period, ai_message, ai_conversation, "
        "category_embedding, ai_usage_log, app_user"
    )
    ```

    NB: TRUNCATE на несуществующей таблице выбросит ошибку — но `truncate_db` уже использует `if EXISTS`-эквивалент через cascade? Нет, не использует. Чтобы не сломать существующие тесты, делаем graceful: оборачиваем в DO/EXISTS-блок.

    Решение: меняем тело `truncate_db` чтобы использовать `TRUNCATE ... IF EXISTS` НЕ доступно в стандартном SQL Postgres, но мы можем составить список существующих таблиц через `to_regclass`. Проще: оставляем _DEFAULT_TRUNCATE_TABLES без `ai_usage_log` ПОКА; добавляем переменную:
    ```python
    _DEFAULT_TRUNCATE_TABLES = (
        "category, planned_transaction, actual_transaction, plan_template_item, "
        "subscription, budget_period, ai_message, ai_conversation, "
        "category_embedding, app_user"
    )
    _PHASE13_TRUNCATE_TABLES = _DEFAULT_TRUNCATE_TABLES + ", ai_usage_log"
    ```

    А truncate_db оставляем как есть. Тесты Phase 13 при необходимости передадут explicit `tables=_PHASE13_TRUNCATE_TABLES` ИЛИ truncate_db расширим параметром `include_phase13: bool = False`. Финальное решение: добавить параметр:
    ```python
    async def truncate_db(*, tables: str = _DEFAULT_TRUNCATE_TABLES, include_phase13: bool = False) -> None:
        ...
        target_tables = (_PHASE13_TRUNCATE_TABLES if include_phase13 else tables)
        # Только если не передан explicit tables (т.е. дефолт):
        # Чтобы не сломать сигнатуру — простой подход:
    ```

    ОКОНЧАТЕЛЬНОЕ РЕШЕНИЕ для безопасности: НЕ менять существующий `_DEFAULT_TRUNCATE_TABLES` и НЕ менять сигнатуру `truncate_db`. Вместо этого:
    - Добавить отдельную константу `_PHASE13_TRUNCATE_TABLES`
    - Добавить новую функцию `truncate_db_phase13()` для Phase 13 тестов
    - `seed_ai_usage_log` остаётся как есть (insert via raw text)

    ```python
    _PHASE13_TRUNCATE_TABLES = (
        "category, planned_transaction, actual_transaction, plan_template_item, "
        "subscription, budget_period, ai_message, ai_conversation, "
        "category_embedding, ai_usage_log, app_user"
    )

    async def truncate_db_phase13() -> None:
        """Phase 13 variant: includes ai_usage_log table in truncate set.

        Used by tests/test_admin_users_api.py and tests/test_admin_ai_usage_api.py
        which seed ai_usage_log rows. Will fail with ProgrammingError until
        Plan 13-02 creates the ai_usage_log table — that is by design (RED phase).
        """
        await truncate_db(tables=_PHASE13_TRUNCATE_TABLES)
    ```

    Также добавить импорт `Optional` если ещё не импортирован — он импортирован (line 17).

    Не забыть импорт `timezone` — он импортирован: `from datetime import date, datetime, timezone`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from tests.helpers.seed import seed_two_role_tenants, seed_ai_usage_log, truncate_db_phase13, _PHASE13_TRUNCATE_TABLES; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from tests.helpers.seed import seed_two_role_tenants, seed_ai_usage_log, truncate_db_phase13"` → exit 0
    - `grep -c "seed_two_role_tenants" tests/helpers/seed.py` → ≥ 1
    - `grep -c "seed_ai_usage_log" tests/helpers/seed.py` → ≥ 1
    - `grep -c "ai_usage_log" tests/helpers/seed.py` → ≥ 2 (один в truncate-list, один-два в seed функции)
    - Существующие tests НЕ сломаны: `cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_role_based_auth.py --collect-only 2>&1 | grep -c "error"` → 0
  </acceptance_criteria>
  <done>
    seed.py содержит 2 новых helper'а и phase13 truncate-вариант; existing helpers не тронуты; commit готов.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_admin_users_api.py — RED tests for ADM-03/04/05/06 (list + invite + revoke + RBAC)</name>
  <files>tests/test_admin_users_api.py</files>
  <read_first>
    - app/api/dependencies.py (require_owner, get_current_user — Plan 12-02 контракт)
    - app/db/models.py (AppUser, UserRole, ActualTransaction)
    - tests/conftest.py (db_client pattern, make_init_data, owner_tg_id, bot_token)
    - tests/test_require_owner.py (паттерн RBAC-теста; не дублировать)
    - tests/helpers/seed.py (seed_user, seed_category, seed_budget_period, seed_actual_transaction, seed_two_role_tenants — добавлено в Task 1)
  </read_first>
  <behavior>
    Один тест-файл — 12 интеграционных тестов. Все DB-backed, skip when DATABASE_URL is unset.

    GROUP A — ADM-06 GET /api/v1/admin/users:
    - test_admin_list_users_returns_owner_and_member — seed owner + 2 members → GET /admin/users (auth as owner) → 200 + 3 строк, owner first.
    - test_admin_list_users_includes_required_fields — каждый response item содержит {id, tg_user_id, role, last_seen_at, onboarded_at, created_at, tg_chat_id}.
    - test_admin_list_users_403_for_member — auth as member → 403.

    GROUP B — ADM-04 + ADM-06 POST /api/v1/admin/users:
    - test_admin_create_user_returns_201_with_member_role — body {tg_user_id: 9_555_555_001} (≥ 10_000) → 201, role="member", role не "owner".
    - test_admin_create_user_409_on_duplicate — invite ID 9_555_555_001 дважды → 2-й request → 409 (status code).
    - test_admin_create_user_422_on_short_id — body {tg_user_id: 999} (4 digits) → 422 validation error.
    - test_admin_create_user_403_for_member — caller role=member → 403 (Depends(require_owner) blocks).

    GROUP C — ADM-05 DELETE /api/v1/admin/users/{user_id}:
    - test_admin_delete_user_204 — owner+member seeded; DELETE /admin/users/{member_id} → 204; следующий GET /admin/users → 1 row (owner only).
    - test_admin_delete_user_self_403 — owner вызывает DELETE /admin/users/{owner_id} → 403 (self-revoke forbidden).
    - test_admin_delete_user_404_unknown — DELETE /admin/users/999_999 → 404.
    - test_admin_delete_user_403_for_member — caller=member, target=другой member → 403.
    - test_admin_delete_user_cascade_purges_data — seed member с категорией+период+actual_transaction; DELETE → 204; затем raw SELECT count from each table WHERE user_id=member_id → 0 (cascade purge сработал). Также проверить что owner-данные нетронуты.

    Каждый тест:
    1. Использует db_client fixture (TRUNCATE через ADMIN_DATABASE_URL + dependency_overrides[get_db]).
    2. Seed users / data через seed_two_role_tenants + seed_category / seed_actual_transaction.
    3. HTTP request: client.METHOD("/api/v1/admin/...", headers={"X-Telegram-Init-Data": make_init_data(caller_tg_id, bot_token)}).
    4. Assert на status code + body / DB state.

    RED expectation:
    - Все GET/POST/DELETE → 404 (route не зарегистрирован в app/api/router.py пока) ИЛИ 403 (если admin_router добавлен но без endpoints — тогда require_owner отвечает раньше).
    - test_admin_delete_user_cascade_purges_data → AssertionError (row count != 0 потому что endpoint не реализован).
  </behavior>
  <action>
    Создать tests/test_admin_users_api.py со следующим содержимым (полный текст файла; вставить как есть):

    ```python
    """RED tests for Phase 13 ADM-03/04/05/06 — admin users CRUD + RBAC.

    Endpoints under test (will be created in Plan 13-04):
      GET    /api/v1/admin/users        list whitelist
      POST   /api/v1/admin/users        invite by tg_user_id (creates AppUser role=member)
      DELETE /api/v1/admin/users/{id}   revoke + cascade purge

    All endpoints protected by Depends(require_owner) → 403 for member/revoked.

    All tests are integration (real DB). Skip when DATABASE_URL is unset.

    These tests are RED until Plans 13-02 (alembic 0008), 13-03 (ai_usage_log
    persistence), and 13-04 (admin routes wiring) land. The cascade test also
    depends on Plan 13-04 service-layer purge logic (no DB ON DELETE CASCADE
    on domain tables — see Plan 11-02 D-NOTE).
    """
    from __future__ import annotations

    import os
    from datetime import date, datetime, timezone

    import pytest
    import pytest_asyncio
    from sqlalchemy import text


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set — integration test requires DB")


    @pytest_asyncio.fixture
    async def db_client(async_client):
        _require_db()
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
        from app.api.dependencies import get_db
        from app.main_api import app
        from tests.helpers.seed import _PHASE13_TRUNCATE_TABLES

        admin_url = os.environ.get("ADMIN_DATABASE_URL") or os.environ["DATABASE_URL"]
        admin_engine = create_async_engine(admin_url, echo=False)
        async with admin_engine.begin() as conn:
            # ai_usage_log might not exist yet (RED phase before Plan 13-02).
            # Truncate fallback: try with ai_usage_log first, on failure retry without.
            try:
                await conn.execute(
                    text(f"TRUNCATE TABLE {_PHASE13_TRUNCATE_TABLES} RESTART IDENTITY CASCADE")
                )
            except Exception:
                from tests.helpers.seed import _DEFAULT_TRUNCATE_TABLES
                await conn.execute(
                    text(f"TRUNCATE TABLE {_DEFAULT_TRUNCATE_TABLES} RESTART IDENTITY CASCADE")
                )
        await admin_engine.dispose()

        runtime_url = os.environ["DATABASE_URL"]
        engine = create_async_engine(runtime_url, echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

        async def real_get_db():
            async with SessionLocal() as session:
                try:
                    yield session
                    await session.commit()
                except Exception:
                    await session.rollback()
                    raise

        app.dependency_overrides[get_db] = real_get_db
        yield async_client, SessionLocal
        await engine.dispose()


    # ---------- GROUP A: GET /api/v1/admin/users (ADM-06 list) ----------

    @pytest.mark.asyncio
    async def test_admin_list_users_returns_owner_and_member(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_two_role_tenants, seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            ids = await seed_two_role_tenants(
                s, owner_tg_user_id=owner_tg_id, member_tg_user_id=9_555_555_001
            )
            await seed_user(s, tg_user_id=9_555_555_002, role=UserRole.member)
            await s.commit()

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/admin/users", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert isinstance(body, list)
        assert len(body) == 3, f"expected 3 users (owner + 2 members), got {len(body)}"
        # Owner first — ADM-03 sort contract.
        assert body[0]["role"] == "owner", f"owner must be first row, got {body[0]}"
        assert body[0]["tg_user_id"] == owner_tg_id


    @pytest.mark.asyncio
    async def test_admin_list_users_includes_required_fields(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_two_role_tenants

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_two_role_tenants(
                s, owner_tg_user_id=owner_tg_id, member_tg_user_id=9_555_555_010
            )

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/admin/users", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        for row in body:
            for fld in ("id", "tg_user_id", "role", "last_seen_at",
                        "onboarded_at", "created_at", "tg_chat_id"):
                assert fld in row, f"missing field {fld!r} in {row}"


    @pytest.mark.asyncio
    async def test_admin_list_users_403_for_member(db_client, bot_token):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=9_555_555_020, role=UserRole.member)
            await s.commit()

        init_data = make_init_data(9_555_555_020, bot_token)
        resp = await client.get(
            "/api/v1/admin/users", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 403, (
            f"member must be blocked by require_owner, got {resp.status_code}"
        )


    # ---------- GROUP B: POST /api/v1/admin/users (ADM-04 invite) ----------

    @pytest.mark.asyncio
    async def test_admin_create_user_returns_201_with_member_role(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.post(
            "/api/v1/admin/users",
            json={"tg_user_id": 9_555_555_100},
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 201, resp.text
        body = resp.json()
        assert body["tg_user_id"] == 9_555_555_100
        assert body["role"] == "member", f"new user must be member, got {body['role']}"


    @pytest.mark.asyncio
    async def test_admin_create_user_409_on_duplicate(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()

        init_data = make_init_data(owner_tg_id, bot_token)
        # First invite ok.
        r1 = await client.post(
            "/api/v1/admin/users",
            json={"tg_user_id": 9_555_555_110},
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert r1.status_code == 201, r1.text
        # Second invite same id → 409.
        r2 = await client.post(
            "/api/v1/admin/users",
            json={"tg_user_id": 9_555_555_110},
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert r2.status_code == 409, (
            f"duplicate tg_user_id must return 409, got {r2.status_code}"
        )


    @pytest.mark.asyncio
    async def test_admin_create_user_422_on_short_id(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.post(
            "/api/v1/admin/users",
            json={"tg_user_id": 999},  # 3 digits, below min
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 422, (
            f"short tg_user_id must fail validation (422), got {resp.status_code}"
        )


    @pytest.mark.asyncio
    async def test_admin_create_user_403_for_member(db_client, bot_token):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=9_555_555_200, role=UserRole.member)
            await s.commit()

        init_data = make_init_data(9_555_555_200, bot_token)
        resp = await client.post(
            "/api/v1/admin/users",
            json={"tg_user_id": 9_555_555_201},
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 403


    # ---------- GROUP C: DELETE /api/v1/admin/users/{user_id} (ADM-05 revoke) ----------

    @pytest.mark.asyncio
    async def test_admin_delete_user_204(db_client, bot_token, owner_tg_id):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_two_role_tenants

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            ids = await seed_two_role_tenants(
                s, owner_tg_user_id=owner_tg_id, member_tg_user_id=9_555_555_300
            )
        member_id = ids["member_id"]

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.delete(
            f"/api/v1/admin/users/{member_id}",
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 204, resp.text
        # Verify list now returns only owner.
        list_resp = await client.get(
            "/api/v1/admin/users", headers={"X-Telegram-Init-Data": init_data}
        )
        assert list_resp.status_code == 200
        assert len(list_resp.json()) == 1


    @pytest.mark.asyncio
    async def test_admin_delete_user_self_403(db_client, bot_token, owner_tg_id):
        """ADM-05 + CONTEXT: self-revoke owner запрещён → 403."""
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            owner = await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()
            owner_id = owner.id

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.delete(
            f"/api/v1/admin/users/{owner_id}",
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 403, (
            f"owner self-revoke must be blocked, got {resp.status_code}"
        )


    @pytest.mark.asyncio
    async def test_admin_delete_user_404_unknown(db_client, bot_token, owner_tg_id):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.delete(
            "/api/v1/admin/users/999999",
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 404


    @pytest.mark.asyncio
    async def test_admin_delete_user_403_for_member(db_client, bot_token):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            m1 = await seed_user(s, tg_user_id=9_555_555_400, role=UserRole.member)
            m2 = await seed_user(s, tg_user_id=9_555_555_401, role=UserRole.member)
            await s.commit()
            m2_id = m2.id

        init_data = make_init_data(9_555_555_400, bot_token)
        resp = await client.delete(
            f"/api/v1/admin/users/{m2_id}",
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 403


    @pytest.mark.asyncio
    async def test_admin_delete_user_cascade_purges_data(
        db_client, bot_token, owner_tg_id
    ):
        """ADM-05: revoke удаляет все связанные данные юзера, owner-данные нетронуты."""
        from tests.conftest import make_init_data
        from tests.helpers.seed import (
            seed_two_role_tenants, seed_category, seed_budget_period,
            seed_actual_transaction,
        )
        from app.db.models import CategoryKind, ActualSource

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            ids = await seed_two_role_tenants(
                s, owner_tg_user_id=owner_tg_id, member_tg_user_id=9_555_555_500
            )
            owner_id, member_id = ids["owner_id"], ids["member_id"]
            owner_cat = await seed_category(s, user_id=owner_id, name="OwnerCat")
            member_cat = await seed_category(s, user_id=member_id, name="MemberCat")
            owner_period = await seed_budget_period(
                s, user_id=owner_id,
                period_start=date(2026, 5, 5), period_end=date(2026, 6, 4),
            )
            member_period = await seed_budget_period(
                s, user_id=member_id,
                period_start=date(2026, 5, 5), period_end=date(2026, 6, 4),
            )
            await seed_actual_transaction(
                s, user_id=member_id, period_id=member_period.id,
                kind=CategoryKind.expense, amount_cents=10000,
                category_id=member_cat.id, tx_date=date(2026, 5, 7),
                source=ActualSource.mini_app,
            )
            await seed_actual_transaction(
                s, user_id=owner_id, period_id=owner_period.id,
                kind=CategoryKind.expense, amount_cents=20000,
                category_id=owner_cat.id, tx_date=date(2026, 5, 7),
                source=ActualSource.mini_app,
            )
            await s.commit()

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.delete(
            f"/api/v1/admin/users/{member_id}",
            headers={"X-Telegram-Init-Data": init_data},
        )
        assert resp.status_code == 204, resp.text

        # Verify cascade: every domain table has 0 rows for member, owner intact.
        async with SessionLocal() as s:
            for table in (
                "category", "budget_period", "actual_transaction",
                "planned_transaction", "subscription", "plan_template_item",
                "category_embedding", "ai_conversation", "ai_message",
            ):
                result = await s.execute(
                    text(f"SELECT count(*) FROM {table} WHERE user_id = :uid"),
                    {"uid": member_id},
                )
                assert result.scalar_one() == 0, (
                    f"member rows in {table!r} must be purged after revoke"
                )
            # Owner data intact.
            result = await s.execute(
                text("SELECT count(*) FROM category WHERE user_id = :uid"),
                {"uid": owner_id},
            )
            assert result.scalar_one() == 1, "owner data must remain intact"

            # AppUser row itself deleted.
            result = await s.execute(
                text("SELECT count(*) FROM app_user WHERE id = :uid"),
                {"uid": member_id},
            )
            assert result.scalar_one() == 0, "AppUser row must be deleted"
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_admin_users_api.py --collect-only 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_admin_users_api.py --collect-only` собирает ровно 12 tests, без syntax/import errors на collection стадии
    - `grep -c "/api/v1/admin/users" tests/test_admin_users_api.py` → ≥ 12 (по 1 в каждом тесте)
    - `grep -c "require_owner\|403" tests/test_admin_users_api.py` → ≥ 4 (тесты на RBAC и self-revoke)
    - При запуске на текущем коде (без 13-04): все 12 tests RED — fail by 404 (роут не зарегистрирован) либо AssertionError (cascade не сработал, list пустой)
  </acceptance_criteria>
  <done>
    Файл создан, импортируется, pytest collects 12 items, все ожидаемо RED на текущем коде; commit готов.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/test_admin_ai_usage_api.py — RED tests for AIUSE-01/02/03 (per-user breakdown + cap %)</name>
  <files>tests/test_admin_ai_usage_api.py</files>
  <read_first>
    - app/api/dependencies.py (require_owner)
    - app/api/schemas/ai.py (UsageBucket — будет переиспользован для admin breakdown)
    - tests/helpers/seed.py (seed_ai_usage_log, seed_two_role_tenants — добавлено в Task 1)
    - tests/conftest.py (db_client pattern)
  </read_first>
  <behavior>
    Один тест-файл — 5 интеграционных тестов:

    - test_admin_ai_usage_returns_per_user_breakdown — seed owner + 2 members; для каждого юзера 1+ строк в ai_usage_log; GET /admin/ai-usage → 200 + 3 строки в `users` массиве, у каждой есть user_id, current_month, last_30d, spending_cap_cents, est_cost_cents_current_month, pct_of_cap.
    - test_admin_ai_usage_403_for_member — auth as member → 403.
    - test_admin_ai_usage_current_month_excludes_old_data — seed для одного юзера 2 ai_usage_log records: один внутри current month (Europe/Moscow), один 60 дней назад. current_month aggregation должна включать ТОЛЬКО первую запись; last_30d должен включать current month запись но не 60-day-old; both buckets агрегируются корректно.
    - test_admin_ai_usage_pct_of_cap_warns_at_80_pct — seed user со spending_cap_cents=10000 (USD копейки) и ai_usage_log с est_cost_usd=0.083 (≈8300 копеек). pct_of_cap должно быть ≥ 0.80.
    - test_admin_ai_usage_sort_by_est_cost_desc — seed 2 users с разными est_cost_usd; response[0].est_cost > response[1].est_cost.

    Контракт ответа `/admin/ai-usage` (см. interfaces в context):
    ```json
    {
      "users": [
        {"user_id": int, "tg_user_id": int, "name": str|null, "role": str,
         "spending_cap_cents": int, "current_month": UsageBucket,
         "last_30d": UsageBucket, "est_cost_cents_current_month": int,
         "pct_of_cap": float},
        ...
      ],
      "generated_at": ISO datetime
    }
    ```
    `UsageBucket` идентичен существующему `app/api/schemas/ai.py::UsageBucket`.

    RED expectation: 404 на endpoint (роут не зарегистрирован пока 13-05 не отработал) → assertion на response.status_code == 200 fails.
  </behavior>
  <action>
    Создать tests/test_admin_ai_usage_api.py:

    ```python
    """RED tests for Phase 13 AIUSE-01/02/03 — admin AI usage breakdown.

    Endpoint under test (created in Plan 13-05):
      GET /api/v1/admin/ai-usage → AdminAiUsageResponse (per-user usage)

    Time windows:
      - current_month: from 1st of current month at 00:00 Europe/Moscow
      - last_30d: from now() - 30 days at any TZ (UTC ok)

    Sort: descending by est_cost_cents_current_month.
    Spending cap: app_user.spending_cap_cents (BIGINT, default 46500 in
    Plan 13-02 alembic 0008). pct_of_cap = est_cost_cents_current_month / cap_cents.

    All tests RED until Plans 13-02 (alembic 0008 spending_cap_cents +
    ai_usage_log table), 13-03 (ai_usage_log model), 13-05 (admin route).
    """
    from __future__ import annotations

    import os
    from datetime import datetime, timedelta, timezone

    import pytest
    import pytest_asyncio
    from sqlalchemy import text


    def _require_db():
        if not os.environ.get("DATABASE_URL"):
            pytest.skip("DATABASE_URL not set — integration test requires DB")


    @pytest_asyncio.fixture
    async def db_client(async_client):
        _require_db()
        from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
        from app.api.dependencies import get_db
        from app.main_api import app
        from tests.helpers.seed import _PHASE13_TRUNCATE_TABLES, _DEFAULT_TRUNCATE_TABLES

        admin_url = os.environ.get("ADMIN_DATABASE_URL") or os.environ["DATABASE_URL"]
        admin_engine = create_async_engine(admin_url, echo=False)
        async with admin_engine.begin() as conn:
            try:
                await conn.execute(
                    text(f"TRUNCATE TABLE {_PHASE13_TRUNCATE_TABLES} RESTART IDENTITY CASCADE")
                )
            except Exception:
                await conn.execute(
                    text(f"TRUNCATE TABLE {_DEFAULT_TRUNCATE_TABLES} RESTART IDENTITY CASCADE")
                )
        await admin_engine.dispose()

        engine = create_async_engine(os.environ["DATABASE_URL"], echo=False)
        SessionLocal = async_sessionmaker(engine, expire_on_commit=False)

        async def real_get_db():
            async with SessionLocal() as session:
                try:
                    yield session
                    await session.commit()
                except Exception:
                    await session.rollback()
                    raise

        app.dependency_overrides[get_db] = real_get_db
        yield async_client, SessionLocal
        await engine.dispose()


    @pytest.mark.asyncio
    async def test_admin_ai_usage_returns_per_user_breakdown(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import (
            seed_two_role_tenants, seed_user, seed_ai_usage_log,
        )
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            ids = await seed_two_role_tenants(
                s, owner_tg_user_id=owner_tg_id, member_tg_user_id=9_666_666_001
            )
            m2 = await seed_user(s, tg_user_id=9_666_666_002, role=UserRole.member)
            await s.commit()
            owner_id, member_id, m2_id = ids["owner_id"], ids["member_id"], m2.id

        async with SessionLocal() as s:
            await seed_ai_usage_log(
                s, user_id=owner_id, total_tokens=1000, est_cost_usd=0.005
            )
            await seed_ai_usage_log(
                s, user_id=member_id, total_tokens=500, est_cost_usd=0.002
            )
            await seed_ai_usage_log(
                s, user_id=m2_id, total_tokens=10, est_cost_usd=0.0001
            )

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/admin/ai-usage", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        assert "users" in body
        assert "generated_at" in body
        assert len(body["users"]) == 3, f"expected 3 users in breakdown, got {len(body['users'])}"
        for row in body["users"]:
            for fld in ("user_id", "tg_user_id", "role", "spending_cap_cents",
                        "current_month", "last_30d",
                        "est_cost_cents_current_month", "pct_of_cap"):
                assert fld in row, f"missing field {fld!r} in {row}"
            for bucket_key in ("current_month", "last_30d"):
                bucket = row[bucket_key]
                for bf in ("requests", "prompt_tokens", "completion_tokens",
                           "cached_tokens", "total_tokens", "est_cost_usd"):
                    assert bf in bucket, f"missing {bf!r} in {bucket_key} bucket"


    @pytest.mark.asyncio
    async def test_admin_ai_usage_403_for_member(db_client, bot_token):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            await seed_user(s, tg_user_id=9_666_666_010, role=UserRole.member)
            await s.commit()

        init_data = make_init_data(9_666_666_010, bot_token)
        resp = await client.get(
            "/api/v1/admin/ai-usage", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 403


    @pytest.mark.asyncio
    async def test_admin_ai_usage_current_month_excludes_old_data(
        db_client, bot_token, owner_tg_id
    ):
        """current_month bucket — только записи с начала текущего месяца Europe/Moscow."""
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user, seed_ai_usage_log
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            owner = await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()
            owner_id = owner.id

        now = datetime.now(timezone.utc)
        recent = now - timedelta(hours=1)
        sixty_days_ago = now - timedelta(days=60)
        async with SessionLocal() as s:
            await seed_ai_usage_log(
                s, user_id=owner_id, total_tokens=100,
                est_cost_usd=0.001, ts=recent,
            )
            await seed_ai_usage_log(
                s, user_id=owner_id, total_tokens=999_999,
                est_cost_usd=9.99, ts=sixty_days_ago,
            )

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/admin/ai-usage", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        owner_row = next(r for r in body["users"] if r["user_id"] == owner_id)
        # current_month: ТОЛЬКО recent (1 запрос).
        cm = owner_row["current_month"]
        assert cm["requests"] == 1, f"current_month requests={cm['requests']}, expected 1"
        assert cm["total_tokens"] == 100, (
            f"current_month total_tokens={cm['total_tokens']}, expected 100 (60-day-old not included)"
        )
        # last_30d: только recent (1 запрос — 60-day-old outside window).
        l30 = owner_row["last_30d"]
        assert l30["requests"] == 1
        assert l30["total_tokens"] == 100


    @pytest.mark.asyncio
    async def test_admin_ai_usage_pct_of_cap_warns_at_80_pct(
        db_client, bot_token, owner_tg_id
    ):
        """spending_cap_cents — Phase 13 stub default 46500 копеек USD ($5).

        Тест фиксирует cap≈10000 копеек и проверяет что 80% триггерится.
        Cap settings change: после Plan 13-02 alembic 0008 column NOT NULL DEFAULT 46500.
        Тест меняет cap явно через UPDATE для конкретной строки.
        """
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_user, seed_ai_usage_log
        from app.db.models import UserRole

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            owner = await seed_user(s, tg_user_id=owner_tg_id, role=UserRole.owner)
            await s.commit()
            owner_id = owner.id
            # Override cap to 10000 (USD копейки = $1.00).
            await s.execute(
                text("UPDATE app_user SET spending_cap_cents = :cap WHERE id = :uid"),
                {"cap": 10_000, "uid": owner_id},
            )
            await s.commit()

        async with SessionLocal() as s:
            # est_cost_usd = 0.083 → 8300 копеек USD ≈ 83% от cap 10000.
            await seed_ai_usage_log(
                s, user_id=owner_id, total_tokens=1, est_cost_usd=0.083,
            )

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/admin/ai-usage", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        row = next(r for r in body["users"] if r["user_id"] == owner_id)
        assert row["spending_cap_cents"] == 10_000
        assert row["pct_of_cap"] >= 0.80, (
            f"83% of cap → pct_of_cap should be ≥0.80, got {row['pct_of_cap']}"
        )
        assert row["pct_of_cap"] < 1.0


    @pytest.mark.asyncio
    async def test_admin_ai_usage_sort_by_est_cost_desc(
        db_client, bot_token, owner_tg_id
    ):
        from tests.conftest import make_init_data
        from tests.helpers.seed import seed_two_role_tenants, seed_ai_usage_log

        client, SessionLocal = db_client
        async with SessionLocal() as s:
            ids = await seed_two_role_tenants(
                s, owner_tg_user_id=owner_tg_id, member_tg_user_id=9_666_666_020
            )
            owner_id, member_id = ids["owner_id"], ids["member_id"]

        async with SessionLocal() as s:
            # Member spends MORE this month than owner.
            await seed_ai_usage_log(
                s, user_id=member_id, total_tokens=10_000, est_cost_usd=0.500
            )
            await seed_ai_usage_log(
                s, user_id=owner_id, total_tokens=100, est_cost_usd=0.005
            )

        init_data = make_init_data(owner_tg_id, bot_token)
        resp = await client.get(
            "/api/v1/admin/ai-usage", headers={"X-Telegram-Init-Data": init_data}
        )
        assert resp.status_code == 200, resp.text
        body = resp.json()
        users = body["users"]
        # Top spender first.
        assert users[0]["user_id"] == member_id, (
            f"member should sort first (higher est_cost), got user_id={users[0]['user_id']}"
        )
        assert users[1]["user_id"] == owner_id
    ```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_admin_ai_usage_api.py --collect-only 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_admin_ai_usage_api.py --collect-only` собирает ровно 5 tests без syntax/import errors
    - `grep -c "/api/v1/admin/ai-usage" tests/test_admin_ai_usage_api.py` → ≥ 5
    - `grep -c "spending_cap_cents\|pct_of_cap" tests/test_admin_ai_usage_api.py` → ≥ 4
    - При запуске на текущем коде ВСЕ 5 tests fail (404 от admin endpoint, либо assert на pct_of_cap/sort при отсутствии endpoint вообще)
  </acceptance_criteria>
  <done>
    Файл создан, импортируется, 5 tests собраны, все ожидаемо RED; commit готов.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture seed → DB | Tests insert AppUser rows directly via ORM; bypass HMAC; no production trust impact |
| Test → ai_usage_log table | Helper seed_ai_usage_log uses raw SQL — table may not exist yet (RED phase by design) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | S (Spoofing) | Test fixture seeds users without role → silently default to 'member' | mitigate | seed_two_role_tenants explicitly passes role=owner / role=member; assertion in test_admin_create_user_returns_201_with_member_role checks `role` field is present |
| T-13-01-02 | T (Tampering) | Stale data between tests if TRUNCATE missing → cross-test contamination | mitigate | db_client fixture TRUNCATEs Phase-13 tables CASCADE on every test; ADMIN_DATABASE_URL fallback ensures privilege |
| T-13-01-03 | I (Info Disclosure) | Cascade test asserts member rows purged but owner intact — leak detection | mitigate | test_admin_delete_user_cascade_purges_data explicitly counts owner rows and asserts ≠ 0 |
| T-13-01-04 | E (Elevation) | Member could call admin endpoints if require_owner missing | mitigate | test_admin_list_users_403_for_member, test_admin_create_user_403_for_member, test_admin_delete_user_403_for_member, test_admin_ai_usage_403_for_member each assert 403 for member caller |
| T-13-01-05 | E | Owner self-revoke would lock all admin access permanently | mitigate | test_admin_delete_user_self_403 asserts 403 when owner revokes their own user_id |

(Wave 1 — RED tests only. Production threats mitigated by Plans 13-04, 13-05.)
</threat_model>

<verification>
- pytest --collect-only собирает 17+ tests из обоих файлов без syntax errors
- helper-функции импортируются без ошибок: `python -c "from tests.helpers.seed import seed_two_role_tenants, seed_ai_usage_log"` exit 0
- Запуск pytest tests/test_admin_*_api.py на текущем коде: все 17 tests fail expected RED (либо 404 from FastAPI [route не зарегистрирован], либо ProgrammingError [ai_usage_log table не существует])
- Никакой production код не модифицирован
</verification>

<success_criteria>
- 2 файла в tests/: test_admin_users_api.py (12 tests), test_admin_ai_usage_api.py (5 tests)
- 1 модифицированный файл: tests/helpers/seed.py — добавлены seed_two_role_tenants + seed_ai_usage_log + truncate_db_phase13 + _PHASE13_TRUNCATE_TABLES
- pytest --collect-only --quiet: ≥ 17 items collected from обоих файлов
- При запуске этих файлов: 17 tests fail (expected RED) с понятными сообщениями (404, AssertionError, или ProgrammingError на ai_usage_log)
- Готовность к Plans 13-02..13-05 (которые делают tests GREEN incrementally)
</success_criteria>

<output>
After completion, create `.planning/phases/13-admin-ui-whitelist-ai-usage/13-01-SUMMARY.md`
</output>
