---
phase: 63-subscriptions-v06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift
  - ios/BudgetPlanner/Features/Management/SubscriptionsViewData.swift
autonomous: true
requirements: [SUBS-V10-01, SUBS-V10-02, SUBS-V10-03, SUBS-V10-04]
must_haves:
  truths:
    - "SubscriptionsViewModel грузит подписки через SubscriptionsV10API.list() (не legacy SubscriptionsAPI.list)"
    - "Список подписок показывает 4 load-state (loading / error / ready+empty / ready+content)"
    - "Пользователь может провести подписку (post) — создаётся транзакция, postedTxnId появляется после reload"
    - "Пользователь может отменить проведение подписки (unpost)"
    - "post/unpost защищены submitting-guard и делают full reload на успехе"
    - "На failure post/unpost/delete в banner — фиксированная RU-копия, raw error не утекает"
  artifacts:
    - path: "ios/BudgetPlanner/Features/Management/SubscriptionsView.swift"
      provides: "SubscriptionsViewModel переписан на SubscriptionsV10API + post/unpost мутации + Status state-machine"
      contains: "SubscriptionsV10API"
    - path: "ios/BudgetPlanner/Features/Management/SubscriptionsViewData.swift"
      provides: "Pure helpers: cadence/day RU formatting, monthlyLoad/activeCount, sort, validation"
      contains: "enum SubscriptionsViewData"
  key_links:
    - from: "SubscriptionsViewModel.load"
      to: "SubscriptionsV10API.list"
      via: "async let parallel fetch + CategoriesAPI.list + AccountsAPI.list"
      pattern: "SubscriptionsV10API\\.list"
    - from: "SubscriptionsViewModel.post"
      to: "SubscriptionsV10API.post"
      via: "submitting guard → POST → load()"
      pattern: "SubscriptionsV10API\\.post"
---

<objective>
Мигрировать `SubscriptionsViewModel` (в `Features/Management/SubscriptionsView.swift`) с legacy `SubscriptionsAPI` на `SubscriptionsV10API` для list/patch/post/unpost/delete, добавить денежные мутации post/unpost (с submitting-guard + reload), и вынести pure-helpers в новый `SubscriptionsViewData.swift`.

Purpose: Подписки получают v1.0-функционал (проведение списания, day_of_month, account_id) на едином V10-контракте — паритет с sibling-доменами Savings/Accounts.
Output: Переписанный ViewModel на `SubscriptionV10DTO` + post/unpost + `SubscriptionsViewData` pure-helpers. View-слой (editor extensions + swipe UI + badge) — в Plan 63-02.

**Резолюция open-вопроса create-path (CONTEXT D «create-путь»):** `SubscriptionsV10API` НЕ содержит create-эндпоинта (проверено планировщиком — файл `SubscriptionsV10API.swift` экспортирует только list/post/unpost/patch/delete). Решение: **create остаётся на legacy `SubscriptionsAPI.create(SubscriptionCreateRequest)`** (POST /subscriptions). Все остальные операции — на `SubscriptionsV10API`. day_of_month/account_id на create задаются follow-up PATCH-ом через V10API сразу после create (legacy `SubscriptionCreateRequest` не несёт этих полей). Это зафиксировано в Plan 63-02 (editor save path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-subscriptions-v06/63-CONTEXT.md

<interfaces>
<!-- Контракты, нужные исполнителю. Извлечено из кодовой базы — без повторного探索. -->

SubscriptionsV10API (ios/BudgetPlanner/Networking/Endpoints/SubscriptionsV10API.swift):
```swift
enum SubscriptionsV10API {
    static func list() async throws -> [SubscriptionV10DTO]
    static func post(id: Int) async throws -> SubscriptionPostResponseDTO
    static func unpost(id: Int) async throws            // 200 no body
    static func patch(id: Int, payload: SubscriptionV10UpdateRequest) async throws -> SubscriptionV10DTO
    static func delete(id: Int) async throws            // 204
}
```

SubscriptionV10DTO (ios/BudgetPlanner/Networking/DTO/SubscriptionV10DTO.swift):
```swift
struct SubscriptionV10DTO: Decodable, Identifiable, Equatable {
    let id: Int; let name: String; let amountCents: Int; let cycle: SubCycle
    let nextChargeDate: Date; let categoryId: Int; let notifyDaysBefore: Int; let isActive: Bool
    let dayOfMonth: Int?; let accountId: Int?; let postedTxnId: Int?   // v1.0-extension, decodeIfPresent
}
struct SubscriptionV10UpdateRequest: Encodable { // все поля Optional, encodeIfPresent
    var name, cycle, nextChargeDate: ...; var amountCents, categoryId, notifyDaysBefore, dayOfMonth, accountId: Int?; var isActive: Bool?
}
struct SubscriptionPostResponseDTO: Decodable, Equatable { let txnId: Int; let subscriptionId: Int; let postedAt: String }
```

AccountDTO (ios/BudgetPlanner/Networking/DTO/AccountDTO.swift):
```swift
struct AccountDTO: Decodable, Identifiable, Equatable {
    let id: Int; let bank: String; let mask: String?; let kind: AccountKind
    let balanceCents: Int; let primary: Bool; let createdAt: Date?
}
```

Legacy create (ios/BudgetPlanner/Networking/Endpoints/ManagementAPI.swift) — для create-path ТОЛЬКО:
```swift
enum SubscriptionsAPI { static func create(_ r: SubscriptionCreateRequest) async throws -> SubscriptionDTO }
struct SubscriptionCreateRequest: Encodable { let name: String; let amountCents: Int; let cycle: String; let nextChargeDate: String; let categoryId: Int; let notifyDaysBefore: Int }
```

DateFormatters.isoDate — Europe/Moscow, yyyy-MM-dd (используется в Plan 63-02 editor save).

Sibling-эталон мутаций — SavingsViewModel (ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift):
Status enum {idle,loading,ready,error(String)}; inFlight guard в load(); submitting guard + defer в мутациях; print() raw error + filtered RU mutationError; full reload на успехе; #if DEBUG _setStateForTesting backdoor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать SubscriptionsViewData (pure helpers)</name>
  <files>ios/BudgetPlanner/Features/Management/SubscriptionsViewData.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Savings/SavingsViewData.swift (sibling pure-helper convention: enum namespace, Foundation-only, static func)
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsV10ViewModel.swift (computeActiveCount/computeMonthlyTotal семантика — не трогать V10, только перенять формулы)
    - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift (текущие monthlyLoadCents/activeCount формулы строки 36-44)
  </read_first>
  <behavior>
    - computeActiveCount([SubscriptionV10DTO]) → число isActive
    - computeMonthlyLoadCents([SubscriptionV10DTO]) → сумма по активным: monthly = amountCents, yearly = amountCents/12 (integer, как в текущем VM)
    - sortForDisplay([SubscriptionV10DTO]) → сортировка по nextChargeDate asc (как текущий load sort)
    - formatCadenceRu(cycle:dayOfMonth:) → "ежемесячно, <N> числа" для monthly+dayOfMonth, "ежемесячно" для monthly без дня, "ежегодно" для yearly
    - isPosted(SubscriptionV10DTO) → postedTxnId != nil
    - isValidDraft(name:amountCents:categoryId:submitting:) → name≠empty && amountCents>0 && categoryId≠nil && !submitting
  </behavior>
  <action>
    Создать `enum SubscriptionsViewData` (Foundation-only, без SwiftUI) с pure static-функциями выше. ВАЖНО: имя файла `SubscriptionsViewData.swift` — НЕ `SubscriptionsData.swift` (последнее занято FeaturesV10/Subscriptions; Xcode/Swift запрещает дублирование basename файлов в одном таргете — урок Phase 62-01 SavingsViewData rename). Деньги — Int cents, без float. day_of_month — ordinal 1..28, без timezone. Перенять формулы из текущего SubscriptionsViewModel (monthlyLoadCents строки 40-44) и V10 SubscriptionsData (семантика monthly/yearly), но реализовать заново под SubscriptionV10DTO.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Файл `SubscriptionsViewData.swift` существует с `enum SubscriptionsViewData`, 6 static-функций выше
    - Foundation-only (нет import SwiftUI)
    - Билд зелёный, нет basename-коллизии с FeaturesV10/Subscriptions/SubscriptionsData.swift
  </acceptance_criteria>
  <done>Pure-helpers компилируются, доступны из ViewModel и тестов.</done>
</task>

<task type="auto">
  <name>Task 2: Переписать SubscriptionsViewModel на SubscriptionsV10API + post/unpost</name>
  <files>ios/BudgetPlanner/Features/Management/SubscriptionsView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift (эталон: Status state-machine, inFlight guard, submitting+defer, print()+filtered RU, full reload, _setStateForTesting)
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsV10ViewModel.swift (семантика post/unpost/patch — НЕ модифицировать, только эталон)
    - ios/BudgetPlanner/Features/Management/SubscriptionsView.swift (текущий VM строки 1-45 — цель перезаписи; View body 52-189 НЕ трогать в этом плане, кроме типа DTO/derived getters)
  </read_first>
  <action>
    Переписать `SubscriptionsViewModel` (только класс, верх файла):
    - State: `enum Status {idle,loading,ready,error(String)}`; `private(set) var subscriptions: [SubscriptionV10DTO]`; `private(set) var categories: [CategoryDTO]`; `private(set) var accounts: [AccountDTO]`; `private(set) var status`; `private(set) var submitting`; `var mutationError: String?`; `@ObservationIgnored private var inFlight`.
    - `load()`: inFlight guard; `async let` parallel `SubscriptionsV10API.list()` + `CategoriesAPI.list()` + `AccountsAPI.list()`; categories.filter !isArchived; subscriptions через `SubscriptionsViewData.sortForDisplay`; `await LocalNotifications.reschedule(...)` (адаптировать под V10DTO — см. ниже); status=.ready; catch → print() + status=.error("Не удалось загрузить подписки").
    - `post(_ sub:)` async @discardableResult Bool: submitting guard + defer; `try await SubscriptionsV10API.post(id:)`; на успехе mutationError=nil + `await load()` + return true; catch → print() + mutationError="Не удалось провести подписку" + return false.
    - `unpost(_ sub:)`: тот же паттерн → `SubscriptionsV10API.unpost(id:)`; mutationError="Не удалось отменить проведение".
    - `delete(_ sub:)`: submitting guard → `SubscriptionsV10API.delete(id:)` → load(); mutationError="Не удалось удалить подписку".
    - `patch(_ sub:, payload:)` async @discardableResult Bool: submitting guard → `SubscriptionsV10API.patch(id:payload:)` → load(); mutationError="Не удалось сохранить подписку". (Используется editor edit-path в 63-02.)
    - Derived getters через SubscriptionsViewData: `activeCount`, `monthlyLoadCents`.
    - `#if DEBUG _setStateForTesting(subscriptions:categories:accounts:status:)` backdoor (как Savings).
    - `clearMutationError()`.
    LocalNotifications.reschedule: текущая сигнатура принимает `[SubscriptionDTO]`. Поскольку VM теперь держит `[SubscriptionV10DTO]`, НЕ менять LocalNotifications API в этом плане — если сигнатура несовместима, обернуть вызов в guard/маппинг минимальных полей (id,name,nextChargeDate,notifyDaysBefore,isActive) ИЛИ временно убрать reschedule-вызов с TODO-комментом и зафиксировать в SUMMARY как known-gap (notification rescheduling не регрессирует функционал подписок). Прочитать сигнатуру LocalNotifications.reschedule перед решением; не модифицировать LocalNotifications.swift.
    View body (SubscriptionsView struct, SubscriptionRow, editor) обновить МИНИМАЛЬНО только чтобы компилировалось с SubscriptionV10DTO (sub.category?.name больше нет в V10DTO — использовать categoryId + lookup в categories, или Tokens.Categories.visual(for: name из categories.first{id==categoryId}?.name ?? "")). Полная переработка row/editor — в Plan 63-02.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SubscriptionsV10API" ios/BudgetPlanner/Features/Management/SubscriptionsView.swift` ≥ 4 (list, post, unpost, delete/patch)
    - `SubscriptionsAPI.list` НЕ используется в VM (`grep -v '^//' SubscriptionsView.swift | grep -c "SubscriptionsAPI.list"` == 0)
    - 0 вхождений `error.localizedDescription` в VM-методах (T-63 mitigation): `grep -v '^//' SubscriptionsView.swift | grep -c "localizedDescription"` относится только к view, не к VM catch
    - Билд зелёный (iPhone 17 Pro)
  </acceptance_criteria>
  <done>VM грузит через V10API, post/unpost/delete/patch работают с submitting guard + reload + filtered RU error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app → API (/subscriptions) | UI-инициированные мутации (post/unpost/patch/delete) пересекают сеть; TG initData / RLS на бэке |
| User tap → money mutation | post создаёт реальную транзакцию (списание) — нужен confirmation + double-submit guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering/DoS | SubscriptionsViewModel.post double-tap | mitigate | `submitting` guard + defer на post/unpost/patch/delete; повторный вызов — no-op пока in-flight |
| T-63-02 | Information Disclosure | catch-блоки VM мутаций | mitigate | raw error → ТОЛЬКО print(); UI banner читает фиксированную RU-копию ("Не удалось провести подписку" и т.д.); 0 error.localizedDescription в VM |
| T-63-03 | Information Disclosure | cross-tenant subscription | accept | RLS-protected на бэке (router Depends(get_current_user)); 404 collapse — клиент не различает missing vs cross-tenant |
| T-63-04 | Stale-state | UI после post/unpost | mitigate | full `await load()` после успешной мутации — postedTxnId/badge синхронизируются с сервером |
</threat_model>

<verification>
- `cd ios && xcodegen generate && xcodebuild build -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` зелёный
- VM использует SubscriptionsV10API.list/post/unpost/patch/delete; create-path (legacy) НЕ затронут (он во View/editor, 63-02)
- SubscriptionsViewData.swift — Foundation-only pure-helpers, нет basename-коллизии
- Нет модификаций FeaturesV10/Subscriptions/*
</verification>

<success_criteria>
- SubscriptionsViewModel на V10API; post/unpost мутации с submitting guard + reload + filtered RU error
- SubscriptionsViewData с 6 pure-helpers
- Билд зелёный
</success_criteria>

<output>
After completion, create `.planning/phases/63-subscriptions-v06/63-01-SUMMARY.md`
</output>
