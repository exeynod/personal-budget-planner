---
phase: 04-actual-transactions-and-bot-commands
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_actual_crud.py
  - tests/test_actual_period.py
  - tests/test_balance.py
  - tests/test_internal_bot.py
  - tests/test_bot_parsers.py
  - tests/test_bot_handlers_phase4.py
autonomous: true
requirements:
  - ACT-01
  - ACT-02
  - ACT-03
  - ACT-04
  - ACT-05

must_haves:
  truths:
    - "Тесты Phase 4 существуют в RED-состоянии (упадут с ImportError на app.services.actual / app.services.internal_bot / app.bot.parsers / app.bot.commands)"
    - "test_actual_crud.py покрывает CRUD /actual + filter (kind/category) + auth 403 + Pydantic 422 + archived-cat 400 + kind-mismatch 400 + source='mini_app' override"
    - "test_actual_period.py покрывает ACT-02 (POST вычисляет period_id), ACT-05 (PATCH tx_date пересчитывает period_id), автосоздание исторического периода (D-52), future-date guard (D-58)"
    - "test_balance.py покрывает GET /actual/balance (per-category агрегация + total + balance_now + D-02 sign rule); empty period; period-not-found 404"
    - "test_internal_bot.py покрывает POST /internal/bot/actual (created/ambiguous/not_found), POST /internal/bot/balance, POST /internal/bot/today; X-Internal-Token обязателен; category_query OR category_id (model_validator)"
    - "test_bot_parsers.py — unit-тесты parse_amount (форматы 1500/.50/_/р/₽, edge: 0/-/abc/overflow/3-decimals) + parse_add_command (split args)"
    - "test_bot_handlers_phase4.py — мокаем httpx api_client; cmd_add/income/balance/today/app + disambiguation callback flow"
    - "Все DB-backed тесты используют _require_db self-skip pattern из tests/test_categories.py"
  artifacts:
    - path: "tests/test_actual_crud.py"
      provides: "Integration-тесты actual CRUD + filters + validation guards"
    - path: "tests/test_actual_period.py"
      provides: "Integration-тесты ACT-02/ACT-05 period resolution + future-date guard"
    - path: "tests/test_balance.py"
      provides: "Integration-тесты compute_balance aggregation + sign rule"
    - path: "tests/test_internal_bot.py"
      provides: "Integration-тесты bot internal endpoints (3 шт)"
    - path: "tests/test_bot_parsers.py"
      provides: "Unit-тесты parse_amount + parse_add_command"
    - path: "tests/test_bot_handlers_phase4.py"
      provides: "Unit/integration-тесты bot command handlers + disambiguation"
  key_links:
    - from: "tests/test_actual_*.py, tests/test_balance.py, tests/test_internal_bot.py"
      to: "app.services.actual, app.services.internal_bot, app.api.routes.actual, app.api.routes.internal_bot, app.api.schemas.actual, app.api.schemas.internal_bot"
      via: "import (will fail until later plans)"
      pattern: "from app\\."
    - from: "tests/test_bot_parsers.py, tests/test_bot_handlers_phase4.py"
      to: "app.bot.parsers, app.bot.commands, app.bot.disambiguation, app.bot.api_client"
      via: "import (will fail until later plans)"
      pattern: "from app\\.bot"

user_setup: []
---

<objective>
Wave 0: создать 6 тестовых файлов Phase 4 в RED-состоянии. Тесты пишутся против контрактов из 04-CONTEXT.md, 04-RESEARCH.md и 04-VALIDATION.md (модули `app.services.actual`, `app.services.internal_bot`, `app.api.routes.actual`, `app.api.routes.internal_bot`, `app.api.schemas.actual`, `app.api.schemas.internal_bot`, `app.bot.parsers`, `app.bot.commands`, `app.bot.disambiguation` ещё не существуют — импорт падает с ModuleNotFoundError, это ожидаемое RED). Гарантирует Nyquist compliance: каждое требование Phase 4 имеет автоматизированный тест ДО имплементации.

Purpose: Nyquist compliance — каждое требование (ACT-01..05) и ключевая ветка (disambiguation, period auto-resolve, source override) покрыты тестом до того, как код написан.
Output: 6 тестовых файлов в `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-CONTEXT.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-RESEARCH.md
@.planning/phases/04-actual-transactions-and-bot-commands/04-VALIDATION.md
@tests/test_categories.py
@tests/test_planned.py
@tests/conftest.py
@tests/test_bot_handlers.py
@app/db/models.py
@app/core/period.py
@docs/HLD.md

<interfaces>
<!-- Контракты, которые тесты предполагают (будут реализованы в Plan 04-02..04-04). -->

Pydantic schemas (Plan 04-02):
```python
# app.api.schemas.actual
KindStr = Literal["expense", "income"]
ActualSourceStr = Literal["mini_app", "bot"]

class ActualCreate: { kind: KindStr, amount_cents: int (gt=0), description: Optional[str] (max_len=500), category_id: int (gt=0), tx_date: date }
class ActualUpdate: same fields, all Optional
class ActualRead: { id, period_id, kind, amount_cents, description, category_id, tx_date, source: ActualSourceStr, created_at: datetime } from_attributes=True
class BalanceCategoryRow: { category_id, name, kind, planned_cents, actual_cents, delta_cents }
class BalanceResponse: { period_id, period_start, period_end, starting_balance_cents, planned_total_expense_cents, actual_total_expense_cents, planned_total_income_cents, actual_total_income_cents, balance_now_cents, delta_total_cents, by_category: list[BalanceCategoryRow] }

# app.api.schemas.internal_bot
class BotActualRequest: { tg_user_id: int, kind: KindStr, amount_cents: int (gt=0), description: Optional[str], tx_date: Optional[date], category_query: Optional[str] (max_len=200), category_id: Optional[int] (gt=0) } + model_validator(после: либо category_query либо category_id)
class CategoryCandidate: { id, name, kind: KindStr }
class BotActualResponse: { status: Literal["created","ambiguous","not_found"], actual: Optional[ActualRead], category: Optional[CategoryCandidate], category_balance_cents: Optional[int], candidates: Optional[list[CategoryCandidate]] }
class BotBalanceRequest: { tg_user_id: int }
class BotBalanceResponse: { period_id, period_start, period_end, balance_now_cents, delta_total_cents, planned_total_expense_cents, actual_total_expense_cents, planned_total_income_cents, actual_total_income_cents, by_category: list[BalanceCategoryRow] }
class BotTodayActualRow: { id, kind: KindStr, amount_cents, description: Optional[str], category_id, category_name }
class BotTodayRequest: { tg_user_id: int }
class BotTodayResponse: { actuals: list[BotTodayActualRow], total_expense_cents, total_income_cents }
```

REST contracts (Plan 04-03):
- All public routes под `Depends(get_current_user)` (router-level). Без X-Telegram-Init-Data → 403.
- All internal routes монтируются под `internal_router` (наследуют `Depends(verify_internal_token)`). Без X-Internal-Token → 403.

Public actual routes:
- `GET /api/v1/periods/{period_id}/actual?kind=&category_id=` → 200 list[ActualRead]
- `POST /api/v1/actual` body=ActualCreate → 200 ActualRead (source='mini_app' overridden) | 400 (archived/kind-mismatch/future-date) | 404 (CategoryNotFound) | 422 (Pydantic)
- `PATCH /api/v1/actual/{id}` body=ActualUpdate → 200 ActualRead (period_id пересчитан если tx_date в патче и изменился) | 404 | 400 | 422
- `DELETE /api/v1/actual/{id}` → 200 ActualRead | 404
- `GET /api/v1/actual/balance` → 200 BalanceResponse (для активного периода) | 404 (нет активного периода)

Internal bot routes:
- `POST /api/v1/internal/bot/actual` body=BotActualRequest → 200 BotActualResponse
- `POST /api/v1/internal/bot/balance` body=BotBalanceRequest → 200 BotBalanceResponse | 404
- `POST /api/v1/internal/bot/today` body=BotTodayRequest → 200 BotTodayResponse

Domain exceptions (Plan 04-02):
```python
# app.services.actual (новые)
class ActualNotFoundError(Exception): { actual_id: int }
class FutureDateError(Exception): { tx_date: date, max_date: date }

# Re-используем из app.services.planned:
# InvalidCategoryError, KindMismatchError, PeriodNotFoundError
# Из app.services.categories: CategoryNotFoundError
```

Bot module contracts (Plan 04-04):
```python
# app.bot.parsers
def parse_amount(s: str) -> int | None  # копейки или None
def parse_add_command(args: str | None) -> tuple[int, str, str | None] | None  # (amount_cents, category_query, description_or_None)

# app.bot.disambiguation
@dataclass
class PendingActual:
    chat_id: int
    kind: str  # 'expense' | 'income'
    amount_cents: int
    description: Optional[str]
    tx_date: Optional[str]  # ISO date or None
    candidates: list[dict]
    created_at: datetime  # default factory now
    @property
    def is_expired(self) -> bool  # > 5 minutes
TTL: timedelta = timedelta(minutes=5)
def store_pending(p: PendingActual) -> str  # returns 8-hex token
def pop_pending(token: str) -> Optional[PendingActual]  # remove + return or None if expired/missing

# app.bot.api_client (расширение)
async def bot_create_actual(*, tg_user_id, kind, amount_cents, category_query=None, category_id=None, description=None, tx_date=None) -> dict  # returns BotActualResponse JSON
async def bot_get_balance(tg_user_id: int) -> dict  # returns BotBalanceResponse JSON
async def bot_get_today(tg_user_id: int) -> dict  # returns BotTodayResponse JSON
# All raise InternalApiError on 4xx/5xx/network failures.

# app.bot.commands
router: aiogram.Router
@router.message(Command("add")) async def cmd_add(message, command): ...
@router.message(Command("income")) async def cmd_income(message, command): ...
@router.message(Command("balance")) async def cmd_balance(message): ...
@router.message(Command("today")) async def cmd_today(message): ...
@router.message(Command("app")) async def cmd_app(message): ...
@router.callback_query(F.data.startswith("act:")) async def cb_disambiguation(callback): ...
```

Pre-existing infrastructure to use:
- `tests/conftest.py::async_client` — generic FastAPI test client с DEV_MODE=false и stub get_db override.
- `tests/conftest.py::make_init_data(tg_user_id, bot_token)` — helper для X-Telegram-Init-Data header.
- `tests/conftest.py::bot_token, owner_tg_id, internal_token` fixtures.
- `tests/test_planned.py::db_setup, db_client, seed_categories, seed_period, seed_archived_category` — образцы для копирования fixtures (можно копировать, либо сделать общий conftest — discretion).
- `tests/test_bot_handlers.py` — образец для bot handler test (см. Plan 02-05 SUMMARY); test_main_bot_entry.py — entry-point smoke test.

Direct DB-seed pattern (используется в tests/test_planned.py):
```python
from sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine
from app.db.models import Category, BudgetPeriod, ActualTransaction, PlannedTransaction, ActualSource, PlanSource, CategoryKind, PeriodStatus

# В тесте — напрямую insert через SessionLocal вне HTTP, для setup (категории, период, plan, actual).
```

Pre-existing app code to test against:
- `app/db/models.py` — `ActualTransaction`, `ActualSource`, `BudgetPeriod`, `Category`, `PlannedTransaction`, `PlanSource`, `CategoryKind`, `PeriodStatus`.
- `app/core/period.py::period_for(date, cycle_start_day)` — pure function, used in fixture seed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/test_actual_crud.py, tests/test_actual_period.py, tests/test_balance.py</name>
  <files>tests/test_actual_crud.py, tests/test_actual_period.py, tests/test_balance.py</files>
  <action>
Создать 3 DB-backed тестовых файла. Использовать `tests/test_planned.py:1-200` как референс (header docstring, `_require_db()`, `auth_headers` fixture, `db_setup`/`db_client` fixture с TRUNCATE и dependency_overrides на real Postgres engine).

**Общий паттерн fixtures (повторить из test_planned.py):**
```python
def _require_db():
    if not os.environ.get("DATABASE_URL"):
        pytest.skip("DATABASE_URL not set — skipping DB-backed test")

@pytest.fixture
def auth_headers(bot_token, owner_tg_id):
    from tests.conftest import make_init_data
    return {"X-Telegram-Init-Data": make_init_data(owner_tg_id, bot_token)}

@pytest_asyncio.fixture
async def db_setup(async_client):
    """async_client + real DB session via dependency_overrides; TRUNCATE before yield. Returns (client, SessionLocal)."""
    _require_db()
    # ... copy from test_planned.py:42-78 (TRUNCATE list уже включает actual_transaction)

@pytest_asyncio.fixture
async def db_client(db_setup):
    client, _ = db_setup
    return client

@pytest_asyncio.fixture
async def seed_categories(db_setup):
    """Seed expense_cat (Продукты) + income_cat (Зарплата) + extra expense_cat_2 (Кафе) для filter тестов."""
    # ... copy & extend test_planned.py:88-110

@pytest_asyncio.fixture
async def seed_archived_category(db_setup):
    """Single archived expense category."""
    # ... copy from test_planned.py:113-128

@pytest_asyncio.fixture
async def seed_active_period(db_setup):
    """Active BudgetPeriod that COVERS today (so default tx_date=today resolves to it).
    Использует period_for(today_msk, cycle_start_day=5) для границ."""
    from datetime import datetime
    from zoneinfo import ZoneInfo
    from app.core.period import period_for
    today = datetime.now(ZoneInfo("Europe/Moscow")).date()
    p_start, p_end = period_for(today, 5)
    # ... insert BudgetPeriod with starting_balance_cents=10_000_00 (100 руб) for balance tests
```

---

**tests/test_actual_crud.py** (CRUD /actual + filters + validation guards):

Header docstring объясняет RED-state и зависимость от `app.services.actual` / `app.api.routes.actual` / `app.api.schemas.actual`.

Тесты:
1. `test_list_actual_empty(db_client, auth_headers, seed_active_period)` — `GET /api/v1/periods/{period_id}/actual` → 200, `[]`.
2. `test_create_actual_basic(db_client, auth_headers, seed_categories, seed_active_period)` — POST `/api/v1/actual` body `{kind:'expense', amount_cents:150000, description:'тест', category_id:<expense_cat>, tx_date:<today>}` → 200, response содержит все поля; `source == 'mini_app'`; `period_id == seed_active_period`.
3. `test_create_actual_no_description(db_client, auth_headers, seed_categories, seed_active_period)` — POST без description → 200, `description: null`.
4. `test_create_actual_invalid_category_404(db_client, auth_headers, seed_active_period)` — POST с `category_id=99999` → 404 (CategoryNotFoundError).
5. `test_create_actual_archived_category_400(db_client, auth_headers, seed_archived_category, seed_active_period)` — POST с archived category_id → 400.
6. `test_create_actual_kind_mismatch_400(db_client, auth_headers, seed_categories, seed_active_period)` — POST `{kind:'income', category_id:<expense_cat>}` → 400 (KindMismatchError).
7. `test_create_amount_zero_422(db_client, auth_headers, seed_categories, seed_active_period)` — `amount_cents:0` → 422.
8. `test_create_amount_negative_422(db_client, auth_headers, seed_categories, seed_active_period)` — `amount_cents:-100` → 422.
9. `test_create_description_too_long_422(db_client, auth_headers, seed_categories, seed_active_period)` — description=`'x'*501` → 422 (Field max_length=500).
10. `test_create_source_always_mini_app(db_client, auth_headers, seed_categories, seed_active_period)` — POST с попыткой передать `"source":"bot"` в body → schema игнорирует (поле не в `ActualCreate`); response `source == 'mini_app'`.
11. `test_list_filter_by_kind(db_client, auth_headers, seed_categories, seed_active_period)` — создать 1 expense + 1 income actual → `GET ?kind=expense` возвращает 1, `?kind=income` возвращает 1.
12. `test_list_filter_by_category(db_client, auth_headers, seed_categories, seed_active_period)` — 2 trans для cat A + 1 для cat B → `GET ?category_id=A` возвращает 2.
13. `test_update_actual_amount(db_client, auth_headers, seed_categories, seed_active_period)` — POST → PATCH `{amount_cents:200000}` → 200, проверить через GET.
14. `test_update_actual_description(db_client, auth_headers, seed_categories, seed_active_period)` — PATCH `{description:'обновлено'}` → 200.
15. `test_update_actual_not_found_404(db_client, auth_headers)` — PATCH `/actual/99999` → 404.
16. `test_update_actual_kind_mismatch_400(db_client, auth_headers, seed_categories, seed_active_period)` — POST expense → PATCH `{category_id:<income_cat>}` → 400.
17. `test_delete_actual(db_client, auth_headers, seed_categories, seed_active_period)` — POST → DELETE → 200 → GET возвращает empty.
18. `test_delete_actual_not_found_404(db_client, auth_headers)` — DELETE `/actual/99999` → 404.
19. `test_no_init_data_403_get(db_client, seed_active_period)` — GET без header → 403.
20. `test_no_init_data_403_post(db_client, seed_categories, seed_active_period)` — POST без header → 403.
21. `test_response_includes_all_fields(db_client, auth_headers, seed_categories, seed_active_period)` — POST с полным body, response содержит keys: `id, period_id, kind, amount_cents, description, category_id, tx_date, source, created_at`.

---

**tests/test_actual_period.py** (ACT-02 + ACT-05 + auto-create + future-date):

Header docstring объясняет ACT-02 (period_id auto-computed), ACT-05 (PATCH tx_date пересчитывает), D-52 (auto-create historic period), D-58 (future date guard).

Дополнительные fixtures:
- `seed_user_with_cycle_start_day(db_setup, *, cycle_start_day=5)` — создать AppUser row (для settings.cycle_start_day). Через прямой insert.

Тесты:
1. `test_create_resolves_period_active(db_client, auth_headers, seed_categories, seed_active_period, seed_user_with_cycle_start_day)` — POST с tx_date=today (внутри active period) → period_id == seed_active_period.id.
2. `test_create_resolves_period_specific_date(db_client, auth_headers, seed_categories, seed_user_with_cycle_start_day, db_setup)` — Setup: создать BudgetPeriod 2026-01-05..2026-02-04 (closed). POST с tx_date='2026-01-15' → period_id == этого периода.
3. `test_create_auto_creates_historic_period(db_client, auth_headers, seed_categories, seed_user_with_cycle_start_day, db_setup, seed_active_period)` — Setup: только active period (текущий месяц). POST с tx_date='2024-08-15' (давно в прошлом, нет такого периода) → 200, response.period_id != seed_active_period; в БД появилась новая BudgetPeriod (status='closed', period_start=2024-08-05 если cycle_start_day=5).
4. `test_create_future_date_400(db_client, auth_headers, seed_categories, seed_active_period, seed_user_with_cycle_start_day)` — POST с tx_date=today+30 days → 400 (FutureDateError).
5. `test_create_future_date_within_7d_ok(db_client, auth_headers, seed_categories, seed_active_period, seed_user_with_cycle_start_day)` — POST с tx_date=today+5 days → 200 (within tolerance).
6. `test_patch_recomputes_period_id(db_client, auth_headers, seed_categories, seed_active_period, seed_user_with_cycle_start_day, db_setup)` — Setup: ещё один период (предыдущий месяц). POST актуала с tx_date=today (попадает в seed_active_period). PATCH `{tx_date:'<дата_в_предыдущем_периоде>'}` → 200, response.period_id == previous_period.id (≠ seed_active_period.id).
7. `test_patch_same_tx_date_no_recompute(db_client, auth_headers, seed_categories, seed_active_period, seed_user_with_cycle_start_day)` — POST → PATCH с тем же tx_date в патче → period_id не изменился.
8. `test_patch_no_tx_date_in_body_no_recompute(db_client, auth_headers, seed_categories, seed_active_period, seed_user_with_cycle_start_day)` — POST → PATCH `{amount_cents: 9999}` (без tx_date) → period_id не изменился.

---

**tests/test_balance.py** (compute_balance aggregation + sign rule):

Header docstring объясняет `GET /actual/balance` контракт, D-02 sign rule (expense=plan-act, income=act-plan), включение subscription_auto в planned.

Дополнительный fixture:
- `seed_plan_and_actual(db_setup, seed_categories, seed_active_period)` — создаёт через DB:
  - 1 planned expense (Продукты, 1_000_000 копеек)
  - 1 planned income (Зарплата, 12_000_000 копеек)
  - 1 actual expense (Продукты, 150_000 копеек, today)
  - 1 actual income (Зарплата, 5_000_000 копеек, today)

Тесты:
1. `test_balance_empty_period(db_client, auth_headers, seed_active_period)` — `GET /api/v1/actual/balance` → 200; все totals == 0; balance_now_cents == starting_balance_cents (10_000_00 если такой starting); delta_total_cents == 0; by_category == [].
2. `test_balance_with_plan_and_actual(db_client, auth_headers, seed_categories, seed_active_period, seed_plan_and_actual)` → 200; verify: planned_total_expense_cents=1_000_000, actual_total_expense_cents=150_000, planned_total_income_cents=12_000_000, actual_total_income_cents=5_000_000; balance_now_cents = starting + actual_income - actual_expense; delta_total_cents = (1_000_000-150_000) + (5_000_000-12_000_000) = 850_000 + (-7_000_000) = -6_150_000 (отрицательная — нехорошо).
3. `test_balance_by_category_expense_sign(db_client, auth_headers, seed_categories, seed_active_period, seed_plan_and_actual)` → 200; в by_category найти Продукты: planned_cents=1_000_000, actual_cents=150_000, delta_cents=850_000 (план-факт, положительная — хорошо).
4. `test_balance_by_category_income_sign(db_client, auth_headers, seed_categories, seed_active_period, seed_plan_and_actual)` → 200; в by_category найти Зарплата: planned_cents=12_000_000, actual_cents=5_000_000, delta_cents=-7_000_000 (факт-план для income, отрицательная — нехорошо).
5. `test_balance_subscription_auto_included_in_planned(db_client, auth_headers, seed_categories, seed_active_period, db_setup)` — Setup: создать Subscription + 1 planned с source=subscription_auto (200_000 копеек, Продукты). GET balance → planned_total_expense_cents включает 200_000.
6. `test_balance_period_not_found_404(db_client, auth_headers, db_setup)` — нет активного периода (TRUNCATE budget_period before test) → 404.
7. `test_balance_excludes_archived_categories(db_client, auth_headers, seed_active_period, db_setup, seed_archived_category)` — Setup: создать planned + actual с archived category. GET balance → archived категория не появляется в by_category, но её SUM попадает в totals (или не попадает — спецификация: если категория archived, мы её не показываем НО суммы остаются для honesty? **Решение для теста:** suммы не показываются в by_category, но включаются в totals — потому что план уже создан. Тест проверяет, что category_id архивной не появляется в by_category items).
8. `test_no_init_data_403(db_client)` — GET без header → 403.

  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_actual_crud.py tests/test_actual_period.py tests/test_balance.py -x --collect-only 2>&1 | tail -30</automated>
  </verify>
  <done>
- Файлы tests/test_actual_crud.py (~21 тестов), tests/test_actual_period.py (~8 тестов), tests/test_balance.py (~8 тестов) созданы.
- pytest собирает тесты (collect-only), но они в RED state (либо ImportError на app.services.actual, либо тесты падают при run, потому что endpoints не существуют — оба = RED).
- Все DB-backed тесты используют `_require_db` self-skip pattern.
- Описаны все ветки: success path, validation 422, archived/kind 400, source override, period auto-resolve, ACT-05 cross-period move.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_internal_bot.py, tests/test_bot_parsers.py, tests/test_bot_handlers_phase4.py</name>
  <files>tests/test_internal_bot.py, tests/test_bot_parsers.py, tests/test_bot_handlers_phase4.py</files>
  <action>
Создать 3 файла: один DB-integration (internal_bot), один pure-unit (parsers), один с моками (bot handlers).

---

**tests/test_internal_bot.py** (POST /api/v1/internal/bot/*):

Header docstring объясняет contract: POST endpoints под `verify_internal_token` (parent dep); X-Internal-Token обязателен.

Fixtures:
- Re-use `db_setup` / `db_client` / `seed_categories` / `seed_active_period` / `seed_archived_category` pattern (copy из test_planned.py / test_actual_crud.py).
- Новый fixture `internal_headers(internal_token)` → `{"X-Internal-Token": internal_token}`.
- Дополнительный fixture `seed_categories_with_ambiguity(db_setup)` — создаёт `Транспорт` + `Транспортная карта` (обе expense, обе active) для disambiguation теста.

Тесты:

`POST /api/v1/internal/bot/actual`:
1. `test_internal_actual_no_token_403(db_client, seed_categories, seed_active_period, owner_tg_id)` — POST без X-Internal-Token → 403.
2. `test_internal_actual_wrong_token_403(db_client, seed_categories, seed_active_period, owner_tg_id)` — POST с `X-Internal-Token: wrong` → 403.
3. `test_internal_actual_create_via_query(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — POST `{tg_user_id, kind:'expense', amount_cents:150000, category_query:'продукт', description:'pyaterochka'}` → 200, status='created', actual.source=='bot', actual.amount_cents=150000, category.id matches.
4. `test_internal_actual_create_via_id(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — POST с `category_id` (без category_query) → 200, status='created'.
5. `test_internal_actual_neither_query_nor_id_422(db_client, internal_headers, seed_active_period, owner_tg_id)` — POST без category_query и без category_id → 422 (model_validator).
6. `test_internal_actual_not_found(db_client, internal_headers, seed_active_period, owner_tg_id)` — POST `category_query:'несуществующая_категория_xyz'` → 200, status='not_found', candidates=None or [].
7. `test_internal_actual_ambiguous(db_client, internal_headers, seed_categories_with_ambiguity, seed_active_period, owner_tg_id)` — POST `category_query:'транс'` (matches both) → 200, status='ambiguous', candidates=2 шт., каждый CategoryCandidate имеет id/name/kind.
8. `test_internal_actual_query_excludes_archived(db_client, internal_headers, seed_archived_category, seed_active_period, owner_tg_id)` — POST `category_query` matching archived category → status='not_found' (archived игнорируется).
9. `test_internal_actual_explicit_id_minует_disambiguation(db_client, internal_headers, seed_categories_with_ambiguity, seed_active_period, owner_tg_id)` — POST с category_id (один из ambiguous кандидатов) → 200, status='created' (disambiguation минута).
10. `test_internal_actual_default_tx_date_today(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — POST без tx_date → response.actual.tx_date == today_in_msk.
11. `test_internal_actual_explicit_tx_date(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — POST с `tx_date:'2026-04-15'` (within active period) → response.actual.tx_date == '2026-04-15'.
12. `test_internal_actual_kind_mismatch_400(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — POST `{kind:'income', category_id:<expense_cat>}` → 400.
13. `test_internal_actual_response_includes_balance(db_client, internal_headers, seed_categories, seed_active_period, db_setup, owner_tg_id)` — Setup: planned 1_000_000 копеек для Продукты + actual 200_000 (preexisting). POST `{kind:'expense', amount_cents:300000, category_query:'продукт'}` → response.category_balance_cents == 1_000_000 - (200_000 + 300_000) = 500_000.

`POST /api/v1/internal/bot/balance`:
14. `test_internal_balance_no_token_403(db_client, owner_tg_id)` — без header → 403.
15. `test_internal_balance_returns_data(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — POST `{tg_user_id}` → 200, includes period_id, balance_now_cents, by_category list. Поля совпадают со спецификацией Pydantic.
16. `test_internal_balance_no_active_period_404(db_client, internal_headers, db_setup, owner_tg_id)` — нет активного периода → 404.

`POST /api/v1/internal/bot/today`:
17. `test_internal_today_no_token_403(db_client, owner_tg_id)` — без header → 403.
18. `test_internal_today_empty(db_client, internal_headers, seed_active_period, owner_tg_id)` — нет actual'ов сегодня → 200, actuals=[], totals=0.
19. `test_internal_today_with_actuals(db_client, internal_headers, seed_categories, seed_active_period, owner_tg_id)` — Setup: создать 2 actual'а на today + 1 на yesterday. POST → 200, actuals=2 (только сегодняшние); каждый row содержит `category_name`; total_expense/income посчитан верно.

---

**tests/test_bot_parsers.py** (pure unit; НЕТ DB, НЕТ HTTP):

Header docstring объясняет: чистые тесты без зависимостей от Postgres/aiogram.

Тесты `parse_amount`:
1. `test_parse_amount_int_kopecks()` — `parse_amount('1500') == 150000`.
2. `test_parse_amount_decimal()` — `parse_amount('1500.50') == 150050`.
3. `test_parse_amount_decimal_comma()` — `parse_amount('1500,50') == 150050`.
4. `test_parse_amount_with_thousand_space()` — `parse_amount('1 500') == 150000`.
5. `test_parse_amount_with_thousand_nbsp()` — `parse_amount('1 500') == 150000` (NBSP).
6. `test_parse_amount_with_ruble_suffix()` — `parse_amount('1500р') == 150000`.
7. `test_parse_amount_with_ruble_symbol()` — `parse_amount('1500₽') == 150000`.
8. `test_parse_amount_with_rub_word()` — `parse_amount('1500руб') == 150000`.
9. `test_parse_amount_combined()` — `parse_amount('1 500.50 ₽') == 150050`.
10. `test_parse_amount_zero_returns_none()` — `parse_amount('0') is None`.
11. `test_parse_amount_negative_returns_none()` — `parse_amount('-100') is None`.
12. `test_parse_amount_empty_returns_none()` — `parse_amount('') is None` (после strip).
13. `test_parse_amount_alpha_returns_none()` — `parse_amount('abc') is None`.
14. `test_parse_amount_three_decimals_rejected()` — `parse_amount('1500.555') is None` (regex max 2 decimals).
15. `test_parse_amount_overflow_returns_none()` — `parse_amount('100000000000000') is None` (cap 10^12 копеек).
16. `test_parse_amount_decimal_only()` — `parse_amount('.50') is None` (нет integer части — defensive).

Тесты `parse_add_command`:
17. `test_parse_add_minimal()` — `parse_add_command('1500 продукты') == (150000, 'продукты', None)`.
18. `test_parse_add_with_description()` — `parse_add_command('1500 продукты пятёрочка') == (150000, 'продукты', 'пятёрочка')`.
19. `test_parse_add_multi_word_description()` — `parse_add_command('1500 кафе обед в столовой') == (150000, 'кафе', 'обед в столовой')`.
20. `test_parse_add_invalid_amount()` — `parse_add_command('abc продукты') is None`.
21. `test_parse_add_only_amount()` — `parse_add_command('1500') is None` (нужна категория).
22. `test_parse_add_empty()` — `parse_add_command('') is None` and `parse_add_command(None) is None`.
23. `test_parse_add_with_decimal_amount()` — `parse_add_command('1500.50 продукты') == (150050, 'продукты', None)`.
24. `test_parse_add_with_ruble_suffix()` — `parse_add_command('1500₽ продукты') == (150000, 'продукты', None)`.

---

**tests/test_bot_handlers_phase4.py** (мокаем httpx через monkeypatch на api_client функции):

Header docstring: НЕ DB-backed; мокаем `app.bot.api_client.bot_create_actual`/`bot_get_balance`/`bot_get_today` через monkeypatch. Используем `aiogram.types.Message`/`CallbackQuery` mock-объекты.

Helper для создания mock Message:
```python
class MockUser:
    def __init__(self, user_id: int):
        self.id = user_id
class MockChat:
    def __init__(self, chat_id: int):
        self.id = chat_id
class MockMessage:
    def __init__(self, user_id: int, chat_id: int, text: str = ""):
        self.from_user = MockUser(user_id)
        self.chat = MockChat(chat_id)
        self.text = text
        self.answers: list[tuple[str, dict]] = []
    async def answer(self, text: str, **kwargs) -> None:
        self.answers.append((text, kwargs))

class MockCommandObject:
    def __init__(self, args: str | None):
        self.args = args
```

Аналогично `MockCallbackQuery`.

Fixtures:
- `mock_owner_message(owner_tg_id)` — `MockMessage(user_id=owner_tg_id, chat_id=owner_tg_id)`.
- `mock_non_owner_message()` — `MockMessage(user_id=999, chat_id=999)`.

Тесты `cmd_add` (через monkeypatch на `bot_create_actual`):
1. `test_cmd_add_owner_creates(mock_owner_message, monkeypatch, owner_tg_id)` — patch `bot_create_actual` чтобы возвращало `{status:'created', actual:{...amount_cents:150000...}, category:{name:'Продукты', kind:'expense', id:1}, category_balance_cents:850000}`. Call `cmd_add(mock_owner_message, MockCommandObject('1500 продукты'))`. Assert: `bot_create_actual` вызван с правильным kwargs; `mock_owner_message.answers` содержит 1 ответ, текст содержит '1 500' и 'Продукты' и 'Остаток'.
2. `test_cmd_add_non_owner_silent(mock_non_owner_message, monkeypatch)` — patch `bot_create_actual` чтобы НЕ был вызван. Call `cmd_add(mock_non_owner_message, MockCommandObject('1500 продукты'))`. Assert: `bot_create_actual` НЕ вызывался; `mock_non_owner_message.answers == []`.
3. `test_cmd_add_invalid_format_responds_usage(mock_owner_message, monkeypatch)` — `cmd_add(mock_owner_message, MockCommandObject('abc xyz'))` (parse_amount returns None). Assert: bot_create_actual НЕ вызван; ответ содержит «Использование» или «/add <сумма>».
4. `test_cmd_add_empty_args(mock_owner_message, monkeypatch)` — `cmd_add(mock_owner_message, MockCommandObject(None))`. Assert: usage-помощь.
5. `test_cmd_add_ambiguous_response_builds_kbd(mock_owner_message, monkeypatch, owner_tg_id)` — patch returns `{status:'ambiguous', candidates:[{id:1,name:'Транспорт',kind:'expense'},{id:2,name:'Транспортная карта',kind:'expense'}]}`. Call `cmd_add(mock_owner_message, MockCommandObject('1500 транс'))`. Assert: `mock_owner_message.answers` содержит 1 ответ с `reply_markup` (InlineKeyboardMarkup), 2 кнопки в keyboard, callback_data starts with `act:`. Также проверить — token сохранён в `_PENDING` (импорт `app.bot.disambiguation._PENDING` для inspection).
6. `test_cmd_add_not_found_response(mock_owner_message, monkeypatch, owner_tg_id)` — patch returns `{status:'not_found'}`. Assert: ответ содержит «не найдена» или «не нашёл».
7. `test_cmd_add_api_error_responds(mock_owner_message, monkeypatch, owner_tg_id)` — patch `bot_create_actual` raise `InternalApiError("connection refused")`. Assert: ответ содержит «Не удалось» или «Сервер».

Тесты `cmd_income` (минимально, т.к. shares logic with cmd_add):
8. `test_cmd_income_owner_creates(mock_owner_message, monkeypatch, owner_tg_id)` — analog (1) but kind='income' и patch returns category kind='income'. Assert: `bot_create_actual` вызван с `kind='income'`.

Тесты `cmd_balance` (через monkeypatch на `bot_get_balance`):
9. `test_cmd_balance_owner(mock_owner_message, monkeypatch, owner_tg_id)` — patch `bot_get_balance` returns `{period_id:1, period_start:'2026-05-05', period_end:'2026-06-03', balance_now_cents:23_45_000, delta_total_cents:5_20_000, planned_total_expense_cents:..., actual_total_expense_cents:..., planned_total_income_cents:..., actual_total_income_cents:..., by_category:[{category_id:1,name:'Продукты',kind:'expense',planned_cents:1_000_000,actual_cents:850_000,delta_cents:150_000}, ...5 items]}`. Assert: ответ содержит «💰 Баланс», «Δ периода», «Топ-5», «5 май», эмодзи (✓/⚠/🔴) согласно процентам.
10. `test_cmd_balance_non_owner_silent(mock_non_owner_message, monkeypatch)` — patch не вызван; нет ответа.
11. `test_cmd_balance_api_error(mock_owner_message, monkeypatch, owner_tg_id)` — InternalApiError → ответ «Сервер».

Тесты `cmd_today` (через monkeypatch на `bot_get_today`):
12. `test_cmd_today_with_data(mock_owner_message, monkeypatch, owner_tg_id)` — patch returns `{actuals:[{id:1,kind:'expense',amount_cents:150000,description:'pyaterochka',category_id:1,category_name:'Продукты'}], total_expense_cents:150000, total_income_cents:0}`. Assert: ответ содержит «Сегодня», «Продукты», «1 500».
13. `test_cmd_today_empty(mock_owner_message, monkeypatch, owner_tg_id)` — patch returns `{actuals:[], total_expense_cents:0, total_income_cents:0}`. Assert: ответ «Сегодня нет факт-трат».

Тесты `cmd_app`:
14. `test_cmd_app_owner(mock_owner_message)` — Call `cmd_app(mock_owner_message)`. Assert: ответ содержит «Mini App» + reply_markup с WebApp кнопкой URL=settings.MINI_APP_URL.

Тесты `cb_disambiguation` (через monkeypatch + mock CallbackQuery):
15. `test_cb_disambiguation_completes(monkeypatch, owner_tg_id)` — Setup: вручную добавить `PendingActual` в `_PENDING` через `store_pending`. Создать MockCallbackQuery с `data=f"act:{token}:5"`. Patch `bot_create_actual` returns `{status:'created', ...}`. Call `cb_disambiguation(callback)`. Assert: `bot_create_actual` вызван с `category_id=5`; `pop_pending(token)` returns None теперь; callback.message.edit_text called с success-текстом; callback.answer called.
16. `test_cb_invalid_format(monkeypatch, owner_tg_id)` — MockCallbackQuery с `data='garbage'` (не starts with `act:`). По F-фильтру handler не должен вызываться, но если кто-то напрямую вызовет: assert graceful answer без crash. **Опц:** этот тест необязательный, т.к. F-filter защищает.
17. `test_cb_invalid_token_alerts(monkeypatch, owner_tg_id)` — MockCallbackQuery с `data='act:nonexistent:5'`. Call cb. Assert: callback.answer called с `show_alert=True` и текстом про истёкшее время; `bot_create_actual` НЕ вызван.
18. `test_cb_invalid_category_id_format(monkeypatch, owner_tg_id)` — `data='act:abcdef12:not_int'`. Assert: graceful answer без crash; bot_create_actual НЕ вызван.

Тесты disambiguation in-memory:
19. `test_pop_pending_after_ttl_returns_none(monkeypatch)` — Создать PendingActual, set `created_at = datetime.utcnow() - timedelta(minutes=10)` (заглубили в past), вызвать `pop_pending(token)` → None.
20. `test_store_pending_returns_8hex_token()` — token = `store_pending(p)`; assert `len(token) == 8`; assert все символы hex.
21. `test_callback_data_format_within_64b()` — формат `f"act:{8hex}:{int}"` для category_id=999_999_999 → длина < 64.

Edge cases:
22. `test_cmd_add_ambiguous_then_callback_e2e(mock_owner_message, monkeypatch, owner_tg_id)` — Полный flow: cmd_add получает ambiguous → kbd создан → token сохранён → симулируем callback → bot_create_actual вызван с правильным category_id.

  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/test_internal_bot.py tests/test_bot_parsers.py tests/test_bot_handlers_phase4.py -x --collect-only 2>&1 | tail -40</automated>
  </verify>
  <done>
- Файлы tests/test_internal_bot.py (~19 тестов), tests/test_bot_parsers.py (~24 теста), tests/test_bot_handlers_phase4.py (~22 теста) созданы.
- pytest собирает все тесты (collect-only); они в RED state (ImportError на app.services.internal_bot, app.bot.parsers, app.bot.commands, app.bot.disambiguation).
- test_internal_bot.py — DB-backed (использует _require_db).
- test_bot_parsers.py — pure unit без БД и aiogram.
- test_bot_handlers_phase4.py — мокает api_client через monkeypatch; использует MockMessage/MockCallbackQuery patterns.
- Total: 6 test files Phase 4, 80+ test cases, all in RED state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → app code | Tests import from app.* — at this stage все импорты Phase 4 модулей падают; это ожидаемое RED |
| Mock api_client → bot handler | Tests substitute network call с возвращаемыми dict'ами; гарантирует unit-isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | tests/conftest.py | accept | Существующий pattern из Phase 1-3; не модифицируем conftest |
| T-04-02 | Tampering | DB-backed tests без TRUNCATE | mitigate | Все DB файлы используют `db_setup` fixture с TRUNCATE — гарантированно clean state (`actual_transaction` уже в TRUNCATE list из Phase 3) |
| T-04-03 | Information Disclosure | Test data utечёт в production DB | mitigate | `_require_db` self-skip + `DATABASE_URL=...budget_test` (отдельная test-БД) |
| T-04-04 | Tampering | mock api_client возвращает invalid shape | mitigate | Тесты проверяют shape responses (status, candidates list, и т.д.) |
| T-04-05 | DoS | Тесты Phase 4 раздуваются | accept | 6 файлов, 80+ тестов — стандартный объём для phase с complexity Phase 4 |
</threat_model>

<verification>
- pytest collect-only на всех 6 файлах не выдаёт SyntaxError.
- Тесты падают на ImportError (модули `app.services.actual`, `app.services.internal_bot`, `app.bot.parsers`, `app.bot.commands`, `app.bot.disambiguation` не существуют) — это RED-state, ожидаемо.
- Перед Plan 04-02 imports `app.api.schemas.actual` и `app.services.actual` начнут разрешаться → тесты начнут падать на 404/422/200-mismatch — следующий уровень RED.
- После Plan 04-03 (routes wired) backend-тесты (test_actual_*, test_balance, test_internal_bot) должны стать зелёными.
- После Plan 04-04 (bot wired) bot-тесты должны стать зелёными.
</verification>

<success_criteria>
- 6 тестовых файлов существуют в `tests/`.
- Все RED.
- Pytest config (`asyncio_mode=auto`) применяется (фреймворк работает).
- Wave 0 RED gate documented в 04-VALIDATION.md выполнен.
</success_criteria>

<output>
After completion, create `.planning/phases/04-actual-transactions-and-bot-commands/04-01-SUMMARY.md`.
</output>
