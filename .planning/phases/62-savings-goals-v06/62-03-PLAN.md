---
phase: 62-savings-goals-v06
plan: 03
type: execute
wave: 3
gap_closure: true
depends_on: [62-01, 62-02]
autonomous: true
requirements: [SAVINGS-GOALDETAIL, SAVINGS-NEWGOAL, SAVINGS-DEPOSIT]
files_modified:
  - ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift
  - ios/BudgetPlanner/Features/Savings/GoalDetailView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift
  - ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift
  - ios/BudgetPlanner/Features/Savings/SavingsViewData.swift
  - ios/BudgetPlanner/Networking/DTO/GoalDTO.swift
  - ios/BudgetPlannerTests/Features/Savings/GoalDetailViewModelTests.swift
  - ios/BudgetPlannerTests/Features/Savings/SavingsViewDataTests.swift

must_haves:
  truths:
    - "Tap goal-row pushes GoalDetailView that LOADS and renders the goal (name + progress bar + currentCents/targetCents + percentage + due), not an infinite spinner"
    - "GoalDetailView «Удалить цель» Menu → confirmationDialog → calls GoalsAPI.delete and dismisses on success"
    - "GoalDetailView «Пополнить» CTA opens a pre-filled SavingsDepositSheet for this goal"
    - "NewGoalSheet has functional inputs (name + target amount + optional due) and «Создать» calls createGoal; dismisses on success, KEEPS the form + shows error on failure"
    - "DepositSheet has functional inputs (amount + account Picker + optional goal Picker) and «Пополнить» calls deposit; account=0/unselected is rejected by the validation gate"
    - "DatePicker due date encodes the calendar day the user picked (no MSK→UTC day-shift)"
  artifacts:
    - path: "ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift"
      provides: "load() fetches goal+accounts, deleteGoal() calls API"
      contains: "GoalsAPI.list"
    - path: "ios/BudgetPlanner/Features/Savings/GoalDetailView.swift"
      provides: "Hero + progress + delete Menu + Deposit CTA"
      contains: "ProgressView(value:"
    - path: "ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift"
      provides: "Form: name + target + optional due + Создать"
      contains: "MoneyParser.parseToCents"
    - path: "ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift"
      provides: "Form: amount + account Picker + goal Picker + Пополнить"
      contains: "Picker"
    - path: "ios/BudgetPlannerTests/Features/Savings/GoalDetailViewModelTests.swift"
      provides: "VM unit tests"
      min_lines: 30
  key_links:
    - from: "ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift"
      to: "GoalsAPI.list / AccountsAPI.list / GoalsAPI.delete"
      via: "load() and deleteGoal()"
      pattern: "GoalsAPI\\.(list|delete)"
    - from: "ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift"
      to: "onCreate closure (→ SavingsViewModel.createGoal)"
      via: "Создать button .confirmationAction"
      pattern: "onCreate\\("
    - from: "ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift"
      to: "onDeposit closure (→ SavingsViewModel.deposit)"
      via: "Пополнить button .confirmationAction"
      pattern: "onDeposit\\("
---

<objective>
Close the 3 verified gaps in Phase 62 (62-VERIFICATION.md, 2/4): the GoalDetailView/VM and the two Form sheets remain literal 62-01 stubs. The master SavingsView (truth 1) is DONE and is OUT OF SCOPE here — do not re-plan or modify SavingsView.swift / SavingsViewModel.swift.

This plan implements:
1. `GoalDetailViewModel.load()` + `deleteGoal()` (currently empty no-op / returns false).
2. `GoalDetailView` body — Hero + progress + delete Menu + «Пополнить» CTA (currently infinite spinner).
3. `SavingsNewGoalSheet` Form body (currently placeholder text).
4. `SavingsDepositSheet` Form body (currently placeholder text).

Plus two folded-in code-review fixes that live on these surfaces:
- **WR-05**: `SavingsViewData.isValidDepositDraft` accepts `accountId == 0`; tighten to require `accountId > 0`.
- **IN-04**: `GoalCreateRequest.encode` formats `due` with a UTC `DateFormatter`; a DatePicker in Europe/Moscow produces MSK-midnight = previous-day 21:00 UTC, so the wire `yyyy-MM-dd` can be one day earlier than the user picked. Re-encode with an MSK calendar.

Purpose: every ROADMAP-62 deliverable becomes functionally testable (not a stub), so re-verification passes.
Output: 4 implemented Savings screens, 2 review fixes, new VM + validation unit tests, green build.

NOT in scope (leave for a separate polish pass — note it in the SUMMARY): WR-01 / WR-02 / WR-03 / WR-04 / WR-06 — these are in the already-shipped SavingsViewModel.swift master-view mutation paths, which this plan does not touch. Do NOT edit SavingsViewModel.swift or SavingsView.swift.

NOTE on deposit history: there is NO per-goal deposit-list endpoint. `SavingsSummaryDTO` only exposes aggregate `goals`. GoalDetailView therefore renders the goal hero + actions only (matches 62-CONTEXT.md decision which lists Hero + «Пополнить» Action + delete Menu, with no history list). Do NOT invent a deposits-history API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-savings-goals-v06/62-CONTEXT.md
@.planning/phases/62-savings-goals-v06/62-VERIFICATION.md
@.planning/phases/62-savings-goals-v06/62-REVIEW.md

<interfaces>
<!-- Contracts the executor needs. Already in the codebase — do not re-derive. -->

Closure signatures the parent SavingsView ALREADY wires (match them EXACTLY):
```swift
// SavingsView .sheet for newGoal:
SavingsNewGoalSheet(
    submitting: viewModel.submitting,
    onCreate: { name, targetCents, due in
        await viewModel.createGoal(name: name, targetCents: targetCents, due: due)
    },
    onCancel: { viewModel.sheet = .none }
)
// → struct already declares:
//   let submitting: Bool
//   let onCreate: (_ name: String, _ targetCents: Int, _ due: Date?) async -> Bool
//   let onCancel: () -> Void

// SavingsView .sheet for deposit:
SavingsDepositSheet(
    submitting: viewModel.submitting,
    goals: viewModel.goals,
    accounts: viewModel.accounts,
    initialGoalId: depositInitialGoalId,
    onDeposit: { amount, accountId, goalId in
        await viewModel.deposit(amountCents: amount, accountId: accountId, goalId: goalId)
    },
    onCancel: { viewModel.sheet = .none }
)
// → struct already declares:
//   let submitting: Bool
//   let goals: [GoalDTO]
//   let accounts: [AccountDTO]
//   let initialGoalId: Int?
//   let onDeposit: (_ amountCents: Int, _ accountId: Int, _ goalId: Int?) async -> Bool
//   let onCancel: () -> Void

// GoalDetailView is pushed via navigationDestination(for: SavingsRoute.self):
//   case .goal(let id): GoalDetailView(goalId: id)
// GoalDetailView already declares: init(goalId: Int), @State viewModel = GoalDetailViewModel(goalId:)
```

DTOs:
```swift
struct GoalDTO: Decodable, Identifiable, Equatable {
    let id: Int; let name: String; let targetCents: Int
    let currentCents: Int; let due: Date?; let createdAt: Date
}
struct AccountDTO: Decodable, Identifiable, Equatable {
    let id: Int; let bank: String; let mask: String?
    let kind: AccountKind; let balanceCents: Int; let primary: Bool; let createdAt: Date?
}
```

APIs (all @MainActor enum, async throws):
```swift
GoalsAPI.list() -> [GoalDTO]          // no GET /goals/{id}; list + filter by id
GoalsAPI.delete(id: Int) -> Void      // requestVoid, 204
AccountsAPI.list() -> [AccountDTO]
SavingsViewData.progressPercentage(currentCents:targetCents:) -> Int  // clamped 0..100
SavingsViewData.formatDue(_ date: Date?, calendar: Calendar) -> String?
SavingsViewData.isValidGoalDraft(name: String, targetCents: Int) -> Bool
MoneyFormatter.format(cents: Int) -> String      // "1 500,50", no symbol
MoneyParser.parseToCents(_ raw: String) -> Int?  // ru_RU comma/space aware; nil on garbage
```

Existing VM stub state (GoalDetailViewModel) to fill — KEEP property names:
```swift
let goalId: Int
private(set) var status: Status = .idle  // .idle/.loading/.ready/.error(String)
private(set) var goal: GoalDTO?
private(set) var accounts: [AccountDTO] = []
private(set) var submitting: Bool = false
var mutationError: String? = nil
@ObservationIgnored private var inFlight: Bool = false
func clearMutationError()  // exists
```
</interfaces>

# Files being modified (read in full before editing) — also read the shipped analogs
@ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift
@ios/BudgetPlanner/Features/Savings/GoalDetailView.swift
@ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift
@ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift
@ios/BudgetPlanner/Features/Savings/SavingsViewData.swift
@ios/BudgetPlanner/Networking/DTO/GoalDTO.swift

# Parent (closure signatures + master patterns) — READ ONLY, do not modify
@ios/BudgetPlanner/Features/Savings/SavingsView.swift
@ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift

# Shipped analogs to mirror exactly (Phase 60)
@ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift
@ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountsNewSheet.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GoalDetailViewModel.load/delete + WR-05 + IN-04 fixes</name>
  <files>ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift, ios/BudgetPlanner/Features/Savings/SavingsViewData.swift, ios/BudgetPlanner/Networking/DTO/GoalDTO.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift (the stub being filled — keep property names + threat-model doc comments)
    - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (THE template: inFlight guard, status machine, "list + filter by id" pattern because there's no GET /{id}, cross-tenant single-message guard, T-60-03 filtered-copy catch with print() only)
    - ios/BudgetPlanner/Features/Savings/SavingsViewData.swift (isValidDepositDraft to fix — WR-05)
    - ios/BudgetPlanner/Networking/DTO/GoalDTO.swift (GoalCreateRequest.encode — UTC formatter to fix — IN-04)
    - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift (deleteGoal pattern: submitting guard + DELETE + filtered copy)
  </read_first>
  <behavior>
    GoalDetailViewModel.load():
    - Test: when GoalsAPI.list returns a goal whose id matches goalId → status == .ready, goal != nil with matching id, accounts populated from AccountsAPI.list.
    - Test (edge): when no goal matches goalId (deleted / cross-tenant) → status == .error("Цель не найдена"), goal stays nil (single message, no existence leak — mirror AccountDetail "Счёт не найден").
    - Test (edge): outer throw → status == .error("Не удалось загрузить цель"); raw error only via print().
    - Test (guard): re-entrant load() while inFlight returns early (no second fetch).
    GoalDetailViewModel.deleteGoal():
    - Test: success path returns true (so caller dismisses); submitting guard prevents double-delete; failure returns false and sets mutationError == "Не удалось удалить цель".
    SavingsViewData.isValidDepositDraft (WR-05):
    - Test: accountId == 0 → false; accountId == nil → false; negative → false; positive id + amount>0 → true; amount==0 → false.
    GoalCreateRequest due encoding (IN-04):
    - Test: a Date constructed at Europe/Moscow midnight for 2026-09-12 encodes to "2026-09-12" (NOT "2026-09-11").
  </behavior>
  <action>
    1. **GoalDetailViewModel.load()** — fill the no-op body. Mirror AccountDetailViewModel.load() exactly:
       - `if inFlight { return }`; `inFlight = true`; `defer { inFlight = false }`; `status = .loading`.
       - `do { async let goalsTask = GoalsAPI.list(); async let accsTask = AccountsAPI.list(); let (goals, accs) = try await (goalsTask, accsTask) }`.
       - `guard let g = goals.first(where: { $0.id == goalId }) else { status = .error("Цель не найдена"); return }` (T-62-03 / IN-01: this finally USES goalId — keeps the property live).
       - Set `self.goal = g`, `self.accounts = accs`, `status = .ready`.
       - `catch { print("[GoalDetailViewModel] load failed: \(error)"); status = .error("Не удалось загрузить цель") }` (T-62-03: raw error → print only).
    2. **GoalDetailViewModel.deleteGoal()** — fill. Mirror SavingsViewModel.deleteGoal but return Bool:
       - `guard !submitting else { return false }`; `submitting = true`; `defer { submitting = false }`.
       - `do { try await GoalsAPI.delete(id: goalId); return true } catch { print("[GoalDetailViewModel] deleteGoal failed: \(error)"); mutationError = "Не удалось удалить цель"; return false }`.
       - Do NOT call load() after delete (the goal is gone; caller dismisses on true).
    3. Add `#if DEBUG _setStateForTesting(goal:accounts:status:)` backdoor mirroring AccountDetailViewModel's, so tests can inject state without network.
    4. **SavingsViewData.isValidDepositDraft (WR-05)** — change body to: `guard let accountId else { return false }; return amountCents > 0 && accountId > 0`. Update the doc comment to note `gt=0` enforcement.
    5. **GoalCreateRequest.encode (IN-04)** — in GoalDTO.swift, change the `due` formatter's `timeZone` from `TimeZone(identifier: "UTC")` to `TimeZone(identifier: "Europe/Moscow") ?? TimeZone(identifier: "UTC")!`. This makes the wire `yyyy-MM-dd` match the calendar day the user picked in MSK. Update the inline comment to explain the MSK rationale (replaces the UTC comment).
  </action>
  <verify>
    <automated>cd ios && xcodebuild -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - GoalDetailViewModel.load() fetches GoalsAPI.list + AccountsAPI.list, filters by goalId, sets goal/accounts/status; no-op body GONE.
    - deleteGoal() calls GoalsAPI.delete and returns true on success, false + mutationError on failure.
    - `grep -v '^[[:space:]]*//' SavingsViewData.swift | grep -c "accountId > 0"` ≥ 1 (WR-05 fixed).
    - GoalDTO.swift `due` encoder uses Europe/Moscow timezone (IN-04 fixed); no remaining UTC formatter for `due`.
    - File compiles (build succeeds).
  </acceptance_criteria>
  <done>load()/deleteGoal() wired to real APIs; isValidDepositDraft rejects accountId<=0; due encodes in MSK; build green.</done>
</task>

<task type="auto">
  <name>Task 2: GoalDetailView body + NewGoalSheet + DepositSheet Form bodies</name>
  <files>ios/BudgetPlanner/Features/Savings/GoalDetailView.swift, ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift, ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Savings/GoalDetailView.swift / SavingsNewGoalSheet.swift / SavingsDepositSheet.swift (the stub bodies being filled — keep struct names, init, and the EXACT closure signatures already declared)
    - ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift (Hero Section + 4 render-states pattern, ContentUnavailableView, MoneyFormatter usage)
    - ios/BudgetPlanner/Features/Accounts/AccountsNewSheet.swift (THE Form-sheet template: NavigationStack+Form, .decimalPad + MoneyParser.parseToCents binding, .confirmationAction with disabled !canCreate, .cancellationAction, .presentationDetents, .interactiveDismissDisabled(submitting), submitting label swap)
    - ios/BudgetPlanner/Features/Savings/SavingsView.swift (master SavingsGoalRow uses ProgressView(value:total:).tint(.green) + percentage + MSK formatDue calendar — mirror the hero; also confirm the closure call shapes)
  </read_first>
  <action>
    **A. GoalDetailView body** — replace the placeholder `List { ProgressView("Загрузка…") }` with a 4-state `List(.insetGrouped)` switch on `viewModel.status` (mirror AccountDetailView):
      - `.idle/.loading` → Section { ProgressView().frame(maxWidth:.infinity) }.
      - `.error(let msg)` → Section { Label(msg, systemImage:"exclamationmark.triangle").foregroundStyle(.red) }.
      - `.ready` → `if let goal = viewModel.goal { heroSection(goal); actionSection } else { defensive "Цель не найдена" Label }`.
      - **heroSection(goal):** Section { VStack(alignment:.leading): name `.title2.weight(.semibold)`; big `ProgressView(value: Double(max(0, goal.currentCents)), total: Double(max(1, goal.targetCents))).tint(.green)`; line "\(MoneyFormatter.format(cents: goal.currentCents)) ₽ из \(MoneyFormatter.format(cents: goal.targetCents)) ₽ · \(SavingsViewData.progressPercentage(currentCents:targetCents:))%"; if-let due via `SavingsViewData.formatDue(goal.due, calendar: mskCalendar)` (copy the static mskCalendar from SavingsGoalRow); checkmark.seal.fill when currentCents >= targetCents && targetCents > 0 }.
      - **actionSection:** Section { Button("Пополнить", systemImage:"arrow.down.circle") { showDeposit = true } }.
      - **delete Menu** in `.toolbar { ToolbarItem(.topBarTrailing) { Menu { Button("Удалить цель", role:.destructive){ confirmDelete = true } } label: { Image(systemName:"ellipsis.circle") } } }`.
      - `.confirmationDialog("Удалить цель?", isPresented: $confirmDelete, titleVisibility:.visible)` → "Удалить" destructive Button → `Task { if await viewModel.deleteGoal() { dismiss() } }`; "Отмена" cancel. message: "Удаление цели нельзя отменить. Накопления останутся в общей копилке."
      - Add `@Environment(\.dismiss) private var dismiss`, `@State private var confirmDelete = false`, `@State private var showDeposit = false`.
      - **«Пополнить» CTA → pre-filled DepositSheet:** `.sheet(isPresented: $showDeposit) { SavingsDepositSheet(submitting: viewModel.submitting, goals: viewModel.goal.map { [$0] } ?? [], accounts: viewModel.accounts, initialGoalId: viewModel.goalId, onDeposit: { amount, accountId, goalId in let ok = await viewModel.deposit-via... }) }`. NOTE: GoalDetailViewModel has no deposit() method — instead route the deposit through GoalDetail by calling `SavingsAPI.postDeposit` directly is OUT OF SCOPE; SIMPLER: the CTA opens the sheet, and on success it `await viewModel.load()` + `showDeposit = false`. Implement onDeposit closure inline: `{ amount, accountId, goalId in do { _ = try await SavingsAPI.postDeposit(amountCents: amount, accountId: accountId, goalId: goalId); await viewModel.load(); return true } catch { print(...); return false } }`. Keep onCancel `{ showDeposit = false }`. (This keeps GoalDetail self-contained without touching SavingsViewModel.) Add a tiny `submitting` pass-through using `viewModel.submitting`.
      - Add a `mutationError` banner Section at top of `.ready` when `viewModel.mutationError != nil` (delete-failure copy), with a clear button calling `viewModel.clearMutationError()` — mirror SavingsView.mutationErrorBanner.
      - Keep `.task { await viewModel.load() }` and add `.refreshable { await viewModel.load() }`.
    **B. SavingsNewGoalSheet body** — replace placeholder Text. Mirror AccountsNewSheet:
      - `@State name = ""`, `@State targetText = ""`, `@State hasDue = false`, `@State dueDate = Date()` (or tomorrow).
      - Form: Section("Название") { TextField("Например, Отпуск", text: $name).textInputAutocapitalization(.sentences) }; Section("Целевая сумма") { HStack { TextField("0", text: $targetText).keyboardType(.decimalPad).multilineTextAlignment(.trailing).monospacedDigit(); Text("₽").foregroundStyle(.secondary) } }; Section { Toggle("Добавить срок", isOn: $hasDue); if hasDue { DatePicker("Срок", selection: $dueDate, in: Calendar.current.date(byAdding:.day, value:1, to: Date())!..., displayedComponents: .date).datePickerStyle(.compact) } }.
      - Derived: `targetCents = MoneyParser.parseToCents(targetText) ?? 0`; `canCreate = SavingsViewData.isValidGoalDraft(name: name, targetCents: targetCents) && !submitting`.
      - Toolbar: .cancellationAction "Отмена" { onCancel() }.disabled(submitting); .confirmationAction Button(submitting ? "Создание…" : "Создать") { Task { _ = await onCreate(name.trimmingCharacters(in:.whitespacesAndNewlines), targetCents, hasDue ? dueDate : nil) } }.disabled(!canCreate).
      - `.presentationDetents([.medium, .large])`, `.interactiveDismissDisabled(submitting)`, navigationTitle "Новая цель".
      - **WR-02 awareness**: SavingsViewModel.createGoal currently dismisses on failure via `sheet = .none` (NOT this plan's scope to fix). Do NOT add extra dismissal here; the sheet binding in SavingsView reacts to viewModel.sheet. Just call onCreate; do not flip any local presented flag.
    **C. SavingsDepositSheet body** — replace placeholder Text. Mirror AccountsNewSheet form patterns:
      - `@State amountText = ""`, `@State selectedGoalId: Int?` initialized from `initialGoalId` (use init or `.onAppear`/`@State` with default), `@State selectedAccountId: Int?` (default to first account id, or accounts.first(where:\.primary)?.id).
      - Form: Section("Цель") { Picker("Цель", selection: $selectedGoalId) { Text("Общая копилка").tag(Int?.none); ForEach(goals) { g in Text(g.name).tag(Int?.some(g.id)) } } }; Section("Сумма") { HStack { TextField("0", text:$amountText).keyboardType(.decimalPad).multilineTextAlignment(.trailing).monospacedDigit(); Text("₽").foregroundStyle(.secondary) } }; Section("Счёт списания") { Picker("Счёт", selection: $selectedAccountId) { ForEach(accounts) { a in Text(accountLabel(a)).tag(Int?.some(a.id)) } } } footer: { Text("Списание со счёта; на счёт-копилку — будет приход.") }.
      - `accountLabel(a)`: `a.bank` + (a.mask.map { " ·\($0)" } ?? "").
      - Derived: `amountCents = MoneyParser.parseToCents(amountText) ?? 0`; `canDeposit = SavingsViewData.isValidDepositDraft(amountCents: amountCents, accountId: selectedAccountId) && !submitting` (relies on Task-1 WR-05 fix → rejects nil/0).
      - Toolbar: .cancellationAction "Отмена".disabled(submitting); .confirmationAction Button(submitting ? "Пополнение…" : "Пополнить") { if let acc = selectedAccountId { Task { _ = await onDeposit(amountCents, acc, selectedGoalId) } } }.disabled(!canDeposit).
      - `.presentationDetents([.medium, .large])`, `.interactiveDismissDisabled(submitting)`, navigationTitle "Пополнение".
      - Set initial selectedAccountId via an init or `.task`/`.onAppear` since accounts arrive as a prop.
  </action>
  <verify>
    <automated>cd ios && xcodebuild -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - GoalDetailView: no remaining `ProgressView("Загрузка…")` placeholder; renders Hero (name + ProgressView(value:total:) + cents/target + percentage + due) on .ready, error/loading states, delete Menu→confirmationDialog→deleteGoal()→dismiss, «Пополнить» CTA opens pre-filled SavingsDepositSheet.
    - SavingsNewGoalSheet: no remaining "Plan 62-03" placeholder text; has TextField name + .decimalPad target + Toggle/DatePicker due + «Создать» calling onCreate, disabled until canCreate.
    - SavingsDepositSheet: no remaining "Plan 62-03" placeholder text; has goal Picker (pre-filled initialGoalId) + .decimalPad amount + account Picker + «Пополнить» calling onDeposit, disabled until canDeposit.
    - `grep -rL "Plan 62-03 заполнит\|Plan 62-03 fills" ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift` lists BOTH files (placeholders gone).
    - Build succeeds.
  </acceptance_criteria>
  <done>Three screens render functional UI wired to real closures/APIs; placeholders removed; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Unit tests (GoalDetailViewModel + deposit validation) + xcodegen + full suite</name>
  <files>ios/BudgetPlannerTests/Features/Savings/GoalDetailViewModelTests.swift, ios/BudgetPlannerTests/Features/Savings/SavingsViewDataTests.swift</files>
  <read_first>
    - ios/BudgetPlannerTests/Features/Savings/SavingsViewModelTests.swift (test file conventions: @MainActor XCTestCase, @testable import BudgetPlanner, _setStateForTesting backdoor usage)
    - ios/BudgetPlannerTests/Features/Savings/SavingsViewDataTests.swift (existing isValidDepositDraft tests — extend for accountId==0)
    - ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift (final state after Task 1 — the backdoor + status enum + deleteGoal signature)
    - ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift (analog VM-test patterns for a list+filter VM)
  </read_first>
  <action>
    1. Create `GoalDetailViewModelTests.swift` (`@MainActor final class ... : XCTestCase`, `@testable import BudgetPlanner`):
       - `test_initialState_idle`: fresh VM(goalId: 1) → status .idle, goal nil, submitting false.
       - `test_setStateForTesting_readyRendersGoal`: inject a GoalDTO via `_setStateForTesting(goal:accounts:status:.ready)` → goal != nil, status .ready (verifies the .ready render path has data — no network).
       - `test_clearMutationError`: set mutationError then clear → nil.
       - `test_status_equatable`: .error("a") != .error("b"), .ready == .ready (mirror SavingsViewModelTests Status equality coverage).
       - (Network paths load()/deleteGoal() are not unit-testable without an injectable API seam — same constraint documented in 62-REVIEW WR-06; assert state-machine + backdoor only, and note the seam gap in the SUMMARY.)
    2. Extend `SavingsViewDataTests.swift` with WR-05 cases:
       - `test_isValidDepositDraft_rejectsZeroAccount`: `isValidDepositDraft(amountCents: 5000, accountId: 0)` == false.
       - `test_isValidDepositDraft_rejectsNegativeAccount`: accountId: -3 == false.
       - keep/verify existing nil + positive cases still pass.
    3. Regenerate the Xcode project so the new test file is included: `cd ios && xcodegen generate`.
    4. Run the full suite.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 16' test 2>&1 | tail -15 | grep -q "TEST SUCCEEDED\|** TEST SUCCEEDED **"</automated>
  </verify>
  <acceptance_criteria>
    - GoalDetailViewModelTests.swift exists with ≥4 tests covering initial state, ready-with-goal injection, clearMutationError, Status equality.
    - SavingsViewDataTests.swift has new accountId==0 and negative-account cases asserting false.
    - `cd ios && xcodegen generate` ran (new test file is in the project).
    - Full `xcodebuild ... test` reports TEST SUCCEEDED (all prior 32+ tests still green, new tests pass).
  </acceptance_criteria>
  <done>New VM + validation tests pass; project regenerated; entire test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini-app UI → REST API | User-entered goal name / target / deposit amount / account id cross to the backend |
| GoalDetail → goal resolution | A goalId from navigation may not belong to the user (cross-tenant / stale id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | NewGoalSheet targetCents / DepositSheet amount | mitigate | UI gate: isValidGoalDraft (target>0), isValidDepositDraft (amount>0 && accountId>0 after WR-05); backend Pydantic gt=0 defence-in-depth |
| T-62-02 | Repudiation | GoalDetailView delete | mitigate | `.confirmationDialog("Удалить цель?")` before GoalsAPI.delete (mirrors master + Phase 59) |
| T-62-03 | Information disclosure | GoalDetailViewModel load/delete catch | mitigate | Cross-tenant/missing id → single "Цель не найдена"; outer catch → fixed "Не удалось загрузить цель"/"Не удалось удалить цель"; raw error → print() only |
| T-62-04 | Denial of service (double-submit) | deleteGoal / deposit | mitigate | `submitting` guard + `.disabled(submitting)` on toolbar buttons + interactiveDismissDisabled(submitting) |
| T-62-05 | Stale state | GoalDetail deposit success | mitigate | `await viewModel.load()` after a successful deposit refreshes hero/progress |
| T-62-06 | Tampering (date drift) | GoalCreateRequest.due encoding | mitigate | IN-04: encode `due` with Europe/Moscow calendar so wire day == picked day |
</threat_model>

<verification>
- Build succeeds for the BudgetPlanner scheme (iPhone 16 simulator).
- Full XCTest suite passes (existing 32+ tests + new GoalDetailViewModel + WR-05 validation tests).
- `grep` confirms no remaining stub placeholders ("Plan 62-03 заполнит", no-op `load()` body) in the four Savings files.
- `grep` confirms `accountId > 0` in SavingsViewData and Europe/Moscow timezone in GoalDTO due encoder.
- Manual (human-verify, deferred to verifier with live env): tap goal-row → GoalDetail loads + renders; «Создать»/«Пополнить» round-trip; delete confirmation flow.
</verification>

<success_criteria>
- GoalDetailView loads and renders a real goal (Hero + progress + cents/target + percentage + due), not an infinite spinner.
- GoalDetailViewModel.load() and deleteGoal() call real APIs (GoalsAPI.list / AccountsAPI.list / GoalsAPI.delete).
- SavingsNewGoalSheet is a functional Form (name + target via MoneyParser + optional MSK-correct due) wired to onCreate.
- SavingsDepositSheet is a functional Form (amount + required account Picker + optional goal Picker, pre-filled) wired to onDeposit; accountId==0/nil rejected.
- WR-05 and IN-04 review fixes applied with tests.
- Build + full test suite green; project regenerated via xcodegen.
</success_criteria>

<output>
After completion, create `.planning/phases/62-savings-goals-v06/62-03-SUMMARY.md`.
Note in the SUMMARY: which review warnings remain OPEN (WR-01/02/03/04/06 in SavingsViewModel master mutations — intentionally out of this gap-closure scope), and that GoalDetailViewModel network paths lack an injectable API seam for unit tests (same constraint as WR-06; only state-machine asserted).
</output>
