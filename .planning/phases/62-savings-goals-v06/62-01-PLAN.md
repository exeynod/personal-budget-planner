---
phase: 62
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Features/Management/ManagementView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsRoute.swift
  - ios/BudgetPlanner/Features/Savings/SavingsData.swift
  - ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift
  - ios/BudgetPlanner/Features/Savings/SavingsView.swift
  - ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift
  - ios/BudgetPlanner/Features/Savings/GoalDetailView.swift
  - ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift
  - ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift
autonomous: true
requirements:
  - v1.1.2-62-CONTEXT-area-1-navigation
  - v1.1.2-62-CONTEXT-area-6-coexistence
threat_ids:
  - T-62-05
must_haves:
  truths:
    - "ManagementItem.ID enum имеет case `.savings` зарегистрированный перед `.template` (CONTEXT D-1)."
    - "ManagementItem.all массив содержит entry «Копилка» (icon `banknote.fill`, ownerOnly: false) перед entry `.template`."
    - "ManagementView.destination(for:) обрабатывает case `.savings → SavingsView()`."
    - "Каталог `ios/BudgetPlanner/Features/Savings/` существует."
    - "8 stub-файлов компилируются (типы определены: SavingsRoute, SavingsData enum, SavingsViewModel + GoalDetailViewModel @Observable, SavingsView + GoalDetailView + 2 sheet structs)."
    - "Имя SavingsNewGoalSheet (а не NewGoalSheet) — избегаем Swift symbol collision с `FeaturesV10/Savings/NewGoalSheet.swift` в том же таргете."
    - "Имя SavingsDepositSheet (а не DepositSheet) — избегаем collision с `FeaturesV10/Savings/DepositSheet.swift`."
    - "SavingsRoute typed enum (`case goal(id: Int)`) — избегаем collision с AccountsView Int.self и PlanEditorRoute в shared ManagementView NavigationStack."
    - "FeaturesV10/Savings/* НЕ модифицирован (5 файлов V10 untouched: SavingsV10View, SavingsV10ViewModel, SavingsData, NewGoalSheet, DepositSheet)."
    - "MainShell.swift НЕ модифицирован."
    - "Build: `make build` clean (0 errors, 0 new warnings)."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Management/ManagementView.swift"
      provides: "ManagementItem.savings registration + destination(for:.savings) dispatch"
      contains: "case savings"
    - path: "ios/BudgetPlanner/Features/Savings/SavingsRoute.swift"
      provides: "Typed-route enum SavingsRoute { case goal(id: Int) }"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/Savings/SavingsData.swift"
      provides: "Pure-compute helpers stub (enum SavingsData) — будет заполнен Plan 62-02"
      min_lines: 5
    - path: "ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift"
      provides: "@MainActor @Observable SavingsViewModel stub (Status, SheetMode, snapshot, accounts, sheet, submitting, lastCreatedGoalId)"
      min_lines: 35
    - path: "ios/BudgetPlanner/Features/Savings/SavingsView.swift"
      provides: "Native SavingsView stub (List placeholder body)"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift"
      provides: "@MainActor @Observable GoalDetailViewModel stub (goalId init + Status enum + goal property)"
      min_lines: 20
    - path: "ios/BudgetPlanner/Features/Savings/GoalDetailView.swift"
      provides: "GoalDetailView(goalId: Int) stub"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift"
      provides: "SavingsNewGoalSheet stub view (renamed to avoid Swift collision)"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift"
      provides: "SavingsDepositSheet stub view (renamed to avoid Swift collision)"
      min_lines: 10
  key_links:
    - from: "ManagementView.destination(for:)"
      to: "SavingsView"
      via: "switch case .savings"
      pattern: "case .savings:\\s*SavingsView"
    - from: "ManagementItem.all"
      to: "Features/Savings/SavingsView.swift"
      via: "ManagementItem(id: .savings, ...) entry"
      pattern: "id: .savings"
    - from: "SavingsView body (будущий)"
      to: "GoalDetailView"
      via: ".navigationDestination(for: SavingsRoute.self)"
      pattern: "SavingsRoute"
---

<objective>
Зарегистрировать новый домен «Копилка» в v06 native shell через расширение `ManagementItem.ID` + `ManagementItem.all` + `ManagementView.destination(for:)` и создать каталог `ios/BudgetPlanner/Features/Savings/` с восемью stub-файлами (SavingsRoute, SavingsData, SavingsViewModel, SavingsView, GoalDetailViewModel, GoalDetailView, SavingsNewGoalSheet, SavingsDepositSheet). Build должен оставаться зелёным. Реальная функциональность будет добавлена в Plans 62-02 / 62-03.

Purpose: установить точку входа в Management hub + создать пустые типы, чтобы downstream plans могли независимо заполнять body / VM без блокирующих cross-file правок. ManagementItem.savings должен быть видим в UI сразу после этого плана (tap → SavingsView с ProgressView placeholder). Это симметрично Phase 60-01 (Accounts) и 61-01 (PlanEditor).

Output:
- ManagementItem.ID enum расширен `.savings`.
- ManagementItem.all содержит entry «Копилка» перед `.template`.
- ManagementView.destination(for:) обрабатывает `.savings`.
- 8 stub-файлов в `Features/Savings/` компилируются.
- `xcodegen generate` пересобирает `.xcodeproj` чтобы захватить новые файлы.
- `make build` clean.
- FeaturesV10/Savings/* + MainShell.swift untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/62-savings-goals-v06/62-CONTEXT.md
@.planning/phases/61-plan-editor-v06/61-01-SUMMARY.md
@.planning/phases/60-accounts-v06/60-01-SUMMARY.md
@ios/BudgetPlanner/Features/Management/ManagementView.swift
@ios/BudgetPlanner/Networking/DTO/SavingsDTO.swift
@ios/BudgetPlanner/Networking/DTO/GoalDTO.swift
@ios/BudgetPlanner/Networking/DTO/AccountDTO.swift
@ios/BudgetPlanner/Networking/Endpoints/SavingsAPI.swift
@ios/BudgetPlanner/Networking/Endpoints/GoalsAPI.swift
@ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift
@ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift

<interfaces>
<!-- Key types/contracts. DO NOT re-explore the codebase. -->

From ios/BudgetPlanner/Features/Management/ManagementView.swift (existing, post-Phase 61):
```swift
enum ID: String, Hashable {
    case analytics, subscriptions, template, accounts, categories, settings, access, planEditor
}

static let all: [ManagementItem] = [
    .init(id: .analytics, ...),
    .init(id: .planEditor, ...),
    .init(id: .subscriptions, ...),
    .init(id: .template, ...),       // <-- ВСТАВИТЬ .savings ПЕРЕД ЭТИМ
    .init(id: .accounts, ...),
    .init(id: .categories, ...),
    .init(id: .settings, ...),
    .init(id: .access, ..., ownerOnly: true),
]

// destination switch already covers all existing IDs; ДОБАВИТЬ case .savings: SavingsView().
```

From ios/BudgetPlanner/Networking/DTO/SavingsDTO.swift (existing):
```swift
struct SavingsConfigDTO: Codable, Equatable {
    let roundupEnabled: Bool
    let roundupBase: Int  // ∈ {10, 50, 100}
}

struct SavingsSummaryDTO: Decodable, Equatable {
    let totalCents: Int
    let monthInCents: Int
    let config: SavingsConfigDTO
    let goals: [GoalDTO]
}

struct DepositResponseDTO: Decodable, Equatable {
    let id: Int
    let amountCents: Int  // signed
    let accountId: Int?
    let categoryId: Int
    let txDate: Date
    let description: String?
}
```

From ios/BudgetPlanner/Networking/DTO/GoalDTO.swift (existing):
```swift
struct GoalDTO: Decodable, Identifiable, Equatable {
    let id: Int
    let name: String
    let targetCents: Int
    let currentCents: Int
    let due: Date?
    let createdAt: Date
}

struct GoalCreateRequest: Encodable {
    let name: String
    let targetCents: Int
    let due: Date?
    // Custom Encodable serialises due как yyyy-MM-dd для backend Pydantic parse.
}
```

From ios/BudgetPlanner/Networking/Endpoints/SavingsAPI.swift (existing):
```swift
@MainActor
enum SavingsAPI {
    static func summary() async throws -> SavingsSummaryDTO
    static func patchConfig(roundupEnabled: Bool? = nil, roundupBase: Int? = nil) async throws -> SavingsConfigDTO
    static func postDeposit(amountCents: Int, accountId: Int, goalId: Int?) async throws -> DepositResponseDTO
}
```

From ios/BudgetPlanner/Networking/Endpoints/GoalsAPI.swift (existing):
```swift
@MainActor
enum GoalsAPI {
    static func list() async throws -> [GoalDTO]
    static func create(_ req: GoalCreateRequest) async throws -> GoalDTO
    static func delete(id: Int) async throws
}
```

From ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift (existing):
```swift
@MainActor
enum AccountsAPI {
    static func list() async throws -> [AccountDTO]
    static func create(_ request: AccountCreateRequest) async throws -> AccountDTO
}
```

From ios/BudgetPlanner/Features/PlanEditor/PlanEditorRoute.swift (existing pattern reference):
```swift
enum PlanEditorRoute: Hashable {
    case row(categoryId: Int)
}
// SavingsRoute mirror'ит этот pattern: own typed enum избегает collision
// с AccountsView Int.self destination в shared NavigationStack.
```

Coexistence guard (CRITICAL — Swift symbol collision):
- `FeaturesV10/Savings/NewGoalSheet.swift` определяет `struct NewGoalSheet` в том же таргете BudgetPlanner.
- `FeaturesV10/Savings/DepositSheet.swift` определяет `struct DepositSheet`.
- `FeaturesV10/Savings/SavingsData.swift` определяет `enum SavingsData`.
- Swift НЕ разрешает два type с одинаковым именем в одном модуле. Поэтому в Features/Savings/:
  - V10 NewGoalSheet → v06 `SavingsNewGoalSheet` (в файле SavingsNewGoalSheet.swift)
  - V10 DepositSheet → v06 `SavingsDepositSheet` (в файле SavingsDepositSheet.swift)
  - V10 SavingsData → **COLLISION!** Та же ситуация что и в Phase 60 (AccountsData). **Решение для нашего модуля**: используем уникальное имя `SavingsViewData` (а не `SavingsData`) для v06 helpers enum. Файл `Features/Savings/SavingsData.swift` называет enum `SavingsViewData` чтобы не конфликтовать с FeaturesV10/Savings/SavingsData.swift.
- ВЕРИФИЦИРОВАНО: оба файла будут в одном таргете BudgetPlanner; Swift не разрешает collision → MUST rename our types. Имя файла остаётся `SavingsData.swift` (filesystem path не уникализирует Swift typealias) — пользователю удобнее искать `Features/Savings/SavingsData.swift`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register ManagementItem.savings в ManagementView.swift</name>
  <files>ios/BudgetPlanner/Features/Management/ManagementView.swift</files>
  <action>
    Внести три модификации в `ios/BudgetPlanner/Features/Management/ManagementView.swift`:

    1. **Enum `ManagementItem.ID`** (line ~134): добавить `.savings` case. Точная замена:
       ```swift
       // ДО (post-Phase 61):
       enum ID: String, Hashable {
           case analytics, subscriptions, template, accounts, categories, settings, access, planEditor
       }
       // ПОСЛЕ:
       enum ID: String, Hashable {
           case analytics, subscriptions, template, accounts, categories, settings, access, planEditor, savings
       }
       ```
       (Add `savings` в конце enum — order rawValue не важен для UI; `ManagementItem.all` определяет порядок отображения.)

    2. **Массив `ManagementItem.all`**: вставить новый `.init(...)` entry для `.savings` **между** `.subscriptions` и `.template` entries (CONTEXT D-1: «перед `.template`»). Шаблон:
       ```swift
       .init(id: .savings, label: "Копилка",
             description: "Цели и накопления",
             icon: "banknote.fill", ownerOnly: false),
       ```

    3. **Метод `destination(for:)`** (line ~108): добавить case `.savings: SavingsView()`. Добавлять можно в любом порядке внутри switch, но логически — рядом с `.planEditor` (новые v06 домены вместе). Шаблон:
       ```swift
       case .savings: SavingsView()
       ```

    NO иных изменений в файле. Сохранить порядок остальных case-ов / entry-ей.

    **Coexistence guard**: НЕ трогать `MainShell.swift`. НЕ трогать `FeaturesV10/Savings/*`. Только `ManagementView.swift`.

    После этого таска build будет failing до Task 2 (SavingsView пока не существует). Это acceptable plan-boundary breakage; Task 2 закрывает.
  </action>
  <verify>
    <automated>grep -c "case analytics, subscriptions, template, accounts, categories, settings, access, planEditor, savings" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (enum case list updated). -->
    <automated>grep -c "id: .savings, label: \"Копилка\"" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (ManagementItem entry). -->
    <automated>grep -c "case .savings: SavingsView()" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (destination dispatch). -->
    <automated>grep -c "banknote.fill" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (icon per CONTEXT D-1). -->
  </verify>
  <done>
    Все четыре grep gates pass. Build будет broken до завершения Task 2 (SavingsView не существует) — acceptable plan-boundary state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 8 scaffold files в Features/Savings/ + regenerate Xcode project</name>
  <files>ios/BudgetPlanner/Features/Savings/SavingsRoute.swift, ios/BudgetPlanner/Features/Savings/SavingsData.swift, ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift, ios/BudgetPlanner/Features/Savings/SavingsView.swift, ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift, ios/BudgetPlanner/Features/Savings/GoalDetailView.swift, ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift, ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift</files>
  <action>
    Создать каталог `ios/BudgetPlanner/Features/Savings/` и восемь stub-файлов. Все stubs должны компилироваться (типы определены полностью; body / load() заполняется в downstream plans 62-02, 62-03).

    **File 1: `ios/BudgetPlanner/Features/Savings/SavingsRoute.swift`** (typed-route enum):
    ```swift
    import Foundation

    /// Phase 62 — typed-route enum для Savings (Копилка) NavigationLinks.
    ///
    /// Используется вместо `Int` чтобы избежать collision с
    /// `AccountsView.navigationDestination(for: Int.self)` и
    /// `PlanEditorView.navigationDestination(for: PlanEditorRoute.self)`
    /// в shared ManagementView NavigationStack. Когда user находится
    /// на /accounts, Int-binding уже занят AccountDetailView push'ем —
    /// поэтому Savings использует свой typed enum.
    ///
    /// Hashable conformance обязательна для NavigationLink(value:) API.
    enum SavingsRoute: Hashable {
        /// Push на детальный view одной цели.
        case goal(id: Int)
    }
    ```

    **File 2: `ios/BudgetPlanner/Features/Savings/SavingsData.swift`** (pure helpers stub — заполняется в 62-02):
    ```swift
    // Phase 62 — pure-compute helpers для v06 Savings (Копилка) screen.
    //
    // Stub (Plan 62-01). Полная реализация — Plan 62-02:
    //   - progressPercentage(currentCents:targetCents:) -> Int (0..100)
    //   - formatDue(_:calendar:) -> String? («до D <month_genitive> YYYY»)
    //   - sortGoalsForDisplay(_:) -> [GoalDTO]
    //   - isValidGoalDraft(name:targetCents:) -> Bool
    //   - isValidDepositDraft(amountCents:accountId:) -> Bool
    //
    // **Naming**: enum `SavingsViewData` (не `SavingsData`) — избегаем Swift
    // symbol collision с FeaturesV10/Savings/SavingsData.swift в том же
    // таргете BudgetPlanner. Имя файла остаётся SavingsData.swift для
    // удобства поиска.
    //
    // Foundation only — no SwiftUI imports — unit-test cheaply.

    import Foundation

    enum SavingsViewData {
        // Plan 62-02 fills helpers.
    }
    ```

    **File 3: `ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift`** (VM stub):
    ```swift
    import Foundation
    import Observation

    /// Phase 62 — SavingsViewModel для v06 SavingsView (master list).
    ///
    /// Stub (Plan 62-01). Полная реализация load() / toggleRoundup /
    /// selectBase / createGoal / deleteGoal / deposit — Plan 62-02.
    ///
    /// Pattern: parallel to V10 SavingsV10ViewModel (FeaturesV10), но v06
    /// native shell — никакого poster-styling. Discriminated SheetMode для
    /// dual-sheet state (new goal + deposit с optional pre-filled goalId).
    @MainActor
    @Observable
    final class SavingsViewModel {
        enum Status: Equatable {
            case idle
            case loading
            case ready
            case error(String)
        }

        /// Discriminated sheet state — устраняет multi-boolean
        /// inconsistency class of bug. `.deposit(goalId:)` carries optional
        /// pre-selected goal так goal-row tap может flow напрямую в
        /// pre-filled DepositSheet.
        enum SheetMode: Equatable {
            case none
            case newGoal
            case deposit(goalId: Int?)
        }

        // MARK: - State

        private(set) var status: Status = .idle
        private(set) var snapshot: SavingsSummaryDTO? = nil
        private(set) var accounts: [AccountDTO] = []

        var sheet: SheetMode = .none
        private(set) var submitting: Bool = false

        /// Filtered Russian copy на mutation failure (T-62-03 mitigation):
        /// «Не удалось создать цель», «Не удалось пополнить», «Не удалось
        /// удалить цель», «Не удалось обновить округление». UI читает в
        /// banner Section. Cleared автоматически на next successful mutation
        /// or manually через clearMutationError().
        var mutationError: String? = nil

        /// Set by createGoal() on success — triggers ScrollViewReader
        /// scrollTo в SavingsView. Cleared after consumption.
        var lastCreatedGoalId: Int? = nil

        @ObservationIgnored
        private var inFlight: Bool = false

        // MARK: - Load (filled in 62-02)
        func load() async {
            // Plan 62-02 fills this body.
        }

        // MARK: - Mutations (filled in 62-02)
        func toggleRoundup(_ enabled: Bool) async {
            // Plan 62-02 fills.
        }

        func selectBase(_ base: Int) async {
            // Plan 62-02 fills.
        }

        func createGoal(name: String, targetCents: Int, due: Date?) async -> Bool {
            // Plan 62-02 fills. Returns true on success.
            return false
        }

        func deleteGoal(id: Int) async {
            // Plan 62-02 fills.
        }

        func deposit(amountCents: Int, accountId: Int, goalId: Int?) async -> Bool {
            // Plan 62-02 fills. Returns true on success.
            return false
        }

        // MARK: - Helpers
        func clearMutationError() {
            self.mutationError = nil
        }

        func clearLastCreatedGoalId() {
            self.lastCreatedGoalId = nil
        }

        // MARK: - Derived
        var totalCents: Int { snapshot?.totalCents ?? 0 }
        var monthInCents: Int { snapshot?.monthInCents ?? 0 }
        var goals: [GoalDTO] { snapshot?.goals ?? [] }
        var roundupEnabled: Bool { snapshot?.config.roundupEnabled ?? false }
        var roundupBase: Int { snapshot?.config.roundupBase ?? 50 }
    }
    ```

    **File 4: `ios/BudgetPlanner/Features/Savings/SavingsView.swift`** (master view stub):
    ```swift
    import SwiftUI

    /// Phase 62 — native SavingsView (Копилка master list).
    ///
    /// Stub (Plan 62-01) — placeholder body. Реальный List(.insetGrouped)
    /// с Hero / Roundup / Goals sections / swipe-to-delete / Menu toolbar
    /// landing в Plan 62-02. NavigationStack принадлежит родителю
    /// (ManagementView) — здесь только .navigationTitle("Копилка") +
    /// destination dispatch для SavingsRoute.
    struct SavingsView: View {
        @State private var viewModel = SavingsViewModel()

        var body: some View {
            List {
                Section {
                    ProgressView("Загрузка…")
                        .frame(maxWidth: .infinity)
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Копилка")
            .task { await viewModel.load() }
        }
    }
    ```

    **File 5: `ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift`** (detail VM stub):
    ```swift
    import Foundation
    import Observation

    /// Phase 62 — GoalDetailViewModel для v06 GoalDetailView.
    ///
    /// Stub (Plan 62-01). Полная реализация load() / delete() / accounts
    /// fetch — Plan 62-03.
    ///
    /// T-62-03 (Information Disclosure) — mitigation:
    ///   - cross-tenant / missing id collapses в одно сообщение «Цель
    ///     не найдена» (single message без existence leak);
    ///   - outer catch выдаёт фиксированный Russian copy «Не удалось
    ///     загрузить цель»; raw error → print().
    @MainActor
    @Observable
    final class GoalDetailViewModel {
        enum Status: Equatable {
            case idle
            case loading
            case ready
            case error(String)
        }

        let goalId: Int

        private(set) var status: Status = .idle
        private(set) var goal: GoalDTO?
        private(set) var accounts: [AccountDTO] = []
        private(set) var submitting: Bool = false

        /// Filtered Russian copy на delete failure (T-62-03).
        var mutationError: String? = nil

        @ObservationIgnored
        private var inFlight: Bool = false

        init(goalId: Int) {
            self.goalId = goalId
        }

        // MARK: - Load (filled in 62-03)
        func load() async {
            // Plan 62-03 fills this body.
        }

        // MARK: - Mutations (filled in 62-03)
        func deleteGoal() async -> Bool {
            // Plan 62-03 fills. Returns true on success (caller dismisses).
            return false
        }

        // MARK: - Helpers
        func clearMutationError() {
            self.mutationError = nil
        }
    }
    ```

    **File 6: `ios/BudgetPlanner/Features/Savings/GoalDetailView.swift`** (detail view stub):
    ```swift
    import SwiftUI

    /// Phase 62 — native GoalDetailView (Копилка goal detail).
    ///
    /// Stub (Plan 62-01) — placeholder body. Реальный Hero (name +
    /// progress + cents/target + due) + Deposit CTA + delete Menu
    /// landing в Plan 62-03.
    struct GoalDetailView: View {
        let goalId: Int

        @State private var viewModel: GoalDetailViewModel

        init(goalId: Int) {
            self.goalId = goalId
            self._viewModel = State(wrappedValue: GoalDetailViewModel(goalId: goalId))
        }

        var body: some View {
            List {
                Section {
                    ProgressView("Загрузка…")
                        .frame(maxWidth: .infinity)
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Цель")
            .navigationBarTitleDisplayMode(.inline)
            .task { await viewModel.load() }
        }
    }
    ```

    **File 7: `ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift`** (sheet stub):
    ```swift
    import SwiftUI

    /// Phase 62 — native Form sheet для создания цели.
    ///
    /// **Symbol-collision avoidance**: FeaturesV10/Savings/NewGoalSheet.swift
    /// уже определяет `struct NewGoalSheet` в том же модуле BudgetPlanner.
    /// Swift не разрешает два struct с одинаковым именем в одном модуле,
    /// поэтому v06 native sheet называется `SavingsNewGoalSheet`. Plan 62-03
    /// заполняет полный Form body (TextField name + MoneyParser target +
    /// optional DatePicker due + live validation).
    struct SavingsNewGoalSheet: View {
        let submitting: Bool
        let onCreate: (_ name: String, _ targetCents: Int, _ due: Date?) async -> Bool
        let onCancel: () -> Void

        var body: some View {
            // Plan 62-03 fills this body with native Form.
            NavigationStack {
                Form {
                    Section {
                        Text("Plan 62-03 заполнит этот sheet")
                            .foregroundStyle(.secondary)
                    }
                }
                .navigationTitle("Новая цель")
                .navigationBarTitleDisplayMode(.inline)
                .toolbar {
                    ToolbarItem(placement: .cancellationAction) {
                        Button("Отмена") { onCancel() }
                    }
                }
            }
            .presentationDetents([.medium, .large])
        }
    }
    ```

    **File 8: `ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift`** (sheet stub):
    ```swift
    import SwiftUI

    /// Phase 62 — native Form sheet для пополнения копилки.
    ///
    /// **Symbol-collision avoidance**: FeaturesV10/Savings/DepositSheet.swift
    /// уже определяет `struct DepositSheet`. Поэтому v06 native sheet
    /// называется `SavingsDepositSheet`. Plan 62-03 заполняет полный Form
    /// body (optional goal Picker pre-filled / MoneyParser amount /
    /// account Picker required / live validation).
    struct SavingsDepositSheet: View {
        let submitting: Bool
        let goals: [GoalDTO]
        let accounts: [AccountDTO]
        let initialGoalId: Int?
        let onDeposit: (_ amountCents: Int, _ accountId: Int, _ goalId: Int?) async -> Bool
        let onCancel: () -> Void

        var body: some View {
            // Plan 62-03 fills this body with native Form.
            NavigationStack {
                Form {
                    Section {
                        Text("Plan 62-03 заполнит этот sheet")
                            .foregroundStyle(.secondary)
                    }
                }
                .navigationTitle("Пополнение")
                .navigationBarTitleDisplayMode(.inline)
                .toolbar {
                    ToolbarItem(placement: .cancellationAction) {
                        Button("Отмена") { onCancel() }
                    }
                }
            }
            .presentationDetents([.medium, .large])
        }
    }
    ```

    После создания всех восьми файлов:
    1. Запустить `cd ios && xcodegen generate` чтобы `.xcodeproj` подхватил новые файлы (project.yml использует sources path `BudgetPlanner` → автоматически включит Features/Savings/).
    2. Запустить `cd ios && make build` чтобы убедиться что сборка clean.

    **Coexistence guards (mandatory verify)**:
    - FeaturesV10/Savings/* НЕ модифицирован — `git diff` должен показать только Features/Savings/* и ManagementView.swift в diff.
    - MainShell.swift НЕ модифицирован.

    **Symbol collision check**: оба `NewGoalSheet.swift` файла существуют (один в FeaturesV10/Savings/, struct `NewGoalSheet`), один в Features/Savings/SavingsNewGoalSheet.swift со struct `SavingsNewGoalSheet`. Аналогично для DepositSheet. Аналогично для SavingsData enum (V10 `SavingsData` vs v06 `SavingsViewData`). Swift compiler не ругается.

    **DO NOT**:
    - НЕ создавать unit tests в этом плане (придут в 62-02 для SavingsViewModel + SavingsViewData, 62-03 для GoalDetailViewModel + sheet validations).
    - НЕ заполнять load() / mutation bodies — это будет работой 62-02 / 62-03; downstream plans переписывают.
    - НЕ модифицировать DTO / API файлы — все типы существуют.
    - НЕ делать `import FeaturesV10` или симметричное — наши stubs самодостаточны.
  </action>
  <verify>
    <automated>test -d ios/BudgetPlanner/Features/Savings</automated>
    <!-- Каталог создан. -->
    <automated>test -f ios/BudgetPlanner/Features/Savings/SavingsRoute.swift && test -f ios/BudgetPlanner/Features/Savings/SavingsData.swift && test -f ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift && test -f ios/BudgetPlanner/Features/Savings/SavingsView.swift && test -f ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift && test -f ios/BudgetPlanner/Features/Savings/GoalDetailView.swift && test -f ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift && test -f ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift</automated>
    <!-- Все восемь файлов созданы. -->
    <automated>grep -c "enum SavingsRoute: Hashable" ios/BudgetPlanner/Features/Savings/SavingsRoute.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "case goal(id: Int)" ios/BudgetPlanner/Features/Savings/SavingsRoute.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "enum SavingsViewData" ios/BudgetPlanner/Features/Savings/SavingsData.swift</automated>
    <!-- Expected: 1 (renamed to avoid collision) -->
    <automated>grep -c "enum SavingsData" ios/BudgetPlanner/Features/Savings/SavingsData.swift</automated>
    <!-- Expected: 0 (no collision with FeaturesV10) -->
    <automated>grep -c "final class SavingsViewModel" ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "case deposit(goalId: Int?)" ios/BudgetPlanner/Features/Savings/SavingsViewModel.swift</automated>
    <!-- Expected: 1 (discriminated SheetMode per CONTEXT D-1/D-2). -->
    <automated>grep -c "struct SavingsView" ios/BudgetPlanner/Features/Savings/SavingsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "final class GoalDetailViewModel" ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "let goalId: Int" ios/BudgetPlanner/Features/Savings/GoalDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "struct GoalDetailView" ios/BudgetPlanner/Features/Savings/GoalDetailView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "struct SavingsNewGoalSheet" ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift</automated>
    <!-- Expected: 1 (renamed to avoid V10 collision) -->
    <automated>grep -c "struct NewGoalSheet" ios/BudgetPlanner/Features/Savings/SavingsNewGoalSheet.swift</automated>
    <!-- Expected: 0 (no collision with FeaturesV10) -->
    <automated>grep -c "struct SavingsDepositSheet" ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift</automated>
    <!-- Expected: 1 (renamed to avoid V10 collision) -->
    <automated>grep -c "struct DepositSheet" ios/BudgetPlanner/Features/Savings/SavingsDepositSheet.swift</automated>
    <!-- Expected: 0 (no collision with FeaturesV10) -->
    <automated>cd ios && xcodegen generate 2>&1 | tail -3</automated>
    <!-- xcodegen output должно быть clean. -->
    <automated>cd ios && make build 2>&1 | tail -20 | grep -cE "Build Succeeded|BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 — make build clean. -->
    <automated>git status --porcelain ios/BudgetPlanner/FeaturesV10/Savings/ | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 — FeaturesV10/Savings/* untouched. -->
    <automated>git status --porcelain ios/BudgetPlanner/MainShell.swift | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 — MainShell.swift untouched. -->
  </verify>
  <done>
    Все grep gates pass; `make build` succeeded; `git status` confirms scope. Plan 62-01 готов; downstream plans (62-02, 62-03) заполнят body / load / mutation logic в соответствующих файлах.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app ↔ /api/v1/savings + /api/v1/goals | Untrusted backend; DTO decoded через APIClient.shared.decoder. В этом плане только типы — runtime fetch в 62-02. |
| User → UI (tap navigation) | NavigationLink(value: .savings) → SavingsView; не несёт untrusted input в этом плане. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-05 | Stale-state | Stub VM properties | accept | В stubs нет live state; `load()` no-op возвращает immediately. В 62-02 будет добавлен full reload pattern (snapshot refetch после createGoal/deposit/patchConfig). В этом плане нет stale-state risk. |
</threat_model>

<verification>
- `grep -c "case .savings: SavingsView()" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 1.
- `grep -c "id: .savings, label: \"Копилка\"" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 1.
- Все 8 файлов в `ios/BudgetPlanner/Features/Savings/` существуют (test -f).
- `cd ios && make build 2>&1 | tail -5 | grep -cE "Build Succeeded|BUILD SUCCEEDED"` returns ≥1.
- `git status --porcelain ios/BudgetPlanner/FeaturesV10/Savings/` returns пусто (0 lines).
- `git status --porcelain ios/BudgetPlanner/MainShell.swift` returns пусто (0 lines).
- All renamed types: `SavingsViewData` (не SavingsData), `SavingsNewGoalSheet` (не NewGoalSheet), `SavingsDepositSheet` (не DepositSheet) — Swift symbol-collision avoidance.
</verification>

<success_criteria>
1. ManagementItem.ID enum расширен `.savings` case.
2. ManagementItem.all массив содержит entry «Копилка» с icon `banknote.fill`, ownerOnly: false, в позиции перед `.template` entry.
3. ManagementView.destination(for:) обрабатывает `.savings → SavingsView()`.
4. 8 stub-файлов созданы в `ios/BudgetPlanner/Features/Savings/`: SavingsRoute, SavingsData, SavingsViewModel, SavingsView, GoalDetailViewModel, GoalDetailView, SavingsNewGoalSheet, SavingsDepositSheet.
5. SavingsRoute — typed enum `case goal(id: Int)` (Hashable).
6. SavingsData — enum `SavingsViewData` (renamed) — заглушка для 62-02 helpers.
7. SavingsViewModel определяет `Status` enum, `SheetMode` enum (`.none / .newGoal / .deposit(goalId: Int?)`), `snapshot: SavingsSummaryDTO?`, `accounts: [AccountDTO]`, `sheet`, `submitting`, `mutationError: String?`, `lastCreatedGoalId: Int?`, derived getters (totalCents / monthInCents / goals / roundupEnabled / roundupBase).
8. GoalDetailViewModel определяет `goalId: Int` (let), Status enum, `goal: GoalDTO?`, `accounts: [AccountDTO]`, `submitting`, `mutationError: String?`.
9. SavingsNewGoalSheet.swift содержит `struct SavingsNewGoalSheet` (rename — избегаем collision с FeaturesV10/Savings/NewGoalSheet.swift).
10. SavingsDepositSheet.swift содержит `struct SavingsDepositSheet` (rename — избегаем collision с FeaturesV10/Savings/DepositSheet.swift).
11. SavingsData.swift содержит `enum SavingsViewData` (rename — избегаем collision с FeaturesV10/Savings/SavingsData.swift).
12. `xcodegen generate` и `make build` clean.
13. FeaturesV10/Savings/* и MainShell.swift НЕ модифицированы (git status confirms).
</success_criteria>

<output>
After completion, create `.planning/phases/62-savings-goals-v06/62-01-SUMMARY.md` summarising:
- ManagementItem registration (3 правки в ManagementView.swift)
- 8 stub files созданы в Features/Savings/
- Symbol-collision resolution (3 renames: SavingsViewData / SavingsNewGoalSheet / SavingsDepositSheet)
- Build verification (make build SUCCEEDED)
- Coexistence guard verification (git status confirms FeaturesV10/Savings/* + MainShell.swift untouched)
- Stub interface surface для downstream plans 62-02 / 62-03
</output>
