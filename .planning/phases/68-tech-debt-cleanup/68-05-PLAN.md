---
phase: 68-tech-debt-cleanup
plan: 05
type: execute
wave: 3
depends_on: [68-02]
files_modified:
  - tests/conftest.py
  - tests/helpers/seed.py
  - tests/helpers/onboarding.py
  - tests/test_actual_crud.py
  - tests/test_planned.py
  - tests/test_templates.py
  - tests/test_snapshot.py
  - tests/test_roundup.py
  - tests/test_onboarding.py
  - tests/test_onboarding_v10.py
  - tests/test_migrations_v1_0.py
  - tests/test_rls_policy.py
  - tests/test_admin_users_api.py
autonomous: true
requirements: [A2-suite]
must_haves:
  truths:
    - "Full backend suite `pytest tests/ -q` is green: 0 failed, 0 errors (skips/xfail acceptable)"
    - "No raw Category(...) constructor remains in tests that bypasses seed_category() NOT-NULL code/ord"
    - "onboarding-v10 fixtures grant pdn_consent_at (no PdnConsentRequiredError)"
    - "Legacy onboarding-body tests migrated to the v1.0 contract (income_cents/accounts/category_plans + consent)"
    - "All changes are test-side (tests/, conftest, helpers); zero product/route/migration changes"
  artifacts:
    - path: "tests/helpers/seed.py"
      provides: "seed_user grants optional pdn_consent_at; seed_category authoritative for code/ord"
      contains: "pdn_consent_at"
    - path: "tests/helpers/onboarding.py"
      provides: "shared complete_onboarding_v10(client, headers, ...) helper for v1.0 body"
      contains: "def complete_onboarding_v10"
---

# Plan 68-05: Full backend suite green (extend A2 systemic cleanup suite-wide)

## Objective
Phase 68 success criterion 1 requires **backend pytest fully green — no pre-existing failures**.
Verification found 62 failed + 64 errors, ALL pre-existing TEST-DEBT (confirmed reproduced on parent
commit `3ad115d`; zero product bugs — full triage in agent report). The systemic `seed_category` fix
landed in 68-02 but the broad suite was never migrated onto it. This plan finishes the migration so the
suite is a trusted green baseline for Phase 69 (codegen).

## Root-cause classes (from triage — all TEST-DEBT)
- **A (~70):** raw `Category(...)` seeds bypass `seed_category()` → `null value in column "code"`/`ord`.
  Files: test_actual_crud, test_actual_period, test_apply_template, test_balance, test_internal_bot,
  test_multitenancy_isolation, test_planned, test_snapshot, test_templates, test_close_period_job,
  test_worker_charge, test_security_probes, test_postgres_role_runtime, test_rls_policy setup,
  test_onboarding_concurrent — likely a shared `conftest.py` category fixture is the highest-leverage culprit.
- **B (24):** onboarding-v10 fixtures missing `pdn_consent_at` → `PdnConsentRequiredError`.
  Files: tests/services/test_onboarding_v10.py `fresh_user`, tests/api/test_onboarding_v10_api.py user fixture.
- **C (~13):** stale legacy onboarding body (422 extra_forbidden) → migrate to v1.0 contract.
  Files: test_onboarding, test_onboarding_gate, test_onboarding_existing_user_safety, test_periods,
  test_settings, test_security_probes (sp11/sp12).
- **D (4):** test_roundup re-reads stale ORM identity-map object after raw UPDATE…RETURNING.
  Fix: `await db_session.refresh(acct)` / `expire_all()` before balance re-read.
- **E (3):** stale references to dropped `plan_template_item` table (alembic 0013).
  Files: test_admin_users_api.py, test_rls_policy.py (`..._all_nine_tables` → eight).
- **F (1):** test_migrations_v1_0.py head allow-list capped at 0012–0016; real head is 0026.
- **G (4):** template/snapshot WRITE surface returns 410 Gone — **deprecated by design (Phase 22 CR-05)**.
  DECISION (owner-autonomous): keep endpoints deprecated; update tests to assert 410.
  Files: test_templates.py (update/delete/create-invalid-cat), test_snapshot.py::test_snapshot_period_not_found_404.

## Tasks
### Task 1 — shared seed/onboarding helpers (highest leverage)
- Extend `tests/helpers/seed.py::seed_user` with optional `pdn_consent_at` (mirror the `pro_active_until` pattern from 68-01; default None = no consent, backward-compatible).
- Confirm `seed_category()` remains authoritative for `code`+`ord` (from 68-02).
- Add `tests/helpers/onboarding.py` with `complete_onboarding_v10(client, headers, *, income_cents=..., accounts=..., category_plans=...)` that POSTs the v1.0 body and grants consent — to avoid re-templating in 6 files.
- Fix the shared `tests/conftest.py` category-seeding fixture(s) to route through `seed_category()` first; re-run the suite to measure how many classes clear at once.

### Task 2 — migrate remaining seeds & contracts (classes A, B, C)
- Migrate every raw `Category(...)` in the listed test files to `seed_category()` (multiline-aware: confirm `rg -U --pcre2 '(?<!seed_)Category\(' tests/` returns zero matches outside seed.py).
- Grant `pdn_consent_at` in the onboarding-v10 fixtures (class B).
- Migrate legacy-onboarding-body tests to `complete_onboarding_v10` / v1.0 contract (class C); preserve original test INTENT (don't weaken assertions — adjust counts to real v1.0 contract like 68-02 did: 9 categories, etc.).

### Task 3 — surgical fixes (classes D, E, F, G)
- D: `db_session.refresh(acct)`/`expire_all()` before balance re-read in test_roundup.py.
- E: drop `plan_template_item` from expected-table lists; rename nine→eight in test_rls_policy.
- F: add `0026` to accepted migration head set (or assert `>= 0015`) in test_migrations_v1_0.py.
- G: flip the 4 template/snapshot write asserts to expect 410 (endpoints deprecated by design).

## Verification
- Run inside docker test stack (broken local .venv): rebuild api if needed, then
  `docker compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.test.yml exec -T api /app/.venv/bin/python -m pytest tests/ -q`
- GREEN GATE: 0 failed, 0 errors (skipped/xfail acceptable). Capture the final summary line.
- Confirm zero raw `Category(` outside seed.py: `rg -U --pcre2 '(?<!seed_)Category\(' tests/`.
- `git diff --stat` shows ONLY test-side files (tests/, conftest, helpers) — no app/ changes.
- Restore stack: `docker compose up -d`.

## Notes / discipline
- Test-only; NO product/route/migration changes. Money BIGINT cents no float. Atomic commits per class/group.
- End commit messages with: `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>`
- Pre-existing observation (out of scope): `bot` container crash-loops in local dev (no TG token) — note, don't fix here.
