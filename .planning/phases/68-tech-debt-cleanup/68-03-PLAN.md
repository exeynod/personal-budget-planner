---
phase: 68-tech-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/tsconfig.test.json
  - frontend/src/screensV10/__tests__/TxV10TabDemote.test.tsx
  - frontend/src/screensV10/Ai/__tests__/AiView.test.tsx
  - frontend/src/screensV10/Management/__tests__/SettingsView.test.tsx
autonomous: true
requirements: [A3]
must_haves:
  truths:
    - "@types/node is a devDependency so node:fs / __dirname type-check in tests"
    - "Prop-drift in AiView/SettingsView/TxV10TabDemote test fixtures is fixed"
    - "Test files are back under type-check via a typecheck:test script"
    - "npm run build (prod tsc -b + vite) stays green and still excludes tests"
    - "npx vitest run is green"
  artifacts:
    - path: "frontend/package.json"
      provides: "@types/node devDep + typecheck:test script"
      contains: "typecheck:test"
    - path: "frontend/tsconfig.test.json"
      provides: "test-only type-check project including test files + node types"
      contains: "test"
  key_links:
    - from: "frontend/package.json typecheck:test"
      to: "frontend/tsconfig.test.json"
      via: "tsc -p tsconfig.test.json --noEmit"
      pattern: "tsconfig.test.json"
    - from: "test files"
      to: "@types/node"
      via: "node:fs / path / __dirname resolve under typecheck:test"
      pattern: "@types/node"
---

<objective>
Close **A3** (web tsc test-gate, R5/FE-F3 tail). Phase 67 excluded test files from
the prod `tsc -b` (tsconfig.app.json `exclude`) to unblock the build. Pre-existing
type errors remained in tests:
- `node:fs` / `path` / `__dirname` fail to type-check without `@types/node`
  (confirmed: TxV10TabDemote.test.tsx imports `node:fs` + `node:path`).
- prop-drift in `AiView.test.tsx`, `SettingsView.test.tsx`, `TxV10TabDemote.test.tsx`
  (test fixtures pass props that drifted from current component prop types).

Fix:
1. `npm i -D @types/node`.
2. Fix the prop fixtures in the three `.test.tsx` so they match current component
   prop types.
3. Return tests under type-check via a **separate `typecheck:test` script** +
   `tsconfig.test.json` (Claude's-discretion choice from CONTEXT — keeps prod
   `tsc -b` fast and the test exclusions in tsconfig.app.json intact, while
   re-covering tests). Both gates run: prod `npm run build` AND `npm run typecheck:test`.

Purpose: green web baseline (build + test-typecheck + vitest) — gate for Phase 69
codegen migration.
Output: @types/node installed, prop fixtures fixed, typecheck:test script, all green.

NOTE: web is a codebase-independent stack → this plan runs in Wave 1, parallel
with the backend A1 plan (68-01) and the iOS A4 plan (68-04). Zero file overlap
with those plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE SPEC — read §ФАЗА 68 A3 (lines 48-52) + "Окружение и дисциплина" (135-141)
@.planning/CONVERGENCE-AND-DEBT-PLAN.md

<interfaces>
<!-- frontend/tsconfig.json: solution file, references tsconfig.node.json + tsconfig.app.json -->
<!-- frontend/tsconfig.app.json: include ["src"], exclude ["src/**/__tests__/**","src/**/*.test.ts","src/**/*.test.tsx"] (Phase 67) -->
<!-- frontend/package.json scripts: "build": "tsc -b && vite build", "test": "vitest run" — NO typecheck:test, NO @types/node -->
<!-- TxV10TabDemote.test.tsx imports: `import fs from 'node:fs'`, `import path from 'node:path'` (need @types/node) -->
<!-- The three test files: src/screensV10/__tests__/TxV10TabDemote.test.tsx, src/screensV10/Ai/__tests__/AiView.test.tsx, src/screensV10/Management/__tests__/SettingsView.test.tsx -->
</interfaces>

<!-- ENVIRONMENT: web stack. All commands under frontend/. -->
<!-- Gates: `cd frontend && npm run build` (tsc -b + vite) AND `npm run typecheck:test` AND `npx vitest run` must all be green. -->
<!-- Do NOT re-enable test files in tsconfig.app.json (that would slow/redden the prod build); use a dedicated test project. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @types/node + tsconfig.test.json + typecheck:test script</name>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/tsconfig.test.json</files>
  <read_first>
    - frontend/package.json (scripts + devDependencies)
    - frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json
  </read_first>
  <action>
    1. `cd frontend && npm i -D @types/node` (matching the project's Node/TS major;
       pin a version compatible with TS ^5.6).
    2. Create `frontend/tsconfig.test.json` extending tsconfig.app.json's compiler
       options but: (a) `include` the test files and any test setup, (b) add
       `"types": ["node", "vitest/globals"]` (or appropriate) + DOM libs so
       `node:fs`/`path`/`__dirname` and vitest globals resolve, (c) `noEmit: true`,
       (d) its own tsBuildInfoFile to avoid clobbering the app build cache. Do NOT
       remove the test `exclude` from tsconfig.app.json — prod build stays test-free.
    3. Add a `"typecheck:test": "tsc -p tsconfig.test.json --noEmit"` script to
       package.json. Leave `"build"` and `"test"` unchanged.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && node -e "const p=require('./package.json'); if(!p.scripts['typecheck:test']) throw new Error('no typecheck:test'); if(!(p.devDependencies['@types/node'])) throw new Error('no @types/node')" && test -f tsconfig.test.json</automated>
  </verify>
  <done>@types/node in devDependencies; tsconfig.test.json includes test files + node types; typecheck:test script present; tsconfig.app.json test-exclude untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix prop-drift in the 3 test fixtures; make build + typecheck:test + vitest green</name>
  <files>frontend/src/screensV10/Ai/__tests__/AiView.test.tsx, frontend/src/screensV10/Management/__tests__/SettingsView.test.tsx, frontend/src/screensV10/__tests__/TxV10TabDemote.test.tsx</files>
  <read_first>
    - the three test files
    - the components under test (AiView, SettingsView, and TxV10TabDemote's mounts) for current prop types
  </read_first>
  <behavior>
    - `npm run typecheck:test` reports zero errors (node:fs/path/__dirname + props all resolve).
    - `npm run build` (prod tsc -b + vite) stays green.
    - `npx vitest run` stays green (no behavioural change to the tests).
  </behavior>
  <action>
    Run `npm run typecheck:test` to surface the exact prop-drift errors, then fix
    each test's fixtures so the props match the CURRENT component prop interfaces
    (add missing required props, drop removed ones, correct types). Do NOT change
    production component prop types to match stale tests — update the test fixtures
    to current reality. Keep test assertions/behaviour intact (vitest must still
    pass). The node:fs/path/__dirname errors are resolved by Task 1's @types/node;
    confirm no remaining `any`-casts mask real drift.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm run build && npm run typecheck:test && npx vitest run</automated>
  </verify>
  <done>All three gates green: prod build, typecheck:test (zero errors), vitest run. Prop fixtures match current component types; no production prop-type changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build/CI gate | type-check is a correctness gate; excluding tests hid real type drift |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-03-01 | Tampering | type-check coverage | mitigate | typecheck:test re-covers test files so prop/type drift is caught at build time again; prod build stays test-free and fast. |
| T-68-03-02 | Repudiation | stale test fixtures | accept | Test-only fixture corrections; no production component contract changes — drift fixed at the test, where it belongs. |
</threat_model>

<verification>
- `cd frontend && npm run build` → green (prod tsc -b + vite; tests still excluded).
- `cd frontend && npm run typecheck:test` → zero errors.
- `cd frontend && npx vitest run` → green.
- package.json has @types/node devDep + typecheck:test script; tsconfig.test.json exists.
</verification>

<success_criteria>
@types/node installed; tsconfig.test.json + typecheck:test re-cover test files; prop-drift in 3 test files fixed; prod build + test-typecheck + vitest all green.
</success_criteria>

<output>
After completion, create `.planning/phases/68-tech-debt-cleanup/68-03-SUMMARY.md`
</output>
