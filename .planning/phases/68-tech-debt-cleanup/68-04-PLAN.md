---
phase: 68-tech-debt-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift
autonomous: true
requirements: [A4]
must_haves:
  truths:
    - "The SuggestCategoryDTO doc-comment states the 0.35 threshold (matches backend after P2-5)"
    - "No 0.5 threshold reference remains in AISuggestCategoryAPI.swift"
  artifacts:
    - path: "ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift"
      provides: "doc-comment with correct 0.35 threshold"
      contains: "0.35"
  key_links:
    - from: "AISuggestCategoryAPI.swift doc-comment"
      to: "backend ai_suggest threshold (0.35 after P2-5)"
      via: "comment text reflects the real backend threshold"
      pattern: "0.35"
---

<objective>
Close **A4** (cosmetic, comment-only). The doc-comment on `SuggestCategoryDTO` in
`ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift` says
`categoryId`/`name` are nil when confidence is below its **0.5** threshold. The
backend threshold is **0.35** since Phase 67 P2-5. Update the comment text only —
no code/logic change.

Purpose: remove stale documentation drift; trustworthy baseline before Phase 69.
Output: comment reads 0.35.

NOTE: iOS is a codebase-independent stack → this plan runs in Wave 1, parallel
with backend A1 (68-01) and web A3 (68-03). Zero file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE SPEC — read §ФАЗА 68 A4 (lines 54-55) + "Окружение и дисциплина" (135-141)
@.planning/CONVERGENCE-AND-DEBT-PLAN.md

<interfaces>
<!-- ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift ~line 23: -->
<!--   /// `categoryId`/`name` are nil when the backend confidence is below its 0.5 -->
<!--   /// threshold; the chip is only shown when `categoryId != nil`. -->
<!-- Backend actual threshold after P2-5: 0.35 (see app/api/routes/ai_suggest.py docstring, fixed in Phase 67). -->
</interfaces>

<!-- ENVIRONMENT: A4 is comment-only — no Xcode build needed and the file is hand-written -->
<!-- (NOT generated), so run swift-format on the touched file (cd ios && make format, or swift-format on the file). -->
<!-- No xcodegen needed (no new .swift file). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the threshold comment 0.5 → 0.35 and swift-format the file</name>
  <files>ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift</files>
  <read_first>
    - ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift (the SuggestCategoryDTO doc-comment, ~lines 21-24)
  </read_first>
  <action>
    Edit the doc-comment so "below its 0.5 threshold" becomes "below its 0.35
    threshold" (match the backend post-P2-5 value). Change ONLY the comment text —
    no struct/field/logic change. Then run swift-format on the touched file
    (`cd ios && make format`, or `swift-format -i` on the file) since it is a
    hand-written, non-generated source file.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && grep -q "0.35" ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift && test "$(grep -c '0.5 threshold' ios/BudgetPlanner/Networking/Endpoints/AISuggestCategoryAPI.swift)" = "0"</automated>
  </verify>
  <done>Doc-comment reads 0.35; no "0.5 threshold" string remains; file swift-formatted; no logic change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none) | Comment-only documentation change; no runtime trust boundary touched |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-04-01 | Repudiation | stale doc-comment | mitigate | Comment now matches backend 0.35 threshold; eliminates misleading documentation. No code path affected. |
</threat_model>

<verification>
- grep "0.35" present in AISuggestCategoryAPI.swift.
- grep "0.5 threshold" absent.
- File swift-formatted (no diff churn beyond the comment + formatting).
</verification>

<success_criteria>
The SuggestCategoryDTO doc-comment states the 0.35 threshold; no 0.5 reference remains; file swift-formatted; comment-only change.
</success_criteria>

<output>
After completion, create `.planning/phases/68-tech-debt-cleanup/68-04-SUMMARY.md`
</output>
