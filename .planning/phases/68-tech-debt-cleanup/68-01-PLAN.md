---
phase: 68-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/helpers/seed.py
  - tests/test_ai_cap_integration.py
  - tests/test_spend_cap_concurrent.py
  - app/api/dependencies.py
autonomous: true
requirements: [A1]
must_haves:
  truths:
    - "A non-pro user hitting an AI cap endpoint gets 402 PRO_TIER_REQUIRED"
    - "A pro user over the spend cap gets 429 spending_cap_exceeded (not 402)"
    - "tests/test_ai_cap_integration.py (4 cap tests) pass: 429 on pro-over-cap (incl. test_chat_blocked_when_at_cap_returns_429 at :76, which seeds an owner at cap=100/spend=100 — must be Pro to get 429 not 402)"
    - "tests/test_spend_cap_concurrent.py (2 tests) pass: one-pass-one-429 / both-pass"
    - "seed_user can produce a Pro user (paid or reverse-trial) for cap tests"
  artifacts:
    - path: "tests/helpers/seed.py"
      provides: "seed_user with optional pro/trial parameters so cap tests seed Pro users"
      contains: "def seed_user"
    - path: "tests/test_ai_cap_integration.py"
      provides: "all 4 cap tests seeding Pro users, asserting 429 on over-cap"
      contains: "429"
    - path: "tests/test_spend_cap_concurrent.py"
      provides: "concurrent cap tests seeding Pro users"
      contains: "seed_user"
  key_links:
    - from: "tests/test_ai_cap_integration.py"
      to: "seed_user (Pro)"
      via: "pro_active_until / trial_ends_at seeded so require_pro passes"
      pattern: "seed_user"
    - from: "app/api/routes/ai_suggest.py router deps"
      to: "require_pro then enforce_spending_cap"
      via: "Pro user passes require_pro, then cap fires 429"
      pattern: "enforce_spending_cap"
---

<objective>
Close **A1** (BLOCKER for a clean backend pytest): six tests expect **429**
(spending_cap_exceeded) but receive **402** PRO_TIER_REQUIRED.

Root cause (confirmed from code): the AI routers chain `require_pro` (402) BEFORE
`enforce_spending_cap` (429) at router level. `tests/test_ai_cap_integration.py`
and `tests/test_spend_cap_concurrent.py` seed **free-tier** users via
`seed_user(...)` (which sets no `pro_active_until` / `trial_ends_at`), so
`require_pro` correctly rejects them with 402 before the cap check ever runs.

**Investigate the actual code first** (this is a `checkpoint:decision`-free
spec-driven phase, but the executor must read before editing). The intended
semantics per CONTEXT/spec:
- non-pro user → **402** (correct as-is)
- **pro** user over cap → **429**

The cap tests are about cap enforcement, so they must seed **Pro** users. The
fix is therefore primarily a **fixture fix** (seed Pro), NOT a DI-reorder — the
gate order `require_pro → enforce_spending_cap` is intentional and correct
(`/suggest-category` line 31-36; `/ai/chat` POST line 587 + router-level cap
line 70). Verify this hypothesis empirically before changing anything. If, and
only if, the investigation shows the order is wrong for a legitimate scenario,
adjust ordering — but the default expectation is fixture-fix + bringing test
assertions into line.

Purpose: unblock a fully-green backend pytest baseline (gate for Phase 69).
Output: `seed_user` can mint a Pro user; the 6 cap tests assert and receive 429.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE SPEC — read §ФАЗА 68 A1 (lines 36-40) + "Окружение и дисциплина" (135-141)
@.planning/CONVERGENCE-AND-DEBT-PLAN.md
# Exact failing test names + root-cause hypotheses
@.planning/phases/67-remediation-cleanup/deferred-items.md

<interfaces>
<!-- app/api/dependencies.py:418-448 require_pro → 402 PRO_TIER_REQUIRED via app.services.tier.is_pro -->
<!-- app/api/dependencies.py:451-490 enforce_spending_cap → 429 when spend >= spending_cap_cents -->
<!-- app/api/routes/ai_suggest.py:31-36 router deps: get_current_user, require_onboarded, require_pro, enforce_spending_cap (THIS ORDER) -->
<!-- app/api/routes/ai.py:64-72 chat router deps: get_current_user, require_onboarded, enforce_spending_cap; POST /chat line 587 ALSO Depends(require_pro) -->
<!-- app/services/tier.py:10-27 effective_tier/is_pro: pro_active_until > now OR trial_ends_at > now → Pro -->
<!-- tests/helpers/seed.py:49-69 seed_user(session, *, tg_user_id, tg_chat_id, role, cycle_start_day, onboarded_at) — NO pro/trial params today -->
<!-- AppUser columns of interest: pro_active_until (TIMESTAMPTZ), trial_ends_at (TIMESTAMPTZ), spending_cap_cents (BIGINT) -->
</interfaces>

<!-- ENVIRONMENT (CRITICAL): local .venv is BROKEN. Run pytest inside the docker `api` container. -->
<!-- Setup once: docker compose up -d --build api -->
<!-- Run tests: docker compose exec -T api python -m pytest <paths> -x -q -->
<!-- After the plan: restore the stack to base+dev (docker compose up -d) so other work is unaffected. -->
<!-- Money is BIGINT cents, NO float. Do NOT add alembic migrations (A1 needs none). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empirically confirm A1 root cause, then extend seed_user for Pro users</name>
  <files>app/api/dependencies.py, tests/helpers/seed.py</files>
  <read_first>
    - app/api/dependencies.py (require_pro :418-448, enforce_spending_cap :451-490)
    - app/api/routes/ai_suggest.py (:28-37 router deps order) and app/api/routes/ai.py (:64-72, POST /chat :587)
    - app/services/tier.py (:10-27 is_pro / effective_tier)
    - tests/helpers/seed.py (:49-69 seed_user)
    - .planning/CONVERGENCE-AND-DEBT-PLAN.md §A1 (36-40)
  </read_first>
  <action>
    1. INVESTIGATE FIRST (do not edit until confirmed): bring up the api container
       (`docker compose up -d --build api`) and run the 6 failing tests to capture
       the actual status codes:
         docker compose exec -T api python -m pytest tests/test_ai_cap_integration.py tests/test_spend_cap_concurrent.py -q
       Confirm the 402-before-429 hypothesis: the seeded users are free-tier and
       `require_pro` rejects them. Inspect `is_pro` to confirm exactly which AppUser
       field makes a user Pro (`pro_active_until` future OR `trial_ends_at` future).

    2. Extend `seed_user` in tests/helpers/seed.py with OPTIONAL Pro parameters that
       default to today's free-tier behaviour (backward-compatible — existing callers
       unchanged):
         - `pro_active_until: Optional[datetime] = None`
         - `trial_ends_at: Optional[datetime] = None`
       Pass them through to the `AppUser(...)` constructor. Do NOT change the existing
       signature semantics for callers that omit them.

    3. Do NOT reorder the DI gates unless step 1 proves the order is genuinely wrong
       for a real scenario. The spec hypothesis is fixture-fix (preferred). If you DO
       end up changing app/api/dependencies.py, document why in the SUMMARY; otherwise
       leave dependencies.py untouched (it is listed in files_modified only because
       the investigation may surface a real ordering bug).

    Per CLAUDE.md: money stays BIGINT cents; no float; no alembic migration.
  </action>
  <verify>
    <automated>docker compose exec -T api python -c "import inspect, tests.helpers.seed as s; sig=inspect.signature(s.seed_user); assert 'pro_active_until' in sig.parameters and 'trial_ends_at' in sig.parameters, sig"</automated>
  </verify>
  <done>Root cause confirmed by a captured test run; seed_user accepts pro_active_until/trial_ends_at (backward-compatible); DI order changed only if investigation proved a real bug.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed Pro users in the 6 cap tests so they assert and receive 429</name>
  <files>tests/test_ai_cap_integration.py, tests/test_spend_cap_concurrent.py</files>
  <read_first>
    - tests/test_ai_cap_integration.py — there are FOUR cap tests in this file (the gate runs the WHOLE file, so ALL four must seed Pro):
        1. test_chat_blocked_when_at_cap_returns_429 (:76) — owner, cap=100, spend=100 → POST /ai/chat → 429
        2. test_chat_unblocked_after_admin_patches_cap_higher
        3. test_suggest_category_blocked_when_at_cap
        4. test_cap_zero_blocks_chat_and_suggest
    - tests/test_spend_cap_concurrent.py (test_concurrent_ai_chat_at_cap_yields_one_pass_one_429, test_concurrent_ai_chat_different_users_both_pass)
  </read_first>
  <behavior>
    - test_chat_blocked_when_at_cap_returns_429 (:76): Pro owner, cap=100/spend=100 → POST /ai/chat → 429 (currently 402 — was uncounted; must seed Pro like the rest).
    - test_suggest_category_blocked_when_at_cap: Pro user at cap → suggest-category → 429.
    - test_cap_zero_blocks_chat_and_suggest: Pro user, cap=0 → chat & suggest → 429.
    - test_chat_unblocked_after_admin_patches_cap_higher: Pro user, initial 429, after PATCH cap higher → 200.
    - test_concurrent_ai_chat_at_cap_yields_one_pass_one_429: Pro users → exactly one 200, one 429.
    - test_concurrent_ai_chat_different_users_both_pass: two Pro users under cap → both 200.
    - A non-pro user on the same endpoints still yields 402 (do not regress; assert at least once if a non-pro path exists in these modules).
  </behavior>
  <action>
    Update EVERY cap test's `seed_user(...)` call to mint a Pro user — there are
    4 in tests/test_ai_cap_integration.py (including
    test_chat_blocked_when_at_cap_returns_429 at :76, which was previously
    miscounted; it seeds a free-tier owner and so gets 402 under the current
    gate order) and 2 in tests/test_spend_cap_concurrent.py (6 total). Use e.g.
    `pro_active_until=datetime.now(timezone.utc) + timedelta(days=30)` (or
    `trial_ends_at=` if the test intends reverse-trial). Keep the existing
    spending_cap_cents UPDATE statements. Now `require_pro` passes and the cap
    gate (429) is the one that fires — bringing assertions into line with the
    spec (pro-over-cap → 429). Do not weaken any assertion to 402; the tests
    must assert 429 where cap is exceeded. Mirror the existing fixture/style in
    each module (db_client / seed session / tenant scope).
  </action>
  <verify>
    <automated>docker compose exec -T api python -m pytest tests/test_ai_cap_integration.py tests/test_spend_cap_concurrent.py -q</automated>
  </verify>
  <done>All 6 cap tests pass (4 in test_ai_cap_integration.py + 2 in test_spend_cap_concurrent.py); pro-over-cap yields 429; non-pro still yields 402; no DI-order regression. Stack restored to base+dev after the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→AI API | AI endpoints gated by tier (require_pro, 402) then spend cap (enforce_spending_cap, 429) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01-01 | Elevation of Privilege | Pro gate ordering | accept | Test-only fixture change mints Pro users in tests; production require_pro→enforce_spending_cap order unchanged. Non-pro still 402. |
| T-68-01-02 | DoS | spend cap (429) | mitigate | Tests now exercise the real 429 cap path for Pro users, confirming the cap actually blocks over-cap spend (was masked by 402). |
</threat_model>

<verification>
- `docker compose exec -T api python -m pytest tests/test_ai_cap_integration.py tests/test_spend_cap_concurrent.py -q` → all green (6 cap tests).
- seed_user signature exposes pro_active_until + trial_ends_at (inspect check).
- No alembic migration added; no float introduced.
- Stack restored: `docker compose up -d` (base+dev) after testing.
</verification>

<success_criteria>
6 cap tests green: pro-over-cap→429, non-pro→402 confirmed; seed_user can mint Pro users; gate order unchanged unless investigation proved otherwise (documented in SUMMARY).
</success_criteria>

<output>
After completion, create `.planning/phases/68-tech-debt-cleanup/68-01-SUMMARY.md`
</output>
