---
phase: 68-tech-debt-cleanup
plan: 02
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - tests/helpers/seed.py
  - tests/test_categories.py
  - tests/test_e2e_multi_user_lifecycle.py
autonomous: true
requirements: [A2]
must_haves:
  truths:
    - "seed_category sets NOT-NULL Category.code and Category.ord systemically"
    - "No future test needs an inline code/ord seed hack (existing inline hacks removed)"
    - "tests/test_categories.py::test_seed_creates_14_categories passes"
    - "tests/test_e2e_multi_user_lifecycle.py test_e2e_1/3/4/6 pass"
    - "The actual onboarding/complete 422 root cause is identified and fixed"
  artifacts:
    - path: "tests/helpers/seed.py"
      provides: "seed_category populating code + ord (valid ord ~ ^[0-9]{2}$)"
      contains: "ord"
    - path: "tests/test_categories.py"
      provides: "test_seed_creates_14_categories green; inline code/ord hack removed"
      contains: "test_seed_creates_14_categories"
    - path: "tests/test_e2e_multi_user_lifecycle.py"
      provides: "e2e tests using seed_category (no inline Category(...) without code/ord)"
      contains: "seed_category"
  key_links:
    - from: "tests/helpers/seed.py seed_category"
      to: "Category.code + Category.ord NOT-NULL columns"
      via: "constructor sets both fields (ord validates ^[0-9]{2}$)"
      pattern: "ord"
    - from: "e2e + categories tests"
      to: "shared seed_category"
      via: "inline Category(...) seeds replaced by seed_category"
      pattern: "seed_category"
---

<objective>
Close **A2** (systemic seed-helper fix — explicit acceptance criterion: no inline
seed hacks). Two distinct roots, both must be addressed:

**Root 1 — `category.code`/`ord` seed-drift (Phase 22):** the shared
`seed_category` helper (tests/helpers/seed.py:72-87) does NOT set the NOT-NULL
columns `Category.code` (`String(40)`, slug) and `Category.ord`
(`String(2)`, DB check `ord ~ '^[0-9]{2}$'`). Several phases worked around this
with inline hacks (e.g. test_categories.py:296-298 sets `code="coffee", ord="00"`;
e2e tests construct `Category(...)` inline at lines ~351-352 and ~510-514 without
code/ord). Fix MUST be in the shared helper so no future test needs an inline hack.

  NOTE on the schema constraints (verify against app/db/models.py:296-302 +
  table check at :372 before coding — the ROADMAP/spec text loosely says
  `code` matches `^[0-9]{2}$`, but in the ACTUAL model that regex check is on
  **`ord`**, while `code` is a free `String(40)` slug like 'food'/'cafe'.
  Use the real constraints, not the loose spec wording.)

**Root 2 — `POST /onboarding/complete` → 422:** `test_seed_creates_14_categories`
fails with 422. The onboarding route works in e2e (test_e2e_1 posts it and asserts
200), so the 422 is environment/fixture-specific. **Investigate the actual
response body** (capture `response.text`) before deciding the fix — it may be the
`db_client` fixture's onboarded_at reset path, an auth/header mismatch, or a real
validator rejection. Fix the genuine cause; do not paper over it.

Purpose: green backend pytest baseline + a seed-helper that future tests trust.
Output: systemic seed_category, inline hacks removed, onboarding 422 fixed.

NOTE on ordering: this plan runs in Wave 2 (depends_on 68-01) because it also
edits `tests/helpers/seed.py` — serialized after 68-01's seed_user change to
avoid a merge conflict on the shared helper file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# THE SPEC — read §ФАЗА 68 A2 (lines 42-46) + "Окружение и дисциплина" (135-141)
@.planning/CONVERGENCE-AND-DEBT-PLAN.md
# Exact failing test names + root-cause hypotheses
@.planning/phases/67-remediation-cleanup/deferred-items.md

<interfaces>
<!-- app/db/models.py:299 code: Mapped[str] = mapped_column(String(40), nullable=False)  — slug, NOT a 2-digit code -->
<!-- app/db/models.py:302 ord: Mapped[str] = mapped_column(String(2), nullable=False)    — display ordinal -->
<!-- app/db/models.py:372 table check: "ord ~ '^[0-9]{2}$'"  (the 2-digit regex is on ORD) -->
<!-- app/db/models.py:354-358 partial unique index uq_category_user_code on (user_id, code) WHERE NOT is_archived -->
<!-- CODE DEFAULT (LOW-1): do NOT default code to f"c{sort_order}". Two active categories for the same user can share a sort_order, which would collide on the partial-unique (user_id, code) WHERE NOT is_archived index. Use a collision-resistant default instead: either a process-wide monotonic counter (e.g. itertools.count) or a slugified name with a numeric suffix on collision (e.g. "food", "food-2"), truncated to String(40). Uniqueness must hold across multiple seed_category calls for the SAME user within a test. -->
<!-- tests/helpers/seed.py:72-87 seed_category(session, *, user_id, name, kind, is_archived, sort_order) — NO code/ord today -->
<!-- tests/test_categories.py:296-298 INLINE HACK: code="coffee", ord="00"  (remove after helper fix) -->
<!-- tests/test_e2e_multi_user_lifecycle.py ~351-352 + ~510-514: inline Category(...) without code/ord (note 510 is a MULTI-LINE construction: `cat = Category(` with user_id on the next line) -->
<!-- app/services/onboarding.py:160-161 calls cat_svc.seed_default_categories (production path that DOES set code/ord) -->
<!-- app/api/schemas/onboarding.py:20-22 OnboardingCompleteRequest: starting_balance_cents:int, cycle_start_day:int Field(ge=1,le=28), seed_default_categories:bool -->
</interfaces>

<!-- ENVIRONMENT (CRITICAL): local .venv is BROKEN. Run pytest inside the docker `api` container. -->
<!-- Setup once: docker compose up -d --build api -->
<!-- Run tests: docker compose exec -T api python -m pytest <paths> -x -q -->
<!-- After the plan: restore the stack to base+dev (docker compose up -d). -->
<!-- Money is BIGINT cents, NO float. Do NOT add alembic migrations (A2 needs none — schema already has code/ord). -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Systemic seed_category (code + ord) and investigate the 422</name>
  <files>tests/helpers/seed.py, tests/test_categories.py</files>
  <read_first>
    - app/db/models.py (Category :258-372 — code :299, ord :302, check :372, uq index :354-358)
    - tests/helpers/seed.py (:72-87 seed_category)
    - tests/test_categories.py (:198-229 test_seed_creates_14_categories, :290-298 inline code/ord hack)
    - app/services/onboarding.py (:76-178 complete_onboarding + seed_default_categories call)
    - .planning/CONVERGENCE-AND-DEBT-PLAN.md §A2 (42-46)
  </read_first>
  <action>
    1. Extend `seed_category` to populate the NOT-NULL columns systemically. Add
       parameters with sensible auto-defaults so callers need NOTHING extra:
         - `code: Optional[str] = None` → when None, derive a COLLISION-RESISTANT
           unique-per-user slug. Do NOT use `f"c{sort_order}"` (LOW-1): two active
           categories for the same user can share a sort_order and would collide on
           the partial-unique (user_id, code) WHERE NOT is_archived index. Instead
           use either a process-wide monotonic counter (e.g. `itertools.count`) or a
           slugified `name` with a numeric suffix on collision (e.g. "food",
           "food-2"). Truncate to String(40); must satisfy the partial-unique
           (user_id, code) index for active rows across repeated calls for the SAME
           user.
         - `ord: Optional[str] = None` → when None, default to a valid 2-digit
           string matching `^[0-9]{2}$` (e.g. derive from sort_order: `f"{n:02d}"`,
           clamped 00-99).
       Set both on the `Category(...)` constructor. Verify against the REAL model
       constraints (code = String(40) slug; ord = 2-digit regex), not the loose
       spec wording.

    2. Remove the inline hack at tests/test_categories.py:290-298 — that test must
       rely on the helper (or, if it constructs Category directly, drop the now-
       redundant explicit code/ord and route through seed_category).

    3. INVESTIGATE the 422: in the api container, run
         docker compose exec -T api python -m pytest tests/test_categories.py::test_seed_creates_14_categories -q
       and capture the failure. If it is a 422, print `response.text` (temporarily
       or via the assertion message already present) to learn what the validator
       rejects. Determine whether it is (a) the db_client fixture's onboarded_at
       reset not committing, (b) auth header mismatch, or (c) a real schema reject.
       Fix the genuine cause in the test/fixture (NOT by loosening production
       schema). Document the actual root cause in the SUMMARY.

    Per CLAUDE.md: BIGINT cents, no float, no alembic migration.
  </action>
  <verify>
    <automated>docker compose exec -T api python -m pytest tests/test_categories.py -q && docker compose exec -T api python -c "import inspect, tests.helpers.seed as s; p=inspect.signature(s.seed_category).parameters; assert 'code' in p and 'ord' in p, p"</automated>
  </verify>
  <done>seed_category sets code+ord with valid, collision-resistant auto-defaults (no sort_order-based code collisions); inline hack removed; test_seed_creates_14_categories green; 422 root cause identified and fixed (documented).</done>
</task>

<task type="auto">
  <name>Task 2: Route e2e tests through seed_category (remove inline Category seeds)</name>
  <files>tests/test_e2e_multi_user_lifecycle.py</files>
  <read_first>
    - tests/test_e2e_multi_user_lifecycle.py (test_e2e_1 :112, test_e2e_3 :253, test_e2e_4 :323 inline Category :351-352, test_e2e_6 :480 inline Category :510 — MULTI-LINE: `cat = Category(` with user_id=member_id on the next line)
  </read_first>
  <behavior>
    - test_e2e_1_owner_happy_path: passes (onboarding/complete path green).
    - test_e2e_3_member_onboarding_seeds_and_embeddings: passes.
    - test_e2e_4_cross_tenant_isolation: category seeding no longer violates code NOT NULL.
    - test_e2e_6_revoke_cascade_purge: category seeding no longer violates code NOT NULL.
  </behavior>
  <action>
    Replace BOTH inline `Category(...)` constructions with calls to the now-systemic
    `seed_category(session, user_id=..., name=..., kind=..., sort_order=...)` so
    code/ord are populated by the helper:
      - lines ~351-352 (single-line `cat_a = Category(...)` / `cat_b = Category(...)`)
      - line ~510 (MULTI-LINE `cat = Category(` with `user_id=member_id` on the next
        line — easy to miss; convert this one too).
    Preserve each test's intent (cross-tenant isolation: A and B each own a
    category; revoke-cascade: the seeded category + usage rows). Keep tenant-scope /
    commit semantics identical. Do NOT add inline code/ord — that is the anti-pattern
    this plan eliminates; the helper supplies them.
  </action>
  <verify>
    <automated>docker compose exec -T api python -m pytest tests/test_e2e_multi_user_lifecycle.py -q && test "$(rg -U --pcre2 '(?<!seed_)Category\(' tests/test_e2e_multi_user_lifecycle.py | grep -v '^#' | grep -c 'Category(')" = "0"</automated>
  </verify>
  <done>test_e2e_1/3/4/6 green; zero raw Category(...) constructions remain in the e2e module (multiline-aware check, including the :510 multi-line seed); all category seeding flows through seed_category. Stack restored to base+dev.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test seed → DB | Test fixtures insert Category rows; must satisfy NOT-NULL code/ord + (user_id,code) unique index |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02-01 | Tampering | seed_category defaults | accept | Test-only helper; auto-derived collision-resistant code + valid ord satisfy real model constraints (incl. partial-unique index); no production schema change. |
| T-68-02-02 | Information Disclosure | onboarding/complete 422 | mitigate | 422 root cause fixed at its true source (fixture/validator), not by loosening production validation — keeps the route's input contract intact. |
</threat_model>

<verification>
- `docker compose exec -T api python -m pytest tests/test_categories.py tests/test_e2e_multi_user_lifecycle.py -q` → green.
- seed_category signature exposes code + ord (inspect check).
- Zero raw `Category(...)` constructions remain in the e2e module (multiline-aware grep, catches the :510 multi-line seed).
- No alembic migration; no float.
- Stack restored: `docker compose up -d` after testing.
</verification>

<success_criteria>
seed_category systemically sets code (collision-resistant default, no sort_order collision) + ord; inline hacks removed; test_seed_creates_14_categories + e2e_1/3/4/6 green; onboarding 422 root cause fixed and documented; no future test needs an inline seed hack.
</success_criteria>

<output>
After completion, create `.planning/phases/68-tech-debt-cleanup/68-02-SUMMARY.md`
</output>
