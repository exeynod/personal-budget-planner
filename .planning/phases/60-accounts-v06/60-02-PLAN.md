---
plan_id: 60-02
phase: 60
title: AccountsViewModel.load + AccountsView body (Hero + List + Empty + tap-to-detail) + ViewModel tests
wave: 2
depends_on:
  - 60-01
files_modified:
  - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
  - ios/BudgetPlanner/Features/Accounts/AccountsView.swift
  - ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift
autonomous: true
requirements:
  - v1.1.2-60-CONTEXT-area-1-navigation
  - v1.1.2-60-CONTEXT-area-2-list-structure
threat_ids:
  - T-60-03
must_haves:
  truths:
    - "AccountsViewModel.load() вызывает AccountsAPI.list() и заполняет accounts: [AccountDTO]."
    - "AccountsViewModel.status переходит .idle → .loading → .ready на success; .error(String) на failure (filtered Russian copy, no .localizedDescription)."
    - "AccountsViewModel.inFlight guard предотвращает re-entrant load()."
    - "AccountsView рендерит Hero summary section без header (title «Всего на счетах» + sumBalancesCents formatted + count subtitle)."
    - "AccountsView рендерит Section «Счета» со строками: kind icon + bank + kind/mask subtitle + balance trailing + star.fill orange при primary."
    - "AccountsView показывает ContentUnavailableView empty-state когда accounts.isEmpty (после загрузки)."
    - "AccountsView toolbar `+` button открывает sheet (модально показывает AccountsNewSheet stub из 60-01; реальный Form в 60-03)."
    - "Tap по row → NavigationLink(value: account.id) → push в AccountDetailView(accountId:) через .navigationDestination(for: Int.self)."
    - "ViewModel tests покрывают load success / sumBalancesCents / inFlight no-op."
    - "Build: make build clean; xcodebuild test -only-testing:BudgetPlannerTests/AccountsViewModelTests passes."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift"
      provides: "load() implementation + status transitions + inFlight guard"
      contains: "AccountsAPI.list()"
      contains_not: "error.localizedDescription"
    - path: "ios/BudgetPlanner/Features/Accounts/AccountsView.swift"
      provides: "Native List body с Hero + rows + empty state + toolbar + tap-to-detail"
      contains: "ContentUnavailableView"
      min_lines: 80
    - path: "ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift"
      provides: "Unit tests для AccountsViewModel state machine + derived properties"
      min_lines: 80
  key_links:
    - from: "AccountsViewModel.load()"
      to: "AccountsAPI.list()"
      via: "try await вызов"
      pattern: "AccountsAPI\\.list"
    - from: "AccountsView (row tap)"
      to: "AccountDetailView(accountId:)"
      via: "NavigationLink(value: account.id) + .navigationDestination(for: Int.self)"
      pattern: "navigationDestination\\(for: Int\\.self\\)"
    - from: "AccountsView toolbar `+`"
      to: "viewModel.sheet = .newAccount"
      via: "Button action"
      pattern: "viewModel.sheet = .newAccount"
---

<objective>
Заполнить `AccountsViewModel.load()` (вызов `AccountsAPI.list()` + status transitions + inFlight guard) и полностью переписать body `AccountsView` (Hero summary section + Section «Счета» с rows + empty state + toolbar `+` + tap-to-detail navigation). Добавить unit tests для ViewModel. Sheet body (Form) остаётся stub — реализация в Plan 60-03; в этом плане sheet просто открывается и показывает stub-placeholder.

Purpose: пользователь сразу после этого плана может зайти в Управление → Счета и увидеть свой реальный список счетов (loaded из backend), Hero summary с общей суммой, primary-account orange star indicator, empty state если счетов нет, и push на (пока stub) AccountDetailView. Это «functional skeleton» — backend integration работает, UI elements в местах, без полноценных детального экрана / sheet form.

Output:
- AccountsViewModel.load() заполнен (AccountsAPI.list + inFlight + status + T-60-03 filtered error copy).
- AccountsView рендерит native List(.insetGrouped) с Hero section (без header) + «Счета» section с rows + empty state + toolbar `+` + .sheet binding + .navigationDestination(for: Int.self) → AccountDetailView.
- AccountsViewModelTests создан с минимум 6 тестами.
- Build clean; tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-accounts-v06/60-CONTEXT.md
@.planning/phases/60-accounts-v06/60-01-SUMMARY.md
@.planning/phases/59-transactions-v06/59-01-SUMMARY.md
@ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountsView.swift
@ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift
@ios/BudgetPlanner/Networking/DTO/AccountDTO.swift
@ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift
@ios/BudgetPlanner/Domain/MoneyFormatter.swift
@ios/BudgetPlanner/Features/Management/CategoriesView.swift
@ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep2AccountsView.swift
@ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift

<interfaces>
<!-- Key types/contracts. DO NOT re-explore the codebase. -->

From ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post 60-01):
```swift
@MainActor
@Observable
final class AccountsViewModel {
    enum Status: Equatable { case idle, loading, ready, error(String) }
    enum SheetMode: Equatable { case none, newAccount }

    private(set) var status: Status
    private(set) var accounts: [AccountDTO]
    var sheet: SheetMode
    private(set) var submitting: Bool
    var lastCreatedAccountId: Int?

    @ObservationIgnored private var inFlight: Bool

    func load() async { /* EMPTY — fill in 60-02 */ }
    func createAccount(...) async -> Bool { return false /* 60-03 */ }

    var sumBalancesCents: Int { accounts.reduce(0) { $0 + $1.balanceCents } }
    var accountCount: Int { accounts.count }
}
```

From ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift:
```swift
@MainActor
enum AccountsAPI {
    static func list() async throws -> [AccountDTO]
    static func create(_ request: AccountCreateRequest) async throws -> AccountDTO
}
```

From ios/BudgetPlanner/Networking/DTO/AccountDTO.swift:
```swift
enum AccountKind: String, Codable { case card, cash, savings }
struct AccountDTO: Decodable, Identifiable, Equatable {
    let id: Int
    let bank: String
    let mask: String?
    let kind: AccountKind
    let balanceCents: Int
    let primary: Bool
    let createdAt: Date?
}
```

From ios/BudgetPlanner/Domain/MoneyFormatter.swift:
```swift
enum MoneyFormatter {
    static func format(cents: Int) -> String
    static func formatWithSymbol(cents: Int) -> String  // "1 500,50 ₽"
}
```

T-60-03 mitigation pattern (Phase 59-01 reference):
- В catch блоках использовать filtered Russian copy. НЕ assign `error.localizedDescription` к status.
- Console-only: `print("[AccountsViewModel] load failed: \(error)")`.

V10 reference pattern (для load implementation):
```swift
func load() async {
    if inFlight { return }
    inFlight = true
    defer { inFlight = false }
    status = .loading
    do {
        self.accounts = try await AccountsAPI.list()
        status = .ready
    } catch {
        print("[AccountsViewModel] load failed: \(error)")
        status = .error("Не удалось загрузить счета")
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AccountsViewModel.load() + helper methods</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post 60-01 stub — полный файл)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsListV10ViewModel.swift (full — pattern для inFlight + Status transitions)
    - ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift (full)
    - ios/BudgetPlanner/Features/Transactions/TransactionsView.swift (lines 1-95 — pattern для inFlight + Status enum в @Observable VM)
  </read_first>
  <behavior>
    Test 1 — initial state: vm.status == .idle, vm.accounts == [], vm.sheet == .none, vm.submitting == false, vm.lastCreatedAccountId == nil.
    Test 2 — sumBalancesCents = Σ balanceCents (positive + negative значения суммируются как-есть, signed).
    Test 3 — accountCount = accounts.count.
    Test 4 — clearLastCreatedAccountId(): после вызова lastCreatedAccountId == nil.
    Test 5 — status equatable: .error("foo") != .error("bar"); .ready == .ready.

    (Load network success/failure тесты — не покрываем т.к. требуют мок APIClient; ручное smoke в downstream.)
  </behavior>
  <action>
    Заменить body всех методов и добавить новый helper в `ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift`.

    1. **load()** — заменить пустое тело реализацией:
       ```swift
       func load() async {
           if inFlight { return }
           inFlight = true
           defer { inFlight = false }
           status = .loading
           do {
               self.accounts = try await AccountsAPI.list()
               status = .ready
           } catch {
               print("[AccountsViewModel] load failed: \(error)")
               status = .error("Не удалось загрузить счета")
           }
       }
       ```

    2. **createAccount(...)** — оставить stub (Plan 60-03 заполнит). Текущая сигнатура сохраняется, тело остаётся `return false`.

    3. **Добавить новый helper** `clearLastCreatedAccountId()`:
       ```swift
       func clearLastCreatedAccountId() {
           self.lastCreatedAccountId = nil
       }
       ```
       Rationale: ScrollViewReader.scrollTo вызывается в `.onChange(of: viewModel.lastCreatedAccountId)` в AccountsView (60-03 setup); после scroll нужно clear чтобы повторное создание того же id триггерило onChange.

    **T-60-03 guard**: убедиться что НИ в одном catch блоке нет `error.localizedDescription`. Только filtered Russian copy через status = .error("...").

    NO иных правок в файле. Сохранить порядок member declarations.
  </action>
  <verify>
    <automated>grep -c "AccountsAPI.list()" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 (load использует API). -->
    <automated>grep -c "if inFlight { return }" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: ≥1 (inFlight guard at top of load). -->
    <automated>grep -c "Не удалось загрузить счета" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 (T-60-03 filtered copy). -->
    <automated>grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 0 (T-60-03 — no raw error leak). -->
    <automated>grep -c "clearLastCreatedAccountId" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 (helper added). -->
  </verify>
  <acceptance_criteria>
    - load() полностью реализован: inFlight guard + status transitions + AccountsAPI.list() + try/catch с filtered Russian error.
    - clearLastCreatedAccountId() helper существует.
    - createAccount() остаётся stub (return false) — это explicit handoff к 60-03.
    - No error.localizedDescription anywhere.
  </acceptance_criteria>
  <done>
    Все grep gates pass. Build будет broken до Task 2 (AccountsView ещё stub) — acceptable since Task 2 fixes сразу.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite AccountsView body (Hero + List + Empty + toolbar + navigation)</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountsView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountsView.swift (post 60-01 stub)
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post Task 1 — surface для binding)
    - ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift (struct AccountsNewSheet — sheet content для toolbar +)
    - ios/BudgetPlanner/Features/Management/CategoriesView.swift (lines 80-186 — pattern для toolbar `+` + NavigationLink + List(.insetGrouped))
    - ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift (full — hero section pattern)
    - ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep2AccountsView.swift (lines 1-141 — row layout pattern с kind icon + bank + balance + star)
    - ios/BudgetPlanner/Domain/MoneyFormatter.swift (full — formatWithSymbol для balance)
  </read_first>
  <behavior>
    Render scenarios:
    - viewModel.status == .idle || .loading: показывать ProgressView в одной Section (NOT empty state).
    - viewModel.status == .error(let msg): показывать Section с Label + Image «exclamationmark.triangle» + текст msg, foregroundStyle(.red).
    - viewModel.status == .ready && accounts.isEmpty: показывать Section с ContentUnavailableView("Нет счетов", systemImage: "creditcard", description: Text("Добавьте первый счёт через «+»")). NO Hero section в empty state.
    - viewModel.status == .ready && !accounts.isEmpty: показывать Hero section (без header) + Section «Счета» с rows.

    Hero section (без header):
    - VStack alignment: leading:
      - Text("Всего на счетах").font(.caption).foregroundStyle(.secondary).textCase(.uppercase)
      - HStack(.lastTextBaseline, spacing: 4):
        - Text(MoneyFormatter.format(cents: viewModel.sumBalancesCents)).font(.title.monospacedDigit().weight(.semibold))
        - Text("₽").foregroundStyle(.secondary)
      - Text("\(viewModel.accountCount) \(pluralizedAccountWord)").font(.caption).foregroundStyle(.secondary) — где pluralizedAccountWord = "счёт" / "счёта" / "счетов" в зависимости от числа.

    Account row layout (HStack spacing: 12):
    - Leading: Image(systemName: iconForKind(account.kind)).foregroundStyle(Tokens.Accent.primary).frame(width: 28)
    - VStack alignment: leading, spacing: 2:
        - Text(account.bank).font(.body).foregroundStyle(.primary)
        - Text(subtitleForAccount(account)).font(.caption).foregroundStyle(.secondary)
    - Spacer()
    - HStack(spacing: 6):
        - Text(MoneyFormatter.format(cents: account.balanceCents)).font(.body.monospacedDigit())
        - Text("₽").foregroundStyle(.secondary)
        - if account.primary: Image(systemName: "star.fill").foregroundStyle(.orange).accessibilityLabel("Основной счёт")

    iconForKind mapping (per CONTEXT D-2):
    - .card → "creditcard.fill"
    - .cash → "banknote"
    - .savings → "tray.full.fill"

    subtitleForAccount mapping (per CONTEXT D-2):
    - .card + mask non-empty → "Карта •\(mask)"
    - .card + nil/empty mask → "Карта"
    - .cash → "Наличные"
    - .savings → "Накопительный счёт"

    Toolbar `+`:
    - ToolbarItem(placement: .topBarTrailing): Button { viewModel.sheet = .newAccount } label: { Image(systemName: "plus.circle.fill") }

    Sheet binding:
    - `.sheet(isPresented: Binding(get: { viewModel.sheet == .newAccount }, set: { if !$0 { viewModel.sheet = .none } }))`
    - Sheet content: AccountsNewSheet(submitting: viewModel.submitting, onCreate: { ... return false }, onCancel: { viewModel.sheet = .none }) — onCreate в этом плане возвращает false (60-03 заполнит). Это допустимо т.к. в 60-02 sheet body тоже stub из 60-01.

    Navigation:
    - List Row внутри ForEach обёрнут в `NavigationLink(value: account.id)` — value: Int (account.id).
    - На уровне List контейнера: `.navigationDestination(for: Int.self) { id in AccountDetailView(accountId: id) }`.

    Lifecycle:
    - .task { await viewModel.load() } — initial fetch.
    - .refreshable { await viewModel.load() } — pull-to-refresh.
  </behavior>
  <action>
    Полностью переписать body файла `ios/BudgetPlanner/Features/Accounts/AccountsView.swift`. Сохранить top-of-file комментарий о Phase 60. Удалить старый stub body.

    Структура (порядок):
    1. `import SwiftUI`
    2. Doc-comment блок.
    3. `struct AccountsView: View` с `@State private var viewModel = AccountsViewModel()`.
    4. `var body: some View` — List(.insetGrouped) внутри:
       ```swift
       List {
           switch viewModel.status {
           case .idle, .loading:
               loadingSection
           case .error(let msg):
               errorSection(msg)
           case .ready:
               if viewModel.accounts.isEmpty {
                   emptySection
               } else {
                   heroSection
                   accountsSection
               }
           }
       }
       .listStyle(.insetGrouped)
       .navigationTitle("Счета")
       .toolbar {
           ToolbarItem(placement: .topBarTrailing) {
               Button { viewModel.sheet = .newAccount } label: {
                   Image(systemName: "plus.circle.fill")
               }
               .accessibilityLabel("Добавить счёт")
           }
       }
       .navigationDestination(for: Int.self) { id in
           AccountDetailView(accountId: id)
       }
       .task { await viewModel.load() }
       .refreshable { await viewModel.load() }
       .sheet(isPresented: sheetBinding) {
           AccountsNewSheet(
               submitting: viewModel.submitting,
               onCreate: { bank, kind, mask, balanceCents, primary in
                   await viewModel.createAccount(
                       bank: bank,
                       kind: kind,
                       mask: mask,
                       balanceCents: balanceCents,
                       primary: primary
                   )
               },
               onCancel: {
                   viewModel.sheet = .none
               }
           )
       }
       ```

    5. **`private var sheetBinding: Binding<Bool>`** computed:
       ```swift
       private var sheetBinding: Binding<Bool> {
           Binding(
               get: { viewModel.sheet == .newAccount },
               set: { if !$0 { viewModel.sheet = .none } }
           )
       }
       ```

    6. **`private var loadingSection: some View`**:
       ```swift
       private var loadingSection: some View {
           Section { ProgressView().frame(maxWidth: .infinity) }
       }
       ```

    7. **`private func errorSection(_ msg: String) -> some View`**:
       ```swift
       private func errorSection(_ msg: String) -> some View {
           Section {
               Label(msg, systemImage: "exclamationmark.triangle")
                   .foregroundStyle(.red)
           }
       }
       ```

    8. **`private var emptySection: some View`**:
       ```swift
       private var emptySection: some View {
           Section {
               ContentUnavailableView(
                   "Нет счетов",
                   systemImage: "creditcard",
                   description: Text("Добавьте первый счёт через «+»")
               )
               .listRowBackground(Color.clear)
           }
       }
       ```

    9. **`private var heroSection: some View`** (без header — Section { ... }):
       ```swift
       private var heroSection: some View {
           Section {
               VStack(alignment: .leading, spacing: 8) {
                   Text("Всего на счетах")
                       .font(.caption)
                       .foregroundStyle(.secondary)
                       .textCase(.uppercase)
                   HStack(alignment: .lastTextBaseline, spacing: 4) {
                       Text(MoneyFormatter.format(cents: viewModel.sumBalancesCents))
                           .font(.title.monospacedDigit().weight(.semibold))
                           .foregroundStyle(.primary)
                       Text("₽").foregroundStyle(.secondary)
                       Spacer()
                   }
                   Text(accountCountLabel)
                       .font(.caption)
                       .foregroundStyle(.secondary)
               }
               .padding(.vertical, 4)
           }
       }

       private var accountCountLabel: String {
           let n = viewModel.accountCount
           let mod10 = n % 10
           let mod100 = n % 100
           let word: String
           if mod10 == 1 && mod100 != 11 { word = "счёт" }
           else if (2...4).contains(mod10) && !(12...14).contains(mod100) { word = "счёта" }
           else { word = "счетов" }
           return "\(n) \(word)"
       }
       ```

    10. **`private var accountsSection: some View`**:
        ```swift
        private var accountsSection: some View {
            Section("Счета") {
                ForEach(viewModel.accounts) { acct in
                    NavigationLink(value: acct.id) {
                        AccountRow(account: acct)
                    }
                }
            }
        }
        ```

    11. **`private struct AccountRow: View`**:
        ```swift
        private struct AccountRow: View {
            let account: AccountDTO

            var body: some View {
                HStack(spacing: 12) {
                    Image(systemName: iconForKind(account.kind))
                        .foregroundStyle(Tokens.Accent.primary)
                        .frame(width: 28)
                    VStack(alignment: .leading, spacing: 2) {
                        Text(account.bank)
                            .font(.body)
                            .foregroundStyle(.primary)
                        Text(subtitleFor(account))
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                    Spacer(minLength: 8)
                    HStack(spacing: 6) {
                        Text(MoneyFormatter.format(cents: account.balanceCents))
                            .font(.body.monospacedDigit())
                            .foregroundStyle(.primary)
                        Text("₽").foregroundStyle(.secondary)
                        if account.primary {
                            Image(systemName: "star.fill")
                                .foregroundStyle(.orange)
                                .accessibilityLabel("Основной счёт")
                        }
                    }
                }
            }

            private func iconForKind(_ kind: AccountKind) -> String {
                switch kind {
                case .card: return "creditcard.fill"
                case .cash: return "banknote"
                case .savings: return "tray.full.fill"
                }
            }

            private func subtitleFor(_ a: AccountDTO) -> String {
                switch a.kind {
                case .card:
                    if let mask = a.mask, !mask.isEmpty { return "Карта •\(mask)" }
                    return "Карта"
                case .cash: return "Наличные"
                case .savings: return "Накопительный счёт"
                }
            }
        }
        ```

    NO ScrollViewReader wiring в этом плане — это будет в 60-03 (scroll-to-new). Сейчас `lastCreatedAccountId` остаётся unused в View.

    NO swipe-actions, NO context menu, NO «сделать основным» button — backend не поддерживает (CONTEXT scope).
  </action>
  <verify>
    <automated>grep -c "navigationDestination(for: Int.self)" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "ContentUnavailableView" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c 'systemImage: "plus.circle.fill"' ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (toolbar +) -->
    <automated>grep -c '"creditcard.fill"\|"banknote"\|"tray.full.fill"' ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: ≥3 (3 kind icons) -->
    <automated>grep -c '"star.fill"' ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (primary indicator) -->
    <automated>grep -c "AccountsNewSheet" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (sheet content) -->
    <automated>grep -c "NavigationLink(value:" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (row → detail push) -->
    <automated>grep -c "AccountDetailView(accountId:" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (navigationDestination dispatch) -->
    <automated>cd ios && make build 2>&1 | tail -10 | grep -cE "BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - List(.insetGrouped) с switch на viewModel.status → loading / error / empty / ready состояния.
    - Hero section без header содержит «Всего на счетах» + sum + count label с правильной русской pluralization.
    - Section «Счета» с rows: kind icon (creditcard.fill/banknote/tray.full.fill) + bank + subtitle (Карта •XXXX / Наличные / Накопительный счёт) + balance + star.fill orange при primary.
    - Empty state — ContentUnavailableView с «Нет счетов» + «Добавьте первый счёт через «+»».
    - Toolbar `+` (plus.circle.fill) открывает sheet с AccountsNewSheet.
    - Tap по row → NavigationLink(value: Int) → .navigationDestination(for: Int.self) → AccountDetailView(accountId:).
    - .task + .refreshable вызывают viewModel.load().
    - Build clean.
  </acceptance_criteria>
  <done>
    Все grep gates pass; make build SUCCEEDED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for AccountsViewModel state machine + derived properties</name>
  <files>ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift</files>
  <read_first>
    - ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift (full — JSON-decode fixture pattern + @MainActor XCTest + makeActual helper)
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post Task 1 — surface для тестирования)
    - ios/BudgetPlanner/Networking/DTO/AccountDTO.swift (full — для JSON fixture)
  </read_first>
  <behavior>
    Test 1 — test_initialState_idleEmpty: vm.status == .idle, vm.accounts == [], vm.sheet == .none, vm.submitting == false, vm.lastCreatedAccountId == nil.
    Test 2 — test_sumBalancesCents_sumsAllAccounts: vm.accounts = [a(1000), a(2500), a(-300)] → vm.sumBalancesCents == 3200.
    Test 3 — test_sumBalancesCents_emptyReturnsZero: vm.accounts = [] → vm.sumBalancesCents == 0.
    Test 4 — test_accountCount_returnsLength: vm.accounts = [a, a, a] → vm.accountCount == 3.
    Test 5 — test_clearLastCreatedAccountId_setsNil: vm.lastCreatedAccountId = 42 → call clearLastCreatedAccountId() → vm.lastCreatedAccountId == nil.
    Test 6 — test_status_equatable: .error("foo") != .error("bar"); .ready == .ready; .loading == .loading.
    Test 7 — test_sheetMode_toggling: vm.sheet = .newAccount → vm.sheet == .newAccount; vm.sheet = .none → vm.sheet == .none.
  </behavior>
  <action>
    Создать новую директорию `ios/BudgetPlannerTests/Features/Accounts/` и файл `AccountsViewModelTests.swift`. Использовать pattern из `TransactionsViewModelTests.swift` для JSON-decode helper (поскольку AccountDTO имеет custom `createdAt: Date?` который не synthesizes без APIClient decoder).

    Структура:
    ```swift
    import XCTest
    @testable import BudgetPlanner

    @MainActor
    final class AccountsViewModelTests: XCTestCase {

        // MARK: - Helpers

        private func makeAccount(
            id: Int,
            bank: String = "Т-Банк",
            kind: String = "card",
            mask: String? = nil,
            balanceCents: Int = 0,
            primary: Bool = false
        ) -> AccountDTO {
            let json: [String: Any] = [
                "id": id,
                "bank": bank,
                "kind": kind,
                "mask": mask as Any,
                "balanceCents": balanceCents,
                "primary": primary,
                "createdAt": NSNull()
            ]
            let data = try! JSONSerialization.data(withJSONObject: json)
            let decoder = JSONDecoder()
            decoder.keyDecodingStrategy = .convertFromSnakeCase
            return try! decoder.decode(AccountDTO.self, from: data)
        }

        // MARK: - Tests

        func test_initialState_idleEmpty() {
            let vm = AccountsViewModel()
            XCTAssertEqual(vm.status, .idle)
            XCTAssertTrue(vm.accounts.isEmpty)
            XCTAssertEqual(vm.sheet, .none)
            XCTAssertFalse(vm.submitting)
            XCTAssertNil(vm.lastCreatedAccountId)
        }

        func test_sumBalancesCents_sumsAllAccounts() {
            let vm = AccountsViewModel()
            // (accounts is private(set) — нужен writable backdoor. Если @Observable
            // делает property private(set), то писать напрямую нельзя. Решение:
            // вызвать через @testable и убрать private(set) — НЕТ, сохраним private(set).
            // Подход: тест-helper в самом VM через #if DEBUG, ИЛИ Mirror reflection.
            //
            // Простейшее решение: в AccountsViewModel.swift добавить
            //   #if DEBUG
            //   func _setAccountsForTesting(_ list: [AccountDTO]) { self.accounts = list }
            //   #endif
            // — это будет частью Task 1 (см. Task 1 action note ниже).
            vm._setAccountsForTesting([
                makeAccount(id: 1, balanceCents: 1000),
                makeAccount(id: 2, balanceCents: 2500),
                makeAccount(id: 3, balanceCents: -300),
            ])
            XCTAssertEqual(vm.sumBalancesCents, 3200)
        }

        func test_sumBalancesCents_emptyReturnsZero() {
            let vm = AccountsViewModel()
            XCTAssertEqual(vm.sumBalancesCents, 0)
        }

        func test_accountCount_returnsLength() {
            let vm = AccountsViewModel()
            vm._setAccountsForTesting([
                makeAccount(id: 1),
                makeAccount(id: 2),
                makeAccount(id: 3),
            ])
            XCTAssertEqual(vm.accountCount, 3)
        }

        func test_clearLastCreatedAccountId_setsNil() {
            let vm = AccountsViewModel()
            vm.lastCreatedAccountId = 42
            vm.clearLastCreatedAccountId()
            XCTAssertNil(vm.lastCreatedAccountId)
        }

        func test_status_equatable_distinguishesErrorMessages() {
            XCTAssertNotEqual(
                AccountsViewModel.Status.error("foo"),
                AccountsViewModel.Status.error("bar")
            )
            XCTAssertEqual(AccountsViewModel.Status.ready, .ready)
            XCTAssertEqual(AccountsViewModel.Status.loading, .loading)
        }

        func test_sheetMode_toggling() {
            let vm = AccountsViewModel()
            XCTAssertEqual(vm.sheet, .none)
            vm.sheet = .newAccount
            XCTAssertEqual(vm.sheet, .newAccount)
            vm.sheet = .none
            XCTAssertEqual(vm.sheet, .none)
        }
    }
    ```

    **CRITICAL — Task 1 backdoor требуется**: чтобы тесты могли заполнять `accounts` (которая private(set)), нужно добавить в Task 1 (в начале этого плана, до коммита) тестовый mutator в AccountsViewModel.swift:
    ```swift
    #if DEBUG
    func _setAccountsForTesting(_ list: [AccountDTO]) {
        self.accounts = list
    }
    #endif
    ```

    **IMPORTANT**: исполнитель должен сначала добавить `_setAccountsForTesting` в Task 1 (см. Task 1 action — добавить как часть AccountsViewModel правок), потом написать тесты в Task 3. Если Task 1 уже закоммичен без backdoor, добавить backdoor отдельным amend или новой fix-правкой в начале Task 3 в том же файле AccountsViewModel.swift.

    После создания файла тестов:
    1. `cd ios && xcodegen generate` чтобы захватить новый файл в BudgetPlannerTests target.
    2. `cd ios && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/AccountsViewModelTests CODE_SIGNING_ALLOWED=NO` — все ≥6 тестов pass.

    Alternative: использовать `make test-accounts` если такая target существует в Makefile; иначе direct xcodebuild как выше.
  </action>
  <verify>
    <automated>test -f ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift</automated>
    <automated>grep -c "func test_" ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift</automated>
    <!-- Expected: ≥6 -->
    <automated>grep -c "@testable import BudgetPlanner" ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "_setAccountsForTesting" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 (backdoor exists for tests) -->
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/AccountsViewModelTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20 | grep -cE "Test Suite 'AccountsViewModelTests' passed|TEST SUCCEEDED"</automated>
    <!-- Expected: ≥1 — tests pass. -->
  </verify>
  <acceptance_criteria>
    - AccountsViewModelTests.swift содержит ≥6 @MainActor test_ функций.
    - makeAccount JSON-decode helper использует .convertFromSnakeCase decoder.
    - _setAccountsForTesting #if DEBUG backdoor добавлен в AccountsViewModel.
    - Все тесты pass через xcodebuild test.
  </acceptance_criteria>
  <done>
    test grep gates pass; xcodebuild test реально проходит (TEST SUCCEEDED). xcodegen regen successful.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app ↔ /api/v1/accounts | Untrusted backend; AccountDTO decoded через APIClient.shared.decoder. Mitigation: AccountKind enum case match через rawValue — invalid value throws decoder error → status = .error("Не удалось загрузить счета"). |
| User → toolbar `+` button | Trusted input (just opens sheet). Sheet validation в 60-03. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Information Disclosure | AccountsViewModel.load() catch block | mitigate | Fixed Russian copy "Не удалось загрузить счета". Full error printed via `print(...)` only, never assigned to user-visible state. Verified via `grep -c "error.localizedDescription" → 0`. |
</threat_model>

<verification>
- `grep -c "AccountsAPI.list()" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift` returns 1.
- `grep -c "if inFlight { return }" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift` returns ≥1.
- `grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift` returns 0.
- `grep -c "navigationDestination(for: Int.self)" ios/BudgetPlanner/Features/Accounts/AccountsView.swift` returns 1.
- `grep -c "ContentUnavailableView" ios/BudgetPlanner/Features/Accounts/AccountsView.swift` returns 1.
- `grep -c '"star.fill"' ios/BudgetPlanner/Features/Accounts/AccountsView.swift` returns 1.
- `cd ios && make build` clean.
- `xcodebuild test -only-testing:BudgetPlannerTests/AccountsViewModelTests` passes ≥6 tests.
</verification>

<success_criteria>
1. AccountsViewModel.load() полностью реализован: inFlight + status transitions + AccountsAPI.list() + filtered Russian error copy.
2. AccountsViewModel получает `clearLastCreatedAccountId()` + `_setAccountsForTesting()` (#if DEBUG).
3. AccountsView рендерит native iOS-26 List(.insetGrouped) с 4 состояниями (loading / error / empty / ready).
4. Hero section (без header) показывает «Всего на счетах» + sum + русская pluralization count.
5. «Счета» Section с rows: kind icon + bank + subtitle + balance + star.fill orange.
6. Empty state — ContentUnavailableView.
7. Toolbar `+` (plus.circle.fill) открывает sheet → AccountsNewSheet (body stub в 60-01 пока).
8. Tap по row → NavigationLink(value: Int) + .navigationDestination(for: Int.self) → AccountDetailView (stub в 60-01).
9. AccountsViewModelTests с ≥6 тестами, все pass.
10. make build clean.
11. Manual: open Управление → Счета → видны реальные счета загруженные из backend; primary имеет orange star; tap по row → push в AccountDetailView (пустой stub из 60-01 — это ожидается, наполнение в 60-04).
</success_criteria>

<output>
After completion, create `.planning/phases/60-accounts-v06/60-02-SUMMARY.md` summarising:
- AccountsViewModel.load() implementation (inFlight + Status transitions + T-60-03 mitigation)
- AccountsView body composition (heroSection / accountsSection / emptySection / errorSection / loadingSection)
- Account row layout (icon + bank + subtitle + balance + primary star)
- Russian pluralization helper для счёт/счёта/счетов
- AccountsViewModelTests test coverage matrix
- Sheet binding pattern (placeholder onCreate — будет заполнен в 60-03)
- Navigation pattern (NavigationLink value: Int + .navigationDestination(for: Int.self))
- Known acceptance: NewAccountSheet Form body всё ещё stub из 60-01 — 60-03 заменяет
- Known acceptance: AccountDetailView body всё ещё stub из 60-01 — 60-04 заменяет
- Manual smoke notes
</output>
