---
plan_id: 60-04
phase: 60
title: AccountDetailViewModel.load + AccountDetailView body (Hero + History per period grouped by day)
wave: 4
depends_on:
  - 60-01
files_modified:
  - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift
  - ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift
  - ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift
autonomous: false
requirements:
  - v1.1.2-60-CONTEXT-area-3-account-detail
threat_ids:
  - T-60-03
must_haves:
  truths:
    - "AccountDetailViewModel.load() параллельно фетчит AccountsAPI.list() (для lookup account by id) + CategoriesV10API.list() + PeriodsAPI.current(); затем sequentially ActualV10API.list(periodId:) с client-side filter accountId == self.accountId."
    - "AccountDetailViewModel handles period-404 gracefully (mid-onboarding): period == nil → actuals == [], status == .ready."
    - "AccountDetailViewModel handles account-not-found (cross-tenant / missing): status == .error(«Счёт не найден») — no existence leak (T-60-03 ID-rationale)."
    - "AccountDetailView Hero section: bank .largeTitle + kind+mask subhead + balance .title2.monospacedDigit + star.fill orange при primary."
    - "AccountDetailView History section: actuals filtered по accountId группированы по дате в Europe/Moscow TZ; group header «\(dateLabel)» с MoneyFormatter sum; rows: time + description + signed amount."
    - "Empty history: ContentUnavailableView «Нет операций» / «В текущем периоде на этом счёте нет операций»."
    - "Toolbar: только дефолтный Back (NavigationStack handles); НЕТ Menu (нет API для make-primary/edit/delete)."
    - "Day grouping reuses AccountsData.filterByAccount + locally-implemented groupByDay (или TransactionsData.groupByDay reuse) с Europe/Moscow Calendar."
    - "ViewModel tests: filterByAccount + day grouping + empty state."
    - "Build: make build clean; ViewModel tests pass."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift"
      provides: "load() implementation + computed properties (filteredByDay groups)"
      contains: "ActualV10API.list"
      contains: "Europe/Moscow"
      contains_not: "error.localizedDescription"
    - path: "ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift"
      provides: "Hero + history sections + empty state + Day-group sections"
      contains: "ContentUnavailableView"
      min_lines: 100
    - path: "ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift"
      provides: "Unit tests для filter + day grouping + month label"
      min_lines: 80
  key_links:
    - from: "AccountDetailViewModel.load()"
      to: "ActualV10API.list(periodId:)"
      via: "after period + accounts resolved"
      pattern: "ActualV10API\\.list\\(periodId:"
    - from: "AccountDetailViewModel.load()"
      to: "AccountsData.filterByAccount"
      via: "client-side filter accountId match"
      pattern: "AccountsData\\.filterByAccount|filter \\{ \\$0\\.accountId =="
    - from: "AccountDetailViewModel.calendar"
      to: "TimeZone Europe/Moscow"
      via: "static defaultCalendar()"
      pattern: 'TimeZone\\(identifier: "Europe/Moscow"\\)'
---

<objective>
Заполнить `AccountDetailViewModel.load()` (parallel fetch accounts/categories/period + sequential ActualV10API.list + filter) и полностью переписать body `AccountDetailView` (Hero section с bank/kind+mask/balance/primary star + History section с day-grouped actuals + empty state). Toolbar только дефолтный Back (нет API для action menu). Добавить unit tests.

Purpose: tap по row в AccountsView → push в полностью функциональный AccountDetailView с детальным просмотром одного счёта + история транзакций по этому счёту за current period. Завершает Phase 60 feature-complete UX.

Output:
- AccountDetailViewModel.load() заполнен с graceful period-404 + cross-tenant guards.
- AccountDetailView Hero + history sections + empty state + day grouping.
- AccountDetailViewModelTests с filter + grouping tests.
- Build clean; manual smoke checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-accounts-v06/60-CONTEXT.md
@.planning/phases/60-accounts-v06/60-01-SUMMARY.md
@.planning/phases/59-transactions-v06/59-01-SUMMARY.md
@.planning/phases/59-transactions-v06/59-02-SUMMARY.md
@ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift
@ios/BudgetPlanner/FeaturesV10/Accounts/AccountDetailV10ViewModel.swift
@ios/BudgetPlanner/FeaturesV10/Accounts/AccountsData.swift
@ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift
@ios/BudgetPlanner/Networking/DTO/AccountDTO.swift
@ios/BudgetPlanner/Networking/DTO/TransactionDTO.swift
@ios/BudgetPlanner/Networking/DTO/CategoryV10DTO.swift
@ios/BudgetPlanner/Networking/DTO/CommonDTO.swift
@ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift
@ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift
@ios/BudgetPlanner/Networking/Endpoints/CategoriesV10API.swift
@ios/BudgetPlanner/Networking/Endpoints/AuthAPI.swift
@ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift
@ios/BudgetPlanner/Domain/MoneyFormatter.swift

<interfaces>
<!-- Key types/contracts. -->

From ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (post 60-01):
```swift
@MainActor @Observable
final class AccountDetailViewModel {
    enum Status: Equatable { case idle, loading, ready, error(String) }
    let accountId: Int
    private(set) var status: Status
    private(set) var account: AccountDTO?
    private(set) var actuals: [ActualV10DTO]
    private(set) var categories: [CategoryV10DTO]
    private(set) var period: PeriodDTO?
    @ObservationIgnored private var inFlight: Bool
    @ObservationIgnored var calendar: Calendar  // Europe/Moscow
    private static func defaultCalendar() -> Calendar { ... }
    init(accountId: Int)
    func load() async { /* EMPTY — fill в 60-04 */ }
}
```

From ios/BudgetPlanner/FeaturesV10/Accounts/AccountsData.swift:
```swift
enum AccountsData {
    static func sumBalances(_ list: [AccountDTO]) -> Int
    static func count(_ list: [AccountDTO]) -> Int
    static func formatBankSubtitle(_ a: AccountDTO) -> String
    static func filterByAccount(_ actuals: [ActualV10DTO], accountId: Int) -> [ActualV10DTO]
    static func sumPeriodOps(_ actuals: [ActualV10DTO], periodStart: Date, periodEnd: Date) -> (count: Int, sumCents: Int)
    static func isValidNewAccountDraft(bank: String, balanceCents: Int) -> Bool
}
// NOTE: AccountsData LIVES in FeaturesV10/Accounts/ — accessible from Features/Accounts/
// since both compile в same BudgetPlanner module. NO need to re-implement; reuse.
```

From ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift:
```swift
struct TxDayGroup: Identifiable {
    let id: Int  // yyyymmdd integer key
    let date: Date  // anchor (start of day in calendar TZ)
    let rows: [ActualV10DTO]
    var sumCents: Int { rows.reduce(0) { $0 + abs($1.amountCents) } }
}

enum TransactionsData {
    static func groupByDay(_ actuals: [ActualV10DTO], today: Date, calendar: Calendar) -> [TxDayGroup]
    // Returns groups sorted by anchor date DESC. Within-group sort by createdAt ?? txDate DESC.
}
// NOTE: TransactionsData LIVES in FeaturesV10/Transactions/ — reusable.
```

From ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift:
```swift
enum ActualV10API {
    static func list(periodId: Int, kind: ActualKindV10? = nil, categoryId: Int? = nil) async throws -> [ActualV10DTO]
}
```

From ios/BudgetPlanner/Networking/Endpoints/CategoriesV10API.swift:
```swift
enum CategoriesV10API {
    static func list(includeArchived: Bool = false) async throws -> [CategoryV10DTO]
}
```

From ios/BudgetPlanner/Networking/Endpoints/AuthAPI.swift (PeriodsAPI):
```swift
enum PeriodsAPI {
    static func current() async throws -> PeriodDTO
    static func list() async throws -> [PeriodDTO]
}
```

From ios/BudgetPlanner/Networking/DTO/CommonDTO.swift:
```swift
struct PeriodDTO: Decodable, Identifiable, Equatable {
    let id: Int
    let periodStart: Date
    let periodEnd: Date
    // ... + other fields
}
```

APIError reference (for graceful 404 handling — pattern from Features/Management/CategoryDetailScreen.swift line 34):
```swift
// catch APIError.notFound { state = .noActivePeriod }
// — accept that AccountDetailViewModel может тоже handle this case.
// Однако наш Status enum проще: catch на period отдельно, set period = nil, продолжить.
```

V10 reference (AccountDetailV10ViewModel — pattern для load + cross-tenant guard):
```swift
func load() async {
    if inFlight { return }
    inFlight = true
    defer { inFlight = false }
    status = .loading
    do {
        async let accsTask = AccountsAPI.list()
        async let catsTask = CategoriesV10API.list()
        let accs = try await accsTask
        let cats = try await catsTask
        guard let acc = accs.first(where: { $0.id == accountId }) else {
            status = .error("Счёт не найден")  // T-60-03 — single message no leak
            return
        }
        self.account = acc
        self.categories = cats
        // Period 404 tolerated
        let per: PeriodDTO?
        do { per = try await PeriodsAPI.current() } catch { per = nil }
        self.period = per
        if let pid = per?.id {
            do {
                let allActuals = try await ActualV10API.list(periodId: pid)
                self.actuals = AccountsData.filterByAccount(allActuals, accountId: accountId)
            } catch { self.actuals = [] }
        } else {
            self.actuals = []
        }
        status = .ready
    } catch {
        status = .error("Не удалось загрузить счёт")
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AccountDetailViewModel.load() + derived computed properties</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (post 60-01)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountDetailV10ViewModel.swift (lines 50-100 — load pattern)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsData.swift (full — filterByAccount, sumPeriodOps)
    - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsData.swift (groupByDay signature + TxDayGroup struct)
    - ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift (lines 14-40 — pattern для load + APIError.notFound handling)
  </read_first>
  <behavior>
    Test 1 — initial state: status == .idle; account == nil; actuals == []; categories == []; period == nil.
    Test 2 — filterByAccount: реализован через AccountsData.filterByAccount — тесты live в AccountsDataTests (existing); тут просто verify что VM использует helper.
    Test 3 — dayGroups computed property: actuals (post-filter) → грузим через TransactionsData.groupByDay(filteredActuals, today: Date(), calendar: calendar) → returns sorted DESC.
    Test 4 — categoryName(_:) lookup: vm.categories = [cat(1, "Food")]; vm.categoryName(1) == "Food"; vm.categoryName(999) == nil.
    Test 5 — Calendar TZ == Europe/Moscow (или fallback .current).
    (Network-driven load() success/failure — manual smoke в Task 3.)
  </behavior>
  <action>
    Заменить body `load()` и добавить computed properties в `AccountDetailViewModel`:

    1. **Заменить `load()`**:
       ```swift
       func load() async {
           if inFlight { return }
           inFlight = true
           defer { inFlight = false }

           status = .loading

           do {
               // Phase 60 D-3: parallel fetch accounts + categories.
               async let accsTask = AccountsAPI.list()
               async let catsTask = CategoriesV10API.list()
               let accs = try await accsTask
               let cats = try await catsTask

               // T-60-03 (ID rationale): cross-tenant / missing id collapses to
               // single user-facing message без existence leak.
               guard let acc = accs.first(where: { $0.id == accountId }) else {
                   status = .error("Счёт не найден")
                   return
               }
               self.account = acc
               self.categories = cats

               // Period 404 tolerated (mid-onboarding scenarios).
               let per: PeriodDTO?
               do { per = try await PeriodsAPI.current() } catch { per = nil }
               self.period = per

               // Actuals filter by accountId — reuse AccountsData helper.
               if let pid = per?.id {
                   do {
                       let allActuals = try await ActualV10API.list(periodId: pid)
                       self.actuals = AccountsData.filterByAccount(allActuals, accountId: accountId)
                   } catch {
                       print("[AccountDetailViewModel] actuals fetch failed: \(error)")
                       self.actuals = []
                   }
               } else {
                   self.actuals = []
               }

               status = .ready
           } catch {
               print("[AccountDetailViewModel] load failed: \(error)")
               // T-60-03: filtered Russian copy, no .localizedDescription.
               status = .error("Не удалось загрузить счёт")
           }
       }
       ```

    2. **Добавить computed properties**:
       ```swift
       /// Reuses TransactionsData.groupByDay для day-by-day секции в History.
       /// Today является anchor для today/yesterday label-flagging — actual logic
       /// в groupByDay (mirrors Phase 59-01 pattern).
       var dayGroups: [TxDayGroup] {
           TransactionsData.groupByDay(actuals, today: Date(), calendar: calendar)
       }

       /// Lookup category name by id (для history row category-label sub-line).
       func categoryName(_ categoryId: Int) -> String? {
           categories.first(where: { $0.id == categoryId })?.name
       }

       /// Has any actuals (после account filter) в current period?
       var hasActuals: Bool {
           !actuals.isEmpty
       }
       ```

    3. **Добавить #if DEBUG backdoor** для тестирования (mirror 60-02 pattern):
       ```swift
       #if DEBUG
       func _setStateForTesting(
           account: AccountDTO? = nil,
           actuals: [ActualV10DTO] = [],
           categories: [CategoryV10DTO] = [],
           period: PeriodDTO? = nil
       ) {
           self.account = account
           self.actuals = actuals
           self.categories = categories
           self.period = period
       }
       #endif
       ```

    T-60-03 verify: NO `error.localizedDescription` в любой catch блок.
  </action>
  <verify>
    <automated>grep -c "ActualV10API.list(periodId:" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "AccountsData.filterByAccount" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "CategoriesV10API.list" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "AccountsAPI.list" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "Счёт не найден" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 (cross-tenant guard) -->
    <automated>grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 0 (T-60-03) -->
    <automated>grep -c "TransactionsData.groupByDay" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 (dayGroups reuses helper) -->
    <automated>grep -c "_setStateForTesting" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 (DEBUG backdoor) -->
  </verify>
  <acceptance_criteria>
    - load() parallel-fetches accounts/categories, sequential для period (graceful 404) + actuals filtered.
    - Cross-tenant guard: account not in list → status = .error("Счёт не найден") (no leak).
    - dayGroups + categoryName + hasActuals computed properties доступны View.
    - No error.localizedDescription anywhere.
    - DEBUG backdoor exists.
  </acceptance_criteria>
  <done>
    All grep gates pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite AccountDetailView body (Hero + History day groups + Empty state)</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift (post 60-01 stub)
    - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (post Task 1)
    - ios/BudgetPlanner/Features/Management/CategoryDetailScreen.swift (full — hero section + history List pattern)
    - ios/BudgetPlanner/Features/Transactions/TransactionsView.swift (lines 250-400 — day group rendering pattern, ActualV10DTO row layout)
    - ios/BudgetPlanner/Domain/MoneyFormatter.swift (full)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsData.swift (formatBankSubtitle — может reuse для subtitle "Карта •0420")
  </read_first>
  <behavior>
    Render scenarios:
    - .idle / .loading: List { Section { ProgressView() } }
    - .error(let msg): List { Section { Label(msg, "exclamationmark.triangle").foregroundStyle(.red) } }
    - .ready, account == nil (defensive — shouldn't happen post-guard): same .error
    - .ready, account != nil: Hero section + History section(s)

    Hero section (without header):
    - VStack(alignment: .leading, spacing: 8):
        - Text(account.bank).font(.largeTitle.weight(.semibold)) (или .title — пусть будет .title2 чтобы fit на narrow iPhone; CONTEXT D-3 говорит .largeTitle но мы согласуем с CategoryDetailScreen)
        - HStack: Text(kindLabel).font(.subheadline).foregroundStyle(.secondary) + if let mask: Text("•\(mask)").foregroundStyle(.secondary)
        - HStack(.lastTextBaseline, spacing: 4): Text(MoneyFormatter.format(cents: account.balanceCents)).font(.title2.monospacedDigit().weight(.semibold)) + Text("₽").foregroundStyle(.secondary) + Spacer + if account.primary: Image("star.fill").foregroundStyle(.orange).accessibilityLabel("Основной счёт")

    kindLabel computed:
    - .card → "Карта"
    - .cash → "Наличные"
    - .savings → "Сбережения"

    History sections:
    - Each TxDayGroup → Section header «\(dateLabel)» (e.g. «Сегодня», «Вчера», «12 мая») + Σ amount label на right
    - Section content: ForEach group.rows → ActualHistoryRow
    - dateLabel logic: if isToday → "Сегодня"; elif isYesterday → "Вчера"; else NumberFormatter ru_RU style "12 мая".

    ActualHistoryRow layout (HStack spacing: 12):
    - VStack alignment: leading, spacing: 2:
        - Text(description ?? "—").font(.body).lineLimit(2)
        - Text(categoryName ?? "—").font(.caption).foregroundStyle(.secondary)
    - Spacer(minLength: 8)
    - VStack alignment: trailing, spacing: 2:
        - Text(signedAmountStr).font(.body.monospacedDigit().weight(.semibold)).foregroundStyle(colorForKind)
        - Text(timeStr).font(.caption.monospacedDigit()).foregroundStyle(.secondary)  // HH:mm в Moscow TZ

    Color for kind (matching Phase 59-02 conventions):
    - .expense → .primary
    - .income → .green
    - .roundup → .orange
    - .deposit → .blue

    Empty history (account != nil но actuals.isEmpty):
    - Section { ContentUnavailableView("Нет операций", systemImage: "tray", description: Text("В текущем периоде на этом счёте нет операций")) .listRowBackground(Color.clear) }

    Toolbar: только default Back (NavigationStack handles).
    navigationBarTitleDisplayMode(.inline) с navigationTitle("Счёт").
    NO Menu, NO refresh button (.refreshable handles).

    .task { await viewModel.load() }
    .refreshable { await viewModel.load() }
  </behavior>
  <action>
    Полностью переписать body `AccountDetailView` в `ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift`. Сохранить top-of-file комментарий + init.

    Структура:
    ```swift
    import SwiftUI

    /// (existing docstring сохранён, добавить «Plan 60-04: реализация»)
    struct AccountDetailView: View {
        let accountId: Int

        @State private var viewModel: AccountDetailViewModel

        init(accountId: Int) {
            self.accountId = accountId
            self._viewModel = State(wrappedValue: AccountDetailViewModel(accountId: accountId))
        }

        var body: some View {
            List {
                switch viewModel.status {
                case .idle, .loading:
                    Section { ProgressView().frame(maxWidth: .infinity) }
                case .error(let msg):
                    Section {
                        Label(msg, systemImage: "exclamationmark.triangle")
                            .foregroundStyle(.red)
                    }
                case .ready:
                    if let acc = viewModel.account {
                        heroSection(acc)
                        historySections
                    } else {
                        Section {
                            Label("Счёт не найден", systemImage: "exclamationmark.triangle")
                                .foregroundStyle(.red)
                        }
                    }
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Счёт")
            .navigationBarTitleDisplayMode(.inline)
            .task { await viewModel.load() }
            .refreshable { await viewModel.load() }
        }

        // MARK: - Hero

        @ViewBuilder
        private func heroSection(_ acc: AccountDTO) -> some View {
            Section {
                VStack(alignment: .leading, spacing: 8) {
                    Text(acc.bank)
                        .font(.title2.weight(.semibold))
                        .foregroundStyle(.primary)
                    HStack(spacing: 6) {
                        Text(kindLabel(acc.kind))
                            .font(.subheadline)
                            .foregroundStyle(.secondary)
                        if let mask = acc.mask, !mask.isEmpty {
                            Text("•\(mask)")
                                .font(.subheadline.monospacedDigit())
                                .foregroundStyle(.secondary)
                        }
                    }
                    HStack(alignment: .lastTextBaseline, spacing: 4) {
                        Text(MoneyFormatter.format(cents: acc.balanceCents))
                            .font(.title2.monospacedDigit().weight(.semibold))
                            .foregroundStyle(.primary)
                        Text("₽").foregroundStyle(.secondary)
                        Spacer()
                        if acc.primary {
                            Image(systemName: "star.fill")
                                .foregroundStyle(.orange)
                                .accessibilityLabel("Основной счёт")
                        }
                    }
                }
                .padding(.vertical, 4)
            }
        }

        private func kindLabel(_ kind: AccountKind) -> String {
            switch kind {
            case .card: return "Карта"
            case .cash: return "Наличные"
            case .savings: return "Сбережения"
            }
        }

        // MARK: - History

        @ViewBuilder
        private var historySections: some View {
            let groups = viewModel.dayGroups
            if groups.isEmpty {
                Section {
                    ContentUnavailableView(
                        "Нет операций",
                        systemImage: "tray",
                        description: Text("В текущем периоде на этом счёте нет операций")
                    )
                    .listRowBackground(Color.clear)
                }
            } else {
                ForEach(groups) { group in
                    Section {
                        ForEach(group.rows) { actual in
                            ActualHistoryRow(
                                actual: actual,
                                categoryName: viewModel.categoryName(actual.categoryId),
                                calendar: viewModel.calendar
                            )
                        }
                    } header: {
                        HStack {
                            Text(dayHeaderLabel(group.date))
                            Spacer()
                            Text(MoneyFormatter.format(cents: group.sumCents))
                                .monospacedDigit()
                                .foregroundStyle(.secondary)
                        }
                    }
                }
            }
        }

        /// «Сегодня» / «Вчера» / «12 мая» по Europe/Moscow Calendar.
        private func dayHeaderLabel(_ date: Date) -> String {
            let cal = viewModel.calendar
            if cal.isDateInToday(date) { return "Сегодня" }
            if cal.isDateInYesterday(date) { return "Вчера" }
            let f = DateFormatter()
            f.locale = Locale(identifier: "ru_RU")
            f.calendar = cal
            f.dateFormat = "d MMMM"
            return f.string(from: date)
        }
    }

    // MARK: - Row

    private struct ActualHistoryRow: View {
        let actual: ActualV10DTO
        let categoryName: String?
        let calendar: Calendar

        var body: some View {
            HStack(spacing: 12) {
                VStack(alignment: .leading, spacing: 2) {
                    Text(displayDescription)
                        .font(.body)
                        .foregroundStyle(.primary)
                        .lineLimit(2)
                    Text(categoryName ?? "—")
                        .font(.caption)
                        .foregroundStyle(.secondary)
                        .lineLimit(1)
                }
                Spacer(minLength: 8)
                VStack(alignment: .trailing, spacing: 2) {
                    Text(signedAmount)
                        .font(.body.monospacedDigit().weight(.semibold))
                        .foregroundStyle(amountColor)
                    Text(timeLabel)
                        .font(.caption.monospacedDigit())
                        .foregroundStyle(.secondary)
                }
            }
        }

        private var displayDescription: String {
            let raw = actual.description?.trimmingCharacters(in: .whitespaces) ?? ""
            return raw.isEmpty ? "Без описания" : raw
        }

        /// Расходы / roundup → negative sign; income / deposit → positive sign.
        /// Backend already stores abs(amountCents) for expense; знак в UI per Phase 59 convention.
        private var signedAmount: String {
            let value = MoneyFormatter.format(cents: actual.amountCents)
            switch actual.kind {
            case .expense, .roundup:
                return "−\(value) ₽"
            case .income, .deposit:
                return "+\(value) ₽"
            }
        }

        private var amountColor: Color {
            switch actual.kind {
            case .expense: return .primary
            case .income: return .green
            case .roundup: return .orange
            case .deposit: return .blue
            }
        }

        private var timeLabel: String {
            let f = DateFormatter()
            f.calendar = calendar
            f.timeZone = calendar.timeZone
            f.locale = Locale(identifier: "ru_RU")
            f.dateFormat = "HH:mm"
            return f.string(from: actual.createdAt ?? actual.txDate)
        }
    }
    ```

    NO toolbar Menu (CONTEXT D-3 «только Back»). NO Edit / Delete / Make-primary buttons (нет API).

    NO observation of Notification.Name.txnCreated (Out-of-scope для AccountDetail; refresh через pull-to-refresh).
  </action>
  <verify>
    <automated>grep -c "viewModel.dayGroups" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "ContentUnavailableView" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: 1 (empty history state) -->
    <automated>grep -c "ActualHistoryRow" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: ≥2 (struct + usage) -->
    <automated>grep -c "Карта\|Наличные\|Сбережения" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: ≥3 -->
    <automated>grep -c "star.fill" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: 1 (primary indicator in hero) -->
    <automated>grep -c "Сегодня\|Вчера" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: ≥2 -->
    <automated>grep -c "dayHeaderLabel\|kindLabel" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: ≥2 (helpers) -->
    <automated>cd ios && make build 2>&1 | tail -10 | grep -cE "BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - List(.insetGrouped) с switch на status → loading / error / ready / fallback.
    - Hero Section: bank .title2 + kindLabel + mask «•0420» + balance .title2.monospacedDigit + star.fill orange при primary.
    - History sections — ForEach по viewModel.dayGroups с Section header «Сегодня»/«Вчера»/«12 мая» + Σ sum trailing.
    - ActualHistoryRow: description + category name + signed amount (colored по kind) + time HH:mm в Europe/Moscow.
    - Empty history: ContentUnavailableView «Нет операций».
    - Toolbar только default Back. No Menu.
    - Build clean.
  </acceptance_criteria>
  <done>
    All grep gates pass; make build SUCCEEDED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for AccountDetailViewModel filter + day grouping</name>
  <files>ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift</files>
  <read_first>
    - ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift (post 60-02 — pattern для makeAccount JSON-decode helper)
    - ios/BudgetPlannerTests/Features/Transactions/TransactionsViewModelTests.swift (full — pattern для makeActual JSON-decode helper + Europe/Moscow Calendar fixture)
    - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift (post Task 1 — surface)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsData.swift (filterByAccount signature)
  </read_first>
  <behavior>
    Test 1 — test_initialState_idleEmpty: status == .idle; account == nil; actuals == []; categories == []; period == nil.
    Test 2 — test_categoryName_returnsMatchingName: categories = [cat(1, "Food"), cat(2, "Зарплата")]; categoryName(1) == "Food"; categoryName(2) == "Зарплата"; categoryName(999) == nil.
    Test 3 — test_dayGroups_emptyActuals_returnsEmpty: actuals = [] → dayGroups == [].
    Test 4 — test_dayGroups_threeDaysInMoscowTZ_returnsSortedDesc: three actuals on different days → 3 TxDayGroups sorted DESC by date.
    Test 5 — test_dayGroups_sumsAbsoluteAmounts: actuals на одном дне с amounts [1000, 2500, 500] → sumCents = 4000.
    Test 6 — test_hasActuals_reflectsCount: actuals == [] → hasActuals == false; actuals.count > 0 → hasActuals == true.
    Test 7 — test_calendar_isEuropeMoscow: vm.calendar.timeZone.identifier == "Europe/Moscow" ИЛИ fallback приемлем (.current — non-strict).
  </behavior>
  <action>
    Создать `ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift`:
    ```swift
    import XCTest
    @testable import BudgetPlanner

    @MainActor
    final class AccountDetailViewModelTests: XCTestCase {

        // MARK: - Helpers

        private var moscow: Calendar {
            var c = Calendar(identifier: .gregorian)
            c.timeZone = TimeZone(identifier: "Europe/Moscow") ?? .current
            return c
        }

        private func date(_ y: Int, _ m: Int, _ d: Int, _ h: Int = 12, _ mi: Int = 0) -> Date {
            var comps = DateComponents()
            comps.year = y; comps.month = m; comps.day = d; comps.hour = h; comps.minute = mi
            return moscow.date(from: comps)!
        }

        private func makeAccount(id: Int, primary: Bool = false) -> AccountDTO {
            let json: [String: Any] = [
                "id": id,
                "bank": "Т-Банк",
                "kind": "card",
                "mask": "1234",
                "balanceCents": 100000,
                "primary": primary,
                "createdAt": NSNull()
            ]
            let data = try! JSONSerialization.data(withJSONObject: json)
            let decoder = JSONDecoder()
            decoder.keyDecodingStrategy = .convertFromSnakeCase
            return try! decoder.decode(AccountDTO.self, from: data)
        }

        private func makeCategory(id: Int, name: String) -> CategoryV10DTO {
            let json: [String: Any] = [
                "id": id,
                "name": name,
                "kind": "expense",
                "isArchived": false,
                "sortOrder": 0,
                "createdAt": NSNull()
            ]
            let data = try! JSONSerialization.data(withJSONObject: json)
            let decoder = JSONDecoder()
            decoder.keyDecodingStrategy = .convertFromSnakeCase
            return try! decoder.decode(CategoryV10DTO.self, from: data)
        }

        private let iso8601: ISO8601DateFormatter = {
            let f = ISO8601DateFormatter()
            f.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
            return f
        }()

        private func makeActual(id: Int, categoryId: Int, amountCents: Int, accountId: Int?, txDate: Date) -> ActualV10DTO {
            let dateStr = iso8601.string(from: txDate)
            let json: [String: Any] = [
                "id": id,
                "periodId": 1,
                "kind": "expense",
                "amountCents": amountCents,
                "description": "test",
                "categoryId": categoryId,
                "txDate": dateStr,
                "source": "mini_app",
                "createdAt": dateStr,
                "accountId": accountId as Any,
                "parentTxnId": NSNull()
            ]
            let data = try! JSONSerialization.data(withJSONObject: json)
            let decoder = JSONDecoder()
            decoder.keyDecodingStrategy = .convertFromSnakeCase
            let f = ISO8601DateFormatter()
            f.formatOptions = [.withInternetDateTime, .withFractionalSeconds]
            decoder.dateDecodingStrategy = .custom { dec in
                let str = try dec.singleValueContainer().decode(String.self)
                if let d = f.date(from: str) { return d }
                let f2 = ISO8601DateFormatter()
                f2.formatOptions = [.withInternetDateTime]
                return f2.date(from: str) ?? Date()
            }
            return try! decoder.decode(ActualV10DTO.self, from: data)
        }

        // MARK: - Tests

        func test_initialState_idleEmpty() {
            let vm = AccountDetailViewModel(accountId: 42)
            XCTAssertEqual(vm.status, .idle)
            XCTAssertNil(vm.account)
            XCTAssertTrue(vm.actuals.isEmpty)
            XCTAssertTrue(vm.categories.isEmpty)
            XCTAssertNil(vm.period)
            XCTAssertEqual(vm.accountId, 42)
        }

        func test_categoryName_returnsMatchingName() {
            let vm = AccountDetailViewModel(accountId: 1)
            vm._setStateForTesting(
                categories: [makeCategory(id: 1, name: "Food"), makeCategory(id: 2, name: "Зарплата")]
            )
            XCTAssertEqual(vm.categoryName(1), "Food")
            XCTAssertEqual(vm.categoryName(2), "Зарплата")
            XCTAssertNil(vm.categoryName(999))
        }

        func test_dayGroups_emptyActuals_returnsEmpty() {
            let vm = AccountDetailViewModel(accountId: 1)
            XCTAssertTrue(vm.dayGroups.isEmpty)
        }

        func test_dayGroups_threeDaysInMoscowTZ_returnsSortedDesc() {
            let vm = AccountDetailViewModel(accountId: 1)
            vm._setStateForTesting(actuals: [
                makeActual(id: 1, categoryId: 1, amountCents: 1000, accountId: 1, txDate: date(2026, 5, 10)),
                makeActual(id: 2, categoryId: 1, amountCents: 2000, accountId: 1, txDate: date(2026, 5, 12)),
                makeActual(id: 3, categoryId: 1, amountCents: 3000, accountId: 1, txDate: date(2026, 5, 11)),
            ])
            let groups = vm.dayGroups
            XCTAssertEqual(groups.count, 3)
            // Sorted DESC: 12, 11, 10
            XCTAssertTrue(groups[0].date > groups[1].date)
            XCTAssertTrue(groups[1].date > groups[2].date)
        }

        func test_dayGroups_sumsAbsoluteAmounts() {
            let vm = AccountDetailViewModel(accountId: 1)
            let day = date(2026, 5, 12)
            vm._setStateForTesting(actuals: [
                makeActual(id: 1, categoryId: 1, amountCents: 1000, accountId: 1, txDate: day),
                makeActual(id: 2, categoryId: 1, amountCents: 2500, accountId: 1, txDate: day),
                makeActual(id: 3, categoryId: 1, amountCents: 500, accountId: 1, txDate: day),
            ])
            let groups = vm.dayGroups
            XCTAssertEqual(groups.count, 1)
            XCTAssertEqual(groups[0].sumCents, 4000)
        }

        func test_hasActuals_reflectsCount() {
            let vm = AccountDetailViewModel(accountId: 1)
            XCTAssertFalse(vm.hasActuals)
            vm._setStateForTesting(actuals: [
                makeActual(id: 1, categoryId: 1, amountCents: 1000, accountId: 1, txDate: date(2026, 5, 12)),
            ])
            XCTAssertTrue(vm.hasActuals)
        }

        func test_calendar_isEuropeMoscowOrFallback() {
            let vm = AccountDetailViewModel(accountId: 1)
            // Strict: identifier == "Europe/Moscow". На simulator всегда доступно.
            XCTAssertEqual(vm.calendar.timeZone.identifier, "Europe/Moscow")
        }
    }
    ```

    После:
    1. `cd ios && xcodegen generate` — захватить файл.
    2. `cd ios && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/AccountDetailViewModelTests CODE_SIGNING_ALLOWED=NO`.
  </action>
  <verify>
    <automated>test -f ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift</automated>
    <automated>grep -c "func test_" ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift</automated>
    <!-- Expected: ≥7 -->
    <automated>grep -c "@testable import BudgetPlanner" ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c 'TimeZone(identifier: "Europe/Moscow")' ios/BudgetPlannerTests/Features/Accounts/AccountDetailViewModelTests.swift</automated>
    <!-- Expected: ≥1 -->
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/AccountDetailViewModelTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20 | grep -cE "Test Suite 'AccountDetailViewModelTests' passed|TEST SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - Файл создан с ≥7 test_ функциями.
    - Все тесты pass через xcodebuild.
    - Helpers: makeAccount / makeCategory / makeActual через JSON-decode.
    - Europe/Moscow Calendar fixture.
  </acceptance_criteria>
  <done>
    grep gates pass; xcodebuild test SUCCEEDED.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    AccountDetailView complete (Hero + History day groups + empty state) + AccountDetailViewModel.load() с graceful period-404 + cross-tenant guard. Wave 4 завершает Phase 60 feature scope.
  </what-built>
  <how-to-verify>
    1. Запустить app в симуляторе. Authenticated session.
    2. Управление → Счета → tap по строке существующего счёта → push в AccountDetailView.
    3. Hero section:
       a. Bank name отображается крупным шрифтом (.title2 weight semibold).
       b. Под bank: «Карта •0420» (если card+mask) или «Карта» (если без mask) или «Наличные» / «Сбережения».
       c. Balance строчка: «1 234,56» monospacedDigit + «₽» secondary + (если primary) orange star.fill справа.
    4. History section:
       a. Если в текущем периоде ЕСТЬ операции на этом счёте → Section'ы по дням, header «Сегодня» / «Вчера» / «12 мая» + sum trailing secondary.
       b. Rows: description (или «Без описания») + category name (caption secondary) + signed amount (−red для expense, +green для income, orange для roundup, blue для deposit) + time HH:mm.
       c. Sort DESC: новые дни сверху, внутри дня — новые createdAt сверху.
    5. Empty case:
       a. Если на счёте нет операций в current period → ContentUnavailableView «Нет операций» / «В текущем периоде на этом счёте нет операций».
       b. Hero остаётся видим.
    6. Cross-tenant / missing id case:
       a. (Если есть способ симулировать — например, перейти на legacy URL: `userdefaults set DEV_OPEN_ACCOUNT_ID 999999`) → AccountDetailView показывает «Счёт не найден» в red Label.
       b. Если нет — пропустить этот шаг (manual coverage в production через cross-tenant prevention).
    7. Period-404 case:
       a. Если у пользователя нет current period (новый аккаунт): tap по счёту → Hero отображается, history секция — ContentUnavailableView без period info.
    8. Navigation:
       a. Default back button работает; swipe-back gesture работает.
       b. Title «Счёт» inline displayMode.
       c. NO Menu / Edit button (CONTEXT D-3 — нет API для актиона).
    9. Pull-to-refresh: тянуть List вниз → перезагрузка успешна.
    10. Build / lint:
       a. `cd ios && make build` clean.
       b. `xcodebuild test -only-testing:BudgetPlannerTests/AccountDetailViewModelTests` 7+ pass.
       c. Все Phase 60 тесты вместе: `xcodebuild test -only-testing:BudgetPlannerTests/AccountsViewModelTests -only-testing:BudgetPlannerTests/AccountsNewSheetValidationTests -only-testing:BudgetPlannerTests/AccountDetailViewModelTests` — все pass.
  </how-to-verify>
  <resume-signal>
    Введите «approved» если всё работает, или опишите конкретные баги для исправления.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app ↔ /api/v1/accounts | Backend response decode; cross-tenant id (not в user's list) → status = .error("Счёт не найден") (no existence leak — same single message). |
| iOS app ↔ /api/v1/periods/{id}/actual | actuals filter accountId == self.accountId client-side; server returns all actuals for period. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Information Disclosure | load() catch blocks + cross-tenant guard | mitigate | Filtered Russian copy «Не удалось загрузить счёт» / «Счёт не найден». Full error printed via `print(...)` only. Cross-tenant guard collapses both «account doesn't exist» и «account belongs to other user» в single message → no existence leak. Verified: `grep -c "error.localizedDescription" → 0`. |
</threat_model>

<verification>
- `grep -c "ActualV10API.list(periodId:" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift` returns 1.
- `grep -c "AccountsData.filterByAccount" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift` returns 1.
- `grep -c "TransactionsData.groupByDay" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift` returns 1.
- `grep -c "Счёт не найден" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift` returns 1.
- `grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift` returns 0.
- `grep -c "ContentUnavailableView" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift` returns 1.
- `grep -c "ActualHistoryRow" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift` returns ≥2.
- `cd ios && make build` clean.
- `xcodebuild test -only-testing:BudgetPlannerTests/AccountDetailViewModelTests` passes ≥7 tests.
- Manual smoke approved (checkpoint).
</verification>

<success_criteria>
1. AccountDetailViewModel.load() parallel-fetches AccountsAPI.list + CategoriesV10API.list, sequential для PeriodsAPI.current (graceful 404) + ActualV10API.list + AccountsData.filterByAccount.
2. Cross-tenant guard: account not в list → status = .error("Счёт не найден") (no leak, single message).
3. dayGroups computed reuses TransactionsData.groupByDay с Europe/Moscow Calendar.
4. AccountDetailView Hero section с bank/kindLabel/mask «•0420»/balance monospacedDigit/primary star.fill orange.
5. History sections с day headers («Сегодня»/«Вчера»/«12 мая») + sum trailing + ActualHistoryRow (description + category + signed amount kind-colored + time HH:mm Europe/Moscow).
6. Empty history: ContentUnavailableView «Нет операций» / «В текущем периоде на этом счёте нет операций».
7. Toolbar только default Back. NO Menu (нет API для action).
8. T-60-03 verified: no error.localizedDescription anywhere.
9. AccountDetailViewModelTests ≥7 tests pass.
10. make build clean.
11. Manual smoke approved.
</success_criteria>

<output>
After completion, create `.planning/phases/60-accounts-v06/60-04-SUMMARY.md` summarising:
- AccountDetailViewModel.load() control flow (parallel + sequential + graceful period-404 + cross-tenant guard)
- Reuse of AccountsData.filterByAccount + TransactionsData.groupByDay (FeaturesV10 helpers accessible — same module)
- AccountDetailView composition (heroSection + historySections + ActualHistoryRow)
- Day header label logic («Сегодня»/«Вчера»/«d MMMM» ru_RU + Europe/Moscow)
- Signed amount + kind color matrix (expense/income/roundup/deposit)
- Empty-state vs error-state semantics
- Tests coverage matrix
- Manual smoke result
- Phase 60 closure note: после approval можно создать 60-VERIFICATION.md + mark complete в STATE.md
</output>
