---
plan_id: 60-03
phase: 60
title: NewAccountSheet (Form) + AccountsViewModel.createAccount + ScrollViewReader scroll-to-new + inline error banner
wave: 3
depends_on:
  - 60-02
files_modified:
  - ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift
  - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
  - ios/BudgetPlanner/Features/Accounts/AccountsView.swift
  - ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift
autonomous: false
requirements:
  - v1.1.2-60-CONTEXT-area-4-new-account-sheet
threat_ids:
  - T-60-01
  - T-60-02
  - T-60-03
user_setup: []
must_haves:
  truths:
    - "NewAccountSheet (struct AccountsNewSheet) рендерит native Form с 5 полями: bank TextField, kind Picker(.segmented), conditional mask TextField, balance TextField (decimalPad), primary Toggle."
    - "Mask TextField появляется только при kind == .card (conditional render)."
    - "Mask sanitization: keystroke filter digits-only + truncate prefix(4); valid form requires `^\\d{4}$` match при kind == .card (T-60-02)."
    - "Bank sanitized: trim whitespace; canCreate requires bank.trimmed.count >= 1."
    - "Balance parsed через MoneyParser.parseToCents; canCreate requires balanceCents >= 0."
    - "Submit button «Создать» в .confirmationAction disabled до canCreate; cancel «Отмена» в .cancellationAction."
    - "AccountsViewModel.createAccount() реально POST'ит через AccountsAPI.create, on success: dismiss sheet + reload + set lastCreatedAccountId."
    - "On API failure: createAccount() sets viewModel.createError: String? с filtered Russian copy (T-60-03)."
    - "AccountsView wraps Section list в ScrollViewReader; .onChange(of: viewModel.lastCreatedAccountId) → withAnimation scrollTo(id, anchor: .center) → clearLastCreatedAccountId."
    - "createError banner inline в AccountsView (НЕ в sheet — sheet dismisses; banner appears на updated list)."
    - "T-60-01 (primary race): после createAccount → load() refetches accounts; UI отображает actual server state (backend сам снимает primary с других)."
    - "Build: make build clean; sheet validation tests pass."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift"
      provides: "Native Form AccountsNewSheet с live validation + canCreate computed"
      contains: "MoneyParser.parseToCents"
      contains: "kind == .card"
      min_lines: 100
    - path: "ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift"
      provides: "createAccount() real implementation + createError state + ScrollViewReader hook via lastCreatedAccountId"
      contains: "AccountsAPI.create("
      contains_not: "error.localizedDescription"
    - path: "ios/BudgetPlanner/Features/Accounts/AccountsView.swift"
      provides: "ScrollViewReader wrapper + scroll-to-new + createError banner section"
      contains: "ScrollViewReader"
    - path: "ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift"
      provides: "Unit tests для canCreate validation matrix"
      min_lines: 60
  key_links:
    - from: "AccountsViewModel.createAccount()"
      to: "AccountsAPI.create(_:)"
      via: "try await POST"
      pattern: "AccountsAPI\\.create\\("
    - from: "AccountsView ScrollViewReader proxy"
      to: "viewModel.lastCreatedAccountId"
      via: ".onChange(of: viewModel.lastCreatedAccountId) → proxy.scrollTo(id, anchor: .center)"
      pattern: "proxy.scrollTo\\("
    - from: "NewAccountSheet (struct AccountsNewSheet)"
      to: "onCreate callback in AccountsView sheet content"
      via: "async closure passing (bank, kind, mask, balanceCents, primary)"
      pattern: "onCreate\\b"
---

<objective>
Заменить stub-body `AccountsNewSheet` (NewAccountSheet.swift) на полный native Form с live validation (bank trim, mask regex `^\d{4}$` при kind==.card, balance >= 0). Реализовать `AccountsViewModel.createAccount()` (real `AccountsAPI.create` + reload + lastCreatedAccountId + createError state). Добавить в `AccountsView` ScrollViewReader wrapper, .onChange(of: lastCreatedAccountId) для scroll-to-new с анимацией, и createError inline banner. Написать unit tests для validation logic.

Purpose: пользователь может создать новый счёт через native Form в `.sheet`; primary Toggle снимает primary с других через server-side (T-60-01); UI прокручивает к новой строке после save; ошибки API surfaced inline banner (НЕ alert) с filtered Russian copy (T-60-03). NewAccountSheet — последняя «feature-complete» составляющая Accounts list UX.

Output:
- AccountsNewSheet полностью реализован (Form, validation, submit, error в sheet inline).
- AccountsViewModel.createAccount() — реальная POST mutation.
- AccountsView обёрнут в ScrollViewReader + .onChange handler + createError banner.
- Unit tests для validation в AccountsNewSheetValidationTests.swift.
- Manual smoke checkpoint (sheet — user-facing UX flow).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-accounts-v06/60-CONTEXT.md
@.planning/phases/60-accounts-v06/60-01-SUMMARY.md
@.planning/phases/60-accounts-v06/60-02-SUMMARY.md
@.planning/phases/59-transactions-v06/59-03-SUMMARY.md
@.planning/phases/57-onboarding-4step/57-CONTEXT.md
@ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift
@ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
@ios/BudgetPlanner/Features/Accounts/AccountsView.swift
@ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep2AccountsView.swift
@ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep1IncomeView.swift
@ios/BudgetPlanner/Domain/MoneyFormatter.swift
@ios/BudgetPlanner/Networking/DTO/AccountDTO.swift
@ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift
@ios/BudgetPlanner/FeaturesV10/Accounts/NewAccountSheet.swift

<interfaces>
<!-- Key types/contracts. -->

From ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift (post 60-01 stub):
```swift
struct AccountsNewSheet: View {
    let submitting: Bool
    let onCreate: (_ bank: String, _ kind: AccountKind, _ mask: String?, _ balanceCents: Int, _ primary: Bool) async -> Bool
    let onCancel: () -> Void
    var body: some View { /* STUB — заполняем */ }
}
```

From ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post 60-02):
```swift
@MainActor @Observable
final class AccountsViewModel {
    enum Status: Equatable { case idle, loading, ready, error(String) }
    enum SheetMode: Equatable { case none, newAccount }
    private(set) var status: Status
    private(set) var accounts: [AccountDTO]
    var sheet: SheetMode
    private(set) var submitting: Bool
    var lastCreatedAccountId: Int?
    func load() async { /* реализовано в 60-02 */ }
    func createAccount(...) async -> Bool { return false /* fill in 60-03 */ }
    var sumBalancesCents: Int
    var accountCount: Int
    func clearLastCreatedAccountId()
    #if DEBUG func _setAccountsForTesting(_ list: [AccountDTO]) #endif
}
```

From ios/BudgetPlanner/Domain/MoneyFormatter.swift:
```swift
enum MoneyParser {
    /// "100" → 10000; "1 500,50" → 150050; "abc" → nil.
    static func parseToCents(_ raw: String) -> Int?
}
```

From ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift:
```swift
static func create(_ request: AccountCreateRequest) async throws -> AccountDTO
// Returns server-assigned id + created_at.
// Backend (Phase 22 BE-02) sorts response GET /accounts с primary first, id ASC.
```

From ios/BudgetPlanner/Networking/DTO/AccountDTO.swift:
```swift
struct AccountCreateRequest: Encodable {
    let bank: String
    let kind: AccountKind
    let mask: String?
    let balanceCents: Int
    let primary: Bool?  // optional — encodeIfPresent на wire
}
```

Symbol-collision note:
- FeaturesV10/Accounts/NewAccountSheet.swift объявляет `struct NewAccountSheet` (V10 implementation).
- Наш v06 файл NewAccountSheet.swift объявляет `struct AccountsNewSheet` (rename per 60-01) — same file name, different struct name. NO collision.

T-60-01 mitigation (primary race):
- Не делаем client-side update других accounts. Backend сам управляет primary uniqueness (Phase 22 BE-02: при создании primary=true сервер UPDATE'ит другие primary→false в одной транзакции).
- После create → load() заново — UI отображает actual server state.

T-60-02 mitigation (mask injection):
- UI gate: keystroke handler filters digits + truncates prefix(4).
- canCreate ENFORCES `^\d{4}$` regex для kind==.card.
- Defence-in-depth: backend Pydantic `max_length=16` на mask поле.

T-60-03 mitigation (error.localizedDescription):
- createError: String? в AccountsViewModel set с fixed Russian copy ("Не удалось создать счёт" / "Проверьте подключение и попробуйте ещё раз").
- Sheet dismissed на failure (т.к. createAccount returns false), banner shown в AccountsView (inline section above hero).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AccountsViewModel.createAccount() + createError state</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post 60-02)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsListV10ViewModel.swift (lines 56-85 — createAccount pattern + submitting flag)
    - ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift (full)
  </read_first>
  <behavior>
    Test 1 — initial state: vm.createError == nil.
    Test 2 — clearCreateError() sets createError to nil after manual set.
    (Реальные network success/failure тесты для createAccount не покрываем — требуют мок APIClient; ручное smoke в Task 4.)
  </behavior>
  <action>
    Изменить `AccountsViewModel`:

    1. **Добавить state `createError: String?`** (после `lastCreatedAccountId`):
       ```swift
       /// User-visible inline banner copy на create failure.
       /// Cleared automatically на successful load() refresh ИЛИ manually через clearCreateError().
       var createError: String? = nil
       ```

    2. **Изменить `submitting` с `private(set)` на `private(set) var submitting: Bool = false`** (без изменений — уже так). Подтвердить что флаг exposed через computed чтения view (private(set) → readable from View).

    3. **Полностью заменить body `createAccount(...)`**:
       ```swift
       /// Returns true on success, false on failure. Caller (sheet) использует
       /// return value для decision: dismiss sheet / показать inline error в sheet.
       /// Текущий план dismiss'ит sheet ВСЕГДА — error banner шоуится в AccountsView.
       func createAccount(
           bank: String,
           kind: AccountKind,
           mask: String?,
           balanceCents: Int,
           primary: Bool
       ) async -> Bool {
           submitting = true
           defer { submitting = false }

           // Normalize mask: nil if not card OR empty string.
           let normalisedMask: String? = (kind == .card && mask?.isEmpty == false) ? mask : nil

           let request = AccountCreateRequest(
               bank: bank.trimmingCharacters(in: .whitespacesAndNewlines),
               kind: kind,
               mask: normalisedMask,
               balanceCents: balanceCents,
               primary: primary ? true : nil
           )

           do {
               let created = try await AccountsAPI.create(request)
               createError = nil
               // Reload list — backend сериализует primary uniqueness (T-60-01)
               // и возвращает sorted list (primary first, id ASC).
               await load()
               // Trigger ScrollViewReader.scrollTo via .onChange.
               lastCreatedAccountId = created.id
               sheet = .none
               return true
           } catch {
               print("[AccountsViewModel] createAccount failed: \(error)")
               // T-60-03: filtered Russian copy, no .localizedDescription.
               createError = "Не удалось создать счёт. Проверьте подключение и попробуйте ещё раз."
               // Sheet dismisses anyway per UX decision in CONTEXT D-4 «на failure: inline banner»;
               // banner живёт на AccountsView. Closing sheet keeps user в list view с error visible.
               sheet = .none
               return false
           }
       }
       ```

    4. **Добавить helper** `clearCreateError()`:
       ```swift
       func clearCreateError() {
           self.createError = nil
       }
       ```

    Сохранить existing surface (Status, SheetMode, load(), _setAccountsForTesting и т.д.) без изменений.

    T-60-03 verify: `grep -c "error.localizedDescription" → 0`.
  </action>
  <verify>
    <automated>grep -c "AccountsAPI.create(" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "createError" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: ≥3 (var declaration + set on failure + clearCreateError) -->
    <automated>grep -c "lastCreatedAccountId = created.id" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 — scroll-to hook fires on success. -->
    <automated>grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 0 (T-60-03). -->
    <automated>grep -c "Не удалось создать счёт" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 -->
  </verify>
  <acceptance_criteria>
    - createAccount() POST'ит через AccountsAPI.create, on success: load() + set lastCreatedAccountId + dismiss sheet + return true.
    - On failure: createError set с filtered Russian copy, dismiss sheet, return false.
    - No error.localizedDescription anywhere.
    - clearCreateError() helper exists.
  </acceptance_criteria>
  <done>
    Все grep gates pass. Build будет broken до Task 2/3 (sheet body всё ещё stub, view ещё без ScrollViewReader) — но acceptable plan-boundary т.к. оба завершаются в этом же плане.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Native Form body for AccountsNewSheet (validation + submit)</name>
  <files>ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift (post 60-01)
    - ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep2AccountsView.swift (lines 144-200 — AddAccountSheet pattern: NavigationStack + Form + .confirmationAction + decimalPad)
    - ios/BudgetPlanner/Features/Onboarding/NativeOnboardingStep1IncomeView.swift (full — MoneyParser usage + .onChange)
    - ios/BudgetPlanner/Features/Management/CategoriesView.swift (lines 217-265 — NewCategorySheet pattern: presentationDetents)
    - ios/BudgetPlanner/FeaturesV10/Accounts/NewAccountSheet.swift (full — V10 reference для mask sanitization + validation matrix; **NO визуальный port** — V10 это poster-style, мы делаем native)
    - ios/BudgetPlanner/Domain/MoneyFormatter.swift (full)
  </read_first>
  <behavior>
    Form fields rendering:
    - Section «Банк»: TextField($bank), autocorrectionDisabled, textInputAutocapitalization(.words).
    - Section «Тип»: Picker(selection: $kind, .segmented) с 3 опциями (Карта / Наличные / Сбережения).
    - Section «Последние 4 цифры» (conditional, kind == .card only): TextField($mask), keyboardType(.numberPad), .onChange sanitize digits + prefix(4).
    - Section «Текущий баланс»: HStack TextField($balanceText, keyboardType(.decimalPad), .multilineTextAlignment(.trailing), monospacedDigit) + Text("₽").
    - Section «Основной счёт»: Toggle($primary). Footer text «Снимет статус с другого счёта» только когда primary == true (informative).

    Validation (canCreate computed):
    - bank.trimmingCharacters(in: .whitespaces).count >= 1
    - if kind == .card: mask.matches `^\d{4}$` (NSRegularExpression OR mask.count == 4 && mask.allSatisfy(\.isNumber))
    - balanceCents >= 0 (MoneyParser.parseToCents(balanceText) returns Int >= 0; nil → 0 fallback NOT acceptable for валидной формы — но empty string должна вернуть 0 как валидную «нулевой баланс» интерпретацию: пустой баланс == 0 копеек).
    - !submitting

    Submit:
    - .toolbar { ToolbarItem(.confirmationAction) { Button("Создать") { Task { ... } } .disabled(!canCreate) } }
    - .toolbar { ToolbarItem(.cancellationAction) { Button("Отмена") { onCancel() } } }

    Submit flow:
    - Task { let success = await onCreate(bank, kind, normalisedMask, balanceCents, primary); /* sheet dismisses через onCreate ВСЕГДА (внутри createAccount sheet = .none); НЕТ необходимости вызывать dismiss() из sheet */ }
    - Compute normalisedMask: kind == .card && !mask.isEmpty ? mask : nil
    - Compute balanceCents: MoneyParser.parseToCents(balanceText) ?? 0 (empty → 0).
  </behavior>
  <action>
    Полностью переписать body `AccountsNewSheet` в `ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift`. Сохранить top-of-file комментарий и docstring. Удалить stub body.

    Структура:
    ```swift
    import SwiftUI

    /// (existing docstring сохранён)
    struct AccountsNewSheet: View {
        let submitting: Bool
        let onCreate: (_ bank: String, _ kind: AccountKind, _ mask: String?, _ balanceCents: Int, _ primary: Bool) async -> Bool
        let onCancel: () -> Void

        @State private var bank: String = ""
        @State private var kind: AccountKind = .card
        @State private var mask: String = ""
        @State private var balanceText: String = ""
        @State private var primary: Bool = false

        private var balanceCents: Int {
            // empty → 0 (нулевой баланс — валидно)
            guard !balanceText.trimmingCharacters(in: .whitespaces).isEmpty else { return 0 }
            return MoneyParser.parseToCents(balanceText) ?? 0
        }

        private var trimmedBank: String {
            bank.trimmingCharacters(in: .whitespacesAndNewlines)
        }

        private var maskMatchesPattern: Bool {
            mask.count == 4 && mask.allSatisfy(\.isNumber)
        }

        var canCreate: Bool {
            guard !submitting else { return false }
            guard !trimmedBank.isEmpty else { return false }
            if kind == .card {
                guard maskMatchesPattern else { return false }
            }
            // balance может быть 0 — это «открыл счёт, баланс уточню позже».
            // Negative balance не допускаем.
            guard balanceCents >= 0 else { return false }
            return true
        }

        private var normalisedMask: String? {
            (kind == .card && !mask.isEmpty) ? mask : nil
        }

        var body: some View {
            NavigationStack {
                Form {
                    Section("Банк") {
                        TextField("Например, Т-Банк", text: $bank)
                            .autocorrectionDisabled()
                            .textInputAutocapitalization(.words)
                    }

                    Section("Тип") {
                        Picker("Тип", selection: $kind) {
                            Text("Карта").tag(AccountKind.card)
                            Text("Наличные").tag(AccountKind.cash)
                            Text("Сбережения").tag(AccountKind.savings)
                        }
                        .pickerStyle(.segmented)
                    }

                    if kind == .card {
                        Section("Последние 4 цифры") {
                            TextField("0000", text: Binding(
                                get: { mask },
                                set: { newVal in
                                    // T-60-02: digits-only + prefix(4)
                                    let digits = newVal.filter(\.isNumber)
                                    mask = String(digits.prefix(4))
                                }
                            ))
                            .keyboardType(.numberPad)
                            .monospacedDigit()
                        }
                    }

                    Section("Текущий баланс") {
                        HStack {
                            TextField("0", text: $balanceText)
                                .keyboardType(.decimalPad)
                                .multilineTextAlignment(.trailing)
                                .monospacedDigit()
                            Text("₽").foregroundStyle(.secondary)
                        }
                    }

                    Section {
                        Toggle(isOn: $primary) {
                            Text("Основной счёт")
                        }
                    } footer: {
                        if primary {
                            Text("Снимет статус «основной» с другого счёта.")
                        }
                    }
                }
                .navigationTitle("Новый счёт")
                .navigationBarTitleDisplayMode(.inline)
                .toolbar {
                    ToolbarItem(placement: .cancellationAction) {
                        Button("Отмена") { onCancel() }
                    }
                    ToolbarItem(placement: .confirmationAction) {
                        Button(submitting ? "Создание…" : "Создать") {
                            Task {
                                _ = await onCreate(
                                    trimmedBank,
                                    kind,
                                    normalisedMask,
                                    balanceCents,
                                    primary
                                )
                                // Sheet dismissal управляется AccountsViewModel.createAccount
                                // (sheet = .none on both success AND failure). Banner
                                // на failure живёт в AccountsView outside sheet.
                            }
                        }
                        .disabled(!canCreate)
                    }
                }
            }
            .presentationDetents([.medium, .large])
            .interactiveDismissDisabled(submitting)
        }
    }
    ```

    Доступ к `canCreate` и `normalisedMask` сделать `var` (НЕ private) чтобы unit tests могли использовать их через @testable import. ИЛИ оставить private и тестировать через separate pure helper.

    **Decision**: вынести в pure helper `enum AccountsNewSheetValidation { static func canCreate(bank:kind:mask:balanceCents:submitting:) -> Bool }` для тестирования. Кратко:
    ```swift
    /// Pure validation helper для NewAccountSheet — testable без SwiftUI runtime.
    enum AccountsNewSheetValidation {
        static func canCreate(
            bank: String,
            kind: AccountKind,
            mask: String,
            balanceCents: Int,
            submitting: Bool
        ) -> Bool {
            guard !submitting else { return false }
            let trimmed = bank.trimmingCharacters(in: .whitespacesAndNewlines)
            guard !trimmed.isEmpty else { return false }
            if kind == .card {
                guard mask.count == 4, mask.allSatisfy(\.isNumber) else { return false }
            }
            guard balanceCents >= 0 else { return false }
            return true
        }

        static func normaliseMask(_ raw: String, kind: AccountKind) -> String? {
            (kind == .card && !raw.isEmpty) ? raw : nil
        }
    }
    ```

    AccountsNewSheet body использует `AccountsNewSheetValidation.canCreate(...)` для disabled-binding submit.

    NO sheet-level error banner — banner shown в AccountsView outside sheet (per CONTEXT D-4 «failure: inline banner» — мы интерпретируем «inline» как «в context list, не в alert»).
  </action>
  <verify>
    <automated>grep -c "AccountKind.card\|AccountKind.cash\|AccountKind.savings" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: ≥3 (3 picker options) -->
    <automated>grep -c "MoneyParser.parseToCents" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: ≥1 -->
    <automated>grep -c "AccountsNewSheetValidation" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: ≥2 (helper + usage in canCreate binding) -->
    <automated>grep -c "kind == .card" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: ≥2 (conditional mask field + validation branch) -->
    <automated>grep -c "Button(\"Создать\"\|Button(submitting ? \"Создание" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: ≥1 (submit button) -->
    <automated>grep -c ".confirmationAction\|.cancellationAction" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: ≥2 (toolbar items) -->
    <automated>grep -c "presentationDetents" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: 1 -->
  </verify>
  <acceptance_criteria>
    - NavigationStack + Form с 5 секциями (Банк / Тип / Последние 4 цифры [conditional] / Текущий баланс / Toggle).
    - Picker(.segmented) с 3 опциями (Карта / Наличные / Сбережения).
    - Mask field появляется только при kind == .card.
    - Mask onChange sanitize digits + prefix(4) (T-60-02).
    - Balance parsed через MoneyParser.parseToCents; empty → 0 как валидная интерпретация.
    - Primary Toggle с conditional footer text.
    - Submit Button «Создать» в .confirmationAction, disabled до canCreate; во время submitting label «Создание…».
    - Cancel Button «Отмена» в .cancellationAction → onCancel().
    - presentationDetents([.medium, .large]).
    - interactiveDismissDisabled(submitting) — нельзя swipe-down во время submit.
    - AccountsNewSheetValidation pure helper exists в том же файле (testable).
  </acceptance_criteria>
  <done>
    Все grep gates pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AccountsView ScrollViewReader + scroll-to-new + createError banner</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountsView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Accounts/AccountsView.swift (post 60-02)
    - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift (post Task 1 этого плана)
    - ios/BudgetPlanner/Features/Transactions/TransactionsView.swift (deleteError banner pattern — overlay ZStack — НО мы используем inline Section banner в этом плане для consistency с CategoriesView)
    - ios/BudgetPlanner/Features/Management/CategoriesView.swift (lines 90-100 — Label + foregroundStyle(.red) error pattern)
  </read_first>
  <behavior>
    1. Обернуть существующий List в `ScrollViewReader { proxy in ... }`.
    2. ForEach row внутри NavigationLink — добавить `.id(acct.id)` чтобы ScrollViewReader мог scrollTo.
    3. `.onChange(of: viewModel.lastCreatedAccountId)` callback:
       - Если new value != nil → `withAnimation(.easeInOut(duration: 0.3)) { proxy.scrollTo(newId, anchor: .center) }`, затем `viewModel.clearLastCreatedAccountId()`.
    4. Добавить **createError banner section** перед heroSection (только если viewModel.createError != nil И viewModel.status == .ready) — это inline Section со cross-button:
       ```swift
       Section {
           HStack(spacing: 12) {
               Image(systemName: "exclamationmark.triangle.fill")
                   .foregroundStyle(.red)
               Text(msg)
                   .font(.callout)
                   .foregroundStyle(.primary)
               Spacer()
               Button {
                   viewModel.clearCreateError()
               } label: {
                   Image(systemName: "xmark.circle.fill")
                       .foregroundStyle(.secondary)
               }
               .buttonStyle(.borderless)
           }
       }
       ```
    5. createError banner ОТДЕЛЕН от error status section (загрузка); load() success не очищает createError автоматически — только user dismisses через xmark OR another successful create resets его в Task 1's createAccount() (где createError = nil на success).
  </behavior>
  <action>
    Модифицировать `ios/BudgetPlanner/Features/Accounts/AccountsView.swift`. Конкретные правки:

    1. Заменить root `List { ... }` блок (внутри body) на:
       ```swift
       ScrollViewReader { proxy in
           List {
               // existing switch на viewModel.status...
               // BUT: в .ready ветке вставить createError banner перед hero.
           }
           .listStyle(.insetGrouped)
           // ... existing modifiers (navigationTitle, toolbar, navigationDestination, task, refreshable, sheet)
           .onChange(of: viewModel.lastCreatedAccountId) { _, newValue in
               guard let newId = newValue else { return }
               withAnimation(.easeInOut(duration: 0.3)) {
                   proxy.scrollTo(newId, anchor: .center)
               }
               viewModel.clearLastCreatedAccountId()
           }
       }
       ```

    2. Изменить switch ready branch:
       ```swift
       case .ready:
           if let msg = viewModel.createError {
               createErrorBanner(msg)
           }
           if viewModel.accounts.isEmpty {
               emptySection
           } else {
               heroSection
               accountsSection
           }
       ```

    3. Добавить **`private func createErrorBanner(_ msg: String) -> some View`**:
       ```swift
       private func createErrorBanner(_ msg: String) -> some View {
           Section {
               HStack(spacing: 12) {
                   Image(systemName: "exclamationmark.triangle.fill")
                       .foregroundStyle(.red)
                   Text(msg)
                       .font(.callout)
                       .foregroundStyle(.primary)
                   Spacer(minLength: 8)
                   Button {
                       viewModel.clearCreateError()
                   } label: {
                       Image(systemName: "xmark.circle.fill")
                           .foregroundStyle(.secondary)
                   }
                   .buttonStyle(.borderless)
                   .accessibilityLabel("Скрыть ошибку")
               }
           }
       }
       ```

    4. Изменить `accountsSection` чтобы добавить `.id(acct.id)` на ForEach row:
       ```swift
       private var accountsSection: some View {
           Section("Счета") {
               ForEach(viewModel.accounts) { acct in
                   NavigationLink(value: acct.id) {
                       AccountRow(account: acct)
                   }
                   .id(acct.id)  // <-- ADD: ScrollViewReader target.
               }
           }
       }
       ```

    NO иных правок. Сохранить остальной structure (loadingSection / errorSection / emptySection / heroSection / iconForKind / subtitleFor / AccountRow / accountCountLabel).

    Сохранить sheet content — onCreate уже передаёт closure в viewModel.createAccount; теперь viewModel.createAccount реально работает (Task 1 заполнил).
  </action>
  <verify>
    <automated>grep -c "ScrollViewReader" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "proxy.scrollTo(" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c ".onChange(of: viewModel.lastCreatedAccountId)" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "createErrorBanner" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: ≥2 (define + usage) -->
    <automated>grep -c "viewModel.clearCreateError" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "exclamationmark.triangle.fill" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (banner icon) -->
    <automated>grep -c ".id(acct.id)" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 (ScrollViewReader target) -->
    <automated>cd ios && make build 2>&1 | tail -10 | grep -cE "BUILD SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - ScrollViewReader wraps List внутри body.
    - .onChange(of: viewModel.lastCreatedAccountId) триггерит withAnimation proxy.scrollTo + clearLastCreatedAccountId.
    - createErrorBanner Section добавлена в .ready branch перед empty/hero/list, видна только при createError != nil.
    - xmark.circle.fill button → viewModel.clearCreateError().
    - ForEach row имеет .id(acct.id) — ScrollViewReader target.
    - Build clean.
  </acceptance_criteria>
  <done>
    All grep gates pass; make build SUCCEEDED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit tests for AccountsNewSheetValidation</name>
  <files>ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift</files>
  <read_first>
    - ios/BudgetPlannerTests/Features/Accounts/AccountsViewModelTests.swift (post 60-02 — file structure pattern)
    - ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift (post Task 2 — AccountsNewSheetValidation surface)
  </read_first>
  <behavior>
    Test 1 — test_canCreate_emptyBank_returnsFalse: bank="" / "   " → false.
    Test 2 — test_canCreate_validCard_passes: bank="Т-Банк", kind=.card, mask="1234", balanceCents=0 → true.
    Test 3 — test_canCreate_cardMissingMask_fails: kind=.card, mask="" → false.
    Test 4 — test_canCreate_cardInvalidMaskLength_fails: kind=.card, mask="123" → false; mask="12345" → false.
    Test 5 — test_canCreate_cardInvalidMaskNonDigit_fails: kind=.card, mask="12A4" → false.
    Test 6 — test_canCreate_cashIgnoresMask: kind=.cash, mask irrelevant → true with valid bank.
    Test 7 — test_canCreate_savingsIgnoresMask: kind=.savings → true with valid bank.
    Test 8 — test_canCreate_negativeBalance_fails: balanceCents=-100 → false.
    Test 9 — test_canCreate_submittingForcesFalse: submitting=true → false (even с valid form).
    Test 10 — test_normaliseMask_cardWithMask_returnsValue: kind=.card, raw="1234" → "1234".
    Test 11 — test_normaliseMask_cardEmpty_returnsNil: kind=.card, raw="" → nil.
    Test 12 — test_normaliseMask_cash_returnsNil: kind=.cash, raw="1234" → nil (ignored на не-card).
  </behavior>
  <action>
    Создать файл `ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift`:
    ```swift
    import XCTest
    @testable import BudgetPlanner

    final class AccountsNewSheetValidationTests: XCTestCase {

        // MARK: - canCreate

        func test_canCreate_emptyBank_returnsFalse() {
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "", kind: .cash, mask: "", balanceCents: 0, submitting: false
            ))
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "   ", kind: .cash, mask: "", balanceCents: 0, submitting: false
            ))
        }

        func test_canCreate_validCard_passes() {
            XCTAssertTrue(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "1234", balanceCents: 0, submitting: false
            ))
        }

        func test_canCreate_cardMissingMask_fails() {
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "", balanceCents: 0, submitting: false
            ))
        }

        func test_canCreate_cardInvalidMaskLength_fails() {
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "123", balanceCents: 0, submitting: false
            ))
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "12345", balanceCents: 0, submitting: false
            ))
        }

        func test_canCreate_cardInvalidMaskNonDigit_fails() {
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "12A4", balanceCents: 0, submitting: false
            ))
        }

        func test_canCreate_cashIgnoresMask() {
            XCTAssertTrue(AccountsNewSheetValidation.canCreate(
                bank: "Наличные", kind: .cash, mask: "", balanceCents: 0, submitting: false
            ))
            XCTAssertTrue(AccountsNewSheetValidation.canCreate(
                bank: "Наличные", kind: .cash, mask: "wat", balanceCents: 5000, submitting: false
            ))
        }

        func test_canCreate_savingsIgnoresMask() {
            XCTAssertTrue(AccountsNewSheetValidation.canCreate(
                bank: "Накопит. счёт", kind: .savings, mask: "", balanceCents: 100000, submitting: false
            ))
        }

        func test_canCreate_negativeBalance_fails() {
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "1234", balanceCents: -100, submitting: false
            ))
        }

        func test_canCreate_submittingForcesFalse() {
            XCTAssertFalse(AccountsNewSheetValidation.canCreate(
                bank: "Т-Банк", kind: .card, mask: "1234", balanceCents: 0, submitting: true
            ))
        }

        // MARK: - normaliseMask

        func test_normaliseMask_cardWithMask_returnsValue() {
            XCTAssertEqual(
                AccountsNewSheetValidation.normaliseMask("1234", kind: .card),
                "1234"
            )
        }

        func test_normaliseMask_cardEmpty_returnsNil() {
            XCTAssertNil(AccountsNewSheetValidation.normaliseMask("", kind: .card))
        }

        func test_normaliseMask_cash_returnsNil() {
            XCTAssertNil(AccountsNewSheetValidation.normaliseMask("1234", kind: .cash))
        }

        func test_normaliseMask_savings_returnsNil() {
            XCTAssertNil(AccountsNewSheetValidation.normaliseMask("1234", kind: .savings))
        }
    }
    ```

    После создания:
    1. `cd ios && xcodegen generate` — захватить файл.
    2. `cd ios && xcodebuild test -only-testing:BudgetPlannerTests/AccountsNewSheetValidationTests CODE_SIGNING_ALLOWED=NO -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro'` — все 12+ tests pass.
  </action>
  <verify>
    <automated>test -f ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift</automated>
    <automated>grep -c "func test_" ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift</automated>
    <!-- Expected: ≥12 -->
    <automated>grep -c "@testable import BudgetPlanner" ios/BudgetPlannerTests/Features/Accounts/AccountsNewSheetValidationTests.swift</automated>
    <!-- Expected: 1 -->
    <automated>cd ios && xcodegen generate >/dev/null 2>&1 && xcodebuild test -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/AccountsNewSheetValidationTests CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20 | grep -cE "Test Suite 'AccountsNewSheetValidationTests' passed|TEST SUCCEEDED"</automated>
    <!-- Expected: ≥1 -->
  </verify>
  <acceptance_criteria>
    - Файл создан с ≥12 test_ функциями.
    - Все тесты pass через xcodebuild.
    - Покрывает: empty bank / card mask matrix / cash & savings ignore mask / negative balance / submitting=true.
  </acceptance_criteria>
  <done>
    grep gates pass; xcodebuild test SUCCEEDED.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    NewAccountSheet (native Form) + AccountsViewModel.createAccount real implementation + ScrollViewReader scroll-to-new + inline createError banner. Wave 3 завершает create flow для Accounts.
  </what-built>
  <how-to-verify>
    1. Запустить app в симуляторе (iPhone 17 Pro, BACKEND_URL=http://192.168.31.117:8000 или localhost).
    2. Tab «Управление» → row «Счета» → AccountsView показывает реальные счета.
    3. Tap toolbar `+` (plus.circle.fill) → AccountsNewSheet открывается с native iOS Form в `.medium` detent.
    4. Validation matrix:
       a. Без bank → «Создать» disabled.
       b. Bank=«Т-Банк», kind=Карта, mask=«123» (3 цифры) → «Создать» disabled.
       c. Bank=«Т-Банк», kind=Карта, mask=«1234» → «Создать» enabled.
       d. Kind=Наличные → mask секция исчезает; «Создать» enabled с bank only.
       e. Kind=Сбережения → mask секция исчезает; «Создать» enabled.
       f. Balance ноль → валидно; balance отрицательное (digits-only клавиатура не позволяет — должно быть автоматически).
       g. Primary toggle ON → footer text «Снимет статус «основной» с другого счёта.» появляется.
    5. Submit success path: заполни валидные значения → tap «Создать» → label меняется на «Создание…», disabled; sheet закрывается; List перерисовывается; новая строка появляется (если scroll нужен — автоматически скроллится с анимацией в .center anchor); orange star на новой строке если primary=true; existing primary потерял star (T-60-01 — backend refetch).
    6. Submit failure path (для теста: остановить backend / убрать WiFi):
       a. Заполни валидные значения → tap «Создать».
       b. Sheet закрывается; в AccountsView появляется красный inline banner Section с exclamationmark.triangle.fill + «Не удалось создать счёт. Проверьте подключение и попробуйте ещё раз.» + xmark.circle.fill кнопка.
       c. Tap xmark → banner исчезает.
    7. Pull-to-refresh работает (тянуть List вниз → spinner → reload).
    8. Cancel button «Отмена» в toolbar → sheet закрывается без mutation.
    9. Interactive dismiss (swipe down) во время submit — заблокирован (`interactiveDismissDisabled(submitting)`).
    10. Build / lint: `cd ios && make build` clean; `xcodebuild test -only-testing:BudgetPlannerTests/AccountsNewSheetValidationTests` 12/12 pass; `AccountsViewModelTests` 6/6 pass.
  </how-to-verify>
  <resume-signal>
    Введите «approved» если всё работает, или опишите конкретные баги для исправления.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → AccountsNewSheet Form | Untrusted input (bank text, mask digits, balance digits, primary toggle). Mitigation: UI gates (T-60-02) + backend Pydantic. |
| iOS app → POST /api/v1/accounts | Untrusted backend response; status code != 2xx → catch → filtered Russian copy banner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | Integrity (primary race) | createAccount() + AccountsAPI.create | mitigate | UI не делает явный update других accounts. Server-side (Phase 22 BE-02) сериализует primary uniqueness в одной транзакции. После create → load() refetch — UI отображает actual server state. |
| T-60-02 | Input Validation / Tampering | NewAccountSheet mask field | mitigate | UI gate: keystroke filter digits-only + truncate prefix(4) в TextField onChange; canCreate enforces `mask.count == 4 && allSatisfy(\.isNumber)` для kind == .card; AccountsNewSheetValidation.canCreate testable. Backend Pydantic max_length=16 defence-in-depth. |
| T-60-03 | Information Disclosure | createAccount() catch block | mitigate | createError = "Не удалось создать счёт. Проверьте подключение и попробуйте ещё раз." — fixed Russian copy. Full error printed via `print(...)` only. Verified: `grep -c "error.localizedDescription" → 0` в AccountsViewModel + NewAccountSheet + AccountsView. |
</threat_model>

<verification>
- `grep -c "AccountsAPI.create(" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift` returns 1.
- `grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift` returns 0.
- `grep -c "error.localizedDescription" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift` returns 0.
- `grep -c "AccountsNewSheetValidation" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift` returns ≥2.
- `grep -c "ScrollViewReader" ios/BudgetPlanner/Features/Accounts/AccountsView.swift` returns 1.
- `grep -c "proxy.scrollTo(" ios/BudgetPlanner/Features/Accounts/AccountsView.swift` returns 1.
- `cd ios && make build` clean.
- `xcodebuild test -only-testing:BudgetPlannerTests/AccountsNewSheetValidationTests` passes ≥12 tests.
- Manual smoke approved (checkpoint).
</verification>

<success_criteria>
1. NewAccountSheet (struct AccountsNewSheet) — native Form с 5 секциями, conditional mask, Picker(.segmented), MoneyParser balance, primary Toggle.
2. AccountsNewSheetValidation pure helper в том же файле testable через @testable.
3. AccountsViewModel.createAccount() реально POST'ит, on success: load() + lastCreatedAccountId + dismiss; on failure: createError + dismiss.
4. AccountsView wrapped в ScrollViewReader; .onChange(of: lastCreatedAccountId) → scrollTo + clear; createError inline banner с xmark dismiss.
5. T-60-01 mitigation: client не управляет primary uniqueness — backend serializes; UI reload отображает actual state.
6. T-60-02 mitigation: mask sanitized keystroke + canCreate enforces ^\d{4}$ для .card.
7. T-60-03 mitigation: НЕТ error.localizedDescription anywhere; filtered Russian copy в banner.
8. AccountsNewSheetValidationTests ≥12 tests pass.
9. make build clean.
10. Manual smoke approved.
</success_criteria>

<output>
After completion, create `.planning/phases/60-accounts-v06/60-03-SUMMARY.md` summarising:
- NewAccountSheet composition (NavigationStack + Form + 5 sections + conditional mask + .segmented Picker)
- AccountsNewSheetValidation pure helper API (canCreate + normaliseMask)
- AccountsViewModel.createAccount() control flow (submitting flag + AccountsAPI.create + load() + lastCreatedAccountId + createError)
- ScrollViewReader integration в AccountsView (.onChange(of: lastCreatedAccountId) → proxy.scrollTo с easeInOut 0.3s)
- createError banner UX (red icon + filtered copy + xmark dismiss; Section перед hero в .ready state)
- Validation tests coverage matrix (12+ scenarios)
- Manual smoke result (approved/issues)
- Threat mitigations (T-60-01, T-60-02, T-60-03 — verified)
</output>
