---
plan_id: 60-01
phase: 60
title: ManagementItem.accounts registration + scaffold files for Features/Accounts
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Features/Management/ManagementView.swift
  - ios/BudgetPlanner/Features/Accounts/AccountsView.swift
  - ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift
  - ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift
  - ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift
  - ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift
autonomous: true
requirements:
  - v1.1.2-60-CONTEXT-area-1-navigation
  - v1.1.2-60-CONTEXT-area-5-coexistence
threat_ids:
  - T-60-03
must_haves:
  truths:
    - "ManagementItem.ID enum имеет case `.accounts` зарегистрированный перед `.categories`."
    - "ManagementItem.all массив содержит entry «Счета» (icon `creditcard.fill`, ownerOnly: false) перед entry .categories."
    - "ManagementView.destination(for:) обрабатывает case `.accounts → AccountsView()`."
    - "Каталог `ios/BudgetPlanner/Features/Accounts/` существует."
    - "Stub-файлы AccountsView, AccountsViewModel, AccountDetailView, AccountDetailViewModel, NewAccountSheet компилируются (типы определены, body заполняется в 60-02/60-03/60-04)."
    - "FeaturesV10/Accounts/* НЕ модифицирован (NewAccountSheet остаётся V10 implementation в FeaturesV10/Accounts/)."
    - "MainShell.swift НЕ модифицирован."
    - "Build: `make build` clean (0 errors, 0 new warnings)."
  artifacts:
    - path: "ios/BudgetPlanner/Features/Management/ManagementView.swift"
      provides: "ManagementItem.accounts registration + destination(for:.accounts) dispatch"
      contains: "case accounts"
      contains_not: "FeaturesV10/Accounts"
    - path: "ios/BudgetPlanner/Features/Accounts/AccountsView.swift"
      provides: "Native AccountsView stub (struct + body returns ProgressView placeholder)"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift"
      provides: "@MainActor @Observable AccountsViewModel stub with Status enum + properties (filled in 60-02)"
      min_lines: 30
    - path: "ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift"
      provides: "AccountDetailView(accountId: Int) stub"
      min_lines: 10
    - path: "ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift"
      provides: "@MainActor @Observable AccountDetailViewModel stub (init with accountId)"
      min_lines: 15
    - path: "ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift"
      provides: "NewAccountSheet stub view (struct conforming to View) — body returns ProgressView placeholder; Form implementation lands in 60-03"
      min_lines: 10
  key_links:
    - from: "ManagementView.destination(for:)"
      to: "AccountsView"
      via: "switch case .accounts"
      pattern: "case .accounts:\\s*AccountsView"
    - from: "ManagementItem.all"
      to: "Features/Accounts/AccountsView.swift"
      via: "ManagementItem(id: .accounts, ...) entry"
      pattern: "id: .accounts"
---

<objective>
Зарегистрировать новый домен «Счета» в v06 native shell через расширение `ManagementItem.ID` + `ManagementItem.all` + `ManagementView.destination(for:)` и создать каталог `ios/BudgetPlanner/Features/Accounts/` с пятью stub-файлами (AccountsView / AccountsViewModel / AccountDetailView / AccountDetailViewModel / NewAccountSheet). Сборка должна оставаться зелёной (make build clean). Реальная функциональность будет добавлена в Plans 60-02 / 60-03 / 60-04.

Purpose: установить точку входа в Management hub + создать пустые типы, чтобы downstream plans могли независимо заполнять body / VM без блокирующих cross-file правок. ManagementItem.accounts должен быть видим в UI сразу после этого плана (tap → AccountsView с ProgressView placeholder). Это даёт визуальное подтверждение интеграции до того, как реальный UI готов.

Output:
- ManagementItem.ID enum расширен `.accounts`.
- ManagementItem.all содержит entry «Счета» перед .categories.
- ManagementView.destination(for:) обрабатывает .accounts.
- 5 stub-файлов в `Features/Accounts/` компилируются как часть таргета BudgetPlanner.
- `xcodegen generate` пересобирает `.xcodeproj` чтобы захватить новые файлы.
- `make build` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/60-accounts-v06/60-CONTEXT.md
@.planning/phases/59-transactions-v06/59-01-SUMMARY.md
@.planning/phases/57-onboarding-4step/57-CONTEXT.md
@ios/BudgetPlanner/Features/Management/ManagementView.swift
@ios/BudgetPlanner/Networking/DTO/AccountDTO.swift
@ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift

<interfaces>
<!-- Key types/contracts. DO NOT re-explore the codebase. -->

From ios/BudgetPlanner/Features/Management/ManagementView.swift (existing):
```swift
struct ManagementItem: Identifiable, Hashable {
    enum ID: String, Hashable {
        case analytics, subscriptions, template, categories, settings, access
    }
    let id: ID
    let label: String
    let description: String
    let icon: String
    let ownerOnly: Bool

    static let all: [ManagementItem] = [
        .init(id: .analytics, ...),
        .init(id: .subscriptions, ...),
        .init(id: .template, ...),
        .init(id: .categories, ...),  // <-- new .accounts entry inserted BEFORE this one
        .init(id: .settings, ...),
        .init(id: .access, ..., ownerOnly: true),
    ]
}

// ManagementView.destination(for:) switch уже содержит case-ы для всех existing ID;
// добавляем case .accounts: AccountsView().
```

From ios/BudgetPlanner/Networking/DTO/AccountDTO.swift (existing):
```swift
enum AccountKind: String, Codable {
    case card
    case cash
    case savings
}

struct AccountDTO: Decodable, Identifiable, Equatable {
    let id: Int
    let bank: String
    let mask: String?
    let kind: AccountKind
    let balanceCents: Int
    let primary: Bool
    let createdAt: Date?
}

struct AccountCreateRequest: Encodable {
    let bank: String
    let kind: AccountKind
    let mask: String?
    let balanceCents: Int
    let primary: Bool?
}
```

From ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift:
```swift
@MainActor
enum AccountsAPI {
    static func list() async throws -> [AccountDTO]
    static func create(_ request: AccountCreateRequest) async throws -> AccountDTO
}
// NO update / NO delete / NO setPrimary — backend not implemented.
```

Coexistence guard:
- FeaturesV10/Accounts/* остаётся untouched. Свой собственный `NewAccountSheet` живёт там в namespace FeaturesV10/Accounts — Swift allows two `NewAccountSheet` structs в разных модулях/папках но не в одном таргете. Решение: новый sheet в Features/Accounts/ называется `NewAccountSheet` точно так же — будет collision на module-level. **Mitigation: rename our v06 sheet to `AccountsNewSheet`** (избегаем коллизии с `FeaturesV10/Accounts/NewAccountSheet.swift`).
- Однако CONTEXT D-4 говорит «NewAccountSheet». Файл назовём `NewAccountSheet.swift` (имя файла отличается от FeaturesV10), но **struct** внутри будет назван `AccountsNewSheet` чтобы избежать Swift symbol collision. Plan 60-03 обновит NewAccountSheet.swift с full Form body.
- ВЕРИФИЦИРОВАНО: оба файла будут в одном таргете BudgetPlanner; Swift не разрешает два `struct NewAccountSheet` в одном модуле → MUST rename our struct. Имя файла остаётся «NewAccountSheet.swift» (Filesystem path не уникализирует Swift typealias).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ManagementItem.accounts registration to ManagementView.swift</name>
  <files>ios/BudgetPlanner/Features/Management/ManagementView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Management/ManagementView.swift (full — все 163 строки)
    - ios/BudgetPlanner/Features/Accounts/AccountsView.swift (DOES NOT EXIST YET — Task 2 создаёт; в этом таске ManagementView пока ссылается на AccountsView() который компилируется после Task 2)
  </read_first>
  <action>
    Внести три модификации в `ios/BudgetPlanner/Features/Management/ManagementView.swift`:

    1. **Enum `ManagementItem.ID`** (line ~132): добавить `.accounts` case. Точная замена:
       ```swift
       // ДО:
       enum ID: String, Hashable {
           case analytics, subscriptions, template, categories, settings, access
       }
       // ПОСЛЕ:
       enum ID: String, Hashable {
           case analytics, subscriptions, template, accounts, categories, settings, access
       }
       ```

    2. **Массив `ManagementItem.all`** (line ~142): вставить новый `.init(...)` entry для `.accounts` **между** `.template` и `.categories` entries (CONTEXT D-1: «перед .categories»). Шаблон:
       ```swift
       .init(id: .accounts, label: "Счета",
             description: "Карты и наличные, основной счёт",
             icon: "creditcard.fill", ownerOnly: false),
       ```

    3. **Метод `destination(for:)`** (line ~108): добавить case `.accounts: AccountsView()` **перед** `case .categories:`. Шаблон:
       ```swift
       case .accounts: AccountsView()
       ```

    NO иных изменений в файле. Сохранить порядок остальных case-ов / entry-ей.

    **Coexistence guard**: НЕ трогать `MainShell.swift`. НЕ трогать `FeaturesV10/Accounts/*`. Только `ManagementView.swift`.

    После этого таска `xcodegen generate` НЕ нужен (файл уже в таргете) — но build будет failing до Task 2 (AccountsView пока не существует). Это acceptable plan-boundary breakage; Task 2 закрывает.
  </action>
  <verify>
    <automated>grep -c "case analytics, subscriptions, template, accounts, categories" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (enum case list updated). -->
    <automated>grep -c "id: .accounts, label: \"Счета\"" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (ManagementItem entry). -->
    <automated>grep -c "case .accounts: AccountsView()" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (destination dispatch). -->
    <automated>grep -c "creditcard.fill" ios/BudgetPlanner/Features/Management/ManagementView.swift</automated>
    <!-- Expected: 1 (icon per D-1). -->
  </verify>
  <acceptance_criteria>
    - ManagementItem.ID enum имеет case `.accounts` между `.template` и `.categories`.
    - ManagementItem.all массив имеет entry «Счета» в правильной позиции (index 3, между template и categories).
    - destination(for:) дispatch обрабатывает `.accounts` → возвращает `AccountsView()`.
    - File otherwise unchanged (diff scoped к этим трём правкам).
    - MainShell.swift, FeaturesV10/Accounts/* НЕ модифицированы.
  </acceptance_criteria>
  <done>
    Все четыре grep gates pass. Build будет broken до завершения Task 2 (AccountsView не существует) — acceptable plan-boundary state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create scaffold files in Features/Accounts/ + regenerate Xcode project</name>
  <files>ios/BudgetPlanner/Features/Accounts/AccountsView.swift, ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift, ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift, ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift, ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Management/CategoriesView.swift (lines 80-186 — pattern для @MainActor @Observable VM + List(.insetGrouped) View)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountsListV10ViewModel.swift (full — Status enum / SheetMode / inFlight / submitting pattern)
    - ios/BudgetPlanner/FeaturesV10/Accounts/AccountDetailV10ViewModel.swift (full — accountId init + Status pattern)
    - ios/BudgetPlanner/Networking/DTO/AccountDTO.swift (full)
    - ios/BudgetPlanner/Networking/Endpoints/AccountsAPI.swift (full)
    - ios/Makefile (только для команды `make build` syntax — НЕ модифицировать)
  </read_first>
  <action>
    Создать каталог `ios/BudgetPlanner/Features/Accounts/` и пять stub-файлов. Все stubs должны компилироваться (типы определены полностью; body заполняется в downstream plans 60-02..60-04).

    **File 1: `ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift`** (per D-1, D-2; полный VM stub с полями — заполняется логикой в 60-02):
    ```swift
    import Foundation
    import Observation

    /// Phase 60 (v06 Native Rebuild): ViewModel для AccountsView.
    ///
    /// Stub-структура (Plan 60-01). Полная реализация load() / sumBalances /
    /// createAccount() — Plan 60-02. NewAccountSheet integration — Plan 60-03.
    ///
    /// Pattern: parallel to AccountsListV10ViewModel (FeaturesV10), но v06 native
    /// shell. Никакого V10 styling / poster-tokens.
    @MainActor
    @Observable
    final class AccountsViewModel {
        enum Status: Equatable {
            case idle
            case loading
            case ready
            case error(String)
        }

        enum SheetMode: Equatable {
            case none
            case newAccount
        }

        private(set) var status: Status = .idle
        private(set) var accounts: [AccountDTO] = []
        var sheet: SheetMode = .none
        private(set) var submitting: Bool = false

        /// Indicates last-created account id (for ScrollViewReader.scrollTo).
        /// Set by createAccount() on success; cleared after consumption.
        var lastCreatedAccountId: Int? = nil

        @ObservationIgnored
        private var inFlight: Bool = false

        // MARK: - Load (filled in 60-02)
        func load() async {
            // Plan 60-02 fills this body.
        }

        // MARK: - Mutations (filled in 60-03)
        func createAccount(
            bank: String,
            kind: AccountKind,
            mask: String?,
            balanceCents: Int,
            primary: Bool
        ) async -> Bool {
            // Plan 60-03 fills this body. Returns true on success.
            return false
        }

        // MARK: - Derived
        var sumBalancesCents: Int {
            accounts.reduce(0) { $0 + $1.balanceCents }
        }

        var accountCount: Int { accounts.count }
    }
    ```

    **File 2: `ios/BudgetPlanner/Features/Accounts/AccountsView.swift`** (stub — реальный body в 60-02):
    ```swift
    import SwiftUI

    /// Phase 60 (v06 Native Rebuild): native AccountsView со списком счетов.
    ///
    /// Stub (Plan 60-01) — placeholder body. Реальная List + Hero + rows
    /// landing в Plan 60-02. NewAccountSheet integration — 60-03. Push на
    /// AccountDetailView — 60-04.
    struct AccountsView: View {
        @State private var viewModel = AccountsViewModel()

        var body: some View {
            List {
                Section {
                    ProgressView("Загрузка…")
                        .frame(maxWidth: .infinity)
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Счета")
            .task { await viewModel.load() }
        }
    }
    ```

    **File 3: `ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift`** (stub — реальный body в 60-04):
    ```swift
    import Foundation
    import Observation

    /// Phase 60 (v06 Native Rebuild): ViewModel для AccountDetailView.
    ///
    /// Stub (Plan 60-01). Полная реализация load() / periodOps / day grouping
    /// — Plan 60-04.
    @MainActor
    @Observable
    final class AccountDetailViewModel {
        enum Status: Equatable {
            case idle
            case loading
            case ready
            case error(String)
        }

        let accountId: Int

        private(set) var status: Status = .idle
        private(set) var account: AccountDTO?
        private(set) var actuals: [ActualV10DTO] = []
        private(set) var categories: [CategoryV10DTO] = []
        private(set) var period: PeriodDTO?

        @ObservationIgnored
        private var inFlight: Bool = false

        @ObservationIgnored
        var calendar: Calendar = AccountDetailViewModel.defaultCalendar()

        private static func defaultCalendar() -> Calendar {
            var c = Calendar(identifier: .gregorian)
            c.timeZone = TimeZone(identifier: "Europe/Moscow") ?? .current
            return c
        }

        init(accountId: Int) {
            self.accountId = accountId
        }

        func load() async {
            // Plan 60-04 fills this body.
        }
    }
    ```

    **File 4: `ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift`** (stub — реальный body в 60-04):
    ```swift
    import SwiftUI

    /// Phase 60 (v06 Native Rebuild): native AccountDetailView.
    ///
    /// Stub (Plan 60-01). Hero (bank/kind/mask/balance) + history section —
    /// Plan 60-04.
    struct AccountDetailView: View {
        let accountId: Int

        @State private var viewModel: AccountDetailViewModel

        init(accountId: Int) {
            self.accountId = accountId
            self._viewModel = State(wrappedValue: AccountDetailViewModel(accountId: accountId))
        }

        var body: some View {
            List {
                Section {
                    ProgressView("Загрузка…")
                        .frame(maxWidth: .infinity)
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Счёт")
            .navigationBarTitleDisplayMode(.inline)
            .task { await viewModel.load() }
        }
    }
    ```

    **File 5: `ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift`** (stub — Form body в 60-03):
    ```swift
    import SwiftUI

    /// Phase 60 (v06 Native Rebuild): native Form sheet для создания счёта.
    ///
    /// **Symbol-collision avoidance**: FeaturesV10/Accounts/NewAccountSheet.swift
    /// уже определяет `struct NewAccountSheet` в том же модуле BudgetPlanner.
    /// Swift не разрешает два struct с одинаковым именем в одном модуле, поэтому
    /// v06 native sheet называется `AccountsNewSheet` (file path differs, struct
    /// name differs). Plan 60-03 заполняет полный Form body.
    struct AccountsNewSheet: View {
        let submitting: Bool
        let onCreate: (_ bank: String, _ kind: AccountKind, _ mask: String?, _ balanceCents: Int, _ primary: Bool) async -> Bool
        let onCancel: () -> Void

        var body: some View {
            // Plan 60-03 fills this body with native Form.
            NavigationStack {
                Form {
                    Section {
                        Text("Plan 60-03 заполнит этот sheet")
                            .foregroundStyle(.secondary)
                    }
                }
                .navigationTitle("Новый счёт")
                .navigationBarTitleDisplayMode(.inline)
                .toolbar {
                    ToolbarItem(placement: .cancellationAction) {
                        Button("Отмена") { onCancel() }
                    }
                }
            }
            .presentationDetents([.medium, .large])
        }
    }
    ```

    После создания всех пяти файлов:
    1. Запустить `cd ios && xcodegen generate` чтобы `.xcodeproj` подхватил новые файлы (project.yml использует sources path `BudgetPlanner` → автоматически включит Features/Accounts/).
    2. Запустить `cd ios && make build` чтобы убедиться что сборка clean.

    **Coexistence guards (mandatory verify)**:
    - FeaturesV10/Accounts/* НЕ модифицирован — `git status` должен показать только Features/Accounts/* и ManagementView.swift в diff.
    - MainShell.swift НЕ модифицирован.

    **Symbol collision check**: оба `NewAccountSheet.swift` файла существуют, но **struct types отличаются** (`NewAccountSheet` в FeaturesV10/Accounts/ vs `AccountsNewSheet` в Features/Accounts/). Swift compiler не ругается.

    **DO NOT**:
    - Не создавать unit tests в этом плане (придут в 60-02 для AccountsViewModel, 60-04 для AccountDetailViewModel).
    - Не заполнять body / load() — это будет признаком работы 60-01 за scope; downstream plans переписывают.
    - Не модифицировать AccountDTO, AccountsAPI, ActualV10DTO, CategoryV10DTO, PeriodDTO — все типы существуют.
    - Не делать `import FeaturesV10` или симметричное — наши stubs самодостаточны.
  </action>
  <verify>
    <automated>test -d ios/BudgetPlanner/Features/Accounts</automated>
    <!-- Каталог создан. -->
    <automated>test -f ios/BudgetPlanner/Features/Accounts/AccountsView.swift && test -f ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift && test -f ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift && test -f ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift && test -f ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Все пять файлов созданы. -->
    <automated>grep -c "struct AccountsView" ios/BudgetPlanner/Features/Accounts/AccountsView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "@Observable" ios/BudgetPlanner/Features/Accounts/AccountsViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "struct AccountDetailView" ios/BudgetPlanner/Features/Accounts/AccountDetailView.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "let accountId: Int" ios/BudgetPlanner/Features/Accounts/AccountDetailViewModel.swift</automated>
    <!-- Expected: 1 -->
    <automated>grep -c "struct AccountsNewSheet" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: 1 (symbol-collision-avoiding name) -->
    <automated>grep -c "struct NewAccountSheet" ios/BudgetPlanner/Features/Accounts/NewAccountSheet.swift</automated>
    <!-- Expected: 0 (no collision with FeaturesV10) -->
    <automated>cd ios && xcodegen generate 2>&1 | tail -5</automated>
    <!-- xcodegen output должно быть clean. -->
    <automated>cd ios && make build 2>&1 | tail -20 | grep -cE "BUILD SUCCEEDED|** BUILD SUCCEEDED \*\*"</automated>
    <!-- Expected: ≥1 — make build clean. -->
    <automated>git status --porcelain ios/BudgetPlanner/FeaturesV10/Accounts/ | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 — FeaturesV10/Accounts/* untouched. -->
    <automated>git status --porcelain ios/BudgetPlanner/MainShell.swift | wc -l | tr -d ' '</automated>
    <!-- Expected: 0 — MainShell.swift untouched. -->
  </verify>
  <acceptance_criteria>
    - 5 stub-файлов созданы в `ios/BudgetPlanner/Features/Accounts/`.
    - Все 5 файлов компилируются (типы определены: AccountsViewModel с Status/SheetMode enum, accounts: [AccountDTO], submitting, sumBalancesCents; AccountDetailViewModel с accountId / calendar Europe/Moscow; AccountsNewSheet struct с onCreate/onCancel callbacks).
    - Имя struct в NewAccountSheet.swift = `AccountsNewSheet` (избегаем collision с FeaturesV10/Accounts/NewAccountSheet.swift который объявляет `struct NewAccountSheet`).
    - `xcodegen generate` clean.
    - `make build` clean (0 errors).
    - `git status` показывает только `Features/Accounts/* + Features/Management/ManagementView.swift` в diff. FeaturesV10/* и MainShell.swift не модифицированы.
  </acceptance_criteria>
  <done>
    Все grep gates pass; `make build` succeeded; `git status` confirms scope. Plan 60-01 готов; downstream plans (60-02, 60-03, 60-04) заполнят body соответствующих файлов.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app ↔ /api/v1/accounts | Untrusted backend; AccountDTO decoded через APIClient.shared.decoder (snake_case convert). В этом плане только типы — runtime fetch в 60-02. |
| User → UI (tap navigation) | NavigationLink(value: .accounts) → AccountsView; не несёт untrusted input в этом плане. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | Information Disclosure | Stub error states | accept | В stubs нет catch блоков; в 60-02/60-04 будут добавлены filtered Russian copy banners (паттерн Phase 59 T-59-03). В этом плане типы не surface ошибки. |
</threat_model>

<verification>
- `grep -c "case analytics, subscriptions, template, accounts, categories" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 1.
- `grep -c "id: .accounts, label: \"Счета\"" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 1.
- `grep -c "case .accounts: AccountsView()" ios/BudgetPlanner/Features/Management/ManagementView.swift` returns 1.
- Все 5 файлов в `ios/BudgetPlanner/Features/Accounts/` существуют (test -f).
- `cd ios && make build 2>&1 | grep -c "BUILD SUCCEEDED"` returns ≥1.
- `git status --porcelain ios/BudgetPlanner/FeaturesV10/Accounts/` returns пусто (0 lines).
- `git status --porcelain ios/BudgetPlanner/MainShell.swift` returns пусто (0 lines).
</verification>

<success_criteria>
1. ManagementItem.ID enum расширен `.accounts` case между `.template` и `.categories`.
2. ManagementItem.all массив содержит entry «Счета» с icon `creditcard.fill`, ownerOnly: false, в позиции index 3.
3. ManagementView.destination(for:) обрабатывает `.accounts → AccountsView()`.
4. 5 stub-файлов созданы в `ios/BudgetPlanner/Features/Accounts/`: AccountsView, AccountsViewModel, AccountDetailView, AccountDetailViewModel, NewAccountSheet.
5. AccountsViewModel определяет `Status` enum (.idle / .loading / .ready / .error(String)), `SheetMode` enum, `accounts: [AccountDTO]`, `submitting`, `sumBalancesCents`, `accountCount`, `lastCreatedAccountId: Int?`.
6. AccountDetailViewModel определяет `accountId: Int` (let), `Status` enum, `account: AccountDTO?`, `actuals: [ActualV10DTO]`, `categories: [CategoryV10DTO]`, `period: PeriodDTO?`, `calendar` с Europe/Moscow TZ.
7. NewAccountSheet.swift содержит `struct AccountsNewSheet` (rename per Swift symbol-collision avoidance с FeaturesV10/Accounts/NewAccountSheet.swift).
8. `xcodegen generate` и `make build` clean.
9. FeaturesV10/Accounts/* и MainShell.swift НЕ модифицированы (git status confirms).
10. Manual run в симуляторе: open Управление → видна row «Счета» с creditcard.fill icon (между «Шаблон бюджета» и «Категории») → tap → push в AccountsView с ProgressView placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/60-accounts-v06/60-01-SUMMARY.md` summarising:
- ManagementItem registration (3 правки в ManagementView.swift)
- 5 stub files созданы в Features/Accounts/
- Symbol-collision resolution: struct AccountsNewSheet в NewAccountSheet.swift (rename т.к. FeaturesV10 уже занял `NewAccountSheet`)
- Build verification (make build SUCCEEDED)
- Coexistence guard verification (git status confirms FeaturesV10/Accounts/* + MainShell.swift untouched)
- Stub interface surface для downstream plans 60-02 / 60-03 / 60-04
</output>
