---
phase: 35-paywall
plan: 01
type: execute
wave: 1
depends_on: []
requirements:
  - REQ-35-01
  - REQ-35-03
commit: f7a8b73
---

<objective>
User tier resolution + reverse-trial schema. Alembic migration adds
`app_user.trial_ends_at` + `app_user.pro_active_until` (TIMESTAMPTZ,
nullable). Service `effective_tier(user, now=None)` returns `"free"` |
`"pro"` per precedence: `pro_active_until > now` OR `trial_ends_at > now`.
</objective>

## Task

1. Migration `0022_user_tier_columns.py` — adds two TIMESTAMPTZ columns to
   `app_user` (nullable, no default). Round-trip clean.
2. SQLAlchemy mapping in `app/db/models.py` — extends AppUser with both
   `Optional[datetime]` mapped columns.
3. Service `app/services/tier.py` — `effective_tier()` + `is_pro()` helpers
   with explicit `now` argument for deterministic tests.
4. Unit coverage `tests/test_tier_resolution.py` — 6 scenarios (free, pro
   via paid, pro via trial, expired trial, pro-overrides-expired-trial,
   explicit-now branch).

## Acceptance

- `pytest tests/test_tier_resolution.py -v` → 6 passed.
- Migration upgrade/downgrade round-trip clean.

## Commit

`f7a8b73` — feat(35-01): user tier resolution + reverse-trial schema (REQ-35-01)
