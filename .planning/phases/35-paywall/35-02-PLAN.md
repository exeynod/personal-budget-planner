---
phase: 35-paywall
plan: 02
type: execute
wave: 2
depends_on: [35-01]
requirements:
  - REQ-35-02
commit: e161686
---

<objective>
`require_pro` FastAPI dependency + AI Pro-gate + `GET /api/v1/me/tier`
endpoint. Free users получают 402 PRO_TIER_REQUIRED на AI; trial/Pro
users проходят дальше. /me/tier возвращает текущий tier + флаги trial.
</objective>

## Task

1. `require_pro` dependency в `app/api/dependencies.py` — читает текущий
   user из `get_current_user`, вычисляет `effective_tier`, кидает 402 если
   `"free"` с JSON detail `{error: "PRO_TIER_REQUIRED", current_tier,
   upgrade_url: "/billing"}`.
2. Применить `Depends(require_pro)` на `POST /ai/chat` и
   `GET /ai/suggest-category`.
3. `GET /api/v1/me/tier` — возвращает `{tier, is_trial_active,
   trial_ends_at, pro_active_until}` для UI.

## Acceptance

- Free user → /ai/chat → 402.
- Trial user → /ai/chat → не 402 (downstream LLM может фейлить — orthogonal).
- Pro user → /me/tier → `tier=pro, is_trial_active=false`.
- `pytest tests/test_tier_gating.py -v` → 5 passed.

## Commit

`e161686` — feat(35-02): require_pro dependency + AI Pro-gate + /me/tier endpoint (REQ-35-02)
