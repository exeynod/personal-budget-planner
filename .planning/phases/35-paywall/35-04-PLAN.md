---
phase: 35-paywall
plan: 04
type: execute
wave: 4
depends_on: [35-01]
requirements:
  - REQ-35-04
commit: 0637ab6
---

<objective>
Grant 14-day reverse-trial on new user creation. При первом INSERT в
`app_user` через dev-resolve путь (Playwright fixture / local dev)
выставлять `trial_ends_at = NOW() + 14d`. ON CONFLICT path должен НЕ
трогать `trial_ends_at` — re-resolving existing user не должен
рефрешить или продлевать триал.
</objective>

## Task

1. Extend `_dev_mode_resolve_test_user` в `app/api/dependencies.py` —
   `pg_insert(AppUser).values(..., trial_ends_at=now+14d)`, `on_conflict_do_update`
   меняет только `role`, оставляет trial_ends_at нетронутым.
2. То же для `_dev_mode_resolve_owner` (production-equivalent path для OWNER).
3. Integration test `tests/test_reverse_trial.py` — GET /api/v1/me с
   X-Test-User для fresh tg_user_id → row's trial_ends_at within ±60s
   of NOW()+14d.

## Acceptance

- `pytest tests/test_reverse_trial.py -v` → 1 passed.
- `tests/test_tier_resolution.py` + `tests/test_tier_gating.py` остаются green (11/11).

## Commit

`0637ab6` — feat(35-04): grant 14-day reverse-trial on user creation (REQ-35-04)
