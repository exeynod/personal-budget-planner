---
phase: 05-dashboard-period-lifecycle
plan: 06
type: execute
wave: 5
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified: []
autonomous: false
requirements: [DSH-01, DSH-02, DSH-03, DSH-04, DSH-05, DSH-06, PER-04]
tags: [verification, uat, manual-check, dashboard, worker-job]
must_haves:
  truths:
    - "Все 5 предыдущих планов 05-01..05-05 завершены и зафиксированы"
    - "pytest полностью зелёный (test_periods_api.py + test_close_period_job.py + предыдущие тесты не сломаны)"
    - "Frontend TS-компиляция чистая, Vite build проходит"
    - "Визуальная проверка дашборда в DEV/Mini App соответствует 05-UI-SPEC.md"
    - "Все 7 требований (DSH-01..06, PER-04) визуально или функционально подтверждены"
  artifacts:
    - path: .planning/phases/05-dashboard-period-lifecycle/05-VERIFICATION.md
      provides: "UAT checklist results — каждое требование проверено"
      contains: "DSH-01"
  key_links:
    - from: .planning/phases/05-dashboard-period-lifecycle/05-VERIFICATION.md
      to: ROADMAP.md Phase 5 success criteria
      via: "пять критериев успеха фазы переотзеркалены в чек-листе"
      pattern: "DSH-0"
---

<objective>
Финальный verification checkpoint Phase 5: убедиться, что все 7 requirements (DSH-01..06, PER-04) и 5 success criteria из ROADMAP реализованы и работают end-to-end.

Этот план — comprehensive UAT-проверка перед сдачей фазы. Включает:
1. Автоматизированные финальные регрессионные проверки (pytest + tsc + vite build)
2. Визуальная проверка человеком всех edge-state'ов через Mini App / DEV-браузер с подменой данных
3. Worker-job verification: ручной trigger close_period + проверка БД-результата
4. Создание `05-VERIFICATION.md` с pass/fail-ом по каждому требованию

Output: signed-off VERIFICATION.md + git commit "feat(05): Phase 5 complete".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md
@.planning/phases/05-dashboard-period-lifecycle/05-UI-SPEC.md
@CLAUDE.md
@.planning/phases/05-dashboard-period-lifecycle/05-01-SUMMARY.md
@.planning/phases/05-dashboard-period-lifecycle/05-02-SUMMARY.md
@.planning/phases/05-dashboard-period-lifecycle/05-03-SUMMARY.md
@.planning/phases/05-dashboard-period-lifecycle/05-04-SUMMARY.md
@.planning/phases/05-dashboard-period-lifecycle/05-05-SUMMARY.md

<interfaces>
<!-- Sources of truth для UAT-проверки -->

DSH-01 (REQUIREMENTS.md:55): "Главный экран Mini App — tabs Расходы/Доходы + hero-карточка баланса + aggr-блок План/Факт/Δ + плотный список категорий с прогресс-барами факт/план"

DSH-02 (REQUIREMENTS.md:56): "Знак дельты «положительная = хорошо»: расходы План−Факт, доходы Факт−План. Зелёный для положительной, красный для отрицательной"

DSH-03 (REQUIREMENTS.md:57): "Состояние in-progress: warn-стили (≥80% = жёлтая обводка), danger-стили (>100% = красная обводка + бейдж процента)"

DSH-04 (REQUIREMENTS.md:58): "Состояние empty (нет плана): empty-state с кнопками «Применить шаблон» / «Добавить вручную»"

DSH-05 (REQUIREMENTS.md:59): "Состояние closed: read-only, MainButton дизейблен с надписью «Период закрыт», бейдж «Закрыт» на period-switcher"

DSH-06 (REQUIREMENTS.md:60): "Переключатель периодов (← / →); навигация в архивные периоды только для просмотра"

PER-04 (REQUIREMENTS.md:29): "Worker-job ежедневно 00:01 МСК закрывает истёкший активный период и создаёт следующий"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Регрессионная автоматизированная проверка</name>
  <files></files>
  <read_first>
    - .planning/phases/05-dashboard-period-lifecycle/05-01-SUMMARY.md (если существует)
    - .planning/phases/05-dashboard-period-lifecycle/05-02-SUMMARY.md
    - .planning/phases/05-dashboard-period-lifecycle/05-03-SUMMARY.md
    - .planning/phases/05-dashboard-period-lifecycle/05-04-SUMMARY.md
    - .planning/phases/05-dashboard-period-lifecycle/05-05-SUMMARY.md
  </read_first>
  <behavior>
    - Все pytest-тесты проходят (зелёные)
    - Frontend TS-компиляция без ошибок
    - Vite build проходит
    - Никаких new lint warnings (если линтер настроен)
  </behavior>
  <action>
    Запустить три проверки последовательно и записать результаты:

    1. **Backend pytest full**:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner && \
         DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test \
         pytest -x --no-header 2>&1 | tail -20
       ```
       Записать: количество passed/failed/skipped.

    2. **Frontend TypeScript**:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | tail -20
       ```
       Должно быть 0 ошибок (или только pre-existing — если есть, документировать в VERIFICATION.md как неблокирующие).

    3. **Frontend production build**:
       ```bash
       cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx vite build 2>&1 | tail -20
       ```
       Должно завершиться без ошибок. Записать build size + main bundle path.

    4. Если любая проверка упала — НЕ создавать VERIFICATION.md, вернуться к соответствующему плану и исправить. Это блокирующий шаг для Task 2/3.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test pytest --no-header 2>&1 | tail -5 && cd frontend && npx tsc --noEmit 2>&1 | tail -3 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pytest: 0 failed, 0 errors (skipped tests на DB-backed без DATABASE_URL допустимы)
    - tsc --noEmit: exit code 0
    - vite build: exit code 0, dist/ создана
    - Все три статус-показателя записаны (pytest counts, tsc result, vite size)
  </acceptance_criteria>
  <done>
    Все автоматизированные проверки прошли; результаты записаны (для дальнейшего использования в VERIFICATION.md в Task 3).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Визуальная проверка дашборда (UAT) — все edge-states</name>
  <what-built>
    Полный Phase 5 dashboard:
    - HeroCard (gradient + balance + delta + period range)
    - PeriodSwitcher (‹ Месяц Год ›, бейдж "Закрыт")
    - TabBar (Расходы / Доходы) sticky
    - AggrStrip (План / Факт / Δ)
    - DashboardCategoryRow (с прогресс-барами; warn ≥80%; overspend >100%)
    - Edge states: empty, in-progress, warn, overspend, closed
    - FAB visibility logic + MainButton "Период закрыт"
  </what-built>
  <how-to-verify>
    Запустить frontend в DEV-режиме:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner/frontend && npm run dev
    ```
    Открыть в браузере http://localhost:5173 (или соответствующий порт).
    Backend должен быть запущен (`docker compose up api db` или Postgres + uvicorn).

    **Проверочный чек-лист** (записать pass/fail для каждого пункта):

    **DSH-01** — Layout:
    1. ☐ Видна HeroCard сверху с градиентом #1d2740→#2d1d40 (premium feel)
    2. ☐ Под hero — PeriodSwitcher с текущим месяцем (например "Май 2026")
    3. ☐ Под switcher — sticky TabBar [Расходы / Доходы], при скролле остаётся вверху
    4. ☐ Под tabs — AggrStrip с 3 колонками (План / Факт / Δ)
    5. ☐ Ниже — список DashboardCategoryRow с прогресс-барами

    **DSH-02** — Delta sign:
    6. ☐ Вкладка "Расходы": если planned > actual → AggrStrip Δ зелёный (+); если actual > planned → красный (-)
    7. ☐ Вкладка "Доходы": если actual > planned → зелёный (+); если planned > actual → красный (-)
    8. ☐ HeroCard delta цвет совпадает по правилу D-02

    **DSH-03** — In-progress edge states:
    9. ☐ Создать категорию с planned=10000, actual=8500 (85%) → row имеет жёлтую обводку (warn) + жёлтый прогресс-бар
    10. ☐ Создать категорию с planned=10000, actual=12300 (123%) → row имеет красную обводку + красный прогресс-бар + бейдж "123%" в правом углу
    11. ☐ Категория с planned=0 — НЕТ прогресс-бара под строкой

    **DSH-04** — Empty state:
    12. ☐ Если в активном периоде нет ни одной planned-строки (никто не применил шаблон + нет manual) — виден empty-state с заголовком "Бюджет не запланирован", кнопкой "Применить шаблон" и кнопкой "Добавить вручную"
    13. ☐ Клик "Применить шаблон" — toast "Шаблон применён", список категорий заполняется
    14. ☐ Клик "Добавить вручную" — переход на PlannedScreen

    **DSH-05** — Closed period:
    15. ☐ Через PeriodSwitcher переключиться на closed period (если в БД есть такой) → бейдж "Закрыт" виден на switcher
    16. ☐ FAB (+ кнопка) скрыт
    17. ☐ MainButton "Период закрыт" виден внизу, дизейблен (серый, не кликабелен)
    18. ☐ HeroCard показывает label "Итог периода" вместо "Баланс", сумма = ending_balance_cents закрытого периода

    **DSH-06** — Period switcher:
    19. ☐ ‹ кнопка переключает на старый период; данные обновляются (HeroCard, AggrStrip, список)
    20. ☐ › кнопка возвращает на более новый период
    21. ☐ На самом старом периоде ‹ disabled (серый, не кликабелен)
    22. ☐ На текущем active периоде › disabled

    **FAB pipeline (regression check)**:
    23. ☐ На активном текущем периоде FAB виден, клик открывает BottomSheet с ActualEditor
    24. ☐ Save в editor создаёт actual, toast "Транзакция добавлена", дашборд обновляется
    25. ☐ FAB не показывается на архивных периодах

    **Если БД не имеет нужных edge-cases** для проверки (например, нет closed period или нет warn-категории), создать их вручную через psql или временно через API:
    ```sql
    -- Создать closed period для теста DSH-05
    INSERT INTO budget_period(period_start, period_end, starting_balance_cents, ending_balance_cents, status, closed_at)
    VALUES ('2026-04-05', '2026-05-04', 100000, 95000, 'closed', NOW());

    -- Создать planned + actual для warn-теста
    INSERT INTO planned_transaction(period_id, kind, amount_cents, category_id, source) VALUES (1, 'expense', 1000000, 1, 'manual');
    INSERT INTO actual_transaction(period_id, kind, amount_cents, category_id, tx_date, source) VALUES (1, 'expense', 850000, 1, CURRENT_DATE, 'mini_app');
    ```

    **Если найдены проблемы** — описать их в response, НЕ approve до фикса. Назад к соответствующему плану (05-04 для компонентов, 05-05 для интеграции).
  </how-to-verify>
  <resume-signal>
    Type **"approved"** чтобы пройти gate, или опиши конкретные проблемы (cite item numbers) — например: "fail #15: бейдж 'Закрыт' не виден; fail #20: › disabled остаётся даже после переключения".
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PER-04 worker-job manual verification + создание VERIFICATION.md</name>
  <what-built>
    PER-04 close_period_job:
    - Daily cron в 00:01 Europe/Moscow
    - Закрывает expired active period с правильным ending_balance_cents
    - Создаёт следующий period с inherited starting_balance_cents (PER-03)
    - pg_try_advisory_lock защита
    - Idempotent: повторный запуск в тот же день — no-op
  </what-built>
  <how-to-verify>
    **Step 1**: Manual trigger close_period_job через Python REPL (вместо ожидания 00:01 МСК):

    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      python -c "
    import asyncio
    from app.worker.jobs.close_period import close_period_job
    asyncio.run(close_period_job())
    " 2>&1
    ```

    Должны увидеть один из трёх log-output:
    - `close_period.skipped.no_expired_period` — если нет expired active period (нормально)
    - `close_period.done period_id_closed=X period_id_created=Y ending_balance_cents=Z` — если был expired и обработан
    - `close_period.skipped.lock_not_acquired` — если другой процесс уже держит lock (редко)

    **Step 2**: Создать тестовый expired period через psql:
    ```sql
    -- Backup current state first
    SELECT * FROM budget_period;

    -- Create expired active period (period_end < today)
    INSERT INTO budget_period(period_start, period_end, starting_balance_cents, status)
    VALUES (CURRENT_DATE - INTERVAL '40 days', CURRENT_DATE - INTERVAL '10 days', 100000, 'active')
    RETURNING id;
    -- Запомнить returned id
    ```

    **Step 3**: Запустить close_period_job снова:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      python -c "
    import asyncio
    from app.worker.jobs.close_period import close_period_job
    asyncio.run(close_period_job())
    "
    ```

    Должны увидеть log "close_period.done" с правильными id.

    **Step 4**: Проверить результат в БД:
    ```sql
    SELECT id, period_start, period_end, starting_balance_cents, ending_balance_cents, status, closed_at
    FROM budget_period
    ORDER BY period_start DESC LIMIT 3;
    ```

    Проверочный чек-лист:
    1. ☐ Старый expired period: `status=closed`, `ending_balance_cents` не null, `closed_at` не null
    2. ☐ Новый period появился: `status=active`, `period_start=today` (или согласно cycle_start_day), `starting_balance_cents` равен `ending_balance_cents` старого (PER-03 inheritance)
    3. ☐ Запустить close_period_job ЕЩЁ РАЗ → log `close_period.skipped.no_expired_period` (idempotency)

    **Step 5**: Проверка scheduler конфигурации:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && grep -nE "(close_period|hour=0.*minute=1)" main_worker.py
    ```
    Должны увидеть две строки: import close_period_job + scheduler.add_job с hour=0 minute=1.

    **Step 6** (опциональный smoke): запустить worker контейнер на 30 секунд, проверить scheduler.started log:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && timeout 5 python main_worker.py 2>&1 | head -30
    ```
    Должны увидеть `worker.scheduler.started` (if all imports OK).

    **Step 7**: Создать `.planning/phases/05-dashboard-period-lifecycle/05-VERIFICATION.md` со следующей структурой:

    ```markdown
    # Phase 5: Dashboard & Period Lifecycle — Verification

    **Verified:** YYYY-MM-DD
    **Status:** PASS | PARTIAL | FAIL

    ## Automated Checks (Task 1)

    | Check | Result | Notes |
    |---|---|---|
    | pytest | N passed, N failed, N skipped | (paste tail of pytest output) |
    | tsc --noEmit | exit 0 / N errors | |
    | vite build | exit 0, bundle size N kb | |

    ## UAT Visual Checks (Task 2)

    | # | DSH-XX / Item | Result | Notes |
    |---|---|---|---|
    | 1 | DSH-01 HeroCard visible | PASS | |
    | 2 | DSH-01 PeriodSwitcher | PASS | |
    | ... | ... | ... | ... |
    | 25 | FAB hidden on archived | PASS | |

    ## PER-04 Worker Job (Task 3)

    | Item | Result | Notes |
    |---|---|---|
    | manual close_period_job ran | PASS | log close_period.done |
    | expired period closed correctly | PASS | ending_balance_cents = X |
    | next period created with inherited balance | PASS | starting_balance_cents = X (PER-03) |
    | idempotent second run | PASS | log close_period.skipped.no_expired_period |
    | scheduler config (cron 00:01 MOSCOW_TZ) | PASS | grep main_worker.py |

    ## Phase Success Criteria (ROADMAP.md:87-92)

    1. ☐ tabs + hero + aggr + список категорий с прогресс-барами — `PASS|FAIL`
    2. ☐ Знак дельты «положительная = хорошо», цвет — `PASS|FAIL`
    3. ☐ Все 4 edge-state работают (empty/in-progress/warn/overspend/closed) — `PASS|FAIL`
    4. ☐ Переключатель периодов работает, мутации в архив заблокированы — `PASS|FAIL`
    5. ☐ Worker-job close_period работает, idempotent — `PASS|FAIL`

    ## Requirements Coverage

    - DSH-01: COMPLETE (Plan 05-04, 05-05)
    - DSH-02: COMPLETE (Plan 05-04 AggrStrip + HeroCard delta sign)
    - DSH-03: COMPLETE (Plan 05-04 DashboardCategoryRow warn/overspend)
    - DSH-04: COMPLETE (Plan 05-05 empty state)
    - DSH-05: COMPLETE (Plan 05-04 PeriodSwitcher badge + Plan 05-05 MainButton)
    - DSH-06: COMPLETE (Plan 05-04 PeriodSwitcher + Plan 05-05 selectedPeriodId)
    - PER-04: COMPLETE (Plan 05-02 close_period_job)

    ## Issues Found

    (None | List)

    ## Sign-off

    - Phase 5 complete: YES | NO
    - Ready to commit + advance to Phase 6: YES | NO
    ```

    Заполнить файл реальными результатами Tasks 1-3.

    **Step 8**: Git commit с описанием фазы:
    ```bash
    cd /Users/exy/pet_projects/tg-budget-planner && \
      git add -A && \
      git commit -m "$(cat <<'EOF'
    feat(05): Phase 5 complete — Dashboard & Period Lifecycle

    Implements DSH-01..06 and PER-04:
    - Backend: GET /periods + GET /periods/{id}/balance endpoints (Plan 05-01)
    - Worker: close_period_job daily 00:01 MSK with pg_try_advisory_lock (Plan 05-02)
    - Frontend: data layer (utils/format, periods api, usePeriods, useDashboard) (Plan 05-03)
    - Frontend: 4 dashboard components (HeroCard, PeriodSwitcher, AggrStrip, DashboardCategoryRow) (Plan 05-04)
    - Frontend: HomeScreen full replacement with edge states + integration (Plan 05-05)

    All 5 phase success criteria verified (see 05-VERIFICATION.md).

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    Если в Step 1-7 найдены блокирующие проблемы — НЕ commit, описать в response.
  </how-to-verify>
  <resume-signal>
    Type **"approved"** + сообщить commit hash, или описать конкретные проблемы (cite item numbers).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tester → DB | Manual SQL inserts для создания edge-cases — выполняются на тестовой/dev БД, не production |
| Tester → Worker | Manual python invocation close_period_job — те же permissions что у worker контейнера; на dev среде безопасно |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-27 | Tampering | manual SQL inserts портят production data | mitigate | Verification на dev/local БД (DATABASE_URL=...localhost:5432/budget_test). Не запускать Step 2 SQL на production. Backup БД перед manual triggers (Step 2 включает SELECT * FROM budget_period — backup current state) |
| T-05-28 | Repudiation | UAT pass/fail без trail | mitigate | VERIFICATION.md фиксирует результаты каждого пункта; git commit включает ссылку на VERIFICATION.md |
| T-05-29 | Information Disclosure | UAT screenshots | accept | Screenshots могут попасть в git accidentally — gitignore стандартный (никаких *.png в src), VERIFICATION.md без скриншотов (только текстовый чек-лист) |
</threat_model>

<verification>
- Все 3 task'а выполнены
- 05-VERIFICATION.md создан и содержит результаты по всем 7 требованиям
- pytest зелёный, tsc чистый, vite build OK
- Manual UI walkthrough по 25 пунктам — pass
- Worker job manual trigger показал ожидаемое поведение
- Git commit "feat(05): Phase 5 complete" создан
</verification>

<success_criteria>
1. Все 7 requirements (DSH-01..06, PER-04) — статус PASS в 05-VERIFICATION.md
2. 5 phase success criteria из ROADMAP.md — все PASS
3. Никаких регрессий в pytest или TypeScript
4. Phase 5 готова к закрытию — STATE.md можно обновить (current_phase: 6)
</success_criteria>

<output>
After completion:
1. Создать `.planning/phases/05-dashboard-period-lifecycle/05-VERIFICATION.md` (Task 3 Step 7)
2. Создать `.planning/phases/05-dashboard-period-lifecycle/05-06-SUMMARY.md` containing:
   - Phase 5 completion confirmation
   - Reference to VERIFICATION.md
   - Git commit hash
   - Notes for Phase 6 (что готово к старту: subscriptions нуждается в worker pattern из Plan 05-02 — ADVISORY_LOCK_KEY 20250502/20250503 зарезервированы)
3. Git commit (Task 3 Step 8) объединяет все Phase 5 deliverables
</output>
