---
phase: 05-dashboard-period-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_periods_api.py
  - app/services/periods.py
  - app/api/routes/periods.py
autonomous: true
requirements: [DSH-06]
tags: [backend, fastapi, periods, dashboard]
must_haves:
  truths:
    - "GET /api/v1/periods возвращает список всех периодов отсортированный по period_start desc"
    - "GET /api/v1/periods/{id}/balance возвращает BalanceResponse для архивного/активного периода"
    - "GET /api/v1/periods/{id}/balance с несуществующим id возвращает 404"
    - "Оба endpoint защищены validate_init_data (без X-Telegram-Init-Data → 403)"
  artifacts:
    - path: tests/test_periods_api.py
      provides: "RED-тесты для GET /periods и GET /periods/{id}/balance"
      contains: "async def test_list_periods_"
    - path: app/services/periods.py
      provides: "list_all_periods async function"
      contains: "async def list_all_periods"
    - path: app/api/routes/periods.py
      provides: "GET /periods и GET /periods/{period_id}/balance handlers"
      contains: "async def list_periods"
  key_links:
    - from: app/api/routes/periods.py
      to: app/services/periods.py::list_all_periods
      via: "import + await period_svc.list_all_periods(db)"
      pattern: "list_all_periods\\("
    - from: app/api/routes/periods.py
      to: app/services/actual.py::compute_balance
      via: "import + await actual_svc.compute_balance(db, period_id)"
      pattern: "compute_balance\\(db,\\s*period_id\\)"
---

<objective>
Расширить existing `app/api/routes/periods.py` двумя endpoint'ами для Phase 5 dashboard:
1. `GET /api/v1/periods` — список всех периодов (id, period_start, period_end, status, balances), отсортирован по period_start desc. Используется PeriodSwitcher (DSH-06).
2. `GET /api/v1/periods/{period_id}/balance` — `BalanceResponse` для конкретного периода (любой status). Используется useDashboard при просмотре архивных периодов (DSH-05/06).

Покрывает требование **DSH-06** (переключатель периодов: ← / →, навигация в архив только для просмотра — мутации не блокируются на этом эндпоинте, потому что list/balance доступны read-only для всех периодов; блокировка мутаций реализована во frontend через FAB visibility).

Purpose: Поставить data-layer для PeriodSwitcher и useDashboard ДО написания frontend хуков (Wave 2). Сервис `compute_balance(db, period_id)` уже существует в `app/services/actual.py:323` и принимает period_id — wrapper-route тривиален.

Output: Два новых RED-тестовых файла + расширенный periods.py route + новый сервисный helper `list_all_periods`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard-period-lifecycle/05-CONTEXT.md
@.planning/phases/05-dashboard-period-lifecycle/05-PATTERNS.md
@CLAUDE.md
@app/api/routes/periods.py
@app/api/routes/actual.py
@app/api/schemas/periods.py
@app/api/schemas/actual.py
@app/services/periods.py
@app/services/actual.py
@app/services/planned.py
@tests/test_balance.py
@tests/conftest.py

<interfaces>
<!-- Уже существующие в кодовой базе (НЕ переписывать!) — экзекьютор использует напрямую -->

From `app/api/schemas/periods.py`:
```python
class PeriodRead(BaseModel):
    model_config = ConfigDict(from_attributes=True)
    id: int
    period_start: date
    period_end: date
    starting_balance_cents: int
    ending_balance_cents: Optional[int]
    status: PeriodStatusStr  # Literal["active", "closed"]
    closed_at: Optional[datetime]
```

From `app/api/schemas/actual.py`:
```python
class BalanceResponse(BaseModel):
    period_id: int
    period_start: date
    period_end: date
    starting_balance_cents: int
    planned_total_expense_cents: int
    actual_total_expense_cents: int
    planned_total_income_cents: int
    actual_total_income_cents: int
    balance_now_cents: int
    delta_total_cents: int
    by_category: list[BalanceCategoryRow]
```

From `app/services/actual.py:323`:
```python
async def compute_balance(db: AsyncSession, period_id: int) -> dict
# Raises PeriodNotFoundError if budget period not found
```

From `app/services/planned.py:48`:
```python
class PeriodNotFoundError(Exception):
    def __init__(self, period_id: int) -> None: ...
```

From `app/services/periods.py:19`:
```python
async def get_current_active_period(db: AsyncSession) -> Optional[BudgetPeriod]
# Returns active period or None — does NOT do by-id lookup
```

From `app/db/models.py:99`:
```python
class BudgetPeriod(Base):
    id: Mapped[int]
    period_start: Mapped[date]      # UNIQUE
    period_end: Mapped[date]
    starting_balance_cents: Mapped[int]
    ending_balance_cents: Mapped[Optional[int]]  # NULL while active
    status: Mapped[PeriodStatus]    # active | closed
    closed_at: Mapped[Optional[datetime]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — RED тесты для GET /periods и GET /periods/{id}/balance</name>
  <files>tests/test_periods_api.py</files>
  <read_first>
    - tests/conftest.py (полностью — паттерн async_client + make_init_data)
    - tests/test_balance.py (полностью — паттерн _require_db + db_setup + auth_headers)
    - app/api/routes/periods.py (понять текущий state — только GET /current)
    - app/api/schemas/periods.py (PeriodRead schema)
    - app/api/schemas/actual.py (BalanceResponse schema)
  </read_first>
  <behavior>
    - **test_list_periods_requires_init_data**: GET /api/v1/periods без X-Telegram-Init-Data → 403 (router-level auth)
    - **test_list_periods_empty_returns_empty_list**: пустая БД (TRUNCATE applied) → GET /periods вернёт 200 + `[]`
    - **test_list_periods_returns_all_sorted_desc**: вставить 3 периода с разными period_start → GET /periods вернёт массив длины 3, отсортированный по period_start DESC
    - **test_list_periods_response_shape**: проверить что каждый элемент содержит ключи `id`, `period_start`, `period_end`, `starting_balance_cents`, `ending_balance_cents`, `status`, `closed_at`
    - **test_get_period_balance_requires_init_data**: GET /api/v1/periods/1/balance без header → 403
    - **test_get_period_balance_returns_balance_for_existing_period**: создать period + planned + actual → GET /periods/{id}/balance вернёт 200 + ключи `period_id`, `balance_now_cents`, `delta_total_cents`, `by_category`
    - **test_get_period_balance_404_when_period_missing**: GET /periods/99999/balance → 404 detail содержит "99999" or "not found"
    - **test_get_period_balance_works_for_closed_period**: создать closed period с ending_balance_cents → endpoint вернёт 200 (не блокирует closed-периоды)
  </behavior>
  <action>
    Создать `tests/test_periods_api.py` следуя паттерну из `tests/test_balance.py`:
    1. Заголовок docstring + `_require_db()` helper (skip если DATABASE_URL не задан) — копируется из test_balance.py:21-23.
    2. Импортировать `make_init_data` из `tests.conftest`; pytest fixture `auth_headers` (см. test_balance.py:26-29).
    3. `db_setup` async fixture с TRUNCATE TABLE + `app.dependency_overrides[get_db]` — точная копия из test_balance.py:32-65.
    4. Импортировать модели через `from app.db.models import BudgetPeriod, PeriodStatus, Category, CategoryKind, PlannedTransaction, ActualTransaction, ActualSource, PlanSource`.
    5. `seed_periods` fixture: создаёт 3 BudgetPeriod (status=active для одного с today, status=closed для двух предыдущих с ending_balance_cents). Использует `period_for(today, cycle_start_day=5)` если нужно вычислить bounds, либо хардкодит даты `date(2026, 3, 5)`, `date(2026, 4, 5)`, `date(2026, 5, 5)`.
    6. Все 8 тестов из `<behavior>`. Базовый URL — `/api/v1/periods` (НЕ `/periods` — Caddy проксирует под `/api/v1`, но в pytest используем абсолютный путь как в test_balance.py — там используется `/api/v1/actual/balance`).
    7. Каждый DB-backed тест декорирован `@pytest.mark.asyncio`.
    8. Тесты сейчас FAIL (RED) потому что endpoint'ов нет; после Task 2-3 PASS.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test pytest tests/test_periods_api.py -x --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/test_periods_api.py`
    - Contains all 8 test functions: `grep -c "^async def test_" tests/test_periods_api.py` returns 8 (or `grep -c "^def test_"` for sync ones — but they use asyncio so should be `async def`)
    - Has `_require_db()` helper: `grep -c "_require_db" tests/test_periods_api.py` ≥ 2
    - Imports from conftest: `grep -c "from tests.conftest import make_init_data" tests/test_periods_api.py` == 1
    - Tests fail BEFORE Task 2-3 (RED state) OR pass AFTER (GREEN state). Acceptable: pytest reports 8 failures with reasons "404 Not Found" or "AttributeError: list_all_periods" — confirms tests are wired
  </acceptance_criteria>
  <done>
    `tests/test_periods_api.py` существует, содержит 8 async-тестов с правильными именами, использует тот же DB-fixture pattern что и test_balance.py, тесты FAIL до реализации (RED) или PASS после (GREEN).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Сервис list_all_periods + endpoint GET /api/v1/periods</name>
  <files>app/services/periods.py, app/api/routes/periods.py</files>
  <read_first>
    - app/services/periods.py (полностью — добавляем функцию)
    - app/api/routes/periods.py (полностью — добавляем endpoint)
    - app/api/schemas/periods.py (PeriodRead для response_model)
    - app/db/models.py:99-123 (BudgetPeriod ORM)
  </read_first>
  <behavior>
    - **list_all_periods(db)** возвращает list[BudgetPeriod] отсортированный по period_start DESC, без фильтра по status (включает active+closed)
    - GET /api/v1/periods возвращает 200 + JSON-массив `[PeriodRead, ...]` (даже если пусто — `[]`, НЕ 404)
    - Endpoint унаследует `Depends(get_current_user)` от router-level (уже стоит на строке 19 periods.py)
  </behavior>
  <action>
    1. **В `app/services/periods.py` добавить функцию** (между `get_current_active_period` и `create_first_period`):

    ```python
    async def list_all_periods(db: AsyncSession) -> list[BudgetPeriod]:
        """Return ALL budget periods (active + closed), ordered by period_start DESC.

        Used by GET /api/v1/periods (DSH-06 PeriodSwitcher) to populate the
        navigation dropdown. Returns empty list (not 404) when no periods exist
        yet (e.g. before onboarding).
        """
        result = await db.execute(
            select(BudgetPeriod).order_by(BudgetPeriod.period_start.desc())
        )
        return list(result.scalars().all())
    ```

    2. **В `app/api/routes/periods.py` добавить endpoint** (перед существующим `get_current_period`, чтобы literal `/current` НЕ был перехвачен path param):

    ```python
    @periods_router.get("", response_model=list[PeriodRead])
    async def list_periods(
        db: Annotated[AsyncSession, Depends(get_db)],
    ) -> list[PeriodRead]:
        """GET /api/v1/periods — list all budget periods, newest first (DSH-06).

        Used by PeriodSwitcher to populate navigation. Returns an empty list
        (not 404) when no periods exist (onboarding incomplete).
        Includes both active and closed periods.
        """
        periods = await period_svc.list_all_periods(db)
        return [PeriodRead.model_validate(p) for p in periods]
    ```

    Decorator path is empty string `""` because router-level `prefix="/periods"` already gives the full prefix (FastAPI joins them → `/periods`). NOT `/` and NOT `/list`.

    3. Импорт `select` уже есть; `BudgetPeriod` уже импортирован в periods.py:11. PeriodRead уже импортирован в routes/periods.py:12. Никаких новых импортов.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test pytest tests/test_periods_api.py -x -k "list_periods" --no-header 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^async def list_all_periods" app/services/periods.py` == 1
    - `grep -c "list_all_periods" app/services/periods.py` ≥ 2 (def + docstring or usage)
    - `grep -c '@periods_router.get(""' app/api/routes/periods.py` == 1
    - `grep -c "async def list_periods" app/api/routes/periods.py` == 1
    - `grep -c "period_svc.list_all_periods" app/api/routes/periods.py` == 1
    - All 4 list_periods tests PASS in pytest output (4 PASS, 0 FAIL)
  </acceptance_criteria>
  <done>
    `list_all_periods` сервис создан, GET /api/v1/periods endpoint работает, все 4 list_periods-тесты PASS.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Endpoint GET /api/v1/periods/{period_id}/balance</name>
  <files>app/api/routes/periods.py</files>
  <read_first>
    - app/api/routes/periods.py (после Task 2 — добавляем третий handler)
    - app/api/routes/actual.py:144-170 (паттерн get_balance — копируем структуру exception handling)
    - app/services/actual.py:323-438 (compute_balance — уже принимает period_id)
    - app/services/planned.py:48-60 (PeriodNotFoundError)
    - app/api/schemas/actual.py (BalanceResponse)
  </read_first>
  <behavior>
    - GET /api/v1/periods/{period_id}/balance возвращает 200 + BalanceResponse для существующего периода
    - Возвращает 404 + detail "Budget period {id} not found" для несуществующего period_id
    - Работает для closed периодов (не фильтрует по status — допускает архивные)
    - Защищён validate_init_data (унаследовано через router-level dep)
  </behavior>
  <action>
    1. **Добавить импорты в `app/api/routes/periods.py`** (после существующих):
    ```python
    from app.api.schemas.actual import BalanceResponse
    from app.services import actual as actual_svc
    from app.services.planned import PeriodNotFoundError
    ```

    2. **Добавить handler в конец файла** (после `get_current_period`):
    ```python
    @periods_router.get(
        "/{period_id}/balance",
        response_model=BalanceResponse,
    )
    async def get_period_balance(
        period_id: int,
        db: Annotated[AsyncSession, Depends(get_db)],
    ) -> BalanceResponse:
        """GET /api/v1/periods/{period_id}/balance — balance for any period (DSH-05/06).

        Allows viewing data for archived (closed) periods via PeriodSwitcher.
        Reuses compute_balance service from actual.py — same response shape as
        GET /actual/balance but accepts an explicit period_id instead of
        defaulting to the active period.

        Status codes:
            200: balance data
            404: period does not exist
        """
        try:
            bal = await actual_svc.compute_balance(db, period_id)
        except PeriodNotFoundError as exc:
            raise HTTPException(
                status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)
            ) from exc
        return BalanceResponse(**bal)
    ```

    Path is `/{period_id}/balance` — combined with router prefix `/periods` it becomes `/periods/{period_id}/balance`. The literal `/current` and empty `""` declared earlier in file take priority over `{period_id}` matching.

    3. Никаких изменений в comment-block в `app/api/router.py` (pythonpath: `app.api.routes.periods.periods_router` уже подключён через `public_router.include_router(periods_router)` на строке 100). Новые endpoint'ы появятся автоматически.

    4. **NOT** регистрируем endpoint под `internal_router` — это public, защищается validate_init_data.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && DATABASE_URL=postgresql+asyncpg://budget:budget@localhost:5432/budget_test pytest tests/test_periods_api.py -x --no-header 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'periods_router.get(\s*"/{period_id}/balance"' app/api/routes/periods.py` == 1
    - `grep -c "async def get_period_balance" app/api/routes/periods.py` == 1
    - `grep -c "from app.services.planned import PeriodNotFoundError" app/api/routes/periods.py` == 1
    - `grep -c "from app.api.schemas.actual import BalanceResponse" app/api/routes/periods.py` == 1
    - `grep -c "actual_svc.compute_balance" app/api/routes/periods.py` == 1
    - All 8 tests in tests/test_periods_api.py PASS (`pytest tests/test_periods_api.py` → 8 passed, 0 failed)
    - Existing `pytest tests/test_balance.py` still PASSES (regression check) → run `pytest tests/test_balance.py -x` returns 0 exit code
  </acceptance_criteria>
  <done>
    GET /periods/{id}/balance работает для active/closed периодов, возвращает 404 для отсутствующих, все 8 тестов test_periods_api.py PASS, test_balance.py не сломан.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App → API | initData проверяется HMAC-SHA256 на каждом запросе (унаследовано через `validate_init_data` dep) |
| Caddy → API | `/api/v1/periods/*` доступен снаружи через TLS, `/api/v1/internal/*` блокируется на edge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | GET /periods, GET /periods/{id}/balance | mitigate | Router-level `Depends(get_current_user)` (уже в periods_router:19) — без валидной X-Telegram-Init-Data → 403. Подтверждается в test_periods_api.py::test_list_periods_requires_init_data |
| T-05-02 | Information Disclosure | GET /periods/{id}/balance | accept | Single-tenant MVP: OWNER_TG_ID whitelist в `get_current_user` гарантирует, что только OWNER может видеть свой balance. IDOR на period_id неэффективен — все periods принадлежат OWNER. Multi-tenant миграция (MUL-01..03) — отдельная задача |
| T-05-03 | Tampering | GET /periods/{id}/balance | accept | Read-only endpoint, не принимает body, ничего не мутирует. Caddy не проксирует наружу `/api/v1/internal/*`, но эти endpoint'ы — public под initData |
| T-05-04 | Denial of Service | GET /periods (полный список) | accept | В single-tenant ≤ 24 периода в год; нет paginate — приемлемо. Sorting by indexed `period_start` (UNIQUE constraint = индекс) |
| T-05-05 | Information Disclosure | error message in 404 | mitigate | `PeriodNotFoundError` text "Budget period {id} not found" — без чувствительных полей, только id (который пользователь и так знает) |
</threat_model>

<verification>
- Все 3 task'а выполнены
- pytest tests/test_periods_api.py → 8/8 passed
- pytest tests/test_balance.py → no regressions (still passing)
- pytest tests/test_auth.py — auth-paths не затронуты
- Manual smoke: `curl -H "X-Telegram-Init-Data: ..." http://localhost:8000/api/v1/periods` → 200 + JSON array
</verification>

<success_criteria>
1. GET /api/v1/periods отвечает 200 + JSON-массивом всех периодов сортировка desc by period_start
2. GET /api/v1/periods/{id}/balance отвечает 200 + BalanceResponse для существующего period_id (любой status)
3. GET /api/v1/periods/{id}/balance отвечает 404 для несуществующего period_id
4. Без X-Telegram-Init-Data → 403 на обоих endpoint'ах
5. Существующие tests/test_balance.py и tests/test_periods.py продолжают проходить
6. Test coverage: 8 новых тестов в test_periods_api.py все PASS
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-period-lifecycle/05-01-SUMMARY.md` containing:
- Files created/modified
- Test results (passing/failing counts)
- API contract verification (curl example for each endpoint)
- Notes for Plan 05-03 (frontend hooks usePeriods, useDashboard) — endpoint paths, response shapes
</output>
