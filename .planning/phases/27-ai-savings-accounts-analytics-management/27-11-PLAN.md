---
phase: 27-ai-savings-accounts-analytics-management
plan: 11
subsystem: ios-mgmt-shell
type: execute
wave: 3
depends_on: [27-07, 27-08, 27-09, 27-10]
files_modified:
  - ios/BudgetPlanner/FeaturesV10/Management/MgmtHubView.swift
  - ios/BudgetPlanner/FeaturesV10/Management/MgmtHubViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift
  - ios/BudgetPlanner/FeaturesV10/Management/SettingsV10ViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Management/AccessV10View.swift
  - ios/BudgetPlanner/FeaturesV10/Management/AccessV10ViewModel.swift
  - ios/BudgetPlanner/Networking/Endpoints/SettingsAPI.swift
  - ios/BudgetPlanner/Networking/Endpoints/AdminAPI.swift
  - ios/BudgetPlannerTests/FeaturesV10/MgmtHubTests.swift
  - ios/BudgetPlanner/App/V10MainShell.swift
autonomous: true
requirements: [MGMT-V10-01, MGMT-V10-02, MGMT-V10-03, MGMT-V10-04]
tags: [ios, swiftui, observable, mgmt-hub, settings, access, owner-gate, shell-wire]

must_haves:
  truths:
    - "User видит iOS Mgmt-хаб на чёрном фоне с 5 numbered list-rows (01 PLAN МЕСЯЦА / 02 СЧЕТА / 03 АНАЛИТИКА / 04 НАСТРОЙКИ / 05 ДОСТУП); 05 ДОСТУП — только при me.role == 'owner'."
    - "Tap на row → router?.push соответствующего View (PlanView, AccountsListV10View, AnalyticsV10View, SettingsV10View, AccessV10View)."
    - "User в Settings видит rewrite v0.6 form в poster-стиле (cycle_start_day Stepper, notify_days_before Stepper, AI-categorization Toggle, AI spend cap read-only)."
    - "User-owner в Access видит admin Users + AI Usage tabs в poster-стиле."
    - "V10MainShell handleTabChange: home → popToRoot, savings → push SavingsV10View, ai → push AiV10View, mgmt → push MgmtHubView."
  artifacts:
    - path: "ios/BudgetPlanner/FeaturesV10/Management/MgmtHubView.swift"
      provides: "SwiftUI 5-row numbered list + owner gate"
      min_lines: 120
    - path: "ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift"
      provides: "SwiftUI poster-styled settings form"
      min_lines: 140
    - path: "ios/BudgetPlanner/FeaturesV10/Management/AccessV10View.swift"
      provides: "SwiftUI poster admin Users + AI Usage tabs"
      min_lines: 160
    - path: "ios/BudgetPlanner/App/V10MainShell.swift"
      provides: "Updated handleTabChange — replaces placeholders with real Views"
      contains: "SavingsV10View"
  key_links:
    - from: "V10MainShell handleTabChange"
      to: "router.push(SavingsV10View() | AiV10View() | MgmtHubView())"
      pattern: "router.push"
    - from: "MgmtHubView onRowTap"
      to: "router?.push of corresponding V10 View"
      pattern: "router?.push"
    - from: "MgmtHubView ДОСТУП row visibility"
      to: "me.role == 'owner' check"
      pattern: "isOwner"
---

<objective>
iOS Mgmt-хаб (5 numbered rows) + Settings + Access (poster rewrite) + V10MainShell tab wire.

Финальный iOS plan Phase 27 — depends on 27-07/08/09/10. Wires real Views в MainShell handleTabChange.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-ai-savings-accounts-analytics-management/27-CONTEXT.md
@ios/BudgetPlanner/App/V10MainShell.swift
@ios/BudgetPlanner/Features/Management/SettingsView.swift
@ios/BudgetPlanner/Features/Management/AccessView.swift
@ios/BudgetPlanner/Networking/Endpoints/MeAPI.swift

<interfaces>
<!-- v0.6 Settings — verify exact API by reading ios/BudgetPlanner/Features/Management/SettingsView.swift:
- Endpoint: GET /api/v1/settings → SettingsRead { cycle_start_day, notify_days_before,
  ai_categorization_enabled, ai_spend_cap_cents }
- PATCH /api/v1/settings → SettingsRead

v0.6 Admin (Features/Management/AccessView.swift):
- GET /api/v1/admin/users → list
- GET /api/v1/admin/ai-usage → usage stats

iOS APIClient.shared pattern same as other endpoints.

V10MainShell.swift current handleTabChange (Plan 25-07):
  case .home: router.popToRoot()
  case .savings: router.push(AccountsListPlaceholderView())
  case .ai: router.push(PlanViewPlaceholderView())
  case .mgmt: router.push(PlanViewPlaceholderView())

After Phase 27:
  case .savings: router.push(SavingsV10View())
  case .ai: router.push(AiV10View())
  case .mgmt: router.push(MgmtHubView())

MeV10API.shared.fetchMeV10() returns MeV10Response with `role: String?` (verify field name).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MgmtHubView + ViewModel + tests</name>
  <read_first>
    - ios/BudgetPlanner/Networking/Endpoints/MeAPI.swift (fetchMeV10 + MeV10Response shape)
    - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionsV10View.swift
    - ios/BudgetPlanner/FeaturesV10/Common/* (Mass, Eyebrow, PosterButton)
  </read_first>
  <action>
    1. **`MgmtHubViewModel.swift`** (NEW, ~80 LOC):
       ```swift
       @MainActor @Observable final class MgmtHubViewModel {
         var isOwner: Bool = false
         private var inFlight: Bool = false

         func load() async {
           if inFlight { return }
           inFlight = true; defer { inFlight = false }
           do {
             let me = try await MeV10API.shared.fetchMeV10()
             // Verify field name; might be `role` or `userRole`.
             isOwner = (me.role == "owner")
           } catch {
             isOwner = false
           }
         }
       }
       ```

    2. **`MgmtHubView.swift`** (NEW, ~150 LOC):
       ```swift
       enum MgmtRowId: String { case plan, accounts, analytics, settings, access }

       struct MgmtHubView: View {
         @State private var model = MgmtHubViewModel()
         @Environment(\.posterRouter) private var router

         struct Row { let id: MgmtRowId; let n: String; let name: String; let ownerOnly: Bool }
         private let rows: [Row] = [
           .init(id: .plan, n: "01", name: "PLAN МЕСЯЦА", ownerOnly: false),
           .init(id: .accounts, n: "02", name: "СЧЕТА", ownerOnly: false),
           .init(id: .analytics, n: "03", name: "АНАЛИТИКА", ownerOnly: false),
           .init(id: .settings, n: "04", name: "НАСТРОЙКИ", ownerOnly: false),
           .init(id: .access, n: "05", name: "ДОСТУП", ownerOnly: true),
         ]

         var body: some View {
           ZStack {
             PosterTokens.Color.black.ignoresSafeArea()
             ScrollView {
               VStack(alignment: .leading, spacing: 24) {
                 // Header
                 HStack {
                   if router?.canPop == true {
                     Button("← НАЗАД") { router?.pop() }
                       .foregroundColor(.paper)
                   }
                   Spacer()
                   Eyebrow("MANAGEMENT / УПРАВЛЕНИЕ", color: .paper)
                 }
                 .padding(.top, 56)

                 Mass("Управление.", italic: true, size: 70)
                   .foregroundColor(.paper)

                 // Numbered list
                 VStack(spacing: 0) {
                   ForEach(rows.filter { !$0.ownerOnly || model.isOwner }, id: \.id) { row in
                     Button(action: { onTap(row.id) }) {
                       HStack(alignment: .firstTextBaseline, spacing: 16) {
                         Text(row.n)
                           .font(.custom(PosterTokens.Font.jetBrainsMono, size: 14))
                           .foregroundColor(.paper.opacity(0.7))
                         Text(row.name)
                           .font(.custom(PosterTokens.Font.archivoBlack, size: 16))
                           .foregroundColor(.paper)
                           .tracking(0.04 * 16)
                         Spacer()
                         Text("→")
                           .font(.custom(PosterTokens.Font.jetBrainsMono, size: 16))
                           .foregroundColor(.paper.opacity(0.7))
                       }
                       .padding(.vertical, 22)
                     }
                     .buttonStyle(.plain)
                     Divider().background(PosterTokens.Color.paper.opacity(0.18))
                   }
                 }
               }
               .padding(.horizontal, 22)
               .padding(.bottom, 90)
             }
           }
           .task { await model.load() }
         }

         private func onTap(_ id: MgmtRowId) {
           switch id {
           case .plan: router?.push(PlanView())
           case .accounts: router?.push(AccountsListV10View())
           case .analytics: router?.push(AnalyticsV10View())
           case .settings: router?.push(SettingsV10View())
           case .access: router?.push(AccessV10View())
           }
         }
       }
       ```

    3. **`MgmtHubTests.swift`** (NEW, 4-6 cases):
       - test_isOwner_default_false.
       - test_load_sets_owner_when_role_owner — mock MeV10API to return role 'owner' → assert isOwner = true.
       - test_load_sets_not_owner_when_role_member.
       - test_load_silent_on_error — fetchMeV10 throws → isOwner stays false.

    Commit: `feat(27-11): MgmtHubView + VM + tests`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodebuild test -only-testing:BudgetPlannerTests/MgmtHubTests -scheme BudgetPlanner -destination "platform=iOS Simulator,name=iPhone 17 Pro" 2>&1 | grep -E "Test Suite.*passed|FAILED" | head -3</automated>
  </verify>
  <done>
    - MgmtHubView + VM + tests created.
    - 4+ tests pass.
    - `grep -c "PLAN МЕСЯЦА\|СЧЕТА\|АНАЛИТИКА\|НАСТРОЙКИ\|ДОСТУП" ios/BudgetPlanner/FeaturesV10/Management/MgmtHubView.swift` ≥ 5.
  </done>
</task>

<task type="auto">
  <name>Task 2: SettingsV10View + ViewModel + AccessV10View + ViewModel + APIs</name>
  <read_first>
    - ios/BudgetPlanner/Features/Management/SettingsView.swift (v0.6 form fields)
    - ios/BudgetPlanner/Features/Management/AccessView.swift (v0.6 admin tabs)
    - ios/BudgetPlanner/Networking/Endpoints/MeAPI.swift
    - app/api/routes/settings.py (verify endpoint paths)
    - app/api/routes/admin.py (verify admin endpoints)
  </read_first>
  <action>
    1. **`SettingsAPI.swift`** (NEW or extend existing):
       ```swift
       struct SettingsDTO: Codable, Equatable {
         let cycleStartDay: Int
         let notifyDaysBefore: Int
         let aiCategorizationEnabled: Bool
         let aiSpendCapCents: Int
       }
       struct SettingsPatchRequest: Encodable {
         let cycleStartDay: Int?
         let notifyDaysBefore: Int?
         let aiCategorizationEnabled: Bool?
         func encode(to encoder: Encoder) throws { /* encodeIfPresent each */ }
         enum CodingKeys: String, CodingKey { case cycleStartDay, notifyDaysBefore, aiCategorizationEnabled }
       }
       enum SettingsAPI {
         static func get() async throws -> SettingsDTO {
           try await APIClient.shared.request("GET", "/api/v1/settings")
         }
         static func patch(_ req: SettingsPatchRequest) async throws -> SettingsDTO {
           try await APIClient.shared.request("PATCH", "/api/v1/settings", body: req)
         }
       }
       ```

    2. **`SettingsV10ViewModel.swift`** (NEW, ~120 LOC):
       Status state machine + load + 3 mutation methods (changeCycleDay, changeNotifyDays, toggleAiCat).

    3. **`SettingsV10View.swift`** (NEW, ~180 LOC):
       Composition (PAPER bg, ink text):
       - Header «← НАЗАД» + Eyebrow «SETTINGS / НАСТРОЙКИ».
       - Mass «Настройки.» italic 56 ink.
       - Form rows:
         - Eyebrow «День начала цикла» + HStack(Stepper(value: $day, in: 1...28)).
         - Eyebrow «Напоминать за дней до подписки» + Stepper(0...30).
         - Eyebrow «AI авто-категоризация» + Toggle.
         - Eyebrow «AI лимит расходов» + Text «\(cap_cents/100) ₽» mono read-only.
       - .task { await model.load() } first appear.
       - .onChange of each field — call mutation.

    4. **`AdminAPI.swift`** (NEW or extend):
       ```swift
       struct AdminUserDTO: Decodable, Identifiable, Equatable {
         let id: Int; let tgUserId: Int; let username: String?; let role: String
       }
       struct AdminAiUsageDTO: Decodable, Identifiable, Equatable {
         let date: Date; let tokens: Int; let costCents: Int
         var id: String { ISO8601DateFormatter().string(from: date) }
       }
       enum AdminAPI {
         static func users() async throws -> [AdminUserDTO] {
           try await APIClient.shared.request("GET", "/api/v1/admin/users")
         }
         static func aiUsage() async throws -> [AdminAiUsageDTO] {
           try await APIClient.shared.request("GET", "/api/v1/admin/ai-usage")
         }
       }
       ```

    5. **`AccessV10ViewModel.swift`** (NEW, ~100 LOC):
       Tab state, parallel fetch users + aiUsage on load.

    6. **`AccessV10View.swift`** (NEW, ~200 LOC):
       Composition (BLACK bg, paper text):
       - Header + Mass «Доступ.» italic 56 paper.
       - HStack 2 chips «Пользователи» / «AI Usage» — selected drives activeTab.
       - if activeTab == .users: ForEach(users) — name + role badge.
       - if activeTab == .aiUsage: ForEach(usage) — date + tokens + cost.
       - .task { await model.load() }.

    Commit: `feat(27-11): SettingsV10View + AccessV10View + APIs`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodebuild build -scheme BudgetPlanner -destination "platform=iOS Simulator,name=iPhone 17 Pro" 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | head -5</automated>
  </verify>
  <done>
    - SettingsV10View + AccessV10View + 2 VMs + 2 APIs created.
    - xcodebuild build succeeds.
    - `grep -c "Настройки\|День начала цикла\|AI авто-категоризация" ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift` ≥ 3.
    - `grep -c "Доступ\|Пользователи\|AI Usage" ios/BudgetPlanner/FeaturesV10/Management/AccessV10View.swift` ≥ 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: V10MainShell.swift handleTabChange — replace placeholders with real Views</name>
  <read_first>
    - ios/BudgetPlanner/App/V10MainShell.swift (current routing)
    - ios/BudgetPlanner/FeaturesV10/Home/HomePlaceholders.swift (zero-touch swap pattern reference)
  </read_first>
  <action>
    1. **`V10MainShell.swift`** — modify handleTabChange:
       ```swift
       // Top of file — add imports if needed (Swift modules don't require explicit
       // imports for same-target types — but `import SavingsV10View` нет, types are
       // available globally in BudgetPlanner module). No import changes needed.

       private func handleTabChange(_ tab: TabId) {
         switch tab {
         case .home:
           router.popToRoot()
         case .savings:
           router.push(SavingsV10View())
         case .ai:
           router.push(AiV10View())
         case .mgmt:
           router.push(MgmtHubView())
         }
       }
       ```

       Update the inline doc comment block above handleTabChange to reflect new routing
       («real screens land in Phase 27» → «real screens (Plans 27-07..11)»).

    2. **DO NOT** delete `HomePlaceholders.swift` — it's still referenced by older tests или
       potential future use; just leave it untouched.

    3. xcodebuild build + test pass:
       - `xcodebuild build -scheme BudgetPlanner -destination "platform=iOS Simulator,name=iPhone 17 Pro"` — exit 0.
       - `xcodebuild test -scheme BudgetPlanner -destination "platform=iOS Simulator,name=iPhone 17 Pro"` — full suite no regressions.

    Commit: `feat(27-11): V10MainShell handleTabChange wires real Savings/Ai/Mgmt views`.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/ios && xcodebuild build -scheme BudgetPlanner -destination "platform=iOS Simulator,name=iPhone 17 Pro" 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:" | head -5 && grep -c "SavingsV10View\|AiV10View\|MgmtHubView" ios/BudgetPlanner/App/V10MainShell.swift</automated>
  </verify>
  <done>
    - V10MainShell.swift handleTabChange pushes real Views для savings/ai/mgmt.
    - `grep -c "AccountsListPlaceholderView\|PlanViewPlaceholderView" ios/BudgetPlanner/App/V10MainShell.swift` == 0 (replaced; placeholders only used by HomePlaceholders.swift swap, not in shell).
    - `grep -c "SavingsV10View\|AiV10View\|MgmtHubView" ios/BudgetPlanner/App/V10MainShell.swift` ≥ 3.
    - xcodebuild build succeeds.
    - Full test suite no regressions.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-27-11-01 | Elevation | non-owner sees ДОСТУП row | mitigate | `if !ownerOnly || model.isOwner` filter; AccessV10ViewModel re-checks; backend admin routes already require owner. |
| T-27-11-02 | Tampering | settings PATCH out of range | mitigate | Stepper bounds + backend Pydantic validators. |
| T-27-11-03 | Repudiation | accidental settings change | accept | Inline change immediate; user sees value live. |
| T-27-11-04 | Information Disclosure | admin Users / AI Usage cross-tenant | accept | Backend gate + RLS. |
</threat_model>

<verification>
- xcodebuild build + test pass; full suite no regressions.
- MgmtHubTests ≥4 pass.
- All grep gates met:
  - V10MainShell имеет SavingsV10View / AiV10View / MgmtHubView.
  - V10MainShell не имеет AccountsListPlaceholderView / PlanViewPlaceholderView в handleTabChange.
</verification>

<success_criteria>
MGMT-V10-01..04 covered for iOS. iOS Phase 27 complete: все 4 tab routes wire real screens; Mgmt hub gates ДОСТУП за owner-role.
</success_criteria>

<output>
`.planning/phases/27-ai-savings-accounts-analytics-management/27-11-SUMMARY.md`
</output>
