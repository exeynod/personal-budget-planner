---
phase: 51-liquid-glass-system
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - frontend/src/componentsV10/GlassCard.tsx
  - frontend/src/componentsV10/GlassCard.module.css
  - frontend/src/componentsV10/__tests__/GlassCard.test.tsx
  - frontend/src/componentsV10/index.ts (export)
  - ios/BudgetPlanner/FeaturesV10/Common/GlassCard.swift
autonomous: true
requirements: [LG-SYS-05]
status: complete
commits:
  - 78d6a27 (web)
  - ac7689b (ios)
---

# Plan 51-02 — GlassCard primitive (web + iOS)

Web `<GlassCard>` React component: translucent surface через `backdrop-filter`, 4 material levels,
4 elevation levels, optional inner border highlight, optional onClick → button role.
LG token consumption через CSS-vars (`--lg-radius-card`, `--lg-glass-tint-*`, `--lg-shadow-*`,
`--lg-material-*`); fallback solid surface когда `data-theme != liquid_glass`.

iOS `GlassCard<Content>` SwiftUI view: SwiftUI Material backing (`.ultraThinMaterial` /
`.thinMaterial` / `.regularMaterial` / `.thickMaterial`) + RoundedRectangle clip + inner gradient
stroke (innerBorder) + shadow per elevation + optional Button wrapper для tap-action.
`.glassEffect()` upgrade — Phase 53.

## Verification

- Web vitest GlassCard.test.tsx — 6/6 pass.
- `tsc --noEmit` — clean (no GlassCard errors).
- iOS `make build` — Build Succeeded (GlassCard.swift compiles на iPhone 17 Pro Simulator).

**Commits:** 78d6a27 (web), ac7689b (ios).
