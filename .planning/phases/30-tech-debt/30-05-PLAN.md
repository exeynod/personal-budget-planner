---
phase: 30-tech-debt
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/screensV10/Transactions/TransactionsView.tsx
  - frontend/src/screensV10/Transactions/TransactionsView.module.css
autonomous: true
requirements:
  - DEBT-05
must_haves:
  truths:
    - "Touch-only: swipe-left на transaction row exposes red «УДАЛИТЬ» action ≥80px wide; tap → confirms + DELETE call."
    - "Desktop fallback: right-click context-menu с «Удалить» option (для разработки/тестов)."
    - "Удалённый row дисapears из DOM; parent refetch не required (optimistic remove)."
  artifacts:
    - path: ".planning/phases/30-tech-debt/30-05-SUMMARY.md"
      contains: "DEBT-05"
      min_lines: 10
---

<objective>
Add swipe-left to delete на web Transactions row, parity с iOS swipeActions. Fallback right-click context-menu для desktop.

Реализация: CSS scroll-snap (touch-action: pan-x) для swipe motion + onContextMenu для desktop. Минимально — без npm-зависимостей.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tech-debt/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransactionRow swipe + delete handler</name>
  <files>
    frontend/src/screensV10/Transactions/TransactionsView.tsx
    frontend/src/screensV10/Transactions/TransactionsView.module.css
  </files>
  <action>
    1. Read existing TransactionsView — find row component.
    2. Wrap each row в container `.swipeContainer` со side-by-side {row, delete-action}; CSS: row scrolls left to reveal action behind via `scroll-snap-type: x mandatory`.
    3. Add `onScroll` handler — если scroll-position > threshold → snap to action; else snap back.
    4. Delete button onClick → calls existing delete API (`deleteActualV10` or similar) → optimistic remove from local state.
    5. onContextMenu → preventDefault + show simple `<menu>` overlay с «Удалить» / «Отмена» (desktop fallback).
    6. CSS: action button 80px width, bg `--poster-red`, color `--poster-paper`, font Manrope 13px UPPERCASE.
    7. Test smoke: `npx vitest run frontend/src/screensV10/Transactions/` exit 0 (no regression).
  </action>
  <done>Swipe-left delete works on mobile + desktop right-click fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Commit + SUMMARY</name>
  <action>`feat(30-05): add swipe-left delete on web Transactions row`. SUMMARY.</action>
  <done>Commit + SUMMARY.</done>
</task>

</tasks>

<verification>
1. Vitest Transactions tests green.
2. tsc clean (no new errors).
3. SUMMARY exists.
</verification>

<output>30-05-SUMMARY.md per template.</output>
