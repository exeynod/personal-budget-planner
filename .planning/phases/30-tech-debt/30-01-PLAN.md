---
phase: 30-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/api/v10/analytics.ts
  - frontend/src/screensV10/Ai/AiView.tsx
  - frontend/src/screensV10/Ai/__tests__/AiView.test.tsx
  - frontend/src/screensV10/__tests__/TxV10TabDemote.test.tsx
autonomous: true
requirements:
  - DEBT-01
must_haves:
  truths:
    - "`cd frontend && npx tsc --noEmit` exit 0 (на всём codebase, не только затронутые файлы)."
    - "`cd frontend && npx vitest run` exit 0 (683+ tests pass, regression-проверено)."
  artifacts:
    - path: ".planning/phases/30-tech-debt/30-01-SUMMARY.md"
      provides: "Список fixed TS errors с classification (false positive / missing import / type narrowing / test signature mismatch)."
      min_lines: 15
      contains: "DEBT-01"
---

<objective>
Закрыть pre-existing TS errors в codebase: `analytics.ts`, `AiView.tsx`, `AiView.test.tsx`, `TxV10TabDemote.test.tsx`. Эти errors были documented в v1.0 milestone tech debt, blocking `tsc --noEmit` clean exit.

Output: tsc exit 0 + vitest 683/683 green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tech-debt/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify + fix TS errors</name>
  <files>
    frontend/src/api/v10/analytics.ts
    frontend/src/screensV10/Ai/AiView.tsx
    frontend/src/screensV10/Ai/__tests__/AiView.test.tsx
    frontend/src/screensV10/__tests__/TxV10TabDemote.test.tsx
  </files>
  <action>
    1. Run `cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc-before.log` — capture all current errors.
    2. For each error: classify as (a) false positive (e.g. test signature mismatch), (b) missing import, (c) type narrowing issue, (d) actual type bug. Fix smallest-first.
    3. Common patterns expected:
       - `analytics.ts`: missing response-type annotation → add explicit interface.
       - `AiView.tsx`: `unknown` from JSON.parse → add type guard or assertion.
       - `AiView.test.tsx`: mock function signature mismatch → update mock to match real signature.
       - `TxV10TabDemote.test.tsx`: `node:fs/__dirname` issue in test env → check vitest config or use `import.meta.url`.
    4. After each file fix: `npx tsc --noEmit` to verify reduction. Don't break vitest specs — re-run `npx vitest run` if test files touched.
    5. Final verification: tsc exit 0 + vitest 683+ green.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "error TS" && exit 1 || exit 0</automated>
  </verify>
  <done>tsc clean, vitest 683+ pass.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` exit 0.
2. `cd frontend && npx vitest run` exit 0.
3. SUMMARY.md created.
</verification>

<output>
Create `.planning/phases/30-tech-debt/30-01-SUMMARY.md` documenting:
- TS errors before count + after count (0).
- Per-file classification + fix.
- Any deviations.
</output>
