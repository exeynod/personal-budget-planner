---
phase: 30-tech-debt
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/screensV10/Subscriptions/SubscriptionMenuSheet.tsx
  - ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionMenuSheet.swift
autonomous: true
requirements:
  - DEBT-04
must_haves:
  truths:
    - "Web: day/price editor PATCH failure показывает PosterToast с error message (НЕ silent fail)."
    - "iOS: day/price editor PATCH failure показывает PosterToast с error message; toast не блокирует UI."
    - "Retry path: либо toast имеет «Повторить» action, либо user может re-trigger через тот же editor input."
  artifacts:
    - path: ".planning/phases/30-tech-debt/30-04-SUMMARY.md"
      contains: "DEBT-04"
      min_lines: 10
---

<objective>
Surface backend errors из day/price editor patch на subscriptions screen — web + iOS. Текущее поведение: PATCH 4xx/5xx → silent (только console). Должно: PosterToast с error message + auto-dismiss 4s.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tech-debt/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web — wrap PATCH in try/catch + PosterToast</name>
  <files>frontend/src/screensV10/Subscriptions/SubscriptionMenuSheet.tsx</files>
  <action>
    1. Read existing `SubscriptionMenuSheet.tsx` — find day/price editor PATCH calls (likely `updateSubscription({...})`).
    2. Wrap call in try/catch. On catch: `setToast({ message: 'Не удалось обновить · ' + err.message, kind: 'error' })`.
    3. Toast: использовать `<Toast>` из `frontend/src/componentsV10/Toast.tsx`. Если subscription parent не имеет toast state — поднять в parent SubscriptionsMount.
    4. Verify `npx vitest run frontend/src/screensV10/Subscriptions/ 2>&1` exit 0.
  </action>
  <done>Web subscription PATCH failures surface via toast.</done>
</task>

<task type="auto">
  <name>Task 2: iOS — equivalent PATCH error surfacing</name>
  <files>ios/BudgetPlanner/FeaturesV10/Subscriptions/SubscriptionMenuSheet.swift</files>
  <action>
    1. Read existing Swift file, find PATCH callsite (likely in associated ViewModel).
    2. Wrap async call in do/catch. On catch: trigger `PosterToast` через existing toast manager (если есть) или local `@State` toast binding.
    3. Build: `cd ios && make build` exit 0.
  </action>
  <done>iOS subscription PATCH failures surface via toast.</done>
</task>

<task type="auto">
  <name>Task 3: Commit + SUMMARY</name>
  <action>Atomic commit `fix(30-04): surface subscription PATCH errors via toast (web+iOS)`. Write SUMMARY.</action>
  <done>Commit + SUMMARY.md.</done>
</task>

</tasks>

<verification>
1. Web vitest green for Subscriptions tests.
2. iOS build clean.
3. SUMMARY exists.
</verification>

<output>
30-04-SUMMARY.md per gsd template.
</output>
