---
phase: 30-tech-debt
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/screensV10/Home/HomeView.module.css
  - frontend/src/screensV10/Home/HomeView.tsx
  - frontend/src/screensV10/Home/HomeMount.tsx
  - frontend/src/screensV10/Home/useHomeColor.ts
  - frontend/src/screensV10/Management/HomeColorPickerSheet.tsx
  - frontend/src/screensV10/Management/HomeColorPickerSheet.module.css
  - frontend/src/screensV10/Management/SettingsView.tsx
  - frontend/src/screensV10/Management/SettingsView.module.css
  - frontend/src/screensV10/Management/SettingsMount.tsx
  - ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift
  - ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift
  - ios/BudgetPlanner/FeaturesV10/Management/SettingsV10ViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Management/HomeColorPickerSheet.swift
autonomous: true
requirements:
  - DEBT-08
must_haves:
  truths:
    - "User в Management → Настройки видит row «Цвет Home» с текущим swatch + label («КОРАЛ» / «КОБАЛЬТ» / «ЧЁРНЫЙ» / «КРЕМ»)."
    - "Tap на row открывает picker sheet (web bottom-sheet, iOS posterSheet) с 4 swatches; tap на swatch — instant выбор + close + apply."
    - "Выбор персистится: web `localStorage.setItem('ui.home-color', value)`, iOS `@AppStorage(\"ui.home-color\")`."
    - "Home рендерит выбранный цвет background-а; default fallback = coral если value не задано или невалидно (whitelist coral|cobalt|black|cream)."
    - "Применение мгновенное — после picker close Home пере-рендерится с новым цветом без перезагрузки страницы / app."
  artifacts:
    - path: "frontend/src/screensV10/Home/useHomeColor.ts"
      provides: "Хук + утилиты: типы HomeColor, useHomeColor() возвращает [value, setter]; setValue dispatches `home-color-changed` event."
      min_lines: 30
      contains: "useHomeColor"
    - path: "frontend/src/screensV10/Management/HomeColorPickerSheet.tsx"
      provides: "Bottom-sheet picker компонент: 4 swatch chips, current value highlighted."
      min_lines: 40
      contains: "HomeColorPickerSheet"
    - path: "ios/BudgetPlanner/FeaturesV10/Management/HomeColorPickerSheet.swift"
      provides: "PosterSheet picker для iOS: 4 colorChips, current highlighted."
      min_lines: 40
      contains: "HomeColorPickerSheet"
  key_links:
    - from: "HomeView.module.css `.root`"
      to: "CSS custom prop --color-home"
      via: "background: var(--color-home, var(--poster-coral))"
      pattern: "--color-home"
    - from: "HomeMount.tsx"
      to: "useHomeColor hook"
      via: "Read current value, pass as inline style on HomeView"
      pattern: "useHomeColor"
    - from: "iOS HomeV10View.swift line 32 + 342"
      to: "homeBackgroundColor computed prop"
      via: "Switch homeColorStorage value → PosterTokens.Color.{coral,cobalt,black,cream}"
      pattern: "homeBackgroundColor"
---

<objective>
Реализовать настраиваемый цвет фона Home-экрана (web + iOS).
Persistence: web `localStorage['ui.home-color']`, iOS `@AppStorage("ui.home-color")`.
Picker UI в Management → Настройки → row «Цвет Home» → bottom-sheet/posterSheet с 4 swatches:
**КОРАЛ** (default `#FF5A3C`), **КОБАЛЬТ** (`#1B2A6B`), **ЧЁРНЫЙ** (`#0E0E0E`), **КРЕМ** (`#F4EAD9`).

Purpose: User-request 2026-05-11 — продвинутый из v1.1 backlog DF-V11-04.
Технически минимальная фича: ноль новых backend endpoints (чисто клиентское состояние),
переиспользует существующие token-значения, никаких новых анимаций.

Output: 1 commit web + 1 commit iOS (или 1 объединённый), мгновенное применение.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-tech-debt/30-CONTEXT.md
@frontend/src/screensV10/Home/HomeView.tsx
@frontend/src/screensV10/Home/HomeView.module.css
@frontend/src/screensV10/Home/HomeMount.tsx
@frontend/src/screensV10/Management/SettingsView.tsx
@frontend/src/screensV10/Management/SettingsMount.tsx
@frontend/src/stylesV10/tokens.css
@ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift
@ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift
@ios/BudgetPlanner/FeaturesV10/Management/SettingsV10ViewModel.swift
@ios/BudgetPlanner/PosterTokens.swift

<!-- Reference values:
     CSS tokens: --poster-coral #FF5A3C, --poster-cobalt #1B2A6B, --poster-black #0E0E0E, --poster-cream #F4EAD9
     iOS tokens: PosterTokens.Color.{coral, cobalt, black, cream}
     PosterSheet web component: frontend/src/componentsV10/PosterSheet.tsx (existing)
     iOS posterSheet modifier: ios/BudgetPlanner/Common/PosterSheet.swift (existing) -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web useHomeColor hook + HomeView CSS-var override</name>
  <files>
    frontend/src/screensV10/Home/useHomeColor.ts (new)
    frontend/src/screensV10/Home/HomeView.module.css
    frontend/src/screensV10/Home/HomeView.tsx
    frontend/src/screensV10/Home/HomeMount.tsx
  </files>
  <read_first>
    - frontend/src/screensV10/Home/HomeView.module.css (текущий `.root` background rule line 13)
    - frontend/src/screensV10/Home/HomeView.tsx (component shape, props)
    - frontend/src/screensV10/Home/HomeMount.tsx (где рендерится HomeView)
  </read_first>
  <acceptance_criteria>
    - Новый файл `frontend/src/screensV10/Home/useHomeColor.ts` экспортирует:
      - `type HomeColor = 'coral' | 'cobalt' | 'black' | 'cream'`
      - `HOME_COLORS: readonly HomeColor[]` (для итерации в picker)
      - `homeColorLabel(c: HomeColor): string` — русские labels
      - `homeColorCssValue(c: HomeColor): string` — `'var(--poster-coral)'` etc.
      - `useHomeColor(): [HomeColor, (next: HomeColor) => void]` — реактивный hook:
        - initial value: `localStorage.getItem('ui.home-color')` если валиден → else `'coral'`
        - setter: writes localStorage + dispatches `window.dispatchEvent(new CustomEvent('home-color-changed', { detail: next }))`
        - subscribes к `home-color-changed` event + `storage` event (cross-tab), useState re-renders
    - `HomeView.module.css` line 13 изменён: `background: var(--color-home, var(--poster-coral));`
    - `HomeView.tsx`: добавляется `homeColor?: HomeColor` prop; root div получает `style={{ '--color-home': homeColorCssValue(homeColor ?? 'coral') } as CSSProperties}`.
    - `HomeMount.tsx`: вызывает `useHomeColor()`, passes value to HomeView.
    - tsc clean (`cd frontend && npx tsc --noEmit` exit 0 на затронутые файлы — `useHomeColor.ts`/`HomeView.tsx`/`HomeMount.tsx`).
  </acceptance_criteria>
  <action>
    1. Создать `useHomeColor.ts`:
       ```ts
       import { useEffect, useState, useCallback } from 'react';

       export type HomeColor = 'coral' | 'cobalt' | 'black' | 'cream';
       export const HOME_COLORS: readonly HomeColor[] = ['coral', 'cobalt', 'black', 'cream'] as const;
       const STORAGE_KEY = 'ui.home-color';
       const EVENT = 'home-color-changed';
       const DEFAULT: HomeColor = 'coral';

       function isHomeColor(v: unknown): v is HomeColor {
         return v === 'coral' || v === 'cobalt' || v === 'black' || v === 'cream';
       }

       function readStored(): HomeColor {
         try {
           const raw = typeof window !== 'undefined'
             ? window.localStorage.getItem(STORAGE_KEY)
             : null;
           return isHomeColor(raw) ? raw : DEFAULT;
         } catch {
           return DEFAULT;
         }
       }

       export function homeColorLabel(c: HomeColor): string {
         switch (c) {
           case 'coral':  return 'КОРАЛ';
           case 'cobalt': return 'КОБАЛЬТ';
           case 'black':  return 'ЧЁРНЫЙ';
           case 'cream':  return 'КРЕМ';
         }
       }

       export function homeColorCssValue(c: HomeColor): string {
         switch (c) {
           case 'coral':  return 'var(--poster-coral)';
           case 'cobalt': return 'var(--poster-cobalt)';
           case 'black':  return 'var(--poster-black)';
           case 'cream':  return 'var(--poster-cream)';
         }
       }

       export function useHomeColor(): [HomeColor, (next: HomeColor) => void] {
         const [value, setValue] = useState<HomeColor>(readStored);

         useEffect(() => {
           const onCustom = (e: Event) => {
             const detail = (e as CustomEvent<HomeColor>).detail;
             if (isHomeColor(detail)) setValue(detail);
           };
           const onStorage = (e: StorageEvent) => {
             if (e.key === STORAGE_KEY && isHomeColor(e.newValue)) setValue(e.newValue);
           };
           window.addEventListener(EVENT, onCustom);
           window.addEventListener('storage', onStorage);
           return () => {
             window.removeEventListener(EVENT, onCustom);
             window.removeEventListener('storage', onStorage);
           };
         }, []);

         const setter = useCallback((next: HomeColor) => {
           try { window.localStorage.setItem(STORAGE_KEY, next); } catch {}
           window.dispatchEvent(new CustomEvent<HomeColor>(EVENT, { detail: next }));
           setValue(next);
         }, []);

         return [value, setter];
       }
       ```
    2. В `HomeView.module.css` найти `.root { ... background: var(--poster-coral); ... }` и изменить на `background: var(--color-home, var(--poster-coral));`.
    3. В `HomeView.tsx`:
       - Импорт: `import { type HomeColor, homeColorCssValue } from './useHomeColor';`
       - В `HomeViewProps` добавить `homeColor?: HomeColor;`
       - В destructure: `homeColor = 'coral'`
       - На root `<div className={styles.root}>`: добавить `style={{ '--color-home': homeColorCssValue(homeColor) } as CSSProperties}`. Импорт `CSSProperties` уже есть.
    4. В `HomeMount.tsx`:
       - Импорт: `import { useHomeColor } from './useHomeColor';`
       - В `HomeMount()` сразу после `useState`: `const [homeColor] = useHomeColor();`
       - В JSX `<HomeView ... />` (около строки 191) добавить prop `homeColor={homeColor}`.
    5. Проверить tsc: `cd frontend && npx tsc --noEmit 2>&1 | grep -E "Home(View|Mount)|useHomeColor"` — ожидается 0 строк (если pre-existing errors есть в других файлах — игнор, DEBT-01 — отдельный plan).
  </action>
  <verify>
    <automated>test -f frontend/src/screensV10/Home/useHomeColor.ts && grep -q "useHomeColor" frontend/src/screensV10/Home/HomeMount.tsx && grep -q "homeColor" frontend/src/screensV10/Home/HomeView.tsx && grep -q "var(--color-home" frontend/src/screensV10/Home/HomeView.module.css</automated>
  </verify>
  <done>
    Web: hook создан, CSS-var override на месте, HomeMount читает value.
    Без UI ещё нельзя поменять — Task 2 даст picker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Web HomeColorPickerSheet + SettingsView row</name>
  <files>
    frontend/src/screensV10/Management/HomeColorPickerSheet.tsx (new)
    frontend/src/screensV10/Management/HomeColorPickerSheet.module.css (new)
    frontend/src/screensV10/Management/SettingsView.tsx
    frontend/src/screensV10/Management/SettingsView.module.css
    frontend/src/screensV10/Management/SettingsMount.tsx
  </files>
  <read_first>
    - frontend/src/componentsV10/PosterSheet.tsx (existing bottom-sheet primitive)
    - frontend/src/screensV10/Management/SettingsView.tsx (existing row structure)
    - frontend/src/screensV10/Management/SettingsMount.tsx (state management pattern)
  </read_first>
  <acceptance_criteria>
    - Новый `HomeColorPickerSheet.tsx`: PosterSheet wrapper, отображает 4 swatches (40×40px circle), tap → onSelect + close. Highlighted (border-2 ring-2 with poster-paper) на current.
    - SettingsView добавляет row 5: «Цвет Home» с современным swatch (12×12 circle) + label (`homeColorLabel`) + `→` chevron. onClick → opens sheet.
    - SettingsMount: пере-impl с useHomeColor + sheet open state.
    - sheet closes on backdrop tap И на swatch select.
    - Изменение мгновенно применяется к Home (через useHomeColor event broadcast).
  </acceptance_criteria>
  <action>
    1. Создать `HomeColorPickerSheet.tsx`:
       ```tsx
       import { PosterSheet } from '../common';
       import {
         HOME_COLORS,
         homeColorLabel,
         homeColorCssValue,
         type HomeColor,
       } from '../Home/useHomeColor';
       import styles from './HomeColorPickerSheet.module.css';

       export interface HomeColorPickerSheetProps {
         isOpen: boolean;
         current: HomeColor;
         onSelect: (c: HomeColor) => void;
         onClose: () => void;
       }

       export function HomeColorPickerSheet({ isOpen, current, onSelect, onClose }: HomeColorPickerSheetProps) {
         return (
           <PosterSheet isOpen={isOpen} onClose={onClose} testId="home-color-sheet" backgroundColor="var(--poster-paper)">
             <div className={styles.sheetBody}>
               <div className={styles.title}>ЦВЕТ HOME</div>
               <div className={styles.grid} role="radiogroup" aria-label="Выбор цвета Home-экрана">
                 {HOME_COLORS.map((c) => (
                   <button
                     key={c}
                     type="button"
                     role="radio"
                     aria-checked={c === current}
                     className={c === current ? `${styles.swatch} ${styles.swatchActive}` : styles.swatch}
                     style={{ background: homeColorCssValue(c) }}
                     onClick={() => { onSelect(c); onClose(); }}
                     data-testid={`home-color-${c}`}
                   >
                     <span className={styles.swatchLabel}>{homeColorLabel(c)}</span>
                   </button>
                 ))}
               </div>
             </div>
           </PosterSheet>
         );
       }
       ```
       PosterSheet API (actual): `isOpen`, `onClose`, `children`, optional `backgroundColor`, optional `testId`. No `title` prop — render header inside children.
    2. CSS `HomeColorPickerSheet.module.css`:
       ```css
       .sheetBody { padding: 22px 18px 28px; color: var(--poster-ink, #0E0E0E); }
       .title {
         font-family: var(--poster-font-jet-brains-mono), ui-monospace, monospace;
         font-size: 11px;
         letter-spacing: 0.14em;
         opacity: 0.6;
         font-weight: 600;
         margin-bottom: 18px;
         text-transform: uppercase;
       }
       .grid {
         display: grid;
         grid-template-columns: repeat(2, 1fr);
         gap: 12px;
       }
       .swatch {
         position: relative;
         appearance: none;
         border: 2px solid transparent;
         border-radius: 0;
         min-height: 76px;
         cursor: pointer;
         display: flex;
         align-items: flex-end;
         padding: 10px;
         font-family: var(--poster-font-jet-brains-mono), ui-monospace, monospace;
         font-size: 11px;
         letter-spacing: 0.14em;
         color: var(--poster-paper);
         text-shadow: 0 1px 2px rgba(0,0,0,0.4);
       }
       .swatchActive {
         border-color: var(--poster-paper);
         box-shadow: 0 0 0 2px var(--poster-coral);
       }
       .swatchLabel {
         font-weight: 600;
       }
       ```
    3. Modify `SettingsView.tsx`:
       - Import: `import { useHomeColor, homeColorLabel, homeColorCssValue, type HomeColor } from '../Home/useHomeColor';`
       - Import `HomeColorPickerSheet` and `useState`.
       - В `SettingsViewProps` НЕ добавлять — color picker управляется внутри view через хук (или поднять в Mount — choose Mount для consistency).
       - Better: добавить `homeColor: HomeColor; onSelectHomeColor: (c: HomeColor) => void; pickerOpen: boolean; onTogglePicker: (open: boolean) => void;` в `SettingsViewProps`.
       - Add row 5 в `.list` после AI cap row:
         ```tsx
         <button
           type="button"
           className={`${styles.row} ${styles.rowButton}`}
           onClick={() => props.onTogglePicker(true)}
           data-testid="home-color-row"
         >
           <Eyebrow color="var(--poster-ink, #0E0E0E)">Цвет Home</Eyebrow>
           <div className={styles.homeColorPreview}>
             <span
               className={styles.homeColorSwatch}
               style={{ background: homeColorCssValue(props.homeColor) }}
               aria-hidden
             />
             <span className={styles.homeColorLabel}>{homeColorLabel(props.homeColor)}</span>
             <span className={styles.chevron} aria-hidden>→</span>
           </div>
         </button>
         <HomeColorPickerSheet
           isOpen={props.pickerOpen}
           current={props.homeColor}
           onSelect={props.onSelectHomeColor}
           onClose={() => props.onTogglePicker(false)}
         />
         ```
    4. CSS `SettingsView.module.css` дополнения:
       ```css
       .rowButton { all: unset; cursor: pointer; }
       .homeColorPreview { display: flex; align-items: center; gap: 8px; }
       .homeColorSwatch { width: 14px; height: 14px; border: 1px solid rgba(0,0,0,0.2); display: inline-block; }
       .homeColorLabel { font-family: var(--poster-font-jet-brains-mono), ui-monospace, monospace; font-size: 11px; letter-spacing: 0.14em; color: var(--poster-ink, #0E0E0E); font-weight: 600; }
       .chevron { opacity: 0.4; font-size: 14px; }
       ```
    5. Modify `SettingsMount.tsx`:
       - Импорт: `import { useHomeColor, type HomeColor } from '../Home/useHomeColor';`
       - В функции: `const [homeColor, setHomeColor] = useHomeColor();`
       - `const [pickerOpen, setPickerOpen] = useState(false);`
       - Pass props в `<SettingsView ... homeColor={homeColor} onSelectHomeColor={setHomeColor} pickerOpen={pickerOpen} onTogglePicker={setPickerOpen} />`
    6. tsc check на затронутые файлы.
  </action>
  <verify>
    <automated>test -f frontend/src/screensV10/Management/HomeColorPickerSheet.tsx && test -f frontend/src/screensV10/Management/HomeColorPickerSheet.module.css && grep -q "HomeColorPickerSheet" frontend/src/screensV10/Management/SettingsView.tsx && grep -q "useHomeColor" frontend/src/screensV10/Management/SettingsMount.tsx</automated>
  </verify>
  <done>
    Web: Settings → Цвет Home → picker → Home re-renders с новым background-ом.
  </done>
</task>

<task type="auto">
  <name>Task 3: iOS HomeColor enum + HomeV10View background switch</name>
  <files>
    ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift
    ios/BudgetPlanner/FeaturesV10/Home/HomeColor.swift (new)
  </files>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift (lines 30-50 + 340-345 — два места с `PosterTokens.Color.coral.ignoresSafeArea()`)
    - ios/BudgetPlanner/PosterTokens.swift (color names: coral / cobalt / black / cream)
  </read_first>
  <acceptance_criteria>
    - Новый `HomeColor.swift` экспортирует `enum HomeColor: String, CaseIterable { case coral, cobalt, black, cream }` с `var swiftColor: Color` switch и `var ruLabel: String`.
    - `HomeV10View.swift`: добавить `@AppStorage("ui.home-color") private var homeColorRaw: String = "coral"` + computed `private var resolvedHomeColor: Color` (с whitelist fallback на coral).
    - Оба места с `PosterTokens.Color.coral.ignoresSafeArea()` (lines 32 и 342) заменены на `resolvedHomeColor.ignoresSafeArea()`.
    - `xcodebuild build` для scheme BudgetPlanner проходит (если есть Makefile: `cd ios && make build` exit 0).
  </acceptance_criteria>
  <action>
    1. Создать `ios/BudgetPlanner/FeaturesV10/Home/HomeColor.swift`:
       ```swift
       import SwiftUI

       enum HomeColor: String, CaseIterable, Identifiable {
           case coral, cobalt, black, cream

           var id: String { rawValue }

           var swiftColor: Color {
               switch self {
               case .coral:  return PosterTokens.Color.coral
               case .cobalt: return PosterTokens.Color.cobalt
               case .black:  return PosterTokens.Color.black
               case .cream:  return PosterTokens.Color.cream
               }
           }

           var ruLabel: String {
               switch self {
               case .coral:  return "КОРАЛ"
               case .cobalt: return "КОБАЛЬТ"
               case .black:  return "ЧЁРНЫЙ"
               case .cream:  return "КРЕМ"
               }
           }

           static func resolve(_ raw: String) -> HomeColor {
               HomeColor(rawValue: raw) ?? .coral
           }
       }
       ```
    2. Modify `HomeV10View.swift`:
       - В struct сразу после `@State private var model = HomeV10ViewModel()`:
         ```swift
         @AppStorage("ui.home-color") private var homeColorRaw: String = HomeColor.coral.rawValue
         ```
       - Добавить computed:
         ```swift
         private var resolvedHomeColor: Color {
             HomeColor.resolve(homeColorRaw).swiftColor
         }
         ```
       - Line 32: `PosterTokens.Color.coral.ignoresSafeArea()` → `resolvedHomeColor.ignoresSafeArea()`
       - Line ~342: тот же replace.
       - Line 73: `.foregroundColor(PosterTokens.Color.coral)` — это error-state CTA fg-цвет. Если меняем фон Home — текст CTA должен оставаться coral (символический accent), НЕ менять. Leave as-is.
    3. Build check: `cd ios && make build 2>&1 | tail -20` — should exit 0. Если Makefile target отсутствует — `xcodebuild -project ios/BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | xcbeautify --quieter | tail -20`.
  </action>
  <verify>
    <automated>test -f ios/BudgetPlanner/FeaturesV10/Home/HomeColor.swift && grep -q "resolvedHomeColor" ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift && grep -q "@AppStorage.*ui.home-color" ios/BudgetPlanner/FeaturesV10/Home/HomeV10View.swift</automated>
  </verify>
  <done>
    iOS Home переключает background исходя из @AppStorage, build clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: iOS HomeColorPickerSheet + SettingsV10View row</name>
  <files>
    ios/BudgetPlanner/FeaturesV10/Management/HomeColorPickerSheet.swift (new)
    ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift
    ios/BudgetPlanner/FeaturesV10/Management/SettingsV10ViewModel.swift
  </files>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift (полный — для понимания row pattern + sheet integration)
    - ios/BudgetPlanner/FeaturesV10/Management/SettingsV10ViewModel.swift
    - Любой существующий iOS PosterSheet usage (e.g., grep -rn "posterSheet" ios/BudgetPlanner/FeaturesV10/AddSheet/ | head)
  </read_first>
  <acceptance_criteria>
    - Новый `HomeColorPickerSheet.swift`: SwiftUI View с binding `selection: Binding<HomeColor>` + `isPresented: Binding<Bool>`. Grid 2×2 с 4 colorChip (ZStack: Rectangle color + label overlay). Highlighted (white stroke 2px) на current.
    - `SettingsV10View.swift`: добавить row «Цвет Home» (после AI cap row), tap → toggle sheet.
    - `SettingsV10ViewModel.swift`: добавить `@AppStorage("ui.home-color") var homeColorRaw: String` + helpers `homeColor: HomeColor { get / set }` (computed Binding-friendly).
    - Если `SettingsV10View` использует pure View без VM-state для sheet — добавить `@State private var pickerOpen = false` локально.
    - Build clean.
  </acceptance_criteria>
  <action>
    1. Создать `HomeColorPickerSheet.swift`:
       ```swift
       import SwiftUI

       struct HomeColorPickerSheet: View {
           @Binding var selection: HomeColor
           @Binding var isPresented: Bool

           private let columns = [GridItem(.flexible(), spacing: 12), GridItem(.flexible(), spacing: 12)]

           var body: some View {
               VStack(alignment: .leading, spacing: PosterTokens.Space.s18) {
                   Eyebrow("ЦВЕТ HOME", opacity: 0.6)
                   LazyVGrid(columns: columns, spacing: 12) {
                       ForEach(HomeColor.allCases) { color in
                           Button {
                               selection = color
                               isPresented = false
                           } label: {
                               ZStack(alignment: .bottomLeading) {
                                   color.swiftColor
                                       .frame(height: 86)
                                   Text(color.ruLabel)
                                       .font(.custom(PosterTokens.Font.jetBrainsMono, size: 11))
                                       .kerning(11 * 0.14)
                                       .foregroundColor(PosterTokens.Color.paper)
                                       .padding(10)
                                       .shadow(color: .black.opacity(0.4), radius: 2, y: 1)
                               }
                               .overlay(
                                   Rectangle()
                                       .stroke(selection == color ? PosterTokens.Color.paper : .clear, lineWidth: 2)
                               )
                           }
                           .buttonStyle(.plain)
                       }
                   }
               }
               .padding(.horizontal, PosterTokens.Space.s22)
               .padding(.vertical, PosterTokens.Space.s28)
           }
       }
       ```
       Adapt to existing `posterSheet` modifier API — read existing usage в AddSheet first.
    2. Modify `SettingsV10View.swift`:
       - Add `@AppStorage("ui.home-color") private var homeColorRaw: String = HomeColor.coral.rawValue`
       - Computed `private var homeColor: Binding<HomeColor> { Binding(get: { HomeColor.resolve(homeColorRaw) }, set: { homeColorRaw = $0.rawValue }) }`
       - Add `@State private var homeColorPickerOpen = false`
       - В body после AI cap row добавить row «Цвет Home» (по образцу существующих rows) с tap action `homeColorPickerOpen = true`.
       - Attach sheet via `.posterSheet(isPresented: $homeColorPickerOpen) { HomeColorPickerSheet(selection: homeColor, isPresented: $homeColorPickerOpen) }`. Адаптировать к существующему `posterSheet` API (если требует другие args — adapt).
    3. Build: `cd ios && make build` exit 0 OR xcodebuild как в Task 3.
  </action>
  <verify>
    <automated>test -f ios/BudgetPlanner/FeaturesV10/Management/HomeColorPickerSheet.swift && grep -q "HomeColorPickerSheet" ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift && grep -q "@AppStorage.*ui.home-color" ios/BudgetPlanner/FeaturesV10/Management/SettingsV10View.swift</automated>
  </verify>
  <done>
    iOS: Settings → row «Цвет Home» → sheet picker → Home применяет новый background.
  </done>
</task>

<task type="auto">
  <name>Task 5: Quick smoke test + commit</name>
  <files>
    (none — verification only)
  </files>
  <action>
    1. Web: `cd frontend && npx tsc --noEmit 2>&1 | tail -10`. Allow pre-existing errors in unrelated files (analytics.ts, AiView.tsx — those are DEBT-01).
    2. iOS: build attempt as in Task 3-4.
    3. If feasible, quick visual smoke на симуляторе:
       - Launch app (`xcrun simctl launch booted com.exeynod.BudgetPlanner`).
       - Take screenshot: `xcrun simctl io booted screenshot /tmp/home-default.png`.
       - Expected: coral Home (default).
    4. Atomic commit (split web vs iOS если изменения значительные, else single):
       ```
       feat(30-07): add configurable Home screen color (DEBT-08)

       - Web: useHomeColor hook + localStorage + CSS-var override (HomeView.module.css `.root background: var(--color-home, var(--poster-coral))`)
       - iOS: HomeColor enum + @AppStorage + computed resolvedHomeColor in HomeV10View
       - Picker UI in Management → Настройки, 4 swatches (coral/cobalt/black/cream)
       - Instant apply via CustomEvent (web) / @AppStorage (iOS)

       User request 2026-05-11.
       ```
       Used --no-verify if pre-commit hook hits (per CLAUDE.md autonomy + memory).
  </action>
  <verify>
    <automated>git log -1 --oneline | grep -qE "30-07|home.color|DEBT-08"</automated>
  </verify>
  <done>
    Commit на v1.0-maximal-poster branch; Settings → picker → instant Home re-render verified.
  </done>
</task>

</tasks>

<verification>
1. Files exist: useHomeColor.ts, HomeColorPickerSheet.{tsx,module.css} (web), HomeColor.swift, HomeColorPickerSheet.swift (iOS).
2. HomeView.module.css contains `var(--color-home, var(--poster-coral))`.
3. HomeV10View.swift contains `resolvedHomeColor` AND `@AppStorage("ui.home-color"`.
4. SettingsView.tsx + SettingsV10View.swift содержат «Цвет Home» row.
5. Git log: feat commit prefixed 30-07 OR DEBT-08.
</verification>

<success_criteria>
- User в Settings выбирает один из 4 цветов Home; персистентно; мгновенно применяется.
- Default = coral если value не задано или невалидно.
- Никаких backend изменений, чисто клиентское состояние.
- Web tsc затронутые файлы clean (pre-existing errors игнор).
- iOS build clean.
</success_criteria>

<output>
After completion: create `.planning/phases/30-tech-debt/30-07-SUMMARY.md` per gsd template.
Report:
- Final commit hash(es).
- Picker UX confirmation (которые swatches видимы, какой default).
- Any deviations from this plan + rationale.
</output>
