---
phase: 30-tech-debt
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/FeaturesV10/Common/PosterStyle.swift
  - ios/BudgetPlanner/FeaturesV10/AddSheet/KeypadView.swift
  - ios/BudgetPlanner/Networking/Endpoints/SettingsAPI.swift
  - ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift
autonomous: true
requirements:
  - DEBT-06
  - DEBT-07
must_haves:
  truths:
    - "DEBT-06: PosterStyle.swift:44 + KeypadView.swift:72 заменены с `.animation(.easeOut...)` на `.posterAnimation(...)` modifier с reduce-motion respected."
    - "DEBT-07: `SettingsAPI` enum extracted из `TransactionsAPI.swift` в новый `SettingsAPI.swift` файл; imports updated, build clean."
    - "`cd ios && make build` exit 0."
  artifacts:
    - path: ".planning/phases/30-tech-debt/30-06-SUMMARY.md"
      contains: "DEBT-06"
      min_lines: 12
---

<objective>
Два related iOS tech-debt items в одном плане (оба cosmetic / consistency):
- DEBT-06: bare `.animation()` callsites → `.posterAnimation` modifier (consistent with project's reduce-motion-aware pattern).
- DEBT-07: extract SettingsAPI enum в свой файл (Plan 27-11 frontmatter intent — cosmetic re-org).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tech-debt/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEBT-06 — bare .animation → .posterAnimation</name>
  <files>
    ios/BudgetPlanner/FeaturesV10/Common/PosterStyle.swift
    ios/BudgetPlanner/FeaturesV10/AddSheet/KeypadView.swift
  </files>
  <action>
    1. Read PosterStyle.swift line 44 area — find `.animation(.easeOut(duration: X), value: pressed)` pattern.
    2. Look up appropriate `PosterAnimations` alias (likely `.snap` or `.press`). Read `ios/BudgetPlanner/PosterAnimations.swift` first.
    3. Replace: `.posterAnimation(PosterAnimations.snap, value: pressed)` или подходящий alias.
    4. Same для KeypadView.swift line 72.
    5. Verify `.posterAnimation` modifier respects reduce-motion (existing modifier should — verify by reading its definition).
  </action>
  <done>Both callsites use .posterAnimation.</done>
</task>

<task type="auto">
  <name>Task 2: DEBT-07 — SettingsAPI file split</name>
  <files>
    ios/BudgetPlanner/Networking/Endpoints/SettingsAPI.swift (new)
    ios/BudgetPlanner/Networking/Endpoints/TransactionsAPI.swift
  </files>
  <action>
    1. Find `enum SettingsAPI` (или class/struct) внутри TransactionsAPI.swift.
    2. Move весь блок в новый файл `SettingsAPI.swift`. Copy imports from TransactionsAPI.swift.
    3. Verify call-sites: `grep -rn "SettingsAPI" ios/BudgetPlanner` — should compile без import changes (same module).
    4. Build clean.
  </action>
  <done>SettingsAPI в своём файле, build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Build verification + commit + SUMMARY</name>
  <action>
    1. `cd ios && make build` exit 0.
    2. Atomic commit `refactor(30-06): posterAnimation modifier + SettingsAPI file split (DEBT-06+07)`.
    3. SUMMARY.
  </action>
  <done>Build clean, commit + SUMMARY.</done>
</task>

</tasks>

<verification>
1. iOS build clean.
2. No `.animation(.easeOut` regex match в PosterStyle.swift/KeypadView.swift.
3. `test -f ios/BudgetPlanner/Networking/Endpoints/SettingsAPI.swift`.
</verification>

<output>30-06-SUMMARY.md per template.</output>
