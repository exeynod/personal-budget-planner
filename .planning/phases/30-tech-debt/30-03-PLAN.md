---
phase: 30-tech-debt
plan: 03
type: execute
wave: 2
depends_on:
  - "30-07"
files_modified:
  - ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetView.swift
  - ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Home/HomeV10ViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10ViewModel.swift
  - ios/BudgetPlanner/FeaturesV10/AddSheet/AccountPickerSheet.swift
autonomous: true
requirements:
  - DEBT-02
  - DEBT-03
must_haves:
  truths:
    - "DEBT-02 (iOS): AddSheet successful create → Home + Transactions ViewModels reload."
    - "DEBT-03 (iOS): Account picker — posterSheet с list, replaces dialog/cycler."
    - "iOS build clean."
  artifacts:
    - path: ".planning/phases/30-tech-debt/30-03-SUMMARY.md"
      contains: "DEBT-02"
      min_lines: 12
---

<objective>
iOS-parity для web DEBT-02 (refetch) + DEBT-03 (account picker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-tech-debt/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS refetch wiring (DEBT-02)</name>
  <files>
    ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetViewModel.swift
    ios/BudgetPlanner/FeaturesV10/Home/HomeV10ViewModel.swift
    ios/BudgetPlanner/FeaturesV10/Transactions/TransactionsV10ViewModel.swift
  </files>
  <action>
    1. Use NotificationCenter pattern OR shared @Observable AppEnvironment with `lastCreatedTxnAt: Date?` published property.
    2. AddSheetViewModel — after successful create, post notification `Notification.Name.txnCreated` (or update env var).
    3. HomeV10ViewModel — observe notification, call `await load()`.
    4. TransactionsV10ViewModel — same.
    5. Build clean.
  </action>
  <done>Submit on AddSheet → both view models reload.</done>
</task>

<task type="auto">
  <name>Task 2: AccountPickerSheet (DEBT-03 iOS)</name>
  <files>
    ios/BudgetPlanner/FeaturesV10/AddSheet/AccountPickerSheet.swift (new)
    ios/BudgetPlanner/FeaturesV10/AddSheet/AddSheetView.swift
  </files>
  <action>
    1. Create `AccountPickerSheet.swift` — SwiftUI View с binding `selection: Binding<Int?>` + `isPresented: Binding<Bool>` + `accounts: [AccountReadDTO]`. Body: List(.plain) с rows: name + balance + badge.
    2. Tap row → selection = id + isPresented = false.
    3. AddSheetView.swift — replace existing account row UI с `.posterSheet(isPresented: $accountPickerOpen) { AccountPickerSheet(...) }` triggered by tap.
    4. Build clean.
  </action>
  <done>iOS AccountPicker sheet replaces dialog/cycler.</done>
</task>

<task type="auto">
  <name>Task 3: Commit + SUMMARY</name>
  <action>
    `cd ios && make build` exit 0. Commit `feat(30-03): AddSheet refetch + AccountPickerSheet (DEBT-02+03 iOS)`. SUMMARY.
  </action>
</task>

</tasks>

<verification>
1. iOS build clean.
2. Manual: убедиться что AccountPicker появляется в UI через grep ссылки на новый View.
3. SUMMARY.
</verification>

<output>30-03-SUMMARY.md per template.</output>
