---
phase: 55-polish-acceptance
plan: 01
type: execute
wave: 1
depends_on: ['50', '51', '52', '53', '54']
files_modified:
  - docs/THEMES.md (new)
autonomous: true
requirements: [LG-POL-02, LG-POL-05]
status: complete
commit: 8b050c2
---

# Plan 55-01 — docs/THEMES.md + reduce-motion verification

Ship `docs/THEMES.md` — multi-theme architecture documentation для new-contributor
onboarding. Includes:
- Theme registry overview (Maximal Poster / Liquid Glass / iOS Default).
- Storage + source-of-truth architecture (web `localStorage` + iOS `@AppStorage`,
  `design/tokens.json` + codegen pipeline).
- Web mechanism (`useTheme()` hook, override stylesheets, GlassCard primitive).
- iOS mechanism (`@AppStorage` env binding, ThemedBackground helper, GlassCard).
- Switcher UI (web + iOS ThemePickerSheet).
- Token comparison table (Background / Surface / Display font / Text font / Body
  radius / Animations per theme).
- Adding-a-new-theme recipe (5 steps).
- Accessibility notes (`prefers-reduced-motion` + `prefers-color-scheme: dark` +
  WCAG AA manual audit deferred).
- Known limitations (pixel snapshots, screenshots, a11y audit, perf measurement).
- File map (web + iOS + codegen).

**reduce-motion verification:** `frontend/src/stylesV10/liquid-glass.css` lines
95-103 уже содержат `@media (prefers-reduced-motion: reduce)` block активный
под `[data-theme="liquid_glass"] *` — `animation-duration: 0.01ms`,
`transition-duration: 0.01ms`, `scroll-behavior: auto`. Re-verified в этом плане
(LG-POL-02 ✅).

**Commit:** 8b050c2.
