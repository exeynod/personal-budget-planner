---
phase: 66-settings-ai-polish-v06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/BudgetPlanner/Features/Management/ThemeOption.swift
  - ios/BudgetPlanner/Features/Management/SettingsView.swift
  - ios/BudgetPlannerTests/Features/Management/ThemeOptionTests.swift
autonomous: true
requirements: [MGMT-V06-THEME]

must_haves:
  truths:
    - "Из v06 Settings → секция «Дизайн» показывает 4 выбираемых ряда: MAXIMAL POSTER / LIQUID GLASS / IOS DEFAULT / СТАРЫЙ IOS"
    - "Текущий выбор отмечен checkmark (резолв из @AppStorage('ui.theme'))"
    - "Tap по V10-теме (maximal_poster/liquid_glass/ios_default) → пишет ui.theme → AppRouter переключает на V10MainShell с этой темой"
    - "Tap по «СТАРЫЙ IOS» из v06 → пишет 'v06' → шелл остаётся MainShell (валидный no-op-stay)"
    - "Выбор персистит между перезапусками (@AppStorage)"
    - "VERIFY-ONLY (pre-existing, НЕ трогать): aiSpendSection отображает AI cost cap; AIChatView на v1.0 /ai/chat SSE; ManagementView.ManagementItem.all содержит accounts/savings/planEditor ряды"
  artifacts:
    - path: "ios/BudgetPlanner/Features/Management/ThemeOption.swift"
      provides: "Чистый Foundation-only helper: enum ThemeOption + selected(forRaw:)/rawValue(for:)/allOptions/ruLabel"
      contains: "enum ThemeOption"
    - path: "ios/BudgetPlanner/Features/Management/SettingsView.swift"
      provides: "designSection с 4 selectable Button-рядами + checkmark, bound to @AppStorage('ui.theme')"
      contains: "ThemeOption"
    - path: "ios/BudgetPlannerTests/Features/Management/ThemeOptionTests.swift"
      provides: "Unit-тесты резолва/round-trip ThemeOption"
      contains: "ThemeOptionTests"
  key_links:
    - from: "ios/BudgetPlanner/Features/Management/SettingsView.swift designSection"
      to: "@AppStorage('ui.theme') themeRaw"
      via: "Button action: themeRaw = ThemeOption.rawValue(for: option)"
      pattern: "themeRaw = ThemeOption"
    - from: "ios/BudgetPlanner/Features/Management/SettingsView.swift designSection"
      to: "ThemeOption.selected(forRaw:)"
      via: "checkmark выводится для option == selected(forRaw: themeRaw)"
      pattern: "ThemeOption.selected\\(forRaw:"
---

<objective>
Заменить в v06 `SettingsView` секции «Дизайн» одиночную кнопку «Переключить на V10» на полноценный native theme picker (паритет с V10 `ThemePickerSheet`): 4 выбираемых ряда — MAXIMAL POSTER / LIQUID GLASS / IOS DEFAULT / СТАРЫЙ IOS — с checkmark у текущего выбора. Tap пишет `@AppStorage("ui.theme")`, AppRouter реактивно переключает шелл.

Это ЕДИНСТВЕННЫЙ net-new deliverable Phase 66. Остальные две области цели ROADMAP (AI cost cap display, AI chat SSE, Management Hub rows) уже реализованы — они verify-only, код НЕ трогаем.

Purpose: Друг-пользователь и владелец должны мочь вернуться на v06 и переключаться между темами прямо ИЗ v06 Settings (сейчас обратный путь есть только в V10 `SettingsV10View`).

Output: чистый тестируемый `ThemeOption` helper + переработанный `designSection` + unit-тесты резолва/round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-settings-ai-polish-v06/66-CONTEXT.md

<interfaces>
<!-- Контракты, которые исполнитель использует напрямую — кодовая база НЕ исследуется. -->

Из ios/BudgetPlanner/FeaturesV10/Common/PosterTokens.swift:
```swift
enum Theme: String, CaseIterable {
    case maximalPoster = "maximal_poster"
    case liquidGlass = "liquid_glass"
    case iosDefault = "ios_default"
    static func resolve(_ raw: String) -> Theme   // неизвестный raw → .maximalPoster
    var ruLabel: String                            // "MAXIMAL POSTER" / "LIQUID GLASS" / "IOS DEFAULT"
}
```

Из ios/BudgetPlanner/App/AppRouter.swift:
```swift
@AppStorage("ui.theme") private var themeRaw: String = Theme.maximalPoster.rawValue
private var isLegacyV06Shell: Bool { themeRaw == "v06" }   // "v06" → MainShell, иначе → V10MainShell
```

Из ios/BudgetPlanner/Features/Management/SettingsView.swift (текущий designSection — заменить целиком):
```swift
@AppStorage("ui.theme") private var themeRaw: String = Theme.maximalPoster.rawValue
private var designSection: some View {
    Section {
        Button { themeRaw = Theme.maximalPoster.rawValue } label: {
            LabeledContent { Text("MAXIMAL POSTER") ... } label: {
                Label("Переключить на V10", systemImage: "paintpalette") ...
            }
        }
    } header: { Text("Дизайн") } footer: { Text("...") }
}
```

V10-эталон содержимого (РЕФЕРЕНС, НЕ импортировать, НЕ менять):
ios/BudgetPlanner/FeaturesV10/Management/ThemePickerSheet.swift — 4 опции (3 Theme.allCases + sentinel "v06"), per-theme swatch, label, checkmark. v06 sentinel value = `"v06"`, label = «СТАРЫЙ IOS».
</interfaces>

<ios_notes>
- XcodeGen: project.yml использует folder-глобы. Новый .swift файл (`ThemeOption.swift`) и новый тест (`ThemeOptionTests.swift`) → ОБЯЗАТЕЛЬНО `cd ios && xcodegen generate` (или `make generate`) ПЕРЕД сборкой, иначе файлы не попадут в target.
- Симулятор: `iPhone 17 Pro` (см. ios/Makefile SIM).
- Build: `cd ios && make build` (xcbeautify pipeline) или `xcodebuild -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build`.
- Tests: `xcodebuild test -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`.
- Паттерн helper+тест уже устоялся: `Features/Management/SubscriptionsViewData.swift` + `BudgetPlannerTests/Features/Management/SubscriptionsViewDataTests.swift`. ThemeOption следует тому же расположению.
</ios_notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Чистый ThemeOption helper + unit-тесты (RED→GREEN)</name>
  <files>ios/BudgetPlanner/Features/Management/ThemeOption.swift, ios/BudgetPlannerTests/Features/Management/ThemeOptionTests.swift</files>
  <read_first>
    - ios/BudgetPlanner/FeaturesV10/Common/PosterTokens.swift (enum Theme: rawValues, resolve, ruLabel)
    - ios/BudgetPlanner/App/AppRouter.swift (sentinel "v06" семантика)
    - ios/BudgetPlannerTests/Features/Management/SubscriptionsViewDataTests.swift (паттерн теста: import XCTest, @testable import BudgetPlanner, final class ...: XCTestCase)
  </read_first>
  <behavior>
    - selected(forRaw: "maximal_poster") == .maximalPoster
    - selected(forRaw: "liquid_glass") == .liquidGlass
    - selected(forRaw: "ios_default") == .iosDefault
    - selected(forRaw: "v06") == .legacyV06
    - selected(forRaw: "garbage" / "" ) == .maximalPoster  (зеркалит Theme.resolve — неизвестный → maximalPoster)
    - rawValue(for: .maximalPoster) == "maximal_poster"; .liquidGlass == "liquid_glass"; .iosDefault == "ios_default"; .legacyV06 == "v06"
    - round-trip: для каждого option из allOptions → selected(forRaw: rawValue(for: option)) == option
    - allOptions порядок == [.maximalPoster, .liquidGlass, .iosDefault, .legacyV06]
    - ruLabel: .legacyV06 == "СТАРЫЙ IOS"; три остальных совпадают с Theme.ruLabel соответствующего кейса
  </behavior>
  <action>
    Создай `ThemeOption.swift` — чистый Foundation-only helper (НЕ импортировать SwiftUI; разрешён `import Foundation` и тип `Theme` доступен в том же модуле):

    ```
    enum ThemeOption: CaseIterable, Equatable {
        case maximalPoster, liquidGlass, iosDefault, legacyV06

        static let legacyV06Raw = "v06"
        static var allOptions: [ThemeOption] { [.maximalPoster, .liquidGlass, .iosDefault, .legacyV06] }

        static func selected(forRaw raw: String) -> ThemeOption {
            if raw == legacyV06Raw { return .legacyV06 }
            switch Theme.resolve(raw) {            // неизвестный raw → .maximalPoster (mirror)
            case .maximalPoster: return .maximalPoster
            case .liquidGlass:   return .liquidGlass
            case .iosDefault:    return .iosDefault
            }
        }

        static func rawValue(for option: ThemeOption) -> String {
            switch option {
            case .maximalPoster: return Theme.maximalPoster.rawValue
            case .liquidGlass:   return Theme.liquidGlass.rawValue
            case .iosDefault:    return Theme.iosDefault.rawValue
            case .legacyV06:     return legacyV06Raw
            }
        }

        var ruLabel: String {
            switch self {
            case .maximalPoster: return Theme.maximalPoster.ruLabel
            case .liquidGlass:   return Theme.liquidGlass.ruLabel
            case .iosDefault:    return Theme.iosDefault.ruLabel
            case .legacyV06:     return "СТАРЫЙ IOS"
            }
        }
    }
    ```

    Реализует MGMT-V06-THEME (тестируемое ядро picker). Зеркалит `Theme.resolve` для неизвестного raw (per 66-CONTEXT decisions).

    Затем создай `ThemeOptionTests.swift` (`import XCTest`, `@testable import BudgetPlanner`, `final class ThemeOptionTests: XCTestCase`), покрывающий КАЖДЫЙ пункт <behavior>. Один XCTAssert на пункт минимум; round-trip — циклом по allOptions.

    После создания обоих файлов: `cd ios && xcodegen generate` (новые файлы в target), затем запусти тесты — должны быть зелёными.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild test -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BudgetPlannerTests/ThemeOptionTests 2>&1 | xcbeautify | tail -20</automated>
  </verify>
  <done>ThemeOption.swift и ThemeOptionTests.swift существуют; xcodegen generate выполнен; все ThemeOptionTests зелёные (включая unknown-raw→maximalPoster, "v06"→legacyV06, round-trip по allOptions).</done>
</task>

<task type="auto">
  <name>Task 2: Переработать designSection — 4 selectable native ряда + checkmark</name>
  <files>ios/BudgetPlanner/Features/Management/SettingsView.swift</files>
  <read_first>
    - ios/BudgetPlanner/Features/Management/SettingsView.swift (текущий designSection строки ~194-213; @AppStorage("ui.theme") строка ~74; Form-конвенции секций)
    - ios/BudgetPlanner/Features/Management/ThemeOption.swift (создан в Task 1 — API selected(forRaw:)/rawValue(for:)/allOptions/ruLabel)
    - ios/BudgetPlanner/FeaturesV10/Management/ThemePickerSheet.swift (РЕФЕРЕНС визуала: swatch цвета per-theme, описания — воспроизвести в native Form idiom, БЕЗ poster chrome)
  </read_first>
  <action>
    Замени тело `designSection` (НЕ трогай остальные секции/ViewModel/@AppStorage-объявление) на Section с рядом-`Button` на каждый `ThemeOption.allOptions`:

    - Текущий выбор: `let current = ThemeOption.selected(forRaw: themeRaw)`.
    - На каждый `option`: `Button { themeRaw = ThemeOption.rawValue(for: option) } label: { ... }`.
    - Label ряда — native Form idiom: `Label(option.ruLabel, systemImage: ...)` слева + `Image(systemName: "checkmark")` справа когда `option == current` (через `Spacer()` или `LabeledContent`/`HStack`). `.foregroundStyle(.primary)` для label; checkmark `.tint`/accent.
    - Discretion (66-CONTEXT): можно добавить маленький per-theme цветовой swatch (как ThemePickerSheet: maximalPoster→coral, liquidGlass→light grey, iosDefault→grey, legacyV06→system house). Если добавляешь — компактно, не ломая Form-вид. Допустимо обойтись без swatch (icon + label + checkmark) если так чище.
    - `.accessibilityIdentifier("theme-\(ThemeOption.rawValue(for: option))")` на каждом ряду (паритет с ThemePickerSheet, для возможной UI-автоматизации).
    - Header: `Text("Дизайн")`. Footer: переписать — больше не «Переключить на V10», а пояснение про выбор стиля и что «СТАРЫЙ IOS» = текущий нативный шелл (например: «Выберите стиль интерфейса. СТАРЫЙ IOS — нативный SwiftUI-шелл; остальные открывают V10-шелл с выбранной темой.»).

    НЕ использовать PosterRouter / `.posterSheet` / любые FeaturesV10/* зависимости — v06 SettingsView вне poster-контекста. Только native Form/Button/Label. Отдельный sheet НЕ создавать — выбор прямо в Form-секции.

    НЕ модифицировать: aiSection, aiSpendSection, cycleSection, notifySection, SettingsViewModel, AppRouter, FeaturesV10/*.

    После правки: `cd ios && xcodegen generate` (на случай если Task 1 файлы добавились) и собрать.
  </action>
  <verify>
    <automated>cd ios && xcodegen generate && xcodebuild build -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | xcbeautify | tail -15</automated>
  </verify>
  <done>designSection рендерит 4 Button-ряда (MAXIMAL POSTER / LIQUID GLASS / IOS DEFAULT / СТАРЫЙ IOS); checkmark на ряду == ThemeOption.selected(forRaw: themeRaw); каждый Button пишет themeRaw = ThemeOption.rawValue(for: option); сборка зелёная для iPhone 17 Pro; нет ссылок на PosterRouter/.posterSheet/FeaturesV10 из SettingsView.</done>
</task>

</tasks>

<verification>
1. `grep -n "themeRaw = ThemeOption" ios/BudgetPlanner/Features/Management/SettingsView.swift` → присутствует (picker пишет @AppStorage через helper).
2. `grep -n "ThemeOption.selected(forRaw:" ios/BudgetPlanner/Features/Management/SettingsView.swift` → присутствует (checkmark резолвится через helper).
3. `grep -c "PosterRouter\|posterSheet" ios/BudgetPlanner/Features/Management/SettingsView.swift` → 0 (нет poster-зависимостей).
4. Полный прогон тестов зелёный: `cd ios && xcodebuild test -project BudgetPlanner.xcodeproj -scheme BudgetPlanner -destination 'platform=iOS Simulator,name=iPhone 17 Pro' | xcbeautify`.
5. Сборка зелёная для iPhone 17 Pro.

VERIFY-ONLY (подтвердить наличие, код НЕ менять):
6. `grep -n "aiSpendSection" ios/BudgetPlanner/Features/Management/SettingsView.swift` → AI cost cap display на месте (pre-existing).
7. `grep -rn "ai/chat" ios/BudgetPlanner/Features/AI/` → AIChatAPI.stream на v1.0 /api/v1/ai/chat SSE (pre-existing).
8. `grep -n "accounts\|savings\|planEditor" ios/BudgetPlanner/Features/Management/ManagementView.swift` → ManagementItem ряды для новых доменов зарегистрированы (pre-existing).
</verification>

<success_criteria>
- v06 Settings → секция «Дизайн» показывает 4 выбираемых ряда с checkmark у текущего выбора.
- Tap по V10-теме переключает на V10MainShell с этой темой; tap по «СТАРЫЙ IOS» оставляет MainShell (no-op-stay, пишет "v06").
- Выбор персистит между запусками (@AppStorage).
- ThemeOption helper покрыт unit-тестами (резолв всех raw, unknown→maximalPoster, round-trip).
- Все iOS-тесты + сборка зелёные на iPhone 17 Pro.
- 3 pre-existing области (AI cost cap, AI chat SSE, Management rows) подтверждены present, код не тронут.
</success_criteria>

<output>
After completion, create `.planning/phases/66-settings-ai-polish-v06/66-01-SUMMARY.md`
</output>
