---
phase: 03-plan-template-and-planned-transactions
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - app/api/routes/templates.py
  - app/api/routes/planned.py
  - app/api/router.py
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - PLN-01
  - PLN-02
  - PLN-03

must_haves:
  truths:
    - "Все новые routes под Depends(get_current_user) (router-level, как в Phase 2)"
    - "Domain exceptions из service-слоя мапятся на HTTPException с правильными status codes (404, 400, 422)"
    - "Pydantic body validation возвращает 422 на gt=0 / le=31 / max_length"
    - "Backend test suite для Phase 3 полностью GREEN: tests/test_templates.py, test_planned.py, test_apply_template.py, test_snapshot.py"
    - "Существующие Phase 1+2 тесты не сломаны (regression check)"
  artifacts:
    - path: "app/api/routes/templates.py"
      provides: "templates_router with GET/POST/PATCH/DELETE /template/items + POST /template/snapshot-from-period/{id}"
      exports: ["templates_router"]
    - path: "app/api/routes/planned.py"
      provides: "planned_router with GET/POST /periods/{id}/planned + POST /periods/{id}/apply-template + PATCH/DELETE /planned/{id}"
      exports: ["planned_router"]
    - path: "app/api/router.py"
      provides: "Регистрация templates_router и planned_router в public_router"
      contains: "include_router(templates_router)"
  key_links:
    - from: "app/api/router.py"
      to: "app/api/routes/templates.py, app/api/routes/planned.py"
      via: "public_router.include_router(...)"
      pattern: "include_router\\(templates_router\\)"
    - from: "app/api/routes/templates.py, planned.py"
      to: "app/api/dependencies.py::get_current_user, get_db"
      via: "router-level dependencies=[Depends(get_current_user)]"
      pattern: "dependencies=\\[Depends\\(get_current_user\\)\\]"
    - from: "app/api/routes/templates.py"
      to: "app/services/templates.py + app/services/categories.py::CategoryNotFoundError"
      via: "exception → HTTPException mapping"
      pattern: "TemplateItemNotFoundError|InvalidCategoryError"
    - from: "app/api/routes/planned.py"
      to: "app/services/planned.py"
      via: "exception → HTTPException mapping"
      pattern: "PlannedNotFoundError|PeriodNotFoundError|KindMismatchError|SubscriptionPlannedReadOnlyError"

user_setup: []
---

<objective>
Wave 2: Создать тонкие FastAPI-роуты для templates + planned + apply-template + snapshot. Зарегистрировать новые routers в `app/api/router.py`. После этого backend test suite Phase 3 (4 файла из Wave 0) становится полностью GREEN.

Purpose: Завершить backend Phase 3 — после этой задачи можно вручную дёргать все API endpoints через curl, и frontend (Plan 03-04, 03-05) может разрабатываться против реального API.
Output: 2 новых route файла + 1 модифицированный router.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-plan-template-and-planned-transactions/03-CONTEXT.md
@.planning/phases/03-plan-template-and-planned-transactions/03-RESEARCH.md
@.planning/phases/03-plan-template-and-planned-transactions/03-VALIDATION.md
@app/api/routes/categories.py
@app/api/routes/periods.py
@app/api/routes/onboarding.py
@app/api/router.py
@app/api/schemas/templates.py
@app/api/schemas/planned.py
@app/services/templates.py
@app/services/planned.py
@docs/HLD.md

<interfaces>
<!-- Готовые контракты из Plan 03-02 (services + schemas) -->

Services to consume:
```python
# app.services.templates
from app.services.templates import (
    list_template_items, create_template_item, update_template_item,
    delete_template_item, snapshot_from_period, TemplateItemNotFoundError,
)

# app.services.planned
from app.services.planned import (
    list_planned_for_period, create_manual_planned, update_planned,
    delete_planned, apply_template_to_period,
    PlannedNotFoundError, PeriodNotFoundError, InvalidCategoryError,
    KindMismatchError, SubscriptionPlannedReadOnlyError,
)

# app.services.categories
from app.services.categories import CategoryNotFoundError
```

Schemas to consume:
```python
from app.api.schemas.templates import (
    TemplateItemCreate, TemplateItemUpdate, TemplateItemRead,
    SnapshotFromPeriodResponse,
)
from app.api.schemas.planned import (
    PlannedCreate, PlannedUpdate, PlannedRead,
    ApplyTemplateResponse, PlanSourceStr, KindStr,
)
```

Pre-existing patterns (Phase 2):
- `app/api/routes/categories.py` — thin handler pattern: `cat_svc.create_category(...) → return CategoryRead.model_validate(cat)`.
- Exception → HTTPException mapping: `try: ... except CategoryNotFoundError as exc: raise HTTPException(status_code=404, detail=str(exc)) from exc`.
- Router-level Depends: `categories_router = APIRouter(prefix="/categories", tags=["categories"], dependencies=[Depends(get_current_user)])`.
- Registration: `public_router.include_router(categories_router)` в `app/api/router.py:80`.

Exception → HTTP status mapping для Phase 3:
| Exception | HTTP Status | Detail |
|-----------|------------|--------|
| TemplateItemNotFoundError | 404 | str(exc) |
| PlannedNotFoundError | 404 | str(exc) |
| PeriodNotFoundError | 404 | str(exc) |
| CategoryNotFoundError | 404 | str(exc) |
| InvalidCategoryError | 400 | str(exc) |
| KindMismatchError | 400 | str(exc) |
| SubscriptionPlannedReadOnlyError | 400 | str(exc) |
| Pydantic ValidationError (auto-handled by FastAPI) | 422 | (auto) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Templates router — app/api/routes/templates.py</name>
  <files>app/api/routes/templates.py</files>
  <behavior>
- 5 эндпоинтов: GET /template/items, POST /template/items, PATCH /template/items/{id}, DELETE /template/items/{id}, POST /template/snapshot-from-period/{period_id}.
- Router-level `Depends(get_current_user)`.
- All endpoints передают AsyncSession через `Depends(get_db)`.
- Domain exceptions мапятся на правильные HTTP status (см. таблицу).
- Response model — соответствующий Pydantic Read schema.
- Тесты test_templates.py и test_snapshot.py начинают проходить (с реальным DATABASE_URL).
  </behavior>
  <action>
Создать `app/api/routes/templates.py`. Скопировать структуру из `app/api/routes/categories.py` (header docstring, imports, router definition, 4-5 thin handlers, exception mapping).

```python
"""Plan template REST routes — TPL-01, TPL-02 UI backend, TPL-03 snapshot.

Thin handlers over ``app.services.templates``: routes do request/response
shape mapping (Pydantic <-> ORM) and domain-exception → HTTP status mapping.
All business logic — including the destructive snapshot semantics (D-32),
archived-category guard (D-36), and ordering — lives in the service layer.
"""
from typing import Annotated

from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db
from app.api.schemas.templates import (
    SnapshotFromPeriodResponse, TemplateItemCreate, TemplateItemRead,
    TemplateItemUpdate,
)
from app.services import templates as tpl_svc
from app.services.categories import CategoryNotFoundError
from app.services.planned import InvalidCategoryError, PeriodNotFoundError
from app.services.templates import TemplateItemNotFoundError


templates_router = APIRouter(
    prefix="/template",
    tags=["templates"],
    dependencies=[Depends(get_current_user)],
)


@templates_router.get("/items", response_model=list[TemplateItemRead])
async def list_template_items(
    db: Annotated[AsyncSession, Depends(get_db)],
) -> list[TemplateItemRead]:
    """GET /api/v1/template/items — list all template items (TPL-01)."""
    items = await tpl_svc.list_template_items(db)
    return [TemplateItemRead.model_validate(it) for it in items]


@templates_router.post(
    "/items", response_model=TemplateItemRead, status_code=status.HTTP_200_OK,
)
async def create_template_item(
    body: TemplateItemCreate,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> TemplateItemRead:
    """POST /api/v1/template/items — create a new template item.

    422: Pydantic validation (amount_cents <= 0, day_of_period out of range).
    404: category does not exist.
    400: category exists but is_archived=True.
    """
    try:
        item = await tpl_svc.create_template_item(db, body=body)
    except CategoryNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except InvalidCategoryError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    return TemplateItemRead.model_validate(item)


@templates_router.patch("/items/{item_id}", response_model=TemplateItemRead)
async def update_template_item(
    item_id: int,
    body: TemplateItemUpdate,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> TemplateItemRead:
    """PATCH /api/v1/template/items/{id} — partial update.

    422: Pydantic validation.
    404: template item or new category_id does not exist.
    400: new category is archived.
    """
    try:
        item = await tpl_svc.update_template_item(db, item_id, body)
    except TemplateItemNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except CategoryNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except InvalidCategoryError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    return TemplateItemRead.model_validate(item)


@templates_router.delete("/items/{item_id}", response_model=TemplateItemRead)
async def delete_template_item(
    item_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> TemplateItemRead:
    """DELETE /api/v1/template/items/{id} — hard delete (CLAUDE.md soft-delete only for category)."""
    try:
        item = await tpl_svc.delete_template_item(db, item_id)
    except TemplateItemNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    return TemplateItemRead.model_validate(item)


@templates_router.post(
    "/snapshot-from-period/{period_id}",
    response_model=SnapshotFromPeriodResponse,
    status_code=status.HTTP_200_OK,
)
async def snapshot_from_period(
    period_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> SnapshotFromPeriodResponse:
    """POST /api/v1/template/snapshot-from-period/{period_id} — TPL-03.

    Destructively overwrites PlanTemplate from the period's planned rows.
    Includes source IN (template, manual); EXCLUDES subscription_auto (D-32).

    Returns {template_items: [...], replaced: <prev_count>}.
    404: period does not exist.
    """
    try:
        result = await tpl_svc.snapshot_from_period(db, period_id=period_id)
    except PeriodNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    return SnapshotFromPeriodResponse(
        template_items=[TemplateItemRead.model_validate(it) for it in result["template_items"]],
        replaced=result["replaced"],
    )
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.api.routes.templates import templates_router; routes = [(r.path, list(r.methods or ())) for r in templates_router.routes]; print(routes); assert ('/template/items', ['GET']) in [(p, m) for p, m in routes] or any(p=='/template/items' and 'GET' in m for p,m in routes); print('OK')"</automated>
  </verify>
  <done>
- app/api/routes/templates.py существует и экспортирует templates_router.
- 5 endpoints: GET /items, POST /items, PATCH /items/{id}, DELETE /items/{id}, POST /snapshot-from-period/{period_id}.
- Все exception классы (TemplateItemNotFoundError, CategoryNotFoundError, InvalidCategoryError, PeriodNotFoundError) мапятся на правильные HTTP status.
- Router-level Depends(get_current_user) применён.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Planned router — app/api/routes/planned.py</name>
  <files>app/api/routes/planned.py</files>
  <behavior>
- 5 эндпоинтов: GET /periods/{id}/planned, POST /periods/{id}/planned, POST /periods/{id}/apply-template, PATCH /planned/{id}, DELETE /planned/{id}.
- **Tricky:** один router с двумя группами URL — `/periods/{id}/planned*` и `/planned/{id}`. Обе должны быть под одним router'ом для единого Depends.
  - Использовать ОДИН APIRouter без `prefix` (или с prefix=""), и явно прописывать пути в декораторах: `@planned_router.get("/periods/{period_id}/planned")` и `@planned_router.patch("/planned/{planned_id}")`.
- Router-level `Depends(get_current_user)`.
- Domain exceptions мапятся:
  - PlannedNotFoundError, PeriodNotFoundError, CategoryNotFoundError → 404
  - InvalidCategoryError, KindMismatchError, SubscriptionPlannedReadOnlyError → 400
- Query parameters для GET: `kind: Optional[KindStr] = None`, `category_id: Optional[int] = None`.
- Тесты test_planned.py и test_apply_template.py начинают проходить.
  </behavior>
  <action>
Создать `app/api/routes/planned.py`. Single router с явными path'ами (не prefix-based, потому что эндпоинты разделены).

```python
"""Planned transactions REST routes — PLN-01, PLN-02, PLN-03 backend, TPL-04 apply.

Two groups of endpoints under one router (single Depends(get_current_user)):

Period-scoped:
  GET  /periods/{id}/planned?kind=&category_id=
  POST /periods/{id}/planned
  POST /periods/{id}/apply-template

Item-scoped:
  PATCH  /planned/{id}
  DELETE /planned/{id}

The split mirrors HLD §4.4 + §4.5 (period actions vs single-row CRUD).
"""
from typing import Annotated, Optional

from fastapi import APIRouter, Depends, HTTPException, Query, status
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies import get_current_user, get_db
from app.api.schemas.planned import (
    ApplyTemplateResponse, KindStr, PlannedCreate, PlannedRead, PlannedUpdate,
)
from app.services import planned as plan_svc
from app.services.categories import CategoryNotFoundError
from app.services.planned import (
    InvalidCategoryError, KindMismatchError, PeriodNotFoundError,
    PlannedNotFoundError, SubscriptionPlannedReadOnlyError,
)


planned_router = APIRouter(
    tags=["planned"],
    dependencies=[Depends(get_current_user)],
)


# ---------- Period-scoped endpoints ----------

@planned_router.get(
    "/periods/{period_id}/planned",
    response_model=list[PlannedRead],
)
async def list_planned(
    period_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
    kind: Optional[KindStr] = Query(default=None),
    category_id: Optional[int] = Query(default=None, gt=0),
) -> list[PlannedRead]:
    """GET /api/v1/periods/{period_id}/planned — list planned rows for a period.

    Optional filters: ``kind=expense|income``, ``category_id=<int>``.
    Returns empty list if period does not exist (no 404 — UI uses
    GET /periods/current separately to detect onboarding state).
    """
    rows = await plan_svc.list_planned_for_period(
        db, period_id, kind=kind, category_id=category_id,
    )
    return [PlannedRead.model_validate(r) for r in rows]


@planned_router.post(
    "/periods/{period_id}/planned",
    response_model=PlannedRead,
    status_code=status.HTTP_200_OK,
)
async def create_manual_planned(
    period_id: int,
    body: PlannedCreate,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> PlannedRead:
    """POST /api/v1/periods/{period_id}/planned — create with source=manual.

    422: Pydantic validation.
    404: period or category does not exist.
    400: category archived OR kind mismatch with category.
    """
    try:
        row = await plan_svc.create_manual_planned(db, period_id, body)
    except PeriodNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except CategoryNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except InvalidCategoryError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    except KindMismatchError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    return PlannedRead.model_validate(row)


@planned_router.post(
    "/periods/{period_id}/apply-template",
    response_model=ApplyTemplateResponse,
    status_code=status.HTTP_200_OK,
)
async def apply_template(
    period_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> ApplyTemplateResponse:
    """POST /api/v1/periods/{period_id}/apply-template — TPL-04, PER-05.

    D-31 idempotent: повторный вызов = 200 + created=0 (existing rows returned).
    Phase 5 worker close_period также будет звать этот endpoint.

    404: period does not exist.
    """
    try:
        result = await plan_svc.apply_template_to_period(db, period_id=period_id)
    except PeriodNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    return ApplyTemplateResponse(
        period_id=result["period_id"],
        created=result["created"],
        planned=[PlannedRead.model_validate(r) for r in result["planned"]],
    )


# ---------- Item-scoped endpoints ----------

@planned_router.patch("/planned/{planned_id}", response_model=PlannedRead)
async def update_planned(
    planned_id: int,
    body: PlannedUpdate,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> PlannedRead:
    """PATCH /api/v1/planned/{id} — partial update.

    422: Pydantic.
    404: planned row or new category_id does not exist.
    400: new category archived OR kind mismatch OR row is subscription_auto.
    """
    try:
        row = await plan_svc.update_planned(db, planned_id, body)
    except PlannedNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except CategoryNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except InvalidCategoryError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    except KindMismatchError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    except SubscriptionPlannedReadOnlyError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    return PlannedRead.model_validate(row)


@planned_router.delete("/planned/{planned_id}", response_model=PlannedRead)
async def delete_planned(
    planned_id: int,
    db: Annotated[AsyncSession, Depends(get_db)],
) -> PlannedRead:
    """DELETE /api/v1/planned/{id} — hard delete.

    400: row is subscription_auto (managed by worker, not user).
    """
    try:
        row = await plan_svc.delete_planned(db, planned_id)
    except PlannedNotFoundError as exc:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=str(exc)) from exc
    except SubscriptionPlannedReadOnlyError as exc:
        raise HTTPException(status_code=status.HTTP_400_BAD_REQUEST, detail=str(exc)) from exc
    return PlannedRead.model_validate(row)
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.api.routes.planned import planned_router; routes = [(r.path, sorted(list(r.methods or ()))) for r in planned_router.routes]; print(routes); paths = [p for p, _ in routes]; assert '/periods/{period_id}/planned' in paths and '/periods/{period_id}/apply-template' in paths and '/planned/{planned_id}' in paths, 'missing endpoints'; print('OK')"</automated>
  </verify>
  <done>
- app/api/routes/planned.py существует и экспортирует planned_router.
- 5 endpoints: GET /periods/{id}/planned, POST /periods/{id}/planned, POST /periods/{id}/apply-template, PATCH /planned/{id}, DELETE /planned/{id}.
- Все 5 exception классов мапятся на правильные HTTP status.
- Query params для GET: kind (optional Literal), category_id (optional int gt=0).
- Router-level Depends(get_current_user) применён.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register routers in app/api/router.py + run full test suite</name>
  <files>app/api/router.py</files>
  <behavior>
- Импортировать `templates_router` и `planned_router`.
- Зарегистрировать в `public_router` через `include_router(...)` (после существующих register'ов).
- Не трогать `internal_router` (Phase 3 не использует internal endpoints).
- Полный backend test suite Phase 1+2+3 должен пройти GREEN (включая новые тесты Phase 3, которые требуют DATABASE_URL — обработка как в Phase 2: self-skip без DATABASE_URL).
- Существующие тесты Phase 1+2 не сломаны.
  </behavior>
  <action>
Модифицировать `app/api/router.py`:
1. Добавить импорты после строки 32 (`from app.api.routes.settings import settings_router`):
```python
from app.api.routes.templates import templates_router
from app.api.routes.planned import planned_router
```
2. Добавить регистрацию после строки 83 (`public_router.include_router(settings_router)`):
```python
public_router.include_router(templates_router)
public_router.include_router(planned_router)
```
3. Обновить module docstring: добавить Phase 3 routes секцию (по аналогии с Phase 2 строками 9-14):
```python
Phase 3 routes (added via include_router):
- ``/template/items`` (GET/POST/PATCH/DELETE) — TPL-01, TPL-02
- ``/template/snapshot-from-period/{id}`` (POST) — TPL-03
- ``/periods/{id}/planned`` (GET/POST) — PLN-01, PLN-02
- ``/periods/{id}/apply-template`` (POST) — TPL-04, PER-05
- ``/planned/{id}`` (PATCH/DELETE) — PLN-01, PLN-03 enforcement
```

После модификации запустить полный test suite:
```bash
cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/ -v 2>&1 | tail -60
```

**Если DATABASE_URL не задан в окружении:** DB-backed тесты (Phase 2 + Phase 3) self-skip-ятся. Это OK — означает что мы не сломали Phase 1 (auth, health, internal).

**Если DATABASE_URL задан:** все 4 новых тестовых файла должны быть GREEN. Если есть failures — debug:
- Pydantic 422 mismatch → проверить Field(...) attributes.
- Exception → HTTP status mismatch → проверить try/except mappings в роутах.
- Idempotency test fails → проверить что в `apply_template_to_period` SELECT count правильный (WHERE period_id=:pid AND source=template).
- Snapshot test fails → проверить `WHERE source.in_([template, manual])` (не subscription_auto).

**ВАЖНО:** не редактировать ничего в `app/services/*.py` или `app/api/schemas/*.py` без серьёзной причины — Plan 03-02 уже их зафиксировал. Если тест падает на service-level — это РЕАЛЬНЫЙ bug, нужно вернуться и исправить service. Но если падает на route-level — починить роут.

Если хотя бы один Phase 1/2 тест red'нул из-за Plan 03-03 изменений — это regression, fix immediate.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/ -v 2>&1 | tail -40 | grep -v '^#' | grep -E '(PASSED|FAILED|ERROR|skipped|passed|failed|error)' | head -40</automated>
  </verify>
  <done>
- app/api/router.py импортирует templates_router и planned_router.
- public_router.include_router(templates_router) и public_router.include_router(planned_router) добавлены.
- Module docstring обновлён с Phase 3 routes описанием.
- Полный pytest run:
  - Если DATABASE_URL задан: 0 failures, 0 errors, новые Phase 3 тесты PASSED.
  - Если DATABASE_URL не задан: Phase 3 DB-backed тесты skipped (как Phase 2 в том же режиме); auth + health + internal тесты PASSED; нет regression.
- OpenAPI генерируется без ошибок (можно проверить `uv run python -c "from app.main_api import app; print(len(app.routes))"` — count routes увеличился на 10 относительно Phase 2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caddy edge → API | Phase 1 рутина: TLS termination + path-routing. /api/v1/internal/* блокируется снаружи (но Phase 3 не вводит internal endpoints) |
| Mini App client → API | initData header → get_current_user → OWNER whitelist. Phase 3 routes под router-level Depends. |
| API ← Pydantic body | Pydantic v2 валидирует все типы + custom Field validators (gt=0, le=31, max_length=500). |
| API → Service | Service raises domain exceptions; route maps to HTTPException. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Spoofing / Auth bypass | Кто-то делает API call без initData | mitigate | Router-level Depends(get_current_user) на templates_router и planned_router; тесты test_*_no_init_data_403 покрывают |
| T-03-12 | Tampering | Передача amount_cents как float / string | mitigate | Pydantic int + Field(gt=0) → 422 на нелегитимные типы |
| T-03-13 | Tampering | Cross-period mutation (PATCH /planned/{X} где X из другого периода) | accept | Single-tenant — все периоды принадлежат одному пользователю; нет per-period auth check необходим |
| T-03-14 | Tampering | apply-template race condition (D-31) | accept | Single-tenant + atomic transaction per request; для гарантии сериализации Phase 5 worker использует pg_advisory_lock (отдельная задача Phase 5) |
| T-03-15 | Information Disclosure | Возврат subscription_id в PlannedRead для не-связанной строки | mitigate | subscription_id == NULL для template/manual rows (никаких лишних данных не утекает) |
| T-03-16 | DoS | apply-template на периоде с гигантским шаблоном (10k items) | accept | Single-tenant pet, маловероятно; bulk insert через add_all efficient. Если будет проблема — Phase 5+ можно перейти на bulk_insert_mappings (с потерей id'шников в response — приемлемо) |
| T-03-17 | Tampering | DELETE category, у которой есть planned_transaction → FK constraint violation | mitigate | CAT-02 soft-archive не делает DELETE; FK NOT NULL hold integrity; в Phase 3 DELETE категории не возможен (только archive) |
</threat_model>

<verification>
- pytest на полном suite: 0 failures, 0 errors.
- Если DATABASE_URL задан, новые тесты Phase 3 GREEN (4 файла, 30+ тестов).
- Phase 1+2 тесты не regress'ят.
- API экспортирует все новые endpoints (можно проверить через `app.openapi()` или count routes).
- Manual smoke check (опц.): `curl -X GET http://localhost:8000/api/v1/template/items -H "X-Telegram-Init-Data: dev-mode-stub"` → 200 + [].
</verification>

<success_criteria>
- 2 route-файла созданы (templates.py, planned.py).
- router.py обновлён (импорты + 2 include_router + docstring).
- Backend test suite Phase 3 GREEN (с DATABASE_URL).
- Нет regressions в Phase 1+2.
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-template-and-planned-transactions/03-03-SUMMARY.md`.
</output>
