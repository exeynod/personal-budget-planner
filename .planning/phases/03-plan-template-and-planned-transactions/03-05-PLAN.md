---
phase: 03-plan-template-and-planned-transactions
plan: 05
type: execute
wave: 4
depends_on: [03, 04]
files_modified:
  - frontend/src/api/planned.ts
  - frontend/src/hooks/usePlanned.ts
  - frontend/src/hooks/useCurrentPeriod.ts
  - frontend/src/screens/PlannedScreen.tsx
  - frontend/src/screens/PlannedScreen.module.css
  - frontend/src/App.tsx
autonomous: true
requirements:
  - PLN-01
  - PLN-02
  - PLN-03
  - TPL-03
  - TPL-04

must_haves:
  truths:
    - "PlannedScreen отображает строки плана текущего периода с группировкой kind→category (sketch 005-B)"
    - "Кнопка «Применить шаблон» отображается только когда planned.length === 0; на клик POST /apply-template, refetch, toast"
    - "Кнопка «Перенести план в шаблон» вызывает window.confirm перед POST /snapshot-from-period"
    - "Inline-edit amount + tap-to-open-editor работают (reuse PlanRow из Plan 03-04)"
    - "Subscription_auto строки read-only с badge «🔁 Подписка» (PLN-03 — verified через mock-injection)"
    - "DEV-only helper window.__injectMockPlanned__ существует для PLN-03 verification"
    - "Frontend type-check passes (npx tsc --noEmit exit=0)"
    - "Vite build passes"
  artifacts:
    - path: "frontend/src/api/planned.ts"
      provides: "listPlanned, createPlanned, updatePlanned, deletePlanned, applyTemplate"
    - path: "frontend/src/hooks/useCurrentPeriod.ts"
      provides: "useCurrentPeriod() hook fetching GET /periods/current"
    - path: "frontend/src/hooks/usePlanned.ts"
      provides: "usePlanned(periodId) hook with state + refetch"
    - path: "frontend/src/screens/PlannedScreen.tsx"
      provides: "Group-by-kind→category layout + actions row + apply-template + snapshot + 🔁 mock"
  key_links:
    - from: "frontend/src/screens/PlannedScreen.tsx"
      to: "/api/v1/periods/current, /api/v1/periods/{id}/planned*, /api/v1/planned/{id}, /api/v1/template/snapshot-from-period/{id}"
      via: "useCurrentPeriod + usePlanned + planned.ts client + templates.ts (snapshotFromPeriod)"
      pattern: "apiFetch.*(planned|periods/.*/apply-template|snapshot-from-period)"
    - from: "frontend/src/screens/PlannedScreen.tsx"
      to: "BottomSheet + PlanItemEditor + PlanRow components (Plan 03-04)"
      via: "import от ../components/*"
      pattern: "import.*BottomSheet|PlanItemEditor|PlanRow"

user_setup: []
---

<objective>
Wave 3 (parallel with Plan 03-04, но depends_on 04 для reuse компонентов): построить frontend для PlannedScreen — экран управления планом текущего периода. Включает action-кнопки «Применить шаблон» и «Перенести в шаблон», PLN-03 mock-marker, и dev-helper для injection mock subscription_auto строк.

Purpose: Полностью реализовать PLN-01..03 + TPL-03 (snapshot UI) + TPL-04 (apply UI). После этой задачи пользователь может управлять планом периода через Mini App.
Output: 6 файлов (3 новых — api/hook/screen + 1 css + 2 модифицированных — App.tsx, screens/PlannedScreen).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-plan-template-and-planned-transactions/03-CONTEXT.md
@.planning/phases/03-plan-template-and-planned-transactions/03-RESEARCH.md
@.planning/phases/03-plan-template-and-planned-transactions/03-UI-SPEC.md
@frontend/src/api/client.ts
@frontend/src/api/categories.ts
@frontend/src/api/types.ts
@frontend/src/hooks/useCategories.ts
@frontend/src/screens/CategoriesScreen.tsx
@frontend/src/components/BottomSheet.tsx
@frontend/src/components/PlanItemEditor.tsx
@frontend/src/components/PlanRow.tsx
@frontend/src/screens/TemplateScreen.tsx
@frontend/src/api/templates.ts

<interfaces>
<!-- Backend контракты (готовые из Plan 03-03) -->

REST API:
- `GET /api/v1/periods/current` → `PeriodRead` | 404 (если onboarding не завершён)
- `GET /api/v1/periods/{period_id}/planned?kind=&category_id=` → `PlannedRead[]`
- `POST /api/v1/periods/{period_id}/planned` body=PlannedCreatePayload → `PlannedRead` | 400 | 404 | 422
- `POST /api/v1/periods/{period_id}/apply-template` → `ApplyTemplateResponse` | 404
- `PATCH /api/v1/planned/{id}` body=PlannedUpdatePayload → `PlannedRead` | 400 | 404 | 422
- `DELETE /api/v1/planned/{id}` → `PlannedRead` | 400 (subscription_auto) | 404
- `POST /api/v1/template/snapshot-from-period/{period_id}` → `SnapshotFromPeriodResponse` | 404 (готов из Plan 03-04 templates.ts client)

TS-types определены в Plan 03-04 (frontend/src/api/types.ts):
- PeriodRead — уже существовал в Phase 2.
- PlannedRead, PlannedCreatePayload, PlannedUpdatePayload, ApplyTemplateResponse — добавлены в Plan 03-04.

Reuse from Plan 03-04:
- `BottomSheet`, `PlanItemEditor`, `PlanRow` — готовы.
- `useCategories(false)` — для select в PlanItemEditor.
- `snapshotFromPeriod(periodId)` — из templates.ts.
- `parseRublesToKopecks` / `formatRubles` — внутренние утилиты PlanRow / PlanItemEditor (можно вынести в shared utility, но в Phase 3 — копия).

Pre-existing pattern (Phase 2):
- `useCategories.ts` — паттерн state + refetch + cancelled flag в useEffect.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API client + hooks (planned.ts, useCurrentPeriod.ts, usePlanned.ts)</name>
  <files>frontend/src/api/planned.ts, frontend/src/hooks/useCurrentPeriod.ts, frontend/src/hooks/usePlanned.ts</files>
  <behavior>
- planned.ts экспортирует 5 функций: listPlanned, createPlanned, updatePlanned, deletePlanned, applyTemplate.
- useCurrentPeriod hook делает GET /periods/current; обрабатывает 404 как `{period: null}` (онбординг ещё не завершён).
- usePlanned(periodId) hook делает GET /periods/{periodId}/planned; refetch + loading/error.
- TS check passes.
  </behavior>
  <action>
**1. frontend/src/api/planned.ts**:

```typescript
import { apiFetch } from './client';
import type {
  ApplyTemplateResponse,
  PlannedCreatePayload,
  PlannedRead,
  PlannedUpdatePayload,
} from './types';

/** GET /api/v1/periods/{period_id}/planned?kind=&category_id= */
export async function listPlanned(
  periodId: number,
  filters?: { kind?: 'expense' | 'income'; category_id?: number },
): Promise<PlannedRead[]> {
  const qs = new URLSearchParams();
  if (filters?.kind) qs.set('kind', filters.kind);
  if (filters?.category_id !== undefined) qs.set('category_id', String(filters.category_id));
  const suffix = qs.toString() ? `?${qs.toString()}` : '';
  return apiFetch<PlannedRead[]>(`/periods/${periodId}/planned${suffix}`);
}

/** POST /api/v1/periods/{period_id}/planned (source=manual) */
export async function createPlanned(
  periodId: number,
  payload: PlannedCreatePayload,
): Promise<PlannedRead> {
  return apiFetch<PlannedRead>(`/periods/${periodId}/planned`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

/** PATCH /api/v1/planned/{id} */
export async function updatePlanned(
  id: number,
  patch: PlannedUpdatePayload,
): Promise<PlannedRead> {
  return apiFetch<PlannedRead>(`/planned/${id}`, {
    method: 'PATCH',
    body: JSON.stringify(patch),
  });
}

/** DELETE /api/v1/planned/{id} */
export async function deletePlanned(id: number): Promise<PlannedRead> {
  return apiFetch<PlannedRead>(`/planned/${id}`, { method: 'DELETE' });
}

/** POST /api/v1/periods/{period_id}/apply-template (TPL-04, idempotent) */
export async function applyTemplate(periodId: number): Promise<ApplyTemplateResponse> {
  return apiFetch<ApplyTemplateResponse>(`/periods/${periodId}/apply-template`, {
    method: 'POST',
  });
}
```

**2. frontend/src/hooks/useCurrentPeriod.ts**:

```typescript
import { useCallback, useEffect, useState } from 'react';
import { apiFetch, ApiError } from '../api/client';
import type { PeriodRead } from '../api/types';

export interface UseCurrentPeriodResult {
  period: PeriodRead | null;
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

/**
 * GET /api/v1/periods/current.
 *
 * Returns `period: null` (not error) on 404 — meaning onboarding hasn't
 * completed yet. Caller can render a "complete onboarding first" message.
 */
export function useCurrentPeriod(): UseCurrentPeriodResult {
  const [period, setPeriod] = useState<PeriodRead | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refetch = useCallback(async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await apiFetch<PeriodRead>('/periods/current');
      setPeriod(data);
    } catch (e) {
      if (e instanceof ApiError && e.status === 404) {
        setPeriod(null);
      } else {
        setError(e instanceof Error ? e.message : String(e));
      }
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    setError(null);
    apiFetch<PeriodRead>('/periods/current')
      .then((data) => {
        if (!cancelled) setPeriod(data);
      })
      .catch((e: unknown) => {
        if (cancelled) return;
        if (e instanceof ApiError && e.status === 404) {
          setPeriod(null);
        } else {
          setError(e instanceof Error ? e.message : String(e));
        }
      })
      .finally(() => {
        if (!cancelled) setLoading(false);
      });
    return () => {
      cancelled = true;
    };
  }, []);

  return { period, loading, error, refetch };
}
```

**3. frontend/src/hooks/usePlanned.ts**:

```typescript
import { useCallback, useEffect, useState } from 'react';
import { listPlanned } from '../api/planned';
import type { PlannedRead } from '../api/types';

export interface UsePlannedResult {
  rows: PlannedRead[];
  loading: boolean;
  error: string | null;
  refetch: () => Promise<void>;
}

/**
 * GET /api/v1/periods/{periodId}/planned for the given period.
 *
 * Pass `periodId === null` to skip fetching (e.g., when onboarding incomplete).
 * Re-fetches whenever periodId changes.
 */
export function usePlanned(periodId: number | null): UsePlannedResult {
  const [rows, setRows] = useState<PlannedRead[]>([]);
  const [loading, setLoading] = useState(periodId !== null);
  const [error, setError] = useState<string | null>(null);

  const refetch = useCallback(async () => {
    if (periodId === null) {
      setRows([]);
      setLoading(false);
      return;
    }
    setLoading(true);
    setError(null);
    try {
      const data = await listPlanned(periodId);
      setRows(data);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setLoading(false);
    }
  }, [periodId]);

  useEffect(() => {
    if (periodId === null) {
      setRows([]);
      setLoading(false);
      setError(null);
      return;
    }
    let cancelled = false;
    setLoading(true);
    setError(null);
    listPlanned(periodId)
      .then((data) => {
        if (!cancelled) setRows(data);
      })
      .catch((e: unknown) => {
        if (!cancelled) setError(e instanceof Error ? e.message : String(e));
      })
      .finally(() => {
        if (!cancelled) setLoading(false);
      });
    return () => {
      cancelled = true;
    };
  }, [periodId]);

  return { rows, loading, error, refetch };
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | head -10 && echo "EXIT $?"</automated>
  </verify>
  <done>
- frontend/src/api/planned.ts экспортирует 5 функций.
- useCurrentPeriod корректно обрабатывает 404 как `period: null`.
- usePlanned работает с `periodId: number | null` (skip fetch при null).
- TS check exit=0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlannedScreen + DEV mock-injection helper</name>
  <files>frontend/src/screens/PlannedScreen.tsx, frontend/src/screens/PlannedScreen.module.css, frontend/src/App.tsx</files>
  <behavior>
- PlannedScreen рендерит:
  - Header с back + title + sub-header «Февраль 2026 · 5 фев — 4 мар» (если period exists).
  - Actions row: «Применить шаблон» (conditional на пустой период) + «↻ В шаблон» (всегда).
  - Группировка kind→category (как TemplateScreen).
  - PlanRow для каждой строки; subscription_auto rows получают read-only ветку (handled внутри PlanRow Plan 03-04).
  - BottomSheet с PlanItemEditor для create/edit manual планов.
- Apply-template: на клик POST → toast «Применено N строк» (или «Шаблон пуст — нечего применять» если N==0) → refetch.
- Snapshot: window.confirm → POST → toast «Шаблон обновлён: N строк».
- Empty period (planned пуст): кнопка «Применить шаблон» (если шаблон не пуст) либо placeholder «Шаблон пуст. Перейдите в Шаблон чтобы заполнить» (с link).
- DEV mock-helper window.__injectMockPlanned__ доступен для PLN-03 verification.
- App.tsx обновлён: 'planned' route рендерит PlannedScreen.
- TS check + Vite build passes.
  </behavior>
  <action>
**1. frontend/src/screens/PlannedScreen.tsx**:

```tsx
import { useEffect, useMemo, useState } from 'react';
import {
  applyTemplate,
  createPlanned,
  deletePlanned,
  updatePlanned,
} from '../api/planned';
import { snapshotFromPeriod } from '../api/templates';
import { useCurrentPeriod } from '../hooks/useCurrentPeriod';
import { usePlanned } from '../hooks/usePlanned';
import { useTemplate } from '../hooks/useTemplate';
import { useCategories } from '../hooks/useCategories';
import type { CategoryKind, CategoryRead, PlannedRead } from '../api/types';
import { PlanRow } from '../components/PlanRow';
import { BottomSheet } from '../components/BottomSheet';
import { PlanItemEditor, type EditorSavePayload } from '../components/PlanItemEditor';
import styles from './PlannedScreen.module.css';

export interface PlannedScreenProps {
  onBack: () => void;
  onNavigateToTemplate?: () => void;
}

interface SheetState {
  open: boolean;
  mode: 'create-planned' | 'edit-planned';
  item?: PlannedRead;
  presetCategoryId?: number;
}

interface CategoryGroup {
  kindTitle: 'Расходы' | 'Доходы';
  kind: CategoryKind;
  categories: { category: CategoryRead; rows: PlannedRead[] }[];
}

declare global {
  interface Window {
    __injectMockPlanned__?: (row: PlannedRead) => void;
  }
}

export function PlannedScreen({ onBack, onNavigateToTemplate }: PlannedScreenProps) {
  const { period, loading: perLoading, error: perError } = useCurrentPeriod();
  const { rows: realRows, loading: rowsLoading, error: rowsError, refetch: refetchPlanned } =
    usePlanned(period?.id ?? null);
  const { items: templateItems } = useTemplate();
  const { categories } = useCategories(false);

  const [mockRows, setMockRows] = useState<PlannedRead[]>([]);
  const [sheet, setSheet] = useState<SheetState>({ open: false, mode: 'create-planned' });
  const [toast, setToast] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // PLN-03 DEV-only mock injection helper
  useEffect(() => {
    if (!import.meta.env.DEV) return;
    window.__injectMockPlanned__ = (row: PlannedRead) => {
      setMockRows((prev) => [...prev, row]);
    };
    return () => {
      delete window.__injectMockPlanned__;
    };
  }, []);

  // Combine real + mock rows for display
  const allRows: PlannedRead[] = useMemo(
    () => [...realRows, ...mockRows],
    [realRows, mockRows],
  );

  const groups = useMemo<CategoryGroup[]>(() => {
    const catById = new Map(categories.map((c) => [c.id, c]));
    const byKind: Record<CategoryKind, CategoryGroup> = {
      expense: { kindTitle: 'Расходы', kind: 'expense', categories: [] },
      income: { kindTitle: 'Доходы', kind: 'income', categories: [] },
    };
    const byCat = new Map<number, PlannedRead[]>();
    for (const r of allRows) {
      const arr = byCat.get(r.category_id) ?? [];
      arr.push(r);
      byCat.set(r.category_id, arr);
    }
    for (const [catId, catRows] of byCat.entries()) {
      const cat = catById.get(catId);
      if (!cat) continue;
      catRows.sort((a, b) => {
        const da = a.planned_date ?? '9999-12-31';
        const db = b.planned_date ?? '9999-12-31';
        if (da !== db) return da.localeCompare(db);
        return a.id - b.id;
      });
      byKind[cat.kind].categories.push({ category: cat, rows: catRows });
    }
    for (const k of ['expense', 'income'] as CategoryKind[]) {
      byKind[k].categories.sort(
        (a, b) =>
          a.category.sort_order - b.category.sort_order ||
          a.category.name.localeCompare(b.category.name, 'ru'),
      );
    }
    return [byKind.expense, byKind.income].filter((g) => g.categories.length > 0);
  }, [allRows, categories]);

  const showToast = (msg: string) => {
    setToast(msg);
    window.setTimeout(() => setToast(null), 2200);
  };

  const handleApplyTemplate = async () => {
    if (!period || busy) return;
    setBusy(true);
    setError(null);
    try {
      const result = await applyTemplate(period.id);
      if (result.created === 0 && result.planned.length === 0) {
        showToast('Шаблон пуст — нечего применять');
      } else if (result.created === 0) {
        showToast('Шаблон уже применён');
      } else {
        showToast(`Применено ${result.created} строк`);
      }
      await refetchPlanned();
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setBusy(false);
    }
  };

  const handleSnapshot = async () => {
    if (!period || busy) return;
    if (
      !window.confirm(
        'Перезаписать шаблон текущим планом? Существующий шаблон будет удалён.',
      )
    ) {
      return;
    }
    setBusy(true);
    setError(null);
    try {
      const result = await snapshotFromPeriod(period.id);
      showToast(`Шаблон обновлён: ${result.template_items.length} строк`);
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    } finally {
      setBusy(false);
    }
  };

  const handleAmountSave = async (id: number, newAmountCents: number) => {
    setError(null);
    try {
      await updatePlanned(id, { amount_cents: newAmountCents });
      await refetchPlanned();
    } catch (e) {
      setError(e instanceof Error ? e.message : String(e));
    }
  };

  const handleSave = async (data: EditorSavePayload) => {
    if (!period) return;
    if (sheet.mode === 'create-planned') {
      // Determine kind from category
      const cat = categories.find((c) => c.id === data.category_id);
      if (!cat) throw new Error('Категория не найдена');
      await createPlanned(period.id, {
        kind: cat.kind,
        amount_cents: data.amount_cents,
        description: data.description,
        category_id: data.category_id,
        planned_date: data.planned_date ?? null,
      });
    } else if (sheet.item) {
      const cat = categories.find((c) => c.id === data.category_id);
      if (!cat) throw new Error('Категория не найдена');
      await updatePlanned(sheet.item.id, {
        kind: cat.kind,
        amount_cents: data.amount_cents,
        description: data.description,
        category_id: data.category_id,
        planned_date: data.planned_date ?? null,
      });
    }
    setSheet({ open: false, mode: 'create-planned' });
    await refetchPlanned();
  };

  const handleDelete = async () => {
    if (!sheet.item) return;
    await deletePlanned(sheet.item.id);
    setSheet({ open: false, mode: 'create-planned' });
    await refetchPlanned();
  };

  const openCreate = (categoryId?: number) =>
    setSheet({
      open: true,
      mode: 'create-planned',
      item: undefined,
      presetCategoryId: categoryId,
    });
  const openEdit = (row: PlannedRead) =>
    setSheet({ open: true, mode: 'edit-planned', item: row });

  // Period sub-header copy
  const periodLabel = period
    ? `${new Date(period.period_start).toLocaleDateString('ru-RU', {
        month: 'long',
        year: 'numeric',
      })} · ${new Date(period.period_start).toLocaleDateString('ru-RU', {
        day: '2-digit',
        month: 'short',
      })} — ${new Date(period.period_end).toLocaleDateString('ru-RU', {
        day: '2-digit',
        month: 'short',
      })}`
    : '';

  const isEmptyPlanned = realRows.length === 0;
  const templateIsEmpty = templateItems.length === 0;

  if (perLoading) {
    return <div className={styles.muted}>Загрузка периода…</div>;
  }
  if (perError) {
    return <div className={styles.error}>Ошибка периода: {perError}</div>;
  }
  if (!period) {
    return (
      <div className={styles.root}>
        <header className={styles.header}>
          <button type="button" onClick={onBack} className={styles.backBtn} aria-label="Назад">
            ←
          </button>
          <div className={styles.title}>План</div>
        </header>
        <div className={styles.empty}>Сначала завершите onboarding.</div>
      </div>
    );
  }

  return (
    <div className={styles.root}>
      <header className={styles.header}>
        <button type="button" onClick={onBack} className={styles.backBtn} aria-label="Назад">
          ←
        </button>
        <div className={styles.titleBlock}>
          <div className={styles.title}>План текущего периода</div>
          <div className={styles.subtitle}>{periodLabel}</div>
        </div>
      </header>

      <div className={styles.actionsRow}>
        {isEmptyPlanned && !templateIsEmpty && (
          <button
            type="button"
            onClick={handleApplyTemplate}
            disabled={busy}
            className={styles.primaryAction}
          >
            Применить шаблон
          </button>
        )}
        <button
          type="button"
          onClick={handleSnapshot}
          disabled={busy}
          className={styles.secondaryAction}
        >
          ↻ В шаблон
        </button>
      </div>

      {rowsError && <div className={styles.error}>Ошибка плана: {rowsError}</div>}
      {error && <div className={styles.error}>Ошибка: {error}</div>}

      {!rowsLoading && isEmptyPlanned && templateIsEmpty && (
        <div className={styles.empty}>
          Шаблон пуст. {onNavigateToTemplate ? (
            <button
              type="button"
              onClick={onNavigateToTemplate}
              className={styles.linkBtn}
            >
              Перейдите в «Шаблон»
            </button>
          ) : (
            <>Перейдите в «Шаблон»</>
          )}{' '}
          чтобы заполнить.
        </div>
      )}

      {groups.map((g) => (
        <section key={g.kind} className={styles.kindGroup}>
          <h3 className={styles.kindTitle}>{g.kindTitle}</h3>
          {g.categories.map(({ category, rows }) => (
            <div key={category.id} className={styles.categoryGroup}>
              <h4 className={styles.categoryTitle}>{category.name}</h4>
              {rows.map((row) => (
                <PlanRow
                  key={row.id}
                  item={{ kind: 'planned', row }}
                  category={category}
                  onAmountSave={(cents) => handleAmountSave(row.id, cents)}
                  onOpenEditor={() => openEdit(row)}
                />
              ))}
              <button
                type="button"
                onClick={() => openCreate(category.id)}
                className={styles.addInGroup}
              >
                + Добавить строку в {category.name}
              </button>
            </div>
          ))}
        </section>
      ))}

      {toast && <div className={styles.toast}>{toast}</div>}

      <BottomSheet
        open={sheet.open}
        onClose={() => setSheet({ open: false, mode: 'create-planned' })}
        title={sheet.mode === 'edit-planned' ? 'Изменить строку плана' : 'Новая строка плана'}
      >
        <PlanItemEditor
          mode={sheet.mode}
          initial={
            sheet.item
              ? {
                  category_id: sheet.item.category_id,
                  amount_cents: sheet.item.amount_cents,
                  description: sheet.item.description,
                  planned_date: sheet.item.planned_date,
                }
              : sheet.presetCategoryId
                ? { category_id: sheet.presetCategoryId }
                : undefined
          }
          categories={categories}
          periodBounds={{ start: period.period_start, end: period.period_end }}
          onSave={handleSave}
          onDelete={sheet.mode === 'edit-planned' ? handleDelete : undefined}
          onCancel={() => setSheet({ open: false, mode: 'create-planned' })}
        />
      </BottomSheet>
    </div>
  );
}
```

**PlannedScreen.module.css** (минимальный, переиспользуем токены):

```css
.root {
  min-height: 100vh;
  padding: var(--space-4) var(--space-4) var(--space-12);
  background: var(--color-bg);
  color: var(--color-text);
}

.header {
  display: flex;
  align-items: flex-start;
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}

.backBtn {
  background: transparent;
  color: var(--color-primary);
  border: 0;
  font-size: var(--text-md);
  cursor: pointer;
  padding: 4px 8px;
}

.titleBlock { flex: 1; text-align: center; }

.title {
  font-size: var(--text-lg);
  font-weight: var(--weight-semibold);
}

.subtitle {
  font-size: var(--text-sm);
  color: var(--color-text-muted);
  margin-top: 2px;
}

.actionsRow {
  display: flex;
  gap: var(--space-2);
  margin-bottom: var(--space-4);
  flex-wrap: wrap;
}

.primaryAction {
  background: var(--color-primary);
  color: #fff;
  border: 0;
  border-radius: var(--radius-md);
  padding: 10px 16px;
  font-weight: var(--weight-semibold);
  cursor: pointer;
  flex: 1;
  min-width: 0;
}
.primaryAction:disabled { opacity: 0.5; cursor: not-allowed; }

.secondaryAction {
  background: transparent;
  color: var(--color-text-muted);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 10px 16px;
  cursor: pointer;
}

.muted { color: var(--color-text-muted); padding: var(--space-3); }
.error {
  color: var(--color-danger);
  background: var(--color-danger-soft);
  padding: var(--space-3);
  border-radius: var(--radius-md);
  margin-bottom: var(--space-3);
}
.empty {
  color: var(--color-text-dim);
  text-align: center;
  padding: var(--space-8);
}

.linkBtn {
  background: transparent;
  color: var(--color-primary);
  border: 0;
  text-decoration: underline;
  cursor: pointer;
  padding: 0;
}

.kindGroup { margin-bottom: var(--space-6); }
.kindTitle {
  font-size: var(--text-sm);
  text-transform: uppercase;
  color: var(--color-text-muted);
  margin: var(--space-3) 0 var(--space-2);
  letter-spacing: 0.04em;
}

.categoryGroup { margin-bottom: var(--space-4); }
.categoryTitle {
  font-size: var(--text-base);
  font-weight: var(--weight-medium);
  color: var(--color-text);
  margin: var(--space-2) 0 var(--space-2);
}

.addInGroup {
  background: transparent;
  color: var(--color-primary);
  border: 1px dashed var(--color-border);
  border-radius: var(--radius-md);
  padding: 8px 12px;
  font-size: var(--text-sm);
  cursor: pointer;
  width: 100%;
  text-align: left;
}
.addInGroup:hover { background: var(--color-primary-soft); }

.toast {
  position: fixed;
  left: 50%;
  bottom: calc(var(--safe-bottom) + 16px);
  transform: translateX(-50%);
  background: var(--color-success-soft);
  color: var(--color-success);
  border: 1px solid var(--color-success);
  border-radius: var(--radius-full);
  padding: 8px 16px;
  font-size: var(--text-sm);
  z-index: 50;
}
```

**2. Обновить frontend/src/App.tsx** — заменить placeholder для 'planned' на реальный PlannedScreen:

```tsx
// ... импорт
import { PlannedScreen } from './screens/PlannedScreen';

// ... в App component, заменить placeholder ветку:
  if (screen === 'planned') {
    return (
      <PlannedScreen
        onBack={() => setOverrideScreen('home')}
        onNavigateToTemplate={() => setOverrideScreen('template')}
      />
    );
  }
```

После изменений проверить:
```bash
cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit && npm run build
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner/frontend && npx tsc --noEmit 2>&1 | tail -10 && echo "TS-EXIT $?" && npm run build 2>&1 | tail -10 && echo "BUILD-EXIT $?"</automated>
  </verify>
  <done>
- PlannedScreen.tsx + .module.css созданы.
- PlannedScreen использует BottomSheet, PlanItemEditor, PlanRow из Plan 03-04.
- Action кнопка «Применить шаблон» отображается conditional на `realRows.length === 0 && templateItems.length > 0`.
- Action кнопка «↻ В шаблон» всегда отображается; при клике window.confirm + POST snapshot.
- DEV-only `window.__injectMockPlanned__` helper устанавливается в useEffect (только в DEV).
- Mock rows мерджатся с real в `allRows` для отображения.
- App.tsx: 'planned' route рендерит PlannedScreen вместо placeholder.
- TS check exit=0, Vite build exit=0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mini App ↔ API | apiFetch + initData header |
| DEV-only mock-injection helper | Доступен только в DEV (import.meta.env.DEV); tree-shaken в prod |
| BottomSheet → Telegram BackButton | Lifecycle managed (Plan 03-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-23 | Tampering | window.__injectMockPlanned__ доступен в prod build | mitigate | Условие `import.meta.env.DEV` — Vite tree-shaking уберёт код в prod build (verify в bundle inspection) |
| T-03-24 | Information Disclosure | Mock-row утекает в реальное API call | mitigate | Mock-row хранятся в local state mockRows, не отправляются на backend (только real rows через usePlanned/createPlanned/updatePlanned/deletePlanned) |
| T-03-25 | UX | User кликает Apply дважды быстро | mitigate | busy state блокирует повторные клики; backend D-31 idempotency защищает от логического дубля |
| T-03-26 | UX | Snapshot затирает шаблон случайно | mitigate | window.confirm в handleSnapshot |
| T-03-27 | Tampering | User обходит UI и делает PATCH /planned/{id} напрямую (subscription_auto) | mitigate | Server-side guard SubscriptionPlannedReadOnlyError → 400 (Plan 03-02 уже реализован) |
</threat_model>

<verification>
- TS check exit=0, Vite build exit=0.
- Manual UI verification — Plan 03-06 final checkpoint.
</verification>

<success_criteria>
- 6 файлов созданы / модифицированы.
- PlannedScreen полностью функционирует против реального backend.
- All acceptance criteria из 03-UI-SPEC §Acceptance.2 готовы к manual verification (Plan 03-06).
- DEV-only mock-injection готов для PLN-03 verification.
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-template-and-planned-transactions/03-05-SUMMARY.md`.
</output>
