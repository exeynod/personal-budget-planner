---
phase: 03-plan-template-and-planned-transactions
plan: 06
type: execute
wave: 5
depends_on: [03, 04, 05]
files_modified: []
autonomous: false
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - PLN-01
  - PLN-02
  - PLN-03

must_haves:
  truths:
    - "Полный backend test suite GREEN (Phase 1+2+3): uv run pytest tests/ -v без failures"
    - "Frontend type-check + Vite build GREEN: npx tsc --noEmit && npm run build"
    - "Все 9 acceptance шагов из 03-UI-SPEC §Acceptance.3 выполнены вручную и подтверждены"
    - "PLN-03 mock badge визуально проверен через DevTools console injection"
    - "Phase 1+2 функциональность не сломана (regression check): onboarding, categories CRUD, settings — всё работает"
  artifacts:
    - path: ".planning/phases/03-plan-template-and-planned-transactions/03-06-SUMMARY.md"
      provides: "Финальная сводка фазы — какие commits, какие требования закрыты"
  key_links: []

user_setup: []
---

<objective>
Wave 4: финальная сборка фазы. Запустить полный test suite, выполнить E2E acceptance walkthrough в реальном Telegram Mini App (или browser dev), зафиксировать verification результаты.

Purpose: Гарантировать что фаза завершена «сверху донизу»: backend, frontend, integration, UX-flows.
Output: Согласованное подтверждение того, что Phase 3 готова к merge / переходу в Phase 4. Файлов не модифицирует — только запускает проверки и собирает SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-plan-template-and-planned-transactions/03-CONTEXT.md
@.planning/phases/03-plan-template-and-planned-transactions/03-VALIDATION.md
@.planning/phases/03-plan-template-and-planned-transactions/03-UI-SPEC.md
@.planning/phases/02-domain-foundation-and-onboarding/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite + frontend build</name>
  <files></files>
  <action>
1. **Backend full test suite:**
   ```bash
   cd /Users/exy/pet_projects/tg-budget-planner
   uv run pytest tests/ -v 2>&1 | tee /tmp/phase3-test-output.txt | tail -80
   ```
   Если DATABASE_URL не задан — Phase 2/3 DB-backed тесты self-skip-нутся (как в Phase 2 verification). Это ОК; авторизация/health/internal тесты Phase 1 должны пройти.

   Если DATABASE_URL задан и есть docker-compose db (`docker compose up -d db`):
   - All Phase 1 tests: PASSED
   - All Phase 2 tests: PASSED
   - All Phase 3 tests (test_templates, test_planned, test_apply_template, test_snapshot): PASSED
   - 0 failures, 0 errors
   - Если есть failure — ОБЯЗАТЕЛЬНО пометить как blocker; не продолжать.

2. **Frontend type-check:**
   ```bash
   cd /Users/exy/pet_projects/tg-budget-planner/frontend
   npx tsc --noEmit 2>&1 | tee /tmp/phase3-tsc-output.txt
   echo "EXIT $?"
   ```
   Должен быть exit=0 без ошибок.

3. **Frontend Vite build:**
   ```bash
   cd /Users/exy/pet_projects/tg-budget-planner/frontend
   npm run build 2>&1 | tee /tmp/phase3-build-output.txt | tail -20
   echo "EXIT $?"
   ```
   Должен быть exit=0; bundle создан в `frontend/dist/`.

4. **Production build sanity (опционально):** убедиться что DEV-only `window.__injectMockPlanned__` НЕ попал в prod bundle:
   ```bash
   grep -r "__injectMockPlanned__" frontend/dist/ 2>&1 | head -5
   ```
   Если grep не находит ничего — tree-shaking сработал, mock-helper не утёк в prod. Если находит — fix необходим (проверить условие `import.meta.env.DEV`). Это soft-blocker (важно, но не критично — сам helper безопасен в prod, просто не должен быть там).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run pytest tests/ --tb=no -q 2>&1 | tail -5 && echo "---" && cd frontend && npx tsc --noEmit 2>&1 | tail -3 && echo "TS-EXIT $?"</automated>
  </verify>
  <done>
- Backend pytest run завершён; либо все тесты PASSED, либо DB-backed тесты skipped (без DATABASE_URL); 0 failures, 0 errors.
- Frontend tsc → exit=0.
- Frontend npm run build → exit=0.
- Mock helper НЕ в prod bundle (grep пустой).
- Output трёх команд сохранён для SUMMARY.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 3 backend (apps/services/templates.py, planned.py + routes/templates.py, planned.py) + frontend (TemplateScreen, PlannedScreen, BottomSheet, PlanItemEditor, PlanRow + hooks/api clients).

Все 7 requirements фазы реализованы:
- TPL-01: PlanTemplate структура + items
- TPL-02: CRUD UI с group-by-category + inline-edit + bottom-sheet
- TPL-03: snapshot-from-period (destructive overwrite, exclude subscription_auto)
- TPL-04: apply-template idempotent
- PLN-01: CRUD plan-строк периода
- PLN-02: source enum (template / manual / subscription_auto)
- PLN-03: «🔁 from subscription» visual marker (готов на mock-данных)

Plus косвенно покрыт PER-05 (deferred from Phase 2): apply-template endpoint, который Phase 5 worker будет звать при создании нового периода.
  </what-built>
  <how-to-verify>
**Среда:** docker-compose стек поднят (`docker compose up -d`), БД с данными после Phase 2 onboarding (категории, активный период), Mini App открывается в Telegram (или в browser dev на http://localhost:5173 / Caddy domain) с DEV_MODE=true.

**Acceptance.1 — TemplateScreen (TPL-01, TPL-02):**
1. Открыть Mini App → Home → tap «Шаблон» → переход на TemplateScreen.
2. (Empty случай) Если шаблон пуст: видим placeholder «Шаблон пуст. Добавьте первую строку.» + кнопка `+ Строка`.
3. Создать строку:
   - Tap «+ Строка» (header) → открывается BottomSheet «Новая строка шаблона».
   - Выбрать категорию «Продукты», ввести сумму «15000», описание «Закупка», день «5» → tap «Сохранить».
   - Sheet закрывается, в группе «Расходы » → «Продукты» появляется строка с amount «15 000 ₽», description «Закупка», badge «День 5».
4. Inline-edit amount:
   - Tap на «15 000 ₽» → появляется input с pre-filled, autofocus.
   - Изменить на «20000» → Enter.
   - Строка обновляется на «20 000 ₽».
   - Повторить с Esc → значение не меняется.
5. Полный редактор:
   - Tap на description / day badge → открывается BottomSheet «Изменить строку шаблона».
   - Изменить категорию на «Дом», описание на «Аренда» → tap «Сохранить».
   - Строка переезжает в группу «Дом».
6. Удаление:
   - Открыть BottomSheet edit → tap «Удалить» → window.confirm → confirm.
   - Sheet закрывается, строка исчезает.
7. Telegram BackButton (только в Telegram, не в browser):
   - Открыть BottomSheet → tap Telegram BackButton → sheet закрывается.

**Acceptance.2 — PlannedScreen (PLN-01, PLN-03, TPL-03, TPL-04):**
1. Home → tap «План» → переход на PlannedScreen.
2. (Empty period) Видим actions row с кнопкой «Применить шаблон» (active, если шаблон не пуст).
   - Если и шаблон пуст — placeholder «Шаблон пуст. Перейдите в «Шаблон»…» + кликабельный link.
3. Apply-template:
   - Tap «Применить шаблон» → toast «Применено N строк» (зелёный).
   - На экране появляются N строк (с теми же amount/category/description что в шаблоне).
   - Кнопка «Применить шаблон» исчезла из actions row.
4. Idempotency проверка (через DevTools console):
   - Открыть DevTools → console → выполнить:
     ```js
     fetch('/api/v1/periods/<period_id>/apply-template', {
       method: 'POST',
       headers: {'X-Telegram-Init-Data': 'dev-mode-stub'}
     }).then(r => r.json()).then(console.log)
     ```
   - Response: `{period_id: <id>, created: 0, planned: [...same N items...]}`.
   - На экране ничего не дублируется (если рефетчилось — то же количество).
5. Snapshot:
   - Изменить amount у одной строки inline-edit, добавить ещё одну manual через BottomSheet.
   - Tap «↻ В шаблон» → window.confirm «Перезаписать шаблон…» → confirm → toast «Шаблон обновлён: M строк».
   - Перейти в Шаблон (Home → Шаблон) → видим M строк с обновлёнными значениями.
6. PLN-03 mock badge:
   - Home → План → DevTools console → выполнить:
     ```js
     window.__injectMockPlanned__({
       id: -1,
       period_id: 1,
       kind: 'expense',
       amount_cents: 99000,
       description: 'YouTube Premium',
       category_id: 10,  // или любой существующий expense category_id
       planned_date: '2026-02-10',
       source: 'subscription_auto',
       subscription_id: 1
     })
     ```
   - В категории появляется строка `990 ₽ · YouTube Premium · 🔁 Подписка · [10 фев]`.
   - Опасити визуально снижен (read-only state).
   - Tap на amount — нет inline edit; tap на description — нет open BottomSheet.
7. Refresh страницы → mock-строка исчезает (только real data).

**Acceptance.3 — E2E flow (полный walkthrough из UI-SPEC):**
- Шаги 1-9 из 03-UI-SPEC.md §Acceptance.3 выполнены успешно.

**Regression checks (Phase 1+2 не сломаны):**
- Onboarding (если БД пустая, выполнить заново): 4 секции работают, MainButton enable/disable.
- CategoriesScreen: создать/переименовать/архивировать категорию.
- SettingsScreen: изменить cycle_start_day.
- /me возвращает onboarded user.

**Логи и ошибки:**
- В DevTools console — нет красных ошибок (warnings ОК).
- В backend logs (`docker compose logs api` или `uv run uvicorn ...`) — нет 500/тейлтрейсов.
  </how-to-verify>
  <resume-signal>Type "approved" если все acceptance шаги пройдены. Если есть проблемы — опишите детально (какой шаг, что вместо ожидаемого), будет создан Plan 03-07 для gap closure через `/gsd-plan-phase --gaps`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write 03-06-SUMMARY.md with phase-final state</name>
  <files>.planning/phases/03-plan-template-and-planned-transactions/03-06-SUMMARY.md</files>
  <action>
Создать `.planning/phases/03-plan-template-and-planned-transactions/03-06-SUMMARY.md` со структурой:

```markdown
# Phase 3 Plan 06 — Final Integration & UI Checkpoint Summary

**Phase:** 03-plan-template-and-planned-transactions
**Wave:** 4 (final)
**Status:** Complete (after human checkpoint)

## What was verified

### Backend
- pytest output (Phase 1+2+3 full suite): <PASTE OR SUMMARIZE LAST 20 LINES>
- Test failures: 0
- Test errors: 0
- DB-backed tests: <PASSED | skipped (no DATABASE_URL)>

### Frontend
- TypeScript check: exit=0
- Vite build: exit=0, bundle in frontend/dist/
- Production bundle inspection: __injectMockPlanned__ NOT found in dist/ (DEV-only tree-shaken)

### Manual UI Acceptance (checkpoint:human-verify)
| Acceptance Section | Status |
|---|---|
| 1. TemplateScreen CRUD + inline-edit + BottomSheet (TPL-01, TPL-02) | <approved | issues> |
| 2. PlannedScreen + apply-template + snapshot + PLN-03 mock (PLN-01..03, TPL-03..04) | <approved | issues> |
| 3. E2E flow (9 steps) | <approved | issues> |
| Regression: onboarding / categories / settings | <approved | issues> |

### Issues encountered (если были)
- (заполняется после checkpoint)

## Requirements coverage

| Requirement | Status | Evidence |
|---|---|---|
| TPL-01 | ✓ | PlanTemplateItem CRUD via /api/v1/template/items + UI on TemplateScreen |
| TPL-02 | ✓ | Group-by-category UI + inline-edit + BottomSheet (sketch 005-B) |
| TPL-03 | ✓ | POST /template/snapshot-from-period; UI button с window.confirm; D-32 excludes subscription_auto |
| TPL-04 | ✓ | POST /periods/{id}/apply-template idempotent (D-31 source-check); UI button conditional на пустой период; повторный POST = created=0 |
| PLN-01 | ✓ | CRUD planned via /api/v1/periods/{id}/planned + /api/v1/planned/{id}; UI on PlannedScreen |
| PLN-02 | ✓ | source enum (template/manual/subscription_auto) выставляется correctly: manual для POST, template для apply-template, subscription_auto reserved для Phase 6 worker |
| PLN-03 | ✓ (готов structurно) | UI визуально готов: PlanRow ветка для source='subscription_auto' рендерит badge «🔁 Подписка», read-only; verified via window.__injectMockPlanned__ на mock-данных. Real subscription rows появятся в Phase 6. |
| PER-05 (deferred from Phase 2) | ✓ (structurно) | apply-template endpoint готов; Phase 5 worker close_period будет его звать |

## Files created/modified в Phase 3

### New backend files
- app/api/schemas/templates.py (Plan 03-02)
- app/api/schemas/planned.py (Plan 03-02)
- app/services/templates.py (Plan 03-02)
- app/services/planned.py (Plan 03-02)
- app/api/routes/templates.py (Plan 03-03)
- app/api/routes/planned.py (Plan 03-03)
- tests/test_templates.py (Plan 03-01)
- tests/test_planned.py (Plan 03-01)
- tests/test_apply_template.py (Plan 03-01)
- tests/test_snapshot.py (Plan 03-01)

### Modified backend files
- app/api/router.py (Plan 03-03 — register new routers)

### New frontend files
- frontend/src/api/templates.ts (Plan 03-04)
- frontend/src/api/planned.ts (Plan 03-05)
- frontend/src/hooks/useTemplate.ts (Plan 03-04)
- frontend/src/hooks/useCurrentPeriod.ts (Plan 03-05)
- frontend/src/hooks/usePlanned.ts (Plan 03-05)
- frontend/src/components/BottomSheet.tsx + .module.css (Plan 03-04)
- frontend/src/components/PlanItemEditor.tsx + .module.css (Plan 03-04)
- frontend/src/components/PlanRow.tsx + .module.css (Plan 03-04)
- frontend/src/screens/TemplateScreen.tsx + .module.css (Plan 03-04)
- frontend/src/screens/PlannedScreen.tsx + .module.css (Plan 03-05)

### Modified frontend files
- frontend/src/api/types.ts (Plan 03-04 — добавлены TS типы)
- frontend/src/screens/HomeScreen.tsx (Plan 03-04 — 4 nav кнопки)
- frontend/src/App.tsx (Plan 03-04, 03-05 — Screen union + 'template'/'planned' routing)

## Decisions implemented

| Decision | File | Notes |
|---|---|---|
| D-30 No migrations | (none) | Schema из Phase 1 неизменна |
| D-31 Idempotency via source-check | app/services/planned.py::apply_template_to_period | SELECT count WHERE source=template; if > 0 return existing |
| D-32 Snapshot excludes subscription_auto | app/services/templates.py::snapshot_from_period | WHERE source.in_([template, manual]) |
| D-33..D-35 Routers/services/schemas split | многочисленные файлы | Single-responsibility per HLD §4.3, 4.4, 4.5 |
| D-36 Domain validation (archived/kind/amount) | app/services/planned.py + templates.py | _ensure_category_active + KindMismatchError |
| D-37 PLN-03 visual + read-only guard | PlanRow + planned service SubscriptionPlannedReadOnlyError | Verified via mock-injection |
| D-38..D-39 Apply/snapshot UI conditional + confirm | PlannedScreen | conditional rendering + window.confirm |
| D-40 BottomSheet CSS-only | BottomSheet.tsx | Без библиотек, Telegram BackButton lifecycle |
| D-41..D-42 Screens layout | TemplateScreen + PlannedScreen | sketch 005-B group-by-kind + actions row |
| D-43 Reuse Phase 2 patterns | useCategories, apiFetch, CategoryRow inline-edit | Подтверждено |
| D-44 Wave 0 RED tests | tests/test_*.py | 4 файла, 30+ тестов |

## Known limitations / Phase 4+ TODOs

- PLN-03 end-to-end (с реальными subscription rows в БД) — отложено до Phase 6.
- Worker `close_period` вызов apply-template — Phase 5 (PER-04).
- Drag-n-drop reorder, bulk-edit, CSV import — out of scope MVP.
- BottomSheet будет переиспользован в Phase 4 для add-actual-transaction (sketch 002-B).

## Phase Status

✓ READY FOR PHASE 4 (Actual Transactions & Bot Commands)

---
*Generated: <DATE>*
*Verifier: Claude (executor of Plan 03-06)*
```

Заполнить плейсхолдеры (`<PASTE OR SUMMARIZE>`, `<approved | issues>`, `<DATE>`) на основе результатов tasks 1-2.
  </action>
  <verify>
    <automated>test -f /Users/exy/pet_projects/tg-budget-planner/.planning/phases/03-plan-template-and-planned-transactions/03-06-SUMMARY.md && echo "OK"</automated>
  </verify>
  <done>
- 03-06-SUMMARY.md создан, содержит все секции из шаблона.
- Заполнены результаты pytest / tsc / build.
- Записан результат human checkpoint (approved или issues).
- Requirements coverage table заполнена.
- Files created/modified — точный список.
- Phase status — READY FOR PHASE 4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment ↔ Production code | DEV_MODE=true в test/dev; prod build не имеет mock-helper |
| Manual checkpoint ↔ Automated tests | Human подтверждает то, что автотесты не покрывают (visual UI, Telegram lifecycle, E2E user journey) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-28 | Process | Human checkpoint пропущен | mitigate | gate="blocking" на checkpoint task; нельзя продолжать без resume-signal |
| T-03-29 | Process | Phase помечена done без regression check | mitigate | Acceptance чек включает regression: onboarding/categories/settings проверяются |
</threat_model>

<verification>
- pytest полный suite проходит без failures.
- frontend npm run build проходит без ошибок.
- Human checkpoint completed (approved или explicit gap-list для Plan 03-07).
- 03-06-SUMMARY.md существует с заполненными секциями.
</verification>

<success_criteria>
- Все артефакты Phase 3 верифицированы (backend tests + frontend build + manual UI flows).
- 7 requirements фазы (TPL-01..04, PLN-01..03) подтверждены или (если есть гэп) — задокументированы для gap closure.
- 03-06-SUMMARY.md создан и фиксирует phase-final state.
- Phase 3 готова к merge / переходу в Phase 4.
</success_criteria>

<output>
After completion, the phase is fully verified.
SUMMARY file: .planning/phases/03-plan-template-and-planned-transactions/03-06-SUMMARY.md.

Если acceptance не пройден — пользователь запускает `/gsd-plan-phase 03 --gaps` для создания Plan 03-07 (gap closure).
Если всё approved — пользователь запускает `/gsd-transition` или переходит к Phase 4.
</output>
