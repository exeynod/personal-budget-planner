---
phase: 03-plan-template-and-planned-transactions
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - app/api/schemas/templates.py
  - app/api/schemas/planned.py
  - app/services/templates.py
  - app/services/planned.py
autonomous: true
requirements:
  - TPL-01
  - TPL-03
  - TPL-04
  - PLN-01
  - PLN-02
  - PLN-03

must_haves:
  truths:
    - "Pydantic-схемы templates.py и planned.py определены с валидаторами amount/day_of_period (per D-36)"
    - "Service-слой templates.py содержит CRUD + snapshot_from_period с D-32 семантикой (включает template+manual, исключает subscription_auto)"
    - "Service-слой planned.py содержит CRUD + apply_template_to_period (D-31 idempotent) + read-only guard для subscription_auto (D-37)"
    - "Все сервисные функции — async, без FastAPI imports (D-35 carryover Phase 2)"
    - "Domain exceptions определены: TemplateItemNotFoundError, PlannedNotFoundError, PeriodNotFoundError, InvalidCategoryError, KindMismatchError, SubscriptionPlannedReadOnlyError"
  artifacts:
    - path: "app/api/schemas/templates.py"
      provides: "TemplateItemCreate/Update/Read + SnapshotFromPeriodResponse"
      exports: ["TemplateItemCreate", "TemplateItemUpdate", "TemplateItemRead", "SnapshotFromPeriodResponse"]
    - path: "app/api/schemas/planned.py"
      provides: "PlannedCreate/Update/Read + ApplyTemplateResponse + PlanSourceStr/KindStr literal aliases"
      exports: ["PlannedCreate", "PlannedUpdate", "PlannedRead", "ApplyTemplateResponse", "PlanSourceStr", "KindStr"]
    - path: "app/services/templates.py"
      provides: "list_template_items, create_template_item, update_template_item, delete_template_item, snapshot_from_period"
      exports: ["list_template_items", "create_template_item", "update_template_item", "delete_template_item", "snapshot_from_period", "TemplateItemNotFoundError"]
    - path: "app/services/planned.py"
      provides: "list_planned_for_period, create_manual_planned, update_planned, delete_planned, apply_template_to_period"
      exports: ["list_planned_for_period", "create_manual_planned", "update_planned", "delete_planned", "apply_template_to_period", "PlannedNotFoundError", "PeriodNotFoundError", "InvalidCategoryError", "KindMismatchError", "SubscriptionPlannedReadOnlyError"]
  key_links:
    - from: "app/services/planned.py::apply_template_to_period"
      to: "app/db/models.py::PlanTemplateItem, PlannedTransaction, BudgetPeriod, Category, PlanSource"
      via: "selectinload(PlanTemplateItem.category) + db.add_all + flush"
      pattern: "selectinload.*PlanTemplateItem.category"
    - from: "app/services/templates.py::snapshot_from_period"
      to: "app/db/models.py::PlannedTransaction, PlanTemplateItem"
      via: "delete + add_all atomic в одной session"
      pattern: "delete\\(PlanTemplateItem\\)"
    - from: "app/services/planned.py::create_manual_planned, update_planned"
      to: "app.services.categories::get_or_404 + Category.is_archived check"
      via: "explicit lookup перед insert/update"
      pattern: "is_archived"

user_setup: []
---

<objective>
Wave 1: создать Pydantic-схемы и service-слой для templates и planned. Это backend-bottom-half фазы — pure Python, без FastAPI dependencies, polностью покрывается RED-тестами Wave 0.

Purpose: Изолировать бизнес-логику от HTTP-контракта. Phase 5 worker сможет переиспользовать `apply_template_to_period` без HTTP-роутинга. Domain exceptions — единый язык между service и route layer.
Output: 4 файла в `app/api/schemas/` и `app/services/` (по 2 в каждом каталоге).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-plan-template-and-planned-transactions/03-CONTEXT.md
@.planning/phases/03-plan-template-and-planned-transactions/03-RESEARCH.md
@.planning/phases/03-plan-template-and-planned-transactions/03-VALIDATION.md
@app/db/models.py
@app/services/categories.py
@app/services/periods.py
@app/services/onboarding.py
@app/api/schemas/categories.py
@docs/HLD.md

<interfaces>
<!-- Контракты, которые route layer (Plan 03-03) и тесты (Plan 03-01) предполагают. -->

Domain exceptions (соглашение Phase 2 carryover):
```python
# app/services/templates.py
class TemplateItemNotFoundError(Exception):
    def __init__(self, item_id: int) -> None:
        self.item_id = item_id
        super().__init__(f"Template item {item_id} not found")

# app/services/planned.py
class PlannedNotFoundError(Exception):
    def __init__(self, planned_id: int) -> None:
        self.planned_id = planned_id
        super().__init__(f"Planned transaction {planned_id} not found")

class PeriodNotFoundError(Exception):
    def __init__(self, period_id: int) -> None:
        self.period_id = period_id
        super().__init__(f"Budget period {period_id} not found")

class InvalidCategoryError(Exception):
    def __init__(self, category_id: int, reason: str) -> None:
        self.category_id = category_id
        self.reason = reason
        super().__init__(f"Category {category_id}: {reason}")

class KindMismatchError(Exception):
    def __init__(self, plan_kind: str, category_kind: str) -> None:
        self.plan_kind = plan_kind
        self.category_kind = category_kind
        super().__init__(f"Plan kind '{plan_kind}' does not match category kind '{category_kind}'")

class SubscriptionPlannedReadOnlyError(Exception):
    def __init__(self, planned_id: int) -> None:
        self.planned_id = planned_id
        super().__init__(f"Planned transaction {planned_id} is managed by a subscription and cannot be modified directly")
```

Существующие сервисы (используем):
- `app.services.categories.get_or_404(db, category_id) -> Category` — кидает CategoryNotFoundError если нет.
- `app.services.periods` — пока не нужен напрямую (получаем BudgetPeriod через `db.get(BudgetPeriod, period_id)`).

ORM модели (читаем, не модифицируем):
- `PlanTemplateItem`: id, category_id, amount_cents, description, day_of_period, sort_order, category (relationship).
- `PlannedTransaction`: id, period_id, kind, amount_cents, description, category_id, planned_date, source, subscription_id, original_charge_date, period (rel), category (rel).
- `BudgetPeriod`: id, period_start, period_end, ...
- `Category`: id, name, kind (CategoryKind enum), is_archived.
- `PlanSource` enum: template/manual/subscription_auto.
- `CategoryKind` enum: expense/income.

Pre-existing service patterns (Phase 2):
- `app/services/categories.py:50-62 list_categories` — `select(...).where(...).order_by(...)`, `.execute()`, `.scalars().all()`.
- `app/services/categories.py:65-82 create_category` — `db.add(cat); await db.flush(); await db.refresh(cat); return cat`.
- `app/services/categories.py:99-111 update_category` — `cat = await get_or_404; data = patch.model_dump(exclude_unset=True); for k,v in data.items(): setattr(cat, k, v); await db.flush(); await db.refresh(cat)`.
- `app/services/categories.py:114-120 archive_category` — pure async, no FastAPI imports.

Pre-existing schema patterns (Phase 2):
- `app/api/schemas/categories.py` — Literal types, `Field(min_length=1, max_length=200)`, `ConfigDict(from_attributes=True)`, all Update fields Optional.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic schemas — templates.py and planned.py</name>
  <files>app/api/schemas/templates.py, app/api/schemas/planned.py</files>
  <behavior>
- Все обязательные поля из RESEARCH.md «Code Examples (Pydantic schemas)» определены.
- amount_cents валидируется `Field(gt=0)` → 422 на 0/negative.
- day_of_period валидируется `Field(default=None, ge=1, le=31)`.
- description ограничено `max_length=500`.
- category_id валидируется `Field(gt=0)`.
- ConfigDict(from_attributes=True) для всех Read schemas.
- Update schemas — все поля Optional с defaults None.
- PlanSourceStr и KindStr — Literal type aliases (per D-34, source — `"template" | "manual" | "subscription_auto"`, kind — `"expense" | "income"`).
- Запуск тестов test_templates.py / test_planned.py перестаёт падать на ImportError для схем (но всё ещё падает на сервис/роут imports).
  </behavior>
  <action>
**app/api/schemas/templates.py** — следовать паттерну `app/api/schemas/categories.py:1-46`. Module docstring объясняет назначение. Содержит:

```python
"""Pydantic schemas for /api/v1/template/* endpoints (TPL-01, TPL-03)."""
from typing import Optional
from pydantic import BaseModel, ConfigDict, Field


class TemplateItemCreate(BaseModel):
    """POST /template/items request body."""
    category_id: int = Field(gt=0)
    amount_cents: int = Field(gt=0, description="Amount in kopecks; must be > 0")
    description: Optional[str] = Field(default=None, max_length=500)
    day_of_period: Optional[int] = Field(default=None, ge=1, le=31)
    sort_order: int = Field(default=0, ge=0)


class TemplateItemUpdate(BaseModel):
    """PATCH /template/items/{id} request body — all fields optional."""
    category_id: Optional[int] = Field(default=None, gt=0)
    amount_cents: Optional[int] = Field(default=None, gt=0)
    description: Optional[str] = Field(default=None, max_length=500)
    day_of_period: Optional[int] = Field(default=None, ge=1, le=31)
    sort_order: Optional[int] = Field(default=None, ge=0)


class TemplateItemRead(BaseModel):
    """GET /template/items response item — also returned by POST/PATCH."""
    model_config = ConfigDict(from_attributes=True)
    id: int
    category_id: int
    amount_cents: int
    description: Optional[str]
    day_of_period: Optional[int]
    sort_order: int


class SnapshotFromPeriodResponse(BaseModel):
    """POST /template/snapshot-from-period/{period_id} response (TPL-03)."""
    model_config = ConfigDict(from_attributes=True)
    template_items: list[TemplateItemRead]
    replaced: int = Field(ge=0, description="Number of template items deleted before snapshot")
```

**app/api/schemas/planned.py**:

```python
"""Pydantic schemas for /api/v1/.../planned and /api/v1/planned endpoints (PLN-01, PLN-02, TPL-04)."""
from datetime import date
from typing import Literal, Optional
from pydantic import BaseModel, ConfigDict, Field


PlanSourceStr = Literal["template", "manual", "subscription_auto"]
KindStr = Literal["expense", "income"]


class PlannedCreate(BaseModel):
    """POST /periods/{id}/planned — manual creation only.

    Service layer always sets source='manual' for this endpoint.
    The `kind` field MUST match the kind of the referenced category
    (validated by service layer; mismatch → 400 KindMismatchError).
    """
    kind: KindStr
    amount_cents: int = Field(gt=0)
    description: Optional[str] = Field(default=None, max_length=500)
    category_id: int = Field(gt=0)
    planned_date: Optional[date] = None


class PlannedUpdate(BaseModel):
    """PATCH /planned/{id} — partial update."""
    kind: Optional[KindStr] = None
    amount_cents: Optional[int] = Field(default=None, gt=0)
    description: Optional[str] = Field(default=None, max_length=500)
    category_id: Optional[int] = Field(default=None, gt=0)
    planned_date: Optional[date] = None


class PlannedRead(BaseModel):
    """Response item for GET/POST/PATCH planned endpoints."""
    model_config = ConfigDict(from_attributes=True)
    id: int
    period_id: int
    kind: KindStr
    amount_cents: int
    description: Optional[str]
    category_id: int
    planned_date: Optional[date]
    source: PlanSourceStr
    subscription_id: Optional[int]


class ApplyTemplateResponse(BaseModel):
    """POST /periods/{id}/apply-template response (TPL-04, PER-05)."""
    period_id: int
    created: int = Field(ge=0, description="Number of new planned rows created (0 if idempotent no-op)")
    planned: list[PlannedRead]
```

Не добавлять других классов / экспортов. Не использовать `__all__` (Phase 2 не использует).
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.api.schemas.templates import TemplateItemCreate, TemplateItemUpdate, TemplateItemRead, SnapshotFromPeriodResponse; from app.api.schemas.planned import PlannedCreate, PlannedUpdate, PlannedRead, ApplyTemplateResponse, PlanSourceStr, KindStr; print('OK')"</automated>
  </verify>
  <done>
- app/api/schemas/templates.py существует с 4 классами: TemplateItemCreate, TemplateItemUpdate, TemplateItemRead, SnapshotFromPeriodResponse.
- app/api/schemas/planned.py существует с 4 классами + 2 type aliases: PlannedCreate, PlannedUpdate, PlannedRead, ApplyTemplateResponse, PlanSourceStr, KindStr.
- Импорт работает без ошибок.
- Pydantic валидация в schemas: gt=0 на amount_cents, ge=1 le=31 на day_of_period, max_length=500 на description, ge=0 на sort_order/replaced/created.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Service layer — templates.py</name>
  <files>app/services/templates.py</files>
  <behavior>
- list_template_items возвращает все template-items, отсортированные по (category_id, sort_order, id).
- create_template_item проверяет существование категории + не-archived (через `cat_svc.get_or_404` + `is_archived` check) → INSERT.
- update_template_item: get_or_404 → если patch меняет category_id, повторно проверяем archived → setattr non-None полей → flush+refresh.
- delete_template_item: get_or_404 → db.delete(item) → flush.
- snapshot_from_period (D-32): валидирует period exists → COUNT existing template-items → SELECT planned WHERE source IN (template, manual) ORDER BY category_id, planned_date NULLS LAST, id → DELETE all template-items → INSERT new template-items mapping (sort_order = idx*10) → return {template_items, replaced}.
- Все функции async; никаких FastAPI imports.
- Domain exceptions: TemplateItemNotFoundError, InvalidCategoryError, PeriodNotFoundError (последняя — может импортироваться из planned.py позже; **в этой фазе** определяем PeriodNotFoundError в planned.py и здесь импортируем её — единый источник).
- Тесты test_templates.py и test_snapshot.py начинают переходить из RED в GREEN (импорты разрешаются; runtime тесты требуют route layer Plan 03-03 для конечной зелёности, но service-уровневая часть ОК).
  </behavior>
  <action>
Создать `app/services/templates.py`. Следовать паттерну `app/services/categories.py:1-146` (header docstring, exception class, async functions, domain language без HTTP-имён).

```python
"""Plan template CRUD + snapshot-from-period (TPL-01, TPL-03).

Service layer is HTTP-framework-agnostic: raises domain exceptions
(``TemplateItemNotFoundError``, ``InvalidCategoryError``,
``PeriodNotFoundError``) which the route layer (Plan 03-03) maps to
HTTPException(404 / 400 / 404) respectively.

D-32: snapshot_from_period — destructive overwrite that includes
``source IN ('template', 'manual')`` and **excludes** ``subscription_auto``,
ensuring subscription rows do not pollute the user's template (they're
already added per-period by the worker job in Phase 6).
"""
from sqlalchemy import delete, func, select
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.schemas.templates import TemplateItemCreate, TemplateItemUpdate
from app.db.models import (
    BudgetPeriod, Category, PlanSource, PlanTemplateItem, PlannedTransaction,
)
from app.services import categories as cat_svc
from app.services.planned import InvalidCategoryError, PeriodNotFoundError


class TemplateItemNotFoundError(Exception):
    """Raised when a template item lookup by id returns no row."""
    def __init__(self, item_id: int) -> None:
        self.item_id = item_id
        super().__init__(f"Template item {item_id} not found")


async def _ensure_category_active(db: AsyncSession, category_id: int) -> Category:
    """Validate category exists and is not archived (D-36).

    Raises:
        CategoryNotFoundError (from cat_svc): category does not exist.
        InvalidCategoryError: category exists but is_archived=True.
    """
    cat = await cat_svc.get_or_404(db, category_id)  # raises CategoryNotFoundError → 404
    if cat.is_archived:
        raise InvalidCategoryError(category_id, "Cannot use archived category")
    return cat


async def list_template_items(db: AsyncSession) -> list[PlanTemplateItem]:
    """Return all template items ordered by (category_id, sort_order, id)."""
    result = await db.execute(
        select(PlanTemplateItem).order_by(
            PlanTemplateItem.category_id,
            PlanTemplateItem.sort_order,
            PlanTemplateItem.id,
        )
    )
    return list(result.scalars().all())


async def get_or_404(db: AsyncSession, item_id: int) -> PlanTemplateItem:
    item = await db.get(PlanTemplateItem, item_id)
    if item is None:
        raise TemplateItemNotFoundError(item_id)
    return item


async def create_template_item(
    db: AsyncSession, *, body: TemplateItemCreate,
) -> PlanTemplateItem:
    """Create a new template item (TPL-01)."""
    await _ensure_category_active(db, body.category_id)
    item = PlanTemplateItem(
        category_id=body.category_id,
        amount_cents=body.amount_cents,
        description=body.description,
        day_of_period=body.day_of_period,
        sort_order=body.sort_order,
    )
    db.add(item)
    await db.flush()
    await db.refresh(item)
    return item


async def update_template_item(
    db: AsyncSession, item_id: int, patch: TemplateItemUpdate,
) -> PlanTemplateItem:
    """Apply non-None fields. If category_id changes, re-validate."""
    item = await get_or_404(db, item_id)
    data = patch.model_dump(exclude_unset=True)
    if "category_id" in data and data["category_id"] != item.category_id:
        await _ensure_category_active(db, data["category_id"])
    for field, value in data.items():
        setattr(item, field, value)
    await db.flush()
    await db.refresh(item)
    return item


async def delete_template_item(db: AsyncSession, item_id: int) -> PlanTemplateItem:
    """Hard delete (CLAUDE.md: soft delete только для category)."""
    item = await get_or_404(db, item_id)
    await db.delete(item)
    await db.flush()
    return item  # detached instance — caller can serialize before commit


async def snapshot_from_period(
    db: AsyncSession, *, period_id: int,
) -> dict:
    """D-32: destructive overwrite of PlanTemplate from period's planned rows.

    Includes source IN ('template', 'manual'); excludes 'subscription_auto'.
    Returns {template_items: [...], replaced: int}.

    Raises PeriodNotFoundError if period does not exist.
    """
    period = await db.get(BudgetPeriod, period_id)
    if period is None:
        raise PeriodNotFoundError(period_id)

    # Count existing for response metadata
    prev_count = await db.scalar(
        select(func.count()).select_from(PlanTemplateItem)
    )
    prev_count = int(prev_count or 0)

    # Select planned rows to copy
    result = await db.execute(
        select(PlannedTransaction)
        .where(
            PlannedTransaction.period_id == period_id,
            PlannedTransaction.source.in_([PlanSource.template, PlanSource.manual]),
        )
        .order_by(
            PlannedTransaction.category_id,
            PlannedTransaction.planned_date.is_(None),
            PlannedTransaction.planned_date,
            PlannedTransaction.id,
        )
    )
    rows = list(result.scalars().all())

    # Destructive overwrite
    await db.execute(delete(PlanTemplateItem))
    await db.flush()  # ensure DELETE happens before INSERT

    new_items: list[PlanTemplateItem] = []
    if rows:
        new_items = [
            PlanTemplateItem(
                category_id=row.category_id,
                amount_cents=row.amount_cents,
                description=row.description,
                day_of_period=row.planned_date.day if row.planned_date else None,
                sort_order=idx * 10,
            )
            for idx, row in enumerate(rows)
        ]
        db.add_all(new_items)
        await db.flush()
        for it in new_items:
            await db.refresh(it)

    return {"template_items": new_items, "replaced": prev_count}
```

**Циклический импорт:** `templates.py` импортирует `InvalidCategoryError, PeriodNotFoundError` из `planned.py`. Это нормально, пока `planned.py` НЕ импортирует ничего из `templates.py` (он не должен). Если линтер ругается — можно вынести exceptions в `app/services/exceptions.py` и оба файла импортируют оттуда. Phase 2 паттерн НЕ имеет такого центрального файла, поэтому **используем перекрёстный импорт**: `templates.py imports from planned.py`. Гарантировать через Task 3 порядок.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "import ast; tree = ast.parse(open('app/services/templates.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, ast.AsyncFunctionDef)]; classes = [n.name for n in ast.walk(tree) if isinstance(n, ast.ClassDef)]; print('funcs:', funcs); print('classes:', classes); assert 'list_template_items' in funcs and 'create_template_item' in funcs and 'update_template_item' in funcs and 'delete_template_item' in funcs and 'snapshot_from_period' in funcs, 'missing required async fns'; assert 'TemplateItemNotFoundError' in classes, 'missing exception'; print('OK')"</automated>
  </verify>
  <done>
- app/services/templates.py содержит 6 async функций: _ensure_category_active, list_template_items, get_or_404, create_template_item, update_template_item, delete_template_item, snapshot_from_period.
- TemplateItemNotFoundError класс определён.
- Импорты PeriodNotFoundError и InvalidCategoryError из app.services.planned.
- Никаких FastAPI imports.
- Snapshot включает source IN (template, manual), исключает subscription_auto (явный `WHERE source.in_([...])`).
- DELETE all template + INSERT new в одной session — атомарно.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service layer — planned.py</name>
  <files>app/services/planned.py</files>
  <behavior>
- list_planned_for_period(period_id, *, kind=None, category_id=None) — фильтр + ORDER BY (category_id, planned_date NULLS LAST, id).
- create_manual_planned(period_id, body) — валидирует period + category active + kind matches category.kind → INSERT с source=PlanSource.manual, subscription_id=None.
- update_planned(planned_id, patch) — get_or_404 → если source=subscription_auto → SubscriptionPlannedReadOnlyError → если patch меняет category, валидируем; если меняется kind, проверяем consistency с (новой) категорией.
- delete_planned(planned_id) — get_or_404 → reject subscription_auto → db.delete + flush.
- apply_template_to_period(period_id) (D-31): валидирует period → COUNT existing source=template → если > 0, SELECT и вернуть `{period_id, created: 0, planned: [...]}` → иначе SELECT template-items с selectinload(category) → построить PlannedTransaction[] с kind=item.category.kind, planned_date=_clamp_planned_date(period, day_of_period), source=template → add_all + flush + refresh → return {period_id, created: N, planned: [...]}.
- Все functions async; без FastAPI imports.
- Domain exceptions: PlannedNotFoundError, PeriodNotFoundError, InvalidCategoryError, KindMismatchError, SubscriptionPlannedReadOnlyError — определены в этом файле.
- selectinload(PlanTemplateItem.category) предотвращает MissingGreenlet error при доступе `item.category.kind`.
- _clamp_planned_date обрабатывает: None → None; иначе period_start + (day_of_period - 1) с clamp к period_end.
- Тесты test_planned.py и test_apply_template.py перестают падать на ImportError для service.
  </behavior>
  <action>
Создать `app/services/planned.py`. Следовать паттерну Phase 2.

```python
"""Planned transactions CRUD + apply-template (PLN-01, PLN-02, PLN-03, TPL-04).

Service layer raises domain exceptions; route layer (Plan 03-03) maps them
to HTTP status codes.

Source semantics:
- ``manual``: created via POST /periods/{id}/planned (this service).
- ``template``: created by ``apply_template_to_period`` (D-31 idempotent).
- ``subscription_auto``: created by Phase 6 worker; THIS SERVICE refuses
  to mutate or delete such rows (D-37 / SubscriptionPlannedReadOnlyError).

Idempotency contract for apply_template_to_period (D-31):
- If ANY planned row with ``source=template`` already exists for the period,
  return existing rows without creating new ones (``created=0``). This means
  PER-05 worker can safely call this endpoint on every period creation.
"""
from datetime import date, timedelta
from typing import Optional

from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy.orm import selectinload

from app.api.schemas.planned import PlannedCreate, PlannedUpdate
from app.db.models import (
    BudgetPeriod, Category, CategoryKind, PlannedTransaction, PlanSource,
    PlanTemplateItem,
)
from app.services import categories as cat_svc


# ---------- Domain exceptions ----------

class PlannedNotFoundError(Exception):
    def __init__(self, planned_id: int) -> None:
        self.planned_id = planned_id
        super().__init__(f"Planned transaction {planned_id} not found")


class PeriodNotFoundError(Exception):
    def __init__(self, period_id: int) -> None:
        self.period_id = period_id
        super().__init__(f"Budget period {period_id} not found")


class InvalidCategoryError(Exception):
    def __init__(self, category_id: int, reason: str) -> None:
        self.category_id = category_id
        self.reason = reason
        super().__init__(f"Category {category_id}: {reason}")


class KindMismatchError(Exception):
    def __init__(self, plan_kind: str, category_kind: str) -> None:
        self.plan_kind = plan_kind
        self.category_kind = category_kind
        super().__init__(
            f"Plan kind '{plan_kind}' does not match category kind '{category_kind}'"
        )


class SubscriptionPlannedReadOnlyError(Exception):
    def __init__(self, planned_id: int) -> None:
        self.planned_id = planned_id
        super().__init__(
            f"Planned transaction {planned_id} is managed by a subscription "
            "and cannot be modified directly"
        )


# ---------- Helpers ----------

def _clamp_planned_date(period: BudgetPeriod, day_of_period: Optional[int]) -> Optional[date]:
    """Map template day_of_period to actual date inside period bounds.

    Algorithm:
        candidate = period_start + (day_of_period - 1)
        if candidate > period_end: clamp to period_end
        if day_of_period is None: return None
    """
    if day_of_period is None:
        return None
    candidate = period.period_start + timedelta(days=day_of_period - 1)
    if candidate > period.period_end:
        return period.period_end
    return candidate


async def _ensure_category_active(db: AsyncSession, category_id: int) -> Category:
    cat = await cat_svc.get_or_404(db, category_id)  # CategoryNotFoundError → 404
    if cat.is_archived:
        raise InvalidCategoryError(category_id, "Cannot use archived category")
    return cat


async def _get_period_or_404(db: AsyncSession, period_id: int) -> BudgetPeriod:
    period = await db.get(BudgetPeriod, period_id)
    if period is None:
        raise PeriodNotFoundError(period_id)
    return period


async def get_or_404(db: AsyncSession, planned_id: int) -> PlannedTransaction:
    row = await db.get(PlannedTransaction, planned_id)
    if row is None:
        raise PlannedNotFoundError(planned_id)
    return row


# ---------- CRUD ----------

async def list_planned_for_period(
    db: AsyncSession,
    period_id: int,
    *,
    kind: Optional[str] = None,
    category_id: Optional[int] = None,
) -> list[PlannedTransaction]:
    """List planned rows for a period, optionally filtered by kind/category.

    Note: does NOT raise PeriodNotFoundError if period is missing — returns
    empty list. Caller (route) validates period existence separately if
    needed (typical pattern: GET /periods/current → if 404 then no list).
    """
    stmt = select(PlannedTransaction).where(PlannedTransaction.period_id == period_id)
    if kind is not None:
        stmt = stmt.where(PlannedTransaction.kind == CategoryKind(kind))
    if category_id is not None:
        stmt = stmt.where(PlannedTransaction.category_id == category_id)
    stmt = stmt.order_by(
        PlannedTransaction.category_id,
        PlannedTransaction.planned_date.is_(None),
        PlannedTransaction.planned_date,
        PlannedTransaction.id,
    )
    result = await db.execute(stmt)
    return list(result.scalars().all())


async def create_manual_planned(
    db: AsyncSession, period_id: int, body: PlannedCreate,
) -> PlannedTransaction:
    """Create a planned row with source=manual."""
    await _get_period_or_404(db, period_id)  # raises PeriodNotFoundError
    cat = await _ensure_category_active(db, body.category_id)
    if cat.kind.value != body.kind:
        raise KindMismatchError(body.kind, cat.kind.value)

    row = PlannedTransaction(
        period_id=period_id,
        kind=CategoryKind(body.kind),
        amount_cents=body.amount_cents,
        description=body.description,
        category_id=body.category_id,
        planned_date=body.planned_date,
        source=PlanSource.manual,
        subscription_id=None,
    )
    db.add(row)
    await db.flush()
    await db.refresh(row)
    return row


async def update_planned(
    db: AsyncSession, planned_id: int, patch: PlannedUpdate,
) -> PlannedTransaction:
    """Update a planned row. Rejects subscription_auto rows (D-37)."""
    row = await get_or_404(db, planned_id)
    if row.source == PlanSource.subscription_auto:
        raise SubscriptionPlannedReadOnlyError(planned_id)

    data = patch.model_dump(exclude_unset=True)

    # Pre-validate: if changing category, ensure active
    new_cat: Optional[Category] = None
    if "category_id" in data and data["category_id"] != row.category_id:
        new_cat = await _ensure_category_active(db, data["category_id"])

    # Determine effective kind + effective category for consistency check
    effective_kind = data.get("kind", row.kind.value if isinstance(row.kind, CategoryKind) else row.kind)
    effective_cat_id = data.get("category_id", row.category_id)
    if new_cat is None and ("kind" in data or "category_id" in data):
        # Need cat for consistency check (might be same category as before)
        new_cat = await cat_svc.get_or_404(db, effective_cat_id)
    if new_cat is not None and new_cat.kind.value != effective_kind:
        raise KindMismatchError(effective_kind, new_cat.kind.value)

    for field, value in data.items():
        if field == "kind":
            setattr(row, field, CategoryKind(value))
        else:
            setattr(row, field, value)
    await db.flush()
    await db.refresh(row)
    return row


async def delete_planned(db: AsyncSession, planned_id: int) -> PlannedTransaction:
    """Hard delete. Rejects subscription_auto rows (D-37)."""
    row = await get_or_404(db, planned_id)
    if row.source == PlanSource.subscription_auto:
        raise SubscriptionPlannedReadOnlyError(planned_id)
    await db.delete(row)
    await db.flush()
    return row


# ---------- Apply template (TPL-04, PER-05) ----------

async def apply_template_to_period(
    db: AsyncSession, *, period_id: int,
) -> dict:
    """D-31: idempotent apply-template — skip if any source='template' already exists.

    Returns {period_id, created, planned}. created=0 means no-op (existing
    template rows returned). Empty template returns created=0, planned=[].

    Raises PeriodNotFoundError if period does not exist.
    """
    period = await _get_period_or_404(db, period_id)

    # Idempotency check: any source='template' for this period?
    existing_count = await db.scalar(
        select(func.count())
        .select_from(PlannedTransaction)
        .where(
            PlannedTransaction.period_id == period_id,
            PlannedTransaction.source == PlanSource.template,
        )
    )
    existing_count = int(existing_count or 0)

    if existing_count > 0:
        result = await db.execute(
            select(PlannedTransaction)
            .where(
                PlannedTransaction.period_id == period_id,
                PlannedTransaction.source == PlanSource.template,
            )
            .order_by(PlannedTransaction.id)
        )
        existing = list(result.scalars().all())
        return {"period_id": period_id, "created": 0, "planned": existing}

    # Load template items + their categories (eager-load for kind access)
    items_result = await db.execute(
        select(PlanTemplateItem)
        .options(selectinload(PlanTemplateItem.category))
        .order_by(PlanTemplateItem.sort_order, PlanTemplateItem.id)
    )
    items = list(items_result.scalars().all())

    if not items:
        return {"period_id": period_id, "created": 0, "planned": []}

    new_rows = [
        PlannedTransaction(
            period_id=period_id,
            kind=item.category.kind,
            amount_cents=item.amount_cents,
            description=item.description,
            category_id=item.category_id,
            planned_date=_clamp_planned_date(period, item.day_of_period),
            source=PlanSource.template,
            subscription_id=None,
        )
        for item in items
    ]
    db.add_all(new_rows)
    await db.flush()
    for row in new_rows:
        await db.refresh(row)

    return {"period_id": period_id, "created": len(new_rows), "planned": new_rows}
```

**Critical correctness notes:**
- `cat.kind.value` потому что Category.kind — это `CategoryKind` PgEnum, а body.kind — string "expense"/"income". Сравнение по value, не по объекту.
- При apply-template `kind=item.category.kind` — ORM приводит CategoryKind к ENUM в DB и обратно к Python enum при чтении. selectinload гарантирует, что `.category` доступен без lazy-load.
- В update_planned: важный тонкий момент — если patch меняет category, мы должны убедиться, что новая категория совместима с (потенциально новым) kind. Алгоритм выше делает это в 3 шагах: (a) если category меняется — ensure active, (b) compute effective_kind, (c) compare effective_kind vs new_cat.kind.
- Если patch меняет ТОЛЬКО kind (без category_id), мы должны загрузить current category и проверить — это тоже покрыто веткой `("kind" in data or "category_id" in data)`.
- `row.kind.value` если `row.kind` — enum instance; в SQLAlchemy 2 при чтении из БД это будет CategoryKind enum instance, поэтому `.value` корректно.
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && uv run python -c "from app.services.planned import (list_planned_for_period, create_manual_planned, update_planned, delete_planned, apply_template_to_period, PlannedNotFoundError, PeriodNotFoundError, InvalidCategoryError, KindMismatchError, SubscriptionPlannedReadOnlyError, _clamp_planned_date); from app.services.templates import (list_template_items, create_template_item, update_template_item, delete_template_item, snapshot_from_period, TemplateItemNotFoundError); print('OK all imports')"</automated>
  </verify>
  <done>
- app/services/planned.py содержит 5 async CRUD functions + apply_template_to_period + 5 exception classes + 3 helpers (_clamp_planned_date, _ensure_category_active, _get_period_or_404, get_or_404).
- app/services/templates.py успешно импортирует InvalidCategoryError и PeriodNotFoundError из planned.py.
- selectinload(PlanTemplateItem.category) используется в apply_template_to_period.
- Idempotency check: SELECT count WHERE source=template; если > 0 — return existing.
- _clamp_planned_date обрабатывает None и edge case (candidate > period_end → clamp).
- Никаких FastAPI imports в обоих сервисах.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service layer ← Pydantic schema | Pydantic уже валидировала body на route-уровне; service может доверять типам |
| Service layer → DB | Service строит SQL через SQLAlchemy ORM (защищено от SQL-injection by design) |
| Service layer → Category существование/state | Делегируется cat_svc.get_or_404 + явный is_archived check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | apply_template_to_period с гонкой двух вызовов | accept | Single-tenant — гонка маловероятна; D-31 источник: одна транзакция per request, может появиться один дубль на 2 одновременных запроса. В Phase 5 worker сериализует через advisory lock. |
| T-03-05 | Tampering | Manual planned с category != kind | mitigate | KindMismatchError в create_manual_planned + update_planned |
| T-03-06 | Tampering | Edit subscription_auto через UI/API | mitigate | SubscriptionPlannedReadOnlyError в update_planned + delete_planned (D-37 server-side guard) |
| T-03-07 | Information Disclosure | snapshot включает subscription_auto в шаблон | mitigate | D-32 явный `source.in_([template, manual])` фильтр |
| T-03-08 | Tampering | Template/planned с archived category | mitigate | _ensure_category_active в create + update |
| T-03-09 | Input Validation | day_of_period > длины периода | mitigate | _clamp_planned_date к period_end в apply-template |
| T-03-10 | Input Validation | Pydantic gt=0 / ge=1 le=31 / max_length=500 | mitigate | Schema-level validation возвращает 422 до сервиса |
</threat_model>

<verification>
- Импорт всех новых модулей проходит без ошибок.
- AST содержит ожидаемые async functions и exception classes.
- Тесты test_templates.py / test_planned.py перестают падать на ImportError для `app.services.*` и `app.api.schemas.*`. Они всё ещё падают на runtime (потому что routes не существуют), но это уже Plan 03-03 territory.
- Запуск `uv run pytest tests/test_templates.py tests/test_planned.py tests/test_apply_template.py tests/test_snapshot.py -x --collect-only` — collection успешна (сами тесты ещё не GREEN).
</verification>

<success_criteria>
- 4 файла созданы: 2 schemas + 2 services.
- Cross-import templates.py ← planned.py работает (PeriodNotFoundError, InvalidCategoryError).
- Service-функции реализуют все behaviour из <behavior> блоков.
- Никаких FastAPI / HTTPException imports в services.
- Domain exceptions покрывают все сценарии (TemplateItemNotFoundError, PlannedNotFoundError, PeriodNotFoundError, InvalidCategoryError, KindMismatchError, SubscriptionPlannedReadOnlyError).
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-template-and-planned-transactions/03-02-SUMMARY.md`.
</output>
