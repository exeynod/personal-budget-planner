---
phase: 09-ai-assistant
plan: "05"
type: execute
wave: 3
depends_on:
  - "09-03"
  - "09-04"
files_modified:
  - app/api/routes/ai.py
  - app/api/router.py
autonomous: true
requirements:
  - AI-03
  - AI-06
  - AI-09
  - AI-10

must_haves:
  truths:
    - "POST /api/v1/ai/chat возвращает 200 + Content-Type: text/event-stream"
    - "GET /api/v1/ai/history возвращает 200 + {messages: [...]}"
    - "DELETE /api/v1/ai/conversation возвращает 204"
    - "Все три эндпоинта требуют initData auth (403 без него)"
    - "Превышение 30 req/мин возвращает 429 + Retry-After заголовок"
    - "OPENAI_API_KEY не передаётся на frontend ни в каком ответе"
  artifacts:
    - path: app/api/routes/ai.py
      provides: "POST /ai/chat (SSE), GET /ai/history, DELETE /ai/conversation + rate limiter"
      exports:
        - router
    - path: app/api/router.py
      provides: "Регистрация ai_router в public_router"
  key_links:
    - from: app/api/routes/ai.py
      to: app/ai/llm_client.py
      via: "get_llm_client() в _event_stream"
      pattern: "from app.ai.llm_client import get_llm_client"
    - from: app/api/routes/ai.py
      to: app/ai/tools.py
      via: "TOOL_FUNCTIONS[tool_name](db)"
      pattern: "from app.ai.tools import TOOL_FUNCTIONS"
    - from: app/api/routes/ai.py
      to: app/services/ai_conversation_service.py
      via: "get_or_create_conversation, append_message, get_recent_messages"
      pattern: "from app.services import ai_conversation_service"
    - from: app/api/router.py
      to: app/api/routes/ai.py
      via: "public_router.include_router(ai_router)"
      pattern: "from app.api.routes.ai import router as ai_router"
---

<objective>
Создать API-эндпоинты AI: SSE streaming chat, история разговора, очистка. Интегрировать LLM-клиент, tools и conversation service. Rate limiter in-memory.

Purpose: Это публичная точка входа Phase 9 — frontend вызывает только эти эндпоинты. SSE streaming отдаёт токены в реальном времени (AI-03). Rate limit защищает от злоупотреблений (AI-10).
Output: app/api/routes/ai.py + регистрация в router.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-ai-assistant/09-CONTEXT.md
@.planning/phases/09-ai-assistant/09-PATTERNS.md
@.planning/phases/09-ai-assistant/09-03-SUMMARY.md
@.planning/phases/09-ai-assistant/09-04-SUMMARY.md

<interfaces>
<!-- Паттерн из app/api/routes/analytics.py — router declaration, auth -->
<!-- Контракт из 09-CONTEXT.md: SSE events format, rate limit -->

Router pattern (из analytics.py):
```python
router = APIRouter(
    prefix="/ai",
    tags=["ai"],
    dependencies=[Depends(get_current_user)],
)
```

SSE events формат (из CONTEXT.md decisions):
```
data: {"type": "token", "data": "текст"}\n\n
data: {"type": "tool_start", "data": "get_period_balance"}\n\n
data: {"type": "tool_end", "data": ""}\n\n
data: {"type": "done", "data": ""}\n\n
data: {"type": "error", "data": "сообщение"}\n\n
```

Rate limit: in-memory dict + TTL asyncio (AI-10):
```python
import asyncio
import time
from collections import defaultdict

_rate_buckets: dict[int, list[float]] = defaultdict(list)
_RATE_LIMIT = 30  # req/min
_RATE_WINDOW = 60  # seconds

def _is_rate_limited(user_id: int) -> bool:
    now = time.monotonic()
    bucket = _rate_buckets[user_id]
    # Удалить устаревшие timestamps
    _rate_buckets[user_id] = [t for t in bucket if now - t < _RATE_WINDOW]
    if len(_rate_buckets[user_id]) >= _RATE_LIMIT:
        return True
    _rate_buckets[user_id].append(now)
    return False
```

get_current_user возвращает dict {"id": tg_user_id, ...}:
```python
# Из app/api/dependencies.py — get_current_user returns dict
current_user: Annotated[dict, Depends(get_current_user)]
# current_user["id"] — tg_user_id (int)
```

Из app/ai/llm_client.py (Plan 09-03):
```python
from app.ai.llm_client import get_llm_client
client = get_llm_client()  # возвращает AbstractLLMClient
async for event in client.chat(messages, tools=TOOLS_SCHEMA):
    yield event  # dict {type, data}
```

Из app/ai/tools.py (Plan 09-04):
```python
from app.ai.tools import TOOLS_SCHEMA, TOOL_FUNCTIONS
result = await TOOL_FUNCTIONS[tool_name](db, **kwargs)
```

Из app/services/ai_conversation_service.py (Plan 09-04):
```python
from app.services import ai_conversation_service as conv_svc
conv = await conv_svc.get_or_create_conversation(db)
msgs = await conv_svc.get_recent_messages(db, conv.id, limit=settings.AI_MAX_CONTEXT_MESSAGES)
await conv_svc.append_message(db, conv.id, role="user", content="...")
await conv_svc.clear_conversation(db, conv.id)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: API route /ai/chat (SSE), /ai/history, /ai/conversation</name>
  <files>app/api/routes/ai.py</files>
  <read_first>
    - app/api/routes/analytics.py (полный файл — router declaration, auth pattern, endpoint structure)
    - app/api/dependencies.py (get_current_user сигнатура и возвращаемый тип)
  </read_first>
  <action>
Создать app/api/routes/ai.py:

```python
"""REST эндпоинты Phase 9 AI Assistant (AI-03, AI-06, AI-09, AI-10).

Endpoints:
- POST /ai/chat — SSE streaming chat с tool-use (AI-03)
- GET /ai/history — история разговора (AI-06)
- DELETE /ai/conversation — очистка истории (AI-06)

Auth: router-level Depends(get_current_user) — все эндпоинты защищены.
Rate limit: in-memory sliding window 30 req/мин (AI-10, per-process).
OPENAI_API_KEY: только в backend ENV, никогда не в ответах (AI-09).
"""
from __future__ import annotations

import json
import time
from collections import defaultdict
from typing import Annotated, AsyncGenerator

from fastapi import APIRouter, Depends, HTTPException
from fastapi.responses import StreamingResponse
from sqlalchemy.ext.asyncio import AsyncSession

from app.ai.llm_client import get_llm_client
from app.ai.system_prompt import build_messages
from app.ai.tools import TOOL_FUNCTIONS, TOOLS_SCHEMA
from app.api.dependencies import get_current_user, get_db
from app.api.schemas.ai import ChatHistoryResponse, ChatMessageRead, ChatRequest
from app.core.settings import settings
from app.services import ai_conversation_service as conv_svc

router = APIRouter(
    prefix="/ai",
    tags=["ai"],
    dependencies=[Depends(get_current_user)],
)

# ---- Rate limiter (in-memory, per-process) — AI-10 ----
_rate_buckets: dict[int, list[float]] = defaultdict(list)
_RATE_LIMIT = 30  # запросов в минуту
_RATE_WINDOW = 60.0  # секунд


def _is_rate_limited(user_id: int) -> bool:
    """Sliding window rate limiter. Возвращает True если лимит превышен."""
    now = time.monotonic()
    bucket = _rate_buckets[user_id]
    # Очистить устаревшие записи
    _rate_buckets[user_id] = [t for t in bucket if now - t < _RATE_WINDOW]
    if len(_rate_buckets[user_id]) >= _RATE_LIMIT:
        return True
    _rate_buckets[user_id].append(now)
    return False


def _get_llm_client():
    """Фабрика LLM-клиента — отдельная функция для monkeypatching в тестах."""
    return get_llm_client()


async def _event_stream(
    db: AsyncSession,
    user_id: int,
    message: str,
) -> AsyncGenerator[str, None]:
    """Генератор SSE-событий для POST /ai/chat.

    Протокол:
    1. Получить/создать conversation
    2. Сохранить user message в БД
    3. Получить историю (последние AI_MAX_CONTEXT_MESSAGES)
    4. Собрать messages с system prompt (cache_control)
    5. Стримить события от LLM
    6. Обработать tool_call_complete → вызвать tool → добавить tool result
    7. Продолжить стриминг с tool result
    8. Сохранить assistant response в БД
    9. Отправить done
    """
    try:
        # 1. Conversation persistence
        conv = await conv_svc.get_or_create_conversation(db)
        await conv_svc.append_message(db, conv.id, role="user", content=message)
        await db.flush()

        # 2. История для LLM-контекста
        history_msgs = await conv_svc.get_recent_messages(
            db, conv.id, limit=settings.AI_MAX_CONTEXT_MESSAGES
        )
        # Преобразовать ORM → dict для build_messages
        history_dicts = [
            {"role": m.role, "content": m.content or ""}
            for m in history_msgs[:-1]  # исключить только что добавленный user msg
        ]

        # 3. Собрать messages с cache_control
        llm_messages = build_messages(history_dicts, message)

        # 4. Стримить события
        client = _get_llm_client()
        assistant_content_parts: list[str] = []
        pending_tool_call: dict | None = None

        async for event in client.chat(llm_messages, tools=TOOLS_SCHEMA):
            etype = event.get("type", "")

            if etype == "token":
                assistant_content_parts.append(event["data"])
                yield f"data: {json.dumps({'type': 'token', 'data': event['data']})}\n\n"

            elif etype == "tool_start":
                yield f"data: {json.dumps({'type': 'tool_start', 'data': event['data']})}\n\n"

            elif etype == "tool_call_complete":
                # Internal event — не экспортируем в SSE
                pending_tool_call = json.loads(event["data"])

            elif etype == "tool_end":
                # Вызвать tool и продолжить стриминг
                if pending_tool_call:
                    tool_name = pending_tool_call.get("name", "")
                    raw_args = pending_tool_call.get("arguments", "{}")
                    try:
                        kwargs = json.loads(raw_args) if raw_args else {}
                    except json.JSONDecodeError:
                        kwargs = {}

                    tool_fn = TOOL_FUNCTIONS.get(tool_name)
                    if tool_fn:
                        tool_result = await tool_fn(db, **kwargs)
                    else:
                        tool_result = {"error": f"Неизвестный инструмент: {tool_name}"}

                    # Сохранить tool result в БД
                    await conv_svc.append_message(
                        db,
                        conv.id,
                        role="tool",
                        tool_name=tool_name,
                        tool_result=json.dumps(tool_result, ensure_ascii=False),
                    )

                    # Второй LLM-запрос с tool result
                    tool_result_str = json.dumps(tool_result, ensure_ascii=False)
                    llm_messages_with_result = llm_messages + [
                        {
                            "role": "tool",
                            "tool_call_id": pending_tool_call.get("id", ""),
                            "content": tool_result_str,
                        }
                    ]

                    async for event2 in client.chat(
                        llm_messages_with_result, tools=None
                    ):
                        etype2 = event2.get("type", "")
                        if etype2 == "token":
                            assistant_content_parts.append(event2["data"])
                            yield f"data: {json.dumps({'type': 'token', 'data': event2['data']})}\n\n"
                        elif etype2 == "done":
                            break
                        elif etype2 == "error":
                            yield f"data: {json.dumps({'type': 'error', 'data': event2['data']})}\n\n"
                            return

                yield f"data: {json.dumps({'type': 'tool_end', 'data': ''})}\n\n"
                pending_tool_call = None

            elif etype == "done":
                # Сохранить полный assistant ответ в БД
                full_response = "".join(assistant_content_parts)
                if full_response:
                    await conv_svc.append_message(
                        db, conv.id, role="assistant", content=full_response
                    )
                await db.flush()
                yield f"data: {json.dumps({'type': 'done', 'data': ''})}\n\n"
                return

            elif etype == "error":
                yield f"data: {json.dumps({'type': 'error', 'data': event['data']})}\n\n"
                return

    except Exception as exc:
        yield f"data: {json.dumps({'type': 'error', 'data': str(exc)})}\n\n"


@router.post("/chat")
async def chat(
    body: ChatRequest,
    current_user: Annotated[dict, Depends(get_current_user)],
    db: AsyncSession = Depends(get_db),
) -> StreamingResponse:
    """POST /ai/chat — streaming SSE ответ (AI-03).

    Rate limit: 30 req/мин → 429 + Retry-After (AI-10).
    """
    user_id: int = current_user["id"]

    if _is_rate_limited(user_id):
        raise HTTPException(
            status_code=429,
            detail="Превышен лимит запросов. Попробуй через минуту.",
            headers={"Retry-After": "60"},
        )

    return StreamingResponse(
        _event_stream(db, user_id, body.message),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "X-Accel-Buffering": "no",
        },
    )


@router.get("/history", response_model=ChatHistoryResponse)
async def get_history(
    db: AsyncSession = Depends(get_db),
) -> ChatHistoryResponse:
    """GET /ai/history — история разговора (AI-06)."""
    conv = await conv_svc.get_or_create_conversation(db)
    msgs = await conv_svc.get_recent_messages(
        db, conv.id, limit=settings.AI_MAX_CONTEXT_MESSAGES
    )
    return ChatHistoryResponse(
        messages=[
            ChatMessageRead(
                id=m.id,
                role=m.role,
                content=m.content,
                tool_name=m.tool_name,
                created_at=m.created_at.isoformat(),
            )
            for m in msgs
        ]
    )


@router.delete("/conversation", status_code=204)
async def clear_conversation(
    db: AsyncSession = Depends(get_db),
) -> None:
    """DELETE /ai/conversation — очистить историю разговора (AI-06)."""
    conv = await conv_svc.get_or_create_conversation(db)
    await conv_svc.clear_conversation(db, conv.id)
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.routes.ai import router, _is_rate_limited; print('OK', router.prefix)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "prefix.*\"/ai\"" app/api/routes/ai.py` возвращает строку
    - `grep "Depends(get_current_user)" app/api/routes/ai.py | wc -l` возвращает минимум 2 строки (router-level + chat endpoint)
    - `grep "status_code=429" app/api/routes/ai.py` возвращает строку
    - `grep "Retry-After" app/api/routes/ai.py` возвращает строку с заголовком
    - `grep "text/event-stream" app/api/routes/ai.py` возвращает строку
    - `grep "StreamingResponse" app/api/routes/ai.py` возвращает строку
    - `grep "OPENAI_API_KEY\|openai_api_key" app/api/routes/ai.py | wc -l` возвращает 0 (ключ не в route!)
    - `grep "status_code=204" app/api/routes/ai.py` возвращает строку (DELETE)
    - `python -c "from app.api.routes.ai import router"` — без ошибок
  </acceptance_criteria>
  <done>3 эндпоинта работают: POST /chat (SSE), GET /history, DELETE /conversation; rate limiter активен</done>
</task>

<task type="auto">
  <name>Задача 2: Регистрация ai_router в router.py</name>
  <files>app/api/router.py</files>
  <read_first>
    - app/api/router.py (весь файл — паттерн include_router для analytics_router в конце public_router секции)
  </read_first>
  <action>
Прочитать app/api/router.py. Добавить регистрацию ai_router по аналогии с analytics_router:

1. В блок импортов (после `from app.api.routes.analytics import router as analytics_router`):
```python
from app.api.routes.ai import router as ai_router
```

2. После строки `public_router.include_router(analytics_router)` добавить:
```python
# Phase 9 sub-router — AI chat endpoint (AI-03, AI-06, AI-10).
public_router.include_router(ai_router)
```

3. Обновить docstring файла — добавить в блок Phase 9 routes (после Phase 8):
```
Phase 9 routes (added via include_router):
- ``/ai/chat`` (POST, SSE) — AI streaming chat (AI-03)
- ``/ai/history`` (GET) — AI conversation history (AI-06)
- ``/ai/conversation`` (DELETE) — clear AI history (AI-06)
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && python -c "from app.api.router import public_router; routes = [r.path for r in public_router.routes]; print([r for r in routes if 'ai' in r])"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from app.api.routes.ai import" app/api/router.py` возвращает строку с `ai_router`
    - `grep "include_router(ai_router)" app/api/router.py` возвращает строку
    - `python -c "from app.api.router import public_router"` — без ошибок
    - После импорта public_router содержит маршруты с prefix /ai
  </acceptance_criteria>
  <done>ai_router зарегистрирован в public_router; /api/v1/ai/* эндпоинты доступны</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /ai/chat | Непроверенный user input (message) → LLM prompt |
| LLM tool_call → TOOL_FUNCTIONS | LLM указывает имя tool и аргументы — потенциальная injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Denial of Service | POST /ai/chat | mitigate | Rate limit 30 req/мин с Retry-After header (AI-10); sliding window in-memory |
| T-09-12 | Tampering | tool_name validation | mitigate | `TOOL_FUNCTIONS.get(tool_name)` — только зарегистрированные инструменты; неизвестный → error dict |
| T-09-13 | Information | SSE response body | accept | Single-tenant, данные только владельца; TLS через Caddy |
| T-09-14 | Spoofing | LLM tool arguments | mitigate | JSON parse с try/except → {} при ошибке; tool функции защищены try/except → error dict |
| T-09-15 | Information | OPENAI_API_KEY в ответах | mitigate | Ключ только в settings; route не возвращает config объекты; проверяется в acceptance criteria |
</threat_model>

<verification>
```bash
cd /Users/exy/pet_projects/tg-budget-planner
python -m pytest tests/api/test_ai_chat.py::test_chat_requires_auth \
  tests/api/test_ai_chat.py::test_history_requires_auth \
  tests/api/test_ai_chat.py::test_clear_requires_auth -v
```
Ожидаемый результат: все 3 теста PASS (403 без auth).
</verification>

<success_criteria>
- POST /ai/chat возвращает 200 + text/event-stream (с mock LLM)
- GET /ai/history возвращает 200 + {messages: [...]}
- DELETE /ai/conversation возвращает 204
- Все 3 эндпоинта возвращают 403 без initData
- Rate limit: 31-й запрос → 429 + Retry-After: 60
- RED тесты из Plan 09-01 (test_ai_chat.py) переходят в GREEN
</success_criteria>

<output>
После завершения создать `.planning/phases/09-ai-assistant/09-05-SUMMARY.md`
</output>
