---
phase: 09-ai-assistant
plan: "06"
type: execute
wave: 4
depends_on:
  - "09-05"
files_modified:
  - frontend/src/api/types.ts
  - frontend/src/api/ai.ts
  - frontend/src/hooks/useAiConversation.ts
  - frontend/src/components/ChatMessage.tsx
  - frontend/src/components/ChatMessage.module.css
  - frontend/src/components/ToolUseIndicator.tsx
  - frontend/src/components/ToolUseIndicator.module.css
autonomous: true
requirements:
  - AI-03
  - AI-04
  - AI-06

must_haves:
  truths:
    - "ChatMessageRead, AiStreamEvent типы экспортируются из frontend/src/api/types.ts"
    - "streamChat() использует fetch + ReadableStream (не EventSource) с X-Telegram-Init-Data заголовком"
    - "useAiConversation() возвращает {messages, streaming, toolName, error, sendMessage, clearHistory}"
    - "ChatMessage компонент рендерит user/assistant bubble с inline markdown"
    - "ToolUseIndicator показывает pulse-анимацию с текстом 'Смотрю данные...'"
  artifacts:
    - path: frontend/src/api/types.ts
      provides: "AI типы (ChatMessageRead, AiStreamEvent, ChatHistoryResponse)"
      contains: "AiStreamEvent"
    - path: frontend/src/api/ai.ts
      provides: "streamChat(), getChatHistory(), clearConversation()"
      exports:
        - streamChat
        - getChatHistory
        - clearConversation
    - path: frontend/src/hooks/useAiConversation.ts
      provides: "Hook для управления AI разговором"
      exports:
        - useAiConversation
    - path: frontend/src/components/ChatMessage.tsx
      provides: "Bubble компонент для user/assistant сообщений"
      exports:
        - ChatMessage
    - path: frontend/src/components/ToolUseIndicator.tsx
      provides: "Pulse-pill индикатор вызова tool"
      exports:
        - ToolUseIndicator
  key_links:
    - from: frontend/src/hooks/useAiConversation.ts
      to: frontend/src/api/ai.ts
      via: "streamChat(), getChatHistory()"
      pattern: "import.*from.*api/ai"
    - from: frontend/src/components/ChatMessage.tsx
      to: frontend/src/api/types.ts
      via: "ChatMessageRead тип"
      pattern: "import.*ChatMessageRead"
---

<objective>
Создать frontend data layer для AI чата: TypeScript типы, API клиент с fetch+ReadableStream, hook управления разговором, компоненты сообщений и tool indicator.

Purpose: Это слой между AiScreen (Plan 09-07) и backend API. Архитектура повторяет analytics layer (types→api→hook→component). fetch+ReadableStream вместо EventSource для поддержки custom headers с initData auth (AI-09).
Output: 7 frontend файлов (types, api, hook, 2 компонента × 2 файла CSS).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-ai-assistant/09-CONTEXT.md
@.planning/phases/09-ai-assistant/09-PATTERNS.md
@.planning/phases/09-ai-assistant/09-05-SUMMARY.md

<interfaces>
<!-- Паттерн из frontend/src/api/analytics.ts и frontend/src/api/client.ts -->
<!-- Паттерн из frontend/src/hooks/useAnalytics.ts -->

Из frontend/src/api/client.ts — getInitDataRaw для SSE headers:
```typescript
export function getInitDataRaw(): string | null;
export async function apiFetch<T>(path: string, options?: RequestInit): Promise<T>;
```

Из frontend/src/hooks/useAnalytics.ts — cancelled flag pattern:
```typescript
useEffect(() => {
  let cancelled = false;
  someAsyncCall()
    .then(data => { if (!cancelled) setState(data); })
    .catch((e: unknown) => { if (!cancelled) setError(e instanceof Error ? e.message : String(e)); })
    .finally(() => { if (!cancelled) setLoading(false); });
  return () => { cancelled = true; };
}, [deps]);
```

SSE parsing pattern (TextDecoder, ReadableStream):
```typescript
const reader = res.body!.getReader();
const decoder = new TextDecoder();
let buffer = '';
while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  buffer += decoder.decode(value, { stream: true });
  const lines = buffer.split('\n\n');
  buffer = lines.pop()!;
  for (const line of lines) {
    if (line.startsWith('data: ')) {
      const payload = JSON.parse(line.slice(6));
      onEvent(payload);
    }
  }
}
onDone();
```

Inline markdown parser (bold + ul/ol):
```typescript
function parseMarkdown(text: string): string {
  return text
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
    .replace(/^- (.+)$/gm, '<li>$1</li>')
    .replace(/^(\d+)\. (.+)$/gm, '<li>$2</li>');
}
```

CSS Variables из проекта (Phosphor icons, #a78bfa для AI-таба):
```css
/* AI tab: #a78bfa (фиолетовый) */
/* user bubble: TG primary color */
/* assistant bubble: surface card */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Задача 1: TypeScript типы + API клиент (types.ts + ai.ts)</name>
  <files>frontend/src/api/types.ts, frontend/src/api/ai.ts</files>
  <read_first>
    - frontend/src/api/types.ts (последние 30 строк — конец файла, куда добавить AI типы)
    - frontend/src/api/analytics.ts (весь файл — паттерн apiFetch, импорты)
    - frontend/src/api/client.ts (getInitDataRaw export)
  </read_first>
  <action>
**1. Добавить в конец frontend/src/api/types.ts:**

```typescript
// ---------- Phase 9: AI Assistant ----------

export type AiRole = 'user' | 'assistant' | 'tool';

export interface ChatMessageRead {
  id: number;
  role: AiRole;
  content: string | null;
  tool_name: string | null;
  created_at: string; // ISO datetime
}

export interface ChatHistoryResponse {
  messages: ChatMessageRead[];
}

export type AiEventType = 'token' | 'tool_start' | 'tool_end' | 'done' | 'error';

export interface AiStreamEvent {
  type: AiEventType;
  data: string;
}
```

**2. Создать frontend/src/api/ai.ts:**

```typescript
/**
 * AI Assistant API client (AI-03, AI-06).
 *
 * streamChat() использует fetch + ReadableStream (не EventSource) —
 * для поддержки custom headers X-Telegram-Init-Data (AI-09).
 */
import { apiFetch, getInitDataRaw } from './client';
import type { AiStreamEvent, ChatHistoryResponse } from './types';

/**
 * Получить историю AI разговора (AI-06).
 */
export async function getChatHistory(): Promise<ChatHistoryResponse> {
  return apiFetch<ChatHistoryResponse>('/ai/history');
}

/**
 * Очистить историю AI разговора (AI-06).
 */
export async function clearConversation(): Promise<void> {
  return apiFetch<void>('/ai/conversation', { method: 'DELETE' });
}

/**
 * Отправить сообщение и стримить ответ через fetch + ReadableStream (AI-03).
 *
 * Использует X-Telegram-Init-Data заголовок (не EventSource — не поддерживает headers).
 * Парсит SSE: "data: {...}\n\n" → AiStreamEvent объекты.
 */
export function streamChat(
  message: string,
  onEvent: (event: AiStreamEvent) => void,
  onDone: () => void,
  signal?: AbortSignal,
): void {
  const initDataRaw = getInitDataRaw();
  const headers: Record<string, string> = { 'Content-Type': 'application/json' };
  if (initDataRaw) {
    headers['X-Telegram-Init-Data'] = initDataRaw;
  } else if (import.meta.env.DEV) {
    headers['X-Telegram-Init-Data'] = 'dev-mode-stub';
  }

  fetch('/api/v1/ai/chat', {
    method: 'POST',
    headers,
    body: JSON.stringify({ message }),
    signal,
  })
    .then(async (res) => {
      if (!res.ok || !res.body) {
        onEvent({ type: 'error', data: `HTTP ${res.status}` });
        onDone();
        return;
      }

      const reader = res.body.getReader();
      const decoder = new TextDecoder();
      let buffer = '';

      try {
        while (true) {
          const { done, value } = await reader.read();
          if (done) break;

          buffer += decoder.decode(value, { stream: true });
          const parts = buffer.split('\n\n');
          buffer = parts.pop() ?? '';

          for (const part of parts) {
            const line = part.trim();
            if (line.startsWith('data: ')) {
              try {
                const payload = JSON.parse(line.slice(6)) as AiStreamEvent;
                onEvent(payload);
              } catch {
                // Игнорируем некорректный JSON chunk
              }
            }
          }
        }
      } finally {
        reader.releaseLock();
      }

      onDone();
    })
    .catch((err: unknown) => {
      if (err instanceof Error && err.name === 'AbortError') return;
      onEvent({ type: 'error', data: err instanceof Error ? err.message : String(err) });
      onDone();
    });
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -i "ai.ts\|types.ts" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AiStreamEvent" frontend/src/api/types.ts` возвращает строку с interface
    - `grep "ChatMessageRead" frontend/src/api/types.ts` возвращает строку с interface
    - `grep "AiRole" frontend/src/api/types.ts` возвращает строку с type
    - `grep "streamChat" frontend/src/api/ai.ts` возвращает строку с export function
    - `grep "ReadableStream\|getReader\|TextDecoder" frontend/src/api/ai.ts | wc -l` возвращает минимум 2
    - `grep "EventSource" frontend/src/api/ai.ts | wc -l` возвращает 0 (не используется)
    - `grep "X-Telegram-Init-Data" frontend/src/api/ai.ts` возвращает строку в headers
    - `grep "getChatHistory\|clearConversation" frontend/src/api/ai.ts | wc -l` возвращает 2 (export functions)
    - npx tsc --noEmit не выдаёт ошибок в ai.ts и types.ts
  </acceptance_criteria>
  <done>AI типы и API клиент готовы; streamChat использует fetch+ReadableStream</done>
</task>

<task type="auto">
  <name>Задача 2: useAiConversation hook + ChatMessage + ToolUseIndicator компоненты</name>
  <files>frontend/src/hooks/useAiConversation.ts, frontend/src/components/ChatMessage.tsx, frontend/src/components/ChatMessage.module.css, frontend/src/components/ToolUseIndicator.tsx, frontend/src/components/ToolUseIndicator.module.css</files>
  <read_first>
    - frontend/src/hooks/useAnalytics.ts (cancelled flag pattern, useEffect структура)
    - frontend/src/components/ForecastCard.tsx (паттерн CSS modules компонента)
    - frontend/src/components/ForecastCard.module.css (паттерн CSS переменных)
  </read_first>
  <action>
**Создать frontend/src/hooks/useAiConversation.ts:**

```typescript
/**
 * Hook для управления AI разговором (AI-03, AI-04, AI-06).
 *
 * Загружает историю при монтировании.
 * sendMessage() стримит ответ token-by-token через streamChat().
 * clearHistory() удаляет все сообщения.
 */
import { useCallback, useEffect, useRef, useState } from 'react';
import { clearConversation, getChatHistory, streamChat } from '../api/ai';
import type { AiStreamEvent, ChatMessageRead } from '../api/types';

export interface UseAiConversationResult {
  messages: ChatMessageRead[];
  streaming: boolean;
  toolName: string | null;  // имя активного tool (для ToolUseIndicator)
  streamingText: string;    // частичный текст ответа AI во время стриминга
  error: string | null;
  sendMessage: (text: string) => void;
  clearHistory: () => Promise<void>;
}

// Временный ID для streaming сообщения (не сохраняется в БД до done)
const STREAMING_MSG_ID = -1;

export function useAiConversation(): UseAiConversationResult {
  const [messages, setMessages] = useState<ChatMessageRead[]>([]);
  const [streaming, setStreaming] = useState(false);
  const [toolName, setToolName] = useState<string | null>(null);
  const [streamingText, setStreamingText] = useState('');
  const [error, setError] = useState<string | null>(null);
  const abortRef = useRef<AbortController | null>(null);

  // Загрузить историю при монтировании (cancelled flag pattern)
  useEffect(() => {
    let cancelled = false;
    getChatHistory()
      .then((data) => {
        if (!cancelled) setMessages(data.messages);
      })
      .catch((e: unknown) => {
        if (!cancelled) setError(e instanceof Error ? e.message : String(e));
      });
    return () => {
      cancelled = true;
    };
  }, []);

  const sendMessage = useCallback((text: string) => {
    if (streaming) return;

    // Добавить user message локально (оптимистичное обновление)
    const userMsg: ChatMessageRead = {
      id: Date.now(),
      role: 'user',
      content: text,
      tool_name: null,
      created_at: new Date().toISOString(),
    };
    setMessages((prev) => [...prev, userMsg]);
    setStreaming(true);
    setStreamingText('');
    setToolName(null);
    setError(null);

    const abort = new AbortController();
    abortRef.current = abort;

    let accumulatedText = '';

    const handleEvent = (event: AiStreamEvent) => {
      if (event.type === 'token') {
        accumulatedText += event.data;
        setStreamingText(accumulatedText);
      } else if (event.type === 'tool_start') {
        setToolName(event.data);
      } else if (event.type === 'tool_end') {
        setToolName(null);
      } else if (event.type === 'error') {
        setError(event.data);
      }
    };

    const handleDone = () => {
      setStreaming(false);
      setToolName(null);
      if (accumulatedText) {
        const assistantMsg: ChatMessageRead = {
          id: Date.now() + 1,
          role: 'assistant',
          content: accumulatedText,
          tool_name: null,
          created_at: new Date().toISOString(),
        };
        setMessages((prev) => [...prev, assistantMsg]);
      }
      setStreamingText('');
      // Обновить историю из БД (получить реальные ID)
      getChatHistory()
        .then((data) => setMessages(data.messages))
        .catch(() => {/* оставить локальные данные */});
    };

    streamChat(text, handleEvent, handleDone, abort.signal);
  }, [streaming]);

  const clearHistory = useCallback(async () => {
    await clearConversation();
    setMessages([]);
    setStreamingText('');
    setError(null);
  }, []);

  return { messages, streaming, toolName, streamingText, error, sendMessage, clearHistory };
}
```

**Создать frontend/src/components/ChatMessage.tsx:**

```typescript
/**
 * Bubble компонент для сообщений AI чата (AI-03).
 *
 * Поддерживает роли: user (правый), assistant (левый).
 * Inline markdown: **bold**, - списки, 1. нумерованные списки.
 * Используется для финальных сообщений и streaming-preview.
 */
import type { ChatMessageRead } from '../api/types';
import styles from './ChatMessage.module.css';

interface Props {
  message: ChatMessageRead;
  isStreaming?: boolean;
}

/** Простой inline markdown парсер (bold, ul, ol) без библиотек. */
function parseMarkdown(text: string): string {
  return text
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
    .replace(/^- (.+)$/gm, '<li>$1</li>')
    .replace(/^(\d+)\. (.+)$/gm, '<li>$2</li>');
}

export function ChatMessage({ message, isStreaming = false }: Props) {
  const isUser = message.role === 'user';
  const content = message.content ?? '';

  return (
    <div className={`${styles.bubble} ${isUser ? styles.user : styles.assistant}`}>
      {isUser ? (
        <p className={styles.text}>{content}</p>
      ) : (
        <p
          className={styles.text}
          dangerouslySetInnerHTML={{ __html: parseMarkdown(content) }}
        />
      )}
      {isStreaming && <span className={styles.cursor}>▋</span>}
    </div>
  );
}
```

**Создать frontend/src/components/ChatMessage.module.css:**

```css
.bubble {
  max-width: 85%;
  padding: 10px 14px;
  border-radius: 16px;
  margin-bottom: 8px;
  word-break: break-word;
  line-height: 1.5;
}

.user {
  align-self: flex-end;
  background: var(--tg-theme-button-color, #2481cc);
  color: var(--tg-theme-button-text-color, #ffffff);
  border-bottom-right-radius: 4px;
}

.assistant {
  align-self: flex-start;
  background: var(--tg-theme-secondary-bg-color, #f0f0f0);
  color: var(--tg-theme-text-color, #000000);
  border-bottom-left-radius: 4px;
}

.text {
  margin: 0;
  font-size: 15px;
}

.text strong {
  font-weight: 600;
}

.text li {
  margin-left: 16px;
  list-style: disc;
}

.cursor {
  display: inline-block;
  animation: blink 1s step-start infinite;
  margin-left: 2px;
  opacity: 0.7;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}
```

**Создать frontend/src/components/ToolUseIndicator.tsx:**

```typescript
/**
 * Pulse-pill индикатор вызова AI tool (AI-04).
 *
 * Показывается между user message и AI response пока tool выполняется.
 * Исчезает при первом токене ответа.
 */
import styles from './ToolUseIndicator.module.css';

interface Props {
  toolName?: string | null;
}

/** Человекочитаемые названия tools. */
const TOOL_LABELS: Record<string, string> = {
  get_period_balance: 'Смотрю баланс...',
  get_category_summary: 'Анализирую категории...',
  query_transactions: 'Ищу транзакции...',
  get_forecast: 'Считаю прогноз...',
};

export function ToolUseIndicator({ toolName }: Props) {
  const label = toolName
    ? (TOOL_LABELS[toolName] ?? 'Смотрю данные...')
    : 'Смотрю данные...';

  return (
    <div className={styles.pill}>
      <span className={styles.dot} />
      <span className={styles.dot} />
      <span className={styles.dot} />
      <span className={styles.label}>{label}</span>
    </div>
  );
}
```

**Создать frontend/src/components/ToolUseIndicator.module.css:**

```css
.pill {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  background: var(--tg-theme-secondary-bg-color, #f0f0f0);
  border-radius: 16px;
  padding: 8px 14px;
  align-self: flex-start;
  margin-bottom: 8px;
}

.dot {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: #a78bfa;
  animation: pulse 1.2s ease-in-out infinite;
}

.dot:nth-child(2) {
  animation-delay: 0.2s;
}

.dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes pulse {
  0%, 100% { opacity: 0.3; transform: scale(0.8); }
  50% { opacity: 1; transform: scale(1); }
}

.label {
  font-size: 13px;
  color: var(--tg-theme-hint-color, #999999);
  margin-left: 4px;
}
```
  </action>
  <verify>
    <automated>cd /Users/exy/pet_projects/tg-budget-planner && npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -E "useAiConversation|ChatMessage|ToolUseIndicator" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "useAiConversation" frontend/src/hooks/useAiConversation.ts | grep -c "export function"` возвращает 1
    - `grep "toolName\|streamingText" frontend/src/hooks/useAiConversation.ts | wc -l` возвращает минимум 2 (состояния)
    - `grep "cancelled" frontend/src/hooks/useAiConversation.ts` возвращает строку с cancelled flag
    - `grep "ChatMessage" frontend/src/components/ChatMessage.tsx | grep -c "export function"` возвращает 1
    - `grep "parseMarkdown" frontend/src/components/ChatMessage.tsx` возвращает строку
    - `grep "dangerouslySetInnerHTML" frontend/src/components/ChatMessage.tsx` возвращает строку (только для assistant)
    - `grep "ToolUseIndicator" frontend/src/components/ToolUseIndicator.tsx | grep -c "export function"` возвращает 1
    - `grep "pulse\|animation" frontend/src/components/ToolUseIndicator.module.css | wc -l` возвращает минимум 2
    - `grep "#a78bfa" frontend/src/components/ToolUseIndicator.module.css` возвращает строку (AI фиолетовый цвет)
    - npx tsc --noEmit не выдаёт ошибок для новых файлов
  </acceptance_criteria>
  <done>Hook, ChatMessage и ToolUseIndicator готовы; TypeScript компилируется без ошибок</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → POST /ai/chat | initData в заголовке — берётся через getInitDataRaw() |
| AI response → DOM | dangerouslySetInnerHTML для assistant сообщений с parseMarkdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-16 | Tampering | dangerouslySetInnerHTML | mitigate | parseMarkdown только для assistant (LLM) сообщений; user input рендерится как plain text через {content} |
| T-09-17 | Spoofing | getInitDataRaw | accept | Логика getInitDataRaw — ответственность client.ts (Phase 1); здесь только передача |
| T-09-18 | Information | streamChat abort | accept | AbortController при unmount предотвращает setState на unmounted component |
</threat_model>

<verification>
```bash
cd /Users/exy/pet_projects/tg-budget-planner
npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | tail -5
```
Ожидаемый результат: 0 ошибок TypeScript.
</verification>

<success_criteria>
- 7 файлов созданы (types.ts добавлен, 6 новых)
- TypeScript компиляция без ошибок
- streamChat использует fetch+ReadableStream, не EventSource
- useAiConversation возвращает toolName для ToolUseIndicator
- ChatMessage: user = plain text, assistant = inline markdown
- ToolUseIndicator: pulse-анимация с #a78bfa точками
</success_criteria>

<output>
После завершения создать `.planning/phases/09-ai-assistant/09-06-SUMMARY.md`
</output>
