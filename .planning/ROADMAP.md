### Phase 70: Convergence & Abstractions — R3/R6/R7 (v1.1.2 followup workstreams C/D/E) — planned
**Plans:** 5 plans
**Goal**: Поверх стабильного codegen-контракта — свести legacy/V10 API (R3), извлечь общий доменный слой iOS чтобы два шелла не дрейфовали (R6), ввести инъектируемые cross-cutting абстракции (R7). Спецификация — `.planning/CONVERGENCE-AND-DEBT-PLAN.md` §ФАЗА 70 (workstreams C/D/E). **Решение владельца R6: ОСТАВИТЬ ОБА ШЕЛЛА навсегда** (iOS MainShell↔V10MainShell + web v06-shell НЕ удалять) — извлечь общий слой, схождение на уровне API/DTO, НЕ шеллов. Покрывает C/R3 (аудит legacy-enum vs V10API per route, V10=canonical; пометить legacy `@available(deprecated)`; мигрировать доказуемо-эквивалентные call-sites; debt-реестр), D/R6 (инвентарь дублированных экранов; извлечь общие ViewModels/Data/бизнес-логику в shared-слой, Views остаются per-shell; начать с домена наибольшего риска дрейфа — Subscriptions/Savings — по одному за раз с тестами), E/R7 (E1 error-policy injection: APIClient switch → инъектируемая ErrorHandling-стратегия, корневой фикс класса `suppressForbiddenHandler`; E2 BusinessDate тип отдельно от audit-времён, MSK как свойство типа, убирает MSK-decode band-aid).
**Depends on**: Phase 69 (codegen — фундамент).
**Success Criteria**:
1. Legacy-enums помечены `@available(*, deprecated)`; доказуемо-эквивалентные call-sites мигрированы на V10 canonical; неэквивалентные оставлены + тикет; debt-реестр в `.planning/`. Оба шелла продолжают работать. Build без новых warnings-as-errors.
2. ≥1 iOS-домен (Subscriptions ИЛИ Savings) использует общий VM/Data слой, потребляемый обоими шеллами; поведение идентично; паттерн задан для остальных доменов.
3. APIClient не содержит per-call auth-флагов (error-policy инъектируема); date-decode без эвристики формата (BusinessDate введён).
4. Полные test-suites всех затронутых стеков зелёные.

- [x] 70-01-PLAN.md — [W1] C/R3 API convergence: per-route audit (legacy enum vs V10), `@available(deprecated)` on MeAPI/CategoriesAPI/SubscriptionsAPI/CategoriesWriteAPI + ActualAPI.create/update (ActualAPI.delete kept as canonical-shared); non-equivalent pairs ticketed in `.planning/LEGACY-V10-DEBT-REGISTRY.md` (5 DEBT-70-* tickets + comment-debt index); zero call-site migration (all non-equivalent); both shells build, full iOS suite 609 green
- [ ] 70-02-PLAN.md — [W1] E2/R7 BusinessDate: distinct MSK-pinned type for wire DATE fields (due/nextChargeDate/txDate/plannedDate/periodStart/periodEnd) separate from audit Date; APIClient decoder de-heuristified (yyyy-MM-dd branch removed, timestamp branches kept); APIClientDateDecodeTests MSK-midnight contract preserved; both shells build, suite green
- [ ] 70-03-PLAN.md — [W2] E1/R7 ErrorHandling injection: extract APIClient status→error+logout switch into injectable ErrorHandling strategy (ErrorDecision enum); default policy byte-equivalent to current switch; **REGRESSION GATE** — 67-03/67-05 auth semantics preserved exactly (401 always logout / 403 !skipAuth logout / 403 skipAuth no-logout / 402 require_pro no-logout), APIClientForbiddenTests pass UNMODIFIED + new 402-no-logout assertion; no per-call auth flags; both shells build, suite green
- [ ] 70-04-PLAN.md — [W3] D/R6 shared compute (Subscriptions, chosen for highest drift): consolidate duplicate SubscriptionsData(V10)+SubscriptionsViewData(v06) into one shared SubscriptionsDomain namespace; intentional per-shell differences (formula/sort/copy) kept as named *V10/*V06 variants; duplicate enums + old test files removed; merged SubscriptionsDomainTests cover both shells; both shells build, UI identical, suite green
- [ ] 70-05-PLAN.md — [W4] D/R6 shared store (Subscriptions): extract load+mutation logic + injectable API seam into shared SubscriptionsStore consumed by both VMs; per-shell VMs become thin adapters (v06 fixed-RU banner / V10 toast+menu presentation stay per-VM); submitting-guard + reload-on-success + reloadPending re-entrancy ported verbatim; SubscriptionsStoreTests (seam-injected) + unmodified SubscriptionsViewModelTests pass; R6 pattern proven (Savings = next backlog domain); both shells build, behavior identical, suite green

---

## Dependency Graph (v1.1 / v1.2 / v2.0)

```
