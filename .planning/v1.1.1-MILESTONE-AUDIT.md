---
milestone: v1.1.1
milestone_name: Liquid Glass Theme
status: passed
started_at: "2026-05-11"
completed_at: "2026-05-11"
phases: [50, 51, 52, 53, 54, 55]
plans: 9
gaps: 0
---

# v1.1.1 — Liquid Glass Theme: Milestone Audit

**Status:** ✅ Shipped 2026-05-11

## Summary

6 phases complete: theme registry foundation, Liquid Glass design system, web port,
iOS native, switcher UI, polish + acceptance. Maximal Poster (default) + Liquid Glass
+ iOS Default доступны через единый toggle в Settings — synced web/iOS через
`localStorage['ui.theme']` / `@AppStorage("ui.theme")`.

## Per-phase recap

### Phase 50 — Theme Registry Foundation (2 plans)
- Multi-theme `tokens.json` + codegen extension (Theme Swift enum + per-theme CSS).
- `useTheme()` React hook + iOS `@AppStorage` env binding.
- 6 vitest pass; iOS build clean.

### Phase 51 — Liquid Glass Design System (2 plans)
- LG tokens (palette, materials, SF Pro typography, motion, radius).
- `GlassCard` primitive (web + iOS native `.background(Material)`).
- 6 vitest pass.

### Phase 52 — Web Liquid Glass Port (1 plan)
- Broad-stroke override stylesheets `liquid-glass.css` + `ios-default.css`.
- 9 V10 screens работают под обеими палитрами без переписи .module.css.
- Vite build clean + vitest 719/719 pass.

### Phase 53 — iOS Liquid Glass Native (1 plan)
- `ThemedBackground` SwiftUI helper applied на 15 V10 screens.
- iOS build clean + XCTest 358/358 pass.

### Phase 54 — Theme Switcher UI (2 plans)
- Web `ThemePickerSheet` + SettingsView row «Тема».
- iOS `ThemePickerSheet` + SettingsV10View row.
- 4 vitest pass; iOS build clean.

### Phase 55 — Polish + Acceptance (1 plan)
- `docs/THEMES.md` — architecture + tokens comparison table.
- prefers-reduced-motion CSS — neutralizes blur transitions под LG theme.

## Key metrics

- **Plans:** 9 (50: 2 + 51: 2 + 52: 1 + 53: 1 + 54: 2 + 55: 1)
- **Tests delta:** ~16 new vitest (useTheme + GlassCard + ThemePickerSheet); 358/358 XCTest preserved.
- **Build status:** Web vite + tsc clean; iOS make build clean.
- **Zero regressions** vs v1.1 baseline (default theme = maximal_poster unchanged).

## Manual follow-ups (NOT executed)

1. **27×2 side-by-side screenshots** (LG-POL-01, LG-WEB-04, LG-IOS-03): web Playwright + iOS XcodeBuildMCP под обеими темами — user-side QA.
2. **VoiceOver / WCAG AA audit** (LG-POL-03) — manual a11y review.
3. **Performance measurement <100ms theme switch** (LG-POL-04) — real-user analytics embedded в Phase 38 events; awaits actual users.
4. **Production rollout decision** — when user готов flip default theme, change `useTheme.ts` `DEFAULT` constant + `BudgetPlannerApp` `@AppStorage` initial value.

## Tech debt acknowledged

- Broad-stroke CSS override `[class*="plate"]` selectors — потенциально over-aggressive если future component names random; acceptable для v1.1.1 scope. v1.2 cleanup: explicit `data-surface="plate"` markers.
- `LiquidGlassTokens.Motion` Swift namespace exposes durations only — actual `Animation` curve mapping via comments. v1.2 polish: dedicated Animation helpers.
- iOS `ThemedBackground` использует `AnyView` под капотом switch — minor SwiftUI perf; acceptable для simple background swap.

## Next milestone

v1.2 — Acquisition & Retention (Phases 39-44) — planned, target ship 2026-11-11.
Pivots определяются Month-3 mini-gate (≥2 paying-30d / ≥30 регистраций к 2026-08-11).
