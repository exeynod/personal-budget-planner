// Phase 27-02 Task 1 RED stub: helpers will be filled in GREEN commit.
// This file exists only so vitest can resolve the import; every export
// throws so the assertions explicitly fail (FAIL-first TDD gate).

export const MONTHS_RU_GEN: readonly string[] = [];

export function todayRu(_d: Date): string {
  throw new Error('todayRu not implemented yet (RED)');
}

export const DEFAULT_SUGGESTION_CHIPS: readonly string[] = [];
